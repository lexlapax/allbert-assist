[package]
name = "allbert-proto"
version = "0.13.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
