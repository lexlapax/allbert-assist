# {{display_name}} Theme And Layout Notes

The generated scaffold does not enable snippets or layout overrides.

- Token/theme snippets are documentation only until reviewed and explicitly
  enabled through Settings Central.
- Layout overrides cannot create routes, actions, or authority.
- `app:allbert` is invalid; app destinations use `app:{{app_id}}`.
- Settings and Output remain non-hideable workspace destinations.
