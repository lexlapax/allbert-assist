defmodule {{plugin_module}} do
  @moduledoc {{description_literal}}

  use AllbertAssist.Plugin

  @impl true
  def plugin_id, do: "{{plugin_id}}"

  @impl true
  def display_name, do: {{display_name_literal}}

  @impl true
  def version, do: {{version_literal}}

  @impl true
  def validate(_opts), do: :ok

  @impl true
  def apps, do: [{{app_module}}]

  @impl true
  def actions, do: []

  @impl true
  def skill_paths do
    [Path.expand("../../skills", __DIR__)]
  end
end
