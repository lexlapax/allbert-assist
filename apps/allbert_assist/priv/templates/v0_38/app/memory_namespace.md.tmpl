# {{display_name}} Memory Namespace

The generated app declares an inert, non-writable namespace:

- app_id: `{{app_id}}`
- namespace: `{{app_id}}`
- writable: `false`

Future writes must go through reviewed Allbert memory actions, Security
Central, confirmations when required, and app-memory synchronization rules.
