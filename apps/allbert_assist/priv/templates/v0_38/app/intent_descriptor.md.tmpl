# {{display_name}} Intent Descriptor

This inert descriptor documents how a future reviewed app-intent descriptor can
describe {{display_name}} handoff candidates.

- app_id: `{{app_id}}`
- destination: `{{destination_id}}`
- default action: none
- auto-execution: disabled

Do not treat this file as authority. App routing and action execution still
flow through the registered app/action/runtime boundaries.
