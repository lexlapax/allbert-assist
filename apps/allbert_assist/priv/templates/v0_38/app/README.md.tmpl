# {{display_name}}

{{description}}

This scaffold is a reviewed, inert Allbert workspace app skeleton. It declares
the plugin/app shape, a panel surface, a disabled-by-default settings fragment,
an inert memory namespace declaration, a sample action stub, and docs for
objective/canvas/theme/layout hooks.

It does not change the compile path, register an app, expose the sample action,
enable a schedule, add routes, or grant permissions. After review and compile
path wiring, validate the app module with:

```sh
mix allbert.validate_app {{app_module}}
```

Generated from the v0.38 `app` template pattern.
