# {{display_name}}

Pattern: `{{pattern_id}}`

This scaffold is an inert scheduled/chron flow blueprint. It does not create a
job row, enable scheduler policy, register an action, or start an objective.

- Flow module: `{{flow_module}}`
- Schedule id: `{{schedule_id}}`
- Timezone: `{{timezone}}`

Review `priv/jobs/{{slug}}.json` and `lib/{{module_path}}/flow.ex` before
turning this blueprint into a real scheduled job through registered Allbert
actions.
