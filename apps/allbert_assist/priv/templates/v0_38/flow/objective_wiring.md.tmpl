# Objective Wiring

This scheduled-flow scaffold may later frame an objective:

{{objective}}

The scaffold does not call `AllbertAssist.Objectives`, create objective steps,
or bypass confirmation. Any future runtime wiring must go through registered
actions and the shared objective runtime.
