# {{display_name}}

{{description}}

This scaffold is inert developer source. It does not change the compile path,
register a plugin, enable skills, grant trust, add routes, or grant
permissions. Review the files, wire the plugin into the project deliberately,
and run the normal test gate before relying on it.

Generated from the v0.38 `plugin` template pattern.
