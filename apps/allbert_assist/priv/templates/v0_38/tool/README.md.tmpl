# {{display_name}}

Pattern: `{{pattern_id}}`

This scaffold is an inert reviewed dynamic action template. It does not compile,
register, or integrate anything by itself.

- Action: `{{action_name}}`
- Module: `{{action_module}}`
- Permission: `{{permission}}`
- Live integration eligibility: action shape only, through the v0.36 sandbox
  gate and v0.37 loader after operator confirmation.

Review `source/lib/action.ex` and `dynamic_manifest.json` before using this
artifact as a developer scaffold or templated dynamic draft.
