# {{display_name}}

Pattern: `{{pattern_id}}`

This scaffold is an inert objective-workflow blueprint. It does not create an
objective, write objective steps, start a private loop, or grant authority.

- Workflow id: `{{workflow_id}}`
- Module: `{{objective_module}}`

Review `priv/objectives/{{slug}}.json` and `lib/{{module_path}}/objective.ex`
before mapping this blueprint to registered objective actions.
