# Confirmation Boundary

Objective workflow templates are advisory blueprints only.

- `objective_id` and `step_id` are never authority.
- Advisory proposer output cannot approve or execute a step.
- Runtime execution must use `AllbertAssist.Objectives` and registered actions.
- Any effectful step must keep its existing Security Central confirmation path.
