defmodule StockSage do
  @moduledoc """
  StockSage is Allbert's first plugin-contributed workspace app.

  v0.20 establishes the plugin/app shell and local domain boundary. Analysis
  execution, Python bridge work, LiveViews, and market-data calls belong to
  later milestones.
  """
end
