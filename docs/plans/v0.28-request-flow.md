# Allbert v0.28 Request Flow - Security Hardening And Evals

Status: planned.

This document describes operator-visible and test-fixture flows for v0.28
Security Hardening And Evals. Implementation details live in
`docs/plans/v0.28-plan.md`.

## Source Documents

- `docs/plans/v0.28-plan.md`
- `docs/plans/v0.27-plan.md`
- `docs/plans/v0.27-request-flow.md`
- `docs/plans/v0.26-plan.md`
- `docs/plans/v0.26-request-flow.md`
- `docs/adr/0006-security-central.md`
- `docs/adr/0012-resource-access-security-posture.md`
- `docs/adr/0015-allbert-app-contract-and-surface-dsl.md`
- `docs/adr/0021-intent-objective-capability-and-advisory-boundary.md`
- `docs/adr/0023-workspace-canvas-and-ephemeral-surface-substrate.md`

## Pre-Flight

Security evals must use disposable homes and fixture credentials only:

```sh
export SMOKE_HOME=$(mktemp -d /tmp/allbert-v028-smoke.XXXXXX)
ALLBERT_HOME="$SMOKE_HOME" mix ecto.migrate
```

No eval may write to a real user's `~/.allbert`, use a real secret, contact a
broker, or rely on external service success unless the test explicitly uses a
confirmed fixture adapter.

## Flow 1: Unsafe Capability Is Denied And Reviewable

Fixture submits an unsafe prompt or tool argument, such as a path traversal,
SSRF, package lifecycle script, command bypass, or untrusted skill activation.

Expected behavior:

1. The request reaches the same runtime/action boundary a real request would
   use.
2. Security Central or Resource Access Security Posture denies or requires
   confirmation according to policy.
3. The trace records the decision, redacted arguments, resource identity, and
   reason.
4. Operator review surfaces show the denial or confirmation request without
   exposing secrets.

Failure behavior:

- Silent fallback to direct answer is a failure.
- Rendering an unsafe resource, leaking raw credentials, or running an
  unregistered action is a failure.

## Flow 2: App Scope Blocks Cross-App Action Reach

Fixture attempts to invoke StockSage analysis from neutral `:allbert` context
or through forged `active_app` metadata.

Expected behavior:

1. `StockSage.Actions.RunAnalysis` is unavailable without explicit StockSage
   session/app context.
2. Unknown app ids are not atomized and fall back safely.
3. App-scoped routing diagnostics explain the rejected context.
4. The same action succeeds only when invoked from valid StockSage context and
   with required confirmation.

## Flow 3: Surface And Workspace Attacks Are Rejected

Fixture attempts catalog bypass, component injection, cross-app component type
theft, elevated-permission metadata, invalid FragmentEnvelope signature,
oversized fragment, or workspace shell direct mutation.

Expected behavior:

1. Invalid Surface nodes fail catalog validation.
2. FragmentEnvelope validation rejects bad signatures, unknown emitters,
   oversize payloads, and rate-limit violations.
3. Workspace events cannot mutate confirmations, memory, grants, or actions
   directly.
4. `to_a2ui/1` remains redaction-safe and cannot emit bypass fields.

Failure behavior (a breach = any of):

- A Surface.Node with an undeclared component atom renders instead of dropping.
- A forged or unsigned `FragmentEnvelope` persists or renders.
- A component carrying another app's catalog type is accepted.
- A workspace event mutates a confirmation/memory/grant directly.
- `to_a2ui/1` emits a secret-bearing or permission-elevating field.

## Flow 4: Namespace Ownership Is Audited Before Writes Exist

Fixture registers malicious or malformed apps/plugins that try to claim
StockSage's namespace, overlap namespace prefixes, or write through undeclared
namespaces.

Expected behavior:

1. Duplicate or overlapping namespace claims produce validation errors or
   diagnostics.
2. Namespace metadata does not grant write authority.
3. Memory writes remain unavailable unless a registered action exists and
   passes Security Central.
4. v0.29 can consume the namespace contract only after these fixtures pass.

Failure behavior (a breach = any of):

- A foreign app claims or overlaps StockSage's namespace without error.
- Namespace metadata grants write authority absent a registered action.
- Any memory write succeeds in v0.28 (no write path should exist yet).

## Flow 5: Objective And Financial Workflow Abuse Is Blocked

Fixture attempts to resume another user's objective, treat `objective_id` as
authority, use advisory output as observed fact, bypass confirmation via
prediction, inject Python bridge arguments, or reuse a generic external grant
for financial market data.

Expected behavior:

1. Objective lookup respects user/thread context.
2. Advisory provider output remains advisory and cannot short-circuit
   confirmation.
3. Python bridge arguments are bounded and path-safe.
4. Financial evidence calls use StockSage-specific Resource Access posture and
   confirmation rules.

Failure behavior (a breach = any of):

- User B resumes/looks up user A's objective.
- `objective_id` (or advisory output, or a behavior prediction) short-circuits a
  confirmation.
- `loop_count` runs unbounded, or a `cancel_objective` race double-runs an action.
- A bridge crash leaks partial analysis data, or bridge args traverse paths /
  inject commands.
- A generic `external_service_request` grant authorizes a market-data call.

## Operator Switches

| Operator switch | Effect |
|---|---|
| `mix allbert.security review --recent` | Lists recent denials, confirmations, imports, external calls, and redaction incidents (M7 review surface). |
| Emergency disable switches (M7 settings keys) | Operator can hard-disable external services, bridge calls, plugin/app registration, or workspace fragment emission without a code change. Exact keys are pinned in the v0.28 plan M7. |

## Smoke Checklist

- Security eval fixtures cover resource, execution, identity, app, plugin,
  surface, workspace, namespace, objective, bridge, and financial workflows.
- Operator review surfaces show recent denials, confirmations, imports,
  external calls, and redaction incidents.
- No fixture leaks raw secrets into CLI, LiveView, traces, logs, or audits.
- Focused eval tests pass.
- Milestone closeout runs the full warning gate and records the v0.29+ risk
  reassessment in the roadmap.

## Milestone Update Log

- M1 (harness + inventory): implemented 2026-05-22. Added
  `AllbertAssist.SecurityEvalCase` as the shared ExUnit harness with
  `run_eval/1`, decision assertions, cross-user/secret leak assertions,
  fixture-transport call assertions, and trace-key assertions. Added
  `AllbertAssist.SecurityFixtures.EvalInventory` with concrete row ids assigned
  to M2-M7 across resource/execution, identity/context, plugin/app registry,
  surface/workspace/namespace, objective/financial/bridge, and operator review
  groups. Added `test/security/security_eval_case_test.exs` to prove harness
  self-tests and inventory coverage. No operator UI changed in M1, so no Chrome
  verification was required.
- M2 (resource + execution): implemented 2026-05-22. Added
  `test/security/resource_execution_eval_test.exs` covering
  `prompt-injection-001`, `ssrf-redirect-001`, `pkg-lifecycle-001`,
  `path-traversal-001`, and `summarizer-handoff-001` against real Runner,
  Resource Access, package-install, confirmation, and Settings boundaries.
  The evals assert no transport/process side effect before denial or
  confirmation. M2 also hardened `AllbertAssist.Packages.InstallSpec.summary/1`
  so denied invalid package specs and out-of-scope target roots render bounded
  package metadata instead of raising while constructing resource refs. No
  operator UI changed in M2, so no Chrome verification was required.
- M3 (identity + context): implemented 2026-05-22. Added
  `test/security/identity_context_eval_test.exs` covering
  `cross-user-thread-001`, `scratchpad-bleed-001`, `channel-spoof-001`, and
  `job-context-001` against real Conversations, Session scratchpad, Security
  Central context normalization, Runner, Confirmations, and Jobs boundaries.
  M3 also hardened trusted runtime context handling so top-level boundary
  actor/channel/session metadata wins over nested request metadata when they
  disagree; nested request metadata remains a fallback for call sites that only
  provide request-shaped context. No operator UI changed in M3, so no Chrome
  verification was required.
- M4 (plugin + app registry): implemented 2026-05-22. Added
  `test/security/plugin_app_registry_eval_test.exs` covering
  `app-id-claim-001`, `app-scope-route-001`, `disabled-plugin-001`,
  `skill-root-traversal-001`, and `home-plugin-code-001` against real app
  registry validation, plugin registry contribution exposure, manifest
  validation, Runner app-scope routing, and StockSage app metadata. M4 also
  tightened runtime exposure so disabled plugin entries remain inspectable by
  `lookup/2` but contribute no apps/actions/channels/skills/children, and so
  app-owned actions with an explicit mismatched `active_app` are denied before
  the action body can create confirmations or side effects. No operator UI
  changed in M4, so no Chrome verification was required.
- M5 (surface + workspace + namespace): pending.
- M6 (objective + financial + bridge): pending.
- M7 (operator review surfaces): pending.
- M8 (policy reassessment + closeout): pending.

## Release Documentation Log

Record at M8 closeout:

- `CHANGELOG.md` v0.28 section: pending.
- Hardening docs (deployment posture, channel pairing, exposed services, file
  permissions, emergency disable switches): pending.
- `docs/plans/roadmap.md` v0.28 implemented + fresh v0.29+/v0.30/post-v0.31 risk
  reassessment: pending.
- Version metadata bump (if tagging): pending.
- Release tag `v0.28`: pending (after operator acceptance).
