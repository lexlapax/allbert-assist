# Allbert v0.52 Channel Pack 1 (Discord And Slack) + Cross-Channel Conversation Threading

Request flow: `docs/plans/v0.52-request-flow.md`

ADRs:
- `docs/adr/0016-channel-adapter-boundary-and-identity-mapping.md` (v0.52
  amendment for approval primitives — binding for v0.52 and all later channel
  adapters including v0.53 mobile channels).
- `docs/adr/0056-channel-inbound-trust-tier.md` (the `:channel_message_inbound`
  permission class + per-interaction clicker authorization).
- `docs/adr/0057-cross-channel-conversation-threading.md` (NEW — the canonical
  thread model, the `thread_channel_refs` / `conversation_message_refs` /
  `cross_channel_identity_links` tables, the owner/account/key scope fields, the
  `threading:` descriptor field, the degradation ladder, echo-loop suppression,
  explicit cross-channel identity links, the unified history view, and the
  explicit resume action).
Consumed: `docs/adr/0012-conversation-history-substrate.md`,
`docs/adr/0014-local-workspace-identity.md`,
`docs/adr/0017-allbert-plugin-contract.md`,
`docs/adr/0027-allbert-action-dsl-and-capability-registry.md`,
`docs/adr/0031-settings-schema-fragments-and-authority.md`,
`docs/adr/0047-provider-doctor-contract.md`,
`docs/adr/0049-development-gates-and-test-parallelization.md`.

Status: implemented as `0.52.0`; M0-M8 are complete in code and docs.
The deterministic `release.v052` gate is the implementation handoff; the
real-provider `external-smoke -- discord_slack` lane and the live
inbound/callback checks below remain required before tagging the release. The
2026-06-11 fourth-pass implementation audit added a required M8R remediation
lane for Discord interaction acknowledgement, Discord Gateway RESUME, Slack
ack-before-runtime ordering, and reproducible live-inbound evidence before tag.

Source: `docs/archives/version-1.0-planning-03.md`, post-v0.37 planning pass.
A first readiness pass found shape gaps; the pass-2 readiness patch folded in
the v0.42 closeout lessons and reconciled the codebase drift called out below.
The pass-3 zoom-out patch **expanded v0.52** from "Discord + Slack channels" to
"Discord + Slack + a system-wide cross-channel conversation-thread construct
(ADR 0057), with the existing channels retrofitted onto it." This pass-4
hardening locks the tenant-safe owner/account/key shape and the exact runtime
handoff so no implementation agent can reinterpret provider thread ids as
canonical authority. The substrate is built first so every adapter is native to
it; the version stays a single v0.52 across the resulting nine milestones
(M0-M8).

This milestone broadened in the post-v0.37 planning pass to lock the
channel-approval primitive contract (ADR 0016 amendment) so v0.53 mobile
channels inherit it rather than re-invent rendering, and in the pass-3 zoom-out
to make one Allbert conversation coherent across channels with wildly different
threading models (Slack threads, Discord threads-as-channels, Telegram topics
vs. flat DMs, email reference headers, SMS/no-threading, web/CLI which Allbert
controls) — ADR 0057.

## Readiness Sweep

Status: implementation-ready after this fourth readiness patch. M0 owns the
descriptor schema change, the primitive-selection module surface, the trusted
plugin-owned channel-settings bridge, the `:channel_message_inbound` permission
registration (ADR 0056), the transport-vehicle spike, and the eleven locked
decisions in §"M0 Locked Decisions"; M1-M8 acceptance and lane annotations
consume those decisions directly.

Result:

- **Vision fit.** v0.52 expands channel reach (Discord + Slack) through the
  existing channel adapter boundary (ADR 0016 §Decision) and
  the v0.17 plugin contract. No new runtime, security policy, or implicit
  account system is introduced. External input translates into signals as
  the vision requires (`docs/plans/allbert-jido-vision.md` §"v0.52").
- **Roadmap fit.** v0.52 correctly follows the capability arc through v0.49
  vision and precedes v0.53 mobile channels. The amendment locks the rendering
  contract here so v0.53 inherits it without combining five channel adapters
  into one milestone.
- **Past-plan fit.** v0.16 channel substrate (`channel_events`,
  `Channels.Identity`, session_id derivation, identity mapping,
  `:one_for_one` adapter supervision), v0.17 plugin contract (shipped
  Telegram + email plugins as precedent), v0.27 action DSL, v0.31 Settings
  Central fragment substrate, v0.42 closeout lessons (release smoke,
  doc-sync discipline, runs-as-written, lane annotations, plugin-owned
  settings), and v0.43 pass-3 patterns (doctor live-verify, primitive
  forward pins) all consumed without re-litigation.
- **Future-plan fit.** v0.44 Plan/Build ships before these adapters using the
  existing surfaces; v0.52 adds Discord/Slack renderers that inherit Plan/Build
  summaries through the primitive contract. v0.47 self-improvement does not
  require team-channel traces, but after v0.52 those traces become an
  additional read-only pattern source. v0.53 mobile channels
  (WhatsApp/Signal/Matrix) consume the amendment without additional ADR work
  (`docs/plans/v0.53-plan.md:46-58`).
- **ADR fit.** ADR 0016 amendment (already authored — §"v0.52 Amendment: Channel Approval Primitives")
  plus ADR 0056 and ADR 0057 become plan-accepted implementation inputs at M0.
  Their formal Status lines remain Proposed until M8 release evidence exists,
  then flip to Accepted together. ADR 0014 identity mapping and ADR 0017 plugin
  contract are consumed unchanged.

Readiness guardrails:

- M0 must preserve the eleven decisions in §"M0 Locked Decisions"; any
  change requires an explicit re-decision in this plan and the
  request-flow doc before dependent code lands.
- M0 must add the `primitives:` field to the channel descriptor schema,
  ship `AllbertAssist.Approval.Handoff.render/2`, add the trusted
  plugin-owned `channels.<provider>.*` settings bridge, register the
  `:channel_message_inbound` permission class (ADR 0056) at every spot the
  precedent requires, run the transport-vehicle spike (M0 decision 6), and
  retroactively declare Telegram + email primitives before M2 Discord work
  starts.
- Each milestone must update this plan's request-flow handoff and record
  the focused gate evidence named in the development-lane table.
- The deterministic provider-stub lane (`release.v052`) is the **fast/CI gate**
  and needs no live network; it exercises the real adapter + request-shape code
  per the "prefer fixtures that exercise the real adapter" rule. It does **not**
  satisfy release authority on its own. A **required real-provider smoke**
  against a sandbox Discord bot + Slack workspace records real outbound parent
  + threaded/reply placement and echo-suppression evidence before the v0.52
  tag. Operator manual validation must additionally cover live inbound
  Gateway/Socket Mode delivery, button approval, and unmapped-clicker rejection
  before tagging. This preserves the v0.48 M8R / v0.49 real-provider discipline
  and the AGENTS.md rule that fixtures never satisfy release authority or
  manual validation.

## Purpose

Expand Allbert reach into team and community chat by adding Discord and Slack
channel plugins that reuse the existing channel substrate. Formalize the
channel approval-primitive contract through an ADR 0016 amendment so all
later channels honor one rendering rule.

The two plugins each ship the provider package shape that v0.17 established
for Telegram and email (`Plugins.<Provider>` + `Channels.<Provider>.
{Adapter,Client,Renderer,Parser}`), plus provider settings fragments and
doctor actions. The amendment graduates approval-rendering from each
renderer's hand-rolled choice (v0.16) to a shared primitive selection module
(`Approval.Handoff.render/2`) that consumes descriptor-declared primitive
sets.

## M0 Locked Decisions

The first milestone preserves these eleven decisions. They are no longer
open defaults; later milestones may revisit them only with an explicit
re-decision recorded in this table and in the request-flow doc.

| # | Decision | Chosen value | Trade-off if changed |
|---|---|---|---|
| 1 | `Approval.Handoff` module location | New `AllbertAssist.Approval.Handoff` module owning `render/2`. Separate from existing `AllbertAssist.Intent.ApprovalHandoff` (which keeps its text-line formatter role) and `AllbertAssist.Intent.Handoff` (v0.33 app-intent handoff). | Extending `Intent.ApprovalHandoff` would conflate primitive selection with text formatting; co-locating with v0.33 `Intent.Handoff` would conflate channel rendering with app-intent handoff. A fresh `Approval.*` namespace is the clean boundary. |
| 2 | Discord transport | **Gateway WebSocket** through a reviewed BEAM bridge. No public URL required; bots authenticate with the bot token and receive events over a persistent WebSocket. Gateway-delivered component interactions still require Discord's HTTP interaction callback endpoint for the initial acknowledgement. Mirrors the v0.16 "no public inbound HTTP" posture. | Interactions HTTP webhooks that receive inbound requests would require public HTTPS exposure + Ed25519 verification of every request and remain parked to v0.52.x. Gateway messages plus HTTP interaction callbacks are in v0.52 because Discord requires the callback endpoint for Gateway-delivered buttons. |
| 3 | Slack transport | **Socket Mode** through an App-Level Token. No public URL required; direct analog to Telegram long polling. | Events API HTTP webhook requires public HTTPS + HMAC SHA-256 signing verification. Parked to v0.52.x. |
| 4 | Settings ownership | **Plugin-owned `Settings.Fragment`** (consistency with v0.42 notes_files + v0.43 browser). Core `channels.telegram.*` / `channels.email.*` keys remain where they are (v0.16); retroactive migration to fragments is a v0.52.x follow-on, not v0.52-blocking. | Putting `channels.discord.*` / `channels.slack.*` in core `Settings.Schema` would match v0.16's pattern but grow core surface; v0.42/v0.43 established plugin-owned fragments as the direction for new channel plugins. |
| 5 | Multi-workspace Slack scope | **Single-workspace** bot token at v0.52 (one set of `channels.slack.*` keys; one bot install per Allbert home). Multi-workspace OAuth (workspace install records, per-workspace token storage, install flow) deferred to v0.52.x or v1.x. | Multi-workspace at v0.52 would add OAuth flow, install record schema, per-workspace token rotation, and team-id scoping to every CLI command — substantial scope that does not match the "Pack 1" framing. |
| 6 | Transport implementation vehicle | **Locked by the M0 spike: raw `Req` for REST + direct `websockex` clients for Discord Gateway and Slack Socket Mode.** No Nostrum and no `slack_elixir` in the v0.52 baseline. Discord's Gateway / REST docs expose the needed message, reply, and thread primitives without a higher-level client. Slack Socket Mode exposes envelope delivery + `envelope_id` acknowledgement over the socket, while Web API calls stay ordinary `Req` calls. `websockex` is already present in the lock through `req_llm` and is now a direct `:allbert_assist` dependency to make the reviewed WS boundary explicit. | Ecosystem libraries could reduce adapter code, but Nostrum brings a broader Discord stack with non-Req HTTP/cache assumptions; `slack_elixir` does not buy enough over direct Socket Mode handling for the v0.52 surface and would still need interactivity proof. Any later change must be recorded as a re-decision here and in the request-flow before M2/M3 consumes it. |
| 7 | Provider thread / Allbert conversation mapping | **Provider thread roots scope channel session continuity and reply routing; they never become Allbert conversation authority.** Slack derives channel session continuity from `team_id + channel_id + (thread_ts || message ts) + external_user_id`. Discord derives it from `guild_id_or_dm + channel_id_or_thread_channel_id + external_user_id`; ordinary Discord replies use `message_reference` for reply routing. Internal Allbert `thread_id` (canonical) remains the id selected by `Conversations.resolve_thread/1` during `Runtime.submit_user_input/1`, except when `Conversations.ChannelThread.lookup_thread/1` has already bound the provider key to an existing canonical thread and the adapter passes that known Allbert id back to runtime. **The durable provider-thread mapping table is now plan-accepted (ADR 0057)** — see decision 8 — superseding the prior "future ADR" deferral. | Treating provider threads only as ephemeral output formatting would leak context across Slack/Discord threads in the same channel and lose continuity across restarts. The canonical mapping (decision 8) is the system-wide construct that makes one conversation coherent across channels without treating provider ids as authority. |
| 8 | Canonical thread model (ADR 0057) | **The existing `conversation_threads.id` (`thr_<UUID>`) IS the canonical thread id** (no new id). Durable SQLite tables map it with future tenant-safe scope: `thread_channel_refs` (`owner_scope`, `canonical_thread_id`, `channel`, `receiver_account_ref`, `provider_thread_key`, redacted `provider_thread_ref`) with `UNIQUE(owner_scope, channel, receiver_account_ref, provider_thread_key)`; `conversation_message_refs` (`owner_scope`, `canonical_message_id`, `canonical_thread_id`, `channel`, `receiver_account_ref`, `provider_message_id`, `part_id`, `direction`) with `UNIQUE(owner_scope, channel, receiver_account_ref, provider_message_id, part_id)`; `cross_channel_identity_links` (`owner_scope`, `link_id`, `user_id`, `channel`, `receiver_account_ref`, `external_user_id`). `owner_scope` is `"local"` in v0.52 and exists only to avoid a post-1.0 schema redo; it grants no tenancy authority. `provider_thread_key` is the deterministic bounded unique key; `provider_thread_ref` is redacted structured metadata. `direction` drives echo-loop suppression; `part_id` allows one canonical message → N native messages. | A new canonical id would duplicate the existing thread authority. In-memory mapping (matterbridge's failure mode) loses threading on restart. Unique indexes over raw JSON refs are brittle and unsafe once multiple receiver accounts or tenants exist. Omitting `owner_scope` / `receiver_account_ref` / `provider_thread_key` / `direction` / `part_id` is expensive to retrofit. |
| 9 | Threading capability descriptor (ADR 0057) | Channel descriptors gain a **`threading:`** field (the ADR 0016 descriptor pattern, beside `primitives:`): `:native_threads` (Discord, Slack, Telegram topic chats) / `:reply_chain` (email, Telegram DM) / `:flat` (SMS, future) / `:rich` (web, CLI). Plus `can_create_thread`, `reply_key_type`, `quote_ttl_ms`. `Conversations.ChannelThread` reads it to pick the render/degradation strategy; the registry rejects a missing/invalid declaration. | Hard-coding per-provider threading in each renderer (the v0.16 hand-rolled approach the `primitives:` field already replaced) does not scale to v0.53 mobile channels. |
| 10 | Cross-channel model | **Continuity within each channel + a unified read-only history view + an explicit operator `resume_thread_on_channel` action.** No silent live cross-channel mirroring; E2EE-origin content (future Signal/WhatsApp) is never relayed to another surface without explicit operator action. Cross-channel ordering uses Allbert's own ingest sequence, never provider clocks. | Live auto-mirroring (matterbridge-style fan-out) re-exposes content that arrived on a channel with different trust/encryption and multiplies echo/ordering hazards. Strict isolation would drop the unified-view value the construct exists to deliver. |
| 11 | Cross-channel identity links | **Explicit operator-configured links** declare that several per-channel identities are the same person, on top of the existing per-channel `identity_map`. Never auto-derived from display names/subjects/weak signals. The per-channel `identity_map` remains the authentication gate (ADR 0016/0056); links only group already-mapped identities for the unified view + resume. | Auto-merging on weak signals is a security/privacy footgun (spoofing, "Re: Invoice" collisions). Relying only on the implicit shared `user_id` would leave the link un-surfaceable to the operator. |

M0 decision 6 is now complete. The selected vehicle is raw `Req` REST plus a
reviewed direct `websockex` WS boundary; M2/M3 adapter code must consume that
decision unless a later patch records an explicit re-decision in this table and
the request-flow doc first.

## Research Grounding

The v0.52 design is grounded in:

- **Discord Gateway** (`https://discord.com/developers/docs/topics/gateway`):
  persistent WebSocket connection authenticated with the bot token;
  `READY`, `MESSAGE_CREATE`, `INTERACTION_CREATE` events; heartbeat
  protocol; resume on disconnect; intents (bitmask controlling which
  events the bot receives). Discord REST calls use
  `Authorization: Bot <token>`. The adapter obtains the WSS URL,
  recommended shard count, and session-start-limit state from
  `GET /gateway/bot` at connect time; the response is not cached
  long-term. Bots running on a single shard handle up to ~2,500 guilds;
  v0.52 single-shard. Ed25519 signature verification is
  Interactions-only (not Gateway) and is parked alongside the
  Interactions transport.
- **Slack Socket Mode** (`https://api.slack.com/apis/socket-mode`):
  WebSocket initiated by the bot using an App-Level Token (`xapp-...`);
  `apps.connections.open` is called with the app-level token in the HTTP
  `Authorization` header and returns a temporary WSS URL, so each reconnect
  obtains a fresh URL. Socket Mode carries the same event payloads as
  Events API, wrapped in envelopes that must be acknowledged by
  `envelope_id`. `app_mention`, `message.channels`, and `interactive`
  (Block Kit button taps) envelopes are in scope. Single-workspace bot
  token (`xoxb-...`) for read/write. Multi-workspace OAuth model deferred
  per M0 decision 5.
- **Slack thread model** (`https://docs.slack.dev/reference/methods/chat.postMessage`,
  `https://docs.slack.dev/reference/methods/conversations.replies`): replies
  are addressed by `channel` + `thread_ts`; a top-level message may start the
  thread root and later replies reuse its timestamp. The v0.52
  `provider_thread_key` for Slack is therefore scoped by
  `owner_scope + receiver_account_ref + team_id + channel_id + thread_ts_or_ts`,
  not by timestamp alone.
- **Discord thread model** (`https://discord.com/developers/docs/resources/channel`,
  `https://discord.com/developers/docs/topics/gateway-events`): a thread is a
  channel object; creating a thread from a message yields a thread channel tied
  to the source message, and Gateway events deliver both message and thread
  lifecycle events. The v0.52 key for Discord is scoped by
  `owner_scope + receiver_account_ref + guild_or_dm_id + channel_or_thread_id`,
  while ordinary `message_reference` remains reply placement, not canonical
  thread authority.
- **Telegram topic/reply model** (`https://core.telegram.org/bots/api`): topic
  chats use `message_thread_id`; flat chats and DMs use reply parameters. The
  v0.52 retrofit treats Telegram topic chats as `:native_threads` when the
  thread id is present and DMs/flat chats as `:reply_chain`.
- **SMS/conversation APIs** (e.g.
  `https://www.twilio.com/docs/conversations/api/conversation-resource`): SMS
  itself has no native thread pointer, while higher-level provider
  conversation APIs introduce their own conversation ids. v0.52 does not ship
  SMS, but ADR 0057's `:flat` capability and `receiver_account_ref`/`owner_scope`
  keys are intentionally shaped so a later SMS adapter can key by local number +
  peer or provider conversation sid without redefining canonical threads.
- **Discord Component System** (Block Kit analog): buttons (action rows),
  select menus, modals. v0.52 renders the `:button` primitive through
  Discord components; select menus and modals deferred.
- **Slack Block Kit** (`https://api.slack.com/block-kit`): blocks +
  actions; button taps return `block_actions` interaction events that
  the bot replies to or updates the original message. v0.52 renders the
  `:button` primitive through Block Kit buttons; rich Block Kit composition
  deferred.
- **The v0.16 channel adapter boundary** (ADR 0016 §Decision lines
  76-103): channels are delivery adapters around
  `AllbertAssist.Runtime.submit_user_input/1` and registered Jido
  actions. They do not own intent selection, action execution, Security
  Central policy, Resource Access grants, confirmation storage,
  conversation history, markdown memory, or app registry semantics.
- **The v0.17 shipped-plugin pattern**: Telegram and email plugins live
  under `./plugins/allbert.<provider>/`; each ships a 5-module quartet
  (`Plugins.<Provider>` + `Channels.<Provider>.
  {Adapter,Client,Renderer,Parser}`) and a plugin manifest
  (`allbert_plugin.json`). v0.52 mirrors the shape for Discord and
  Slack.
- **The v0.42 closeout lessons** (`v0.42-impl-audit-{01,02,final,
  completion,release}.md` archived): release smoke gate (`release.vXXX`),
  enumerated doc-sync discipline, runs-as-written operator validation,
  doctor live-verify, plugin-owned settings, lane annotations per
  ADR 0049.

Implementation consequence: v0.52 adds two plugins, one new module
(`Approval.Handoff`), descriptor schema fields (`primitives:` and `threading:`),
two doctor actions (`discord_doctor`, `slack_doctor`), two settings
fragments (`channels.discord.*`, `channels.slack.*`), the ADR 0016
amendment Status flip, and **one new inbound permission class
(`:channel_message_inbound`, ADR 0056)** plus the local M7
`:conversation_write` class for explicit canonical thread resume mapping — but
no new operation classes, no new URI scheme, no new memory namespace, and no
provider-id authority. HTTP uses `Req`. The WebSocket transport
vehicle per provider is **locked by an M0 spike** (M0 decision 6) — raw `Req` +
a single reviewed WS client vs an ecosystem library (Nostrum / `slack_elixir`) —
recorded as a locked decision before M2/M3, reconciled with the Req-only rule and
ADR 0050, rather than borrowed through an unreviewed transitive dependency. The
deterministic provider-stub lane is the fast/CI gate and exercises the real
adapter + request-shape code; required pre-tag provider validation consists of
real outbound/threading/echo smoke evidence plus operator manual live
inbound/callback checks (§"Milestone 8").

## Cross-Channel Conversation Threading (ADR 0057)

v0.52 treats Slack and Discord as more channel inputs and outputs for the same
Allbert runtime, but those providers add their own "thread" concepts — and so do
Telegram, email, and the future mobile channels, each differently. ADR 0057 adds
the system-wide construct that makes one Allbert conversation coherent across all
of them. The implementation keeps four identities separate:

- **Local user identity**: provider user id -> Allbert `user_id` through
  `Channels.Identity` / `channels.<provider>.identity_map`.
- **Channel session identity**: bounded `session_id` derived by
  `AllbertAssist.Channels.derive_session_id/3` (or the provider-specific
  strategy declared in the channel descriptor). This scopes session scratchpad
  and recent runtime continuity for a channel turn.
- **Allbert conversation identity**: internal `thread_id` selected by
  `Conversations.resolve_thread/1` inside `Runtime.submit_user_input/1`.
  This is the durable conversation-history authority.
- **Provider thread identity**: provider-native reply context used for
  dedupe, routing, and output placement. Slack uses `thread_ts` (or the
  inbound message `ts` as the root when starting a thread). Discord uses
  thread channel ids when a message is inside a Discord thread channel and
  `message_reference` for ordinary replies.

Provider thread roots participate in channel session derivation and are recorded
as a redacted `provider_thread_ref`, but they are **never** Allbert `thread_id`
values and never grant authority (ADR 0056). The normalized provider thread ref
also yields a deterministic `provider_thread_key` used for database uniqueness.
The redacted reference is a small map, for example:

```elixir
%{
  provider: "slack" | "discord" | "telegram" | "email",
  workspace_or_guild_id: "...",
  channel_id: "...",
  provider_thread_root: "...",
  provider_message_id: "..."
}
```

### Canonical model + mapping tables (decision 8)

The canonical conversation id **is** the existing `conversation_threads.id`
(`thr_<UUID>`); `Conversations.resolve_thread/1` stays the sole authority for
new threads. Three durable SQLite tables map it to each channel:

- **`thread_channel_refs`** — `owner_scope`, `canonical_thread_id`, `channel`,
  `receiver_account_ref`, deterministic `provider_thread_key`, redacted
  `provider_thread_ref`, timestamps.
  `UNIQUE(owner_scope, channel, receiver_account_ref, provider_thread_key)`.
  Answers "where does this thread live on each channel."
- **`conversation_message_refs`** — `owner_scope`, `canonical_message_id`,
  `canonical_thread_id`, `channel`, `receiver_account_ref`,
  `provider_message_id`, `part_id`, `direction` (`in`/`out`), timestamps.
  `UNIQUE(owner_scope, channel, receiver_account_ref, provider_message_id,
  part_id)`. Powers reply/thread translation, dedup, and
  **echo-loop suppression**: an inbound event whose `provider_message_id`
  matches a recorded `out` ref is Allbert's own echo and is dropped (the
  runaway-relay failure mode). `part_id` allows one canonical message to split
  into N native messages.
- **`cross_channel_identity_links`** — `owner_scope`, `link_id`, `user_id`,
  `channel`, `receiver_account_ref`, `external_user_id`, timestamps.
  Groups already-mapped per-channel identities for unified history + explicit
  resume; it never authenticates a provider user by itself.

`owner_scope` is `"local"` in v0.52. It is a future multi-user/multi-tenant
schema hook, not a hosted-tenancy feature or permission source. `receiver_account_ref`
is the redacted configured Allbert-side receiver/install/account (Slack team
binding, Discord bot/application + guild/DM scope, Telegram bot/chat, email
mailbox, web workspace, CLI profile). `provider_thread_key` is a bounded string
or hash; `provider_thread_ref` is structured redacted metadata and is never used
as a raw JSON unique key.

`AllbertAssist.Conversations.ChannelThread` is the single module that reads/writes
these tables, resolves a reply target's `provider_message_id` → canonical
message → the target channel's native id, and never creates conversation
authority. A channel may pass a `thread_id` to `Runtime.submit_user_input/1`
only when `ChannelThread.lookup_thread/1` has already returned an existing
canonical Allbert thread id for the normalized provider key; otherwise it omits
`thread_id` and links the runtime response thread afterward.

### Threading capability + degradation (decision 9)

Each channel descriptor declares `threading:` — `:native_threads` (Discord,
Slack, Telegram topic chats), `:reply_chain` (email, Telegram DM), `:flat` (SMS,
future), or `:rich` (web, CLI) — plus `can_create_thread`, `reply_key_type`
(`:opaque_id | :timestamp`), and `quote_ttl_ms`. `ChannelThread` picks the render
strategy with this degradation ladder:

1. `:native_threads` → place in the native thread (Slack `thread_ts` / Discord
   thread channel / Telegram `message_thread_id`).
2. `:reply_chain` → set the native reply/quote pointer (email
   `In-Reply-To`+`References`; Telegram Bot API `reply_parameters`, the modern
   reply mechanism that replaces the older `reply_to_message_id` field).
3. `:flat` → one stream per peer; inline a short text quote when context is
   needed (an optional cosmetic `[#id]` tag is never a parse key).
4. last resort → drop threading, keep strict ingest order, rely on the unified
   history view.

Cross-channel ordering uses Allbert's own ingest sequence, never provider clocks.

### Unified view + explicit resume (decisions 10, 11)

A read-mostly **unified history view** (web + CLI) aggregates a canonical
thread's messages across channels, redacted via `Runtime.Redactor`. Continuing a
conversation on a *different* channel is an explicit operator action
(`resume_thread_on_channel`), reusing the same canonical thread id and requiring
the same resolved local `user_id`. **Explicit cross-channel identity links**
group already-mapped per-channel identities under one person for the unified view
and resume; links are operator-configured, never auto-derived. There is no silent
live cross-channel mirroring.

## Approval Primitive Selection (`AllbertAssist.Approval.Handoff`)

The ADR 0016 amendment §"Selection Rule" requires a
shared selection layer with order `:button > :typed_command > :link >
:list`. Today each renderer (`Channels.Telegram.Renderer`,
`Channels.Email.Renderer`) hand-rolls its own choice. v0.52 introduces
the shared module.

### Module surface

`AllbertAssist.Approval.Handoff` lives at
`apps/allbert_assist/lib/allbert_assist/approval/handoff.ex`. It is a
separate module from:

- `AllbertAssist.Intent.ApprovalHandoff` — keeps its existing
  `lines/1` role of producing the text-line representation of an
  approval payload (consumed by all renderers).
- `AllbertAssist.Intent.Handoff` — keeps its existing v0.33 role of
  app-intent handoff and clarification (unrelated despite the similar
  name).

### `render/2` contract

```elixir
@spec render(handoff_payload :: map(), descriptor :: map()) ::
        {:ok, {primitive_kind(), payload :: map()}} | {:error, term()}

@type primitive_kind :: :button | :typed_command | :link | :list
```

The selection rule:

1. Read an effective descriptor. Adapters start from
   `descriptor.primitives` (a non-empty list of `primitive_kind` atoms,
   guaranteed by the registry validator from §"Channel Descriptor
   Primitives Field" to include `:list`) and remove `:button` before calling
   `render/2` when provider settings set `render_approval_buttons: false`.
2. Pick the highest-fidelity primitive the adapter declares, in order
   `:button > :typed_command > :link > :list`. `:link` is eligible only
   when the handoff payload carries a workspace URL in
   `handoff_payload.render_hints.workspace_url` or
   `handoff_payload.workspace_url`; otherwise selection continues to `:list`.
3. Build the payload for that primitive from the `handoff_payload`
   (confirmation id, action label, ObjectiveContext lines, ApprovalHandoff
   lines). The payload shape depends on the primitive:
   - `:button` → `%{text: text, buttons: [%{label, callback_data}]}`
   - `:typed_command` → `%{text: text, commands: ["ALLBERT:APPROVE:<id>", "ALLBERT:DENY:<id>", "ALLBERT:SHOW:<id>"]}`
   - `:link` → `%{text: text, url: workspace_url}`
   - `:list` → `%{text: text, numbered_options: [...]}`
4. Each adapter's `Renderer.render_approval_handoff/1` consumes
   `{primitive_kind, payload}` and formats it into the provider-specific
   wire shape (Telegram inline keyboard, Slack Block Kit blocks, etc.).

The `render/2` signature does not receive settings. Provider renderers own
the settings-to-effective-descriptor step so the module boundary remains
deterministic and testable.

### Renderer retroactive consumption

M0 extracts the primitive selection from
`Channels.Telegram.Renderer.render_approval_handoff/2` and
`Channels.Email.Renderer.render_approval_handoff/2`. Each renderer's
behavior is unchanged at the wire level (Telegram still renders inline
keyboards; email still renders typed commands) — but the *choice* now
flows through `Approval.Handoff.render/2`. M0 acceptance proves the
existing v0.16 Telegram/email approval flows are byte-equivalent before
and after the refactor.

## Channel Descriptor Primitives Field

The amendment requires adapters to declare their supported primitives "in
their plugin descriptor" (§"v0.52 Amendment"). v0.52 makes this concrete.

### Schema extension

The channel descriptor map returned by `<Plugin>.channels/0` (see
`plugins/allbert.telegram/lib/allbert_assist/plugins/telegram.ex:18-36`
for the v0.17 baseline) grows a `primitives:` field:

```elixir
%{
  channel_id: "telegram",
  # ...existing fields...
  primitives: [:button, :typed_command, :list]   # new at v0.52
}
```

### Declared subsets (per the amendment "Adapter Declarations" table)

| Adapter | `primitives:` |
|---|---|
| CLI | `[:list]` |
| LiveView | `[:button, :list]` |
| Telegram | `[:button, :typed_command, :list]` |
| Email | `[:typed_command, :list]` |
| Discord | `[:button, :typed_command, :list]` |
| Slack | `[:button, :typed_command, :list]` |

CLI and LiveView are not shipped plugin descriptors today, but any local
surface that consumes the shared selector must expose the same effective
primitive subset above. LiveView keeps `:list` as the universal fallback even
when its primary affordance is a workspace modal button.

### Registry validation

`AllbertAssist.Plugin.Validator` rejects channel descriptors during
`AllbertAssist.Plugin.Registry` discovery when they:

- omit the `primitives:` field, OR
- declare an empty list, OR
- declare a primitive not in `[:button, :typed_command, :link, :list]`,
  OR
- omit `:list` (the universal fallback).

Rejected descriptors fail plugin discovery with a structured diagnostic; the
adapter does not start. The registry remains the public discovery facade, but
the validator owns the descriptor-shape checks so they are not reimplemented in
individual adapters.

### Migration

M0 adds the field to existing Telegram + email descriptors retroactively
and lands the registry validator. M2 (Discord plugin skeleton) and M3
(Slack plugin skeleton) declare the field from day one. M4 wires the
descriptor field through `Approval.Handoff.render/2` for confirmation
callbacks.

## Channel Settings (`channels.discord.*` / `channels.slack.*`)

Per M0 decision 4, settings live in plugin-owned `Settings.Fragment`
modules (`AllbertDiscord.Settings.Fragment`,
`AllbertSlack.Settings.Fragment`), not in core `Settings.Schema`.
Fragments compose into the operator-visible settings namespace at plugin
discovery time per ADR 0031.

The existing settings schema treats `channels` as a reserved namespace. M0
therefore adds an owner-aware fragment bridge in
`AllbertAssist.Settings.Fragments.plugin_fragments/1` (and the plugin schema
normalization it calls) that permits trusted, source-tree channel plugins to
contribute only their own `channels.<provider>.*` subtree (`discord` may not
claim `slack`, and home/imported/project-pending plugin metadata may not claim
any core channel namespace). M2 and M3 use that bridge for Discord and Slack.
This is a Settings Central change, not a bypass: reads, writes, validation,
audit, and redaction still go through Settings Central.

### `channels.discord.*` (Discord plugin)

```yaml
channels:
  discord:
    enabled: false                       # opt-in master switch
    bot_token_ref: null                  # required when enabled: true
    application_id: null                 # bot application id (string)
    allowed_guild_ids: []                # operator-curated allowlist
    allowed_channel_ids: []              # optional sub-allowlist within allowed guilds
    response_style: "mention"            # mention | always | dm_only
    max_text_bytes: 2000                 # Discord message hard limit
    render_approval_buttons: true        # respects primitive declaration
    identity_map:                       # existing v0.16 list shape
      - external_user_id: "11111"
        user_id: "alice"
        enabled: true
    gateway_intents:                     # opt-in subset; default minimal
      - guild_messages
      - direct_messages
      - message_content
    gateway:
      reconnect_max_backoff_ms: 30000
      heartbeat_jitter: true
```

Discord plugin secret refs (stored through Settings Secrets):

- `channels.discord.bot_token_ref` → `secret://channels/discord/bot_token`

Discord plugin does NOT collect: Interactions signing key (Interactions
transport is parked); OAuth client secret (multi-workspace install
parked per M0 decision 5 — Discord doesn't have multi-workspace OAuth
in the Slack sense, but multi-bot per-Allbert-home is parked).

### `channels.slack.*` (Slack plugin)

```yaml
channels:
  slack:
    enabled: false                       # opt-in master switch
    bot_token_ref: null                  # xoxb-... required when enabled: true
    app_token_ref: null                  # xapp-... required for Socket Mode
    signing_secret_ref: null             # reserved for Events API HTTP (parked)
    workspace_team_id: null              # single-workspace per M0 decision 5
    allowed_channel_ids: []              # operator-curated allowlist
    response_style: "mention"            # mention | always | dm_only
    max_text_bytes: 3000                 # Slack message practical limit
    render_approval_buttons: true        # respects primitive declaration
    identity_map:                       # existing v0.16 list shape
      - external_user_id: "U0123ABCDE"
        user_id: "alice"
        enabled: true
    socket_mode:
      enabled: true                      # invariant at v0.52; Events API parked
      reconnect_max_backoff_ms: 30000
```

Slack plugin secret refs:

- `channels.slack.bot_token_ref` → `secret://channels/slack/bot_token`
- `channels.slack.app_token_ref` → `secret://channels/slack/app_token`
- `channels.slack.signing_secret_ref` → reserved; unused at v0.52

### Safe-write keys

Per fragment, the safe-write keys mirror the YAML keys above with the
following invariants enforced at the fragment schema layer:

- `channels.discord.enabled` and `channels.slack.enabled` default `false`.
- `channels.slack.socket_mode.enabled` is read-only `true` at v0.52
  (decision 3).
- `channels.<provider>.identity_map` keeps the existing v0.16
  `channels_identity_map` list-of-maps shape; v0.52 does not migrate it to a
  raw map.
- Provider fragments add cross-field validation for required-when-enabled
  keys. Discord requires `bot_token_ref`, `application_id`, and at least one
  `allowed_guild_ids` entry when enabled. Slack requires `bot_token_ref`,
  `app_token_ref`, `workspace_team_id`, and at least one
  `allowed_channel_ids` entry when enabled.
- Permission settings are core-owned. v0.52 adds exactly one core permission
  setting, `permissions.channel_message_inbound` (ADR 0056; default
  `needs_confirmation`, floor `needs_confirmation`, allowed values
  `["needs_confirmation", "denied"]`, cannot be lowered below the floor). Channel
  *plugins* still do not invent permission classes; the inbound tier is
  core-owned and ADR-introduced.

## Permission, Operation, And Identity Additions

**One new inbound permission class (ADR 0056), one local conversation-mapping
permission class (M7), and no new operation classes or URI schemes.** v0.52
keeps the v0.16 delivery-adapter boundary — runtime authority lives in Security
Central, Resource Access, `Confirmations`, and registered actions — but,
symmetric with v0.51's ADR 0055 (public-surface inbound) and ADR 0038 (outbound
MCP), it names the channel inbound boundary as a trust tier rather than leaving
it implicit in adapter code.

### `:channel_message_inbound` (ADR 0056)

New permission class **`:channel_message_inbound`**, safety floor
**`:needs_confirmation`** (configured default `needs_confirmation`, cannot be
lowered below the floor). M0 registers it at **every** spot the `:artifact_*` /
`:public_surface_call_inbound` precedent touches — not just the floor/decision
pair — or it half-wires and fails the settings/trace-reason tests:

- `Security.Policy` `@permission_settings` (→ `permissions.channel_message_inbound`),
  `@default_decisions`, the `permission()` typespec union, the
  `permission_classes/0` list, a `safety_floor/2` clause, and the `reason/5`
  trace/status-output clauses;
- `Security.Risk` `tier/1` **and** `reasons/3`;
- `Settings.Schema` `@safe_write_keys` (`permissions.channel_message_inbound`)
  plus the permission `defaults` enum (default `needs_confirmation`, allowed
  values `["needs_confirmation", "denied"]`).

Why a permission class and not just identity mapping: channel inbound is
**untrusted yet distinct** from public-surface inbound. A channel message only
reaches the runtime after it resolves to an allowlisted, identity-mapped local
operator (`Channels.Identity`) and inherits that user's authority — unlike a
public-surface client, which is an anonymous Settings-Central token bearer. The
permission class records the trust tier and floor of the resolved inbound call;
it does **not** replace identity mapping + allowlist, which remain the
authentication gate. Confirmation-decision actions stay `:internal` and are never
reachable through a channel (self-approval denial). See ADR 0056 for the full
posture and the per-interaction clicker-authorization invariant.

### `:conversation_write` (M7 local mapping write)

M7 adds **`:conversation_write`** for the explicit
`resume_thread_on_channel` action. This is a local canonical-conversation mapping
write: it can create/update `thread_channel_refs` for a user-owned
`conversation_threads.id` after the action verifies the same local `user_id` and,
for external channels, an explicit `cross_channel_identity_links` row. It is not
an inbound trust tier, not a provider permission, not a tenant boundary, and not
an operation or URI class. It exists so the resume action is not mislabeled as
read-only or hidden under settings/objective permissions.

## Action Schemas And Capability Metadata

The shared `list_channels` / `show_channel` actions (v0.16) remain the
operator-facing handles, but `show_channel` needs a v0.52 generic detail path
for plugin-owned channels so Discord/Slack are not forced through the old
Telegram/email-only detail clauses. v0.52 adds two doctor actions, one per
provider, and M7 adds the registered `resume_thread_on_channel` action for
explicit canonical thread resume.

| Action | Permission | Execution mode | Exposure | Notes |
|---|---|---|---|---|
| `discord_doctor` | `:read_only` | `:channel_diagnostic` | `:internal` | ADR 0047-style redacted channel-doctor envelope; calls Discord `GET /users/@me` with the configured bot token through `Channels.Discord.Client`; reports gateway connectivity status, bot username, application id, last successful Gateway READY event timestamp, and any auth failures. |
| `slack_doctor` | `:read_only` | `:channel_diagnostic` | `:internal` | ADR 0047-style redacted channel-doctor envelope; calls Slack `auth.test` with the configured bot token; reports workspace name, bot user id, team id, last successful Socket Mode hello-event timestamp, and any auth failures. |
| `resume_thread_on_channel` | `:conversation_write` | `:conversation_resume` | `:agent` | Explicitly resumes a user-owned canonical thread on a target channel; external channels require a matching explicit cross-channel identity link. |

Action schemas:

| Action | Required | Optional |
|---|---|---|
| `discord_doctor` | — | `verbose` |
| `slack_doctor` | — | `verbose` |
| `resume_thread_on_channel` | `thread_id`, `user_id`, `channel` | `receiver_account_ref`, `external_user_id`, `provider_thread_key`, `provider_thread_ref` |

Doctors persist their last result envelope under
`<ALLBERT_HOME>/cache/channels/<provider>/doctor/state.json` so the
adapter's startup path can read the last-known status (v0.43 R2 doctor
live-verify pattern). Adapters do not refuse to start solely on a stale
doctor at v0.52 (channels are passive delivery surfaces, not session-
based runtimes like the browser), but the doctor result surfaces in
`mix allbert.channels show <provider>` output.

## Expected Inputs

- **v0.16 channel adapter boundary** (ADR 0016 §Decision): the
  delivery-adapter posture, `channel_events` table for dedupe and
  metadata, `AllbertAssist.Channels.Identity` for external→local user
  mapping, session_id derivation rules, `:one_for_one` adapter
  supervision under `AllbertAssist.Channels.Supervisor`.
- **v0.17 plugin contract**: `AllbertAssist.Plugin`, `Plugin.Registry`,
  shipped plugin discovery via `./plugins/`, plugin manifest
  (`allbert_plugin.json`), descriptor map shape, plugin-owned child
  supervisor, settings fragment composition.
- **v0.17 shipped Telegram + email plugins** as the operative precedent.
- **v0.27 action DSL** (`AllbertAssist.Action`) for the two doctor
  actions.
- **v0.31 Settings Central fragment substrate** (ADR 0031) for plugin-
  owned settings composition.
- **v0.40 MCP doctor pattern** as the redacted-shape precedent for
  `discord_doctor` / `slack_doctor` (already proven through ADR 0047).
- **v0.42 closeout lessons**: release smoke gate, doc-sync discipline,
  runs-as-written acceptance, plugin-owned settings.
- **v0.43 pass-3 patterns**: doctor live-verify (M0 decision 1 doctor
  contract), per-milestone Implementation/Acceptance/Tests/Operator/
  Expected/Handoff/Lane shape.
- **Existing `AllbertAssist.Intent.ApprovalHandoff`** (text formatter,
  consumed unchanged by all renderers).
- **Existing `Channels.Telegram.Renderer` and `Channels.Email.Renderer`**
  for the M0 retroactive refactor.

## Operator-Visible Win

Allbert can be reached from Discord and Slack with safe mention handling,
threaded replies, and confirmation callbacks.

A first-time Discord flow walks through:

1. Operator: `mix allbert.channels discord set-token secret://...`
   (token reference stored; raw token rejected) →
   `mix allbert.channels discord set-application-id ID`
   → adds guild id to `allowed_guild_ids` → `mix allbert.channels
   discord map --external-user 123456 --user alice` → enables in
   settings.
2. `mix allbert.channels show discord` runs `discord_doctor` and shows
   bot identity + gateway status.
3. External Discord user mentions the bot in an allowed guild channel;
   the message reaches `Runtime.submit_user_input/1` with local user
   resolved to `alice`.
4. Bot replies in the same channel/thread.
5. If the operator's prompt triggers an effectful action, the bot
   renders a confirmation as a Discord button (primitive `:button`
   selected by `Approval.Handoff.render/2`); approve/deny resolves
   the pending confirmation through the standard Approval Handoff
   path.

A first-time Slack flow walks through the same shape with Slack-
specific commands (`set-token`, `set-app-token`, `set-team-id`,
allowlist, identity map, doctor, mention, button approval). DM-only mode
is supported via `response_style: dm_only` for both providers.

## Planned Interfaces

### Plugin packages

- `./plugins/allbert.discord/`:
  - `allbert_plugin.json` (manifest, kind=channel, contributions.channels=["discord"])
  - `AllbertAssist.Plugins.Discord` (Plugin module, declares
    `channels/0` returning the descriptor map with `primitives:
    [:button, :typed_command, :list]`)
  - `AllbertAssist.Channels.Discord.Adapter` (GenServer; Gateway
    WebSocket lifecycle behind `Client.GatewayPort`; reconnect/backoff/
    heartbeat; READY + MESSAGE_CREATE + INTERACTION_CREATE event handling)
  - `AllbertAssist.Channels.Discord.Client` (HTTP REST client for
    `GET /users/@me`, `GET /gateway/bot`,
    `POST /channels/{channel.id}/messages`, channel info, and
    `POST /interactions/{interaction.id}/{interaction.token}/callback`
    for Gateway-delivered component callbacks)
  - `AllbertAssist.Channels.Discord.Renderer` (consumes
    `Approval.Handoff.render/2` output; renders text + buttons via
    Discord components)
  - `AllbertAssist.Channels.Discord.Parser` (normalizes inbound Gateway
    events into the shared `Runtime.submit_user_input/1` shape)
  - `AllbertDiscord.Settings.Fragment` (plugin-owned `channels.discord.*`
    schema)
- `./plugins/allbert.slack/`:
  - `allbert_plugin.json` (manifest, kind=channel)
  - `AllbertAssist.Plugins.Slack` (Plugin module, descriptor with
    `primitives: [:button, :typed_command, :list]`)
  - `AllbertAssist.Channels.Slack.Adapter` (GenServer; Socket Mode
    WebSocket lifecycle behind `Client.SocketModePort`; `hello`,
    `events_api`, `interactive`, envelope ack, and `disconnect`
    handling)
  - `AllbertAssist.Channels.Slack.Client` (HTTP for
    `apps.connections.open`, `auth.test`, `chat.postMessage`,
    `chat.update`)
  - `AllbertAssist.Channels.Slack.Renderer` (consumes
    `Approval.Handoff.render/2`; renders text + Block Kit buttons)
  - `AllbertAssist.Channels.Slack.Parser` (normalizes inbound Socket
    Mode events)
  - `AllbertSlack.Settings.Fragment` (plugin-owned `channels.slack.*`)

### Core additions

- `AllbertAssist.Approval.Handoff` (new module owning `render/2`
  primitive selection; see §"Approval Primitive Selection" above).
- Channel descriptor `primitives:` field added to
  `AllbertAssist.Plugin.Validator` (invoked through `Plugin.Registry`
  discovery), with rejection for missing/empty/invalid declarations.
- `show_channel` generic plugin-channel detail support: descriptor fields,
  `primitives`, `settings_prefix`, effective settings summary, identity-map
  size, allowlist counts, and last doctor result for Discord/Slack.
- `discord_doctor`, `slack_doctor` registered actions under
  `AllbertAssist.Actions.Channels.*` or the analogous v0.40 doctor
  location (`AllbertAssist.Actions.Mcp.DoctorServer` pattern).

### CLI surface

Discord:

```text
mix allbert.channels discord set-token TOKEN_REF
mix allbert.channels discord set-application-id APPLICATION_ID
mix allbert.channels discord add-guild GUILD_ID
mix allbert.channels discord remove-guild GUILD_ID
mix allbert.channels discord add-channel CHANNEL_ID
mix allbert.channels discord remove-channel CHANNEL_ID
mix allbert.channels discord map --external-user EXTERNAL --user USER
mix allbert.channels discord unmap --external-user EXTERNAL
mix allbert.channels discord simulate --guild GUILD --channel CHANNEL --user EXTERNAL "prompt"
mix allbert.channels discord simulate --guild GUILD --channel CHANNEL --thread-channel THREAD --user EXTERNAL "prompt"
mix allbert.channels discord doctor
```

`set-token` accepts only a `secret://` reference and rejects raw token
strings. The operator guide owns raw secret creation/storage; the channel CLI
stores refs only.

Slack:

```text
mix allbert.channels slack set-token TOKEN_REF
mix allbert.channels slack set-app-token APP_TOKEN_REF
mix allbert.channels slack set-team-id TEAM_ID
mix allbert.channels slack add-channel CHANNEL_ID
mix allbert.channels slack remove-channel CHANNEL_ID
mix allbert.channels slack map --external-user EXTERNAL --user USER
mix allbert.channels slack unmap --external-user EXTERNAL
mix allbert.channels slack simulate --channel CHANNEL --user EXTERNAL "prompt"
mix allbert.channels slack simulate --channel CHANNEL --thread-ts TS --user EXTERNAL "prompt"
mix allbert.channels slack doctor
```

`set-token` and `set-app-token` accept only `secret://` references and reject
raw `xoxb-...` / `xapp-...` token strings.

Shared (v0.16 baseline):

```text
mix allbert.channels list                  # lists all configured providers
mix allbert.channels show discord          # full Discord status + doctor result
mix allbert.channels show slack            # full Slack status + doctor result
```

### Intent routing

No intent-routing changes at v0.52. Inbound channel messages reach
`Runtime.submit_user_input/1` directly; the existing intent agent
handles them per its normal contract. Mention parsing and bot-prefix
extraction live in `Channels.<Provider>.Parser`.

## Transport And Lifecycle Decisions

### Discord — Gateway WebSocket (M0 decision 2)

- **Why Gateway, not Interactions HTTP?** Gateway requires no public
  inbound URL — Allbert connects out. Mirrors v0.16's "no public
  inbound HTTP" stance for Telegram long-polling. Interactions HTTP
  requires HTTPS exposure + Ed25519 verification per request and is
  parked to v0.52.x.
- **Message ingestion — privileged intent (M0 decision 3 scope).** To read
  free-text, the bot enables the **privileged `MESSAGE_CONTENT` gateway
  intent** (without it, `MESSAGE_CREATE` arrives with empty `content`). v0.52
  reads free-text only **on @mention or in DMs** — symmetric with the Slack
  mention+DM scope — not by scanning all channel traffic. The operator
  self-enables `MESSAGE_CONTENT` in the Developer Portal (allowed without review
  under 100 guilds; ≥100 guilds requires Discord approval, and Discord may deny
  it where the use case is achievable via interactions). The operator doc
  (`docs/operator/discord-channel.md`) documents the self-enable step and the
  ≥100-guild caveat; the live smoke asserts the Gateway stays connected (no
  `4014` close from an unauthorized privileged intent).
- **Why single shard?** Discord requires sharding only for bots in
  >2,500 guilds. v0.52 targets per-operator personal-scale and small-
  team scale; single shard suffices. Sharding parked.
- **Connection lifecycle.** `Channels.Discord.Adapter` GenServer owns:
  `GET /gateway/bot` through `Req` to obtain the current WSS URL and
  session-start-limit state; initial `IDENTIFY` with `bot_token_ref`
  resolution + intents bitmask; heartbeat loop with `heartbeat_jitter`;
  `READY` event capture; resume with `RESUME` after disconnect using stored
  session id + seq; reconnect with exponential backoff capped at
  `reconnect_max_backoff_ms`; clean shutdown via a normal Gateway close on
  supervisor terminate. `/gateway/bot` output is a connection-time hint, not
  durable configuration.
- **Interaction acknowledgement.** Gateway-delivered component interactions
  are acknowledged through Discord's interaction callback HTTP endpoint using
  the interaction id and token from the payload. They are not sent back over
  the Gateway. Stub tests assert the callback request shape; real callback
  delivery is external-smoke.
- **Crash isolation.** Adapter supervised under
  `AllbertAssist.Channels.Supervisor` with `:one_for_one`; an adapter
  crash takes its own session, not Slack, not core (v0.16 invariant
  preserved).

### Slack — Socket Mode (M0 decision 3)

- **Why Socket Mode?** Direct analog to Telegram long polling. No public
  URL; bot uses an App-Level Token to open a WebSocket to Slack. Events
  API HTTP requires HTTPS + HMAC signing and is parked.
- **Why single workspace?** Multi-workspace requires OAuth install flow,
  per-workspace token storage, team-id scoping on every API call. Out
  of scope for "Pack 1." Parked to v0.52.x or v1.x per M0 decision 5.
- **Connection lifecycle.** `Channels.Slack.Adapter` GenServer owns:
  `apps.connections.open` call through `Req` with the app-level token in
  the `Authorization` header to get a fresh temporary WS URL; WebSocket
  connect; `hello` event capture; `events_api` envelope handling (sends an
  ack for the exact `envelope_id` before or immediately after durable
  `channel_events` receipt); `interactive` envelope handling for button
  taps; `chat.update` or response-target update for the rendered callback
  result; reconnect with exponential backoff and fresh
  `apps.connections.open` per reconnect; clean shutdown.
- **Crash isolation.** Same `:one_for_one` posture as Discord.

### Deferred per-provider

| Item | Reason |
|---|---|
| Discord Interactions HTTP transport | Requires public HTTPS + Ed25519; M0 decision 2 |
| Discord sharding | Not needed at v0.52 scale; M0 decision 2 |
| Discord OAuth (multi-app install) | Not in scope |
| Slack Events API HTTP transport | Requires public HTTPS + HMAC; M0 decision 3 |
| Slack multi-workspace OAuth | M0 decision 5 |
| Discord / Slack channel scanning beyond mention + DM | Out of scope for Pack 1 (both providers read free-text only on @mention or DM) |
| Slack / Discord thread-topic creation and discovery | M3 preserves replies in an existing provider thread or channel. Creating, discovering, or managing provider thread topics is out of scope. |
| Both: rich media uploads | No upload semantics at v0.52 |
| Both: voice channels | v0.48 owns voice modality |

## Why Plugin-Shaped

ADR 0017 made the plugin contract Allbert's contribution boundary. v0.16
established Telegram and email as the proving channel plugins; v0.52
extends the shape to Discord and Slack without growing core surface.
Each plugin contributes:

- one channel descriptor (with the new `primitives:` field);
- one Adapter GenServer for the provider lifecycle;
- one Client for outbound HTTP/WS calls;
- one Renderer that consumes `Approval.Handoff.render/2` output;
- one Parser for inbound payload normalization;
- one `Settings.Fragment` for `channels.<provider>.*`;
- one doctor action (`<provider>_doctor`);
- a `mix allbert.channels <provider> *` CLI subcommand surface.

The four channel plugins (Telegram, Email, Discord, Slack) share the
same v0.16 substrate (`channel_events` for dedupe, `Channels.Identity`
for external→local mapping, session_id derivation, supervised
`:one_for_one`); they diverge only in their provider transport, payload
shape, and primitive subset.

## v0.44 Plan/Build Channel Plan Cards (Inherited Contract)

v0.44 Plan/Build composes objective workflow steps that can render plan
summaries to the surfaces available at that milestone. v0.52 ensures those
summaries are expressible through the primitive contract for Discord and Slack:

- Plan summaries render through `Approval.Handoff.render/2` selecting
  the best primitive the adapter declares.
- Per-step approval prompts reuse the standard Approval Handoff path —
  no v0.52-specific shape required.
- Multi-step plans that exceed `max_text_bytes` per provider are split
  into chunks by the existing renderer chunking logic; primitive
  selection runs once per chunk.

v0.44 needs no ADR 0016 amendment to ship. The primitive contract becomes
binding here, in v0.52, when the additional channel adapters land.

## v0.47 Trace Mining (Post-v0.52 Source)

v0.47 self-improvement ships before team channels and therefore does not
depend on these traces. After v0.52 lands, the same read-only
pattern-discovery action may mine v0.52 team-channel traces as one additional
source for trace-to-skill and trace-to-workflow draft suggestions. v0.52
preserves the v0.16 trace shape:

- `channel_events` carries external identity (redacted), local user
  resolution, dedupe metadata, status; raw provider payloads are
  redacted/truncated, not stored verbatim.
- Trace mining sees the resolved local `user_id`, the channel id, the
  event kind, and the runtime turn — not raw Discord/Slack payloads,
  not bot tokens, not signing secrets.
- The `redact_provider_payload` discipline from v0.16 applies to Discord
  and Slack identically.

v0.47 needs no channel-specific contract. v0.52 supplies the additional traces
later without changing the self-improvement trust tier.

## v0.53 Mobile Channels (Forward Pin)

v0.53 (WhatsApp, Signal, Matrix) consumes the v0.52 amendment directly:

- WhatsApp primitives: `[:button, :typed_command, :link, :list]`
  (`:button` only while provider/session rules permit interactive replies;
  `:typed_command`, `:link`, and mandatory `:list` remain the fallback path).
- Signal primitives: `[:typed_command, :link, :list]` (no native
  buttons).
- Matrix primitives: `[:typed_command, :link, :list]` (no portable Matrix
  bot-button primitive is claimed for v0.53).

Each v0.53 provider declares its `primitives:` field via the M0 schema
extension and is validated by the same `Plugin.Registry` rule. No v0.52-
specific changes required for v0.53 to land.

## Security Posture

Channels do not invent channel-specific action authority or confirmation
shapes. v0.52 adds exactly one ADR-owned inbound permission class
(`:channel_message_inbound`, ADR 0056); no other permission classes are in
scope.
Group/server authorization, workspace mapping, and callback ownership
must be explicit. Approval Handoff remains provider-neutral and
action-owned.

Specific v0.52 invariants:

- **Allowlist before runtime submission.** Discord guild + channel
  allowlists and Slack channel allowlist gate inbound message handling
  before `Runtime.submit_user_input/1` is called. Messages from
  non-allowlisted surfaces produce a `channel_events` rejected record
  and are not submitted to the runtime.
- **External identity must be mapped.** Inbound events without an
  `identity_map` entry produce a `channel_events` rejected-unknown
  record and no runtime submission. There is no implicit `user_id`
  assignment.
- **Bot tokens are secret refs only.** `bot_token_ref`,
  `app_token_ref`, and `signing_secret_ref` are `secret://` references;
  raw values never appear in `mix allbert.channels show` output,
  in `channel_events` rows, in traces, or in audit records.
- **Provider payload redaction.** `Channels.<Provider>.Parser` redacts
  bot tokens, signing secrets, full Authorization headers, full
  message embed URLs with credentials, and any field marked as
  redacted by the parser's allowlist (v0.16 pattern preserved).
- **No callback-data smuggling.** The callback data string format
  (`allbert:v1:<verb>:<id>`) is preserved from v0.16 Telegram. Discord
  custom_id and Slack action_id fields use the same format. Parser
  rejects malformed callback data; the registered confirmation action
  rejects callback data whose verb is not in
  `{approve, deny, show}` or whose confirmation id does not match an
  open pending request for the resolved local user.
- **Clicker re-authorized per interaction (ADR 0056).** A button/component tap
  re-resolves the **clicker's** external id through `Channels.Identity` on every
  tap; the callback payload's embedded user fields are never trusted as
  authority. An unmapped or non-allowlisted clicker is rejected (ephemeral "not
  authorized") and recorded as a rejected `channel_events` row. The confirmation
  action additionally requires that the resolved clicker owns the open pending
  request. Anyone who can see the message can click it, so the identity check is
  server-side on every click — not just at render time.
- **Ack before runtime.** The provider acknowledgement (Slack `envelope_id` ack /
  Discord deferred component response) is sent **before** the slow runtime
  submission, within the provider's response window. `channel_events` dedupe
  absorbs Slack retries (`X-Slack-Retry-Num`) and Discord RESUME redelivery so a
  redelivered message never double-submits and a redelivered click never
  double-approves an already-resolved confirmation.
- **Cross-channel confirmation isolation.** A confirmation pending
  resolution from Slack cannot be approved via Discord (and vice
  versa). The pending request records its origin channel/surface; the
  registered confirmation action enforces the channel match.
- **DM vs workspace surface auth.** DM-routed messages are treated as
  authenticated only when the external user has an `identity_map`
  entry. DM-only mode (`response_style: dm_only`) restricts the bot
  to never reply in shared channels.

## Non-Goals

- No SMS.
- No WhatsApp, Signal, or Matrix in this milestone (v0.53); no iMessage
  (parked).
- No channel-specific action authority.
- No private durable goal loop inside a channel adapter.
- No Discord Interactions HTTP transport (M0 decision 2; parked).
- No Discord sharding (parked).
- No Slack Events API HTTP transport (M0 decision 3; parked).
- No Slack multi-workspace OAuth (M0 decision 5; parked).
- No multi-bot install per Allbert home for either provider.
- No rich media (image/file) upload or download.
- No voice channels (v0.48 voice modality).
- No proactive broadcast (no scheduled outbound messaging).
- No Telegram/email migration to plugin-owned settings fragments at
  v0.52 (decision 4; v0.52.x follow-on).
- No new operation or URI classes. Two v0.52 permission classes are in scope:
  the ADR 0056 inbound class (`:channel_message_inbound`) and the local
  canonical conversation mapping class (`:conversation_write`) used by M7
  `resume_thread_on_channel`.

## Development-Lane Annotations

Per ADR 0049 §6, every milestone carries a development-lane annotation.
All implementation milestones rejoin through the full release gate,
which must reproduce the v0.43 regression oracle green set. The `release`
lane uses provider stubs (fast/CI, exercises the real adapter + request-shape
code); the required `external-smoke` lane records real outbound/threading/echo
evidence before the v0.52 tag, and the operator guides carry the live
inbound/callback checks.

| M | Parallel workstreams | Serial barriers | Gate evidence + rejoin |
|---|---|---|---|
| M0 | Contracts: ADR 0016 amendment + ADR 0056 + **ADR 0057** plan acceptance; `:channel_message_inbound` permission registration (full spot set); transport-vehicle spike (decision 6); `Approval.Handoff` module; descriptor `primitives:` **and `threading:`** schema fields + registry validation; Telegram + email retroactive primitive + capability declarations; trusted channel-settings fragment bridge | `Plugin.Validator` schema validation; Settings Central reserved-namespace bridge; `Security.Policy`/`Security.Risk`/`Settings.Schema` permission registration; spike outcome recorded before M2/M3; existing v0.16 Telegram/email behavior byte-equivalent | focused: `Approval.Handoff`, registry-validator (`primitives:`+`threading:`), trusted bridge, `:channel_message_inbound` policy/floor/risk/settings, Telegram+email byte-equivalence; `mix compile --warnings-as-errors`; rejoin: ADRs plan-accepted + transport vehicle + threading capability set locked before M1 |
| M1 | Canonical thread substrate (ADR 0057): `thread_channel_refs` + `conversation_message_refs` + `cross_channel_identity_links` migrations with `owner_scope`, `receiver_account_ref`, and deterministic `provider_thread_key`; `Conversations.ChannelThread` module (normalize refs, lookup/link canonical ⇄ provider refs, echo-loop suppression via `direction`, `part_id` splitting); wired into `Runtime.submit_user_input/1` without changing `Conversations.resolve_thread/1` authority | migrations; `ChannelThread` API; runtime wiring; thread-authority invariant unchanged; `channel_events.thread_id` canonical-only | focused: mapping-table CRUD + uniqueness by owner/account/key, echo-suppression, part_id splitting, provider-thread-not-authority, runtime-wiring regression, channel_events canonical-only; lanes: db_serial; rejoin: substrate testable + green before any adapter consumes it |
| M2 | Discord plugin package (`Plugins.Discord` + modules + `Settings.Fragment` + manifest); `threading: :native_threads`; writes provider refs via `ChannelThread`; simulated Gateway tests; `mix allbert.channels discord`; `discord_doctor` | plugin discovery/registry; adapter supervisor child spec; transport vehicle per decision 6 | focused: Discord plugin load + descriptor validation + simulated Gateway event + thread-ref write + doctor + CLI; lanes: db_serial + global_process_serial; rejoin: Discord loads, simulated inbound flow green |
| M3 | Slack plugin package (`Plugins.Slack` + modules + `Settings.Fragment` + manifest); `threading: :native_threads`; writes provider refs via `ChannelThread`; simulated Socket Mode tests; `mix allbert.channels slack`; `slack_doctor` | plugin discovery/registry; adapter supervisor child spec; transport vehicle per decision 6 (proven envelope-ack) | focused: Slack plugin load + simulated Socket Mode event + thread-ref write + doctor + CLI; lanes: db_serial + global_process_serial; rejoin: Slack loads, simulated inbound flow green |
| M4 | identity mapping + guild/workspace/channel authorization + **explicit cross-channel identity links** + threaded replies per provider via `ChannelThread` | `Channels.Identity`; identity-link store; `channel_events` shape; runtime submission boundary | focused: identity-resolution, allowlist, identity-link no-auto-merge, threaded-reply (native placement) per provider; lanes: db_serial + global_process_serial; rejoin: identity + links + auth + reply green for both providers |
| M5 | confirmation callbacks through `Approval.Handoff.render/2` for Discord/Slack (`:button`); clicker re-authorization per tap; Telegram + email retroactive descriptor consumption | `Approval.Handoff` selection; `Confirmations.create/2`+`resolve/4`; callback custom_id invariants; clicker re-resolution | focused: confirmation callback per provider incl. `:typed_command` fallback, clicker-authorization, Telegram/email byte-equivalence, cross-provider isolation; lanes: db_serial; rejoin: confirmation flows green for all four channels |
| M6 | Retrofit existing channels (ADR 0057): Telegram, email, web/LiveView, CLI declare `threading:` and adopt `ChannelThread` + echo-suppression | byte-equivalent wire output preserved; web/CLI declare `:rich`; Telegram `:reply_chain`/`:native_threads`; email `:reply_chain` | focused: per-channel capability declaration + thread-ref write + echo-suppression + existing byte-equivalence regression (highest-regression milestone — changes shipped code); lanes: db_serial + liveview_serial; rejoin: all four existing surfaces green on the substrate |
| M7 | Unified history view (web + CLI) aggregated by canonical thread + `resume_thread_on_channel` action + degradation-ladder rendering | `Conversations` aggregation query; `Runtime.Redactor` on the view; resume requires same `user_id` and an explicit cross-channel identity link when source/target channel identities differ; degradation render strategy | focused: unified-view aggregation + redaction, resume-same-user authz, explicit-link-required regression, degradation rendering (native→reply_chain→flat); lanes: db_serial + liveview_serial; rejoin: unified view + resume green |
| M8 | security evals + closeout docs + version metadata + CHANGELOG + roadmap/vision/context-map; `mix allbert.test release.v052` deterministic gate; required real-provider smoke/manual validation; ADR 0016 amendment + ADR 0056 + ADR 0057 Status flips | `EvalInventory` security_eval lane; full release gate; required external smoke and manual live checks before tag; doc reconciliation | full v0.52 security_eval lane (provider stubs); focused v0.42/v0.43 regression; `mix allbert.test release`; `mix allbert.test release.v052`; **required** `mix allbert.test external-smoke -- discord_slack` (threaded/reply placement + echo-suppression) with recorded evidence plus operator live inbound/callback checks before tag; rejoin: docs match implementation; version → 0.52.0 |

## Implementation Order

### Milestone 0: Contracts — ADRs, Permission, Primitive + Threading Schema

Status: implemented. M0 added the permission class, shared handoff selector,
descriptor `primitives:` + `threading:` validation, trusted channel-settings
bridge, Telegram/email descriptor declarations, renderer delegation, and the
locked transport vehicle (`Req` REST + direct `websockex` WS).

Implemented tasks:

- Treat the ADR 0016 v0.52 amendment text (already authored at lines
  205-284), **ADR 0056 (Channel Inbound Trust Tier)**, and **ADR 0057
  (Cross-Channel Conversation Threading)** as plan-accepted implementation
  inputs in this plan and the request-flow doc. M8 closeout flipped all three
  ADR Status lines from `Proposed` to `Accepted` after implementation.
- Register the **`:channel_message_inbound`** permission class (ADR 0056, floor
  `:needs_confirmation`) at **every** spot the `:public_surface_call_inbound` /
  `:artifact_*` precedent touches: `Security.Policy` `@permission_settings`,
  `@default_decisions`, `permission()` type, `permission_classes/0`, a
  `safety_floor/2` clause, and the `reason/5` trace clauses; `Security.Risk`
  `tier/1` + `reasons/3`; `Settings.Schema` `@safe_write_keys`
  (`permissions.channel_message_inbound`) + the permission `defaults` enum
  (default `needs_confirmation`, allowed values `["needs_confirmation",
  "denied"]`). Half-wiring (e.g. omitting `@permission_settings`, `reason/5`, or
  the `Settings.Schema` entries) fails the settings/trace-reason tests.
- Run the **transport-vehicle spike** (M0 decision 6): selected raw `Req` REST
  plus direct `websockex` WS clients for Discord Gateway and Slack Socket Mode.
  Nostrum and `slack_elixir` are not part of the v0.52 baseline.
- Implement the provider-thread/session policy from M0 decision 7 in parser
  normalization and session derivation tests. Persist redacted
  `provider_thread_ref` metadata with channel events, but keep
  `channel_events.thread_id` canonical-only once M1 lands. **The durable
  canonical mapping tables (decision 8, ADR 0057) are built in M1**, not
  deferred.
- Create `AllbertAssist.Approval.Handoff` module at
  `apps/allbert_assist/lib/allbert_assist/approval/handoff.ex` with the
  `render/2` contract described in §"Approval Primitive Selection".
  Selection rule `:button > :typed_command > :link > :list`.
- Extend the channel descriptor schema with the `primitives:` field
  per §"Channel Descriptor Primitives Field" **and the `threading:` field
  (ADR 0057, decision 9)** — `:native_threads | :reply_chain | :flat | :rich`
  plus `can_create_thread` / `reply_key_type` / `quote_ttl_ms`. Add
  `Plugin.Validator` validation, surfaced through `Plugin.Registry`, that rejects
  channel descriptors missing `primitives:` (or empty/unknown/omitting `:list`)
  **or missing/invalid `threading:`**.
- Extend Settings Central's fragment composition with the trusted
  channel-settings bridge described in §"Channel Settings". Tests must prove
  Discord can contribute only `channels.discord.*`, Slack can contribute only
  `channels.slack.*`, and untrusted/home plugin metadata still cannot claim
  `channels.*`.
- Retroactively add `primitives: [:button, :typed_command, :list]` **and
  `threading: :reply_chain`** to the Telegram descriptor in
  `plugins/allbert.telegram/lib/allbert_assist/plugins/telegram.ex`
  and `primitives: [:typed_command, :list]` **and `threading: :reply_chain`** to
  the email descriptor in
  `plugins/allbert.email/lib/allbert_assist/plugins/email.ex` (full
  `ChannelThread` adoption + echo-suppression for these two is M6).
- Refactor `Channels.Telegram.Renderer.render_approval_handoff/2` and
  `Channels.Email.Renderer.render_approval_handoff/2` to consume
  `Approval.Handoff.render/2` output instead of hand-rolling the
  primitive choice. Wire-level behavior must remain byte-equivalent.

Acceptance criteria:

- ADR 0016 v0.52 amendment text exists in the §"v0.52 Amendment" section
  (already present); plan + request-flow record the implementation posture while
  keeping ADR 0016/0056/0057 Status `Proposed` until M8 closeout.
- Channel descriptors declare a valid `threading:` capability; the registry
  rejects a missing/invalid declaration (ADR 0057, decision 9).
- `Approval.Handoff.render/2` exists, selects the highest-fidelity
  primitive the descriptor declares, and returns
  `{primitive_kind, payload}` per the contract.
- `Plugin.Validator` / `Plugin.Registry` reject channel descriptors
  missing/empty/invalid `primitives:` or omitting `:list`, with a structured
  diagnostic.
- Settings Central accepts trusted source-tree channel plugin fragments for
  the provider's own `channels.<provider>.*` subtree and rejects all other
  reserved-channel claims.
- Telegram + email descriptors declare `primitives:` per the amendment
  table.
- Telegram renderer + email renderer produce byte-equivalent output
  before and after the refactor (regression captured by a snapshot
  test against existing v0.16 fixtures).

Automated tests:

- `Approval.Handoff.render/2` tests for each primitive kind, plus
  selection-rule tests (descriptor `[:button, :typed_command, :list]`
  picks `:button`; descriptor `[:typed_command, :list]` picks
  `:typed_command`; descriptor `[:link, :list]` picks `:link` only when a
  workspace URL is present and otherwise falls through to `:list`;
  descriptor `[:list]` picks `:list`).
- `Plugin.Validator` registry-discovery tests: missing field rejected; empty
  list rejected; unknown primitive rejected; missing `:list` rejected;
  valid set accepted.
- Provider-thread/session derivation tests: Slack thread-root sessions isolate
  two Slack threads in the same channel; Discord native thread channel sessions
  isolate from the parent channel; ordinary Discord `message_reference` affects
  reply routing but does not become an Allbert `thread_id`.
- Settings fragment bridge tests for accepted Discord/Slack subtrees,
  cross-provider namespace rejection, and untrusted reserved-namespace
  rejection.
- Telegram + email renderer byte-equivalence snapshot regression
  against existing v0.16 fixtures.

M0 verification:

- `mix test apps/allbert_assist/test/allbert_assist/approval/handoff_test.exs`
  — 5 tests, 0 failures.
- `mix test apps/allbert_assist/test/allbert_assist/security/channel_inbound_policy_test.exs apps/allbert_assist/test/allbert_assist/settings/public_surface_schema_test.exs apps/allbert_assist/test/allbert_assist/security/permission_gate_test.exs apps/allbert_assist/test/allbert_assist/plugin/validator_test.exs apps/allbert_assist/test/allbert_assist/settings_test.exs apps/allbert_assist/test/allbert_assist/channels_test.exs`
  — 96 tests, 0 failures.
- From `apps/allbert_assist`:
  `mix test ../../plugins/allbert.telegram/test/telegram/renderer_test.exs ../../plugins/allbert.email/test/email/renderer_test.exs`
  — 3 tests, 0 failures.
- `MIX_ENV=test mix compile --warnings-as-errors` — passed.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v052-m0-home
rm -rf "$ALLBERT_HOME"
mix run --no-start -e '
  IO.inspect(Code.ensure_loaded?(AllbertAssist.Approval.Handoff));
  IO.inspect(function_exported?(AllbertAssist.Approval.Handoff, :render, 2));
  IO.inspect(AllbertAssist.Plugin.Registry.registered_channels() |> Enum.map(&{&1.channel_id, &1[:primitives], &1[:threading]}))
'
```

Expected behavior:

- `Approval.Handoff` is loaded; `render/2` is exported.
- `Plugin.Registry.registered_channels/0` shows Telegram with
  `[:button, :typed_command, :list]`, `:reply_chain` and email with
  `[:typed_command, :list]`, `:reply_chain`.

Request-flow handoff:

- Update `v0.52-request-flow.md` §"Data Flow" with the primitive-
  selection boundary; §"Authority Invariants" with the descriptor
  field declaration rule.

### Milestone 1: Canonical Thread Mapping Substrate (ADR 0057)

Status: implemented. Built **before** the Discord/Slack adapters so each
adapter is native to it (no double build). M1 ships the durable tables,
`Conversations.ChannelThread`, and generic `Runtime.submit_user_input/1`
provider-ref hooks. M6 now consumes those hooks for Telegram/email/LiveView/CLI
retrofit.

Implemented tasks:

- Add three durable migrations:
  - `thread_channel_refs` (`owner_scope` string default `"local"`,
    `canonical_thread_id` FK → `conversation_threads.id`, `channel`,
    `receiver_account_ref`, deterministic `provider_thread_key`, redacted
    `provider_thread_ref` JSON/map, timestamps;
    `UNIQUE(owner_scope, channel, receiver_account_ref, provider_thread_key)`).
  - `conversation_message_refs` (`canonical_message_id` FK →
    `conversation_messages.id`, `canonical_thread_id`, `owner_scope`, `channel`,
    `receiver_account_ref`, `provider_message_id`, `part_id`, `direction`
    `in|out`, timestamps;
    `UNIQUE(owner_scope, channel, receiver_account_ref, provider_message_id,
    part_id)`).
  - `cross_channel_identity_links` (`owner_scope` string default `"local"`,
    `link_id`, `user_id`, `channel`, `receiver_account_ref`,
    `external_user_id`, timestamps;
    `UNIQUE(owner_scope, link_id, channel, receiver_account_ref,
    external_user_id)`).
- Create `AllbertAssist.Conversations.ChannelThread` with a normalized ref API:
  `normalize_ref/2` or equivalent channel-owned builder,
  `provider_thread_key/1`, `lookup_thread/1`, `link_thread/1`,
  `record_message_ref/1`, `echo?/1`, and `resolve_reply_target/2`. Every API
  call takes `owner_scope`, `channel`, `receiver_account_ref`,
  `provider_thread_key`, and redacted `provider_thread_ref` together; callers do
  not build database keys ad hoc. The module owns **no** conversation authority
  — `Conversations.resolve_thread/1` stays the sole thread authority for new
  threads.
- Wire `ChannelThread` into `Runtime.submit_user_input/1` / the channel response
  path so channel turns record provider refs (in/out) without changing thread
  selection for new threads. If `lookup_thread/1` finds an existing durable
  mapping, the adapter may pass that returned canonical Allbert `thread_id` into
  runtime; if not, it omits `thread_id`, runtime resolves/creates the thread,
  and the response's canonical id is linked afterward. Provider thread metadata
  is never `thread_id` authority (ADR 0056).
- Ensure `channel_events.thread_id` stores only canonical
  `conversation_threads.id` after M1. Provider ids remain in
  `provider_thread_ref` metadata and the ADR 0057 tables.

Acceptance criteria:

- A channel turn records a `thread_channel_refs` row and `conversation_message_refs`
  rows (in for inbound, out for the reply); the same canonical thread maps to
  distinct provider thread refs across channels.
- `thread_channel_refs` uniqueness is by `owner_scope + channel +
  receiver_account_ref + provider_thread_key`; two configured receiver accounts
  may carry the same provider ids without colliding. v0.52 uses
  `owner_scope: "local"` everywhere.
- Echo suppression: replaying Allbert's own outbound `provider_message_id` as an
  inbound event is dropped (no double-submit).
- A canonical message that splits into N native messages records N
  `conversation_message_refs` rows (distinct `part_id`).
- `Conversations.resolve_thread/1` behavior is unchanged (regression green);
  provider thread refs never alter thread selection unless they first resolve
  through `ChannelThread.lookup_thread/1` to an existing canonical Allbert id.

Automated tests:

- mapping-table CRUD + uniqueness by owner/account/key; `echo?/1`; `part_id`
  splitting; `resolve_reply_target/2`; runtime-wiring regression;
  provider-thread-not-authority; `channel_events.thread_id` canonical-only.

M1 verification:

- `MIX_ENV=test mix allbert.ecto.migrate --quiet` — applied the ADR 0057
  mapping-table migration to the test database.
- `mix test apps/allbert_assist/test/allbert_assist/conversations/channel_thread_test.exs`
  — 6 tests, 0 failures.
- `mix test apps/allbert_assist/test/allbert_assist/runtime_test.exs apps/allbert_assist/test/allbert_assist/conversations_test.exs apps/allbert_assist/test/allbert_assist/channels_test.exs`
  — 45 tests, 0 failures.
- `MIX_ENV=test mix compile --warnings-as-errors` — passed.
- M1 operator smoke:
  `MIX_ENV=test mix run --no-start -e 'IO.inspect(Code.ensure_loaded?(AllbertAssist.Conversations.ChannelThread)); IO.inspect(function_exported?(AllbertAssist.Conversations.ChannelThread, :echo?, 1)); IO.inspect(function_exported?(AllbertAssist.Conversations.ChannelThread, :record_message_ref, 1))'`
  — `true`, `true`, `true`.

Request-flow handoff:

- Document the canonical mapping tables + `ChannelThread` data flow in
  `v0.52-request-flow.md` §"Cross-Channel Conversation Threading".

### Milestone 2: Discord Plugin Skeleton

Status: implemented. Declares `threading: :native_threads`; writes provider
thread / message refs through `ChannelThread` (M1) from day one.

Implemented tasks:

- Create `./plugins/allbert.discord/` with:
  - `allbert_plugin.json` manifest (kind: channel, contributions.channels
    `["discord"]`, module `AllbertAssist.Plugins.Discord`,
    version `"0.52.0"`).
  - `AllbertAssist.Plugins.Discord` plugin module declaring `channels/0`
    returning the descriptor map with
    `primitives: [:button, :typed_command, :list]` and the standard
    descriptor fields (channel_id, provider, adapter, child_spec,
    secret_refs, summary_fields, settings_prefix, identity_map_key,
    session_strategy, plugin_id, source, status). The session strategy must
    include `guild_id_or_dm + channel_id_or_thread_channel_id +
    external_user_id`; ordinary `message_reference` is reply-routing metadata,
    not the Allbert conversation `thread_id`.
  - `AllbertAssist.Channels.Discord.Adapter` GenServer skeleton: stub
    Gateway lifecycle, in-memory event handling for simulated events,
    integration with `channel_events` for inbound dedupe.
  - `AllbertAssist.Channels.Discord.Client` skeleton: HTTP REST stub for
    `users.@me` and `chat.postMessage`-equivalent; WS Gateway client
    stub via a `Client.GatewayPort` behaviour with a `Stub`
    implementation for tests and a `Real` implementation for
    external-smoke.
  - `AllbertAssist.Channels.Discord.Renderer` consuming
    `Approval.Handoff.render/2`.
  - `AllbertAssist.Channels.Discord.Parser` normalizing simulated
    Gateway events, including a redacted `provider_thread_ref` with guild/DM,
    channel/thread-channel, inbound message id, and message reference when
    present.
  - `AllbertDiscord.Settings.Fragment` declaring `channels.discord.*`
    keys per §"Channel Settings".
- Wire the shipped plugin into
  `AllbertAssist.Plugin.Discovery.@shipped_modules`,
  `apps/allbert_assist/mix.exs` shipped plugin paths, the root test/precommit
  plugin coverage expected by this repo, and `mix allbert.test` roots.
- Register `discord_doctor` action (`:read_only`,
  `:channel_diagnostic`, `:internal`, contributed by the shipped Discord
  plugin) calling
  `Channels.Discord.Client.users_me/1` through the stub client; persists
  state to `<ALLBERT_HOME>/cache/channels/discord/doctor/state.json`.
- Implement the `mix allbert.channels discord` subcommand surface per
  §"Planned Interfaces" CLI. `set-token` accepts only `secret://` refs.

Acceptance criteria:

- `./plugins/allbert.discord/` discovered by `Plugin.Registry` at boot;
  descriptor validates including `primitives:` field.
- `discord_doctor` returns an ADR 0047-style redacted channel-doctor envelope; the stub client
  returns a deterministic `users.@me`-shaped response.
- `mix allbert.channels show discord` lists the configured descriptor +
  doctor result.
- Simulated inbound Gateway event for a bot-mention message reaches
  `Runtime.submit_user_input/1` via the Adapter+Parser path; the
  resulting response is rendered through `Channels.Discord.Renderer`
  and recorded in `channel_events`.
- Two Discord messages from the same user in different native thread channels
  produce distinct channel `session_id` values. An ordinary reply records
  `message_reference` for output placement without setting an internal Allbert
  `thread_id`.
- `mix allbert.channels discord simulate --guild G --channel C
  --user U "test prompt"` completes end-to-end on the stub client
  without external network.
- §Operator Validation M2 block in `v0.52-request-flow.md` runs
  end-to-end on a disposable `ALLBERT_HOME` without edits (v0.42 R4
  lesson).

Automated tests:

- Plugin discovery + descriptor validation.
- `Plugins.Discord.channels/0` returns the documented descriptor.
- Gateway stub client: simulated `READY`, `MESSAGE_CREATE`,
  `INTERACTION_CREATE` events.
- Adapter lifecycle: start under supervisor, handle simulated events,
  clean shutdown.
- `discord_doctor` with stub client: success path + failure path
  (token rejection, network unavailable).
- Settings fragment validation: required-when-enabled fields; raw token
  rejection for `set-token`.
- CLI: each subcommand under `mix allbert.channels discord *`.

M2 verification:

- `mix test apps/allbert_assist/test/allbert_assist/channels/discord_test.exs apps/allbert_assist/test/allbert_assist/actions/channels/discord_doctor_test.exs apps/allbert_assist/test/allbert_assist/channels_test.exs apps/allbert_assist/test/mix/tasks/allbert_channels_test.exs apps/allbert_assist/test/allbert_assist/plugin/validator_test.exs`
  — 34 tests, 0 failures.
- From `apps/allbert_assist`:
  `mix test ../../plugins/allbert.discord/test/discord/renderer_test.exs`
  — 2 tests, 0 failures.
- `mix allbert.test focused -- plugins/allbert.discord/test/discord/renderer_test.exs`
  — 2 tests, 0 failures.
- `MIX_ENV=test mix compile --warnings-as-errors` — passed.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v052-m2-home
rm -rf "$ALLBERT_HOME"
mix allbert.channels discord set-token secret://channels/discord/bot_token
mix allbert.channels discord set-application-id 123456
mix allbert.channels discord add-guild 987654321
mix allbert.channels discord map --external-user 11111 --user alice
mix allbert.settings set channels.discord.enabled true
mix allbert.channels discord doctor
mix allbert.channels show discord
mix allbert.channels discord simulate --guild 987654321 --channel 22222 --user 11111 "hello allbert"
if rg -n 'RAW_DISCORD_TOKEN|Authorization:|Bot [A-Za-z0-9._-]+' "$ALLBERT_HOME"; then
  echo "raw Discord credential leaked"
  exit 1
fi
```

Expected behavior:

- Bot token stored as `secret://channels/discord/bot_token`; raw token
  never appears in settings.
- `discord doctor` returns an ADR 0047-style redacted channel-doctor envelope
  (success against
  stub).
- `discord show` shows the descriptor + doctor result + identity map
  size.
- `discord simulate` resolves external user 11111 to local user alice
  and completes a runtime turn.
- The final raw-credential scan prints no matches. Key names such as
  `bot_token_ref` and values such as `secret://...` are allowed.

Request-flow handoff:

- Update `v0.52-request-flow.md` with Discord-specific inbound flow,
  CLI commands, and the discord_doctor invocation.

### Milestone 3: Slack Plugin Skeleton

Status: implemented. Declares `threading: :native_threads`; writes provider
thread / message refs through `ChannelThread` (M1).

Implemented tasks:

- Created `./plugins/allbert.slack/` with the same provider package shape as
  Discord (manifest + Plugin module + Adapter + Client + Renderer +
  Parser + Settings.Fragment). Adapter implements Socket Mode lifecycle
  via `Channels.Slack.Client.SocketModePort` (Stub for tests, Real for
  external-smoke). The plugin descriptor's session strategy must include
  `team_id + channel_id + (thread_ts || message ts) + external_user_id`; the
  parser records a redacted `provider_thread_ref` with team, channel, root
  timestamp, and inbound message timestamp.
- Wired the shipped plugin into
  `AllbertAssist.Plugin.Discovery.@shipped_modules`,
  `apps/allbert_assist/mix.exs` shipped plugin paths, the root test/precommit
  plugin coverage expected by this repo, and `mix allbert.test` roots.
- Registered `slack_doctor` action calling `Client.auth_test/1`;
  persists state to
  `<ALLBERT_HOME>/cache/channels/slack/doctor/state.json`.
- Implemented the `mix allbert.channels slack` subcommand surface. `set-token`
  and `set-app-token` accept only `secret://` refs.

Acceptance criteria:

- `./plugins/allbert.slack/` discovered by `Plugin.Registry`;
  descriptor validates.
- `slack_doctor` returns an ADR 0047-style redacted channel-doctor envelope; stub `auth.test` returns
  deterministic workspace + bot-user info.
- `mix allbert.channels show slack` lists the configured descriptor +
  doctor result.
- Simulated Socket Mode inbound event for an `app_mention` reaches
  `Runtime.submit_user_input/1`; response renders through
  `Channels.Slack.Renderer`.
- Two Slack threads in the same channel produce distinct channel `session_id`
  values from `team_id + channel_id + thread_root + external_user_id`; replies
  preserve `thread_ts` separately from Allbert's internal conversation
  `thread_id`.
- `mix allbert.channels slack simulate --channel C --user U "prompt"`
  completes end-to-end on stub.
- §Operator Validation M3 block runs end-to-end without edits.

Automated tests:

- Symmetric to M2 with Slack-specific event shapes (`hello`,
  `events_api` envelope, `interactive` envelope, `disconnect`).
- `slack_doctor` success + failure paths.
- Settings fragment validation: required-when-enabled fields; raw token
  rejection for `set-token` / `set-app-token`.
- CLI tests per subcommand.

Verification evidence:

- `MIX_ENV=test mix compile --warnings-as-errors`
- `mix test apps/allbert_assist/test/allbert_assist/channels/slack_test.exs apps/allbert_assist/test/allbert_assist/actions/channels/slack_doctor_test.exs apps/allbert_assist/test/allbert_assist/channels_test.exs apps/allbert_assist/test/mix/tasks/allbert_channels_test.exs apps/allbert_assist/test/allbert_assist/plugin/validator_test.exs`
- From `apps/allbert_assist`: `mix test ../../plugins/allbert.slack/test/slack/renderer_test.exs`
- `mix allbert.test focused -- plugins/allbert.slack/test/slack/renderer_test.exs`

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v052-m3-home
rm -rf "$ALLBERT_HOME"
mix allbert.channels slack set-token secret://channels/slack/bot_token
mix allbert.channels slack set-app-token secret://channels/slack/app_token
mix allbert.channels slack set-team-id T0123ABCDE
mix allbert.channels slack add-channel C0123ABCDE
mix allbert.channels slack map --external-user U0123ABCDE --user alice
mix allbert.settings set channels.slack.enabled true
mix allbert.channels slack doctor
mix allbert.channels show slack
mix allbert.channels slack simulate --channel C0123ABCDE --user U0123ABCDE "hello allbert"
if rg -n 'RAW_SLACK_BOT_TOKEN|RAW_SLACK_APP_TOKEN|xox[abp]-|Authorization:' "$ALLBERT_HOME"; then
  echo "raw Slack credential leaked"
  exit 1
fi
```

Expected behavior:

- Tokens stored as secret refs only.
- `slack doctor` returns an ADR 0047-style redacted channel-doctor envelope.
- `slack show` and `simulate` work end-to-end on stub.
- The final raw-credential scan prints no matches. Key names and `secret://`
  references are allowed.

Request-flow handoff:

- Update `v0.52-request-flow.md` with Slack-specific inbound flow.

### Milestone 4: Identity Mapping, Authorization, Cross-Channel Links, Threaded Replies

Status: implemented.

Implemented tasks:

- Wired `identity_map` lookups in both providers' adapter processing path:
  unmapped external users produce a `channel_events` rejected-unknown
  record; no runtime submission. Mapped users resolve to local
  `user_id` for `Runtime.submit_user_input/1`.
- Added **explicit cross-channel identity links** (ADR 0057, decision 11): an
  operator-configured store/CLI that declares several already-mapped per-channel
  identities are the same person, grouping them for the unified view + resume.
  Links are never auto-derived from display names/subjects; the per-channel
  `identity_map` remains the authentication gate. Tests assert no-auto-merge.
- Threaded replies use `ChannelThread` (M1) to place each reply in the provider's
  native thread (`:native_threads`): Slack `thread_ts`, Discord thread channel /
  `message_reference`.
- Wired `allowed_guild_ids` (Discord), `workspace_team_id` (Slack), and
  `allowed_channel_ids` (both) enforcement in the adapter processing path: messages
  from non-allowlisted surfaces produce a `channel_events` rejected record;
  no runtime submission.
- Implemented threaded reply behavior:
  - Discord: native Discord thread channels participate in channel
    `session_id` derivation; ordinary replies use `message_reference` to the
    inbound message id where the bot was mentioned. Neither value is an
    internal Allbert `thread_id`.
  - Slack: replies use `thread_ts` to the inbound message ts when the inbound
    message is in a thread; when a new Slack root message starts a channel
    session, the root message `ts` is the provider thread root. Neither value
    is an internal Allbert `thread_id`.
- Callback resolution remains deferred to M5. M4 keeps Discord/Slack
  callback events rejected as deferred; M5 owns the confirmation callback
  resolver and cross-provider pending isolation.

Acceptance criteria:

- Inbound message from unmapped external user produces
  `channel_events` rejected-unknown, no runtime submission, no reply.
- Inbound message from non-allowlisted guild/channel produces
  `channel_events` rejected, no runtime submission, no reply.
- Threaded replies preserve provider thread identity and session isolation
  (Discord thread channel / `message_reference`; Slack `thread_ts` or root
  message `ts`) without treating provider ids as Allbert `thread_id` values.
- §Operator Validation M4 block runs end-to-end without edits.

Automated tests:

- Identity resolution: mapped + unmapped paths per provider.
- Allowlist denial paths per provider (guild + channel for Discord;
  workspace + channel for Slack).
- Threaded reply assertions: outbound includes correct
  message_reference / thread_ts; channel session derivation isolates provider
  thread roots where applicable.
- Explicit identity-link CLI and no-auto-merge assertions.

Verification evidence:

- `MIX_ENV=test mix compile --warnings-as-errors`
- `mix test apps/allbert_assist/test/allbert_assist/conversations/channel_thread_test.exs apps/allbert_assist/test/allbert_assist/channels/discord_test.exs apps/allbert_assist/test/allbert_assist/channels/slack_test.exs apps/allbert_assist/test/mix/tasks/allbert_channels_test.exs`

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v052-m4-home
rm -rf "$ALLBERT_HOME"
# Discord fixture setup
mix allbert.channels discord set-token secret://channels/discord/bot_token
mix allbert.channels discord set-application-id 123456
mix allbert.channels discord add-guild 987654321
mix allbert.channels discord add-channel 22222
mix allbert.channels discord map --external-user 11111 --user alice
mix allbert.settings set channels.discord.enabled true
# Slack fixture setup
mix allbert.channels slack set-token secret://channels/slack/bot_token
mix allbert.channels slack set-app-token secret://channels/slack/app_token
mix allbert.channels slack set-team-id T0123ABCDE
mix allbert.channels slack add-channel C0123ABCDE
mix allbert.channels slack map --external-user U0123ABCDE --user alice
mix allbert.settings set channels.slack.enabled true
# Explicit cross-channel identity link
mix allbert.channels identity-links add --link link_alice --channel discord --receiver discord:app:123456:guild:987654321 --external-user 11111 --user alice
mix allbert.channels identity-links add --link link_alice --channel slack --receiver slack:team:T0123ABCDE --external-user U0123ABCDE --user alice
mix allbert.channels identity-links list --link link_alice
# Simulate inbound from UNMAPPED user → rejected
mix allbert.channels discord simulate --guild 987654321 --channel 22222 --user 99999 "from stranger"
# Simulate inbound from non-allowlisted channel → rejected
mix allbert.channels discord simulate --guild 987654321 --channel 33333 --user 11111 "from denied channel"
# Simulate threaded inbound -> reply preserves provider_thread_ref separately
# from Allbert thread_id
mix allbert.channels discord simulate --guild 987654321 --channel 22222 --thread-channel 44444 --user 11111 "thread test"
mix allbert.channels slack simulate --channel C0123ABCDE --thread-ts 1718040000.000100 --user U0123ABCDE "thread test"
mix run -e '
  import Ecto.Query
  alias AllbertAssist.{Repo, Channels.Event}
  Event
  |> order_by([event], desc: event.inserted_at)
  |> limit(10)
  |> Repo.all()
  |> IO.inspect()
'
```

Expected behavior:

- Stranger and denied-channel messages produce rejected `channel_events`
  records, no runtime submission, no reply.
- `identity-links list --link link_alice` shows exactly the operator-declared
  Discord and Slack external identities; no link is inferred from display names
  or a shared `user_id`.
- Threaded Discord message produces a reply in the native thread channel or
  with `message_reference` set for an ordinary reply. Threaded Slack message
  replies with `thread_ts` set. Both preserve provider-thread metadata without
  setting Allbert's internal `thread_id` from provider ids.

Request-flow handoff:

- Update `v0.52-request-flow.md` with the rejection paths, identity
  resolution semantics, and threaded reply behavior.

### Milestone 5: Confirmation Callbacks Through Primitive Selection

Status: implemented.

Implemented tasks:

- For each provider, wired the confirmation-rendering path through
  `Approval.Handoff.render/2`:
  - Discord: descriptor declares `[:button, :typed_command, :list]`;
    `render/2` selects `:button`; Renderer emits Discord message
    components with `custom_id: "allbert:v1:<verb>:<id>"`; INTERACTION_CREATE
    events parse the custom_id and resolve the pending through the
    registered confirmation action.
  - Slack: descriptor declares `[:button, :typed_command, :list]`;
    `render/2` selects `:button`; Renderer emits Block Kit `actions`
    block with `action_id: "allbert:v1:<verb>:<id>"`; interactive
    envelope parses action_id and resolves pending.
  - Both: fall back to `:typed_command` when buttons are disabled
    (`render_approval_buttons: false`), then to `:list` for the universal
    fallback path.
- Telegram + email retroactive: confirmed M0's primitive-extraction
  refactor still produces the same wire output through the new boundary
  (regression coverage from M0 carries forward).
- Confirmation callback verb invariants: only `approve`, `deny`, `show`
  are accepted; unknown verbs reject; callback id must match a pending
  confirmation owned by the resolved local user; cross-channel callbacks reject.
- Callback adapters perform a strict guard before invoking registered
  confirmation actions: parse `allbert:v1:<verb>:<id>`, load the pending
  confirmation, verify pending status, resolved local user, origin channel,
  and provider response target/thread where present, then call
  `Actions.Runner.run/3` with the existing confirmation action. This guard is
  required because the pre-v0.52 confirmation actions are not sufficient by
  themselves to enforce remote callback ownership for approve, deny, and show.
- Added exact typed-command parsing for Discord/Slack channel messages so
  `ALLBERT:APPROVE:<id>`, `ALLBERT:DENY:<id>`, and `ALLBERT:SHOW:<id>`
  resolve through the same guarded callback path before normal runtime
  submission.

Acceptance criteria:

- Discord button taps resolve the right pending confirmation;
  approve/deny propagate through the standard `Confirmations.resolve/4`
  path; `show` returns the pending detail to the originating channel.
- Slack button taps behave equivalently.
- Disabling `render_approval_buttons` for a provider falls back to
  `:typed_command` rendering; operator typing
  `ALLBERT:APPROVE:<id>` in a reply resolves the pending.
- Cross-provider isolation holds: a Discord-origin pending cannot be
  resolved by Slack and vice versa.
- Telegram + email approval flows are byte-equivalent to v0.16 wire
  output (regression).
- §Operator Validation M5 block runs end-to-end without edits.

Automated tests:

- One confirmation callback flow per provider (Discord + Slack)
  asserting button → INTERACTION_CREATE/interactive → resolve →
  side-effect rollback (since the action is stub-only).
- `:typed_command` fallback test per provider (with
  `render_approval_buttons: false`).
- `:list` fallback test (descriptor temporarily set to `[:list]`).
- Cross-provider isolation test.
- Telegram + email approval regression (carry forward from M0).
- Unknown verb / unknown id / wrong-user / wrong-channel rejection
  tests.

Verification evidence:

- `MIX_ENV=test mix compile --warnings-as-errors`
- `mix test apps/allbert_assist/test/allbert_assist/channels/discord_test.exs apps/allbert_assist/test/allbert_assist/channels/slack_test.exs apps/allbert_assist/test/allbert_assist/channels/telegram_test.exs apps/allbert_assist/test/allbert_assist/channels/email_test.exs`
- From `apps/allbert_assist`: `mix test ../../plugins/allbert.discord/test/discord/renderer_test.exs ../../plugins/allbert.slack/test/slack/renderer_test.exs`
- `mix allbert.test focused -- plugins/allbert.discord/test/discord/renderer_test.exs plugins/allbert.slack/test/slack/renderer_test.exs`

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v052-m5-home
rm -rf "$ALLBERT_HOME"
# Discord fixture setup
mix allbert.channels discord set-token secret://channels/discord/bot_token
mix allbert.channels discord set-application-id 123456
mix allbert.channels discord add-guild 987654321
mix allbert.channels discord add-channel 22222
mix allbert.channels discord map --external-user 11111 --user alice
mix allbert.settings set channels.discord.enabled true
# Slack fixture setup
mix allbert.channels slack set-token secret://channels/slack/bot_token
mix allbert.channels slack set-app-token secret://channels/slack/app_token
mix allbert.channels slack set-team-id T0123ABCDE
mix allbert.channels slack add-channel C0123ABCDE
mix allbert.channels slack map --external-user U0123ABCDE --user alice
mix allbert.settings set channels.slack.enabled true
# Simulate inbound that triggers a confirmation-required action
mix allbert.channels discord simulate --guild 987654321 --channel 22222 --user 11111 "delete that record"
# Inspect pending confirmations
mix allbert.confirmations list
# Simulate a Discord button-tap approve callback (via simulate-callback subcommand)
mix allbert.channels discord simulate-callback --user 11111 --custom-id allbert:v1:approve:CONFIRMATION_ID
mix allbert.confirmations list
# Same with Slack
mix allbert.channels slack simulate --channel C0123ABCDE --user U0123ABCDE "delete that record"
mix allbert.channels slack simulate-callback --user U0123ABCDE --action-id allbert:v1:deny:CONFIRMATION_ID
# Typed-command fallback also resolves through the guarded callback path
mix allbert.channels discord simulate --guild 987654321 --channel 22222 --user 11111 "ALLBERT:SHOW:CONFIRMATION_ID"
```

Expected behavior:

- Discord button-tap resolves pending to approved; effect runs (stub).
- Slack button-tap resolves pending to denied; effect does not run.
- Cross-provider attempts (Slack callback for Discord pending) reject
  cleanly.

Request-flow handoff:

- Update `v0.52-request-flow.md` with confirmation-callback flows per
  provider and the cross-channel isolation invariant.

### Milestone 6: Retrofit Existing Channels Onto The Substrate (ADR 0057)

Status: implemented. **Highest-regression milestone — it changes shipped code.**

Implementation tasks:

- Telegram and email now adopt `Conversations.ChannelThread` (M1): inbound
  turns pass `channel_thread_ref` + native `provider_message_id` into
  `Runtime.submit_user_input/1`, outbound replies record returned/generated
  provider message ids, and inbound echoes are suppressed before a second
  runtime submission.
- Telegram outbound replies set the native reply pointer with Bot API
  `reply_parameters` and preserve topic placement with `message_thread_id` when
  present. Telegram receiver scope is bot-token-ref hash + chat id so per-chat
  message ids cannot collide.
- Email outbound replies continue to set `In-Reply-To`/`References`, now with a
  generated outbound `Message-ID` passed into the SMTP formatter and recorded
  for echo suppression. Email provider thread roots derive from the first
  `References` id, then `In-Reply-To`, then the inbound `Message-ID`.
- Web/LiveView and one-shot CLI declare `:rich` through
  `AllbertAssist.Channels.LocalSurface` and write inbound refs through
  `ChannelThread`. They are **local surfaces, not transport plugins**, keeping
  v0.52 aligned with v0.56's planned first-class TUI/terminal channel.

Acceptance criteria:

- Existing Telegram + email wire output remains stable except for the planned
  reply-pointer placement and recorded refs.
- Each retrofitted surface records `thread_channel_refs` /
  `conversation_message_refs` rows; Telegram/email echo-suppression holds.

Automated tests:

- Focused gate passed:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/conversations/channel_thread_test.exs apps/allbert_assist/test/allbert_assist/channels/telegram_test.exs apps/allbert_assist/test/allbert_assist/channels/email_test.exs apps/allbert_assist/test/allbert_assist/channels_test.exs apps/allbert_assist/test/mix/tasks/allbert_ask_test.exs apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs`
  (core: 69 tests, 0 failures; web: 68 tests, 0 failures).

Request-flow handoff:

- Document the retrofit + per-channel threading capability in
  `v0.52-request-flow.md`.

### Milestone 7: Unified History View + Explicit Resume (ADR 0057)

Status: implemented.

Implemented tasks:

- Build a read-mostly **unified history view** (web panel + a `mix
  allbert.conversations` CLI surface) that aggregates a canonical thread's
  messages across channels, redacted via `Runtime.Redactor`, ordered by
  Allbert's own ingest sequence (never provider clocks). Implemented through
  `AllbertAssist.Conversations.UnifiedHistory`, `mix allbert.conversations
  show THREAD_ID`, and the workspace chat-pane Continuity strip.
- Add the `resume_thread_on_channel` registered action: continue a canonical
  thread on a different channel, reusing the same canonical thread id and
  requiring the same resolved local `user_id`. M7 adds the explicit local
  `:conversation_write` permission boundary for this mapping write. External
  channels require `receiver_account_ref`, `external_user_id`, and a provider
  thread key/ref that matches an existing explicit cross-channel identity link;
  local `:rich` surfaces (`cli`, `live_view`) can resume without an external
  identity link. No silent live mirroring; E2EE-origin content is never relayed
  without this explicit action.
- Render continuity through the degradation ladder when resuming onto a
  `:reply_chain` / `:flat` channel. The action response includes the resolved
  `reply_target` and continuity mode (`:native_thread`, `:reply_chain`,
  `:flat_stream`, or `:rich_surface`).

Acceptance criteria:

- The unified view shows one canonical thread's turns drawn from ≥2 channels,
  redacted, in ingest order.
- `resume_thread_on_channel` rejects a different `user_id`; a resumed turn lands
  in the same canonical thread and renders with the target channel's threading
  capability.

Automated tests:

- Unified-view aggregation + redaction; ingest-order ordering; resume-same-user
  authz; degradation rendering (`:native_threads` → `:reply_chain` → `:flat`).
- Focused gate passed:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/conversations/unified_history_test.exs apps/allbert_assist/test/mix/tasks/allbert_conversations_test.exs apps/allbert_assist/test/allbert_assist/security/permission_gate_test.exs apps/allbert_assist/test/allbert_assist/actions/registry_test.exs apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs`
  (core: 37 tests, 0 failures; web: 69 tests, 0 failures).

Request-flow handoff:

- Document the unified view + resume action in `v0.52-request-flow.md`.

### Milestone 8: Security Evals, Closeout

Status: implemented. M8 added the v0.52 security eval inventory/tests,
`release.v052`, the opt-in `external-smoke -- discord_slack` harness, version
metadata, release docs, ADR status flips, and drift reconciliation across the
roadmap, vision, future-features, context map, operator docs, developer docs,
security-hardening notes, and changelog.

Implementation tasks:

- Add the 28 v0.52 eval rows from `v0.52-request-flow.md` to
  `AllbertAssist.SecurityFixtures.EvalInventory` (milestone `:v052`)
  and the corresponding eval tests (provider-stub fixtures):
  - `discord-slack-spoofing-001`
  - `team-channel-replay-001`
  - `group-leakage-001`
  - `reply-body-command-injection-001`
  - `callback-scope-leakage-001`
  - `dm-vs-workspace-auth-001`
  - `discord-interactions-signature-verification-001` (parked path
    flagged: even though Interactions HTTP isn't shipped, the
    placeholder eval asserts the absent endpoint returns 404, not a
    silent accept)
  - `slack-request-signing-verification-001` (same shape for Events API
    placeholder)
  - `discord-guild-allowlist-001`
  - `slack-workspace-allowlist-001`
  - `slack-channel-allowlist-001`
  - `approval-primitive-honor-discord-001`
  - `approval-primitive-honor-slack-001`
  - `approval-primitive-honor-telegram-001` (retroactive)
  - `approval-primitive-honor-email-001` (retroactive)
  - `channel-descriptor-missing-primitives-rejected-001`
  - `bot-token-secret-redaction-discord-001`
  - `bot-token-secret-redaction-slack-001`
  - `channel-inbound-permission-floor-001` (the `:channel_message_inbound`
    class resolves with floor `:needs_confirmation` and cannot be lowered
    below it; ADR 0056)
  - `channel-inbound-permission-enforcement-001` (a mapped/allowlisted
    Discord or Slack message is rejected before runtime when
    `permissions.channel_message_inbound=denied`; ADR 0056)
  - `callback-clicker-authorization-001` (a button tap by an unmapped /
    non-allowlisted clicker is rejected; the clicker id is re-resolved per
    tap, not trusted from the payload)
  - `provider-thread-not-authority-001` (a provider thread ref never becomes an
    Allbert `thread_id` or grants permission; ADR 0057)
  - `owner-account-thread-key-isolation-001` (the same provider thread ids
    under different `receiver_account_ref` values do not collide; `owner_scope`
    is `"local"` and never a permission source)
  - `echo-loop-suppression-001` (replaying Allbert's own outbound
    `provider_message_id` as inbound is dropped, no double-submit)
  - `cross-channel-resume-same-user-001` (`resume_thread_on_channel` rejects a
    different `user_id`)
  - `threading-capability-missing-rejected-001` (a channel descriptor missing /
    declaring an invalid `threading:` is rejected by the registry)
  - `identity-link-no-auto-merge-001` (cross-channel identity links are never
    auto-derived from display names/subjects)
  - `unified-view-redaction-001` (the unified history view redacts via
    `Runtime.Redactor`; no secret leaks across the aggregated channels)
- Ship `mix allbert.test release.v052` deterministic gate (v0.42 R5
  pattern). No external network. Exercises the full v0.52 surface:
  M0 contracts + Approval.Handoff + `threading:` validation; M1 canonical
  mapping tables + `ChannelThread` + echo-suppression; M2 Discord + M3 Slack
  plugin load + doctor + simulated event + thread-ref write; M4 identity +
  links + allowlist + threaded reply per provider; M5 confirmation callback per
  provider including cross-channel isolation and `:typed_command` fallback; M6
  retrofit byte-equivalence; M7 unified view + resume; raw-credential redaction
  scan; `channel_events` redaction assertion. Evidence to
  `<ALLBERT_HOME>/release_evidence/v052/release-v052-<ts>.json`.
- Add `mix allbert.test external-smoke -- discord_slack` harness. This is a
  **required real-provider release input** before the v0.52 tag (not optional):
  real Discord bot + Slack workspace, real tokens, real Slack parent message +
  threaded reply, real Discord parent message + `message_reference` reply, and
  recorded echo-suppression metadata. It records evidence to
  `<ALLBERT_HOME>/release_evidence/v052/external-smoke-<ts>.json`. The harness is
  opt-in to *run locally* (it needs sandbox credentials), but passing it with
  recorded evidence is mandatory before tag. Live inbound Gateway/Socket Mode
  delivery, button approval, and unmapped-clicker rejection are explicit
  operator manual validation steps because the automated harness does not open a
  long-lived inbound provider session.
- Operator docs: `docs/operator/discord-channel.md` and
  `docs/operator/slack-channel.md` covering bot setup, token storage,
  identity mapping, allowlist configuration, doctor, troubleshooting,
  parked items (multi-workspace, Interactions HTTP, sharding). Discord doc
  documents the `MESSAGE_CONTENT` privileged-intent self-enable (and ≥100-guild
  caveat); Slack doc instructs installing as an **internal, non-Marketplace app**
  (single workspace) to avoid the 2025 distributed-app history/replies
  rate-limit reduction.
- Developer docs: `docs/developer/channel-approval-primitives.md`
  documenting the `Approval.Handoff` contract, descriptor field,
  per-adapter primitive subsets, fallback behavior, callback id
  invariants; and **`docs/developer/cross-channel-threading.md`** documenting
  the ADR 0057 construct (canonical thread id, the two mapping tables,
  `ChannelThread`, the `threading:` capability, the degradation ladder,
  echo-suppression, identity links, unified view, resume).
- Update security-hardening doc with the 28 v0.52 eval rows.
- Reconcile roadmap, future-features, vision, agent-context-map,
  and the ADR 0016 amendment + ADR 0056 + ADR 0057 Status flips.
- Bump version metadata; update README and CHANGELOG per the M8
  closeout discipline.

Acceptance criteria:

- All 28 evals pass on the stub fixtures.
- `mix allbert.test release.v052` runs green on a fresh checkout
  deterministically (no external network) — the fast/CI gate.
- **The required real-provider smoke passes** against a sandbox Discord bot +
  Slack workspace (real tokens, real outbound parent + threaded/reply placement,
  echo-suppression metadata) with recorded evidence. Live inbound
  Gateway/Socket Mode delivery, button approval, and unmapped-clicker rejection
  are verified manually before tag.
- Operator and developer guides exist and match the shipped action
  surface.
- The enumerated M8 closeout discipline (§"M8 Closeout Discipline"
  below) is complete; the closeout commit is single-purpose ("chore:
  ship v0.52 release" or equivalent).
- §Operator Validation M8 block runs end-to-end without edits and
  resolves to `mix allbert.test release.v052`.

Automated tests:

- The full v0.52 stub-provider eval suite.
- Existing v0.40 / v0.41 / v0.42 / v0.43 suites still pass (no
  regression).

Operator closeout test:

```sh
# Deterministic release smoke (primary validation):
mix allbert.test release.v052
# evidence: <ALLBERT_HOME>/release_evidence/v052/release-v052-<ts>.json

# Manual reproduction (debugging only):
export ALLBERT_HOME="$(mktemp -d /tmp/allbert-v052.XXXXXX)"
export ALLBERT_TRACE_ENABLED=true
mix allbert.channels discord set-token secret://channels/discord/bot_token
mix allbert.channels discord set-application-id 123456
mix allbert.channels discord add-guild 987654321
mix allbert.channels discord add-channel 22222
mix allbert.channels discord map --external-user 11111 --user alice
mix allbert.settings set channels.discord.enabled true
mix allbert.channels slack set-token secret://channels/slack/bot_token
mix allbert.channels slack set-app-token secret://channels/slack/app_token
mix allbert.channels slack set-team-id T0123ABCDE
mix allbert.channels slack add-channel C0123ABCDE
mix allbert.channels slack map --external-user U0123ABCDE --user alice
mix allbert.settings set channels.slack.enabled true
mix allbert.channels discord doctor
mix allbert.channels slack doctor
mix allbert.channels list
# Required real-provider smoke before tag. Needs a sandbox Discord bot,
# Slack workspace, and real tokens supplied through ALLBERT_SLACK_BOT_TOKEN,
# ALLBERT_SLACK_CHANNEL_ID, ALLBERT_DISCORD_BOT_TOKEN,
# ALLBERT_DISCORD_CHANNEL_ID, and optional ALLBERT_DISCORD_GUILD_ID.
mix allbert.test external-smoke -- discord_slack
# evidence: <ALLBERT_HOME>/release_evidence/v052/external-smoke-<ts>.json
```

Expected behavior:

- `release.v052` produces a reproducible JSON evidence artifact; all
  v0.52 surface coverage passes; raw-credential scan finds nothing (fast/CI
  gate).
- Doctors return ADR 0047-style redacted channel-doctor envelopes (stub or
  real).
- The required external smoke demonstrates real outbound parent +
  threaded/reply placement and echo-suppression metadata against the sandbox
  bot/workspace, with recorded evidence.
- Operator manual validation demonstrates real inbound @mention/DM delivery,
  button approval, and unmapped-clicker rejection per provider before tag.

#### M8R Transport-Ordering Remediation

The fourth implementation-readiness pass found that the shared v0.52 substrate
is sound but the live-provider transport contract still needs four small,
serial remediation milestones before tag. These are release-blocking unless the
scope is explicitly re-cut to exclude live inbound/callback validation.

| Milestone | Scope | Acceptance |
|---|---|---|
| M8R1 Discord interaction callback acknowledgement | Preserve the Gateway inbound path, but parse the interaction token, add a `POST /interactions/{interaction_id}/{interaction_token}/callback` client boundary, and send a deferred or immediate interaction response before confirmation resolution or other slow work. Follow-up result delivery may remain normal channel REST output, but the initial provider acknowledgement must be separate and first. | Focused Discord parser/client/adapter tests prove token capture, redacted callback request shape, callback-before-confirmation ordering, mapped clicker success, and unmapped/non-allowlisted rejection with no confirmation resolution. |
| M8R2 Discord Gateway RESUME | Keep `GatewayPort.Real` as the reviewed `websockex` boundary, but on reconnect after a stored `READY` session send opcode 6 `RESUME` with bot token, `session_id`, and last sequence. Handle opcode 7 `Reconnect`, opcode 9 `Invalid Session` with resumable boolean, and clear session state only when the session is not resumable. | Focused GatewayPort tests prove initial `IDENTIFY`, stored `READY` session, heartbeat sequence, op 7 reconnect, op 9 resumable resume, op 9 non-resumable identify path, and redacted state/log behavior. |
| M8R3 Slack ack-before-runtime ordering | Keep Socket Mode over `websockex`, but make the port return the exact `envelope_id` ack frame before dispatching the envelope to the adapter/runtime. Use an internal continuation so the owner receives the envelope after the ack callback returns. | Focused SocketModePort tests prove ack payload shape and ordering: no owner dispatch is observable until the ack frame has been returned; hello/disconnect handling remains unchanged. |
| M8R4 live-inbound evidence and docs drift | Add an env-gated `mix allbert.test external-smoke -- messaging_channel_inbound` harness for live messaging-channel sessions and clean stale plugin README wording so docs no longer say real network calls are stubbed. | `external-smoke list` includes the inbound harness. Plugin READMEs, operator docs, request-flow, changelog, and this plan agree that the harness proves Discord Gateway `READY`, Slack Socket Mode `hello`, and mapped @mention delivery for the current Discord/Slack provider pair, while DM delivery, button approve/deny, unmapped/non-allowlisted click rejection, and reconnect/RESUME remain exact manual evidence before tag. |

M8R gate order:

```sh
MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/discord_test.exs
MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/slack_test.exs
MIX_ENV=test mix test apps/allbert_assist/test/security/v052_channel_pack_eval_test.exs
MIX_ENV=test mix allbert.test release.v052
MIX_ENV=test mix allbert.test release
mix allbert.test external-smoke -- discord_slack
mix allbert.test external-smoke -- messaging_channel_inbound
```

The inbound smoke is now part of the final M8R gate set. The operator-guide DM,
button/clicker, and reconnect checklist remains a blocking pre-tag validation
input.

M8R progress:

- **M8R1 implemented.** Discord `INTERACTION_CREATE` parsing retains the
  provider interaction token only in memory, `Channels.Discord.Client` exposes a
  token-redacted interaction callback request boundary, and
  `Channels.Discord.Adapter` sends a deferred component acknowledgement before
  event insertion or confirmation resolution. Focused gate:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/discord_test.exs`
  passed with 13 tests and 0 failures. Drift gate:
  `MIX_ENV=test mix test apps/allbert_assist/test/security/v052_channel_pack_eval_test.exs`
  passed with 5 tests and 0 failures. Warning gate:
  `MIX_ENV=test mix compile --warnings-as-errors` passed.
- **M8R2 corrected.** `GatewayPort.Real` now marks stored `READY` sessions as
  resumable after disconnect/opcode 7/opcode 9 resumable invalid-session paths,
  closes the stored WebSockex transport instead of returning a local
  `{:close, state}` tuple, lets `handle_disconnect/2` own the reconnect
  decision, sends opcode 6 `RESUME` with token, session id, and sequence on the
  next Hello, and clears session state for non-resumable invalid sessions before
  returning to opcode 2 `IDENTIFY`. Focused gate:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/discord_test.exs`
  passed with 15 tests and 0 failures. Combined focused/drift gate:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/discord_test.exs apps/allbert_assist/test/security/v052_channel_pack_eval_test.exs`
  passed with 20 tests and 0 failures. Warning gate:
  `MIX_ENV=test mix compile --warnings-as-errors` passed.
- **M8R3 implemented.** `SocketModePort.Real` now returns the exact Slack
  `envelope_id` acknowledgement before dispatching the envelope to the owner;
  dispatch happens through a self-message continuation after the ack callback
  returns, preserving hello/non-envelope behavior. Focused gate:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/slack_test.exs`
  passed with 14 tests and 0 failures. Drift gate:
  `MIX_ENV=test mix test apps/allbert_assist/test/security/v052_channel_pack_eval_test.exs`
  passed with 5 tests and 0 failures. Warning gate:
  `MIX_ENV=test mix compile --warnings-as-errors` passed.
- **M8R4 implemented.** The generic `messaging_channel_inbound` external-smoke
  lane now opens real Discord Gateway and Slack Socket Mode sessions, waits for
  Discord `READY` and Slack `hello`, prints operator-send marker messages,
  waits for mapped provider-delivered @mentions to reach
  `Runtime.submit_user_input/1`, and writes
  `external-smoke-messaging-inbound-<ts>.json` evidence. Plugin READMEs now
  describe real WebSockex transports as implemented, and operator docs keep DM
  delivery, button approve/deny, unmapped/non-allowlisted click rejection, and
  reconnect/RESUME as manual pre-tag evidence. Focused gate:
  `MIX_ENV=test mix test apps/allbert_assist/test/mix/tasks/allbert_test_task_test.exs apps/allbert_assist/test/external/messaging_channel_inbound_smoke_test.exs`
  passed with 10 tests, 0 failures, and 1 skipped. Smoke-list gate:
  `mix allbert.test external-smoke list` listed `messaging_channel_inbound`.
  Full M8R focused gate:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/discord_test.exs apps/allbert_assist/test/allbert_assist/channels/slack_test.exs apps/allbert_assist/test/security/v052_channel_pack_eval_test.exs apps/allbert_assist/test/mix/tasks/allbert_test_task_test.exs apps/allbert_assist/test/external/messaging_channel_inbound_smoke_test.exs`
  passed with 44 tests, 0 failures, and 1 skipped. Warning gate:
  `MIX_ENV=test mix compile --warnings-as-errors` passed.

Request-flow handoff:

- Mark `v0.52-request-flow.md` Status as "implemented as `0.52.0`"; flip
  per-milestone Status fields; ADR 0016 amendment + ADR 0056 Status flips;
  roadmap pointer updated so v0.53 is the next milestone.

#### M8 Closeout Discipline

The v0.42 release-record drift (D1-D6) was fixed by `7712e3b chore:
sync v0.42 release record` (15 files, +101/-53) and v0.43 adopted the
same enumerated checklist in pass-3. v0.52 M8 bundles all 18 items into
the closeout commit so the same drift does not recur. Closeout is
incomplete until every box below is checked:

1. `mix.exs` version → `0.52.0`
2. `apps/allbert_assist/mix.exs` version → `0.52.0`
3. `apps/allbert_assist_web/mix.exs` version → `0.52.0`
4. `AllbertAssist.App.CoreApp.version/0` → `"0.52.0"`
5. `CHANGELOG.md` `## v0.52.0` section added with Added / Changed /
   Verification blocks citing all 28 evals, `release.v052` results, and
   the full `mix allbert.test release` gate counts (core + web +
   StockSage + plugin tests + Dialyzer)
6. `README.md` current implementation pointer and shipped-milestone list
   updated to v0.52.0, with the next-milestone pointer moved to v0.53.
7. `docs/plans/v0.52-plan.md` top status changes from the planned readiness
   wording to implemented wording.
8. All M0-M8 per-milestone status lines change from planned to implemented.
9. `docs/plans/v0.52-request-flow.md` line 3 records implementation as
   `0.52.0`; this is the final implemented request flow.
10. `docs/adr/0016-channel-adapter-boundary-and-identity-mapping.md`
    amendment Status is `Accepted for v0.52 Channel Pack 1 - Discord
    And Slack`; `docs/adr/0056-channel-inbound-trust-tier.md` Status is
    `Accepted at v0.52 M8`; and
    `docs/adr/0057-cross-channel-conversation-threading.md` Status is
    `Accepted at the v0.52 closeout milestone`
11. `docs/plans/roadmap.md` §v0.52 Status → `implemented as 0.52.0`
    with shipped-scope paragraph mirroring v0.42/v0.43 roadmap pattern
12. `docs/plans/allbert-jido-vision.md` §v0.52 paragraph updated to
    reflect shipped state
13. `docs/developer/agent-context-map.md` v0.52 row → "implemented"
    lifecycle, primitive infrastructure noted
14. Operator Discord guide (`docs/operator/discord-channel.md`) exists
    and matches the shipped action + CLI surface
15. Operator Slack guide (`docs/operator/slack-channel.md`) exists and
    matches the shipped action + CLI surface
16. Developer channel-approval-primitives guide
    (`docs/developer/channel-approval-primitives.md`) exists and
    documents `Approval.Handoff` + descriptor field + per-adapter
    subsets
17. Security-hardening doc (`docs/operator/security-hardening.md`)
    channel section lists all 28 v0.52 eval rows and the
    cross-provider isolation invariant
18. Future-features parking entries reconciled with shipped state
    (Telegram/email plugin-owned-settings migration parked as
    v0.52.x; Discord Interactions HTTP parked; Discord sharding
    parked; Slack Events API parked; Slack multi-workspace OAuth
    parked)

(Items 14-18 may overlap with M8 implementation tasks. The list is
checklist-shaped so M8 closure cannot ship without explicit
verification of each item.)

## Documentation Deliverables

- Operator Discord channel setup guide
  (`docs/operator/discord-channel.md`): bot creation, token storage,
  Gateway intents, guild + channel allowlist, identity mapping,
  doctor, troubleshooting, parked items.
- Operator Slack channel setup guide
  (`docs/operator/slack-channel.md`): bot creation, App-Level Token
  for Socket Mode, workspace + channel allowlist, identity mapping,
  doctor, troubleshooting, parked items.
- Developer channel approval-primitives guide
  (`docs/developer/channel-approval-primitives.md`): `Approval.Handoff`
  module, `render/2` contract, descriptor `primitives:` field,
  registry validation, per-adapter primitive subsets, fallback behavior,
  callback id invariants.
- Developer cross-channel threading guide
  (`docs/developer/cross-channel-threading.md`, ADR 0057): canonical thread id,
  `thread_channel_refs` + `conversation_message_refs` +
  `cross_channel_identity_links`, `owner_scope`, `receiver_account_ref`,
  `provider_thread_key`, `Conversations.ChannelThread`, the `threading:`
  capability + degradation ladder, echo-suppression, explicit identity links,
  the unified history view, and `resume_thread_on_channel`.
- Security-hardening updates
  (`docs/operator/security-hardening.md`): all 28 v0.52 eval rows,
  cross-provider isolation invariant, allowlist + identity mapping
  invariants, bot-token redaction invariants.
- ADR 0016 v0.52 amendment + ADR 0056 + ADR 0057 Status flips at M8 closeout.
- Agent context map (`docs/developer/agent-context-map.md`) v0.52 row
  updated.
- Future-features reconciliation: park multi-workspace Slack, Discord
  Interactions HTTP, Discord sharding, Slack Events API, Telegram/email
  plugin-owned-settings migration as v0.52.x or later.

## Definition Of Done

v0.52 is done when:

- Discord and Slack adapters simulate inbound messages, render replies
  and approvals, and preserve identity/thread mapping;
- ADR 0016 amendment is accepted with the four primitives and adapter
  declaration table; amendment Status flipped to `Accepted` at M8;
- ADR 0056 (Channel Inbound Trust Tier) is accepted and its Status flipped to
  `Accepted` at M8; the `:channel_message_inbound` permission class resolves
  through `Security.Policy` with floor `:needs_confirmation` (registered at every
  spot, not half-wired), `Security.Risk` explains its tier, and it cannot be
  lowered below the floor;
- a button tap re-resolves the clicker's identity per interaction and rejects
  unmapped/non-allowlisted clickers; the provider ack precedes runtime work and
  `channel_events` dedupe prevents double-submit/double-approve on retry or
  RESUME redelivery;
- `AllbertAssist.Approval.Handoff.render/2` exists, selects the
  highest-fidelity primitive the descriptor declares, and is consumed
  by all four channel renderers (Telegram, email, Discord, Slack);
- channel descriptors include the `primitives:` field; the channel
  registry rejects descriptors missing/empty/invalid declarations;
- Discord descriptor declares `[:button, :typed_command, :list]`; Slack
  same; Telegram retroactively `[:button, :typed_command, :list]`;
  email retroactively `[:typed_command, :list]`;
- `discord_doctor` and `slack_doctor` follow the ADR 0047 redaction policy
  and return redacted channel-doctor envelopes;
- `channels.discord.*` and `channels.slack.*` settings live in
  plugin-owned fragments per ADR 0031; bot tokens stored as
  `secret://` references; raw values never appear in traces, audits,
  settings output, or `channel_events`;
- guild/workspace/channel allowlists gate inbound messages before
  runtime submission; unmapped external identities produce
  `channel_events` rejected-unknown records, no runtime submission;
- threaded replies preserve provider thread identity (Discord
  message_reference; Slack thread_ts);
- **ADR 0057 construct (cross-channel threading):** the canonical thread id is
  the existing `conversation_threads.id`; `thread_channel_refs`,
  `conversation_message_refs`, and `cross_channel_identity_links` map it per
  channel using `owner_scope`, `receiver_account_ref`, and deterministic
  `provider_thread_key`; `Conversations.ChannelThread` records refs with
  `direction`/`part_id`; echo-loop suppression drops Allbert's
  own redelivered outbound; every channel descriptor declares a valid
  `threading:` capability (registry-enforced); Telegram/email/web/CLI are
  retrofitted onto the substrate with byte-equivalent existing output; the
  unified history view aggregates a canonical thread across channels (redacted,
  ingest-ordered); `resume_thread_on_channel` continues a thread on another
  channel only for the same `user_id`; explicit cross-channel identity links are
  operator-configured and never auto-merged; provider thread metadata is never
  `thread_id` authority;
- cross-provider confirmation isolation holds: a Slack callback cannot
  resolve a Discord-origin pending and vice versa;
- the v0.52 security eval set (stub-provider) passes and
  `mix allbert.test release.v052` runs green on a fresh checkout with
  reproducible JSON evidence (the fast/CI gate);
- the **required real-provider smoke** (`external-smoke -- discord_slack`)
  passes against a sandbox Discord bot + Slack workspace with recorded
  outbound/threading/echo evidence before the v0.52 tag, and operator manual
  validation covers live inbound delivery, button approval, and
  unmapped-clicker rejection before the tag;
- `docs/plans/v0.52-request-flow.md`, `docs/plans/roadmap.md` §v0.52,
  `docs/plans/allbert-jido-vision.md` §v0.52, ADR 0016 amendment Status,
  ADR 0056 Status, ADR 0057 Status, `docs/developer/agent-context-map.md`,
  `docs/operator/security-hardening.md`, operator Discord/Slack guides,
  developer channel-primitive guide, `docs/developer/cross-channel-threading.md`,
  and CHANGELOG reflect the shipped state;
- v0.44 Plan/Build summaries, v0.47 self-improvement trace mining, v0.53
  mobile channels, and v0.56 TUI inherit the v0.52 primitive + threading
  contracts without additional v0.52-scope work.
