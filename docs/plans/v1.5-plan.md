# Allbert v1.5.0 Spine Enablers

Enabler release. No flagship feature; this release changes the settings and
action spine that every later release builds on, and it is sequenced first for
that reason.

Constraining ADRs: **ADR 0046** (settings schema migration policy), **ADR 0065**
(central action param-contract enforcement). No new ADR — both decisions are
already made and this release executes them.

Companion: `docs/plans/v1.5-request-flow.md`.

Status: **planned — triad drafted 2026-07-30, readiness passes not yet run.**
No Locked Decision below is operator-signed.

## Build Progress

Not started.

## Purpose

Land the three spine changes that everything after 1.5 depends on, and take the
one large mechanical cleanup that shares their blast radius.

1. **A settings migration runner that actually runs.** ADR 0046 has been Accepted
   since v0.59, but the runner has been deferred every release because no
   migration was non-additive enough to force it. Three consumers are now
   queued behind it, and v1.6/v1.7 should not be the release that discovers the
   runner is still missing.
2. **Full cross-action param-contract enforcement.** v0.59 shipped a subset;
   the full v0.54 cross-action scope was never closed. Until it is, the contract
   is advisory in exactly the places nobody checks.
3. **PermissionGate deletion.** The gate is the thing param-contract enforcement
   replaces. Deleting it after enforcement is complete removes a parallel
   authority path that has to be kept in sync by hand.
4. **The action response envelope, consolidated.** Folded in deliberately — see
   the sequencing note below.

**Not in this release:** the preflight gate and the release-sequence rules
(AGENTS.md R1–R4) land in **v1.4 M8.5**, not here. v1.5 inherits them. What
remains for v1.5 on the testing side is the deeper problem preflight does not
solve — absolute suite duration. Preflight makes failures *cheap to find*; it
does not make the authoritative aggregate shorter. If the aggregate still peaks
near its recorded 47.7 minutes after v1.4, M5's test-hygiene work is where that
is attacked, informed by the metrics store rather than by impression.

## Sequencing Note (why the envelope work rides here)

Param-contract enforcement touches all 249 modules under
`apps/allbert_assist/lib/allbert_assist/actions/`. A 2026-07-29 duplication
survey found the same 249 modules carry roughly 4,000 lines of derivable
boilerplate:

| Hand-rolled helper | Modules defining it |
| --- | ---: |
| `defp denied/1` | 130 |
| `defp action/3` | 121 |
| standard four-key `output_schema` | 214 of 249 |
| `defp completed/2` | 56 |
| `defp error_message/…` | 34 |
| `defp denied_status/…` | 32 |
| `defp failed/…` | 30 |
| `defp error/2` | 22 |

`action/3` differs across sites only in the `name:` and `permission:` literals —
both already passed to `use AllbertAssist.Action` and stored as `capability/0`.
The macro (`action.ex:55`) generates no response helpers today.

Doing this in the same milestone as param-contract enforcement means one sweep
over 249 files instead of two, and the enforcement work needs to understand those
envelopes anyway. Held separately, the two would conflict on nearly every file.

Near-clone action families found in the same survey and folded in:
`channels/{slack,discord,telegram,matrix,email}_doctor.ex` (534 LOC, ~83%
identical) and `mcp/scan_{enable,pause,resume,run_once}.ex` (473 LOC, ~84%
identical).

The survey also flagged scattered primitives with **divergent semantics** —
`truthy?/1` with 50 definitions across 8 different truth sets, `json_safe/1` with
12 divergent implementations. Those are behaviour reconciliation, not refactor,
and each needs its own decision about which semantics are correct. They are
**not** folded in here as a refactor; M5 disposes of them explicitly.

### Folded surveys

Three 2026-07-29 surveys are absorbed into this release rather than living as
standalone analysis documents. All three recorded baseline `c7ee5a29`, which is
stale by the v1.3 Search subsystem, and **all three must be re-run at M0 before
any finding is acted on.**

The survey documents themselves are archived for historical verification:
[abstractions](../archives/simplification-opportunity-abstractions.md),
[dead code](../archives/simplification-opportunity-dead-code.md), and
[tests](../archives/simplification-opportunity-tests.md).

| Survey | Findings | Lands in |
| --- | --- | --- |
| Duplicate abstractions | Action envelope (~4,000 lines / 249 modules); channel-doctor and MCP-scan clone families; divergent `truthy?/1` and `json_safe/1`; triplicated `private_ip?/1` | M4, M5 — and the `private_ip?` copy goes to **v1.6 M4**, which owns `HttpPolicy` |
| Dead code | One dead module (`StockSage.Agents.DelegatePlugin`, superseded by `Specialist` which declares the identical signal route); 22 unreferenced public functions; one compiler-flagged unreachable clause at `memory/projection.ex` that did not reconcile with the source | M5 |
| Tests that earn nothing | 53 eval-inventory self-check tests (51 touching no production module) asserting a compile-time literal has atoms in atom fields; 11 near-identical "rows encode concrete pass criteria" tests; the v0.60/v0.60b doc-string sweeps (535 LOC asserting Markdown headings in frozen design docs) | M5 |

The test finding matters beyond tidiness: those tests emit `IO.puts` evidence
rows reading `status=pass` for boundaries they never exercise. That is negative
value, not zero — the gate cannot distinguish them from real proof.

The [dead-code survey](../archives/simplification-opportunity-dead-code.md)
carries an analyzer defect log recording four wrong answers it produced before
the final list, and one proven false positive
(`Paths.search_projection_root/0`, live since v1.3). **Carry that log forward
when re-running**; it is the most reusable part of the survey and it prevents
repeating the same four mistakes.

## Expected Inputs

- v1.4 shipped and tagged.
- The 2026-07-29 duplication survey, re-run at this release's baseline — its
  recorded baseline (`c7ee5a29`) is stale by the v1.3 Search subsystem and had at
  least one proven false positive.

## Current Code State To Re-Verify At Build Start

- `AllbertAssist.Settings.VersionContract` and the ADR 0046 additive-only
  enforcement as narrowed at v1.3 M9.b (annotation-only `deprecated?: true` plus
  non-empty reason is additive; type, default, validation, writability, safety,
  incomplete annotations, and later removal are not).
- `AllbertAssist.Security.PermissionGate` (111 lines) and every caller.
- The v0.59-shipped param-contract scope versus the full v0.54 cross-action
  scope — the backlog entry carries a `Verify:` marker for exactly this diff.
- `AllbertAssist.Action.__using__/1` and `capability/0`.
- `intent.*model_profile` legacy settings keys and their readers.
- Telegram/Email plugin-owned settings shapes.

## Locked Decisions

Numbering is local to this plan. None is operator-signed yet.

1. **The migration runner ships unconditionally**, not "when a migration needs
   it". Three consumers are queued and the deferral has already cost four
   releases.
2. **Runner before consumers.** The telegram/email migration, the legacy
   `intent.*model_profile` removal, and automated rollback are all runner
   consumers and land after it, in that order.
3. **Automated rollback is part of the runner milestone**, not a follow-on. A
   migration runner without rollback is a one-way door.
4. **Param-contract enforcement completes the v0.54 scope**, verified by diff
   against what v0.59 actually shipped rather than by assumption.
5. **PermissionGate deletion happens after enforcement is complete**, never
   alongside it.
6. **The envelope consolidation is generated by the existing macro**, with
   `defoverridable` for genuine outliers. No new abstraction layer, no new
   module.
7. **Envelope consolidation is behaviour-preserving.** Any site whose generated
   envelope would differ from its hand-rolled one is an outlier and keeps its
   override, recorded with the reason.
8. **Divergent primitives are not silently unified.** `truthy?/1` and
   `json_safe/1` each get an explicit decision about correct semantics before any
   consolidation, or are deferred with the reason recorded.
9. **This release is additive at the public contract.** `release.v1` stays green;
   Tier-1 shapes are untouched.

## Operator-Visible Win

Mostly invisible, and that is the point — this is the release that makes the next
three possible. The one visible change: settings migrations become a supported,
reversible operation instead of a thing the operator is warned about.

## Planned Interfaces And Storage Shape

- `Settings.MigrationRunner` — ordered, resumable, journaled, reversible.
- Migration journal under Allbert Home, included in backup.
- No new database. No new permission class. No new settings namespace beyond
  migration bookkeeping.

## Code Seams

- **Extended:** `AllbertAssist.Action.__using__/1` (generates response helpers
  from capability metadata), `Settings.VersionContract`, `Settings.Schema`.
- **New:** `Settings.MigrationRunner`, `Settings.Migration` behaviour, migration
  journal.
- **Deleted:** `Security.PermissionGate` and its parallel path.
- **Touched broadly:** all 249 action modules, once.

## Development Lane Annotation

| Milestone | Lane |
| --- | --- |
| M1–M2 | `app_env_serial`, `home_fs_serial` |
| M3–M4 | `pure_async` where possible — envelope work is mostly pure |
| M5 | `pure_async` |
| M6–M7 | `security_eval_serial`, gate |

### Focused command matrix

```
mix allbert.test focused --only settings_migration
mix allbert.test focused --only param_contract
mix allbert.test focused --only action_envelope
mix allbert.test release.v15
```

## Security Posture

PermissionGate deletion removes an authority path. That is the highest-risk
change in this release and is gated accordingly:

- enforcement must be provably complete before deletion begins;
- every former gate call site is proven to resolve through the param contract
  with an identical decision, red-first;
- the deletion milestone carries adversarial rows attempting to reach an action
  through a path the gate previously covered.

The envelope consolidation must not change any `permission_decision`,
`response_status`, or denial reason. Any difference is a defect, not an
improvement.

## Non-Goals

OAuth, MCP parity, network hardening (all v1.6). Knowledge (v1.7/v1.8). Any new
feature. Any Tier-1 contract change. Dead-code removal beyond what the envelope
sweep naturally subsumes — the dead-code survey needs re-running first and is
tracked separately.

## Milestones

Modes: `[gate]`, `[fv]`.

- **M0: Recon verify + baseline.** `[gate]` Drift table; the v0.54-versus-v0.59
  param-contract diff resolved; PermissionGate caller census; duplication survey
  re-run at this baseline with its analyzer defect log applied.
  *Acceptance:* tables committed before M1; the survey's stale baseline replaced.
- **M1: Migration runner + rollback.** `[gate]` Ordered, resumable, journaled,
  reversible runner; `Settings.Migration` behaviour; journal in backup.
  *Acceptance:* a migration applies, rolls back, and re-applies idempotently;
  interrupted migration resumes; `release.v1` green.
- **M2: Runner consumers.** `[gate]` Telegram/email plugin-owned settings
  migration; legacy `intent.*model_profile` removal. *Acceptance:* both migrate
  and roll back; no operator setting is silently rewritten; stored values survive
  round-trip.
- **M3: Param-contract enforcement.** `[gate]` The full v0.54 cross-action scope.
  *Acceptance:* every action's params are contract-checked; the v0.59 subset is
  proven to be a strict subset of what now runs.
- **M4: Action envelope consolidation.** `[gate]` Macro generates `denied/1`,
  `error/2`, `action/3`, the standard `output_schema`, the confirmation-summary
  helper, and the request-context normalizer from capability metadata;
  `defoverridable` outliers recorded with reasons; channel-doctor and MCP-scan
  families collapsed. *Acceptance:* behaviour-preserving — no
  `permission_decision`, `response_status`, or denial reason changes anywhere;
  LOC delta recorded.
- **M5: Divergent primitives, dead code, and test hygiene.** `[gate]` Explicit
  decision on `truthy?/1` (8 truth sets; `Runtime` accepts integer `1`,
  `Security.Policy` does not) and `json_safe/1` (12 implementations; at least
  two can emit non-JSON-encodable output). Dead-module and dead-function removal
  from the re-run survey. Reconcile the `memory/projection.ex` compiler warning —
  it may be a stale artifact, a diagnostic offset, or a real defect in
  `Forget.suppressed_value?/2`'s contract, and deleting the clause without
  deciding which would paper over an error path. Retire the eval-inventory
  shape-only tests and their false `status=pass` evidence rows, and the
  v0.60/v0.60b doc-string sweeps. *Acceptance:* each divergent primitive is
  unified with a recorded correct-semantics decision or deferred with a recorded
  reason — no silent unification; every deletion is verified against the re-run
  survey, not the stale baseline; no evidence row survives that does not exercise
  a boundary.
- **M6: PermissionGate deletion.** `[gate]` Deletion plus the adversarial
  reachability rows. *Acceptance:* every former call site resolves identically
  through the param contract, proven red-first; no path reaches an action that
  the gate previously covered.
- **M7: Eval rows + `release.v15` gate.** `[gate]` `:v15` enum first; gate
  composed as the exact `release.v14` prefix plus focused spine steps.
  *Acceptance:* prefix proof passes; gate green first roll.
- **M8: Dependency refresh (standing).** `[gate]`
- **M9: Docs, docs-gate coverage, RC, FV, binary release, closeout.**
  `[gate]`+`[fv]` Operator settings-migration guide; **the docs-gate coverage and
  index audit from Documentation Deliverables — 18 unmarked ADR index rows read
  and judged, and archives either brought into the checked set or explicitly
  dispositioned**; CHANGELOG; RC gates; one uninterrupted authoritative gate at
  the pushed candidate SHA; operator-held tag `v1.5.0`; archive and roadmap
  administration. *Acceptance:* every ADR index row carries a status that matches
  its ADR header, and no documentation directory is unchecked by accident — each
  is either gated or carries a recorded decision that it is not.

## Validation Design (churn guardrails, binding)

- Migration rollback is proven on a real settings tree with operator-authored
  values, not a synthetic fixture.
- PermissionGate deletion rows are red-first: each must fail before deletion and
  pass after, proving the param contract genuinely covers the path.
- Envelope consolidation is proven behaviour-preserving by comparing full
  response envelopes before and after across a representative action sample,
  including denial and error paths — not just the happy path.

## Documentation Deliverables

- `docs/operator/settings-version.md` — migration runner, rollback, what an
  operator sees.
- `docs/developer/test-strategy.md` — `release.v15` composition, lanes.
- ADR 0046 records the runner shipping; ADR 0065 records enforcement completion.
- **Docs-gate coverage and index audit (M9, explicit scope).** The v1.3 M9.b.2
  docs gate checks less than it appears to. Two concrete gaps, both to be closed
  or explicitly dispositioned in this release:

  1. **18 of 28 ADR index rows carry no status marker.**
     `docs_check_adr_index_statuses` only validates rows that have one, so those
     18 are silently unchecked. This cannot be fixed by extraction: several ADRs
     carry multi-stage histories whose first status word is not their current
     status — ADR 0046 opens "Proposed (draft begins during v0.45…)" but was
     Accepted at v0.59 and re-scoped since. Writing extracted first-words into an
     index the gate then enforces would lock in wrong statuses. Each of the 18
     needs reading and a judgement.
  2. **Whole directories are outside the gate's view.**
     `@docs_active_index_dirs` covers `docs/operator`, `docs/developer`, and
     `docs/design` only; the explicit file list adds the root, `docs/`, and
     `docs/adr/` READMEs plus active plan files. `docs/archives/README.md` is in
     neither, which is how two orphaned validation screenshots and a filename
     typo (`version-10-planning-02.md`) survived unnoticed until a manual review
     on 2026-07-30. Either bring archives into the checked set, or record the
     decision that frozen history is deliberately unchecked — but do not leave it
     an accident of the directory list.

  Landing here because ADR 0046 and ADR 0065 are already being touched, and
  because M5 removes evidence rows that the gate currently cannot distinguish
  from real proof — the same class of problem.
- `CHANGELOG.md`.

## Definition Of Done

- The migration runner applies, rolls back, resumes, and journals; three
  consumers migrated with no silent rewrite of operator values.
- Param-contract enforcement covers the full v0.54 cross-action scope, with the
  v0.59 subset proven strictly contained.
- PermissionGate is deleted and no path reaches an action it previously covered,
  proven red-first.
- The action envelope is generated from capability metadata across 249 modules
  with zero behaviour change and a recorded LOC delta; outliers carry recorded
  reasons.
- `truthy?/1` and `json_safe/1` each have a recorded semantics decision.
- `release.v15` composes the exact `release.v14` prefix and is green at one clean
  pushed SHA; `v1.5.0` is built, signed, published, tap-installed, and
  revalidated.
