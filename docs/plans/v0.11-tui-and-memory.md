# v0.11 — TUI and adaptive memory

Status: Proposed

## Context

v0.10 shipped the provider expansion and made fresh profiles local-first with Ollama/Gemma4. The next operator-facing gap is not self-improvement yet; it is visibility and memory confidence during normal use.

The current REPL is reliable, but it is intentionally plain: it streams assistant text and local slash-command output without a persistent view of model posture, context pressure, token usage, spend, memory state, active skills, or pending approvals. At the same time, the curated-memory system from v0.5 has the right safety model, but it is too passive: the `memory-curator` skill is available, but the runtime does not make memory curation feel like an always-nearby part of the assistant's working loop.

v0.11 therefore ships two related improvements before self-improvement:

1. A TUI operator surface that makes Allbert's runtime state legible while preserving the existing classic REPL fallback.
2. Adaptive memory routing and recall enhancements that keep Allbert markdown-first and review-first while making memory more reliably available.

This moves the previously planned self-improvement release to v0.12. That sequencing is deliberate: before Allbert authors code and patches itself, the operator should have a better terminal surface, stronger telemetry, and a clearer memory posture.

## Research checkpoint

| Topic | Decision |
| --- | --- |
| Codex-style TUI | Use a split between daemon/protocol event handling and terminal rendering. The TUI is an adapter over existing daemon sessions, not a second runtime. |
| Claude Code-style status line | Add native session telemetry and configurable status-line items. v0.11 does not run arbitrary status-line shell commands; status data comes from the daemon protocol. |
| MemPalace | Add searchable session-episode recall for verbatim or near-verbatim past working context, but do not promote chat history into durable memory. |
| Zep / Graphiti | Add provenance-aware temporal fact metadata and supersession semantics for staged/promoted facts. Keep review gates before durable promotion. |
| Mem0 graph memory | Treat entity/relation extraction as a structured overlay on top of markdown memory, not as the source of truth. |
| Letta memory model | Preserve a small always-visible memory synopsis and searchable deeper memory; improve routing rather than stuffing every memory surface into every prompt. |

Sources:

- [Codex TUI overview](https://deepwiki.com/openai/codex/4.1-project-structure)
- [Claude Code status line](https://code.claude.com/docs/en/statusline)
- [MemPalace](https://github.com/mempalace/mempalace)
- [Graphiti](https://github.com/getzep/graphiti)
- [Zep graph docs](https://help.getzep.com/v2/understanding-the-graph)
- [Mem0 graph memory](https://docs.mem0.ai/open-source/features/graph-memory)
- [Letta archival memory](https://docs.letta.com/guides/core-concepts/memory/archival-memory)

## Goal

Ship an end-user-ready release with:

1. A Ratatui/Crossterm TUI as the default interactive surface for fresh profiles.
2. The existing Reedline REPL preserved as `classic` fallback and upgrade default.
3. Kernel-owned telemetry exposed through the daemon protocol.
4. Configurable status-line items for model, context, tokens, cost, memory, intent, skills, inbox, channel, and trace.
5. Configurable memory routing where `memory-curator` is always eligible but not always active.
6. Searchable session-episode recall that never bypasses the durable-memory staging/promotion contract.
7. Temporal fact metadata for staged/promoted fact memory.
8. Optional semantic/hybrid retrieval behind an off-by-default config flag.
9. Operator docs and upgrade notes that make the new defaults and fallback path clear.

## Non-goals

- No self-modifying Rust rebuild skill in v0.11; that is v0.12.
- No skill-authoring skill in v0.11; that is v0.12.
- No embedded Lua scripting engine in v0.11; that is v0.12.
- No arbitrary shell command status-line renderer.
- No hosted sync, hosted web UI, or cross-network daemon.
- No vendored third-party memory system.
- No hidden durable transcript memory.
- No automatic promotion of staged facts into approved durable memory.
- No semantic retrieval enabled by default.

## Design decisions

### TUI posture

- `repl.ui = "tui"` is the fresh-profile default.
- Existing profiles that lack the key migrate to `repl.ui = "classic"` to avoid surprising operators on upgrade.
- `allbert-cli` with no subcommand launches the configured UI.
- `allbert-cli repl --classic` and `repl.ui = "classic"` keep the current Reedline behavior available.
- The TUI attaches to the daemon via the same local IPC and session model as the current REPL.
- The TUI never boots a private kernel.
- The TUI renderer owns layout, focus, input editing, modals, transcript display, and status-line rendering.
- The daemon/kernel own session state, tools, memory, cost, model usage, approvals, and telemetry.

### Telemetry posture

Telemetry is runtime state, not terminal decoration. v0.11 adds a protocol payload that can be consumed by the TUI, classic REPL slash commands, tests, and future frontends.

The telemetry payload includes:

- session id, channel, turn state, and current agent stack
- provider, model id, API-key state, max output tokens, and optional known context window
- latest model usage and cumulative session usage
- latest context percentage when a context window is known
- session cost, today's cost, daily cap, and active turn budget
- memory synopsis bytes, ephemeral bytes, prefetch hit count, staged count, indexed durable docs, episode count, and fact count
- active skills, always-eligible skills, and last resolved intent
- pending inbox count and trace state

Context percentages use provider-reported usage from the latest response only. v0.11 does not ship tokenizer-estimated preflight context accounting.

### Status-line posture

The TUI status line is native and configurable:

```toml
[repl.tui.status_line]
enabled = true
items = ["model", "context", "tokens", "cost", "memory", "intent", "skills", "inbox", "channel", "trace"]
```

Supported items:

- `model`: provider/model
- `context`: latest input+output tokens over known context window, or `ctx ?` when unknown
- `tokens`: latest and session cumulative tokens
- `cost`: session and today spend
- `memory`: durable/staged/episode/fact counts and synopsis pressure
- `intent`: last resolved intent
- `skills`: active and always-eligible skill count
- `inbox`: pending approval count
- `channel`: attached channel/session
- `trace`: trace on/off

The `/statusline` slash command changes the active session view immediately and can persist changes to config when called with `--save`.

### Memory routing posture

The new default is `always_eligible`, not `always_active`.

```toml
[memory.routing]
mode = "always_eligible"
always_eligible_skills = ["memory-curator"]
auto_activate_intents = ["memory_query"]
auto_activate_cues = ["remember", "what do you remember", "review staged", "promote that", "forget"]
```

Rules:

- Always-eligible skills are surfaced in the manifest and relevant-skill hints every root turn.
- Their full skill bodies are not loaded unless routing policy activates them.
- `memory-curator` auto-activates for `memory_query` intent and explicit memory-review cues.
- Operators can change routing through TOML, setup, or `allbert-cli memory routing show|set`.
- Runtime-generated `~/.allbert/AGENTS.md` remains generated product state; it reflects active routing policy but is not the source of routing truth.

### Episode recall posture

Session journals are indexed as `episode` recall material. This lets Allbert answer questions like "what did we decide in that debugging session?" without pretending transcripts are durable learned memory.

Rules:

- Episode recall is derived from `~/.allbert/sessions/*/turns.md`.
- Episode search is explicit (`tier = "episode"`) unless `memory.episodes.prefetch_enabled = true`.
- The default auto-prefetch path excludes episodes.
- Episode hits are marked as working history, not approved memory.
- Any learning extracted from an episode still goes to staging before durable promotion.

### Temporal fact posture

Facts are a structured overlay over staged/promoted markdown memory:

```yaml
facts:
  - subject: "project storage"
    predicate: "uses"
    object: "Postgres"
    valid_from: "2026-04-24T00:00:00Z"
    valid_until: null
    source:
      kind: "staged_memory"
      id: "stg_..."
    supersedes: []
```

Rules:

- Fact metadata can appear on staged entries and promoted durable notes.
- Fact metadata is indexed for `tier = "fact"` search.
- Facts do not become durable merely by being extracted.
- Superseded facts remain auditable and are filtered from default fact summaries unless explicitly requested.
- The markdown file remains the source of truth.

### Semantic retrieval posture

BM25/Tantivy remains the default retrieval engine. v0.11 may add optional semantic retrieval as a derived index:

```toml
[memory.semantic]
enabled = false
provider = "none"
embedding_model = ""
hybrid_weight = 0.35
```

Rules:

- When disabled, no embedding calls are made and no vector index is required.
- When enabled, semantic hits are fused with BM25 using deterministic scoring.
- The semantic index is derived and rebuildable.
- Provider-free tests use a fake deterministic embedding provider.

## Public interfaces

### Config

Add:

```toml
[repl]
ui = "tui" # fresh default; migrated existing profiles use "classic"
show_inbox_on_attach = true

[repl.tui]
enabled = true
mouse = true
max_transcript_events = 1000

[repl.tui.status_line]
enabled = true
items = ["model", "context", "tokens", "cost", "memory", "intent", "skills", "inbox", "channel", "trace"]

[memory.routing]
mode = "always_eligible"
always_eligible_skills = ["memory-curator"]
auto_activate_intents = ["memory_query"]
auto_activate_cues = ["remember", "what do you remember", "review staged", "promote that", "forget"]

[memory.episodes]
enabled = true
prefetch_enabled = false
max_episode_hits = 5

[memory.facts]
enabled = true
max_facts_per_entry = 12

[memory.semantic]
enabled = false
provider = "none"
embedding_model = ""
hybrid_weight = 0.35
```

Extend:

```toml
[model]
context_window_tokens = 0 # 0 means unknown
```

### CLI and REPL commands

- `/statusline`: show or change status-line item visibility for the current TUI/classic session.
- `/telemetry`: render the current telemetry snapshot.
- `/memory routing`: show memory routing policy.
- `/memory stats`: show memory document, staged, episode, fact, and index stats.
- `allbert-cli telemetry --json`: print the telemetry snapshot for scripts and tests.
- `allbert-cli memory routing show`
- `allbert-cli memory routing set --mode always-eligible --skill memory-curator`
- `allbert-cli memory routing set --auto-activate-intent memory_query`
- `allbert-cli memory stats`

### Daemon protocol

Add:

- `ClientMessage::SessionTelemetry`
- `ServerMessage::SessionTelemetry(TelemetrySnapshot)`

`TelemetrySnapshot` is additive. Existing `SessionStatus` remains for backward-compatible status rendering.

### Memory search

Extend `search_memory` tiers:

- `durable`
- `staging`
- `episode`
- `fact`
- `all`

Auto-prefetch remains conservative:

- durable tier only by default
- staging never auto-injected
- episode excluded unless explicitly enabled
- fact summaries can join durable prefetch only when `memory.facts.enabled = true` and the fact source is approved durable memory

## Milestones

### M0 — Spec freeze and sequencing

Scope:

- Add this v0.11 plan.
- Move the previous self-improvement plan to v0.12.
- Update roadmap, vision, and affected v0.8-v0.10 plan references.
- Add ADRs for TUI adapter boundaries, telemetry ownership, memory routing, episode/fact memory, and optional semantic retrieval.
- Update existing self-improvement ADRs 0067-0073 to reference v0.12.

Tests and validation:

- `cargo fmt --check`
- `env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings`
- `env -u RUSTC_WRAPPER cargo test -q`
- `env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- --help`
- Documentation grep: no roadmap entry points self-improvement at v0.11.

### M1 — Config schema and migration

Scope:

- Add `ReplUiMode`, nested TUI config, memory routing config, episode/fact/semantic config, and optional `model.context_window_tokens`.
- Implement migration so fresh profiles default to TUI and upgraded profiles without `repl.ui` preserve classic REPL.
- Extend setup to show the selected UI and memory-routing defaults.
- Add config validation for status-line item names, routing modes, semantic hybrid weight, and context-window values.

Tests:

- Fresh profile serializes `repl.ui = "tui"`.
- Legacy config without `repl.ui` loads and persists `classic`.
- Invalid status-line item names fail validation.
- Invalid routing mode fails validation.
- `context_window_tokens = 0` means unknown and is accepted.

Validation after milestone:

- `cargo fmt --check`
- `env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings`
- `env -u RUSTC_WRAPPER cargo test -q config`
- `env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- --help`

### M2 — Telemetry protocol and kernel accounting

Scope:

- Add `TelemetrySnapshot` and protocol roundtrip.
- Track latest model usage and session cumulative token usage in `AgentState`.
- Compute context percentage only when context window is known and latest usage is nonzero.
- Include memory counts, pending inbox count, active skills, always-eligible skills, last intent, cost, and trace.
- Add `allbert-cli telemetry --json`.

Tests:

- Protocol JSON roundtrip for `TelemetrySnapshot`.
- Context percentage renders as unknown when context window is zero.
- Latest response usage updates telemetry without changing cost accounting.
- Pending inbox count is identity/session scoped.
- Telemetry command works without a live provider in a temp profile.

Validation after milestone:

- `cargo fmt --check`
- `env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings`
- `env -u RUSTC_WRAPPER cargo test -q telemetry`
- `env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- telemetry --json`

### M3 — TUI shell and classic fallback

Scope:

- Add `ratatui` and `crossterm` dependencies to `allbert-cli`.
- Add `crates/allbert-cli/src/tui/` with event loop, app state, renderer, input composer, transcript buffer, status line, and modal surface.
- Keep daemon event handling independent from rendering.
- Route no-subcommand launch through configured UI.
- Preserve existing `repl.rs` as classic mode.
- Add `allbert-cli repl --classic` and `allbert-cli repl --tui`.

Tests:

- Ratatui `TestBackend` renders transcript, input composer, and status line.
- Narrow terminal falls back to compact status line.
- Classic REPL command parsing remains unchanged.
- TUI can process assistant text, tool call/result events, confirm requests, and turn completion.
- Ctrl-C/Ctrl-D behavior exits or cancels consistently with classic REPL.

Validation after milestone:

- `cargo fmt --check`
- `env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings`
- `env -u RUSTC_WRAPPER cargo test -q tui`
- `env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- --help`

### M4 — Status-line controls

Scope:

- Implement `/statusline` in TUI and classic REPL.
- Implement `/telemetry` in TUI and classic REPL.
- Render status-line items from telemetry only.
- Support session-local item toggles and `--save` persistence.
- Refuse unknown status-line item names with a local error.

Tests:

- Every status item renders from a fixture telemetry payload.
- Unknown context window renders `ctx ?`.
- Cost/tokens/status labels remain stable in snapshots.
- `/statusline hide cost` updates current session state.
- `/statusline show cost --save` persists config.

Validation after milestone:

- `cargo fmt --check`
- `env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings`
- `env -u RUSTC_WRAPPER cargo test -q statusline`
- `env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- --help`

### M5 — Memory routing

Scope:

- Implement `memory.routing` policy in prompt construction.
- Surface always-eligible skills every root turn without loading full bodies.
- Auto-activate `memory-curator` for `memory_query` intent and configured cues.
- Add `allbert-cli memory routing show|set`.
- Ensure generated runtime `AGENTS.md` reflects active routing policy but remains generated.

Tests:

- `memory-curator` is always eligible on root turns.
- `memory-curator` body is not active on plain chat/task turns without cues.
- `memory-curator` auto-activates on `memory_query`.
- Routing changes through CLI update config without code edits.
- Job-level `memory.prefetch: false` still disables prefetch even when memory-curator is eligible.

Validation after milestone:

- `cargo fmt --check`
- `env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings`
- `env -u RUSTC_WRAPPER cargo test -q memory_routing`
- `env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- memory routing show`

### M6 — Episode recall

Scope:

- Extend the memory indexer to add `episode` documents derived from session journals.
- Add episode metadata: session id, channel, timestamp, role, turn number, and snippet path.
- Add `search_memory(tier = "episode")`.
- Add `allbert-cli memory stats` episode counts.
- Keep default prefetch durable-only.

Tests:

- Session journal fixture indexes into `episode` tier.
- Episode search finds prior session text.
- Durable prefetch excludes episodes by default.
- Deleting/forgetting a session removes derived episode docs on rebuild.
- Episode hits are clearly labeled as session working history.

Validation after milestone:

- `cargo fmt --check`
- `env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings`
- `env -u RUSTC_WRAPPER cargo test -q episode`
- `env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- memory stats`

### M7 — Temporal fact overlay

Scope:

- Extend staged-memory frontmatter parsing to tolerate and validate optional `facts`.
- Preserve fact metadata on promotion.
- Index approved durable facts into `tier = "fact"`.
- Add supersession metadata and current-fact filtering helpers.
- Render fact counts in telemetry and memory stats.

Tests:

- Staged fact frontmatter roundtrips.
- Promotion preserves fact metadata.
- Fact search returns approved durable facts.
- Staged facts are not returned by durable/fact auto-prefetch before promotion.
- Superseded facts remain searchable only when explicitly requested.

Validation after milestone:

- `cargo fmt --check`
- `env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings`
- `env -u RUSTC_WRAPPER cargo test -q fact`
- `env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- memory stats`

### M8 — Optional semantic retrieval

Scope:

- Add semantic config and fake deterministic embedding provider for tests.
- Add derived semantic index metadata under `memory/index/semantic/`.
- Implement deterministic hybrid fusion when semantic retrieval is enabled.
- Keep disabled path dependency-light at runtime.
- Document provider requirements and privacy posture.

Tests:

- Disabled semantic config makes no embedding calls.
- Fake embeddings produce stable hybrid ordering.
- Rebuild records semantic index schema and provider metadata.
- Semantic index can be deleted and rebuilt from markdown.
- BM25-only search output remains unchanged when semantic is disabled.

Validation after milestone:

- `cargo fmt --check`
- `env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings`
- `env -u RUSTC_WRAPPER cargo test -q semantic`
- `env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- memory verify`

### M9 — End-to-end release hardening

Scope:

- Add operator docs:
  - `docs/operator/tui.md`
  - `docs/operator/telemetry.md`
  - `docs/operator/adaptive-memory.md`
- Add `docs/notes/v0.11-upgrade-<date>.md`.
- Update README and onboarding to explain TUI defaults, classic fallback, memory routing, and semantic opt-in.
- Update generated bootstrap docs if needed.
- Bump crate versions to `0.11.0`.
- Add release smoke scripts or documented temp-profile smoke commands.

Tests:

- Fresh temp profile launches configured TUI by default in non-interactive smoke-safe mode or test backend.
- Upgraded temp profile preserves classic REPL.
- A scripted fake-provider turn updates telemetry and status-line state.
- A scripted memory turn stages one entry, status line shows staged count, memory-curator can review it, and CLI can promote/reject it.
- `memory search --tier episode` and `--tier fact` work against fixtures.

Release validation:

- `cargo fmt --check`
- `env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings`
- `env -u RUSTC_WRAPPER cargo test -q`
- `env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- --help`
- Temp-home daemon smoke: start, status, telemetry, memory stats, stop.

## End-user release plan

### Upgrade behavior

- Existing profiles keep classic REPL unless the operator opts into TUI.
- Fresh profiles default to TUI.
- Operators can switch with setup, config, or `allbert-cli repl --classic`.
- Memory routing defaults are safe and non-destructive.
- Episode/fact indexes are derived and rebuildable.
- Semantic retrieval is disabled unless explicitly configured.

### Operator messaging

Release notes must explain:

- "The terminal UI is new; classic REPL remains available."
- "Status-line data is local daemon telemetry, not a shell hook."
- "`memory-curator` is always eligible by default, not always loaded into every prompt."
- "Session episodes are searchable working history, not durable memory."
- "Temporal facts still go through staged review before durable promotion."
- "Semantic retrieval is opt-in."

### Acceptance checklist

- Fresh setup can complete without hosted provider keys.
- `allbert-cli` opens the configured UI.
- `allbert-cli repl --classic` works.
- TUI renders model, cost, token, memory, and inbox status without a live provider.
- Memory routing can be shown and changed without editing code.
- Episode/fact search is explicit and clearly labeled.
- Docs and upgrade notes are end-user ready.

## ADRs

- [ADR 0074](../adr/0074-tui-is-a-daemon-attached-adapter-not-a-runtime.md) — TUI adapter boundary.
- [ADR 0075](../adr/0075-session-telemetry-is-kernel-owned-protocol-state.md) — telemetry ownership and protocol payload.
- [ADR 0076](../adr/0076-memory-routing-is-configurable-always-eligible-policy.md) — memory-curator routing policy.
- [ADR 0077](../adr/0077-episode-and-fact-memory-are-indexed-recall-not-durable-memory.md) — episode/fact memory posture.
- [ADR 0078](../adr/0078-semantic-memory-is-optional-derived-retrieval.md) — optional semantic retrieval posture.

## Dependencies

- v0.5 curated memory, staging, and Tantivy retrieval.
- v0.6 session journals and cost-cap hardening.
- v0.7 normalized tool surface and channel capability model.
- v0.8 approval inbox and cross-channel continuity.
- v0.9 contributor validation contract.
- v0.10 provider seam and local-first defaults.

## Exit criteria

- TUI and classic REPL both work against daemon-backed sessions.
- Telemetry snapshot is available through protocol, CLI, and TUI.
- Status line is configurable and test-covered.
- Memory routing is configurable without code changes.
- `memory-curator` always-eligible behavior is proven by tests.
- Episode and fact tiers are explicit, searchable, and never silently promoted.
- Semantic retrieval is off by default and fake-provider tested when enabled.
- Tier A validation is green.
- End-user docs and upgrade notes are complete.

