# Allbert v0.50 Artifacts Central

Request flow: `docs/plans/v0.50-request-flow.md`

ADRs:
- `docs/adr/0053-content-addressable-artifact-store.md` (the content-addressable
  artifact store: identity, layout, dedup, retention, security floors; new in
  v0.50).
- `docs/adr/0042-audio-image-and-media-resource-classes.md` (artifact resource
  class amendment; proposed before implementation, accepted at v0.50 M1
  closeout).
- `docs/adr/0031-settings-schema-fragments-and-authority.md` (the `artifacts.*`
  fragment authority pattern, consumed).
- `docs/adr/0046-settings-schema-migration-policy.md` (`schema_version` pin,
  consumed).
- `docs/adr/0054-artifact-provenance-and-browser-surface.md` (artifact↔thread/
  message provenance linking and the core-kernel-vs-plugin-surface split; new in
  v0.50, consumed here for linking, governs the v0.50b browser).

Status: implemented as `0.50.0` and released through the `v0.50.1` Artifacts
Browser sidecar tag on 2026-06-09. M1-M7 were implemented and focused-verified
on 2026-06-08; the post-remediation release gates passed on 2026-06-09.
v0.50b consumes the core read/query actions delivered here.

Source: current roadmap sequencing. v0.50 is inserted between v0.49 vision and
v0.52 Channel Pack 1, promoting the roadmap's content-addressed artifact-store
seed into a full version so durable media has one canonical home before channels
begin forwarding attachments.

## Purpose

v0.50 adds **Artifacts Central**: a uniform, type-agnostic, content-addressable
store (CAS) for durable artifacts that are **uploaded by the operator**,
**created by Allbert**, or **found by Allbert** through approved tools such as
browser research. Artifacts can be any type — audio, video, images, PDFs, text,
office documents, spreadsheets — because the store addresses content by hash and
never interprets it.

The store is built **in-tree on BEAM primitives**, not on a third-party
package. The research-backed reason (recorded in ADR 0053):

- The CAS-specific Hex packages are dead ends: `hashfs` (last release 2017, repo
  gone, SHA-1/git-blob hashing, no metadata) and `scarab` (2016, pre-1.0, no
  metadata) are abandoned; `dasl` is an IPLD/CID *encoding* layer, not a local
  file store.
- The upload utilities are not content-addressed stores: `waffle` keys storage
  by filename/scope (not content hash) and drags in `hackney` + an Ecto/
  ImageMagick-shaped workflow; `Capsule` (Apache-2.0, zero-dep) is a clean
  `Storage`/`Locator` abstraction but explicitly leaves hashing/addressing to
  the caller.
- The "hard part" is stdlib: `:crypto.hash(:sha256, …)` (streaming-capable),
  `Base.encode16/2`, sharded `Path.join`, and atomic `File.rename` cover hashing
  + layout completely in ~100–150 reviewable lines, with zero supply-chain,
  licensing, or maintenance risk and no heavy deps. `Capsule` remains a parked
  option only if a later version needs a pluggable S3 backend.

v0.50 is deliberately **substrate-heavy and primitive-light**: nearly everything
it needs already exists (Allbert Home roots, the resource-URI scheme system,
Security Central operation/permission classes, the Settings Central fragment
pattern, the allow-list redactor, the default-off retention mechanism, the Jido
action Registry/Runner). The genuinely new primitives are the content-addressed
object store itself, the trace-safe artifact metadata index, the
`artifact://sha256/<hex>` identity model, the codebase's **first supervised
Jido.Sensor ingestion path**, and an **artifact↔thread/message provenance link**
so an artifact knows which conversation (and turn) created or referenced it.

**Provenance and browsing — the app shape.** Artifacts Central is more than a
blob bucket: it links files to the threads/messages that produced them and is
meant to be *browsed* like a small app (StockSage-style operator surfaces over
a local-first record set). v0.50 ships the **core kernel** — the store, the
`artifact://` scheme, security, settings, the actions, the sensor, and the
artifact↔thread/message links — plus the core read/query actions a browser
consumes. The operator **browsing repository** itself (a workspace LiveView
panel, an `/apps/artifacts/<sha>` detail page, and a `mix allbert.artifacts`
CLI) ships in the focused sidecar **v0.50b Artifacts Browser** as a plugin/app,
modeled on StockSage and `allbert.browser`. The split keeps the core release
shippable and dogfoods the App/SurfaceProvider contract; the store stays core
because channels (v0.52), MCP (v0.51), and export (v0.57) depend on it and
durable Home data cannot live in a plugin. Both decisions are recorded in
ADR 0054.

## Readiness Sweep

Status: complete; implementation-ready for M1-M7.

- **Substrate fit.** Artifacts Central is a storage substrate under the runtime,
  not a new runtime. It composes with CLI/workspace/channels through Resource
  Access, Security Central, confirmations, traces, and audits. A stored artifact
  is descriptive evidence, never authority; nothing auto-promotes to memory and
  content-addressed identity never grants permission.
- **Roadmap fit.** v0.50 follows v0.49 vision and precedes v0.52 Channel Pack 1.
  It completes the v0.42 media-resource vision (audio in v0.48, image/screenshot
  in v0.49, durable cross-surface artifacts here) and gives channels a canonical
  artifact home before they forward attachments.
- **Past-plan fit.** Reuses, in place: the `AllbertAssist.Paths` /
  `Runtime.Paths` Home-root facade; the `ResourceURI` scheme-plugin pattern
  (mint/normalize/derive/scope); the `OperationClass` + `Security.Policy`
  closed-vocabulary permission/floor machinery (the v0.48 `voice_floor` and
  `notes_file_write` floors are templates); the `Settings.Schema` voice/vision
  fragment shape (`schema_version` + retention + redact-flag); the allow-list
  `Runtime.Redactor` (which already treats `content_sha256` as trace-safe); the
  default-off `retention_enabled` mechanism + `cleanup_transient_capture`
  lifecycle; the `Actions.Registry` + `Actions.Runner.run/3` action path; and
  the `Marketplace.Bundle` SHA-256 helpers + `sha256:` convention,
  `ImageMetadata.content_sha256`, and the `Ref.digest` slot.
- **Future-plan fit.** **v0.50b Artifacts Browser** ships the operator browsing
  surfaces (panel + page route + CLI) as a plugin/app over the core read
  actions. v0.52+ channel plugins may forward artifact references but do not own
  store authority. v0.51 MCP server mode may expose artifacts as read resources
  under the same redaction policy. v0.57 export/import treats the `artifacts/`
  root as another Home subtree to inventory (complementary, not overlapping —
  v0.57 defines no CAS of its own). Nothing here pre-commits the v1.0 freeze.
- **Provenance fit.** The artifact↔thread/message link reuses the established
  durable-record→thread pattern (objectives, canvas tiles, jobs all carry an
  indexed `thread_id` string column populated from `context.request.thread_id`).
  The link is a queryable SQLite join table plus a denormalized originating
  thread/message id in the markdown sidecar; `thread_id`/`message_id` are
  provenance strings, never authority.
- **Surface fit.** The browsing repository reuses the StockSage / `allbert.browser`
  plugin pattern: an `App` + `SurfaceProvider` contributing a `:canvas_panels`
  panel hydrated by `workspace_panel_surfaces/1`, an `/apps/artifacts/<sha>`
  page LiveView (route in the core router, module plugin-owned), and a
  `mix allbert.artifacts` CLI that is a thin shell over the core `:artifact_read`
  actions. A plugin can render the store but cannot own the scheme, permissions,
  Home root, or sensor.
- **Library fit.** No third-party CAS/upload dependency is added (ADR 0053). The
  store is owned in-tree; `Capsule` is parked for a future pluggable-backend
  need only.
- **ADR fit.** ADR 0053 is new and binding. ADR 0054 is new and binding
  (provenance linking + core/plugin surface split); ADR 0053 is amended to point
  at it. ADR 0042 gains the artifact resource class amendment. ADR 0031
  (fragment authority), ADR 0046 (`schema_version`), and the ADR 0015/0017
  app/plugin contract are consumed unchanged.

## Implementation Seams

The names below are the expected implementation seams; implementations may make
small naming adjustments, but must preserve the boundaries and tests.

- **Core context.** Add `AllbertAssist.Artifacts` as the public facade over
  `AllbertAssist.Artifacts.Store`, `MetadataIndex`, `ThreadLinks`, `GC`, and
  `Doctor`. Store code owns object bytes; metadata/index code owns sidecars and
  list/query summaries; thread-link code owns the Repo-backed provenance edge.
- **Ecto layer.** Add `AllbertAssist.Artifacts.ThreadLink` and a
  `CreateArtifactThreadLinks` migration. The link id is a deterministic string
  derived from `{sha256, user_id, thread_id, message_id || input_signal_id ||
  "thread", role}` so repeated puts upsert the same edge even when
  `message_id` is nil. Add indexes on `[:artifact_sha256]`,
  `[:thread_id, :user_id]`, and `[:user_id, :artifact_sha256]`.
- **Action layer.** Add internal registered actions under
  `AllbertAssist.Actions.Artifacts.{PutArtifact,GetArtifact,ListArtifacts,
  DeleteArtifact,Threads,Doctor}` with names `put_artifact`, `get_artifact`,
  `list_artifacts`, `delete_artifact`, `artifact_threads`, and
  `artifact_doctor`. These carry **permission** classes `:artifact_read`,
  `:artifact_write`, `:artifact_delete` (the security floors live on permission —
  `permissions.artifact_*` in `Security.Policy`; `artifact_doctor` reuses
  `:artifact_read`). Separately add a dispatch **`execution_mode`** descriptor
  (e.g. `:artifact_store`) to the `Actions.Capability.execution_mode` `@type`
  union — `execution_mode` is a routing descriptor (`:read_only`,
  `:voice_provider_call`, …), distinct from and need not equal the permission
  atom. `delete_artifact` remains `confirmation: :required`.
- **Settings and policy.** Register `artifacts.*` in `Settings.Schema` defaults,
  schema, and safe-write keys; register `permissions.artifact_read`,
  `permissions.artifact_write`, and `permissions.artifact_delete` in Settings
  Central and `Security.Policy`.
- **Resource Access.** Add `ResourceURI.artifact/1` and `artifact!/1` (bare
  scheme name, per the non-capture `workflow/1`/`plan_run/1`/`browser_session/1`
  precedent — artifacts are not captures), `artifact://sha256/<hex>`
  normalization, a private `derive/2` clause (reached through `derived_fields/1`)
  matching `host: "sha256"` with the hex from `path` and returning origin kind
  `:artifact_store`, and a `scope_uri/4` clause for artifact grants if a later
  caller needs scoped Resource Access.
- **Sensor shape.** The ingestion sensor uses `use Jido.Sensor` and runs under
  `Jido.Sensor.Runtime` in the application supervision tree (verified against
  current Jido docs via Context7 and the local `deps/jido` runtime seam). It
  implements `init/2` and `handle_event/2`, emits
  ingestion-request signals such as `allbert.artifact.ingest_requested` via
  `{:emit, signal}`, and schedules/polls only when needed. Emission requires a
  configured **dispatch target** in the sensor `context` (a pid / registered
  process name via `context.agent_ref`, or a `Jido.Signal.Dispatch` config);
  without one, `{:emit, signal}` is silently dropped, so the plan must wire the
  signal to its core consumer. It must not write around the core `put` path;
  ingestion either emits a signal consumed by core code or invokes
  `Actions.Runner.run("put_artifact", ...)` with a sensor context that still
  honors `artifacts.enabled`, retention, bounds, redaction, and permission
  floors.
- **Message precision.** Resolving `input_signal_id` to a `conversation_messages`
  row by `{user_id, thread_id, input_signal_id}` requires a **new**
  `Conversations` query (e.g. `get_message_by_input_signal/3`) — none exists
  today, and `conversation_messages.input_signal_id` is **not indexed**, so add a
  supporting index when message-precise linking is enabled. If no row exists, the
  link is still recorded at thread level with `message_id: nil` and
  `metadata.input_signal_id`; message precision is best-effort provenance, not
  a release blocker.
- **Backfill scope.** M5 backfills retained v0.48 voice captures, v0.49 vision
  input media, and v0.49 generated-image output roots:
  `<ALLBERT_HOME>/audio`, `<ALLBERT_HOME>/images`, and
  `<ALLBERT_HOME>/generated_images`, using the existing
  `voice.audio.retention_root`, `vision.media.retention_root`, and
  `image.generation.retention_root` setting keys as the legacy-root inputs.
  Ephemeral scratch and historical Browser cache backfill are excluded.
  Browser/download/cache artifacts may call
  `put_artifact` in future flows, but v0.50 release evidence covers fixtures and
  retained voice/vision/image roots only.
- **Release gate and eval inventory.** Add `:v050` to
  `AllbertAssist.SecurityFixtures.EvalInventory`'s milestone type, add
  `:artifact_store` to the eval surface vocabulary, and back the request-flow
  eval IDs with assertions. Update `Mix.Tasks.Allbert.Test` usage, dispatch,
  `@release_v050_steps`, `release_v050/0`, evidence cleanup, secret scan, and
  `allbert_test_task_test.exs` coverage so `mix allbert.test release.v050`
  writes reproducible evidence under
  `<ALLBERT_HOME>/release_evidence/v050/`.

## Locked Decisions

These choices are binding for v0.50. Changing any requires an explicit
re-decision here and in the request-flow document before code lands.

| # | Decision | Chosen value | Trade-off if changed |
|---|---|---|---|
| 1 | Store implementation | Thin in-tree CAS over BEAM primitives (`:crypto` SHA-256 + `Base.encode16` + sharded objects + atomic temp-write/`rename`); no third-party CAS/upload dependency. | hashfs/scarab are abandoned and metadata-free; waffle/Capsule are not content-addressed stores; adopting one adds supply-chain/maintenance risk and an abstraction that fights the Allbert-Home layout. |
| 2 | Artifact identity | `artifact://sha256/<hex>` — an immutable, dedup-keyed content address minted by hashing bytes, distinct from the mutable capture-id schemes (`mic://`, `image://`, `screen://`). | A mutable or path-based id loses dedup and breaks cross-surface lookup. |
| 3 | Storage layout | `<ALLBERT_HOME>/artifacts/objects/<ab>/<cd>/<sha256>` (two-level shard from the hash prefix); a real Home root registered through `Paths`/`Runtime.Paths`. | A flat dir bloats; an unregistered ad-hoc path repeats the v0.48/v0.49 mistake of media roots living only as settings strings. |
| 4 | Metadata index | Trace-safe, markdown-first per-artifact record (sidecar under `artifacts/index/`) plus an in-memory lookup index; fields are a strict allow-list. The object layer holds bytes; metadata is decoupled (the good idea from scarab's namespace→hash indirection, without scarab). | Embedding metadata in object paths couples identity to mutable attributes; a binary-only DB loses inspectability/portability. |
| 5 | Metadata allow-list | `{sha256, mime, byte_size, origin, source_resource_uri, created_at, retention, redaction_status, lifecycle, provenance}` where `provenance` is a bounded thread/message/id summary only; raw bytes/filenames-as-content never enter traces/audits/assigns/CLI. | A free-form metadata map risks leaking sensitive content into traces. |
| 6 | Permission floors | `artifact_read` floor `:allowed`; `artifact_write` floor `:allowed` (local durable storage, gated by `artifacts.enabled` + retention policy, no external/billable call); `artifact_delete` floor `:needs_confirmation` (destructive, mirrors `notes_file_write`). | Treating delete as `:allowed` lets durable artifacts vanish without confirmation; treating write as `:needs_confirmation` would spam confirmations on every retained capture. |
| 7 | Retention | Default-off (`artifacts.retention_enabled` false), reusing the v0.48 `retention_enabled`-keyed mechanism; durable artifacts are operator-removable; a mark-and-sweep GC reconciles the metadata index against on-disk objects. | Default-on durable retention stores sensitive media without consent. |
| 8 | Ingestion | `put_artifact`/`get_artifact`/`list_artifacts`/`delete_artifact` registered actions plus the codebase's first supervised `Jido.Sensor` path for auto-ingesting the durable-retention branch of existing capture flows. The sensor is advisory ingestion only; it never grants authority. | A custom GenServer watcher would re-invent what `Jido.Sensor` provides; making the sensor authoritative would violate the descriptive-evidence rule. |
| 9 | Backfill scope | Migrate the **retained** branch of v0.48 audio (`<ALLBERT_HOME>/audio`), v0.49 vision media (`<ALLBERT_HOME>/images`), and v0.49 generated-image media (`<ALLBERT_HOME>/generated_images`) into the CAS and formalize those as real Home roots. Ephemeral scratch (`tmp/voice-synthesis`, transient `tmp/voice-captures`, `tmp/telegram-voice`) and historical Browser cache files stay out of M5. | Migrating ephemeral/cache scratch wastes dedup and complicates lifecycle; leaving retained media outside the CAS keeps multiple durable-media schemes. |
| 10 | Authority | Capability/identity are routing predicates only. A content address, a doctor success, or a stored metadata record never grants read/write/send permission. | Metadata-as-authority breaks the Security Central boundary. |
| 11 | Provenance linking | Artifact↔thread/message links live in a SQLite join table `artifact_thread_links(id, artifact_sha256, thread_id, message_id, role, user_id, metadata)` (`role ∈ created_by \| referenced_by`), indexed on `sha256`, `[thread_id, user_id]`, and `[user_id, artifact_sha256]`, plus a denormalized originating thread/message id in the markdown sidecar. The link id is deterministic from the normalized edge tuple so repeated puts are idempotent. `thread_id`/`message_id` come from `context.request` at put-time (message via `request.input_signal_id`); they are provenance strings, never enforced FKs or authority. | A single column on the artifact under-models a content-addressed object shared across threads with different roles; markdown-only loses the query index; non-idempotent links create duplicate browser rows. |
| 12 | Browsing surfaces | The operator browsing repository (workspace panel + `/apps/artifacts/<sha>` page + CLI) ships in **v0.50b Artifacts Browser** as a plugin/app reading the store only through core `:artifact_read` actions. v0.50 ships the core read/query actions it consumes (including `list_artifacts` by thread). | Building the browser into core bloats it and skips the App/SurfaceProvider contract; a plugin owning the store would invert the channel/MCP/export dependency. |

## Content-Addressable Store Contract

The object store is a pure blob layer with a decoupled metadata index.

- **Hashing.** `:crypto.hash(:sha256, bytes)` (or the streaming
  `hash_init/:sha256` → `hash_update` → `hash_final` form for large files),
  lowercased Base16. The hash is the artifact's whole identity.
- **Layout.** `<ALLBERT_HOME>/artifacts/objects/<first2>/<next2>/<full_sha256>`.
- **Dedup.** Identical content hashes to the identical path, so a re-`put` is
  idempotent: write to a temp file in the same filesystem, then `File.rename`
  into place (atomic on same-FS), discarding the temp if the object already
  exists. Concurrent writers of identical content converge without locks.
- **Metadata.** Each object has a markdown sidecar
  `<ALLBERT_HOME>/artifacts/index/<first2>/<next2>/<full_sha256>.md` carrying the
  Locked-Decision-5 allow-list, kept inspectable/portable per Allbert's
  markdown-first value. An in-memory index caches `sha256 → metadata` for
  `list`/lookup.
- **Origin** is one of `:operator_upload`, `:allbert_created`, `:allbert_found`
  — provenance, not authority.
- **Retention/GC.** Honors `artifacts.retention_enabled`; a supervised
  mark-and-sweep task reconciles index vs. objects and performs
  operator-policy-driven removal. `delete` removes both object and sidecar and
  is confirmation-gated.

## Artifact Resource And Authority Model

v0.50 adds the artifact slice of ADR 0042:

- `artifact://sha256/<hex>` identifies a durable, content-addressed artifact.
  The id is the lowercase hex SHA-256 (`^[a-f0-9]{64}$`); ids are opaque,
  Allbert-Home-local, and never provider-selected.
- Transport/capture URIs (`image://`, `screen://`, `mic://`, `browser://`,
  generated-media handles) may **point at or derive** an `artifact://` address,
  but they do not become the store's authority model and the store does not
  inherit their mutability.
- Stored content is default-off for retention and redacted from traces by
  default. Traces record bounded metadata only: artifact URI, byte size, MIME,
  origin, content hash, redaction status. Raw bytes never appear in traces,
  audits, diagnostics, LiveView assigns, or CLI output.

New permission classes:

- `:artifact_read` — floor `:allowed` (read durable metadata/bytes through
  Resource Access).
- `:artifact_write` — floor `:allowed` (local durable write, gated by
  `artifacts.enabled` + retention policy; no external/billable call).
- `:artifact_delete` — floor `:needs_confirmation` (destructive; reuses the
  `notes_file_write`-style confirmation floor).

New operation classes / origin:

- origin kind `:artifact_store`
- operation `:artifact_read`
- operation `:artifact_write`
- operation `:artifact_delete`

## Provenance And Thread Linking

Artifacts Central links every artifact to the conversation that produced or
referenced it, so an operator (and the v0.50b browser) can ask "what files came
from this thread / this turn?" and, from an artifact, "which thread created
this?".

- **Durable representation.** A SQLite join table on `AllbertAssist.Repo`,
  `artifact_thread_links(id, artifact_sha256, thread_id, message_id, role,
  user_id, metadata, timestamps)`, with `role ∈ {created_by, referenced_by}`,
  indexed on `[:artifact_sha256]` (reverse lookup) and `[:thread_id, :user_id]`
  (browse-by-thread), plus `[:user_id, :artifact_sha256]` for scoped reverse
  lookup. This mirrors the existing durable-record→thread pattern
  (`objectives.source_thread_id`, `workspace_canvas_tiles.thread_id`,
  scheduled-job `thread_id`) — a plain indexed string column, **not** an
  enforced FK, consistent with "thread id is never authority". The deterministic
  link id makes duplicate puts idempotent. The originating
  `thread_id`/`message_id`/`input_signal_id` is additionally denormalized into
  the artifact's markdown sidecar as bounded `provenance` metadata for
  human-readable provenance (mirrors the trace `Thread:` line and the canvas
  tile sidecar).
- **Where the link comes from.** At put-time the artifact actions read
  `request = Map.get(context, :request, %{})` then `request.thread_id`,
  `request.user_id`, `request.session_id` — the canonical read used by
  `append_memory`, `objectives`, and `confirmations/origin`. Message-level
  attribution uses `request.input_signal_id` (persisted on the
  `conversation_messages` row); the runtime resolves it by
  `{user_id, thread_id, input_signal_id}` to a `msg_…` id when possible.
  Otherwise the link is thread-level with the signal id retained in metadata.
- **Query path.** "List artifacts created in thread X" is an Ecto query over the
  join table (user-scoped, role-filtered), the same idiom as
  `Conversations.list_messages/2` and `Objectives.list_objectives/2` — not a
  markdown scan. The reverse ("which thread created this sha256") filters the
  same table by `artifact_sha256`.
- **Authority.** A link is provenance, never permission. Listing artifacts by
  thread still resolves through `:artifact_read`; a thread id grants nothing.

## Settings

`artifacts.*` fragment (`schema_version: 1`, ADR 0046; fragment authority per
ADR 0031), cloning the voice/vision fragment shape:

- `artifacts.schema_version` (`writable?: false`, 1..1).
- `artifacts.enabled` (bool, default **false**).
- `artifacts.root` (string, default `<ALLBERT_HOME>/artifacts`).
- `artifacts.retention_enabled` (bool, default **false**).
- `artifacts.max_bytes` (bounded_integer) and `artifacts.allowed_mime` /
  `artifacts.allowed_types` (lists; empty = accept any) — type-agnostic by
  default, constrainable by policy.
- `artifacts.dedup` (enum, default `content_sha256`).
- `artifacts.gc` policy keys (retention window / on-demand only).
- `artifacts.trace.redact_bytes` (bool, default **true**).

## Provider Doctor

A core `artifact_doctor` read action reports, with the ADR 0047 redacted
envelope, additive read-only fields only: `:store_writable`, `:object_count`,
`:index_consistent`, `:retention_enabled`, `:orphan_object_count`,
`:orphan_index_count`, `:link_count`. Never emits raw bytes, absolute paths, or
content. v0.50 surfaces it through the existing doctor surface; the
`mix allbert.artifacts doctor` CLI wrapper ships with v0.50b.

## Security Posture

- A content address, metadata record, or doctor success never grants permission;
  Security Central + Resource Access remain the authority boundary.
- Operator-supplied, Allbert-created, and Allbert-found artifacts only; no
  arbitrary remote fetch beyond already-approved tool paths.
- `artifact_delete` is destructive and confirmation-gated.
- Binary content is bounded, default-off for retention, and absent from
  traces/audits/assigns/CLI; only allow-listed metadata persists.
- The Jido ingestion sensor is advisory: it ingests durable-retention writes but
  never elevates authority.

## Non-Goals

- No third-party CAS/upload dependency (build-own per ADR 0053).
- No arbitrary remote media fetch or sync service.
- No permanent retention by default.
- No content interpretation/transformation (no resize/transcode/OCR) in v0.50.
- No metadata-as-authority; no auto-promotion of artifacts to memory.
- No IPFS/IPLD/CID interchange (dasl-style) in v0.50.

## Development-Lane Annotations

Per ADR 0049. The deterministic `release.v050` lane uses fixture artifacts of
several types, a disposable `ALLBERT_HOME`, and deterministic backend fixtures;
no external service is required for release authority.

| M | Parallel workstreams | Serial barriers | Gate evidence + rejoin |
|---|---|---|---|
| M1 | CAS object store (hash/shard/atomic-write/dedup); `artifacts_root` in `Paths`/`Runtime.Paths`; markdown-first metadata index + in-memory cache; ADR 0053 + ADR 0042 amendment | root registration, atomic-write/dedup correctness, index schema | complete; focused gate passed on 2026-06-08; docs reconciled before M2 |
| M2 | `artifact://` scheme (mint/normalize/derive/scope); `:artifact_read/write/delete` permissions/op-classes/floors; `artifacts` redaction surface; ingest bounds (max_bytes/allowed types) | ResourceURI normalization, policy floors, redactor | complete; focused gate passed on 2026-06-08; docs reconciled before M3 |
| M3 | `artifacts.*` settings fragment; `put_artifact`/`get_artifact`/`list_artifacts`/`delete_artifact` actions; retention + mark-and-sweep GC | fragment schema_version, action registration, GC reconcile | complete; focused gate passed on 2026-06-08; docs reconciled before M4 |
| M4 | Provenance: `artifact_thread_links` join table (Repo migration); deterministic link recorded at put from `context.request` (thread + message via `input_signal_id`); `list_artifacts` by-thread query; reverse `artifact_threads` lookup | migration, link-at-put, by-thread query | complete; focused gate passed on 2026-06-08; docs reconciled before M5 |
| M5 | Backfill: formalize audio/image/generated-image roots; migrate retained v0.48 audio + v0.49 vision-input/generated-image media into the CAS; ingestion hook from the retention path | retained-media migration, dedup across pre-existing files | complete; focused gate passed on 2026-06-08; docs reconciled before M6 |
| M6 | First supervised `Jido.Sensor` for auto-ingesting durable-retention writes | sensor lifecycle under supervision | complete; focused gate passed on 2026-06-08; docs reconciled before M7 |
| M7 | security evals (`:v050`); operator/developer/security docs; `release.v050`; version/changelog closeout | `EvalInventory` (`:v050`), release lane | complete; `release.v050` passed on 2026-06-08; ready for manual validation |

Focused gate map:

- **M1:** `mix test apps/allbert_assist/test/allbert_assist/paths_test.exs
  apps/allbert_assist/test/allbert_assist/runtime/paths_test.exs
  apps/allbert_assist/test/allbert_assist/artifacts/store_test.exs
  apps/allbert_assist/test/allbert_assist/artifacts/metadata_index_test.exs`
  (`app_env_serial` for path config, `home_fs_serial` for artifact fixtures).
- **M2:** `mix test
  apps/allbert_assist/test/allbert_assist/resources/resource_uri_test.exs
  apps/allbert_assist/test/allbert_assist/resources/operation_class_test.exs
  apps/allbert_assist/test/allbert_assist/security/permission_gate_test.exs
  apps/allbert_assist/test/allbert_assist/runtime/redactor_test.exs
  apps/allbert_assist/test/allbert_assist/artifacts/bounds_test.exs
  apps/allbert_assist/test/allbert_assist/settings_test.exs`
  (`app_env_serial` for Settings/Policy, `home_fs_serial` for bounds fixtures).
- **M3:** `mix test
  apps/allbert_assist/test/allbert_assist/actions/artifact_actions_test.exs
  apps/allbert_assist/test/allbert_assist/artifacts/gc_test.exs
  apps/allbert_assist/test/allbert_assist/settings_test.exs
  apps/allbert_assist/test/allbert_assist/actions/registry_test.exs
  apps/allbert_assist/test/allbert_assist/artifacts/store_test.exs
  apps/allbert_assist/test/allbert_assist/artifacts/metadata_index_test.exs
  apps/allbert_assist/test/allbert_assist/artifacts/bounds_test.exs`
  (`app_env_serial`, `home_fs_serial`).
- **M4:** `mix test
  apps/allbert_assist/test/allbert_assist/artifacts/thread_links_test.exs
  apps/allbert_assist/test/allbert_assist/actions/artifact_actions_test.exs
  apps/allbert_assist/test/allbert_assist/conversations_test.exs`
  (`db_serial`, `home_fs_serial`).
- **M5:** `mix test
  apps/allbert_assist/test/allbert_assist/artifacts/backfill_test.exs
  apps/allbert_assist/test/allbert_assist/actions/transcribe_voice_test.exs
  apps/allbert_assist/test/allbert_assist/actions/generate_image_test.exs
  apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs`
  (`home_fs_serial`, `liveview_serial` for web upload retention).
- **M6:** `mix test
  apps/allbert_assist/test/allbert_assist/artifacts/ingestion_sensor_test.exs`
  (`global_process_serial`, `home_fs_serial`). The test must start the
  supervised `Jido.Sensor.Runtime` with deterministic fixture events.
- **M7:** `mix test
  apps/allbert_assist/test/security/v050_artifact_store_eval_test.exs
  apps/allbert_assist/test/security/security_eval_case_test.exs
  apps/allbert_assist/test/mix/tasks/allbert_test_task_test.exs`, then
  `mix allbert.test release.v050` (`security_eval_serial`, release evidence).

## Milestones

### M1: Content-Addressable Object Store + Metadata Index

Status: implemented on 2026-06-08.

Implementation tasks:

- [x] Add `artifacts_root/0` to `AllbertAssist.Paths` (escape-hatch precedence:
  app-root config → `configured(:artifacts_root)` → `ALLBERT_ARTIFACTS_ROOT` →
  `<ALLBERT_HOME>/artifacts`), wire it through `ensure_home!/0`, and expose it
  via `Runtime.Paths` (`root/1`, `@type root_name`, `roots/0`).
- [x] Implement the object store: SHA-256 hashing (whole-binary and streaming),
  lowercase Base16, two-level shard layout, atomic temp-write + `File.rename`,
  idempotent dedup, and read-back-by-hash.
- [x] Implement the markdown-first metadata sidecar index (Locked Decision 5
  allow-list) plus an in-memory `sha256 → metadata` lookup index, reusing the
  `Marketplace.Bundle` `sha256:` helpers where useful.
- [x] Write ADR 0053 (store identity/layout/dedup/retention/security + the
  build-vs-library decision) and the ADR 0042 artifact resource class amendment.

Acceptance criteria: identical content dedups to one object; atomic writes leave
no partial objects on crash; metadata round-trips through the sidecar; the
`artifacts/` root is a registered Home root; ADRs accepted.

Automated tests: object store (hash/shard/atomic/dedup), metadata index,
`Paths`/`Runtime.Paths` root registration.

Evidence: `mix test apps/allbert_assist/test/allbert_assist/paths_test.exs
apps/allbert_assist/test/allbert_assist/runtime/paths_test.exs
apps/allbert_assist/test/allbert_assist/artifacts/store_test.exs
apps/allbert_assist/test/allbert_assist/artifacts/metadata_index_test.exs` passed
on 2026-06-08 with 21 tests, 0 failures.

Request-flow handoff: document the store contract and identity model.

### M2: Resource Scheme, Permissions, Redaction, Bounds

Status: implemented on 2026-06-08.

Implementation tasks:

- [x] Add `artifact://sha256/<hex>` to `ResourceURI`: an `artifact/1` / `artifact!/1`
  mint helper (bare scheme name, per the non-capture `workflow/1`/`plan_run/1`
  precedent), a `normalize_parsed` clause + validator (hex-id guard
  `^[a-f0-9]{64}$`), a private `derive/2` clause matching `host: "sha256"` and
  returning origin kind `:artifact_store`, and a `scope_uri/4` clause if the
  artifact participates in resource grants.
- [x] Add `:artifact_read`/`:artifact_write`/`:artifact_delete` permission classes
  (Security.Policy `@permission_settings` → `permissions.artifact_*`) with
  `safety_floor` clauses per Locked Decision 6, and the matching operation
  classes + origin kind in `OperationClass`.
- [x] Extend `Runtime.Redactor` with an `artifacts` surface
  (`redact_artifact_metadata/1` allow-list + `redact_artifact_resource_uri/1`),
  ensuring raw bytes/paths never enter traces and `content_sha256` stays the
  trace-safe identity.
- [x] Add ingest bounds: enforce `artifacts.max_bytes` and
  `artifacts.allowed_mime`/`allowed_types` before durable write. M2 implements
  the enforcement point (`Artifacts.Bounds` + `Artifacts.put/3`) with default,
  option, and settings-map inputs; M3 wires the full persisted `artifacts.*`
  Settings Central fragment.

Acceptance criteria: malformed artifact URIs rejected; floors match Locked
Decision 6; oversized/disallowed artifacts denied before write; redaction covers
artifact bytes/paths.

Automated tests: ResourceURI, OperationClass, Security.Policy, Redactor, bounds.

Evidence: `mix test
apps/allbert_assist/test/allbert_assist/resources/resource_uri_test.exs
apps/allbert_assist/test/allbert_assist/resources/operation_class_test.exs
apps/allbert_assist/test/allbert_assist/security/permission_gate_test.exs
apps/allbert_assist/test/allbert_assist/runtime/redactor_test.exs
apps/allbert_assist/test/allbert_assist/artifacts/bounds_test.exs
apps/allbert_assist/test/allbert_assist/settings_test.exs` passed on 2026-06-08
with 90 tests, 0 failures. M1/M2 artifact/path regression
(`paths_test`, `runtime/paths_test`, `store_test`, `metadata_index_test`,
`bounds_test`) also passed with 24 tests, 0 failures.

Request-flow handoff: document artifact authority invariants.

### M3: Settings Fragment + Actions + Retention/GC

Status: implemented on 2026-06-08.

Implementation tasks:

- [x] Add the remaining `artifacts.*` settings fragment (`schema_version: 1`) with
  defaults (enabled off, retention off), bounds defaults
  (`max_bytes`, `allowed_mime`, `allowed_types`), safe-write keys, and
  invariants; register it via core fragments and the known-keys list. M2 already
  added the artifact permission settings and the pre-write bounds enforcement
  point.
- [x] Add `put_artifact`/`get_artifact`/`list_artifacts`/`delete_artifact` registered
  actions (`use AllbertAssist.Action`, registered in `Actions.Registry`, run
  through `Runner.run/3`), with `delete` carrying `confirmation: :required`.
- [x] Add the core `artifact_doctor` read action consumed later by v0.50b.
- [x] Implement retention honoring + a supervised mark-and-sweep GC reconciling the
  metadata index against on-disk objects, with operator-policy-driven removal.

Acceptance criteria: settings invariants enforced at write time; actions resolve
through the Registry/Runner; delete confirms; GC removes orphans without touching
retained artifacts.

Automated tests: settings validation, `put_artifact`/`get_artifact`/
`list_artifacts`/`delete_artifact`, GC reconcile.

Evidence: `mix test
apps/allbert_assist/test/allbert_assist/actions/artifact_actions_test.exs
apps/allbert_assist/test/allbert_assist/artifacts/gc_test.exs
apps/allbert_assist/test/allbert_assist/settings_test.exs
apps/allbert_assist/test/allbert_assist/actions/registry_test.exs
apps/allbert_assist/test/allbert_assist/artifacts/store_test.exs
apps/allbert_assist/test/allbert_assist/artifacts/metadata_index_test.exs
apps/allbert_assist/test/allbert_assist/artifacts/bounds_test.exs` passed on
2026-06-08 with 73 tests, 0 failures. `mix compile --warnings-as-errors` passed.

Request-flow handoff: document the action + retention flow.

### M4: Provenance And Thread Linking

Status: complete; focused-verified on 2026-06-08.

Implementation tasks:

- Added the `artifact_thread_links` join table (Repo migration under
  `apps/allbert_assist/priv/repo/migrations/`): string PK, `artifact_sha256`,
  `thread_id`, `message_id` (nullable), `role` (`created_by`/`referenced_by`),
  `user_id`, `metadata`, timestamps; indexes on `[:artifact_sha256]` and
  `[:thread_id, :user_id]`, and `[:user_id, :artifact_sha256]`. The primary key
  is deterministic from `{sha256, user_id, thread_id, message_id ||
  input_signal_id || "thread", role}` so repeated puts are idempotent.
  `thread_id`/`message_id` are plain strings, not FKs. The migration also adds
  `conversation_messages_input_signal_idx` for message-precise lookup.
- Recorded links at put-time from `context.request` (`thread_id`, `user_id`,
  `session_id`; message via `request.input_signal_id`, resolved by
  `{user_id, thread_id, input_signal_id}` when the conversation message row
  exists). If the message lookup misses, record the thread-level link with
  `message_id: nil` and `metadata.input_signal_id`, and denormalize only the
  bounded originating thread/message/input-signal summary into the markdown
  sidecar.
- Added `list_artifacts` by-thread filtering and a reverse `artifact_threads`
  lookup, as Ecto queries over the join table (user-scoped, role-filtered),
  mirroring `Conversations.list_messages/2` / `Objectives.list_objectives/2`.

Acceptance criteria met: a `put` inside a thread records a `created_by` link;
listing by thread returns it; the reverse lookup returns the thread; repeating
the same put does not duplicate link rows; a thread id grants no permission
(read still resolves through `:artifact_read`).

Automated evidence: `mix test
apps/allbert_assist/test/allbert_assist/artifacts/thread_links_test.exs
apps/allbert_assist/test/allbert_assist/actions/artifact_actions_test.exs
apps/allbert_assist/test/allbert_assist/conversations_test.exs` passed on
2026-06-08 with 21 tests, 0 failures. `mix test
apps/allbert_assist/test/allbert_assist/actions/registry_test.exs` passed with
9 tests, 0 failures. `mix compile --warnings-as-errors` passed.

Request-flow handoff: provenance link + by-thread query documented; M5 has
since completed media backfill and retention-path ingestion.

### M5: Media Backfill And Retention-Path Ingestion

Status: complete; focused-verified on 2026-06-08.

Implementation tasks:

- Formalized `<ALLBERT_HOME>/audio`, `<ALLBERT_HOME>/images`, and
  `<ALLBERT_HOME>/generated_images` as real Home roots. The migration reads the
  existing legacy-root setting keys `voice.audio.retention_root`,
  `vision.media.retention_root`, and `image.generation.retention_root`.
  `AllbertAssist.Artifacts.Backfill` migrates the **retained** branch
  of v0.48 voice captures, v0.49 vision input media, and v0.49 generated-image
  outputs into the CAS, deduplicating pre-existing identical files.
- Added retained-media ingestion hooks so generated-image retention, workspace
  microphone retention, and workspace image-input retention write durable bytes
  through Artifacts Central instead of the legacy media roots. Voice
  transcription keeps a named temp scratch file only for the extension-sensitive
  transcription path, then removes it.
- Historical Browser cache files are not backfilled in M5. Approved browser-found
  artifacts can use `put_artifact` in future flows, but Browser cache
  lifecycle/sweep policy remains plugin-owned until a later plan says
  otherwise.

Acceptance criteria met: retained media is content-addressed and deduplicated;
ephemeral scratch is unaffected; existing voice/vision retention tests still
pass; no user data is lost during migration; historical Browser cache files are
neither deleted nor silently migrated.

Automated evidence: `mix test
apps/allbert_assist/test/allbert_assist/artifacts/backfill_test.exs
apps/allbert_assist/test/allbert_assist/actions/transcribe_voice_test.exs
apps/allbert_assist/test/allbert_assist/actions/generate_image_test.exs
apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs`
passed on 2026-06-08 with 14 app tests and 68 workspace tests, 0 failures.
`mix test apps/allbert_assist/test/allbert_assist/paths_test.exs
apps/allbert_assist/test/allbert_assist/runtime/paths_test.exs` passed with
12 tests, 0 failures. `mix compile --warnings-as-errors` passed.

Request-flow handoff: migration + ingestion path documented; M6 has since
landed the first supervised ingestion sensor.

### M6: First Jido Ingestion Sensor

Status: complete; focused-verified on 2026-06-08.

Implementation tasks:

- Added the codebase's first supervised `Jido.Sensor` (no existing precedent) to
  auto-ingest durable-retention writes into the CAS under supervision. Define it
  with `use Jido.Sensor`, start it through `Jido.Sensor.Runtime`, and emit
  ingestion-request signals through the existing signal bus. The implemented
  path is `AllbertAssist.Artifacts.IngestionSupervisor` ->
  `AllbertAssist.Artifacts.IngestionConsumer` + supervised
  `Jido.Sensor.Runtime` running `AllbertAssist.Artifacts.IngestionSensor`.
- Routed retained-media writes through the same core `put_artifact` path used
  by actions. `MediaRetention.put/4` requests a sensor emission, waits for the
  dispatch target to receive/publish the redacted signal, and then calls
  `Actions.Runner.run("put_artifact", ...)` in the caller process so permission,
  settings, bounds, redaction, provenance, and test DB sandbox ownership remain
  on the existing action boundary.
- Wired a dispatch target for emitted signals (pid/registered process or
  `Jido.Signal.Dispatch` config) so `allbert.artifact.ingest_requested` reaches
  the core consumer; the runtime context sets `agent_ref` to
  `AllbertAssist.Artifacts.IngestionConsumer` and does not rely on default
  emission without a target.
- The sensor is **advisory ingestion only**: it never grants authority, never
  auto-promotes to memory, and honors `artifacts.enabled` + retention policy via
  the core action path.

Acceptance criteria met: the sensor ingests a fixture durable write into the CAS;
disabling `artifacts.enabled` disables ingestion; the sensor adds no authority;
it runs cleanly under the supervision tree; emitted signals contain only
redacted metadata.

Automated evidence: `mix test
apps/allbert_assist/test/allbert_assist/artifacts/ingestion_sensor_test.exs`
passed on 2026-06-08 with 4 tests, 0 failures. Retained-media regressions also
passed: `mix test
apps/allbert_assist/test/allbert_assist/artifacts/backfill_test.exs
apps/allbert_assist/test/allbert_assist/actions/generate_image_test.exs` passed
with 10 tests, 0 failures; `mix test
apps/allbert_assist/test/allbert_assist/actions/transcribe_voice_test.exs`
passed with 4 tests, 0 failures; `mix test
apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs`
passed with 68 tests, 0 failures. `mix compile --warnings-as-errors` passed.

Request-flow handoff: sensor ingestion flow documented; M7 has completed the
security eval, release gate, version, changelog, and docs closeout.

### M7: Security Evals, Docs, Closeout

Status: complete; `release.v050` passed on 2026-06-08 and was re-run during
the 2026-06-09 post-implementation static-gate remediation.

Implementation tasks:

- Added the eval rows from `v0.50-request-flow.md` to `EvalInventory`
  (milestone `:v050`, eval surface `:artifact_store`) with backing assertions
  in `test/security/v050_artifact_store_eval_test.exs`.
- Shipped `mix allbert.test release.v050` (fixture artifacts of several types,
  no external service) with task usage/run clause, `@release_v050_steps`,
  evidence cleanup, secret scan, and task tests. Reproducible evidence writes to
  `<ALLBERT_HOME>/release_evidence/v050/`.
- Added `docs/operator/artifacts-central.md`,
  `docs/developer/artifact-store.md`, and the security-hardening artifact
  retention/redaction section. Ran the M7 Closeout Discipline.

Acceptance criteria met: all evals pass on fixtures while exercising real
store/scheme/redaction/link/action paths; `release.v050` is green and
reproducible; docs agree.

Automated evidence: `mix test
apps/allbert_assist/test/security/v050_artifact_store_eval_test.exs
apps/allbert_assist/test/security/security_eval_case_test.exs
apps/allbert_assist/test/mix/tasks/allbert_test_task_test.exs` passed with
18 tests, 0 failures. The post-remediation focused artifact/security/browser
test bundle passed with 31 tests, 0 failures; the plugin-owned v0.50b browser
test bundle passed with 13 tests, 0 failures from `apps/allbert_assist`.
`mix allbert.test release.v050` passed and wrote
`/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v050/p0-13250/home/release_evidence/v050/release-v050-1780973143.json`;
the evidence directory had no `database is locked`, `SQLITE_BUSY`,
`Exqlite.Connection`, or `DBConnection.ConnectionError` hits. The full
post-remediation `mix allbert.test release` handoff also passed with Dialyzer
reporting `Total errors: 0, Skipped: 0, Unnecessary Skips: 0`; evidence:
`/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release/p0-13250/home/release_evidence/gates/release-2026-06-09T03_32_08Z.json`.

#### M7 Closeout Discipline

Single-purpose closeout commit; complete only when every box is checked:

1. `mix.exs` version → `0.50.0`.
2. child app mix versions → `0.50.0`.
3. `AllbertAssist.App.CoreApp.version/0` → `"0.50.0"`.
4. `CHANGELOG.md` `## v0.50.0` section citing the evals + `release.v050`.
5. `mix allbert.test` help lists `release.v050`.
6. plan + request-flow statuses → implemented as `0.50.0`.
7. ADR 0053 accepted (amended to point at ADR 0054); ADR 0054 accepted; ADR 0042
   artifact amendment accepted.
8. roadmap, vision, agent-context-map, README, operator/developer/security docs
   match implementation; v0.50b still points at the core read actions and v0.52
   still points to the shared channel substrate.
9. media-root reconciliation verified: retained v0.48/v0.49 media is in the CAS,
   ephemeral scratch unchanged, no orphaned legacy roots.
10. `git diff --check` clean for the doc/code closeout diff.

Operator closeout test:

```sh
mix allbert.test release.v050
# evidence: <ALLBERT_HOME>/release_evidence/v050/release-v050-<ts>.json
```

## Documentation Deliverables

- Operator artifacts guide (upload, list, retention, removal, by-thread lookup).
- Developer artifact-store guide (CAS layout, identity, actions, links, sensor).
- Security-hardening artifact retention/redaction section.
- ADR 0053 content-addressable artifact store (amended to reference ADR 0054).
- ADR 0054 artifact provenance + browser-surface split.
- ADR 0042 artifact resource class amendment.
- Roadmap/vision/agent-context-map v0.50 + v0.50b reconciliation.
- (The operator/developer browsing-repository guide ships with v0.50b.)

## Definition Of Done

v0.50 is done when:

- a type-agnostic content-addressable store exists under
  `<ALLBERT_HOME>/artifacts`, addressing artifacts by `artifact://sha256/<hex>`
  with deduplicated atomic object storage and a trace-safe markdown-first
  metadata index;
- `put_artifact`/`get_artifact`/`list_artifacts`/`delete_artifact` actions resolve through the Registry/Runner, with
  `delete` confirmation-gated;
- `:artifact_read`/`:artifact_write`/`:artifact_delete` permissions, operation
  classes, floors, and the `artifacts` redaction surface are in place, and
  content-addressed identity never grants permission;
- the `artifacts.*` settings fragment governs enable/retention/bounds/dedup with
  default-off retention;
- artifacts link to the threads/messages that created or referenced them via the
  `artifact_thread_links` join table, populated from `context.request`, with a
  by-thread `list_artifacts` query and reverse lookup, and the link grants no
  authority;
- the retained branch of v0.48 audio, v0.49 vision input media, and v0.49
  generated-image media is migrated into the CAS and those roots are
  formalized, with ephemeral scratch and historical Browser cache untouched;
- the first supervised Jido ingestion sensor auto-ingests durable-retention writes
  advisory-only;
- the v0.50 security eval set + `release.v050` pass on fixtures;
- roadmap/vision/agent-context-map/ADR 0053/0054/0042/CHANGELOG reflect the
  shipped state.

The operator browsing repository (workspace panel, `/apps/artifacts/<sha>` page,
and `mix allbert.artifacts` CLI) is **not** part of v0.50 — it ships in the
**v0.50b Artifacts Browser** plugin/app over the core read actions delivered
here.

Release-state notes: the store is owned in-tree per ADR 0053; no third-party
CAS/upload dependency is added in v0.50. `Capsule` remains a parked option for a
future pluggable-backend need only.
