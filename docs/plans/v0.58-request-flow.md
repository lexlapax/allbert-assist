# Allbert v0.58 Request Flow — Surface Consistency, Settings Enforcement & Web Design System

Status: released as `v0.58.0`; **M13.1A-G audit remediation, guided M14 manual
operator validation, final implementation audit, and M15 closeout are complete**.
The 2026-06-25 Chrome S3 corrective browser pass is historical pre-remediation
evidence and remains regression context, not the final M14 validation result.

Implementation details live in `docs/plans/v0.58-plan.md`.

## RESUME POINT
M0-M15 are complete. The contract barrier is in place,
`AllbertAssist.Surface.Renderer` is the shared runtime-response render path for
channel/TUI adapters, CLI output, public-protocol mappings, and web response
display, and `AllbertAssist.Surface.EventRecorder` records web, Mix, and
public-protocol ingress/rejection/error events to the shared audit spine by
`surface_id`, including M13.1A ACP prompt-guard and MCP `read_resource` rejection
events plus M13.1E ACP sibling-method and MCP tool pre-dispatch rejection events.
Workspace LiveView resolves identity through the channel identity map,
derives its session through the shared channel seam, and reads its Settings
snapshot through a registered action; M13.1A also removed the residual web
`Settings.get/1` reads in layout/template code. M13.1E broadened the Settings
guard beyond the original key allowlist and widened the web drift guard across
production web source. M13.1F adds an explicit guard for unknown production
`System.get_env("ALLBERT_*")` reads unless they are classified as infrastructure.
`AllbertAssist.Surfaces.ContextBuilder`,
`AllbertAssist.Actions.Helper`, and `AllbertAssist.Actions.ErrorExtraction`
centralize surface action context, completed-action handling, and action-response
error extraction across web components, Mix tasks, public protocol, channel
callbacks, and TUI slash-command contexts. Settings Central owns the known
operator-tunable bypasses (`runtime.trace_default`, trace recent-entry bound,
browser host resolver rules, intent decisive confidence, Active Memory candidate
bounds, artifact ingestion timeout), and the Credo guard catches seeded ad-hoc
operator config reads. The built-in web tokens live at the global root; the root
layout emits Settings-backed `data-theme`, `data-high-contrast`, and
`data-reduce-motion`; and `docs/developer/web-design-system.md` records the token
and UX-language contract. The design-system registry resolves catalog variants
from props, the shared modal pattern owns dialog/ARIA/FocusTrap/Escape/click-away
wiring, Jobs/Objectives render through declared catalog atoms inside
`Layouts.operator_shell/1`, `/workspace` is chat-primary with Conversations,
modal ephemerals, and a canvas drawer, and M13.1B accepted `/` as a bounded thin
landing page that still emits the shell contract, consumes tokens, and uses
variant-registry buttons. Raw `btn` drift in the M13.1B-touched operator surfaces
is covered by a production web-source regression guard. M13.1E implements shared
status/error/loading/drawer/table-list primitives and routes the obvious page-local
callouts through them without changing the layout design. M13.1F closes the
drawer/table split by adding root-safe `Patterns.*_class`/`Patterns.*_attrs`
helpers and wiring stateful `UtilityDrawer`, `Table`, `Row`, and `Column` atoms
through the same contract while preserving literal LiveComponent roots. The
workspace offline banner also routes through the shared status-callout contract,
with the static fallback shell mirroring the marker in plain HTML. M13.1G
operator-validation precision closure makes M14 deterministic: it uses an
explicit validation port, stale-server detection, seed data for the Objective
detail and gated-promotion browser checks, exact public-protocol base URLs, and a
bounded post-session event export instead of relying only on the TUI recent-events
window. Intents, Models, and Surface
Policy panels are catalog components backed by v0.56 registered action DTOs. M11 added
the Settings Central-backed `surface_policy.*` namespace, internal
`surface_policy_read/update` actions, one redacted policy DTO, raw-report
explicit-affordance gating, and row/count bounds for the currently covered
operator reports. M13.1C widened policy coverage to Intents and `model_doctor`
reads, with Mix/TUI/web operator entrypoints passing the explicit report
affordance. M12 added
shared helper modules for mixed-key map access, deep key stringification, bounded
limit clamping, and Settings DTO boolean lookup. M13 added the `:v058` eval rows
and `mix allbert.test release.v058` deterministic gate; M13.1C hardened the eval
with behavioral assertions for renderer redaction, rejection events, DTO
redaction, and no-authority operator panels. M13.1E adds the intent-task rejected
path to the release lane; M13.1F adds drawer/table contract parity, settings-guard,
and offline-pattern checks around the third-pass remediation. M13.1G tightens the
M14 operator instructions so browser and public-protocol validation use an
explicit fresh server, deterministic seeded objective/intent-review data, exact
expected output, and a bounded post-session event export. Guided M14 validation on
2026-06-25 passed S0-S6 after correcting three checklist defects found during the
run: MCP tool listing now targets the HTTP surface, MCP missing-resource rejection
captures the expected HTTP 404 body, and the S6 evidence secret-scan regex compiles
under ripgrep. The OpenAI-compatible smoke remains text-only; tool-call payloads are
outside this validation's public-surface contract. M15 accepted the v0.58 ADRs and
amendments, set version metadata to `0.58.0`, reconciled README/CHANGELOG/roadmap
and context docs, and left the release tag as a separate action per project
convention.
Next action: use this request flow only for release replay, regression evidence,
or future audit reference; new implementation work moves to v0.59.

## Source Documents
- `docs/plans/v0.58-plan.md`
- `docs/adr/0073-cross-surface-contract.md`, `docs/adr/0074-web-design-system-and-ux-language.md` (Accepted)
- `docs/adr/0016-*` (channel boundary, v0.58 amendment), `docs/adr/0030-*` (catalog
  boundary, v0.58 amendment), `docs/adr/0070-*` (operator actions, v0.58 amendment)
- `docs/adr/0004-*` / `docs/adr/0031-*` (Settings Central + v0.58 enforcement note)
- `docs/adr/0023-*` (substrate kept), `docs/adr/0024-*` (live layout, v0.58 revision),
  `docs/adr/0015-*` (Surface DSL kept); render primitives
  (`[:typed_command, :list, :button, :link]`) are defined by `docs/adr/0016-*`
- v0.56 DTO handoff (panels), v0.56 M14a surface-summary boundary
- Code seams: `runtime.ex`, `runtime/response.ex`, `channels/identity.ex`,
  `surface/catalog.ex`, `workspace/renderer.ex`, `live/workspace_live.ex`,
  `live/jobs_live.ex`, `live/objective_live.ex`, `assets/css/app.css`,
  `plugins/allbert.tui/...`, `plugins/allbert.telegram/...`, `mix/tasks/allbert.ask.ex`,
  `public_protocol/*`, `settings/schema.ex`
- UX references: ChatGPT, Claude, Hermes (https://hermes-agent.nousresearch.com/)

## Locked Decisions
Recorded in `v0.58-plan.md`: full consolidation; one `Surface.Renderer`; uniform
event/audit by `surface_id`; web reads via the ADR 0070 action layer with
`Identity.resolve`; no operator-tunable config outside Settings Central (+ CI guard);
a documented web design system (tokens + variant registry + pattern library + one
shell + catalog-for-all-pages); no rebuild / no internal rename / no new routes; the
chat-primary re-layout, the v0.56 DTO panels, and the surface-policy layer preserved;
all redundancy consolidated; v0.59 keeps migration / param-contract /
export-import / security sweep.

## Cross-Surface Data Flow (the one path, ADR 0073)
```text
inbound (web keypress/submit | TUI line | channel message | mix arg | MCP/ACP call)
  -> normalize -> Channels.Identity.resolve (list-shaped identity map; no hardcoded "local")
  -> session derive (provider channel id OR surface_id: live_view|cli|mcp_stdio|mcp_http|acp|openai_api)
  -> record inbound event (dedupe + trace) on the shared event spine
  -> user turn:  Runtime.submit_user_input/1
     operation:  Actions.Runner.run/3   (operator reads: :internal/:read_only actions)
  -> Security Central gate (unchanged authority) + Settings Central (only config authority)
  -> typed Runtime.Response (model_payload | surface_payload)
  -> ONE Surface.Renderer.render_response(response, surface_descriptor)
       descriptor = render primitives (ADR 0016); model sees model_payload only; memory persists model_payload only
  -> surface presents (web catalog component | TUI scrollback | channel message | mix stdout | MCP payload)
  -> record rejection/error event uniformly (not discarded)
  -> [web] panels render v0.56 DTOs via registered actions; surface policy governs report shape
```

## Web Render Flow (ADR 0074)
```text
every operator page -> ONE shared app shell (nav + header/switchers + mobile shellbar)
  -> WorkspaceRenderer over the unified catalog (ADR 0030) — workspace, Jobs, Objectives,
     Intents, Settings/Models, Surface-Policy all render through catalog atoms
  -> components consume global design tokens (color/type/spacing/motion scales)
  -> variants are prop-driven (variant registry); modals/loading/empty/error via the pattern library
  -> chat primary (timeline+composer) | collapsible Conversations rail | canvas launcher/drawer
  -> ephemeral surfaces -> pattern-library modal (role=dialog, FocusTrap, escape/click-away)
```

## Authority Invariants
- One spine: surfaces call only `Runtime.submit_user_input/1` + `Actions.Runner.run/3`;
  no direct `Confirmations`/`Settings.Store`/business-logic reads (ADR 0073).
- Security Central is the only authority; surface policy, panels, and the design
  system grant no authority, make no `:internal` action routable, lower no
  confirmation floor, and grant no egress.
- Catalog is the rendering boundary on every page; no model-/data-generated UI.
- Settings Central is the only config authority; no operator-tunable config is read
  ad-hoc (CI guard).
- Identity via `Identity.resolve` on every surface; no hardcoded `"local"`.
- "Conversations" is a UI-string change only; internal identifiers unchanged.
- Descriptor YAML remains routing vocabulary; it does not own report shape.

## Security / Eval Rows (`:v058`)
See `v0.58-plan.md` for the full list: surface-renderer-unified-parity; surface-event-
audit-parity; web-reads-action-backed; web-identity-resolved; surface-invocation-shared;
settings-no-bypass; design-tokens-global; component-variant-registry; pattern-library-
a11y; all-pages-catalog-shell; chat-primary-default; ephemeral-renders-as-modal-dialog;
conversations-relabel-ui-only; fragment-emission-hmac-validated; launcher-selection-
view-only; mobile-single-column-and-reduced-motion; intents-panel-v056-dto-parity;
intents-panel-gated-promotion; models-panel-v056-dto-redaction; surface-policy-raw-
report-affordance; surface-policy-settings-central; surface-policy-no-authority-grant;
redundancy-consolidation-no-regression.

## Smoke Checklist
- Every surface renders through the one renderer; web/Mix/MCP record events.
- Web resolves identity through the map and reads confirmations/settings via actions.
- No operator-tunable config is read ad-hoc (guard passes).
- `/`, chat-primary, ephemerals, canvas drawer, Jobs/Objectives, and operator
  panels render through the shell/token/catalog contract; `/` is the accepted
  thin-landing shape from M13.1B.
- The three panels render v0.56 DTOs with no secret/endpoint leak; surface policy
  edits are audited and fail-closed.
- Same operation across web/TUI/CLI/MCP returns the same redacted DTO.

## Operator Validation
M13.1G is complete, and `release.v058` passed after remediation with evidence at
`/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v058/p0-13380/home/release_evidence/v058/release-v058-1782434923.json`.
Guided M14 manual validation passed on 2026-06-25. Retain **durable, redacted** evidence under
`$HOME/.allbert-release-evidence/v058`; commit only sanitized closeout notes.
Checklist corrections discovered during the run are already incorporated here:
`mix allbert.mcp_server tools list --surface mcp_http`, expected MCP
missing-resource HTTP 404 capture, and the corrected S6 secret-scan regex. Screenshot
capture is optional formal/automated evidence for this guided pass; live operator
failure reporting is sufficient for manual validation.

### Pre-S0 M13.1 Completion Check
M13.1G and M14 are complete. Before rerunning S0-S6 or using this checklist as
closeout evidence, confirm:

- M13.1A R1-A through R1-D remain implemented: profile DTOs are redacted at source,
  profile-read exposure is reconciled in ADR 0070/code, ACP/MCP pre-dispatch
  rejections record events, and residual web `Settings.get/1` reads are gone with
  the Credo guard covering web-lib regressions.
- M13.1B/E/F R2-A through R2-C remain implemented: `/` satisfies
  the shell/token/registry contract as a thin landing page, raw button sites in
  production web source use the variant registry/catalog button atoms, the `/jobs`
  catalog action row remains horizontal, and the shared status/error/loading/
  drawer/table-list pattern baseline is shipped and documented. Stateful
  drawer/table catalog atoms consume the root-safe shared helper contract, not a
  mirrored copy.
- M13.1C R3-A/R3-B are implemented: `:v058` has behavioral assertions and
  surface-policy coverage is widened to the named operator reads with explicit
  operator-report affordance gating.
- `MIX_ENV=test mix allbert.test release.v058`, `MIX_ENV=test mix allbert.test docs`,
  warning-as-error compile, Credo, drawer/table parity tests, settings-guard tests,
  offline-banner tests, and intent task help/behavior tests are clean after the
  remediation.
- The 2026-06-25 Chrome S3 corrective evidence is treated only as regression
  context; any web surface touched by M13.1 was rechecked in Chrome during M14 and
  must be rechecked again for any rerun.
- The M14 browser/public-protocol validation uses the `V058_PORT` exported in S1;
  do not validate against a pre-existing `localhost:4000` server unless S1
  explicitly chose that port and the stale-server check passed.

### S0 - Deterministic Gate
```sh
MIX_ENV=test mix allbert.test release.v058
MIX_ENV=test mix allbert.test docs
MIX_ENV=test mix compile --warnings-as-errors
MIX_ENV=test mix credo --strict
```

Expected: `release.v058` exits 0 and prints an evidence JSON path; the docs gate,
warning gate, and Credo settings-no-bypass guard are clean. If `mix help
allbert.test` does not list `release.v058`, stop: the checkout is stale or M13 was
not applied.

### S1 - Disposable Validation Home
```sh
export V058_HOME="$(mktemp -d /tmp/allbert-v058.XXXXXX)"
export ALLBERT_HOME="$V058_HOME"
export ALLBERT_HOME_DIR="$V058_HOME"
export V058_PORT="${V058_PORT:-4062}"
export V058_BASE_URL="http://127.0.0.1:${V058_PORT}"
export V058_EVIDENCE_DIR="$HOME/.allbert-release-evidence/v058"
export V058_SHOTS="$V058_EVIDENCE_DIR/screenshots"
mkdir -p "$V058_EVIDENCE_DIR" "$V058_SHOTS" "$V058_HOME/db"
if lsof -nP -iTCP:"$V058_PORT" -sTCP:LISTEN >/dev/null; then
  lsof -nP -iTCP:"$V058_PORT" -sTCP:LISTEN
  echo "V058_PORT=$V058_PORT is already in use; stop that server or choose another V058_PORT" >&2
  false
fi
mix allbert.ecto.migrate --quiet
mix allbert.settings set channels.tui.identity_map '[{"external_user_id":"default","user_id":"local","enabled":true}]'
mix allbert.settings set external_services.enabled true
mix allbert.settings set external_services.allowed_hosts '["example.com"]'
mix allbert.settings set external_services.allowed_paths '["/"]'
mix allbert.settings set mcp_server.enabled true
mix allbert.settings set mcp_server.streamable_http.enabled true
mix allbert.settings set mcp_server.tools_enabled direct_answer,get_public_call_result
mix allbert.settings set openai_api.enabled true
mix allbert.settings set openai_api.models_enabled local
mix allbert.settings set openai_api.tools_enabled direct_answer,get_public_call_result
test -d "$ALLBERT_HOME" && echo "home ok"
printf 'V058_HOME=%s\n' "$V058_HOME"
printf 'V058_PORT=%s\n' "$V058_PORT"
printf 'V058_BASE_URL=%s\n' "$V058_BASE_URL"
```

Expected: migration exits 0; `home ok` prints; TUI identity mapping has one enabled
`default -> local` entry; the chosen `V058_PORT` is not already listening; external
request validation is limited to `https://example.com/`; MCP HTTP and
OpenAI-compatible HTTP expose only public-safe tools. No surface may rely on
hardcoded `"local"`.

### S2 - Start Phoenix On The Disposable Home
```sh
ALLBERT_HOME="$V058_HOME" ALLBERT_HOME_DIR="$V058_HOME" PORT="$V058_PORT" MIX_ENV=dev mix phx.server
```

Expected in the server shell: Phoenix starts on the selected port and prints a
line equivalent to `Running AllbertAssistWeb.Endpoint ... at 127.0.0.1:$V058_PORT`
or `Access AllbertAssistWeb.Endpoint at http://localhost:$V058_PORT`. Stop if the
server reports a different port or a different Allbert Home.

In another shell:

```sh
export V058_HOME="/tmp/allbert-v058.REPLACE_WITH_S1_SUFFIX"
export ALLBERT_HOME="$V058_HOME"
export ALLBERT_HOME_DIR="$V058_HOME"
export V058_PORT="REPLACE_WITH_S1_PORT"
export V058_BASE_URL="http://127.0.0.1:${V058_PORT}"
export V058_EVIDENCE_DIR="$HOME/.allbert-release-evidence/v058"
export V058_SHOTS="$V058_EVIDENCE_DIR/screenshots"
curl -fsS -o "$V058_EVIDENCE_DIR/s2-workspace.html" -w "%{http_code}\n" "$V058_BASE_URL/workspace"
rg 'id="workspace-shell"|data-offline-shell-url="/workspace-offline.html"' "$V058_EVIDENCE_DIR/s2-workspace.html"
```

Expected: the server boots against `$V058_HOME`; `curl` prints `200`; the `rg`
command prints the workspace shell/offline-shell marker. Leave this exact server
running for S3-S5.

### S3 - Browser Pass (Web Design System)
Seed the deterministic browser fixtures before opening Chrome:

```sh
cat > "$V058_EVIDENCE_DIR/s3-seed.exs" <<'ELIXIR'
alias AllbertAssist.Intent.Router.DescriptorStore
alias AllbertAssist.Objectives

{:ok, review_path} =
  DescriptorStore.put(:review, %{
    app_id: :allbert,
    action_name: "list_channels",
    label: "List channels",
    examples: ["list my channels"],
    synonyms: ["channels"],
    required_slots: [:channel]
  })

{:ok, generated_path} = DescriptorStore.path(:generated, :allbert, "list_channels")
_ = File.rm(generated_path)

{:ok, objective} =
  Objectives.create_objective(%{
    user_id: "local",
    title: "v0.58 Objective Detail Validation",
    objective: "Validate Objective detail page renders through the shared shell and catalog atoms.",
    status: "blocked",
    active_app: "allbert",
    acceptance_criteria: %{"min_completed_steps" => 1}
  })

{:ok, step} =
  Objectives.create_step(%{
    objective_id: objective.id,
    kind: "action",
    status: "blocked",
    stage: "authorize_step",
    candidate_action: "AllbertAssist.Actions.Intent.ListIntentCandidates",
    confirmation_id: "conf_v058_manual_objective"
  })

{:ok, _event} =
  Objectives.create_event(%{
    objective_id: objective.id,
    step_id: step.id,
    kind: "blocked",
    summary: "Waiting for v0.58 manual validation confirmation."
  })

IO.puts("V058_REVIEW_DESCRIPTOR=#{review_path}")
IO.puts("V058_GENERATED_DESCRIPTOR=#{generated_path}")
IO.puts("V058_OBJECTIVE_ID=#{objective.id}")
ELIXIR

mix run "$V058_EVIDENCE_DIR/s3-seed.exs" | tee "$V058_EVIDENCE_DIR/s3-seed-output.txt"
export V058_REVIEW_DESCRIPTOR="$(awk -F= '/^V058_REVIEW_DESCRIPTOR=/{print $2; exit}' "$V058_EVIDENCE_DIR/s3-seed-output.txt")"
export V058_GENERATED_DESCRIPTOR="$(awk -F= '/^V058_GENERATED_DESCRIPTOR=/{print $2; exit}' "$V058_EVIDENCE_DIR/s3-seed-output.txt")"
export V058_OBJECTIVE_ID="$(awk -F= '/^V058_OBJECTIVE_ID=/{print $2; exit}' "$V058_EVIDENCE_DIR/s3-seed-output.txt")"
test -f "$V058_REVIEW_DESCRIPTOR"
test ! -f "$V058_GENERATED_DESCRIPTOR"
test -n "$V058_OBJECTIVE_ID"
: > "$V058_EVIDENCE_DIR/s3-browser-notes.md"
```

Expected: seed output includes `V058_REVIEW_DESCRIPTOR=.../learned/review/allbert/list_channels.yaml`,
`V058_GENERATED_DESCRIPTOR=.../generated/allbert/list_channels.yaml`, and
`V058_OBJECTIVE_ID=obj_...`; the review descriptor exists and the generated
descriptor does not.

Drive `$V058_BASE_URL/` and `$V058_BASE_URL/workspace` in Chrome against the S2
server. For automated/formal evidence, capture screenshots to the named paths and
append notes to `$V058_EVIDENCE_DIR/s3-browser-notes.md` in the form
`PASS <shot-name> - <observed invariant>`. For guided manual validation, the
operator may validate the same invariants live and report only failures.

1. **Home page shell/token contract.** Expected: `/` either renders through
   `Layouts.operator_shell/1` + catalog atoms or through an ADR-accepted thin
   landing shape that still emits the shared shell data contract, consumes tokens,
   and uses variant-registry buttons. There must be no raw hex/slate palette drift
   or raw `btn` classes. Shot: `$V058_SHOTS/00-home.png`.
2. **Chat-primary layout.** Expected: one main chat column with timeline and pinned
   composer, a collapsible **Conversations** rail, and no co-equal canvas pane.
   Shot: `$V058_SHOTS/01-chat-primary.png`.
3. **New/switch Conversation.** Create a conversation, send a turn, switch away and
   back. Expected: the UI string is "Conversations"; streaming renders; history
   persists. Shot: `$V058_SHOTS/02-conversations.png`.
4. **Ephemeral as modal.** In the workspace composer, submit exactly
   `Fetch https://example.com/ from the internet`. Expected: centered approval
   handoff with `role="dialog"`, `aria-modal="true"`, visible text
   `Approval Required`, target `external_network_request`, enabled Approve/Deny
   controls, focus trap, Escape dismissal, and click-away dismissal. Press Escape
   once after the screenshot; expected: the modal closes without approving or
   denying the pending confirmation. Shot: `$V058_SHOTS/03-modal.png`.
5. **Canvas drawer.** Open the canvas from its launcher. Expected: slide-over/drawer
   with existing tiles intact; default view remains chat. Shot:
   `$V058_SHOTS/04-canvas-drawer.png`.
6. **Operator panels.** Open Intents, Settings/Models, and Surface-Policy. Expected:
   Intents shows coverage, source badges, a non-blocking Eval Gate status
   (deferred until explicit refresh or a gate-backed mutation), and review queue;
   Settings/Models shows the recommendation matrix and bounded inventories;
   Surface-Policy shows per-surface/action report mode, bounds, and affordance.
   There must be no secret refs, endpoint URLs, raw prompts, provider bodies, raw
   descriptor/evidence payloads, or raw button-class drift. Shots:
   `$V058_SHOTS/05-intents.png`, `$V058_SHOTS/06-models.png`,
   `$V058_SHOTS/07-surface-policy.png`.
7. **Global design-system conformance.** Toggle dark mode, high contrast, and reduced
   motion; navigate to `$V058_BASE_URL/jobs` and
   `$V058_BASE_URL/objectives/$V058_OBJECTIVE_ID`.
   Expected: tokens, contrast, and reduced-motion apply on both pages; the shared
   shell chrome is present; pages render through catalog atoms. Shots:
   `$V058_SHOTS/08-dark.png`,
   `$V058_SHOTS/09-jobs.png`, `$V058_SHOTS/10-objectives.png`.
8. **Gated mutation.** Navigate to
   `$V058_BASE_URL/workspace?destination=workspace:intents`, find the
   `list_channels` review row, and click the Promote affordance
   (`#workspace-intent-promote-list_channels` in the DOM). Expected: the panel
   shows `gate failed` and `Intent action status: rejected`; the Promote affordance
   remains visible; the review descriptor remains; no generated descriptor is
   committed. Shot: `$V058_SHOTS/11-gated-promotion.png`.

After the browser check, run:

```sh
test -f "$V058_REVIEW_DESCRIPTOR"
test ! -f "$V058_GENERATED_DESCRIPTOR"
rg 'PASS 00-home|PASS 11-gated-promotion' "$V058_EVIDENCE_DIR/s3-browser-notes.md"
```

Expected: both descriptor checks pass. For automated/formal evidence, the notes
file contains the home and gated-promotion observations.

Chrome corrective pass note (2026-06-25): S3 browser validation found and fixed
four browser-visible issues before implementation audit: router-selected external
network requests now preserve prompt URLs for approval handoff; approval handoff
modals support Escape/click-away dismissal without resolving the pending
confirmation; approval handoff state stays visible even when a response status is
not `:needs_confirmation`; and Intents/Models/Surface Policy destinations are
routable by URL. A follow-up S3 pass also made Intents load fast DTOs before the
full eval gate so the LiveView stays connected; the gated-promotion shot proves
the ADR 0071 rejection remains visible and writes no generated descriptor. Durable
evidence lives under `$HOME/.allbert-release-evidence/v058/`. This evidence is
pre-M13.1 regression context; M14 rechecked the touched web surfaces after M13.1,
and any rerun must recapture those checks against current code.

### S4 - CLI And Warm TUI Parity
Capture the CLI oracle, then use one warm TUI session for operator reads. Do not use
cold `mix allbert.ask` for in-session validation.

```sh
mix allbert.settings model-doctor > "$V058_EVIDENCE_DIR/s4-cli-model-doctor.txt"
mix allbert.intent coverage > "$V058_EVIDENCE_DIR/s4-cli-intent-coverage.txt"
mix allbert.model list > "$V058_EVIDENCE_DIR/s4-cli-model-list.txt"
script -q "$V058_EVIDENCE_DIR/s4-tui.log" mix allbert.tui
```

In the TUI, type:

```text
/models
/intents
/events
```

Expected: CLI `model-doctor`, TUI `/models`, and the web Settings/Models panel show
the same per-purpose rows with the same redaction. CLI `intent coverage`, TUI
`/intents`, and the web Intents panel show the same bounded coverage/review/gate
state. TUI `/events` includes event rows for the web, CLI, and TUI activity using
their `surface_id` values. Leave this TUI session open during S5; run S5 commands
from another shell.

### S5 - Public-Protocol Smoke And Internal-Read Denial
Public protocol validation must use public-safe tools. Internal operator reads such
as `model_doctor`, `intent_coverage`, `intent_list_descriptors`, and
`promote_intent_descriptor` must not appear in MCP/OpenAI tool exposure.

```sh
mix allbert.mcp_server tools list --surface mcp_http > "$V058_EVIDENCE_DIR/s5-mcp-tools.txt"
rg 'direct_answer|get_public_call_result' "$V058_EVIDENCE_DIR/s5-mcp-tools.txt"
! rg 'model_doctor|intent_coverage|intent_list_descriptors|promote_intent_descriptor' "$V058_EVIDENCE_DIR/s5-mcp-tools.txt"

export V058_MCP_TOKEN="$(mix allbert.public_protocol token create --surface mcp_http --client v058-mcp | awk -F= '/^token=/{print $2; exit}')"
export V058_OPENAI_TOKEN="$(mix allbert.public_protocol token create --surface openai_api --client v058-openai | awk -F= '/^token=/{print $2; exit}')"
test -n "$V058_MCP_TOKEN"
test -n "$V058_OPENAI_TOKEN"
mix allbert.public_protocol token list --surface mcp_http > "$V058_EVIDENCE_DIR/s5-mcp-token-list.txt"
mix allbert.public_protocol token list --surface openai_api > "$V058_EVIDENCE_DIR/s5-openai-token-list.txt"
```

Expected: tool list contains only public-safe tools configured in S1; the negative
`rg` command prints no matches; token lists redact raw tokens.

MCP HTTP:

```sh
export V058_MCP_URL="$V058_BASE_URL/mcp"
curl -fsS \
  -H "content-type: application/json" \
  -H "x-allbert-client-id: v058-mcp" \
  -H "authorization: Bearer $V058_MCP_TOKEN" \
  -H "mcp-protocol-version: 2025-06-18" \
  -d '{"jsonrpc":"2.0","id":"tools","method":"tools/list"}' \
  "$V058_MCP_URL" > "$V058_EVIDENCE_DIR/s5-mcp-tools.json"
rg '"direct_answer"|"get_public_call_result"' "$V058_EVIDENCE_DIR/s5-mcp-tools.json"
! rg 'model_doctor|intent_coverage|intent_list_descriptors|promote_intent_descriptor' "$V058_EVIDENCE_DIR/s5-mcp-tools.json"

curl -fsS \
  -H "content-type: application/json" \
  -H "x-allbert-client-id: v058-mcp" \
  -H "authorization: Bearer $V058_MCP_TOKEN" \
  -H "mcp-protocol-version: 2025-06-18" \
  -d '{"jsonrpc":"2.0","id":"direct","method":"tools/call","params":{"name":"direct_answer","arguments":{"text":"hello from v0.58 public protocol smoke"}}}' \
  "$V058_MCP_URL" > "$V058_EVIDENCE_DIR/s5-mcp-direct-answer.json"
rg '"isError":false|"direct_answer"|"completed"' "$V058_EVIDENCE_DIR/s5-mcp-direct-answer.json"

curl -sS \
  -H "content-type: application/json" \
  -H "x-allbert-client-id: v058-mcp" \
  -H "authorization: Bearer $V058_MCP_TOKEN" \
  -H "mcp-protocol-version: 2025-06-18" \
  -d '{"jsonrpc":"2.0","id":"missing-resource","method":"resources/read","params":{"uri":"allbert://missing/namespace"}}' \
  -o "$V058_EVIDENCE_DIR/s5-mcp-resource-rejection.json" \
  -w "%{http_code}\n" \
  "$V058_MCP_URL" | tee "$V058_EVIDENCE_DIR/s5-mcp-resource-rejection.status"
rg '^404$' "$V058_EVIDENCE_DIR/s5-mcp-resource-rejection.status"
rg '"error"|not_found|rejected|invalid' "$V058_EVIDENCE_DIR/s5-mcp-resource-rejection.json"
```

OpenAI-compatible HTTP:

```sh
export V058_OPENAI_BASE="$V058_BASE_URL/v1"
curl -fsS \
  -H "x-allbert-client-id: v058-openai" \
  -H "authorization: Bearer $V058_OPENAI_TOKEN" \
  "$V058_OPENAI_BASE/models" > "$V058_EVIDENCE_DIR/s5-openai-models.json"
rg '"object":"list"|"id":"local"|"owned_by":"allbert"' "$V058_EVIDENCE_DIR/s5-openai-models.json"

curl -fsS \
  -H "content-type: application/json" \
  -H "x-allbert-client-id: v058-openai" \
  -H "authorization: Bearer $V058_OPENAI_TOKEN" \
  -d '{"model":"local","user":"v058-openai-user","messages":[{"role":"developer","content":"Be brief."},{"role":"user","content":"Hello from v0.58 public protocol smoke"}]}' \
  "$V058_OPENAI_BASE/chat/completions" > "$V058_EVIDENCE_DIR/s5-openai-chat.json"
rg '"object":"chat.completion"|"role":"assistant"' "$V058_EVIDENCE_DIR/s5-openai-chat.json"
```

Expected: MCP and OpenAI calls produce successful public-surface events, stay within
public-safe tools, and do not expose internal operator reads. The MCP resource-read
negative probe must return a structured rejection and record a `mcp_http` rejection
event after M13.1. ACP guard rejection recording is covered by the M13.1 behavioral
eval unless an ACP stdio manual smoke is run in this pass.

Return to the S4 TUI session and type:

```text
/events
/quit
```

Expected: `/events` includes `surface_id` rows for `live_view`, `cli`, `tui`,
`mcp_http`, and `openai_api`.

### S6 - Post-Session Evidence Checks
```sh
test -s "$V058_EVIDENCE_DIR/s4-cli-model-doctor.txt"
test -s "$V058_EVIDENCE_DIR/s4-cli-intent-coverage.txt"
test -s "$V058_EVIDENCE_DIR/s4-tui.log"
test -s "$V058_EVIDENCE_DIR/s5-mcp-direct-answer.json"
test -s "$V058_EVIDENCE_DIR/s5-mcp-resource-rejection.json"
test -s "$V058_EVIDENCE_DIR/s5-openai-chat.json"
mix run -e 'alias AllbertAssist.Actions.Runner; ctx = %{actor: "local", operator_id: "local", channel: :mix, surface_id: "cli", request: %{operator_id: "local", channel: :mix, source: "v0.58 S6"}}; {:ok, response} = Runner.run("operator_events", %{limit: 50}, ctx); IO.puts(response.message)' > "$V058_EVIDENCE_DIR/s6-events.txt"
rg 'surface_id=live_view' "$V058_EVIDENCE_DIR/s6-events.txt"
rg 'surface_id=cli' "$V058_EVIDENCE_DIR/s6-events.txt"
rg 'surface_id=tui' "$V058_EVIDENCE_DIR/s6-events.txt"
rg 'surface_id=mcp_http' "$V058_EVIDENCE_DIR/s6-events.txt"
rg 'surface_id=openai_api' "$V058_EVIDENCE_DIR/s6-events.txt"
rg 'surface_id=mcp_http.*status=rejected|status=rejected.*surface_id=mcp_http' "$V058_EVIDENCE_DIR/s6-events.txt"
MIX_ENV=test mix credo --strict > "$V058_EVIDENCE_DIR/s6-credo.txt"
rg -n 'token=\[REDACTED\]' "$V058_EVIDENCE_DIR"/s5-*-token-list.txt
! rg -n 'sk-[A-Za-z0-9_-]+|api_key=[A-Za-z0-9._:/-]+|authorization: Bearer [A-Za-z0-9._-]+|bot_token' "$V058_EVIDENCE_DIR"
```

Expected: CLI, TUI, MCP, and OpenAI artifacts are present; the bounded
`s6-events.txt` evidence covers `live_view`, `cli`, `tui`, `mcp_http`, and
`openai_api`, including the MCP resource-read rejection; Credo is clean; token-list
evidence contains redacted tokens; the evidence secret scan finds no raw bearer/API
material. Screenshot artifacts are optional automated/formal evidence for this
guided pass.
Record a sanitized closeout note; do not commit raw screenshots, raw tokens, or
transcripts containing secrets.

### Pass Criteria
- Browser: `/`, chat-primary, modal ephemerals, canvas drawer, panels, `/jobs`, and
  `/objectives` render through the design system with global tokens and a11y.
- Parity: Settings/Models and Intents show the same redacted DTO state across web,
  warm TUI, and CLI; events are recorded with `surface_id`.
- Public protocol: MCP/OpenAI public smokes pass through public-safe tools only;
  internal operator reads are absent from public exposure.
- Settings: every former bypass routes through Settings Central; the guard is clean.
- Authority: panels, policy, and design grant no authority; gated promotion mutates
  nothing; fragment HMAC and view-only launcher invariants hold.
- Durable redacted evidence is retained outside the repo; raw transcripts/screenshots
  are not committed.
- M14 guided manual validation passed on 2026-06-25 after the three S5/S6 checklist
  corrections above; M15 closeout accepted the release record and docs.

## Docs Closeout Checklist
- ADR 0073/0074 Accepted; ADR 0016/0030/0070 amendments + ADR 0004/0031 note + ADR
  0024 revision recorded.
- `docs/developer/surface-contract.md`, `docs/developer/web-design-system.md`, and
  `docs/operator/workspace.md` published.
- Roadmap section/list entry, vision, README, AGENTS.md, DEVELOPMENT.md, and
  agent-context-map agree on the released v0.58 state and the unchanged v0.59
  boundary (settings migration / param-contract / onboarding / RC).
