# Allbert v0.59 Request Flow - Hardening, Export/Import, Settings Migration, And Param Contracts

Status: planned.

Implementation details live in `docs/plans/v0.59-plan.md`.

## Source Documents

- `docs/plans/v0.59-plan.md`
- `docs/adr/0046-settings-schema-migration-policy.md` (accepted here;
  drafted in v0.45)
- `docs/adr/0065-central-action-param-contract-enforcement.md`
- v0.39 through v0.58 plans/request flows

## Main User/Operator Flows

- Operator exports a disposable Allbert Home.
- Operator dry-runs import on a second machine/home.
- Operator runs `mix allbert.settings.migrate` for any pending schema
  migrations per ADR 0046.
- Operator reviews migration, secret, schema, and rollback diagnostics.
- Operator runs the v0.59 cross-surface hardening and param-contract gates.
- Maintainer verifies the substrate handoff before v0.60-v0.63 product work.

## Data Flow

Export/import reads settings, secrets metadata, memory, skills, threads,
traces, audits, jobs, objectives, confirmations, plugins, marketplace metadata,
self-improvement suggestion state, media retention metadata, channel pairing
metadata, protocol settings, and caches through documented facades, writes
dry-run diagnostics, and records rollback guidance.

## Authority Invariants

No new capability is introduced. Export/import does not expose raw secrets.
Dry-run diagnostics are redacted. Security eval sweep must cover every
v0.39-v0.58 capability class.

## Handles / CLI Surfaces

- Profile export/import dry-run commands or workspace surfaces.
- `mix allbert.settings.migrate` runner per ADR 0046.
- Security review/status surfaces (now surfaces pending settings migrations).
- Central action param-contract validation through the action Runner.
- Product-RC substrate handoff checklist.

## Security Eval Rows

- `profile-export-redaction-001`
- `profile-import-dry-run-rollback-001`
- `secret-migration-policy-001`
- `settings-migration-no-silent-001` (pending migrations require operator
  confirmation)
- `settings-migration-audit-trail-001`
- `v1-eval-sweep-mcp-client-browser-media-001`
- `v1-eval-sweep-mobile-channels-001`
- `v1-eval-sweep-marketplace-001`
- `v1-eval-sweep-public-protocol-surfaces-001`
- `v1-eval-sweep-self-improvement-001`

## Smoke Checklist

- Export disposable home.
- Import dry-run into a second home.
- Run `mix allbert.settings.migrate`; confirm pending migrations are operator-
  confirmable and audited.
- Confirm secrets are redacted and secret-migration policy is clear.
- Run the cross-surface eval sweep across MCP client, browser, channels
  (Discord/Slack + WhatsApp/Signal/Matrix), Plan/Build, marketplace,
  self-improvement, voice, vision, and v0.51 public protocol surfaces (MCP
  server, OpenAI-compatible API, ACP server).
- Confirm v0.47 self-improvement suggestions remain inert after export/import.
- Confirm v0.52/v0.53 channel pairing and v0.51 MCP/OpenAI/ACP public-surface
  settings round-trip through export/import diagnostics without exposing
  secrets.
- Confirm docs and gates are clean.
- Confirm no guided onboarding, packaging, web-UX, or final-RC scope leaked into
  v0.59.

## Operator Validation

Status: planned. Commands are illustrative until the milestone lands; verify and
replace with observed output at closeout.

```sh
export ALLBERT_HOME="$(mktemp -d /tmp/allbert-v059-a.XXXXXX)"
export DEST_HOME="$(mktemp -d /tmp/allbert-v059-b.XXXXXX)"
# Export the first home, dry-run import into the second, run pending migrations.
mix allbert.settings.migrate
rg -i 'token|secret|api_key' "$DEST_HOME" || true
```

Expected behavior:

- export then dry-run import round-trips settings, channel pairings, and
  MCP/OpenAI/ACP public-surface config without exposing secrets.
- pending schema migrations are operator-confirmable and audited (no silent
  migration).
- the cross-surface eval sweep passes and v0.47 self-improvement suggestions
  remain inert after import.

## Docs Closeout Checklist

- Export/import guide exists.
- Settings migration guide exists per ADR 0046.
- Product-RC substrate handoff checklist exists.
- Security/self-improvement/public-protocol docs are reconciled.
