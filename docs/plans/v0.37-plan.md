# Allbert v0.37 Dynamic Code & Config Generation and Live Capability Integration

Status: implemented as `0.37.0` on 2026-05-25; v0.37.1
post-implementation audit hardening and final gates completed on 2026-05-25.
Reopened for v0.37.2 capability-first generator and bounded model-backed
committee implementation before release tagging; v0.37 is not accepted until
Allbert can create, repair, gate, approve, run, and roll back at least one
useful generated action. Reopened again for v0.37.3 before tagging so generated
actions can express delegated write capabilities through reviewed facades, not
only pure read-only computation. Reopened again for v0.37.4 before tagging to
close the third post-implementation audit gaps: public discard workflow,
delegated-write security eval coverage, a separate dynamic-codegen request
permission, validator coherence, and version/docs closeout. Final pre-tag
version metadata was `0.37.4`. Reopened again for v0.37.5 before tagging to
close the fourth audit pass: make delegated facade approval policy explicit,
move discard to a dedicated dynamic-codegen lifecycle permission, and document
confirmation-free discard semantics without blocking generated actions from
running through reviewed facades. Final pre-tag version metadata moves to
`0.37.5`.

Request flow: `docs/plans/v0.37-request-flow.md`

ADRs:

- `docs/adr/0032-dynamic-plugin-generation-and-sandboxed-loading.md` (untrusted
  trial + gated in-core integration)
- `docs/adr/0033-capability-gap-acquisition-and-trust-tiers.md` (capability gap,
  trust tiers incl. `:integrated`)
- `docs/adr/0035-codegen-agents-and-live-integration-loader.md` (code-gen agents
  and the live-integration loader)
- `docs/adr/0037-elixir-otp-sandbox-backend-and-gate-runner.md` (v0.36 sandbox
  prerequisite)

v0.37 is the self-extending-runtime milestone. It consumes the v0.36
Elixir/OTP sandbox and gate runner, detects a capability gap, generates
**Elixir/OTP code and config** to the proven v0.27-v0.35 contract shapes,
stores the draft as file-backed local metadata under Allbert Home, trials it in
the v0.36 sandbox, and — only after the sandbox gate passes and the operator
confirms — hot-loads and registers it into the live core node without a restart,
with audit and rollback.

Shipped `0.37.0` scope: the gated lifecycle, v0.36 sandbox bridge, trusted
validator, live loader, rollback, emergency disablement, and dynamic action
overlay are implemented for reviewed read-only action artifacts.

v0.37.1 post-implementation hardening adds executable v0.37 security evals,
stored-confirmation resume checks, Settings/validator scope alignment, dynamic
codegen lifecycle audit records/signals, bounded sandbox report history, and
documentation reconciliation.

v0.37.2 is a release-blocking capability-first correction. It adds a real
LLM-backed generator for read-only action drafts behind the existing explicit
capability-gap entrypoint, widens the trusted validator for useful pure Elixir
logic, fixes the allowed LiveView approval resume path, and proves the full
gap -> plan -> author -> trial-author -> sandbox -> critic -> repair -> gate ->
operator approval -> live run -> rollback loop with a non-trivial generated
action. The final v0.37.2 closeout adds an explicit
`DynamicPlugins.request_draft_with_gate/3` orchestration path so sandbox
trial/gate and trusted-validation evidence can drive bounded Repair until the
gate passes or the whole-workflow budgets/limits stop the attempt. Generated
apps, panels, settings fragments, memory namespaces, objective wiring, route
pages, dependencies, migrations, and child processes remain deferred live
targets.

v0.37.3 is the delegated-write correction. A generated action may declare the
effective permission it needs (`:read_only`, `:memory_write`, or
`:external_network` in the shipped ceiling), but generated bodies still cannot
reach protected subsystems directly. Any write or external-network effect must
route through `AllbertAssist.DynamicPlugins.Delegate.run/3` to a literal,
operator-allowlisted reviewed facade. The initial facade ceiling is
`append_memory` and `external_network_request`. The facade keeps its existing
Security Central behavior, confirmation policy, and approval surfaces; v0.37.3
does not add a generic dynamic-action confirmation resume path.

The deterministic, template-driven developer/operator creation experience (Mix
tasks, operator flows, Canvas Create surface, scheduled/chron pattern templates)
is v0.38 and reuses the v0.37 engine.

## Purpose

When a request needs a capability no existing plugin/app/action provides,
Allbert should be able to build that capability and make it live — but only
through a gate that keeps generation advisory and authority explicit. v0.37
delivers the full loop:

1. capability gap;
2. advisory generation;
3. v0.36 sandbox trial and warning gate;
4. operator-confirmed trust grant;
5. live loader registration;
6. use through normal action/app boundaries;
7. rollback.

v0.37 prefers existing capability over generated code, in line with the vision's
preference for declarative, validated surfaces over opaque generated code
(`docs/plans/allbert-jido-vision.md`). For a capability gap the resolution order
is: reuse or compose existing registered actions/skills/apps first; then a
declarative `AllbertAssist.Surface` over existing capabilities; and only when
neither can satisfy the gap, generate new Elixir/OTP code. Code generation is the
last resort, not the default response to a gap.

v0.37 is the bounded, non-autonomous realization of the vision's "Deferred Until
The Foundation Settles" self-recompilation item. Generation stays advisory,
integration stays operator-confirmed, and generated authority stays narrow (the
Design Decision 6 permission ceiling). Delegated generated writes are in scope
only when the generated action's declared permission, response metadata, and
literal delegated facade agree with the reviewed facade permission and the
operator has enabled that permission/facade pair. "Fully autonomous skill
creation with broad execution permissions" remains deferred.

## Safety / Trust Model

### 1. v0.36 remains the untrusted boundary

Generated Elixir/OTP code is compiled, tested, and trialed only through the
v0.36 sandbox/gate runner until integration is explicitly confirmed. v0.37 does
not add a second sandbox path, hidden BEAM node boundary, or host-process
fallback.

v0.37 also owns the handoff detail between durable draft files and the v0.36
bundle: generated modules that must compile are materialized into a disposable
project-shaped staging tree under the reserved generated namespace, and those
same generated bytes are scanned before any sandbox command runs. The separate
v0.36 `drafts` and `tests` mounts are evidence inputs, not a compile path by
themselves.

### 2. Code-gen agents are advisory generators

The Planner, Author, TrialAuthor, Critic, and Repair agents may propose source,
config, tests, and repair patches. Their output cannot enable the workflow,
advance trust tiers, bypass the gate, authorize integration, grant
permissions, create routes, or set `active_app`.

For v0.37.2, read-only action authoring itself is in scope. For v0.37.3, the
same committee may also author delegated write actions whose effects route
through reviewed facades. The generator must produce draft source, manifest
metadata, and sandbox trial tests for a useful action target. It must not
collapse the committee into one model-backed Author call surrounded by
deterministic labels. Planner, TrialAuthor, and Critic may share the same
configured provider profile, and Repair is invoked when repair is requested,
but they need separate packet contracts, whole-workflow provider-call
accounting, and redacted provenance. Deterministic validators may normalize,
reject, and gate role output; they do not replace the role's model-backed
reasoning.

### 3. The trust grant is narrow

An artifact is eligible for integration only when:

- v0.36 sandbox trial passed;
- v0.36 in-sandbox warning gate passed; before v0.37 implementation starts,
  v0.36 M10's `ALLBERT_DOCKER_FULL_GATE_TEST=1` smoke should be green on a
  local Docker host to prove the real umbrella full-gate path, not only a
  trivial compile fixture;
- v0.37 static/integrity checks passed;
- the operator explicitly confirms integration through Security Central.

Only that combination permits transition to `:integrated`.

### 4. Trusted loader recompiles reviewed source

The live loader recompiles the operator-reviewed source in core with an
integrity hash. It does not load opaque sandbox-built binaries. It registers
only manifest-declared action modules that pass the generated-permission
ceiling and runtime call-target validator, and records audit events for compile,
load, register, and rollback. Generated app, panel, child, destination,
settings-fragment, memory-namespace, and objective-wiring targets remain
rejected live targets in v0.37.

### 5. Existing authority boundaries still apply

Dynamically integrated actions resolve through the actions registry overlay and
execute through `AllbertAssist.Actions.Runner.run/3`. App surfaces register
through `App.Registry` and v0.34 Canvas/destination boundaries. Settings writes,
permissions, confirmations, Resource Access, and app-scope rules remain
unchanged.

Generated action metadata is not trusted by declaration alone. Static reviewed
actions self-authorize inside their `run/2` bodies; v0.37 must therefore validate
generated action permissions, confirmation metadata, body call targets, and
response action metadata before integration. A generated action cannot declare a
lower permission while performing higher-authority work in its body.

## Expected Inputs

- v0.36 Elixir/OTP Sandbox And Gate Runner implemented and accepted.
- v0.24 objective runtime for ownership and lifecycle state:
  `AllbertAssist.Objectives`, `Objectives.Engine.Agent`,
  `Objectives.Proposer`, `Objectives.Stage`, and `Objectives.Event`.
  Capability-gap / inventory / acquisition vocabulary was reserved by ADR 0021
  and is implemented in v0.37 M1 on top of this substrate; it is not a
  pre-existing module set.
- v0.25 native-agent + Jido.AI pattern for advisory agent committees.
- v0.27-v0.35 contract shapes: manifest, actions, panels, intent descriptors,
  settings fragments, memory namespace, objective wiring, destinations, and
  theming/layout docs.
- v0.31 shared paths, redaction, audit, action DSL, typed responses, extension
  registry, and Settings Central fragments.
- Static actions registry today is compile-time; v0.37 adds a runtime-mutable
  overlay while preserving existing runner/security semantics.

## Design Decisions

### 1. Generation targets Elixir/OTP code and config

v0.37 generates only Elixir/OTP artifacts to the proven project shapes. Other
language targets, package-manager installs, migrations, NIFs, ports, and broad
shell automation stay parked.

Generated artifacts may include plugin/app manifest and modules,
`AllbertAssist.Action` actions, panel surfaces, intent descriptors, settings
fragments, memory namespace stubs, objective wiring, and theming/layout docs.
Generated objective wiring may use only the shared `AllbertAssist.Objectives`
facade or registered objective actions. Generated apps/plugins must not create
private durable goal loops, private objective tables, or private objective
engines; `objective_id` and `step_id` remain correlation data, never authority.

### 2. Code-gen agent committee owns authoring, not authority

Add advisory generator roles:

- `Codegen.Planner`
- `Codegen.Author`
- `Codegen.TrialAuthor`
- `Codegen.Critic`
- `Codegen.Repair`

Raw prompts/completions are redacted or omitted from traces; structured
provenance and bounded diagnostics remain.

The committee runs on the shared `AllbertAssist.JidoBacked` substrate used by the
v0.25 StockSage native coordinator (standard AgentServer child specs, private
command routing, restart rehydration, and the `allbert.jido.debug_trace`
diagnostic gate), rather than a private agent runtime. v0.37 uses the single
Settings Central selection `dynamic_codegen.provider_profile` for the committee
instead of hard-coding provider/model names in code. Per-role model profiles such
as separate fast/capable/thinking aliases are deferred until there is a real
operator need and a settings/audit story for them.

v0.37.2 implements the first producer through an injectable provider boundary
around `Jido.AI.generate_object/3`. Deterministic tests inject a fake
provider with the same structured object contract. Operator remote-generation
smoke tests may source `.env` for `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`,
`OLLAMA_BASE_URL`, or equivalent provider credentials, but tests and logs must
never print those values. Production generation records model/profile, usage
when available, prompt hash, generated file hashes, and bounded diagnostics in
draft metadata.

Research note: `docs/research/codegen-agent-loop-research.md` records the
external research pass behind this v0.37.2 correction. The relevant result is
that coding systems should be structured and bounded, but not single-shot:
planning, test authoring, execution feedback, critique, and repair should each
have explicit packets, and deterministic tests/gates remain the acceptance
authority.

### 3. Draft lifecycle consumes v0.36 reports

Draft state is file-backed local data, not a new database table. v0.37 stores
drafts under `<ALLBERT_HOME>/dynamic_plugins/drafts/<slug>/` with:

- `metadata.yaml` (tier, provenance, source hash, selected target shapes,
  static-validation status, operator decisions, timestamps);
- generated source/config/test files under reviewed subdirectories;
- sandbox reports copied from v0.36;
- bounded diagnostics and repair history.

Minimal metadata shape:

```yaml
schema_version: 1
slug: "weather_summary"
revision: "rev_2026_05_25_001"
tier: "draft"
producer: "codegen_committee"
provider_profile: "local"
target_shapes: ["action"]
source_hashes:
  lib/allbert_assist/dynamic_plugins/generated/weather_summary/action.ex: "sha256:..."
compiled_paths:
  - lib/allbert_assist/dynamic_plugins/generated/weather_summary/action.ex
scan_paths:
  - lib/allbert_assist/dynamic_plugins/generated/weather_summary/action.ex
budget:
  provider_calls_used: 2
  provider_usage_units_used: 1800
gate:
  status: "not_run"
  sandbox_report_id: null
static_validation:
  status: "not_run"
confirmations:
  integration_id: null
  rollback_id: null
diagnostics:
  - code: "draft_created"
    message: "Draft written; no sandbox evidence yet."
repair_history: []
timestamps:
  created_at: "2026-05-25T00:00:00Z"
  updated_at: "2026-05-25T00:00:00Z"
```

Metadata is operator-inspectable and useful for reconciliation, but it is not a
trust anchor by itself. Security Central confirmation records, current file
hashes, and the v0.36 gate report remain the authority checks for integration.

The untrusted draft root is deliberately separate from
`<ALLBERT_HOME>/plugins/<slug>` so ordinary plugin discovery cannot mistake a
draft for a trusted plugin. Integration may copy or materialize a reviewed
artifact into the dynamic integration root only after the trust gate.

Draft tiers:

- `:draft`
- `:sandbox_compiled`
- `:sandbox_trialed`
- `:gate_passed`
- `:integrated`
- `:rolled_back`
- `:discarded`

The first four tiers are evidence states. They grant no live authority. Only the
operator-confirmed loader transition reaches `:integrated`.

Draft creation is a pluggable producer. The v0.37 code-gen committee is one
producer; v0.38 templated creation is another. Everything from `:draft` onward —
the metadata store, staging, `RunDraftTrial`, `RunDraftGate`, the loader, the
actions overlay, and rollback — is producer-agnostic and records the producer in
provenance. v0.37 builds the lifecycle so a later deterministic template producer
can enter at `:draft` without the advisory committee.

### 4. Capability gaps are explicit in v0.37

v0.37 records a capability gap only from an explicit operator request to
create/generate a missing capability or from an explicit objective-owned step
proposal that no existing registered action/app can satisfy. Intent/action
planning may report that no eligible capability exists, and low-confidence
ranking may suggest generation as an option, but classifier output, rejected
candidates, or weak confidence cannot start generation by themselves.
Proactive auto-acquisition from intent confidence is deferred.

The draft lifecycle is owned by the v0.24 objective runtime, not a private loop.
Gap, generation, trial, gate, integration, and rollback are recorded as objective
steps and `objective_events` on `Objectives.Engine`'s state machine: the gap and
acquisition options surface in the propose/evaluate stages;
`GenerateDraft`/`RunDraftTrial`/`RunDraftGate` execute as advisory/evidence steps
that grant no authority; and `IntegrateDraft`/`RollbackIntegration` are the
authorize-then-execute steps gated by Security Central confirmation. The
objective's acceptance criteria reference the v0.36 gate plus operator
confirmation. `objective_id`/`step_id` are correlation only, never authority.

### 5. Draft staging has one compile and scan contract

Durable draft source remains under
`<ALLBERT_HOME>/dynamic_plugins/drafts/<slug>/`, outside ordinary plugin
discovery. For sandbox trial/gate, v0.37 builds a disposable staging project
root and materializes generated files into compile-visible, reviewed paths
inside that staged project:

- generated core modules live under the reserved namespace path for
  `AllbertAssist.DynamicPlugins.Generated.<Slug>...`;
- generated action, app, panel, and support modules must be declared in the
  draft manifest and live under that generated namespace;
- generated tests live under staged umbrella test paths and are invoked by the
  v0.36 gate through `focused_test_paths` / `security_eval_paths`, with
  `focused_test_cwd` / `security_eval_cwd` only when the reviewed shape needs a
  bundle-local subdirectory;
- settings fragments, manifests, and docs are copied as inert data and cannot
  set operator values, secrets, permissions, provider credentials, or resource
  grants.

The staged project includes the same umbrella gate configuration used by the
v0.36 default project bundle: root Mix files, formatter config, Credo config,
Dialyzer ignore config, root config, app source, and reviewed source-tree plugin
inputs needed by the current warning gate. Generated drafts must meet the real
project bar, not a reduced gate fixture.

The staging builder records a source hash for every generated file and passes
the same generated source/test files through `Sandbox.SourcePolicy.scan/2`'s
path-list form before sandbox backend resolution. The gate may compile only the
staged project tree whose generated-file hashes match the scanned draft
manifest. v0.37 can express this with the v0.36 bundle builder's
`project_paths`/`test_paths` inputs plus the path-list scanner; a v0.36 change
is required only if hash enforcement must move into the sandbox facade or
container itself. A draft that compiles from unscanned bytes, or scans bytes
that are not the ones compiled, is invalid.

Generated focused tests and security evals are functional evidence, not a
security boundary. They are authored by advisory code-gen agents and can only
show that selected behavior passed in the sandbox. Security rests on static
validation, body call-target validation, the generated-permission ceiling,
operator review, Security Central confirmation, and normal runtime boundaries.

### 6. Loader is explicit and reversible

`AllbertAssist.DynamicPlugins.Loader` integrates an approved artifact:

- verifies source hash and manifest-declared module set;
- enforces generated namespace rules (`AllbertAssist.DynamicPlugins.Generated`
  or another reviewed generated namespace), and rejects replacement of core,
  static plugin, or shipped app modules;
- rejects undeclared modules, route-page integration without restart, or
  forbidden constructs, including protocols, router edits, application env
  changes, migrations, dependency additions, NIFs, ports, package-manager
  hooks, top-level side effects, `@on_load`, dynamic compile/eval/require, and
  generated macro code that would run during trusted core compilation;
- reconciles the manifest and AST bidirectionally: every discovered `defmodule`
  must be under the generated namespace and listed in the manifest, and every
  manifest-declared module must exist in reviewed source;
- validates generated action metadata against the v0.37 permission ceiling and
  against the action body's call targets;
- reruns v0.37 static/integrity validation over the reviewed source immediately
  before trusted compile;
- recompiles reviewed source in core;
- registers actions through a runtime overlay;
- rejects generated apps, panels, children, settings fragments, memory
  namespaces, objective wiring, and route-page artifacts before core
  compilation;
- writes audit events;
- rolls back by unregistering actions and purging loaded modules.

Trusted-phase validation is default-deny and is not the v0.36 regex scanner.
Before the loader calls any in-core compile API, it parses reviewed generated
source with `Code.string_to_quoted!/2` (or an equivalent non-executing parser)
and walks the AST against an allowlist. Unknown AST forms are denial.

Allowed top-level forms are limited to reviewed `defmodule` declarations under
the generated namespace, normal `def`/`defp` function bodies, a fixed small set
of inert module attributes, and `alias`/`use`/`import`/`require` only for
reviewed allowlisted modules. v0.37.1's shipped generated macro/use allowlist is
enumerated: `use AllbertAssist.Action` for generated action modules. Generated
UI is deferred; future support must be declarative `AllbertAssist.Surface` data
using existing catalog components. Custom Phoenix components, LiveViews, HEEx
sigils, and route modules are denied in v0.37 unless a later plan adds an
explicit reviewed allowlist. Macro options, action DSL options, schema entries,
tags, module attributes, and manifest fields must be inert literals;
compile-time expressions such as `System.get_env/1`, `Application.get_env/2`,
dynamic atom/module construction, or function calls in macro arguments are
denial.

The validator also scans all call sites inside generated function bodies,
callbacks, helper modules, action `run/2` implementations, app callbacks,
surface data builders, and child process callbacks. It allows only local calls
within the generated namespace and an explicit module/function allowlist of
side-effect-free Elixir/Allbert helpers or approved runtime facades. It denies
direct calls to protected authority surfaces including `System`, `Port`, `Code`,
`Mix`, `Application` mutation, `File`/`Path` write-delete traversal operations,
`Node`, `:rpc`, `:os`, dangerous `:erlang` functions, NIF loading, ETS or
`:persistent_term` access to core tables, direct Repo writes, Settings writes,
secret reads/writes, confirmations, Resource grants, local command runners,
package/skill execution, sandbox actions, integration/rollback/disable actions,
and any `apply/3` or dynamic dispatch escape route to those targets. Declared
permission, confirmation metadata, response action metadata, and body call
targets must agree; a mismatch is denial.

Generated action permissions have a ceiling. The hard-coded v0.37 ceiling
allows `:read_only` by default and may allow only operator-enabled,
validator-proven delegations for `:external_network`, `:memory_write`,
`:objective_write`, or `:workspace_canvas_write` through existing registered
actions/facades whose normal Security Central floors still apply. Settings may
select a subset of this ceiling but cannot expand the hard-coded ceiling.
Generated actions are hard-denied from
declaring or implementing host execution, package install, skill import/script
execution, sandbox trial, secret read/write, confirmation decision, Security
Central trust-control, dynamic integration/rollback/disablement, direct Settings
mutation, or Resource-grant mutation. Generated actions are `resumable?: false`
in v0.37; dynamic confirmation resume adapters are deferred.

Generated child processes are not live-loaded in the shipped v0.37 loader.
Future child support must keep the documented state-only rule: callbacks pass
the same call-target validator and do not create autonomous timers, network
calls, shell/package/script execution, durable goal loops, or direct
protected-subsystem writes. Effectful generated workers and private scheduled
loops are deferred.

v0.37's trusted-compile threat model is explicit: the sandbox gate plus
operator review plus AST allowlist plus runtime call-target validation plus
generated-permission ceiling defend the core node against advisory-agent
mistakes, malformed drafts, permission/body mismatch, and obvious injection
before a generated artifact is granted live authority. They are not a
tamper-proof boundary against an operator or local attacker who already has
write access to Allbert Home or the source tree; that remains the local trust
boundary.

Integration is all-or-nothing. The loader records each step it completes during
an integration attempt. If source copy, trusted validation, compile, actions
overlay registration, or audit append fails
before the artifact reaches stable `:integrated`, the loader automatically
unwinds registrations/modules from that attempt and leaves the draft at
`:gate_passed` with bounded diagnostics. Operator-confirmed rollback is needed
only after a stable `:integrated` state has been reached.

Integration copies the approved, reviewed source and metadata into
`<ALLBERT_HOME>/dynamic_plugins/integrated/<slug>/<revision>/`. Ordinary plugin
discovery still ignores this root; the live loader is the only authority that
may register from it. Each source change creates a new revision that returns to
`:draft`, reruns the sandbox gate, and requires a new operator confirmation.
v0.37 does not support in-place mutation of an integrated artifact. A same-name
upgrade must first roll back the currently integrated revision, then integrate
the new gate-passing revision with its own confirmation. Atomic supersede
without an intermediate rollback is deferred.

On boot, the loader performs reconciliation: it may re-register only artifacts
whose metadata says `:integrated`, whose source hash still matches, whose gate
record is present, whose integration confirmation is valid in Security Central's
durable confirmation/audit store, and whose current loader policy and settings
still allow live dynamic code. `metadata.yaml` may cache confirmation ids and
hashes, but it is not a trust anchor by itself. If
`dynamic_codegen.live_loader_enabled=false`, reconciliation must not register
dynamic authority surfaces. The emergency disable action turns the switch off
and clears the action overlay without deleting source. This anchors
reconciliation inside Allbert's local trust boundary; it does not defend against
an operator or local attacker who can rewrite Allbert Home and the Security
Central store.

Rollback's hard guarantee is authority removal: actions no longer resolve,
apps/destinations disappear from registries, and children stop. BEAM module
purge/delete is attempted and audited, but is best effort because loaded code
may be retained by the VM while old references drain.

Rollback, like integration, requires an explicit operator confirmation through
Security Central.

Integration and rollback confirmations are restricted to high-trust operator
surfaces. The confirmation subsystem (ADR 0008/0016) is channel-aware, so the
ordinary `approve_confirmation` path can otherwise be resolved from CLI,
LiveView, Telegram callbacks, email typed commands, or any cross-channel
approver. Because integration hot-loads code into the core node, v0.37 confines
`IntegrateDraft`/`RollbackIntegration` approval to the surfaces named by
`dynamic_codegen.integration_approval_surfaces`, excludes low-trust channel
approval (Telegram, email) and cross-channel approval regardless of
`confirmations.allow_cross_channel_approval`, and denies and audits an approval
that arrives from a disallowed surface.

### 7. Broader generated live targets remain documented limits

The shipped loader integrates reviewed action targets only. v0.37.2 proved pure
read-only actions; v0.37.3 adds delegated `:memory_write` and
`:external_network` actions whose effects can only pass through reviewed facade
delegation. Panel/destination apps, settings fragments, memory namespaces,
objective wiring, child processes, and compile-time Phoenix router page routes
require later plans. The loader must reject these artifacts rather than silently
half-integrating them.

### 8. Actions overlay has collision and precedence rules

`AllbertAssist.DynamicPlugins.ActionsOverlay` sits beside the existing static
and plugin/app action registry paths. It mirrors the existing
`plugin_actions/0` collision-denial precedent and is merged by
`AllbertAssist.Actions.Registry` through the public seams that callers already
use: `modules/0`, `agent_modules/0`, `capabilities/0`,
`agent_capabilities/0`, `internal_capabilities/0`, `resolve/1`,
`capability/1`, `registered_module?/1`, `capabilities_for_app/1`, and
`diagnostics/0`. App attribution for dynamic action capabilities is provided by
the app registry seam (`App.Registry.app_id_for_action/2`) after dynamic app
registration.

It must:

- deny action-name collisions with static, plugin, app, or other dynamic
  actions;
- deny module, app id, plugin id, memory namespace, surface id, and destination
  id collisions before registration;
- expose diagnostics for rejected dynamic contributions;
- preserve `Actions.Runner.run/3`, app-scope, confirmation, Resource Access,
  and Security Central behavior;
- unregister entries atomically during rollback.

Resolution/display precedence is static reviewed actions, then reviewed
source-tree plugin/app actions, then integrated dynamic actions. Collision is
denial, not shadowing, so dynamic actions never outrank static or reviewed
source-tree actions. Integrated dynamic actions enter `capabilities/0`; only
those whose reviewed capability declares `exposure: :agent` enter
`agent_capabilities/0` and intent ranking. Internal-only dynamic actions are
callable only through explicit runtime/operator paths.

### 9. Settings Central owns workflow policy

New settings are default-off and audited:

| Key | Type | Default | Meaning |
|---|---:|---:|---|
| `dynamic_codegen.enabled` | boolean | `false` | Master switch for draft generation |
| `dynamic_codegen.provider_profile` | string/null | null | Required model profile when generation is enabled; `null` fails closed |
| `dynamic_codegen.max_repair_iterations` | integer | bounded default | Repair loop cap |
| `dynamic_codegen.max_provider_calls_per_gap` | integer | bounded default | Whole-workflow provider-call cap for one capability gap |
| `dynamic_codegen.max_provider_usage_units_per_gap` | integer/null | bounded default | Optional usage/token budget when provider usage data is available |
| `dynamic_codegen.max_files` | integer | bounded default | Draft file-count cap |
| `dynamic_codegen.max_bytes` | integer | bounded default | Draft total-size cap |
| `dynamic_codegen.allowed_targets` | string list | `["action"]` | Generated target shapes permitted in the shipped loader |
| `dynamic_codegen.allowed_action_permissions` | string list | `["read_only"]` | Operator-enabled subset of the generated action permission ceiling: `read_only`, `memory_write`, `external_network` |
| `dynamic_codegen.allowed_facades` | string list | `[]` | Operator-enabled subset of reviewed facades generated code may delegate to; shipped hard ceiling is `append_memory`, `external_network_request` |
| `dynamic_codegen.live_loader_enabled` | boolean | `false` | Separate switch for operator-confirmed live integration |
| `dynamic_codegen.integration_approval_surfaces` | string list | `["cli", "liveview"]` | Surfaces allowed to approve integration/rollback; excludes Telegram/email/cross-channel |
| `dynamic_codegen.retention_days` | integer | bounded default | Draft/report cleanup horizon |

Settings store enablement, bounds, and selections. Draft source, metadata,
reports, and diagnostics remain file-backed Allbert Home data surfaced
read-only.

`dynamic_codegen.provider_profile` resolves through the existing
`Settings.resolve_model_profile/1` model-profile path used by the ReqLLM and
Jido.AI consumers. Generation fails closed when the setting is unset, the named
profile is unknown, the provider is disabled, credentials are unavailable,
ReqLLM/Jido.AI is unavailable, or the provider type is unsupported. These
provider calls use the settings-gated provider/model-profile path (the same one
`direct_answer` and the classifier use), not the Resource Access external-service
consumer path; they are bounded by settings rather than per-call resource grants.
Advisory providers never receive secrets, operator memory, or raw conversation in
prompts, traces, draft metadata, or sandbox bundles.

After an explicit generation step is accepted, Planner/Author/TrialAuthor/Critic
and Repair provider calls do not prompt for per-call Security Central
confirmation. Their cost and network use are bounded by
`dynamic_codegen.provider_profile`, `dynamic_codegen.max_repair_iterations`,
provider-call and usage budgets, and audit/trace records. Budget exhaustion
fails closed and leaves the draft at its current evidence tier.

The repair loop is bounded and evidence-driven. A draft iteration stops with
success only when source/test schemas pass, trusted prechecks pass, generated
focused tests pass in the v0.36 sandbox, the warning gate passes, selected
security evals pass, and trusted validation passes immediately before loader
compile. It stops with an impasse when iteration, provider-call, provider-usage,
wall-clock, or repeated-identical-failure limits are reached. The LLM critic may
veto, explain, prioritize, or request repair; it never marks a draft trusted,
advances a tier, or authorizes integration.

`dynamic_codegen.allowed_targets` is restricted to `["action"]` in v0.37. The
`dynamic_codegen.allowed_action_permissions` setting selects only from the
hard-coded generated-action permission ceiling. v0.37.3's shipped ceiling is
`read_only`, `memory_write`, and `external_network`; the default remains
`["read_only"]` until the operator also enables the matching facade in
`dynamic_codegen.allowed_facades`. Unknown target, permission, or facade names;
permission/facade mismatches; hard-denied permission classes; or attempts to
enable host execution, secret access, sandbox execution, confirmation
decisions, integration control, direct trust-control permissions, or
non-allowlisted facades are rejected at settings validation and again by the
loader.

### 10. Signals, traces, and audit

Draft and integration behavior is security-relevant and observable, so it routes
through the signal-driven runtime. Capability-gap proposal, generation, draft
tier transitions, sandbox trial/gate consumption, integration, rollback,
discard, and every denial emit signals on the signal bus and write trace/audit
records through the v0.31 audit/redaction facades. The loader's compile, load,
register, and rollback audit events (ADR 0035) are the same signal/trace path,
not a side channel. Raw prompts/completions are never traced; structured
provenance, tier, and bounded diagnostics are. Signals and audit records carry
no authority: only the operator confirmation plus the v0.36 gate grant trust.

v0.37.1 implements this with `AllbertAssist.DynamicPlugins.Audit`, append-only
markdown records under `<ALLBERT_HOME>/dynamic_plugins/audit/YYYY-MM.md`, and
`allbert.dynamic_codegen.*` lifecycle signals.

## Planned Interfaces

- `AllbertAssist.DynamicPlugins`
- `AllbertAssist.DynamicPlugins.Draft`
- `AllbertAssist.DynamicPlugins.MetadataStore`
- `AllbertAssist.DynamicPlugins.Loader`
- `AllbertAssist.DynamicPlugins.ActionsOverlay`
- `AllbertAssist.DynamicPlugins.Supervisor`
- `AllbertAssist.DynamicPlugins.TrustedValidator`
- `AllbertAssist.DynamicPlugins.Codegen.{Agent,Producer,LLM,Schema}`
- `AllbertAssist.DynamicPlugins.Codegen.{Planner,Author,TrialAuthor,Critic,Repair}`
- `AllbertAssist.DynamicPlugins.Codegen.Targets.Action`
- `AllbertAssist.DynamicPlugins.Codegen.CapabilityGap`
- Read-only Mix task: `mix allbert.dynamic drafts list`
- Read-only Mix task: `mix allbert.dynamic drafts show <slug>`
- Read-only Mix task: `mix allbert.dynamic integrations show <slug>`
- Registered actions:
  - `GenerateDraft`
  - `RunDraftTrial`
  - `RunDraftGate`
  - `IntegrateDraft`
  - `RollbackIntegration`
  - `DiscardDraft`
  - `ListDynamicDrafts`
  - `ShowDynamicDraft`
  - `ShowDynamicIntegration`
  - `DisableDynamicIntegration`

`RunDraftTrial` and `RunDraftGate` call the v0.36 sandbox facade; they do not
construct backend commands directly.

The `mix allbert.dynamic ...` tasks are inspection and operator workflow
wrappers over registered actions and file-backed status reports. `drafts
request` may create a source-bearing draft through the v0.37.2 generator when
`dynamic_codegen.enabled` and `dynamic_codegen.provider_profile` are configured;
trial, gate, integration, and rollback still run through explicit registered
actions and confirmations. The v0.38 templated creation UX remains separate.

New state-bearing modules (`DynamicPlugins.MetadataStore`,
`DynamicPlugins.ActionsOverlay`, `DynamicPlugins.Supervisor`) document their
Jido.Agent-vs-GenServer/DynamicSupervisor substrate choice in their `@moduledoc`
per the AGENTS.md pragmatic rule; the `Codegen.*` coordinator/roles follow the
v0.25 native-agent + Jido.AI precedent while keeping durable truth in draft
metadata.

## Documentation Deliverables

v0.37 introduces the first operator-confirmed live-integration path, so the
implementation must ship docs that make evidence, trust, and rollback
inspectable.

- Create `docs/operator/dynamic-capability-integration.md` with the operator
  workflow for enabling/disabling `dynamic_codegen.*`, inspecting draft
  metadata and sandbox reports, confirming integration, confirming rollback,
  reading audit/status output, understanding emergency disablement, and
  understanding that advisory agents and gate success are not authority.
- Update `docs/operator/security-hardening.md` with v0.37 emergency switches:
  `dynamic_codegen.enabled=false`, `dynamic_codegen.live_loader_enabled=false`,
  and `sandbox.elixir.enabled=false`, plus the action-shadow/core-module
  replacement denial posture.
- Update `docs/operator/onboarding.md` with a release-neutral risky-capability
  section that points to the v0.37 request-flow smoke and the new dynamic
  integration guide instead of only old v0.10 flows.
- Create `docs/developer/dynamic-plugin-drafts.md` with the draft root layout,
  `metadata.yaml` shape, generated namespace rules, static validator
  restrictions, project-shaped staging contract, scanned-vs-compiled hash
  matching, generated-test evidence limits, trusted AST allowlist, runtime
  call-target validation, generated-permission ceiling, literal macro/attribute
  contract, state-only child limits, integration root layout, actions overlay
  collision rules, loader lifecycle, all-or-nothing integration unwind, rollback
  and rollback-before-upgrade semantics, boot reconciliation, and the rule that
  ordinary plugin discovery must not scan
  `<ALLBERT_HOME>/dynamic_plugins/drafts/<slug>/` or
  `<ALLBERT_HOME>/dynamic_plugins/integrated/<slug>/<revision>/`.
- Update `docs/developer/how-to-create-an-allbert-app.md` to distinguish
  source-tree, reviewed compiled apps from v0.37 generated drafts and to point
  generated app work at the dynamic draft guide.

## Implementation Milestones

Milestone commits use the title form `<version> <milestone> <short title>`.
Each code milestone updates the request-flow log with actual modules, focused
tests, and operator verification commands before commit.

### M0: Implementation-Ready Docs

- Create the operator and developer dynamic-integration guides.
- Update onboarding, security hardening, app creation, runtime boundary, and
  agent context docs to name v0.37 facades and authority boundaries.
- Record this milestone breakdown before implementation begins.
- Gate: `git diff --check`.

### M1: Settings, Paths, Draft Store, And Read-Only Inspection

- Add default-off `dynamic_codegen.*` Settings Central keys with validation.
- Add dynamic draft/integrated Allbert Home paths.
- Add `AllbertAssist.DynamicPlugins`, draft structs, metadata store, manifest
  read/write, diagnostics, tier transitions, and source-hash helpers.
- Add read-only registered actions and `mix allbert.dynamic drafts|integrations`
  inspection commands.
- Focused tests cover defaults, invalid settings, metadata round-trips, terminal
  discard rules, source hash tamper detection, and read-only CLI output.

### M2: Staging And v0.36 Gate Bridge

- Build project-shaped staging trees from draft files.
- Enforce scanned-vs-compiled generated byte matching.
- Drive `RunDraftTrial` and `RunDraftGate` through `AllbertAssist.Sandbox`
  and registered sandbox actions only.
- Record sandbox report metadata back into draft metadata.
- Focused tests cover invalid compile paths, unscanned paths, sandbox disabled,
  gate failure, and gate-pass tier transition using test doubles where host
  Docker is unavailable.

### M3: Trusted Validator, Overlay, Loader, Rollback, And Disablement

- Add the trusted AST/body validator with literal macro/attribute checks,
  manifest reconciliation, protected-call denial, permission ceiling, and
  explicit rejection of generated apps, panels, settings fragments, memory
  namespaces, objective wiring, route pages, and children in the v0.37 live
  loader.
- Add the dynamic actions overlay and merge it through `Actions.Registry`.
- Add read-only action loader integration, all-or-nothing unwind, rollback,
  boot reconciliation, and emergency disablement.
- Add integration/rollback actions and high-trust approval surface checks.
- Focused tests and security evals cover the named v0.37 denial rows in the
  request-flow document.

### M4: Capability Gap And Advisory Codegen Producer

- Add objective-owned capability-gap vocabulary and producer-neutral draft
  creation entrypoints.
- Add advisory `Codegen.*` Jido agent scaffolding on `JidoBacked`, provider
  profile resolution, provider-call/usage budgets, and fail-closed provider
  diagnostics.
- Keep generated output bounded, redacted, and inert until it enters the M1-M3
  lifecycle.
- The shipped v0.37.0 producer scaffold recorded inert draft metadata and
  optional objective observations only; M8 supersedes it before release tagging
  with the source-bearing `codegen_llm` action producer.
- Focused tests cover disabled workflow, missing provider profile, budget
  exhaustion, low-confidence auto-generation denial, and explicit
  operator/objective generation entry.

### M5: Release Closeout

- Bump release metadata and app versions touched by v0.37 behavior.
- Update `CHANGELOG.md` and final request-flow status.
- Run focused suites, security evals, `mix compile --warnings-as-errors`,
  `mix format --check-formatted`, `mix credo --strict`, `mix dialyzer`, and
  `mix precommit`.
- Run Chrome extension browser verification if any workspace or LiveView UI
  changed.
- Provide manual verification instructions for a disposable Allbert Home.
- Update `docs/developer/runtime-boundary-map.md` with the v0.37 dynamic plugin
  facades, metadata store, loader, overlay, and authority boundaries.
- Update `docs/developer/jido-agent-pattern.md` only if the code-gen agents
  introduce a reusable Jido-backed pattern or lifecycle caveat; otherwise record
  in the request-flow closeout that no guide change was needed.

## Milestones

Each milestone closes with focused tests, docs/request-flow notes, and
`mix precommit` unless a narrower gate is recorded with why.

### M1: ADRs, tiers, capability-gap vocabulary, settings

Accept ADR 0032/0033 amendments and ADR 0035. Add trust tiers, capability-gap
vocabulary on top of the v0.24 objective runtime, default-off Settings Central
switches, file-backed draft metadata store, and draft roots under Allbert Home.
Pin v0.37 gap creation to explicit operator/objective proposals; low-confidence
intent ranking may suggest generation but cannot start it.

Tests: gap serialization, disabled-workflow behavior, file-backed metadata
round-trip/atomic-write behavior, trust-tier transitions, confirmation-required
security tests, explicit-gap trigger behavior, provider-profile fail-closed
behavior, provider budget exhaustion behavior, and generated-permission setting
validation.

### M2: Draft output contract and static validation

Pin the generated Elixir/OTP code/config contract, project-shaped staging
contract, and static validator: slug/path/allow-list/size/no-traversal,
manifest-declared modules, generated namespace, scanned-vs-compiled hash match,
trusted-compile AST allowlist, bidirectional manifest-to-`defmodule`
reconciliation, literal macro/attribute options, runtime call-target validation,
generated-permission ceiling, shared-objective-runtime wiring only, and no
dependency/migration/NIF/port/package-manager authority.

Tests: valid draft tree, traversal/size denial, undeclared module denial,
forbidden construct denial, generated-namespace enforcement, core-module
replacement denial, unscanned compile-path denial, scanned-but-not-compiled
denial, manifest/AST mismatch denial, trusted-compile AST rejection, macro
literal-option denial, generated runtime protected-call denial, permission/body
mismatch denial, generated permission-ceiling denial, generated objective
private-loop denial, generated settings-fragment authority denial.

### M3: Code-gen agent committee and repair loop

Add advisory agents and sandbox-grounded repair loop.

Tests: agents propose only; agent output cannot enable/trust/integrate; repair
uses v0.36 diagnostics; raw prompts/completions are not traced.

### M4: Sandbox trial and warning gate integration

Wire dynamic draft compile/trial/gate calls to the v0.36 sandbox facade using
the project-shaped staging contract and focused/security eval paths that point
at the generated tests in the staged project tree. Stage the real umbrella gate
config so compile, Credo, Dialyzer, focused tests, and security evals run
against the same project standards as v0.36. Treat generated tests as functional
evidence only, not as the security boundary.

Tests: sandbox unavailable blocks trial; gate pass/fail changes only evidence
tiers; failed artifacts cannot integrate; generated tests compile and run only
from scanned staged bytes; generated-test pass cannot bypass trusted validator,
permission ceiling, or operator confirmation.

### M5: Live integration loader and actions overlay

Add runtime-mutable actions overlay, supervisor integration, integration-root
persistence, boot reconciliation, emergency disablement, integrity checks,
audit, all-or-nothing integration unwind, same-name revision
rollback-before-upgrade semantics, dynamic-action `resumable?: false`
enforcement, explicit unsupported-target denials, read-only inspection Mix
tasks, and rollback. Update every `Actions.Registry` public seam
listed in Design Decision 8 so dynamic actions are visible only through the same
facade paths as static/plugin/app actions, with collision denial and diagnostics
at each seam.

Tests: operator-confirmed `:gate_passed` artifact becomes usable without
restart; rollback requires confirmation and unregisters authority surfaces;
module purge is best-effort/audited; route-page restart limit is enforced;
unconfirmed/ungated/tampered integration is denied; action collisions deny
rather than shadowing; cold-start reconciliation registers only still-valid
integrations; disabling the live loader removes or blocks dynamic authority
surfaces; failed mid-integration attempts auto-unwind and leave the draft at
`:gate_passed`; same-name revisions cannot integrate until the live revision is
rolled back; generated actions cannot register with `resumable?: true`; dynamic
children with timers/effectful callbacks are denied; integration/rollback
confirmations are denied from disallowed or low-trust channel surfaces; read-only
inspection tasks cannot create or integrate; every overlay contribution is
exercised through `modules/0`, `agent_modules/0`, `capabilities/0`,
`agent_capabilities/0`, `internal_capabilities/0`, `resolve/1`,
`capability/1`, `registered_module?/1`, `capabilities_for_app/1`, and
`diagnostics/0`.

### M6: Security evals and closeout

Add evals for untrusted core-load attempt, unscanned compile path, gate skip,
unapproved/auto integration, trusted-compile side effects, loader integrity
tamper, rollback correctness, emergency disablement, cold-start reconciliation,
settings-fragment authority denial, exfiltration, supply-chain denial, sandbox
bypass, proactive low-confidence gap denial, private objective-loop denial,
generated runtime protected-call denial, generated permission-ceiling denial,
permission/body mismatch denial, macro literal-option denial,
manifest/`defmodule` reconciliation denial, dynamic child effect denial,
generated resumability denial, generation budget exhaustion,
integration-approval-surface denial, and advisory-approval bypass. Update
README, roadmap, agent map, future-features, AGENTS (add a
dynamic-codegen/loader/draft-lifecycle subsystem-routing entry, not only a
non-negotiable line), DEVELOPMENT, and ADR pointers. Create/update the operator
and developer docs listed in
Documentation Deliverables, especially
`docs/operator/dynamic-capability-integration.md` and
`docs/developer/dynamic-plugin-drafts.md`. Bump version metadata to `0.37.0`.

### M7: v0.37.1 post-implementation audit hardening

The v0.37.0 implementation shipped the conservative read-only dynamic action
lifecycle, but the post-implementation audit found release-blocking assurance
gaps and contract drift. v0.37.1 closes those gaps before the milestone can be
treated as tag-ready.

Required hardening:

1. **Security eval coverage.** Register the v0.37 `codegen-*` eval rows in the
   v0.28 security eval inventory, add an executable dynamic-codegen eval module,
   and prove the named fail-closed paths for untrusted load, sandbox bypass,
   gate skip, unconfirmed/advisory integration, trusted-compile side effects,
   AST allowlist, manifest/module mismatch, permission ceiling, resumability,
   action shadowing, rollback, emergency disablement, restart reconcile,
   exfiltration/redaction, generation budgets, and approval-surface denial.
2. **Approval resume hardening.** Integration and rollback must not trust a
   caller-supplied `context.confirmation.approved?` flag by itself. The final
   dynamic action/loader boundary must verify the stored confirmation id,
   target action, approved status, resolver surface, and same-channel rule before
   any live authority changes.
3. **Settings and validator contract alignment.** The shipped v0.37 loader
   accepts only reviewed read-only actions. Settings Central must not imply that
   generated apps, panels, settings fragments, memory namespaces, objective
   wiring, or higher permissions are currently loadable. Either narrow the
   validators to `target=action` and `permission=read_only`, or wire both
   Settings Central and `TrustedValidator` to the same explicit intersection.
4. **Dynamic lifecycle audit.** Emit redacted lifecycle records or signals for
   draft request, sandbox report consumption, tier transition, integration
   attempt, trusted-validation denial, overlay registration, rollback,
   emergency disablement, and boot reconciliation denial. Emergency disablement
   must be reviewable by operators.
5. **Durability and diagnostics.** Failed mid-integration attempts must leave no
   ambiguous integrated root or overlay authority, and repeated sandbox reports
   should append bounded diagnostics/report history instead of replacing all
   previous diagnostics.
6. **Documentation reconciliation.** ADRs, roadmap, request flow, operator docs,
   developer docs, changelog, and release notes must distinguish the shipped
   read-only action lifecycle plus inert codegen scaffold from deferred advisory
   provider authoring and broader generated app/config targets.

Implementation result: M0-M4 completed the plan update, approval/settings
hardening, v0.37 security eval coverage, dynamic lifecycle audit, bounded report
history, failed-attempt cleanup, release-doc reconciliation, and final gates.

Verification: focused dynamic plugin, confirmation, settings, sandbox
bridge, and security eval suites first; then finish with
`mix compile --warnings-as-errors`, `mix format --check-formatted`,
`mix credo --strict`, `mix dialyzer`, `git diff --check`, and `mix precommit`.
These gates passed on 2026-05-25. No v0.37.1 tag is allowed until the operator
manual smoke is complete.

### M8: v0.37.2 capability-first generator and useful pure-code runtime

The second post-implementation audit found that v0.37.1 had strong safety
plumbing but did not yet satisfy the milestone's purpose: Allbert must create
and run useful code, not only deny unsafe code. M8 is release-blocking and
implements that missing generator loop for read-only action targets.

Required implementation:

1. **Useful trusted pure-code surface.** Expand `TrustedValidator` so reviewed
   read-only generated actions can express ordinary pure Elixir: arithmetic,
   comparisons, boolean operators, string concatenation/interpolation,
   anonymous functions/captures whose bodies validate, `case`/`cond`/`if`/
   `with`/`for`, and a curated per-function allowlist from pure standard
   modules such as `Enum`, `String`, `Map`, `Keyword`, `List`, `Integer`,
   `Float`, `Tuple`, `Date`, `Time`, and `DateTime`. Keep compile-time
   execution bans, protected runtime authority denials, read-only permission
   ceiling, and default-deny for unknown remote calls.
2. **Positive runnable capability.** Add an acceptance test for a generated
   read-only action that formats a response, does arithmetic/comparison, and
   iterates a list, then integrates and runs through `Actions.Runner.run/3`.
   The milestone is not done until this positive path passes.
3. **LiveView approval resume.** Make loader-side dynamic confirmation
   re-checks derive approval surface from the persisted resolver channel using
   the same channel-first mapping as `approve_confirmation`, then add CLI and
   LiveView allowed-path tests plus Telegram/email/cross-channel denial tests.
4. **LLM-backed generator.** Replace inert-only `Codegen.Producer` behavior
   with a real source-bearing producer for target `action`. It uses an
   injectable provider boundary around `Jido.AI.generate_object/3`,
   writes reviewed source, manifest, generated tests, hashes, provenance,
   usage, prompt hashes, and repair diagnostics under the draft root, and
   grants no live authority.
5. **Generator roles.** Implement the minimum model-backed role pipeline needed
   for a useful action draft: Planner, Author, TrialAuthor, Critic/QualityGate,
   and Repair. Invoked roles emit schema-constrained packets with model/profile,
   prompt hash, usage when available, redacted diagnostics, and authority
   `none`. Repair is invoked when evidence or Critic requests repair, not as a
   fixed extra call on every clean generation. v0.37.2 may use one configured
   provider profile for all roles, but only Author calling the LLM is not
   sufficient.
6. **Bounded critic/repair loop.** Drive repair from deterministic validation
   and v0.36 sandbox/gate evidence. Trial/gate failure, critic veto, or trusted
   precheck failure can request Repair; Repair writes a new revision rather than
   mutating prior evidence. The loop stops on deterministic acceptance or on
   `dynamic_codegen.max_repair_iterations`, provider-call budget,
   provider-usage budget, wall-clock timeout, or repeated identical failure.
   Critic output is advisory only and cannot trust, integrate, or advance tiers.
7. **Eval precision.** Tighten codegen security evals to assert specific denial
   tags where practical, add a real permission/body mismatch fixture, and add
   paired allow/deny tests for widened pure functions versus protected
   neighbors such as `String.to_atom/1`, `System.*`, `File.*`, `Code.*`,
   `apply/3`, and effectful anonymous-function paths.
8. **Remote generation smoke.** Add an opt-in manual/CI smoke that sources
   `.env` for remote LLM credentials and runs an actual generation request
   without printing secrets. Deterministic fake-provider tests remain the
   default warning-gate path.
9. **Documentation reconciliation.** Update ADRs, roadmap, request flow,
   operator docs, developer docs, and changelog so v0.37.2 is described as
   bounded model-backed role generation plus gated live integration, not an
   inert scaffold or a single Author-only model call.

Verification: run focused validator, loader, codegen, dynamic action, security
eval, and Mix task suites first; run the opt-in `.env` remote-generation smoke
when credentials/network are available; then finish with
`mix compile --warnings-as-errors`, `mix format --check-formatted`,
`mix credo --strict`, `mix dialyzer`, `git diff --check`, and `mix precommit`.
No v0.37 release tag is allowed until deterministic gates pass and the operator
manual smoke instructions cover both fake-provider and real-provider
generation.

### M16: v0.37.2 implementation-audit workflow closeout

The implementation audit after M15 found one remaining acceptance gap: the
source-bearing generator, sandbox bridge, trusted validator, live loader, and
rollback path existed, but the public producer path still stopped after draft
creation. M16 closes that gap with a producer-neutral workflow facade.

Required implementation:

1. Add `DynamicPlugins.request_draft_with_gate/3` as the explicit generated
   action workflow for one capability gap. It requests a source-bearing draft,
   runs v0.36 trial evidence, runs v0.36 gate evidence, runs the trusted
   validator over the gate-passed revision, and returns only evidence. It does
   not integrate or grant live authority.
2. Treat sandbox compile/test/gate failures, source-policy denials, timeouts,
   and trusted-validation failures as bounded repair evidence. Invoke Repair
   through the existing model-backed role path, create a new draft revision,
   rerun trial/gate/validation, and stop on pass, repair-iteration limit,
   whole-workflow provider-call/usage limit, wall-clock timeout, or repeated
   identical evidence.
3. Keep Critic and Repair advisory. They can request or produce repairs from
   bounded evidence, but deterministic validators, sandbox reports, trusted
   validation, and operator confirmation remain the only acceptance/trust
   authorities.
4. Add deterministic fake-backend acceptance coverage for the full generated
   read-only action path: draft -> trial -> gate -> trusted validation ->
   integration confirmation -> live run through `Actions.Runner.run/3` ->
   rollback confirmation -> registry removal.
5. Tighten v0.37 security eval precision where stable by asserting concrete
   denial reasons and by making `codegen-permission-body-mismatch-001` a real
   read-only declaration whose body attempts protected authority.

Verification: focused codegen workflow and v0.37 security eval tests first;
then run the v0.37 dynamic-plugin focused suite, the opt-in `.env` remote LLM
smoke when explicitly approved, and the full warning gate before commit.

### M17: v0.37.3 delegated write actions

The implementation audit after M16 found a scope miss: useful generated code
must be able to write through reviewed capabilities with normal approvals, not
only compute read-only responses. M17 keeps generated code narrow by requiring
effectful work to delegate through reviewed facade actions.

Required implementation:

1. Add `dynamic_codegen.allowed_facades` as a Settings Central list whose
   shipped hard ceiling is `append_memory` and `external_network_request`.
   Defaults stay closed: delegated writes require both an enabled generated
   permission in `dynamic_codegen.allowed_action_permissions` and an enabled
   facade in `dynamic_codegen.allowed_facades`.
2. Add `AllbertAssist.DynamicPlugins.Delegate.run/3`. It resolves only
   allowlisted reviewed facades through `AllbertAssist.Actions.Registry` and
   executes them through `AllbertAssist.Actions.Runner.run/3`, returning the
   facade's normal response or failure. Host execution, package install, shell,
   sandbox, secret, Settings, confirmation-decision, and trust-control facades
   remain outside the hard ceiling regardless of settings.
3. Update the trusted validator so generated actions may declare only the
   configured subset of the hard permission ceiling (`:read_only`,
   `:memory_write`, `:external_network`). Non-read-only generated actions must
   call `AllbertAssist.DynamicPlugins.Delegate.run/3` with a binary string
   literal facade name in the configured allowlist, and the declared generated
   permission, response action metadata, and delegated facade permission must
   agree. Non-literal facade names, non-allowlisted facades, direct protected
   calls, and permission/body mismatches are denial.
4. Update the LLM Author/Critic prompts and action target helper so the
   committee can choose either a pure read-only template or a delegated facade
   template. The prompt must make `Delegate.run/3` the only effect path and must
   include only the configured facade names.
5. Keep generated actions `resumable?: false`; v0.37.3 deliberately reuses the
   reviewed facade's existing Security Central behavior instead of adding a
   generic dynamic-action confirmation resume path. Delegated facade approvals
   use the facade's normal approval surface rules, not
   `dynamic_codegen.integration_approval_surfaces`, which remains scoped to
   integration and rollback hot-load authority.
6. Add focused tests and security evals for append-memory delegation,
   external-network confirmation creation through delegation, facade allowlist
   denial, non-literal facade-name denial, generated permission/facade mismatch
   denial, protected-call denial, and the hard permission ceiling.

Verification: focused settings, delegate, trusted-validator, codegen workflow,
dynamic action, external-network confirmation, and v0.37 security eval suites
first; then run the opt-in `.env` remote LLM generation smoke when explicitly
approved and the full warning gate before commit.

Implementation status: completed for the shipped action-only scope. The
implementation adds `dynamic_codegen.allowed_facades`,
`AllbertAssist.DynamicPlugins.Delegate.run/3`, validator-enforced permission /
facade agreement, delegated templates in the LLM prompts, deterministic fake
provider coverage, real `.env` OpenAI delegated-memory smoke validation, and
the full warning gate.

### M18: v0.37.4 audit-remediation plan docs

The third post-implementation audit found release-readiness gaps rather than a
new capability surface. M18 records the implementation-ready remediation plan
before code changes so the follow-up commits can be reviewed against one
explicit checklist.

Required implementation:

1. Update `docs/plans/v0.37-plan.md`, `docs/plans/v0.37-request-flow.md`, and
   `docs/plans/roadmap.md` with the v0.37.4 audit-hardening scope, milestones,
   expected action/CLI surfaces, security eval rows, and verification gates.
2. Make the permission decision explicit: `request_dynamic_draft` moves from
   historical `:skill_write` to a new `:dynamic_codegen_request` permission
   class whose default is `allowed`. The master switches, provider enablement,
   credentials, budgets, explicit-gap requirement, sandbox/gate, and operator
   confirmation remain the substantive safety controls.
3. Keep the delegated invocation approval decision explicit: generated
   delegated actions remain non-resumable and facade-owned confirmations use
   the reviewed facade's normal Security Central approval/resume behavior.
   `dynamic_codegen.integration_approval_surfaces` remains scoped to hot-load
   integration and rollback confirmations only.

Verification: docs-only `git diff --check`, then commit this plan milestone
before implementation.

### M19: registered discard draft workflow

The draft tier model and ADR 0033 already define `:discarded`, but operators
cannot currently reach it through a registered action. M19 makes discard an
actual operator workflow instead of store-only domain logic.

Required implementation:

1. Add `AllbertAssist.Actions.DynamicPlugins.DiscardDraft`, registered as
   `discard_dynamic_draft`.
2. Initially use permission `:settings_write` and confirmation `:not_required`,
   matching the emergency-disable cleanup pattern. M25 supersedes this with
   dedicated permission `:dynamic_codegen_discard` /
   `permissions.dynamic_codegen_discard`, default `allowed`, so discard is not
   mislabeled as a settings write.
3. Deny discard for `:integrated` artifacts until rollback removes live
   authority. Permit non-integrated and rolled-back drafts. Preserve the
   terminal `:discarded` invariant.
4. Add `mix allbert.dynamic drafts discard <slug>` and update CLI usage.
5. Add registry, action, metadata/audit, Mix task, and security-eval coverage.

Verification: focused dynamic metadata/action/Mix/eval suites before commit.

Implementation status: completed in M19 with registered action
`discard_dynamic_draft`, `mix allbert.dynamic drafts discard <slug>`, audit and
signal emission through the existing draft discard store path, registry/action
tests, Mix task tests, and `codegen-discard-draft-001`.

### M20: delegated-write security eval refresh

M17 added unit coverage for delegated writes, but the v0.37 security eval
matrix must also exercise the release-critical scenarios.

Required implementation:

1. Update `codegen-permission-ceiling-001` so it explicitly asserts default
   denial of `:memory_write` when the generated permission setting remains
   `["read_only"]`.
2. Add positive eval coverage for a delegated `:memory_write` action accepted
   only when the generated permission and `append_memory` facade are both
   enabled.
3. Add positive eval coverage for a delegated `:external_network` action that
   creates the normal `external_network_request` confirmation.
4. Add denial evals for disabled runtime facade, non-literal facade name,
   generated permission/facade mismatch, protected direct calls, and rollback
   restoring an unauthorized registry state.

Verification: `mix test apps/allbert_assist/test/security/dynamic_codegen_eval_test.exs`
plus focused delegated validator/loader suites before commit.

Implementation status: complete. The security inventory now includes delegated
write-only, facade allowlist, literal facade, permission/facade/response
coherence, delegated memory allow, delegated network confirmation,
runtime-facade-disabled, delegated rollback authority, and direct-effect denial
rows. The evals exercise both trusted validation and live overlay invocation so
delegated writes are covered at the same level as the original read-only gate.

### M21: dynamic-codegen request permission

`request_dynamic_draft` currently reuses `:skill_write`, which broadens a
historical skill-scaffold permission into LLM-backed source generation. M21
separates the audit trail and operator control.

Required implementation:

1. Add `permissions.dynamic_codegen_request` to Settings Central, Security
   Central policy/risk/type surfaces, status output, settings actions, and
   tests. Default decision: `allowed`.
2. Move `request_dynamic_draft` permission and action metadata from
   `:skill_write` to `:dynamic_codegen_request`.
3. Update registry, runner/security, intent/security-eval inventory, and docs
   references so historical `:skill_write` returns to local skill scaffolding
   semantics.

Verification: focused security/settings/registry/action/codegen tests before
commit.

Implementation status: complete. `request_dynamic_draft` now advertises and
authorizes `:dynamic_codegen_request`; Settings Central exposes
`permissions.dynamic_codegen_request` with default `allowed`; Security Central,
risk/status surfaces, registry tests, request-action tests, and
`codegen-request-permission-001` prove that denying `:skill_write` no longer
blocks draft requests while denying `:dynamic_codegen_request` does.

### M22: validator coherence and low-risk polish

M22 tightens contracts where the implementation currently accepts misleading or
unnecessarily narrow generated code.

Required implementation:

1. Collect delegated facade calls and response action permission metadata from
   the generated `run/2` body, not from dead helper code elsewhere in the
   module.
2. Keep `Delegate.run/3` as the only generated effect bridge and add a code
   comment explaining why `AllbertAssist.DynamicPlugins.Delegate` is not in the
   protected remote-prefix list.
3. Add safe allowlist support for literal `@spec`, `@type`, and `@typep`
   attributes and for `||` alongside `or`.
4. Tighten `use AllbertAssist.Action` option validation for generated actions:
   pin allowed `:permission`, `:exposure`, `:execution_mode`, `:confirmation`,
   `:skill_backed?`, and `:resumable?` values.
5. Leave `disable_dynamic_live_loader` on `:settings_write` because it is an
   emergency safety-reducing switch that removes dynamic authority; document
   that rationale rather than adding confirmation friction.

Verification: focused trusted-validator, loader, registry, and security eval
suites before commit.

Implementation status: complete. Trusted validation now accepts literal
typespec attributes and `||`, documents the Delegate special case, pins
generated `AllbertAssist.Action` capability options, and treats delegated
facade evidence plus response action permission metadata as valid only inside
`run/2`. Delegate calls in helpers are rejected so dead or misleading helper
code cannot satisfy a generated write permission. Emergency disablement remains
`:settings_write` and is documented as an authority-reducing stop.

### M23: version, docs, release gates, and push

M23 closes reviewer-facing drift and validates the full release candidate.

Required implementation:

1. Rewrite AGENTS.md and DEVELOPMENT.md dynamic-loading wording: dynamic loading
   is permitted only through the implemented v0.37 path after the required
   settings, sandbox/gate evidence, trusted validation, and operator
   confirmation; every other dynamic loading path remains forbidden.
2. Bump umbrella/core/web app version metadata to the chosen pre-tag release
   version, `0.37.4`, so the running code reflects the delegated-write plus
   audit-hardening scope.
3. Split or clarify CHANGELOG v0.37.1/v0.37.2/v0.37.3/v0.37.4 entries so the
   pre-tag reopens are reviewable.
4. Update operator/developer docs for discard, the new request permission,
   delegated invocation approval surfaces, validator contract, and manual
   smoke commands.
5. Run final gates: focused suites, `mix compile --warnings-as-errors`,
   `mix format --check-formatted`, `mix credo --strict`, `mix dialyzer`,
   `git diff --check`, and `mix precommit`.
6. Commit each completed milestone using the v0.37 title convention and push
   `main` after the final green gate.

Implementation status: complete. The release candidate now reports version
`0.37.4` across the umbrella, core app, and web app. AGENTS.md,
DEVELOPMENT.md, README.md, roadmap, developer context, runtime-boundary, plan,
request-flow, operator/developer, and changelog docs are reconciled to the
implemented v0.37 path: dynamic loading is permitted only through the audited
draft, sandbox gate, trusted validation, explicit confirmation, and reversible
integration workflow. Final gates passed with `mix compile --warnings-as-errors`,
`mix format --check-formatted`, `git diff --check`, `mix credo --strict`,
`mix dialyzer`, and `mix precommit`.

### M24: fourth-audit closeout plan

M24 records the fourth audit decision before implementation so the v0.37.5
closeout is executable and reviewable.

Required implementation:

1. Preserve v0.37's runnable-by-design posture: generated delegated actions may
   call reviewed facades, and invocation-time facade confirmations intentionally
   follow the facade's normal channel policy rather than the
   integration/rollback-only dynamic approval-surface restriction.
2. Plan a dedicated `:dynamic_codegen_discard` permission and
   `permissions.dynamic_codegen_discard` setting, default `allowed`, so discard
   is no longer mislabeled as `:settings_write`.
3. Keep `discard_dynamic_draft` confirmation-free for all non-integrated tiers,
   including `:gate_passed`, and document that this can irreversibly remove
   draft/gate evidence but never removes live authority.
4. Track the stale audit observations as already resolved where applicable:
   literal typespecs, `||`, and v0.37 eval inventory equality are already
   implemented/tested.

Verification: docs-only `git diff --check`, then commit this plan milestone
and push.

### M25: fourth-audit implementation closeout

M25 implements the v0.37.5 audit decisions and validates the full release
candidate again.

Required implementation:

1. Add `:dynamic_codegen_discard` to Security Central policy, risk
   classification, PermissionGate types, Settings schema/defaults, and
   operator-visible permission status.
2. Move `discard_dynamic_draft` to permission/execution mode
   `:dynamic_codegen_discard`; update registry assertions, action response
   metadata, tags, docs, and security-eval rows.
3. Persist `context[:dynamic_codegen_delegate]` into facade confirmation
   `runner_metadata` for traceability, without using that metadata to deny or
   restrict delegated invocation approvals.
4. Update operator/developer docs and CHANGELOG to state that
   `dynamic_codegen.integration_approval_surfaces` applies only to integration
   and rollback; delegated facade confirmations follow normal facade policy.
5. Add/update tests for the new permission, discard denial, gate-passed
   confirmation-free discard, delegated external-network normal approval policy,
   and dynamic-delegate metadata persistence.
6. Bump umbrella/core/web metadata to `0.37.5`.
7. Run focused tests, `mix compile --warnings-as-errors`,
   `mix format --check-formatted`, `git diff --check`, `mix credo --strict`,
   `mix dialyzer`, and `mix precommit`; commit and push.

Implementation status: complete. The release candidate now reports version
`0.37.5` across the umbrella, core app, and web app. Security Central,
Settings Central, PermissionGate, registry metadata, discard action behavior,
security evals, and operator/developer docs all use
`:dynamic_codegen_discard` / `permissions.dynamic_codegen_discard` for terminal
non-live draft cleanup. Delegated facade confirmations persist dynamic delegate
metadata for audit and intentionally continue to use the reviewed facade's
normal confirmation policy. Final gates passed with focused M25 tests,
`mix compile --warnings-as-errors`, `mix format --check-formatted`,
`git diff --check`, `mix credo --strict`, `mix dialyzer`, and `mix precommit`.

## Non-Goals

- No sandbox backend implementation; v0.37 consumes v0.36.
- No live integration without v0.36 gate pass and operator confirmation.
- No integration authorized by advisory/agent output or auto-trial result.
- No package-manager execution, dependency additions, migrations, NIFs, ports,
  unrestricted network, remote marketplace, remote binary loading, dynamic
  protocol/router generation, or application env mutation.
- No generated action body may hide host execution, package install, skill/script
  execution, sandbox execution, secret access, confirmation decisions,
  integration control, or direct protected-subsystem writes behind a lower
  declared permission.
- No multi-language target beyond Elixir/OTP.
- No templated Create destination or Mix generator UX; that is v0.38.

## Definition Of Done

v0.37 is done when Allbert can detect an objective-owned capability gap,
generate Elixir/OTP code and config through bounded model-backed advisory
roles, repair against deterministic sandbox/gate evidence, trial and gate the
accepted revision through the v0.36 sandbox from a scanned, hash-matched
project-shaped staging tree, and — only after explicit operator confirmation —
hot-load and register it live without a restart, with audit, validated
action/app security semantics, file-backed draft provenance, trusted-compile
AST validation, integration-root persistence, collision-safe actions overlay,
runtime call-target validation, generated-permission ceiling, literal
macro/attribute validation, state-only child limits, all-or-nothing integration,
rollback-before-upgrade revision semantics, boot reconciliation anchored in
Security Central, emergency disablement, and operator-confirmed rollback that
removes live authority.
Security evals prove untrusted, unscanned, ungated, unconfirmed, tampered,
shadowing, trusted-compile side-effect, AST allowlist bypass, runtime
protected-call bypass, generated permission-ceiling bypass, permission/body
mismatch, macro literal-option bypass, manifest/module mismatch,
partial-integration, revision-upgrade, dynamic child effects, generated
resumability, generation-budget exhaustion, emergency-disable bypass,
restart-reconciliation tamper, private-objective-loop, low-confidence autogen,
integration-approval-surface bypass, and advisory-authorized paths fail closed.
Operator docs explain enablement,
provider cost/network budgets, evidence review, integration, rollback, and
emergency disablement; developer docs explain the draft metadata/root, staging
contract, trusted AST/body validator, permission ceiling, loader, overlay,
integrated root, and plugin discovery separation.
