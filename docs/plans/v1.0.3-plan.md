# Allbert v1.0.3 - Test Isolation Phase 2 & Catch-up Binary Release

Status: **implementation in progress — M0 baseline and M1 (ADR 0086
contracts + four pilots) done; plan audited 2026-07-19 against HEAD
`98d3bf39`, the committed manifest/metrics summary, ADR 0086, and current
release tooling.**
Supersedes
the `future-features.md` "Test Suite Speed & Isolation" entry, whose phase-2
closeout content folds in here VERBATIM (backlog entry removed per the
operator's 2026-07-19 direction; no information loss — every scoping fact it
carried appears in Current State / Code Seams below).

**Operator decisions of 2026-07-19 (recorded):**

1. **v1.0.3 exists and is next** — Test Isolation Phase 2 is a dedicated
   version, ahead of v1.1 (roadmap ladder re-sequenced; v1.1 M0 re-anchored
   to the v1.0.3 tag).
2. **The catch-up binary lands HERE (operator final disposition
   2026-07-19).** Decision history: operator transfer → audit restoration
   to v1.0.2 → operator final call returning it here. v1.0.3 M10 is the one
   packaged catch-up: the artifact line moves 1.0.0 → 1.0.3 carrying the
   v1.0.1 + v1.0.2 + v1.0.3 fixes together. v1.0.2 closed as a
   `[skip-artifacts]` source/docs tag after its M8.9-M8.12 quality bars
   (`1d41956c` remains its clean implementation-evidence SHA).
3. **future-features entry removed** — this plan IS the entry's lifecycle
   continuation; phase-3 remainders (if any) return to the operator as
   intake candidates in Build Progress, never unilaterally.

## Build Progress

M0–M3 done (entries below, newest first). Each milestone: implement →
focused test →
drift-check → doc update → commit (`v1.0.3 M<n> <small title>`). Build
entries land here per the v1.0.2 precedent: root-caused drift fixes,
measured acceptance cited from the test-metrics store, intake candidates
held for operator disposition.

**First-pass implementation readiness review (2026-07-19, post-v1.0.2
closure):** stale scratchpad decide protocol replaced by `bench-decide`
corpus decide-v1 throughout (Current State, M0, M7, request-flow §D);
final v1.0.2 measured numbers cited (−83.7%/turn, dirty:false rows); M0
baselines require M8.10 provenance; M4–M6 waves adopt the M8.9 manifest +
drift gate as the standing no-loss ledger; M10 gains the explicit platform
validation matrix — macOS, Linux (both container artifacts + policy
real-host service/vault, PASS or owned SKIP), and **Windows/WSL2
operator-restored** (Linux tarball in WSL2, Tier-2 per install.md,
ending the standing post-1.0 deferral); request-flow §E gains the Linux
and WSL2 rows. Second-pass readiness runs after the operator reviews
these dispositions. **Second-pass readiness repair (2026-07-19):** DB lane
semantics, app-env context, ListChannels propagation, campaign cardinality,
bounded waves, dependency refresh, exact `release.v103` steps, and the
paste-executable binary flow were reconciled before implementation.

**M2 DONE — code + proofs (2026-07-20); spot campaign folded into M3's run.**
DBConnection-ownership class retirement, fixed at the ownership root. The root
is NOT a missing allowance — it is the sandbox OWNERSHIP LEASE.
`DBConnection.Ownership.Proxy` grants the sandbox owner an `:ownership_timeout`
lease that defaults to **120_000 ms** and is completely independent of ExUnit's
per-test `:timeout` budget. `SidebarConsolidationTest` "every destination …
deep-linkable" is the ONLY test in the umbrella whose declared budget
(240_000 ms) exceeds that lease, and it measures **82.8 s solo**
(`mix test …/v061b/sidebar_consolidation_test.exs --slowest 3`, exit 0, 3 tests,
98.8 s file wall). Under full-monolith load it crosses 120 s — hence
deterministic 20/20 there, absent from every lighter gate lane. When the lease
fires the proxy disconnects, the shared owner goes down, the pool mode reverts
to `:manual`, and the next LiveView mount raises the campaign signature.
RECON (recorded, all unpiped exits): the class does NOT reproduce in the web
monolith — the whole `allbert_assist_web` suite in ONE VM (`mix test --seed
1000`, owned env triple) is **293 tests / 0 failures, 1,775.2 s, exit 0**, with
the sidebar sweep passing; the neighborhood is the loaded umbrella VM, not a
web neighbor. The mechanism was then isolated directly: a workspace mount under
a deliberately short lease raises
`(DBConnection.ConnectionError) owner … timed out because it owned the
connection for longer than Nms (set via the :ownership_timeout option)`
followed by
`(DBConnection.OwnershipError) cannot find ownership process for #PID<…>
({Phoenix.LiveView, AllbertAssistWeb.WorkspaceLive, …}) (AllbertAssist.Repo)
using mode :manual` out of `WorkspaceLive.mount/3 →
Conversations.recent_general_thread/1`.
RED (recorded): with ONLY the lease derivation reverted in
`DataCase.setup_sandbox/1` (back to `Sandbox.start_owner!(Repo, shared: not
tags[:async])`), the permanent minimal-composition regression
`apps/allbert_assist_web/test/allbert_assist_web/v103/sidebar_ownership_test.exs`
FAILS. Command (from `apps/allbert_assist_web`, owned env triple, unpiped):
`mix test test/allbert_assist_web/v103/sidebar_ownership_test.exs > red.log
2>&1; echo $?` → **exit 2, 3 tests / 2 failures, 121.8 s**, the boundary test
dying on the exact campaign signature (`cannot find ownership process … using
mode :manual`, raised from `mount_workspace_until/3`). Restored, the same
command is **exit 0, 3 tests / 0 failures, 156.1 s**.
FIX: `AllbertAssist.DataCase.setup_sandbox/1` now sizes the lease from the
test's own declared budget via the new `DataCase.sandbox_ownership_timeout/1`
(`budget + 30_000 ms` headroom; `:infinity` budgets get an infinite lease; a
test with no integer budget keeps the DBConnection 120_000 default). The
invariant: **ExUnit's timeout, never the sandbox lease, is the deadline that
fires.** This is a SINGLE-ROOT fix — `AllbertAssistWeb.ConnCase` delegates to
the same function, so every DataCase and ConnCase test in the umbrella, and
therefore every LiveView mount and every process that reaches the connection
through the case-provided owner, is covered by one change. The two
`@tag timeout: 180_000` canvas-tiles tests were latent members of the same
class and are retired by the same fix. `setup_sandbox/1` additionally returns
the owner pid and its `on_exit` tolerates an owner the test retired itself;
`ConnCase` exposes it as `:sandbox_owner` so the regression can drive the
lease boundary explicitly.
No assertion removed or weakened: `sidebar_consolidation_test.exs` is
BYTE-IDENTICAL (untouched), as is every other test file — the only edits are
the two case templates plus the new regression file.
PROOFS (from `apps/allbert_assist_web`, owned env triple, unpiped exits):
regression solo ×3 exit 0 (3 tests / 0 failures each; `--slowest`: boundary
test 152.3 s, signature test 1.5 s, contract test 0.2 ms); both orders with
`v061b/sidebar_consolidation_test.exs` (`--seed 0`) exit 0, 6 tests / 0
failures each (259.6 s / 261.3 s); `live/workspace` + `v061b` + `v103`
together **123 tests / 0 failures, 1,632.6 s, exit 0**. Core DB surface
(DataCase is umbrella-wide): `objectives` + `conversations_test` + `settings` +
`list_channels_context_test` from `apps/allbert_assist` — **115 tests / 0
failures, exit 0**. Statics clean: `MIX_ENV=test mix compile --force
--warnings-as-errors`, `mix credo --strict` (1,346 files, no issues),
`mix format --check-formatted`, all exit 0.
NO-LOSS: `inventory --manifest` regenerated — **3,213 → 3,216 rows, +3
(exactly the three new regression identities), zero rows removed, zero lanes
changed** (`git diff --stat`: 3 insertions, 0 deletions);
`inventory --check-manifest` exit 0; `inventory --check-tags` exit 0 (523
files, zero unclassified, zero double-counts). NO `@lane_adjudications` entry
was needed — the heuristic classifies the new file `liveview_serial` on its own
(`db;liveview` resource classes via ConnCase), which is the audited lane.
COST (flagged for operator disposition): the boundary proof is inherently
expensive — a red-first proof that a 120 s lease is too short REQUIRES crossing
120 s of real work, and no cheaper honest formulation exists (the fix only ever
lengthens the lease, so any shorter workload is green pre-fix). The file adds
≈156 s to the `liveview_serial` lane, which pushes against the M0 target
"M2+waves liveview ≤300 s max partition"; the packer will bin it, but the
operator may wish to give it a dedicated partition.
UNVERIFIED RESIDUE (stated honestly): the monolith failure itself was not
re-observed in this session — no full-monolith run was executed per the build
instruction. The diagnosis rests on the recon above (only test whose budget
exceeds the lease; 82.8 s solo vs a 120 s lease; deterministic-not-racy 20/20;
absent from the lighter lanes and from the 1-VM web suite; signature reproduced
exactly). The folded M2+M3 five-seed spot campaign is the acceptance.

**M2 operator-agent verification (2026-07-20), appended:** (a) RED
independently re-proved — stashing BOTH support files and
`compile --force` makes the new regression fail 3/3 with
`OwnershipError`/`using mode :manual`; restored, it passes. (b) The
boundary test raced once for me on `render_click` → `has_element?` (the
v1.0.2 M8.6 async-canvas class); a `has_element_until?/3` waiter on the
established 50 ms × 20 budget was added — the assertion is unchanged, only
its settle window. (c) GATE-PATH TRUTH: `fast-local --web-lanes
--partitions 4` (owned per-partition homes/DBs — the way release gates run
web) is GREEN with this fix, 4/4 partitions, 218 tests, 0 failures.
(d) PRE-EXISTING FINDING, not M2's: a bare
`mix test test/allbert_assist_web/v061b/sidebar_consolidation_test.exs`
fails 2/3 with render-race text and ZERO ownership errors, IDENTICALLY
with and without the M2 fix — i.e. that file is not independently runnable
outside the gate's owned environment. Recorded as an intake candidate
(bare-invocation environment dependency); it is not a gate concern and was
not silently "fixed" here. (e) COST recorded: the boundary proof adds
~156 s to `liveview_serial`; the M0 target (liveview max partition ≤300 s)
is now tighter — M9 re-measures and may give the file a dedicated
partition.

**M1 DONE (2026-07-20).** ADR 0086 flipped **Accepted**: all five contracts
documented in test-strategy ("v1.0.3 M1 Conversion Contracts" — contract 5
as the executable 2-VM experiment protocol for M6) and four red-first
pilots landed, one per convertible class, each through its class contract
only. Evidence (all focused commands from `apps/allbert_assist`, exits
captured unpiped as `cmd > log 2>&1; echo $?`):

- **db pilot** `objectives/objective_test.exs` → `db_partition_safe`
  (contract 1). RED (recorded): (a) flipping only `async: true` fails
  "public facade scopes reads…" with `DBConnection.OwnershipError` —
  `Objectives.frame/cancel` delegate through the long-lived Engine agent
  whose Jido `Task.Supervised` children carry the agent, not the test, in
  `$callers`; (b) under the old `async: false` shared mode the committed
  contract-1 ownership-proof test is RED (out-of-chain `spawn` reads
  ambiently, `{:ok, 0}` — no ownership fence). CONVERSION: non-shared
  per-test owner (`use AllbertAssist.DataCase, async: true,
  lane: :db_partition_safe`) + new `DataCase.allow_sandbox/2` allowance
  helper (engine agent allowed per test). PROOFS: solo ×3 exit 0 (6 tests,
  0 failures each); both orders with `objectives/commands_test.exs`
  (`--seed 0`) exit 0 (9 tests each); lane runner wired — core lane list
  now includes `db_partition_safe`, `serial-core --lane db_partition_safe
  --partitions 2` exit 0 (p1 6 tests / 0 failures, p2 empty-green).
  Repo-backed files stay partition-executed, never pure_async (SQLite
  in-VM async left unproven by design — amendment requires evidence).
- **app_env pilot** `intent/eval/gate_test.exs` → `pure_async`
  (contract 2). RED (recorded): substituting the pre-conversion
  `System.put_env("ALLBERT_HOME", …)` + `Application.delete_env` idiom
  into the committed contract-2 proof test's concurrent children is
  deterministically RED — the strict-floor child read the lenient child's
  last-written global home (`{:strict, :ok}`). CONVERSION: new minimal
  `AllbertAssist.ConfigContext` seam (ADR 0086 contract-2 shape
  `[home:, settings_root:, app:]`; `Paths.home/0` + `Paths.settings_root/0`
  consult the process-local context FIRST, omission preserves the prior
  chains byte-for-byte — additive only); Gate calls run in bounded
  `with_context`; descriptor resolution moved off the global registries to
  a private full shipped baseline
  (`RegistryIsolationFixtures.start_shipped_registries/1`) handed through
  the public `descriptors:` option; owned homes pid-qualified+pre-cleaned.
  PROOFS: solo ×3 exit 0 (5 tests each); both orders with
  `intent/golden_set_test.exs` exit 0 (10 tests each); negative concurrent
  two-context proof (contradictory operator floors, rendezvous before
  read, context-free parent still resolves the 0.85 default) green.
- **global_process pilot** `actions/app_actions_test.exs` → `pure_async`
  (contract 3). RED (recorded): pointing the committed two-context
  negative-proof test at the GLOBAL registry pair (pre-conversion idiom)
  is deterministically RED — `{:error, {:app_id_taken, :allbert}}`, the
  singleton cannot host two contexts. CONVERSION: private supervised
  registry pairs (unique pid/partition-qualified names + ETS tables,
  `side_effects: false`); reads travel the internal registry context —
  `ListApps`/`ShowApp` adopted the Runner `:registry` context-map key
  (ADR 0082 pattern, additive; production call sites pass nothing —
  default-path identity proven by focused
  `actions/registry_test.exs` + `agents/intent_agent_test.exs`, 43 tests
  exit 0). Log assertions preserved via module-scoped
  `Logger.put_module_level(AllbertAssist.Signals, :info)` deleted on_exit
  (replaces the VM-wide `Logger.configure`; no async test in the suite
  captures logs — verified). PROOFS: solo ×3 exit 0 (5 tests each); both
  orders with `registry_context_test.exs` exit 0 (10 tests each).
- **home_fs pilot** `actions/browser_actions_test.exs` — converted
  WITHIN-CLASS to the contract-4 idiom; **stays `home_fs_serial` by
  recorded ADR 0031 decision**. RED (recorded): reverting the pre-clean
  helper to the pre-conversion non-cleaning idiom makes the committed
  owned-root proof RED (planted stale `settings.yml` survives — the
  v1.0.2 M5 ranker-flake class); two fresh VM boots minted the identical
  first `unique_integer` suffix (2690 = 2690), proving the cross-boot
  collision class. CONVERSION: root now OS-pid-qualified AND pre-cleaned;
  permanent owned-root proof (every exercised Paths read inside the owned
  root + pre-clean removes planted poison). STAY reasons (seam gaps →
  intake candidates below): ADR 0031-guarded `:allbert_browser, :driver`
  Application-env read; named `AllbertBrowser.Supervisor`/`Session`
  singletons without constructor overrides (contract-3 seam); Paths/
  Settings/Confirmations env writes read by browser processes, out of
  `ConfigContext` reach without those production seams. PROOFS: solo ×3
  exit 0 (13 tests each); both orders with `intent/eval/corpus_test.exs`
  exit 0 (19 tests each).

No-loss ledger: `inventory --manifest` regenerated
(`docs/validation/test-manifest.csv`, 3,211 rows — 13 pre-existing test
identities re-laned in place, 3 permanent contract-proof tests added, zero
rows removed); `inventory --check-manifest` exit 0; `--check-tags` exit 0
(521 files, zero unclassified, zero double-counts; new adjudication
entries carry the audit reasons). Reviewed test-body diffs per pilot: NO
assertion removed or weakened (db: 5 original tests byte-identical;
app_env: original assertion bodies unchanged inside bounded
`with_context` wrappers + `descriptors:` opt; global_process: original
assertions unchanged, registration success still asserted through the
fixture, `lookup(:allbert)` now server-scoped with the same match;
home_fs: all 12 original tests byte-identical). Focused regression
surface green (unpiped): `mix/tasks/allbert_test_task_test.exs` +
`dev_gates` (53 tests), `paths_test` ×3 files (16), `settings` dir (64),
`objectives` dir (36), `intent/eval` dir (29). Production changes are
additive-only: `ConfigContext` (new), two `Paths` read chains
(context-first, omission byte-identical), `ListApps`/`ShowApp` registry
context, `DataCase.allow_sandbox/2`, lane-runner
`db_partition_safe` wiring + `migration_action` clause. `release.v1` is
run by the operator at handoff per the build instruction (no release gate
executed in this session). Intake candidates — pending operator
disposition: (1) `AllbertBrowser` driver read + Supervisor/Session
constructor overrides (would unlock the browser home_fs/global_process
remainder); (2) first `ConfigContext.app_env/3` consumer lands with the
M5 wave file that needs it (no production consumer yet by design).

**M0 DONE (2026-07-20).** Baselines at HEAD `0b43733c`, ALL 82 store rows
`(0b43733c, dirty: false)`: quick 309.7 s green (packing keeps sharpening:
was 362.3 at v1.0.2 close); prepush 853.0 s green with
`high_coverage_fast_local` phase 844 s (that phase record IS the high
baseline; the standalone `fast-local --partitions 4` invocation errors
without a lanes flag — invocation note, not a defect); authoritative full
release green, phase total ≈3,153 s; bench-decide 522.7 ms mean / p50
428.5 (corpus decide-v1). Per-lane max partitions (worst across the
baseline runs, identical commands): db_serial 111.4 s, app_env 119.8 s,
global_process 20.0 s, home_fs 25.5 s, liveview 470.3 s (release web
phase). Anchor drift: none — the second pass audited `98d3bf39`; delta to
`0b43733c` is docs-only. Pinned hotspot ledger: `mix allbert.test
metrics` regenerated at this HEAD (query = latest #{50} slowest-bearing
records, committed summary is the candidate ledger). PRE-REGISTERED
per-wave targets (changed only with recorded reason): M4 db_serial max
≤67 s (−40%); M5 app_env ≤72 s (−40%); M6 global_process ≤15 s, home_fs
≤15 s; M2+waves liveview ≤300 s; M7 decide mean ≤450 ms (the ~15%
attributed residue); external_runtime target set only on a go verdict;
full-release phase total ≤2,400 s at M9.

**M3 DONE — code + proofs (2026-07-20); spot campaign folded, see below.**
ListChannels registry-context retirement, fixed at the ownership root.
RED (independently re-verified by the operator agent, not just recorded):
with the production forwarding reverted (`git stash` of
`actions/channels/list_channels.ex` only), the permanent
minimal-composition regression `list_channels_context_test.exs` FAILS
(exit 2, 1 failure); restored, it passes. The composition reproduces the
7/20 campaign class WITHOUT the monolith: a neighbor leaves a
plugin-owned user setting (`stocksage.bridge_enabled`) in the shared
settings root, the pre-conversion test narrowed the GLOBAL plugin
registry to TUI-only, settings resolution then failed
(`{:unknown_setting, "stocksage"}`), SurfacePolicy degraded, and the
raw-report affordance collapsed to an assistant summary.
FIX: `ListChannels.run/2` forwards the Runner `:registry` context-map key
through `RegistryContext.take/1 |> plugin_opts/1` into
`Channels.list_channels/1` (the M1 ListApps/ShowApp pattern, additive;
production call sites pass nothing and read the global default —
identity asserted by a dedicated test in the regression file).
`list_channels_test.exs` now owns a PRIVATE TUI-only registry
(`RegistryIsolationFixtures`) instead of narrowing the global one; the
global registry keeps the full shipped baseline. SurfacePolicy untouched
(a Settings read, not a registry seam). No assertion removed or
weakened — `channel_count == 1` still asserted, now through the private
context at BOTH call sites (the second call site was the drop-out where
the interrupted agent stopped: it still passed `%{}` and read 8 global
channels; completed here).
PROOFS: channels + security focused suites 79/0; statics
(compile --warnings-as-errors, credo --strict, format) clean;
`--check-manifest` green after regeneration (+2 identities, zero
removed); `--check-tags` green with ONE new adjudication recorded —
the channel-surface heuristic reads the new regression file as external
runtime, but it touches no channel runtime (private registry + settings
root), so `db_serial` is the audited class. GOTCHA re-hit: the
adjudication appeared to be ignored until `mix compile --force` — the
stale-beam trap, third occurrence this release line.
CAMPAIGN DISPOSITION (recorded): the M2 and M3 five-seed spot campaigns
are FOLDED into ONE five-seed monolith run executed after M2 lands —
both fixes compose in the same tree, each seed checks BOTH signatures,
and the plan's per-class acceptance is satisfied strictly (5 seeds per
class, same seeds) at half the wall clock. Deviation from "5-seed spot
campaign" phrased per-milestone; no acceptance weakened.

**OPERATOR RE-PRIORITIZATION (2026-07-20): waves are re-ordered by MEASURED
WALL, and the spot campaign is stopped.** Rationale, recorded honestly: at
this point v1.0.3 had delivered NO speedup — M1 converted four pilot files
(negligible), M2 and M3 were correctness work (and M2's boundary proof ADDED
~156 s to `liveview_serial`), and M4 as planned could not deliver because the
runner hardcodes `--max-cases 1` (amendment `ee7c57d8`). The plan's wave
order also did not follow the M0 measurements: it attacked `db_serial` first
(most files) while the actual wall is elsewhere.

M0 measured per-lane maxima: **liveview 470.3 s**, app_env 119.8 s, db
111.4 s, home_fs 25.5 s, global_process 20.0 s, plus **external_runtime
~480 s single-VM**. Liveview + external_runtime are ~950 s of the ~1,000 s
that matter; db + app_env together cap out near 230 s of theoretical gain.

Campaign disposition: STOPPED after 2 of 5 seeds. Both banked seeds are
`exit 0, 0 failures, sidebar_hits 0, listchannels_hits 0` (5,697 s and
5,665 s) — the first fully green monolith runs in this line (the v1.0.2
baseline carried 1–2 failures/seed from exactly the two classes M2/M3
retired). Partial acceptance is recorded for M2/M3; the full evidence is
NOT waived, it moves to M9's 20-seed RC campaign, which re-proves both
classes at four times the sample for zero extra cost (it was always
scheduled). The remaining three spot seeds would have cost ~4.7 h for
little marginal evidence.

Re-scoped waves (numbering preserved to keep ADR 0086, the request-flow,
and the `release.v103` step table stable):

- **M4 — THE CONCURRENCY VERDICT (linchpin, was: db wave).** Every
  remaining speedup depends on one unanswered question: can Sandbox-owned,
  SQLite-backed tests run CONCURRENTLY inside one VM? Today no serial lane
  can, because `--max-cases 1` is unconditional. M2's per-test ownership +
  allowance work is the seam that makes the question askable. Sweep
  `--max-cases` N ∈ {1, 2, 4, schedulers} over `db_partition_safe` AND a
  liveview candidate set; three consecutive runs per N; record green/red,
  measured lane wall, and `busy_timeout`/`database is locked` incidence.
  The verdict decides M5's scope and is recorded either way — a no-go is a
  legitimate outcome that retires the db and liveview conversion targets
  honestly rather than shipping a false speedup.
- **M5 — highest-wall conversions, verdict-driven (was: app_env wave).**
  Green verdict ⇒ convert the biggest walls first: `liveview_serial`
  (470.3 s) then `db_serial` (111.4 s) into the concurrent lane. No-go ⇒
  convert only what parallelises without a DB connection (app_env →
  `pure_async`, 648 tests / 116 files) and record the DB-backed floor.
- **M6 — remainder (unchanged mechanism).** app_env if not already taken by
  M5, global_process (20.0 s) and home_fs (25.5 s), plus the
  external_runtime 2-VM experiment (~480 s single-VM — the second-largest
  wall; contract 5 protocol, 3×-green or recorded no-go).

Wave acceptance is unchanged in kind: measured pre/post from the store on
identical commands, per-wave manifest regeneration, no test-feature loss.

## Purpose

Global-state ownership is the primary shared cause of the convertible serial
floor and the two surviving composition failures; intentional security-eval
and genuine external-runtime floors remain distinct. Phase 1 (v1.0.2) made
the lanes HONEST (reconciliation 173→0, residue de-flaked, web split,
cost-packed partitions, decide-turn remediation) but converted only a
handful of tests. Phase 2 does the actual migration: convert the measured
serial floor to async where an ownership seam exists, retire the two
monolith-only failure classes at their global-state root, and take the last
attributed decide-turn hot path.

The acceptance bar (AGENTS.md: Purpose is the bar): measured lane-floor
reduction with ZERO test-feature loss — every conversion proves identical
test identities/totals and a reviewed assertion-body diff; the 20-seed
monolith campaign rerun
shows both residual classes at 0; `release.v1` + `release.v103` green; the
version's own binary validated under the standing 1.x release policy.

## Current State — Verified Against Code And Store (2026-07-19)

All numbers below are store-cited (`.test_metrics/runs.jsonl`,
`docs/validation/test-metrics/summary.md`) or campaign-cited
(test-strategy.md "20-Seed Full-Monolith Campaign", "M8.8 Measured
Remediation"). M0 re-verifies at build HEAD.

**Lane floor after phase 1 + M8.8 packing.** Serial-lane max partitions
(quick gate, packed): `db_serial` 123.8 s (633 tests / 112 files),
`app_env_serial` 134.3 s (648 / 116), `global_process_serial` 26.5 s
(142 / 32), `home_fs_serial` 25.8 s (126 / 20). Quick wall 362.3 s; high
883 s and prepush 897 s still carry pre-M8.8 packing (M0 re-measures).
`external_runtime_serial` is a SINGLE-VM block: ~590 tests (12 skipped)
@ ~8 min in release-gate phases. `security_eval_serial` 349 tests @ ~4 min
single-VM. Packing is exhausted as a lever on these floors — further
reduction requires conversion (serial→async) or multi-VM partitioning.

**Why each lane is serial (the conversion target classes).**
- `db_serial`: shared global SQLite DB writes; `DataCase` non-async;
  `pool: Ecto.Adapters.SQL.Sandbox` is ALREADY configured (config/test.exs)
  but ownership is not exercised per-test — conversion = real sandbox
  checkout ownership per test (ADR 0086 contract 1).
- `app_env_serial`: tests mutate `Application.put_env` globals (Paths,
  Settings config); conversion = process-scoped context via the existing
  seams — ADR 0082 registry-context opts + `Store.with_resolved_settings/1`
  pin (contract 2).
- `global_process_serial`: named singleton processes (registries, agents)
  collide across tests; conversion = per-test Registry-addressed instances
  through the ADR 0082 injection seam (contract 3).
- `home_fs_serial`: shared ALLBERT_HOME filesystem; the pid-qualified
  per-test home idiom (M8.3/M8.7) makes most of these convertible
  (contract 4).
- `external_runtime_serial`: real external runtime state (browser, OS
  processes, ports). NOT converted — evaluated for multi-VM partitioning
  with owned envs (contract 5, measured go/no-go).

**The two monolith-only classes (campaign, 20 seeds).**
- `SidebarConsolidationTest` "every destination … deep-linkable": 20/20
  deterministic in the monolith — DBConnection ownership class. Root:
  the sandbox ownership LEASE expires. CORRECTED AT M2 (2026-07-20) — the
  planning hypothesis here was "LiveView processes inherit no sandbox
  ownership"; the measured root is different: `DBConnection.Ownership.Proxy`
  grants a lease (`:ownership_timeout`, default 120_000 ms) that is
  INDEPENDENT of ExUnit's per-test `:timeout`. This sweep is the only
  umbrella test whose declared budget (240 s) exceeds that lease, and it
  runs 82.8 s solo; under monolith load it crosses 120 s, the proxy
  disconnects, the owner dies, the pool reverts to `:manual`, and the NEXT
  LiveView mount raises the signature — which is exactly why the class is
  deterministic in the loaded monolith and absent from the lighter lanes.
  Phase 2 fixes the LEASE at one root (M2), not the test.
- `ListChannelsTest` "operator report mode requires the explicit raw-report
  affordance": 7/20 — registry/ListChannels class fed by monolith-only
  neighbors' registry residue. Phase 2 retires it by completing
  private-context/restore convergence in its neighborhood plus forwarding
  registry context from `ListChannels.run/2` to `Channels.list_channels/1`.
  SurfacePolicy is a Settings read, not a registry seam.
- (Third class, OnboardingTest wizard-rewind, already eliminated by M8
  sweeps — 0/20.)

**Store-ranked hotspots.** The committed audit-head summary is dominated by
workspace LiveView files and reports different non-web values than the earlier
planning snapshot. M0 therefore regenerates one full-SHA-pinned summary and
records the exact query/cutoff; prose here is not the candidate ledger. Each
selected file is converted, split, retained with an ownership reason, or
production-remediated when the cost is in exercised production code.

**Decide-turn residue (production).** After M8.8 (final store-cited
corpus decide-v1 numbers: v1.0.1 3,257.3 ms → v1.0.2 close 529.6 ms mean,
−83.7%/turn, all rows dirty: false), eprof attributes ~15% of the
remaining turn to prefilter-class regex tokenization (`:re` machinery,
`Regex.replace`, `String.Unicode.downcase` at 353k frames/turn). That is
this plan's one production-remediation milestone (M7); anything found
beyond it returns as an intake candidate.

**Existing seams (built in v1.0.2, adopted here).**
- ADR 0082 registry injection: `RegistryContext` opts threaded through
  Actions.Registry/App/Plugin reads — adopted by a handful of tests so far.
- `Store.with_resolved_settings/1` is a validated-read snapshot, not a
  replacement for app-env writes; ADR 0086 contract 2 adds the distinct
  process-scoped configuration context.
- Engine `channel_candidates` seam gaps recorded (v1.0.2 M3/M8.2 Build
  Progress) — the remaining global reads that block engine_test conversion.
- ADR 0031 global read in the Runner boundary keeps `browser_actions` in
  `home_fs_serial` — contract 4 decides convert-vs-stay with a recorded
  reason.
- `DevGates.PartitionPacker` + `TestMetrics.file_costs/0` (M8.8) — every
  conversion immediately improves packing (async files leave the serial
  bins entirely).
- `TestSupport.ShippedRegistries.restore!()` — the registry baseline for
  any test that must still touch the global registries.

## Locked Decisions

1. **Phase 2 + the catch-up binary are ONE version (operator final
   disposition 2026-07-19).** M10 is the transferred catch-up publication:
   tag without `[skip-artifacts]`, CI tarballs + SHA256SUMS cosign bundle,
   Latest and tap 1.0.0→1.0.3, install rehearsal, packaged validation —
   the artifact line then carries v1.0.1 + v1.0.2 + v1.0.3 fixes.
2. **Conversion order is store-ranked (derived).** Waves work the measured
   floor top-down (lane max-partition × file cost), never alphabetically or
   by convenience; every wave's acceptance cites pre/post from the store on
   identical commands (the pre/post metrics rule).
3. **No test-feature loss, proven per wave (derived from the Purpose bar).**
   A conversion changes HOW a test isolates (ownership), never WHAT it
   asserts. Per-wave proof: lane totals identical (moved tests counted in
   their new lane), a reviewed test-body diff shows no removed or weakened
   assertions, full accuracy suites unchanged, and the wave's files green
   solo AND in composition both orders (the residue-matrix pattern).
4. **ADR 0086 binds the conversion contracts (derived).** One contract per
   serial class (sandbox ownership, app-env context, named-process
   injection, home-fs pid idiom, external-runtime partitioning go/no-go);
   a test converts ONLY through its class contract — no bespoke isolation
   inventions per file.
5. **Monolith classes are fixed at the OWNERSHIP root, not the symptom
   (derived).** No retries, no test deletions, no monolith-specific skips:
   Sidebar's fix is sandbox allowance threading; ListChannels' fix is
   registry convergence + context adoption. Acceptance is the rerun
   campaign at 0/20 for both.
6. **external_runtime stays honest (derived).** If M6's partitioning
   experiment cannot prove owned-env isolation across VMs (browser/port
   state), the lane STAYS single-VM and the measured no-go is recorded —
   an honest floor beats a flaky split. (The v0.66 lesson: never gate the
   whole workspace_live battery on optimism.)
7. **Binary scope is version-local.** M10 follows the standing 1.x ceremony:
   operator-held annotated tag without `[skip-artifacts]`, CI tarballs +
   SHA256SUMS cosign bundle, tap fill, install rehearsal, and v1.0.3-specific
   packaged validation. It does not repeat v1.0.2's §F–§I catch-up proof.
   Version bumps land at M9 RC.

## Code Seams

- **S1 — sandbox ownership (M1/M2, ADR 0086 c1).** `DataCase` already starts
  a sandbox owner per test. Phase 2 adds allowance helpers for spawned
  processes and empirically decides whether SQLite permits safe in-VM async
  execution. Repo-backed tests never become `pure_async`: successful
  candidates use `db_partition_safe` unless M1 deliberately amends the
  taxonomy and runner with evidence. Pilot audit target:
  `objectives/objective_test.exs`.
- **S2 — app-env context adoption (M1/M5, c2).** ADR 0086 defines one
  internal process-scoped configuration context, explicitly threaded to
  Paths, Settings reads/writes, and child processes. It is distinct from
  `Store.with_resolved_settings/1` and `RegistryContext`. Pilot audit target:
  `intent/eval/gate_test.exs`. Files whose
  PRODUCTION code path still reads globals get the seam gap recorded (the
  engine `channel_candidates` list seeds this) and stay serial with reason.
- **S3 — named-process injection (M1/M6, c3).** ADR 0082 pattern: the
  test-facing constructor takes a registry/name override; per-test unique
  names via the pid-qualified idiom. Pilot audit target:
  `actions/app_actions_test.exs`.
- **S4 — monolith-class retirement (M2/M3).** Sidebar: thread sandbox
  allowances through the LiveView/live-Runtime boundary
  (`workspace_live_case.ex` + the 5-test remainder). ListChannels:
  `ListChannels.run/2` forwards internal registry context to
  `Channels.list_channels/1`, which already accepts plugin-registry opts;
  contaminating neighbors converge or adopt private contexts. Red-first:
  reproduce each
  class OUTSIDE the monolith with a minimal composition before fixing.
- **S5 — external-runtime partition experiment (M6, c5).** Attempt 2-VM
  packed partitioning with fully owned envs (per-VM homes/DBs/ports);
  accept only on 3× consecutive green + no cross-VM interference evidence;
  otherwise record the measured no-go.
- **S6 — prefilter tokenization (M7, production).** Profile-attributed:
  cache/precompile the prefilter's per-turn regex work (compile-constant
  patterns to persistent_term, single-pass tokenization where semantics
  are provably identical — the M8.8 red-first/identity discipline).
- **S7 — release plumbing (M9/M10).** `release.v103` gate =
  `@release_v1_steps ++` focused v1.0.3 steps (per-wave conversion proofs,
  campaign-class regression solos, packer/no-loss checks); version bumps;
  CHANGELOG; the version-local binary publication.

`release.v103` focused steps are fixed before implementation:

| Step id | Working directory / command | PASS |
|---|---|---|
| `v103_lane_reconciliation` | root — `mix allbert.test inventory --check-tags` | zero findings |
| `v103_manifest_drift` | root — `mix allbert.test inventory --check-manifest` | committed identities, lanes, skips, and multiplicities match |
| `v103_pilot_db` | core — `mix test test/allbert_assist/objectives/objective_test.exs` | nonzero tests; contract-1 ownership proof green |
| `v103_pilot_app_env` | core — `mix test test/allbert_assist/intent/eval/gate_test.exs` | nonzero tests; context isolation proof green |
| `v103_pilot_global_process` | core — `mix test test/allbert_assist/actions/app_actions_test.exs` | nonzero tests; two-context negative proof green |
| `v103_pilot_home_fs` | core — `mix test test/allbert_assist/actions/browser_actions_test.exs` | nonzero tests; owned-root proof green |
| `v103_sidebar_ownership` | web — permanent M2 minimal-composition regression | nonzero tests; no DBConnection ownership failure |
| `v103_list_channels_context` | core — permanent M3 minimal-composition regression | nonzero tests; no registry cross-contamination |
| `v103_wave_proofs` | root — checked-in M4–M6 ledger validator | totals and reviewed dispositions reconcile |
| `v103_external_runtime_verdict` | root — checked-in M6 evidence validator | 2-VM 3× proof or measured no-go present |
| `v103_prefilter_identity` | core — focused prefilter + intent-accuracy suites | semantics/accuracy unchanged |
| `v103_dependency_refresh` | root — checked-in M8 disposition/proof validator | every reviewed dependency has an evidenced disposition |
| `v103_version_consistency` | core — version-consistency focused test | mix trio, SW source/gzip, and release version agree |
| `v103_campaign_evidence` | root — checked-in campaign ledger validator | 20 clean seeds, both retired classes 0/20, no unexplained signature |

The runner test must prove the command is exposed, every step uses an existing
target, and a focused test step with zero executed tests is red.

## Milestones

Execution modes: `[gate]` gate-proved-contract, `[smoke]` scripted host
smoke, `[host]` cross-platform manual, `[browser]` real-browser manual.

- **M0: Baseline, anchors, and bounded candidate ledger.** `[gate]` Re-measure quick/high/
  prepush + full release walls on build HEAD as the phase-2 pre column —
  every baseline row must carry v1.0.2 M8.10 provenance (`dirty: false`,
  full SHA; legacy rows are never baselines); the decide baseline is
  `mix allbert.test bench-decide` (corpus decide-v1), the scratchpad
  protocol being retired; re-verify every Current State anchor (file:line
  drift table in Build Progress); set per-wave numeric targets FROM the
  measured baseline (targets are recorded before conversion starts, not
  fitted after). Regenerate the metrics summary from one full-SHA cutoff and
  commit a ledger containing owner/path/current lane/test count/measured
  cost/resource blockers/contract/destination/disposition. Rank by
  `max-partition contribution × convertibility confidence`. Bound M4 and M5
  to the top 20 files or the files covering 50% of measured lane cost,
  whichever is smaller; M6 takes all small-lane candidates above 1% of that
  lane's wall. *Acceptance:* baseline, drift, candidate, and target tables
  committed, all rows provenance-clean; target relaxation is an operator
  decision.
- **M1: ADR 0086 contracts + one pilot per class.** `[gate]` Write the five
  conversion contracts; audit the S1–S3 targets plus
  `actions/browser_actions_test.exs` for home_fs and convert ONE
  representative file per convertible class red-first: prove the
  pre-conversion serial requirement (composition failure or write trace),
  convert through the contract, prove solo + both-orders + lane-total
  equivalence. If an audit target cannot convert, record the ownership reason
  and take the next M0-ranked candidate in that class. ADR 0086 flips
  Accepted only with all five contracts, four successful pilots, and an
  executable external-runtime experiment contract. *Acceptance:* four pilots
  green ×3; contracts documented in test-strategy; `release.v1` green.
- **M2: DBConnection-ownership class retirement.** `[gate]` S4 Sidebar:
  minimal-composition repro red-first; sandbox allowance threading through
  workspace_live_case + live-Runtime remainder; the class's monolith
  signature gone in a 5-seed spot campaign. *Acceptance:* repro test green;
  5/5 seeds without the signature; web suites green unchanged.
- **M3: Registry/ListChannels class retirement.** `[gate]` S4
  ListChannels: neighborhood private-context/restore convergence +
  `ListChannels.run/2` → `Channels.list_channels/1` plugin-context
  propagation; minimal-composition repro red-first.
  *Acceptance:* repro green; 5/5 seeds clean; channels/security suites
  green unchanged.
- **M4: db_serial conversion wave.** `[gate]` Store-ranked wave through
  contract 1 (targets and the bounded cost-coverage set are fixed at M0).
  Unconvertible files get a recorded reason (shared-write semantics, seam
  gap) — honesty over quota. Every conversion wave (M4–M6) regenerates the
  committed test manifest (`inventory --manifest`): lane re-tags and
  execution multiplicity are the wave's no-loss ledger, and
  `--check-manifest` and `--check-tags` must be green at every wave commit
  (v1.0.2 M8.9
  infrastructure, adopted here as the standing proof vehicle).
  *Acceptance:* per-wave no-loss proof (Locked Decision 3) INCLUDING a
  reviewed manifest and test-body diff; measured lane max-partition reduction
  vs M0 is positive. Missing the committed target returns to the operator; it
  is not silently deferred.
  **M4 AMENDMENT (pre-implementation, 2026-07-20 — read-only analysis while
  the M2/M3 campaign held `_build`):** converting files into
  `db_partition_safe` currently buys ZERO wall clock. The lane runner passes
  `--max-cases 1` to every serial lane unconditionally
  (`allbert.test.ex`, `run_serial_partition_cmd/5`), so an `async: true`
  file in `db_partition_safe` still executes sequentially — the conversion
  would only add a second sequential lane and make the gate slower. The M0
  target (db max partition 111.4 s → ≤67 s) is therefore unreachable by
  conversion alone. M4 opens with the empirical SQLite concurrency verdict
  ADR 0086 contract 1 already requires: raise `--max-cases` for
  `db_partition_safe` ONLY (all other lanes stay at 1), sweep N ∈ {1, 2, 4,
  schedulers}, and record for each N (a) green/red across three consecutive
  runs, (b) measured lane wall, (c) any `busy_timeout`/`database is locked`
  incidence. The wave proceeds only on a green verdict at N > 1, and the
  chosen N is recorded with its evidence; a no-go verdict means the db class
  stays serial with the measurement recorded and the M0 db target formally
  retired (honest floor over a false speedup). Candidate ledger prepared
  from the committed manifest + store: 113 db_serial files / 717 tests;
  ranked head — onboarding 27 tests, channels/email 18, allbert_ask 14,
  channels 13, conversations 13, confirmations CLI 6, objectives engine
  agent 11, serve 11, coding m9 12, unified_history 10.

- **M5: app_env_serial conversion wave.** `[gate]` Contract 2 wave, same
  bar; seam gaps recorded per file (feeds any phase-3 intake).
  *Acceptance:* as M4 for app_env_serial.
- **M6: Small lanes + external-runtime experiment.** `[gate]`
  global_process + home_fs waves (contracts 3/4, incl. the browser_actions
  ADR 0031 decision with recorded reason); S5 external-runtime 2-VM
  experiment with the go/no-go rule (Locked Decision 6). *Acceptance:*
  wave proofs; experiment verdict recorded with evidence either way.
- **M7: Prefilter tokenization remediation (production).** `[gate]` S6
  red-first with the M8.8 discipline: eprof attribution before/after,
  identity on intent accuracy suites (zero adaptations), decide per-turn
  re-measured on the same corpus. *Acceptance:* measured reduction
  recorded; accuracy floors unchanged; any residue recorded as intake.
- **M8: Bounded dependency refresh.** `[gate]` Inventory umbrella and plugin
  dependency trees (`mix hex.outdated` plus lock/tree review); apply bounded
  compatible updates and record every apply/scope-out with reason.
  Persistence changes require migration serialization + backup/restore;
  frontend wrappers require asset byte-diff and browser proof; release
  plumbing changes require packaged smoke. Breaking upgrades or removal of
  planned work require operator disposition. *Acceptance:* disposition table
  complete; focused dependency gates, `release.v1`, and the authoritative
  release gate green.
- **M9: RC assembly.** `[gate]` `release.v103` gate wired and green; fresh
  20-seed monolith campaign (seeds 1000–20000 step 1000; both retired
  classes 0/20, no new unexplained
  signatures); version bumps (mix trio, SW CACHE_NAME, gz); CHANGELOG; final
  measured tables in
  test-strategy; full gate cascade (precommit, release.v1, release.v103,
  full release) green on the RC tree. *Acceptance:* all four gates green;
  campaign table committed; docs reconciled BEFORE tagging (the v0.63
  lesson). `homebrew/allbert.rb` remains on packaged Latest 1.0.0 through
  RC; the separate tap is filled only after published 1.0.3 checksums exist.
- **M10: Binary publication and closeout (catch-up, transferred).**
  `[smoke]`+`[host]`+`[browser]` Prove push parity; cut the operator-held
  annotated tag WITHOUT `[skip-artifacts]`; verify CI's three tarballs,
  mandatory SHA256SUMS cosign bundle and Latest status; fill/audit the tap
  (separate repo, manual sha256 fill). PLATFORM VALIDATION MATRIX
  (explicit, first-pass readiness review 2026-07-19): (a) macOS —
  disposable-Home brew/curl install rehearsal, TUI/browser/channel/ACP
  smokes; (b) Linux — BOTH container artifacts validated AND the
  policy-required real-host Linux service/vault run, PASS or owned SKIP
  with reason; (c) **Windows via WSL2 (operator-restored 2026-07-19,
  ending the standing post-1.0 deferral)** — the Linux tarball installed
  inside WSL2 (Tier-2 posture, `docs/operator/install.md:13`): install
  rehearsal, first chat against a local model, TUI smoke, service basics;
  PASS or owned SKIP with reason recorded in the ledger.
  *Acceptance:* ledger complete with macOS, both Linux artifacts, the Linux
  real-host service/vault row, and WSL2;
  tap and curl resolve 1.0.3; packaged validation passes. Then archive the
  triad and mark the roadmap shipped in a post-tag docs-administration
  commit. **Operator-held.**

## Development Lane Annotation

- **Serial barriers:** M0 first (baselines before any conversion). M1
  before all waves (contracts before conversions). M2/M3 before M9's
  campaign (class fixes need seeds to prove). M7 independent of waves
  (production path) but before M9. M8 dependency refresh rejoins before M9.
  M9 before M10; M10 operator-held.
- **Parallel workstreams:** M2 and M3 are independent (different classes);
  M4/M5/M6 waves can interleave once M1's contracts exist — but never two
  gates concurrently (shared tmp roots), and wave measurement runs are
  strictly sequential.
- **Focused tests/gates:** per-milestone focused commands run from the
  OWNER APP directory (root `mix test` path rule, v1.1 plan matrix
  precedent); `inventory --check-tags` after every lane move (conversions
  RE-TAG files — the check proves the move); no `mix | tail` without
  unpiped exit capture; metrics store records cited for every claim.
- **Full-precommit timing:** M9 RC assembly and once at M0 baseline.

## Documentation Deliverables

- ADR 0086 (global-state ownership conversion contracts).
- `docs/developer/test-strategy.md`: phase-2 wave tables, contract
  documentation, final lane census, campaign rerun table.
- `docs/plans/v1.0.3-request-flow.md` maintained as waves land.
- CHANGELOG `## v1.0.3`; shared release-runbook updates for v1.0.3 and WSL2
  catch-up validation.
- Memory + roadmap turnover at closeout.

## Definition Of Done

- Five conversion contracts Accepted (ADR 0086), with four successful
  conversion pilots plus one executable external-runtime experiment contract,
  red-first where convertible — `[gate-proved-contract]`.
- Both monolith-only classes retired at the ownership root; rerun campaign
  0/20 for both, no new unexplained signatures — `[gate-proved-contract]`.
- Measured lane-floor reduction vs the M0 baseline recorded per wave with
  no-loss proofs (identical identities/totals, reviewed non-weakening
  assertion diffs, accuracy suites green) — `[gate-proved-contract]`.
- Prefilter remediation measured with intent-accuracy identity —
  `[gate-proved-contract]`.
- Dependency refresh disposition complete and its focused gates green —
  `[gate-proved-contract]`.
- `release.v103` + `release.v1` + full release green at RC; the v1.0.3
  binary published, cosigned, tap-filled, and validated across macOS, both
  Linux artifacts, the Linux real-host row, and WSL2 (PASS or operator-owned
  policy SKIP where allowed) —
  `[smoke]`/`[host]` attestations.
- Phase-3 remainders (if any) surfaced as intake candidates in Build
  Progress for operator disposition — nothing unilaterally deferred.
