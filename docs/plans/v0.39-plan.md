# Allbert v0.39 First-Run Onboarding And Provider Control

Request flow: `docs/plans/v0.39-request-flow.md`

ADRs: `docs/adr/0004-domain-settings-engine.md`,
`docs/adr/0005-canonical-allbert-home.md`,
`docs/adr/0008-durable-confirmation-requests.md`,
`docs/adr/0014-local-workspace-identity.md`,
`docs/adr/0021-intent-objective-capability-and-advisory-boundary.md`,
`docs/adr/0047-provider-doctor-contract.md`

Status: implemented as `0.39.0`; ready for operator manual validation before
release tagging.

Source: `docs/archives/version-1.0-planning-03.md`, post-v0.37 planning pass,
post-v0.38 readiness review on 2026-05-27.

This milestone is split from the original "Onboarding + Provider + Identity +
Active Memory" bundle. **Identity slot and Active Memory move to v0.39b** so
each sub-milestone has focused scope and Active Memory has room to land its
deterministic retrieval algorithm carefully.

## Purpose

Make Allbert usable from a fresh checkout. Turn existing Settings Central,
provider/model profiles, Objective Runtime, and channel setup into one guided
operator setup path. Add a real provider doctor with explicit branches for
credentialed-remote and local-endpoint providers.

## Expected Inputs

- v0.38 templated creation accepted through operator use.
- Existing Settings Central provider/model profile schema with `providers.*`
  defaults for `local_ollama`, `openai`, `anthropic`, `openrouter`.
- Existing encrypted Settings Central secret store.
- Existing Objective Runtime (`AllbertAssist.Objectives.frame/2`,
  `advance/2`, `continue/2`, `cancel/3`) and registered action runner.
- Existing `mix allbert.channels` task for telegram/email registration.

## Operator-Visible Win

An operator can run one guided setup from CLI or `/workspace`, choose a local
Ollama instance or a remote model (OpenAI, Anthropic, OpenRouter), validate
credentials or endpoint reachability, optionally connect a channel, optionally
enable model-assisted intent ranking, and reach a useful first conversation in
roughly fifteen minutes.

## Planned Interfaces

CLI:

- `mix allbert.onboard` — guided objective entrypoint; resumable.
- `mix allbert.model list` — redacted listing of provider + model profiles.
- `mix allbert.model use PROFILE` — set `intent.model_profile`; optional
  `--enable-assist` flag flips `intent.model_assist_enabled` to `true`.
- `mix allbert.model doctor PROFILE` — runs the two-branch doctor for a
  model profile. The action resolves `model_profiles.<profile>.provider` to a
  provider profile before choosing the provider branch.

Workspace:

- `workspace:onboard` Canvas destination — added everywhere workspace
  destinations are enumerated today: `AllbertAssistWeb.Live.WorkspaceLive`
  allowlist, `AllbertAssist.Workspace.Catalog` destination and panel metadata,
  `AllbertAssist.App.CoreApp` core panel surface declarations, structural icon
  handling, and the corresponding launcher/catalog/panel tests. Renders the
  same onboarding objective state the CLI surfaces.
- Workspace provider/model picker — view of `list_provider_profiles` +
  `list_model_profiles` with action buttons that call the new doctor and
  set-active actions.

Channels (existing surfaces reused, not new ones):

- `mix allbert.channels telegram set-token TOKEN` and
  `mix allbert.channels email set-password --type {imap,smtp} PASSWORD` are
  the registered surfaces onboarding invokes when the operator opts into a
  channel.

Registered actions added in v0.39:

- `doctor_model_profile` (read-only; permission `:read_only`; confirmation
  `:not_required`). The input is a model profile name such as `local` or
  `anthropic_fast`; the action resolves the referenced provider profile and
  requested model before probing.
- `set_active_model_profile` (permission `:settings_write`; writes
  `intent.model_profile`, optionally `intent.model_assist_enabled`, and the
  selected provider's `providers.<name>.enabled` flag via safe-key paths).
- `onboarding_step_complete` (permission `:objective_write`; records step
  progress on the onboarding objective).

## Onboarding Steps

The onboarding objective frames a single durable `Objective` per operator. The
implementation finds the resumable objective through public
`AllbertAssist.Objectives` APIs using `{user_id,
source_intent: "first_run_onboarding", status in ["open", "running",
"blocked"]}`; it does not reach into lower-level objective store helpers.
If the current facade lacks this lookup, v0.39 adds the lookup to the facade
rather than calling the store directly. Step state is represented by ordinary
`objective_steps` rows and `objective_events`; the first non-completed,
non-skipped step is the resume point.

Effectful steps call registered actions through `Actions.Runner.run/3`.
Selection-only UI steps are rendered by CLI/workspace and then recorded by
`onboarding_step_complete`; they do not pretend to be provider/channel actions.
The operator may skip optional steps.

1. **Welcome + scope** — read-only summary of what onboarding will write
   (`intent.model_profile`, `intent.model_assist_enabled`,
   `providers.<name>.enabled`, `providers.<name>.endpoint_kind`, channel keys).
2. **Pick provider profile** — operator selects one of the shipped providers
   or accepts the default `local_ollama`.
3. **Resolve credential or endpoint** — branches on provider profile's
   `endpoint_kind`:
   - `:credentialed_remote` → set or confirm `api_key_ref` via the existing
     `set_provider_credential` action; secret is stored encrypted, never
     printed.
   - `:local_endpoint` → confirm `base_url` reachability; no credential probe.
4. **Run doctor** — `doctor_model_profile` produces the v0.39 doctor
   summary (see "Provider Doctor — Two Branches" below). Failure modes do
   not block onboarding; operator may retry or skip.
5. **Pick model profile** — operator selects an existing `model_profiles.*`
   entry (default `local`) or accepts the shipped default. Writes
   `intent.model_profile` and the referenced provider's enabled flag via
   `set_active_model_profile`.
6. **Toggle model-assisted intent** — operator opts in (default yes) or out;
   sets `intent.model_assist_enabled`. Explicit prompt because the shipped
   default is `false`.
7. **Optional channel registration** — operator selects telegram, email, or
   neither. The CLI may reuse the existing `mix allbert.channels` task UI, but
   credential writes still resolve to the existing Settings Central secret
   helpers/actions and are recorded under the onboarding objective. Failure or
   skip records `:skipped` on the step.
8. **Identity slot preview** — read-only mention that the inert `identity`
   memory namespace and Active Memory retrieval land in v0.39b. No write
   path in v0.39. Points at `docs/operator/active-memory.md` (stub shipped
   with v0.39b plan revision).
9. **Done** — onboarding objective transitions to `:completed`; operator
   sees a recap and the workspace home.

The objective is resumable: closing CLI or refreshing `/workspace` and
re-entering `mix allbert.onboard` or `workspace:onboard` returns the operator
to the first incomplete step.

## Provider Doctor — Two Branches

The public doctor input is a **model profile**. `doctor_model_profile`
resolves `model_profiles.<profile>.provider`, then distinguishes provider
shape rather than treating all providers as credentialed remote endpoints. The
branch is selected by the resolved provider profile's explicit `endpoint_kind`
field (see "Settings Central Schema Changes" below).

- **`:credentialed_remote`** (OpenAI, Anthropic, OpenRouter, hosted Ollama
  cloud, etc.): a bounded probe call against the provider's models endpoint.
  Validates credential format, reachability, and one model availability check.
  Returns redacted summary only.
- **`:local_endpoint`** (Ollama on `http://localhost:11434`, llama.cpp server,
  LM Studio, etc.): a reachability check + `GET /api/tags` (or provider
  equivalent) to confirm the endpoint is up and the requested model is pulled.
  No credential probe.

### Provider Doctor Return Shape

Both branches return the same redacted summary shape per ADR 0047. The shape
is **additive-only** post-v0.39 and is a Tier-1 freeze candidate for v1.0:

```
%{
  endpoint_kind: :credentialed_remote | :local_endpoint,
  credential_ok: boolean() | nil,   # nil for :local_endpoint
  endpoint_ok: boolean(),
  model_available: boolean() | :unknown,
  context_window: pos_integer() | nil,
  deprecation_warning: String.t() | nil,
  last_seen_rate_limit_hint: String.t() | nil,
  redacted_host: String.t(),         # host only; path/query stripped
  diagnostics: [%{code: atom(), message: String.t()}]
}
```

Failure modes return operator-readable messages in `diagnostics` without
leaking endpoint internals, query strings, raw error bodies, secret values,
or full URLs. Downstream doctors (v0.40 MCP, v0.47 voice, v0.48 vision) must
reuse this shape with domain-specific additive fields.

### Provider Doctor Network Boundary

`doctor_model_profile` is read-only but network-observable: it may make one
bounded probe against the resolved provider profile's configured host. It is
not an arbitrary external-network adapter. The action:

- accepts only a configured model profile name, never a raw URL or model-output
  target;
- derives the host from Settings Central provider profile fields;
- uses `Req`;
- applies the ADR 0047 SSRF, timeout, redirect, response-size, and redaction
  subset before issuing the probe;
- does not create remembered resource grants; and
- returns only host-level, catalog-rendered diagnostics.

If future work needs free-form provider URLs or operator-supplied probe
targets, that work must use the external-network/resource approval path
instead of widening this doctor action.

## Settings Central Schema Changes

Additive-only, behind the existing `providers.*.*` fragment. No new
permission classes; all writes use existing `:settings_write` and
`:settings_write_safe_keys` policy.

- **New field**: `providers.*.endpoint_kind` (atom-like string,
  `"credentialed_remote" | "local_endpoint"`).
  - Default derivation: `local_ollama` → `local_endpoint`; everything else
    → `credentialed_remote`. Override is operator-writable through
    `update_setting`.
- **Safe-key additions** (writable through `update_setting` without an
  elevated confirmation): `providers.*.endpoint_kind`,
  `providers.*.enabled`. `providers.*.base_url`, `providers.*.api_key_ref`
  are already safe-keyed.

## Settings Central Writes During Onboarding

Onboarding writes only safe-keys through registered actions. Each write is
audited under the onboarding objective:

- `intent.model_profile` — set by `set_active_model_profile`.
- `intent.model_assist_enabled` — set by `set_active_model_profile` when the
  operator opts in at step 6.
- `providers.<name>.enabled` — set by `set_active_model_profile` for the
  provider referenced by the selected model profile.
- `providers.<name>.endpoint_kind` — set only when the operator explicitly
  overrides the default derivation.
- `providers.<name>.api_key_ref` — set indirectly through
  `set_provider_credential`; secret is encrypted and never printed.
- `channels.telegram.bot_token_ref`, `channels.email.imap.password_ref`,
  `channels.email.smtp.password_ref` — set indirectly through the existing
  `mix allbert.channels set-token` / `set-password` paths.

Onboarding never grants new permission classes, never lowers a safety floor,
and never writes to non-safe-key settings.

## Default Profile Hygiene

The shipped `model_profiles.local.model` is currently `"gemma4:26b"`, which
is not a real Ollama model name. v0.39 M2 bumps the shipped default to
`"llama3.2:3b"`, a real small Ollama model suitable for first-run local
testing. The fix is a settings schema change only; no action shape or fragment
change.

The doctor must not claim success on a fresh Ollama install where the endpoint
is reachable but the model has not been pulled. The acceptance smoke has two
local rows: fresh endpoint without the model returns `model_available: false`
with a fixed remediation message, and the same profile returns
`model_available: true` after the operator explicitly pulls the shipped
default model. v0.39 does not auto-run `ollama pull`.

## Cross-OS First-Run

Per the v1.0 acceptance matrix (item 1), v0.39 onboarding must succeed on
macOS, Linux, and Windows/WSL2.

- **Paths**: `<ALLBERT_HOME>` resolution per ADR 0005 already handles
  cross-OS. Onboarding must not assume POSIX-only path separators when
  rendering paths back to the operator.
- **Local endpoint reachability**: WSL2 default localhost `127.0.0.1` reaches
  the Linux side; reaching a Windows-host Ollama (e.g.,
  `host.docker.internal` or the host's IP) is documented but not required.
- **Secret store**: existing Settings Central encrypted secrets work on all
  three OSs (no OS keychain dependency in v0.39; that's parked).
- **Terminal UX**: `mix allbert.onboard` frames/resumes the objective and
  records progress through explicit subcommands (`complete`, `skip`, and
  `channel`). Provider secrets stay on the existing Settings Central secret
  helpers and channel tasks; onboarding never reads raw secret text from
  prompts. Windows/WSL2 + Linux + macOS all support the command surface.
- **Smoke matrix**: `docs/plans/v0.39-request-flow.md` adds explicit
  macOS + Linux + Windows/WSL2 verification rows.

## Security Posture

Onboarding is a registered objective whose steps call registered actions.
Provider checks never print raw secrets or raw error bodies. Every effectful
setup step runs through `Actions.Runner.run/3`. Doctor output is bounded,
redacted, and audited per ADR 0047.

Note on fallback: the post-v0.37 planning pass parked model-provider fallback
policy entirely (see `future-features.md` "Model Fallback / Degradation
Policy"). The doctor reports availability; the runtime does not silently
failover. Operators who need fallback can wait for the parked design or
manually switch profiles.

## Non-Goals

- No identity slot write authority in v0.39 — onboarding renders the identity
  slot as a v0.39b preview only.
- No Active Memory in v0.39 (moves to v0.39b).
- No new provider backend.
- No hidden provider failover.
- No automatic channel enrollment without operator input.
- No autonomous skill creation from onboarding traces.
- No OS keychain or remote secrets-manager integration.
- No editing of arbitrary settings keys through onboarding (safe-keys only).

## Milestones

- M1: Onboarding objective skeleton, ADR 0047 accepted, registered
  `doctor_model_profile` / `set_active_model_profile` /
  `onboarding_step_complete` action stubs, and `mix allbert.onboard`
  entrypoint that frames the objective and lists upcoming steps.
- M2: Provider/model list, use, and doctor commands (both branches);
  `providers.*.endpoint_kind` schema addition; shipped
  `model_profiles.local.model` default fix; workspace picker.
- M3: Optional channel registration step under the onboarding objective;
  `workspace:onboard` destination added to all workspace destination/panel
  registries; identity-slot preview step.
- M4: Security evals (credential redaction, doctor-no-leak,
  onboarding-action-boundary, endpoint-kind-derivation, default-model-real,
  local-model-missing-remediation, local-model-present-doctor), cross-OS smoke
  matrix run, operator docs, version metadata bump, README update, CHANGELOG
  entry, agent-context-map verification, release gate.

## Documentation Deliverables

- New ADR `docs/adr/0047-provider-doctor-contract.md`.
- Update `docs/operator/onboarding.md` to split v0.39 (provider/model +
  optional channel) from v0.39b (identity slot + Active Memory), add a
  cross-OS quick-start subsection.
- Update `README.md` near-term milestone summary to reference v0.39 and
  v0.39b as the split sub-milestones, including the two-branch doctor
  shape and `endpoint_kind`.
- Update `docs/developer/agent-context-map.md` v0.39 row to include
  ADR 0047.
- Update `docs/operator/security-hardening.md` "Planned v1.0 Threat
  Surfaces" to include onboarding/doctor eval lines.
- Update `docs/plans/roadmap.md` v0.39 row to reference ADR 0047,
  `endpoint_kind`, and the `intent.model_assist_enabled` toggle.

## Definition Of Done

v0.39 is done when:

- A disposable Allbert Home can finish guided onboarding from CLI and from
  `/workspace`, including the optional channel step and the optional
  model-assist toggle.
- The two-branch provider doctor returns the redacted summary shape from
  ADR 0047 for at least one credentialed-remote provider (Anthropic) and
  one local-endpoint provider (Ollama endpoint missing the model and then
  the same endpoint after the operator explicitly pulls the shipped default
  model).
- Onboarding writes only safe-keys, only through registered actions, and
  only against the framed onboarding objective.
- The shipped `model_profiles.local.model` default is a real Ollama model
  name, and doctor output distinguishes endpoint reachability from model
  availability without auto-pulling.
- Cross-OS smoke passes on macOS, Linux, and Windows/WSL2.
- Security evals from §M4 pass; doctor-no-leak fixture asserts redacted
  output.
- Version metadata bumped to `0.39.0`; README, CHANGELOG, roadmap,
  agent-context-map, operator onboarding doc, and security-hardening doc
  reflect the shipped state.
- Release gate (`mix compile --warnings-as-errors`, `mix credo --strict`,
  `mix dialyzer`, `mix precommit`, `git diff --check`) passes.
- v0.39 satisfies items 1 (first-run setup on macOS/Linux/Windows-WSL2)
  and 2 (operator can choose local Ollama, OpenAI, Anthropic, or
  OpenRouter) of the v1.0 acceptance matrix.
