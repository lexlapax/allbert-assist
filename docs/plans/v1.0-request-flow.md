# Allbert v1.0 Request Flow - Stability Release, Contract Freeze, And Product Launch

Status: planned.

Implementation details live in `docs/plans/v1.0-plan.md`.

## Source Documents

- `docs/plans/v1.0-plan.md`
- `docs/plans/v0.59-plan.md`
- `docs/plans/v0.60-plan.md`
- `docs/plans/v0.61-plan.md`
- `docs/plans/v0.62-plan.md`
- `docs/plans/v0.63-plan.md`
- `docs/plans/v0.64-plan.md`
- `docs/plans/v0.65-plan.md`
- `docs/plans/v0.66-plan.md`
- `docs/plans/v0.51-plan.md`
- `docs/plans/v0.47-plan.md`
- v1.0 acceptance matrix — **canonical** criteria in `docs/plans/roadmap.md`
  ("1.0 Acceptance Matrix (freeze-blocking)"); proof-status view in
  `docs/plans/v1.0-handoff.md`

## Main User/Operator Flows

- Operator reviews the public contract inventory.
- Operator runs final acceptance on a disposable Allbert Home.
- Operator confirms inherited v0.66 evidence and completes the v1.0 DIT evidence for
  the primary local files/notes/memory launch path with a true non-developer operator.
- Maintainer confirms no new feature work is included.
- Maintainer prepares the stability release and tag.

## Data Flow

No new runtime data flow is introduced. The release validates documented public
contracts, release gates, security posture, and operator acceptance evidence.

## Authority Invariants

v1.0 adds no authority. All effectful work remains behind registered actions,
Security Central, confirmations, Resource Access, traces, and audits.

## Handles / CLI Surfaces

- Public contract inventory with per-tier classification.
- Release acceptance checklist.
- Existing release gates.

## Security Eval Rows

The 7 freeze rows are registered in `EvalInventory` (`:v1` milestone) and bound in
`v1_sweep_eval_test.exs` by **exact-name assertions** (plan Freeze Enforcement section):
each row proves a frozen contract still exists by exact name (`function_exported?/3`,
signal literals, `__schema__(:fields)`, Settings-key resolution, `Paths` roots,
`Policy.permission_classes()`). `mix allbert.test release.v1` fails if any row is
prose-only or unbound, or if a frozen symbol was renamed/removed. Additive Tier 2 changes
stay green (rows assert presence, not exhaustive equality).

- `v1-contract-freeze-no-new-features-001`
- `v1-public-contract-docs-001`
- `v1-action-boundary-regression-001`
- `v1-settings-home-layout-freeze-001`
- `v1-public-surface-policy-freeze-001`
- `v1-tier1-tier2-classification-001`
- `v1-adr0021-vocabulary-not-frozen-001`

The install/browser/model/cross-host acceptance-matrix rows are **operator-attested**
(the DIT milestones in `v1.0-handoff.md`), reusing v0.66 evidence; they are not `:v1`
gate rows.

## Smoke Checklist

- Run v1.0 acceptance matrix (17 freeze-blocking criteria).
- Confirm v0.59 substrate evidence and v0.66 product RC evidence remain current.
- Confirm v0.64 trusted install and v0.65 local knowledge evidence remain
  current.
- Confirm no new feature docs or code landed in v1.0.
- Confirm v0.47 self-improvement behavior is frozen rather than expanded.
- Confirm Tier 1 contracts have ≥ 2 consumers documented.
- Confirm Tier 2 carve-outs are explicit (additive-only, single-consumer
  components exempt).
- Confirm v0.51 public-surface policy is frozen for Allbert-owned
  auth/exposure/authority semantics, while MCP/OpenAI-compatible/ACP wire
  details remain upstream-tracking.
- Confirm ADR 0021 A20 records reserved-vocabulary not in the freeze.
- Run `mix allbert.test release.v1` — the `:v1` freeze sweep proves every frozen
  contract exists by exact name (rename/remove fails); confirm the prior
  `release.vNN` gates and docs-only checks pass.
- Complete the DIT operator-attested prerequisites (`v1.0-handoff.md`): WSL2 install,
  true non-developer no-docs first-run, hard one-click model download from empty,
  QuickStart direct-answer enablement, real `v0.66.0` -> v1.0 Home upgrade +
  uninstall, live advanced-surface exercise, and model-backed recall.

## Operator Manual Validation (DIT closeout)

Status: **first pass complete** (2026-07-14) — it surfaced findings R1-R9, all remediated
under plan M7.1/M7.2; evidence in `docs/validation/v1.0/dit4-advanced.md` and the DIT
matrix (`docs/validation/v1.0/README.md`). The **second pass** below is what remains
before the freeze. v1.0 is a freeze release. Verification is two-layer: the deterministic
`mix allbert.test release.v1` gate (checkout-bound; 35 frozen contracts by exact name)
plus the operator-attested **DIT** freeze-prerequisites, which run on real
hosts/providers. Record each under `docs/validation/v1.0/` — redacted, disposable or
explicitly-declared test Home only, never a real operator Home. **DIT-3** (model-backed
`:kept` recall), **DIT-6** (source parity), and DIT-4 classes (a)/(b)/(c) are attested.
No `PENDING-OPERATOR` row may remain before the freeze. Each install step ends with its
cleanup; the canonical uninstall reference is
[`docs/operator/install.md`](../operator/install.md) §Uninstall (`brew uninstall` /
`scripts/install/uninstall.sh` — Allbert Home is preserved unless `--purge`).

### Second pass — remaining sequence (START HERE)

Run in this order. Each numbered step is either inline or names the reference block
below in this document; every command is paste-ready.

**1. Step 0 gates on the remediated checkout:**

```sh
cd <repo>
MIX_ENV=test mix compile         # avoid the stale-beam trap before the gates
mix allbert.test release.v1      # PASS: GREEN 9/9 (v1_freeze_sweep = 35 frozen contracts)
mix allbert.test release.v066    # PASS: GREEN 16/16 (product-RC contract rows)
mix precommit                    # PASS: format, credo --strict, dialyzer, tests all clean
```

**2. DIT-4 class (d) re-run on DEFAULT config** (validates the R8+R9 fixes live; classes
a/b/c are already attested):

```sh
export ALLBERT_HOME=/tmp/allbert-dit4-protocols-home   # first-pass Home; if deleted, redo §DIT-4 (c)+(d) setup
mix allbert.settings set intent.router_strategy two_stage_local   # restore the production default (R8 check)
mix allbert.tui
#   type: run workflow dit4_smoke
#   PASS: gate pauses + prints ALLBERT:APPROVE/DENY/SHOW:<id>; type the APPROVE command;
#         the resolution prints IN-SESSION (silent ignore = R9 regression, blocking);
#         /confirmations shows it approved; no button/URL affordance printed.
#   /quit, then: mix allbert.plan list    # the run's objective present
# append the transcript to docs/validation/v1.0/dit4-advanced.md; flip class (d) to PASS
rm -rf /tmp/allbert-dit4-protocols-home && unset ALLBERT_HOME   # burns the first-pass tokens
```

**3. DIT-2 true non-developer, no-docs first run** — on the FIXED build, so build the
v1.0-candidate packaged binary locally (there is no released artifact with the M7.x
fixes):

```sh
MIX_ENV=prod mix release allbert                       # → _build/prod/rel/allbert/bin/allbert
ollama list                                            # model cache must be EMPTY (ollama rm <model> if needed)
export ALLBERT_HOME=/tmp/allbert-dit2-home && mkdir -p "$ALLBERT_HOME"
_build/prod/rel/allbert/bin/allbert serve              # leave running
# Hand the machine to a real non-developer (decision lock: not a maintainer role-playing):
#   open http://localhost:4000/workspace -> onboarding auto-opens -> Start QuickStart
#   -> one-click model download FROM EMPTY -> first useful chat, no developer docs.
#
#   "Ready for first chat" is OBSERVABLE — all three, without reading docs:
#     - the Guided setup badge shows "Ready" and the step list has advanced to
#       "First chat" (each completed step prints a "Step recorded: <step>." notice)
#     - the guidance text under the step list is the first-chat guidance
#     - a chat question gets a model answer, NOT "The direct-answer model is disabled"
#       (QuickStart must enable direct answers BEFORE their first real question)
#
#   First-chat PASS example — ask a plain question, e.g.
#     "Help me understand what Allbert can do locally."
#   PASS: a substantive answer in plain language (what it can do, how to try it).
#   FAIL examples (each = blocking defect):
#     - "I can run show app, but I need app id first. Please provide the missing
#       details."  (tool deflection demanding an internal identifier)
#     - "The direct-answer model is disabled."  (consent gate not enabled by QuickStart)
#     - any response requiring the user to know app ids, settings keys, or mix tasks.
#   Any stumble, including "I don't know where I am in setup" = blocking defect.
# Item-11 recheck (same session, packaged assets): navigate / -> /workspace;
#   PASS: no transient "Attempting to reconnect" toast (or record the suppress decision).
# notes -> docs/validation/v1.0/dit2-no-docs.md; stop the server;
rm -rf /tmp/allbert-dit2-home && unset ALLBERT_HOME
# (the brew-install affordance itself is INHERITED v0.64/v0.66 evidence; this step
#  proves the fixed first-run UX, which no released artifact carries yet)
```

**4. DIT-5 upgrade + teardown** — run the §"DIT-5" block below as printed (reuses the
step-3 release build as the v1.0 binary). PASS criteria inline in the block.

**5. DIT-1 WSL2** — on a Windows host, run the §"DIT-1" block below; if no host is
available at freeze time, flip the matrix row to `SCOPED-OUT` with that rationale
instead (operator decision, recorded in `docs/validation/v1.0/README.md`).

**6. Close Phase A** — §"Step N" below: flip the matrix rows, commit + push, then
proceed to the Release Closeout.

### Step 0 — deterministic gate (run first)

```sh
cd <repo>
MIX_ENV=test mix compile         # avoid the stale-beam trap before the gates
mix allbert.test release.v1      # freeze gate — GREEN 9/9 (v1_freeze_sweep = 35 frozen contracts)
mix allbert.test release.v066    # GREEN 16/16 (product-RC contract rows)
mix precommit                    # format, credo --strict, dialyzer, tests
```

### DIT-1 — WSL2 cross-host install (on a Windows host, in WSL2)

```sh
curl -fsSL https://raw.githubusercontent.com/lexlapax/allbert-assist/main/scripts/install/install.sh | sh
allbert admin health                       # runtime/web/channels up
curl -fsS http://localhost:4000/health     # {"status":"ok",...}
# save transcript -> docs/validation/v1.0/dit1-wsl2-install.log
# cleanup (docs/operator/install.md §Uninstall) — removes the binary, preserves Home:
sh scripts/install/uninstall.sh
```

### DIT-2 — true non-developer, no-docs first run (clean host, EMPTY model cache)

```sh
brew install lexlapax/allbert/allbert      # or the curl installer above
brew services start allbert                # Linux: systemctl --user start allbert
# Open http://localhost:4000/workspace -> onboarding auto-opens -> "Start QuickStart"
#   -> one-click model download FROM EMPTY (no pre-pulled Ollama model, no API key)
#   -> reach a first useful chat. A real non-developer drives it; no developer docs.
#   QuickStart must enable direct answers before the first real question.
# notes -> docs/validation/v1.0/dit2-no-docs.md (record any stumble as a blocking defect)
# cleanup (docs/operator/install.md §Uninstall) — preserves Home:
brew services stop allbert && brew uninstall allbert
```

### DIT-4 — live advanced-surface regression (configure >=1 provider per class first)

Five classes; each is exercised or recorded SCOPED-OUT + rationale in
`docs/validation/v1.0/dit4-advanced.md` (silently skipping a class is blocking).
Channel smokes read provider credentials from the environment: `set -a; source .env; set +a`
first (never copy `.env` values into evidence).

```sh
# (a) browser research + delegate
mix allbert.test external-smoke -- browser_research
mix allbert.test external-smoke -- browser_research_delegate

# (b) channels — >=1 outbound AND >=1 inbound (inbound is interactive: reply to the bot)
mix allbert.test external-smoke -- telegram
mix allbert.test external-smoke -- inbound_telegram
mix allbert.test external-smoke list        # other channel targets (email/matrix/discord/slack/...)
# each smoke writes evidence JSON under a temp gate Home (release_evidence/...) —
# copy it into docs/validation/v1.0/ before it is purged

# (c) public protocols — v0.51 command set (docs/operator/public-protocol-surfaces.md).
#     All-local, no external providers. Settings writes are real: use a disposable Home.
mkdir /tmp/allbert-dit4-protocols-home
export ALLBERT_HOME=/tmp/allbert-dit4-protocols-home
mix allbert.settings set mcp_server.enabled true
mix allbert.settings set mcp_server.stdio.enabled true
mix allbert.settings set mcp_server.streamable_http.enabled true
mix allbert.settings set mcp_server.tools_enabled direct_answer,external_network_request,get_public_call_result
mix allbert.settings set openai_api.enabled true
mix allbert.settings set openai_api.models_enabled local
mix allbert.settings set acp_server.enabled true
mix allbert.settings set acp_server.stdio.enabled true
mix allbert.public_protocol token create --surface mcp_http --client claude          # raw token prints ONCE
mix allbert.public_protocol token create --surface openai_api --client local-openai  # raw token prints ONCE
mix allbert.mcp_server status               # enabled=true, tools>0
mix allbert.mcp_server tools list
mix allbert.mcp_server resources list
mix allbert.acp_server status               # enabled=true
PORT=4051 mix phx.server                    # leave running; in a second terminal:
curl -s http://127.0.0.1:4051/v1/models \
  -H "x-allbert-client-id: local-openai" -H "authorization: Bearer <openai-token>"
curl -s -X POST http://127.0.0.1:4051/mcp -H "content-type: application/json" \
  -H "x-allbert-client-id: claude" -H "authorization: Bearer <mcp-token>" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'

mix allbert.acp_server status
mix allbert.mcp_server status
# full OpenAI leg — needs local Ollama running with the configured model (`local` alias):
curl -s -X POST http://127.0.0.1:4051/v1/chat/completions \
  -H "content-type: application/json" \
  -H "x-allbert-client-id: local-openai" -H "authorization: Bearer <openai-token>" \
  -d '{"model":"local","messages":[{"role":"user","content":"Reply with exactly: allbert-dit4-ok"}]}'
# PASS: a JSON chat.completion whose message content contains allbert-dit4-ok.
# (Surface is text-only by design; client-supplied tools/functions are rejected.
#  If this disposable Home has no local model configured, a clean authed
#  model-unavailable error proves auth+routing but not the completion leg —
#  configure the model or run this one against the DIT-2 test Home.)
# Redact tokens in evidence; unset ALLBERT_HOME afterwards.

# (d) Plan/Build approval smoke — the :workflow_run_start confirmation gate (v0.44):
#     starting ANY workflow run always requires typed approval. Same disposable Home.
mkdir -p "$ALLBERT_HOME/workflows"
cat > "$ALLBERT_HOME/workflows/dit4_smoke.yaml" <<'EOF'
id: dit4_smoke
version: 1
description: DIT-4 approval smoke.
steps:
  - id: answer
    kind: action
    action: direct_answer
    params:
      text: "Say: dit4 approval smoke complete."
EOF
mix allbert.workflows list                    # dit4_smoke listed
mix allbert.workflows inspect dit4_smoke      # YAML + params validate against action snapshot
# TUI prerequisites on a fresh Home — unmapped terminal input is SILENTLY rejected
# by design (v0.55 security behavior), so map the identity first:
mix allbert.ecto.migrate --quiet
mix allbert.settings set channels.tui.identity_map '[{"external_user_id":"default","user_id":"local","enabled":true}]'
mix allbert.settings set channels.tui.enabled true
mix allbert.tui
#   in the TUI type:   run workflow dit4_smoke
#   PASS criteria (per tui-channel.md §"Legacy v0.55 manual M3 approval smoke"):
#     1. the :workflow_run_start gate pauses the run and prints the exact typed
#        commands ALLBERT:APPROVE:<id>, ALLBERT:DENY:<id>, ALLBERT:SHOW:<id>
#     2. type ALLBERT:APPROVE:<id> at the same prompt
#     3. the resolution prints and the run starts (inspect with /confirmations
#        inside the session; after exit: mix allbert.plan list / show <objective>)
#     4. no button/link affordance or target URL appears in the terminal handoff
#   The gate fires BEFORE step execution — if the disposable Home has no model and
#   the direct_answer step later errors, the approval smoke still PASSes on 1-4.

# -> docs/validation/v1.0/dit4-advanced.md (per-class PASS / SCOPED-OUT + rationale;
#    include the inbound smoke's 3 manual_followups_required: inline approve/deny/show
#    buttons from the mapped clicker, email typed approve command, unmapped-sender
#    rejection — exercised or scoped each)
```

### DIT-5 — real v0.66.0 -> v1.0 upgrade + uninstall preserves Home

Install-method assumptions (no brew requirement):

- **v0.66.0 side**: any real packaged install — `brew install lexlapax/allbert/allbert`
  (tap → 0.66.0) or the curl installer (GitHub Latest). A source checkout does NOT
  satisfy the decision lock.
- **v1.0 side**: brew is impossible pre-tag. Build the v1.0-candidate binary locally
  from `origin/main`: `MIX_ENV=prod mix release allbert` →
  `_build/prod/rel/allbert/bin/allbert` (the same OTP release CI packages).
- **Uninstall leg**: use the same method that installed v0.66.0 (`brew uninstall
  allbert` / `sh scripts/install/uninstall.sh`) — that is where uninstall-preserves-
  Home is proven; the locally built v1.0 binary has no installer (service uninstall +
  delete the build dir).

Command surface (docs/operator/export-import.md): `allbert admin home export` emits a
**redacted JSON portability envelope** via `--out` (there is no tar mode), and
`allbert admin home import PATH` is a **dry-run validator only** — it never applies.
The Home payload itself moves at file level: Allbert Home is the directory
(default `~/.allbert`, resolved via `ALLBERT_HOME`), so the "upgrade" is pointing the
v1.0 binary at the real v0.66.0 Home.

```sh
# On the real v0.66.0 packaged install with data (notes connected, a kept memory):
allbert admin home export --out /tmp/allbert-v066-home.envelope.json   # redacted envelope (evidence)
tar -C "$HOME" -czf /tmp/allbert-v066-home.tar.gz .allbert             # the actual Home payload

# On the second machine / disposable target:
tar -C "$HOME" -xzf /tmp/allbert-v066-home.tar.gz                      # restore the Home directory
# install/point the v1.0 binary at it (default ~/.allbert, or set ALLBERT_HOME), then:
allbert admin home import /tmp/allbert-v066-home.envelope.json         # dry-run validation (does not apply)
allbert admin health                                                   # runtime up against the v0.66.0 Home
# verify in the workspace: connected notes present, the kept memory recalls

# uninstall the service AND the binary, then verify Home preservation
# (docs/operator/install.md §Uninstall):
allbert admin service uninstall            # confirmation-gated: complete via
                                           #   allbert admin confirmations approve <id>
brew uninstall allbert                     # or: sh scripts/install/uninstall.sh  (removes the binary; keeps Home)
ls "${ALLBERT_HOME:-$HOME/.allbert}"       # Home files still present (--purge is the only removal path)
# save transcript -> docs/validation/v1.0/dit5-upgrade-uninstall.log
```

### Step N — close Phase A

Recheck the two item-11 web-usability caveats
(`docs/validation/v0.66/item11-usability-audit.md` Findings 1-2):

1. **Reconnect toast** — on the packaged/prod build (not `mix phx.server` dev assets),
   navigate `/` -> `/workspace`; the transient "Something went wrong! Attempting to
   reconnect" toast should not appear on the first dead->live transition (or record
   the decision to suppress it).
2. **QuickStart direct-answer enablement** — attested inside DIT-2: QuickStart reached
   the direct-answer enable step before the operator's first real question
   (`intent.direct_answer_model_enabled` flipped by onboarding).

Then flip DIT-1/2/4/5 to `PASS` (or `SCOPED-OUT` + rationale) in
`docs/validation/v1.0/README.md`; commit + push. Only then proceed to the
Release Closeout.

Expected behavior at closeout: all 17 acceptance-matrix criteria pass on macOS, Linux,
and (via DIT-1) Windows/WSL2; the no-docs run is a true non-developer with an empty model
cache; the upgrade starts from a real `v0.66.0` packaged Home; no new feature code/docs
landed and v0.47 self-improvement is frozen; Tier-1 contracts have >= 2 documented
consumers and Tier-2 carve-outs are explicit; all release + docs gates pass.

## Release Closeout (after every DIT row is green)

Line-by-line. The tag is effectively irreversible — pushing it publishes a public GitHub
**Latest** release and moves the Homebrew tap. The tag message must **not** contain
`[skip-artifacts]` (that would skip the packaged build).

```sh
# 1. version bump 0.66.0 -> 1.0.0 (three mix.exs files, all line 7)
sed -i '' 's/version: "0.66.0"/version: "1.0.0"/' mix.exs apps/allbert_assist/mix.exs apps/allbert_assist_web/mix.exs
grep -rn 'version:' mix.exs apps/*/mix.exs           # confirm all three = 1.0.0

# 2. edit docs (no AI/co-author trailers):
#    - CHANGELOG.md: add "## v1.0.0 - Stability Release & Public Contract Freeze" at top
#      (no new features; tiered freeze per docs/developer/public-contract-freeze.md;
#       release.v1 enforces 35 frozen contracts by exact name; DIT closeout complete)
#    - docs/plans/roadmap.md: flip item 67 (## v1.0 ...) status to "Released - tagged v1.0.0"
#    - docs/plans/v1.0-plan.md: Status -> released
#    - homebrew/allbert.rb: bump version + URLs
sed -i '' 's/0\.66\.0/1.0.0/g' homebrew/allbert.rb    # sha256 stay REPLACE_* until step 5

# 3. verify + commit
MIX_ENV=test mix compile
mix allbert.test release.v1 && mix allbert.test release.v066 && mix precommit && mix allbert.test docs
git add -A
git commit -m "v1.0.0 closeout: bump 0.66.0->1.0.0, CHANGELOG, roadmap, formula; DIT closeout"
git push

# 4. tag + publish (CI builds macos-arm64/linux-x64/linux-arm64, publishes Latest, cosign-signs)
git tag -a v1.0.0 -m "v1.0.0 - Stability Release & Public Contract Freeze

Tiered public-contract freeze enforced by mix allbert.test release.v1 over 35 frozen
contracts. No new features. DIT closeout complete."
git push origin v1.0.0
gh run watch "$(gh run list --workflow=release-artifacts.yml -L1 --json databaseId --jq '.[0].databaseId')" --exit-status
gh release view v1.0.0 --json assets --jq '.assets[].name'   # 3 tarballs + SHA256SUMS + cosign.bundle

# 5. Homebrew tap (separate repo lexlapax/homebrew-allbert; CI does NOT push it)
gh release download v1.0.0 --pattern SHA256SUMS --dir /tmp --clobber
gh repo clone lexlapax/homebrew-allbert /tmp/tap && cd /tmp/tap
bash <repo>/homebrew/fill-sha256.sh /tmp/SHA256SUMS Formula/allbert.rb
grep -c REPLACE Formula/allbert.rb                    # must be 0
git commit -am "allbert 1.0.0" && git push

# 6. final verification
gh release list -L1                                   # v1.0.0 = Latest
brew update && brew info lexlapax/allbert/allbert     # shows 1.0.0
```

## Docs Closeout Checklist

- Public contract freeze notes exist with per-tier classification.
- Upgrade and rollback notes exist.
- Developer compatibility guidance exists per tier.
- ADR 0021 A20 reserved-vocabulary cross-link exists.
