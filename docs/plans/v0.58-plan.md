# Allbert v0.58 Surface Consistency, Settings Enforcement & Web Design System

Request flow: `docs/plans/v0.58-request-flow.md`

ADRs:
- `docs/adr/0073-cross-surface-contract.md` (NEW — every surface is a thin,
  uniform view over the one spine).
- `docs/adr/0074-web-design-system-and-ux-language.md` (NEW — tokens, variant
  registry, pattern library, catalog-as-boundary-for-all-pages).
- `docs/adr/0016-*` v0.58 amendment (channel boundary extended to web/mix/MCP),
  `docs/adr/0030-*` v0.58 amendment (catalog is the boundary for every page),
  `docs/adr/0070-*` v0.58 amendment (operator-action layer governs the web).
- `docs/adr/0004-*` / `docs/adr/0031-*` v0.58 settings-enforcement note.
- `docs/adr/0024-*` v0.58 revision (chat-primary layout authority), `docs/adr/0023-*`
  (substrate kept), `docs/adr/0015-*` (Surface DSL kept).

Status: implementation complete through M13.1F; **M13.1A-F audit remediation is
complete and v0.58 is ready for the fourth-pass implementation audit, then M14
manual operator validation if no blocker remains**. M13.1F closes the 2026-06-25
third-pass design-system consolidation gap: drawer/table stateless components and
production stateful catalog atoms now consume one root-safe pattern contract. The
2026-06-25 Chrome S3 corrective browser pass fixed browser-visible issues before
implementation audit, but its evidence is pre-remediation evidence, not final M14
validation. Rescoped 2026-06-24 from the earlier narrow web/policy framing into
the **pre-1.0 consolidation release**. The original web re-layout, the v0.56 DTO
panels, and the operator surface-policy layer are preserved as milestones within
a larger four-pillar scope. NEW framing in the 2026-06-09 restructure; rescoped
after the v0.57 closeout.

Implementation readiness note: `mix allbert.test release.v058` is the
deterministic M13/M13.1 handoff gate. M13.1E adds the second-pass remediation
coverage the post-implementation audit requested: the operator-reachable
`mix allbert.intent` rejected-result path, broader `release.v058` coverage,
shared loading/error/status/drawer/table-list patterns, broader Settings and web
drift guards, and additional ACP/MCP rejected-event coverage. M13.1D/E reconcile
the plan, request-flow, ADR, and developer-contract docs. The third-pass audit
then found that drawer/table sharing must be rooted in a single contract that
stateful LiveComponents can consume without breaking Phoenix's static-root rule.
M13.1F closes that gap, broadens the remaining core `System.get_env` guard, routes
or mirrors offline banners through the shared status pattern, and updates the docs
that still underclaimed M13.1E. The touched Chrome checks and `release.v058` gate
run again before the fourth-pass implementation audit and before M14 browser/
operator validation in `docs/plans/v0.58-request-flow.md`.

## Purpose

Make the platform **internally consistent and freezable** before the v1.0 contract
freeze. Four pillars, all on the existing authority spine:

1. **Surface consistency (ADR 0073).** Every surface — web, TUI, channels, Mix,
   public protocol, Pi-mode — becomes a thin, uniform view over one runtime/action/
   settings spine: one renderer, one event/audit path, one identity/session seam,
   operator reads through the registered-action layer, and shared invocation
   helpers. Today the eight channel surfaces conform; web, Mix, and public-protocol
   diverge (five bespoke renderers, no event/audit on three surfaces, web reads
   confirmations/settings directly and hardcodes identity).
2. **Settings enforcement (ADR 0004/0031 note).** All operator-tunable config goes
   through Settings Central; the known bypasses are fixed and a CI guard prevents
   new ones. (Distinct from v0.59's settings *migration* tooling, ADR 0046.)
3. **Web design system & UX language (ADR 0074).** A real token system, a
   prop-driven component-variant registry, a shared pattern library, one app shell,
   and the catalog as the rendering boundary for *every* page (Jobs/Objectives are
   folded in). The chat-primary re-layout and the operator panels are executed on
   this system.
4. **Redundancy consolidation.** Comb the codebase and consolidate the duplicated
   helper families (~8 patterns, 100+ sites) into shared modules, so v0.59 inherits
   a clean base for its final cleanup.

The authority boundary is unchanged: this release changes *how surfaces reach the
spine* and *how the web is built*, never what any surface may do. Security Central
remains the only authority; descriptors remain routing vocabulary; surface policy
remains presentation governance.

## Foundations & Handoff
- **Builds on:** v0.55/v0.55.1 channel + TUI console (ADR 0016/0067/0070), v0.56
  operator-action layer + DTOs (ADR 0070/0071/0072), v0.57 Pi-mode surface
  (ADR 0068), the ADR 0023/0024/0030 Surface substrate + component catalog
  (~54 registered atoms as of 2026-06-24; the count is not load-bearing).
- **Hands off to v0.59:** v0.58 delivers the uniform surface base + clean codebase;
  v0.59 does settings *migration* (ADR 0046), **central action param-contract
  enforcement (ADR 0065 M7)** on the now-uniform invocation path, operator
  onboarding (ADR 0069), the cross-surface security sweep, and RC/freeze prep.

## Cross-Cutting Invariants (apply to every milestone)
- **One spine.** User turns through `Runtime.submit_user_input/1`; operations
  through `Actions.Runner.run/3`. No surface calls business logic, `Confirmations`
  storage, or `Settings.Store` directly (ADR 0073).
- **Settings Central is the only config authority.** Every new key (surface
  policy, the fixed bypasses, web prefs) is registered with specs + defaults +
  `safe_write_keys`; the CI guard fails on ad-hoc operator-tunable config.
- **Security Central is the only authority boundary.** Surface policy, panels, and
  the design system grant no authority, make no `:internal` action routable, lower
  no confirmation floor, and grant no egress.
- **Catalog is the rendering boundary.** No model- or data-generated UI; every page
  renders through the unified catalog inside one shell (ADR 0030/0074).
- **Redaction everywhere.** No panel or renderer prints secret refs, endpoint URLs,
  raw prompts, raw descriptor/evidence payloads, or provider response bodies.

## Conformance Matrix (the standing Pillar-1 check)
M13 brought the main surface spine into place. The second-pass audit found edge
partials that M13.1 must close before M14.

| Surface | Identity.resolve | One renderer | Event+audit | Spine-only reads | Operator-action reads | Shared ctx helper |
|---|---|---|---|---|---|---|
| Channels (7) + TUI | ✓ | ✓ | ✓ | ✓ | ✓ (TUI slash) | ✓ |
| Web workspace + operator pages | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `/` home page | N/A (not a turn surface) | N/A | N/A | ✓ (thin landing emits shell/token contract) | N/A | N/A |
| Mix tasks | ✓ (local/operator ctx) | ✓ | ✓ | ✓ | ✓ | ✓ |
| Public protocol (MCP/ACP/OpenAI) | ✓ (client-id ctx) | ✓ | ✓ | ✓ | ✓ | ✓ |
| Pi-mode (v0.57) | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

Surface-policy report-shape coverage was partial at M13. M13.1C widened the
actual read set so `list_settings`, `list_channels`, `list_model_profiles`,
`list_provider_profiles`, `intent_coverage`, `intent_list_descriptors`,
`intent_list_review`, and `model_doctor` consult `SurfacePolicy.report_policy/3`
and keep raw operator reports behind an explicit affordance.

## Locked Decisions
| # | Decision | Chosen value | Trade-off if changed |
|---|---|---|---|
| 1 | Scope | Full consolidation: full surface unification + full web design system + all redundancy, anchored by ADR 0073/0074. | Deferring leaves divergence/debt to carry through the 1.0 freeze. |
| 2 | One renderer | The five bespoke response renderers collapse into one `Surface.Renderer` over the typed `Runtime.Response` + a surface descriptor. | N renderers re-diverge and re-bloat model context. |
| 3 | Uniform events | Web/Mix/public-protocol record inbound/rejection/error to the shared event spine by `surface_id`. | An audit gap on three surfaces blocks a freezable, auditable platform. |
| 4 | Web reads via actions | Web routes confirmation + settings + operator reads through the ADR 0070 action layer; identity via `Identity.resolve`; no hardcoded `"local"`. | Direct store reads + hardcoded identity violate ADR 0016/0070 and the contract. |
| 5 | Settings access | No operator-tunable config outside Settings Central; CI guard enforces it; the 6 known bypasses fixed. | Ad-hoc config re-accumulates and is unmanageable at 1.0. |
| 6 | Design system | Tokens + prop-driven variant registry + pattern library + one app shell + catalog-for-all-pages; UX language doc. | Class-driven variants + hand-rolled pages keep the web inconsistent. |
| 7 | No rebuild / no rename / no new routes | ADR 0023 substrate, `Conversations.Thread` internals, `Session.Scratchpad`, `/workspace`, and the catalog boundary are unchanged. "Conversations" is UI-string-only. | A rebuild/rename collides with the 1.0 freeze and the existing `Session` concept. |
| 8 | Surface policy | Operator-managed presentation/report-shape policy is Settings-Central-backed and action-mediated, distinct from descriptor YAML and from Security Central authority. | Mixing report shape into descriptors or permissions violates ADR 0060/0062/0006. |
| 9 | Redundancy | Consolidate all 8 helper families now (private helpers, zero public-API impact); verify-before-removing dead candidates. | Leaving duplication makes v0.59 final cleanup larger and riskier. |
| 10 | v0.59 boundary | v0.58 = consistency + consolidation + web system. v0.59 keeps migration (0046), param-contract (0065), onboarding (0069), security sweep, RC. | Pulling 0065 forward over-couples v0.58; leaving consolidation to v0.59 bloats the RC. |

## v0.56 DTO Handoff For Web Panels
The web panels render the v0.56 read/action contracts through `Actions.Runner.run/3`
(ADR 0073) — no Mix scraping, no `DescriptorStore`/`Settings.Store` reads, no new
authority. Reads: `intent_coverage`, `intent_list_descriptors`,
`intent_show_descriptor`, `intent_eval_run`, `intent_list_review`, `model_doctor`,
`list_model_profiles`, `list_provider_profiles`. Mutations (explicit operator
actions, gated): `edit_intent_descriptor`, `disable_intent_descriptor`,
`enable_intent_descriptor`, `promote_intent_descriptor` (ADR 0071 gate),
`optimize_intent_descriptors`, `reindex_intent_descriptors`. CLI/TUI parity oracles:
`mix allbert.intent …`, `mix allbert.settings model-doctor`, `mix allbert.model
list`, TUI `/intents`, `/models`. Redaction + advisory-only authority contract per
the cross-cutting invariants.

## Milestones

Lane summary: **M0** contracts (serial barrier) → **M1** unified renderer ∥ **M2**
uniform events ∥ **M3** web-via-actions + identity → **M4** shared invocation
helpers → **M5** settings enforcement → **M6** tokens/UX-language → **M7**
variants + pattern library → **M8** shared shell + catalog-for-all-pages → **M9**
chat-primary re-layout → **M10** operator panels → **M11** surface-policy layer →
**M12** redundancy consolidation → **M13** evals + `release.v058` → **M13.1**
audit remediation (pass 1 plus second-pass corrective fixes) → **M14** operator
validation (manual gate) → **M15**
closeout. M13 is implemented; M13.1 blocks M14, and M14 blocks M15. Each
milestone's **Implementation** block lists Modules / Files / Settings / Tests /
Verify.

### M0: Contracts (serial barrier)
Author ADR 0073 + 0074; amend ADR 0016/0030/0070; add the ADR 0004/0031 settings
note; revise ADR 0024 (chat-primary). Define the conformance matrix, the
design-token/variant/pattern spec, and the `surface_id` vocabulary. No code.
- **Verify:** ADRs Proposed/revised; conformance matrix + token spec reviewed.
- **Rejoin:** barrier for M1–M3.

### M1: Unified Surface Renderer
Collapse the five bespoke response renderers (web, TUI, channels, Mix, MCP) into one
`Surface.Renderer.render_response(response, surface_descriptor)` over the typed
`Runtime.Response`. Surfaces pass a descriptor (render primitives — the
`[:typed_command, :list, :button, :link]` set defined by ADR 0016); `model_payload`
never carries `surface_payload` chrome; only `model_payload` threads to memory.
- **Files:** a shared renderer module; thin per-surface descriptor adapters; remove
  `telegram/renderer.ex`, `tui/renderer.ex`, web/mix/MCP response formatting in favor
  of the shared path.
- **Tests:** byte-for-byte render parity per surface against the prior output; no UI
  leak into `model_payload`. Eval `surface-renderer-unified-parity-001`.
- **Implemented:** `AllbertAssist.Surface.Renderer` now owns runtime-response text
  selection, stream rendering, approval primitive rendering, media-output summaries,
  chunking, and bounds. Channel/TUI renderers, CLI output, public-protocol response
  mapping, and web display assignments delegate to that path while keeping transport
  envelopes thin and provider-specific.
- **Verified:** warning-as-error compile, focused renderer/channel/TUI tests, CLI and
  public-protocol mapping/controller tests, and web LiveView response-display tests.

### M2: Uniform Surface Event Recording + Audit
Web, Mix, and public-protocol record inbound / rejection / error events to the shared
channel-event spine keyed by `surface_id`
(`live_view`/`cli`/`mcp_stdio`/`mcp_http`/`acp`/`openai_api`), with dedupe + trace,
matching the channel pattern. Operator event reports must render `surface_id` so
manual validation can prove cross-surface parity without database/file greps.
- **Files:** event recording in `workspace_live.ex`, `mix/tasks/allbert.ask.ex`,
  `public_protocol/*`; a shared recorder (decorator) so all surfaces call one path.
- **Tests:** a turn on each surface records an event + trace; a rejection/error is
  persisted, not discarded. Eval `surface-event-audit-parity-001`.
- **Implemented:** `AllbertAssist.Surface.EventRecorder` records web, CLI, ACP,
  MCP stdio/HTTP, and OpenAI API ingress/rejection/error outcomes to the shared
  channel-event spine using the non-channel `surface_id` as the event `channel`.
  Operator event reports now render `surface_id` alongside the stored channel field.
- **Verified:** warning-as-error compile plus focused EventRecorder, CLI,
  ACP/MCP stdio, OpenAI/MCP HTTP, and LiveView prompt tests cover processed,
  rejected, and failed event rows.

### M3: Web Via Actions + Identity
Route web confirmation reads and settings reads through the ADR 0070 read-only action
layer; replace hardcoded `user_id:"local"`/`session_id:"web-local"` with
`Identity.resolve` + the shared session seam; apply operator-read discipline to web
panels (only `:internal`/`:read_only` reads; mutations stay explicit gated actions).
- **Files:** `workspace_live.ex` (remove direct `Confirmations`/`Settings.Store`
  reads), identity wiring.
- **Tests:** web reads resolve through registered actions; identity resolves through
  the map; no direct store read remains. Evals `web-reads-action-backed-001`,
  `web-identity-resolved-001`.
- **Implemented:** Workspace LiveView now resolves its actor through
  `Channels.Identity.resolve/3` against a list-shaped LiveView identity map, derives
  the session id through `Channels.derive_session_id/3`, and uses a registered
  internal `resolved_settings_snapshot` action for its Settings Central snapshot
  instead of reading `Settings.Store` directly.
- **Verified:** warning-as-error compile, settings action + registry tests, and
  LiveView runtime-boundary/identity-map submit tests.
- **Drift note:** M3 closes the web row. CLI/user-option and public-protocol
  client-id context reconciliation was intentionally deferred to M4's shared
  context/helper pass and is now recorded there.

### M4: Shared Surface-Invocation Helpers
Introduce `Surfaces.ContextBuilder` (`cli_context`/`live_view_context`/…),
`Actions.Helper.completed_action/2`, and `ErrorExtraction.from_response/1`; route all
surfaces + Mix tasks + web components through them, deleting the ~20× duplicated
`context/0`/`completed_action/2`/`response_error/1`.
- **Tests:** each surface builds context via the shared builder; error extraction is
  identical everywhere. Eval `surface-invocation-shared-001`.
- **Implemented:** `AllbertAssist.Surfaces.ContextBuilder` now owns shared CLI,
  LiveView, channel-callback, and public-protocol context shapes. `Actions.Helper`
  centralizes completed-action execution with extracted errors and an explicit
  response-preserving mode for legacy CLI printers. `ErrorExtraction.from_response/1`
  is the common action-response error path. Web Workspace/Objective components,
  Settings/Onboarding/Plan Preview/Template Create panels, Mix task action contexts,
  `mix allbert.ask` voice actions, MCP stdio/HTTP/OpenAI ingress, channel
  confirmation callbacks, and TUI slash-command contexts now route through those
  helpers where they build surface/action context.
- **Verified:** warning-as-error compile; expanded Mix task/helper suite
  (92 tests); public-protocol focused suites (26 core + 18 web controller tests);
  LiveView focused checks (6 tests); Plan Preview/Template Create checks
  (3 core + 8 web tests); channel/TUI focused checks (45 tests).
- **Drift note:** status-specific local wrappers remain only where commands
  intentionally accept non-`:completed` statuses (`:needs_confirmation`,
  `:not_found`, rollback/doctor/readback flows) or preserve legacy operator-facing
  error text; those wrappers now delegate to the shared context/error/helper layer
  where the behavior is generic.

### M5: Settings Central Enforcement
Fix the known bypasses (verified present 2026-06-24): the trace key
`runtime.trace_default` **already exists** — M5 removes the `ALLBERT_TRACE_ENABLED`
env-var precedence in `trace.ex` so the Settings key is authoritative (the env var
is checked first today). The remaining bypasses become **new** keys: browser
host-resolver (`plugins/allbert.browser/.../playwright.ex` `System.get_env`) →
`browser.driver.host_resolver_rules`; intent `@decisive_confidence 0.8`
(`intent/router/disambiguator.ex`) → `intent.router_decisive_confidence`;
active-memory `@candidate_limit`/`@excluded_sample_limit` (`memory/active_memory.ex`)
→ keys; trace `@workspace_recent_limit 5` (`trace.ex`) →
`runtime.trace_recent_entries_limit`; artifact `@default_timeout 15_000`
(`artifacts/ingestion_consumer.ex`) → `artifacts.ingestion_timeout_ms`. Add a **CI
guard** (Credo check + test) failing on `Application.get_env`/`System.get_env` for
operator-tunable config, with an allowlist for genuine infra/test config.
- **Settings:** the new keys above (specs + defaults + `safe_write_keys`); defaults
  in the table below mirror the current hardcoded values.
- **Tests:** each former bypass now reads through Settings Central and is operator-
  settable; the guard fails on a seeded ad-hoc read. Eval `settings-no-bypass-001`.
- **Implemented:** `Trace.enabled?/1` no longer reads `ALLBERT_TRACE_ENABLED` or
  trace app-env enablement; request-level trace remains per-turn and
  `runtime.trace_default` is the authoritative operator default. Browser
  host-resolver rules, intent decisive confidence, Active Memory candidate/excluded
  bounds, trace recent-entry bounds, and artifact ingestion timeout now resolve
  through Settings Central. `mix allbert.ask --trace` now submits a request trace
  flag instead of mutating app env. A required custom Credo check guards against
  seeded operator env/app config bypasses while allowlisting tests and genuine
  infrastructure env reads.
- **Verified:** warning-as-error compile; focused Settings/guard/trace/runtime/CLI/
  Active Memory/router/artifact suites (134 tests); expanded context/streaming/TUI
  rerun after Credo cleanups (176 tests); `mix credo --strict` over 1118 source
  files with no issues.
- **Drift note:** retained app-env reads are limited to test injection and
  infrastructure/runtime wiring (writer/model alias hooks, repo paths, test gates,
  app/plugin registries). The operator-tunable v0.58 keys are schema-backed,
  safe-writable, and read via `Settings.get/1`.

### M6: Design Tokens + UX Language
Add documented token scales (color, **type**, **spacing**, radius, **motion**) as
global CSS custom properties (not scoped to `#workspace-shell`); apply high-contrast +
reduced-motion globally. Author the UX-language doc (terminology, scales, patterns,
a11y, copy conventions).
- **Files:** `assets/css/app.css` token layer; `docs/developer/web-design-system.md`.
- **Tests:** components consume tokens (no hardcoded sizes in changed components);
  reduced-motion/high-contrast apply on a non-workspace page. Eval `design-tokens-global-001`.
- **Implemented:** `apps/allbert_assist_web/assets/css/app.css` now defines the
  built-in Allbert token scales at `:root`: color, type, spacing, radius, border,
  motion, elevation, focus, workspace aliases, and field aliases. Dark mode,
  high-contrast, prefers-contrast, explicit reduced-motion, and OS reduced-motion
  all resolve through global selectors. `Layouts.root` emits Settings-backed
  `data-theme`, `data-high-contrast`, and `data-reduce-motion` attributes so `/`,
  `/jobs`, `/objectives`, and future app-shell pages can inherit the same design
  state before a workspace shell exists. Touched interactive controls now consume
  motion/font tokens instead of adding new local timing or font systems.
- **Verified:** warning-as-error compile; focused CSS/layout/theme/a11y/workspace
  batch passed (88 tests); Jobs/Objectives/Page non-workspace rerun passed (7
  tests). Coverage includes the new `design_system_tokens_test.exs` contract and
  the non-workspace root-layout state test.
- **Drift note:** user theme token CSS remains a post-`app.css` override for
  workspace compatibility; M6 moves the built-in defaults global without changing
  the existing theme override route. Full catalog shell coverage for Jobs and
  Objectives remains M8, and the prop-driven variant/pattern work remains M7.

### M7: Component-Variant Registry + Pattern Library
Make variants prop-driven (`<.workspace_button variant=...>`, status tones, card
kinds) through the catalog; add shared accessible patterns (modal/popover,
loading/streaming, empty state, error callout) with built-in focus + ARIA. Replace
ad-hoc variant classes + hand-built modals.
- **Tests:** variant selection is prop-driven; the shared modal carries
  dialog/aria-modal/FocusTrap. Eval `component-variant-registry-001`,
  `pattern-library-a11y-001`.
- **Implemented:** `AllbertAssistWeb.Workspace.Components.Patterns` now owns the
  strict button variant registry (`primary`, `secondary`, `danger`), status-badge
  tone registry (`info`, `neutral`, `warning`, `danger`, `success` plus aliases),
  and reusable `workspace_modal/1` pattern with `role="dialog"`,
  `aria-modal="true"`, `aria-labelledby`, optional `aria-describedby`, FocusTrap,
  Escape, and optional click-away wiring. Catalog `:button`, `:action_button`, and
  `:status_badge` renderers resolve variant/tone from props and fail fast on
  unknown explicit values. The approval handoff modal now uses the shared modal
  pattern; the tile inspector carries the same modal pattern marker while keeping
  the static LiveComponent root Phoenix requires.
- **Verified:** warning-as-error compile; focused pattern/renderer/modal/a11y and
  line-targeted workspace LiveView batch passed (20 tests), covering prop-driven
  catalog variants, fail-fast unknown variants, modal ARIA/FocusTrap wiring, tile
  inspector open/close, and approval handoff rendering.
- **Drift note:** M7 establishes the registry and modal pattern without moving Jobs
  or Objectives into the shared catalog shell; that remains M8. Broader modal/
  drawer/loading/error cleanup continues in M8-M10 as those pages/panels are folded
  through the shell.

### M8: Shared App Shell + Catalog For All Pages
One shared app shell (nav, header/switchers, mobile shellbar) wrapping every operator
page; fold **Jobs** and **Objectives** into the catalog/shell (no hand-rolled HEEx);
define `grid-template` explicitly in `app.css`.
- **Tests:** Jobs/Objectives render through the catalog inside the shell; no
  undeclared atoms; a11y affordances present on those pages. Eval
  `all-pages-catalog-shell-001`.
- **Implemented:** `Layouts.operator_shell/1` now provides shared operator chrome
  with nav, header, mobile shellbar, shell data attributes, and token-backed
  appbar styling. `/jobs` renders `job_card`, `button`, and `empty_state` nodes via
  `WorkspaceRenderer` instead of a hand-rolled table while preserving the existing
  run/pause/resume IDs and events. `/objectives/:id` renders the objective summary,
  action buttons, acceptance rows, steps, events, and missing state through catalog
  surfaces inside the shell; the cancel reason flow uses the shared modal pattern.
  The existing `/workspace` shell now carries the same operator-shell data contract.
  `app.css` defines explicit grid templates for the operator shell, body, catalog
  sections, action rows, and mobile shellbar.
- **Verified:** warning-as-error compile and focused Jobs/Objectives LiveView tests
  passed, covering shell presence, catalog renderer boundaries, declared catalog
  atoms, no table fallback on Jobs, preserved action IDs/events, objective cancel
  flow, terminal/missing states, and workflow progress panel embedding.
- **Drift note:** M8 does not introduce an `/objectives` list route or move the
  workspace runtime tree away from its existing `WorkspaceRenderer` root. Operator
  panel routes/components remain M10, and the chat-primary workspace behavior remains
  M9.

### M9: Chat-Primary Re-Layout (on the design system)
Chat is the primary surface; ephemerals are modals/popovers; canvas demoted to a
launcher/drawer; **"Conversations"** relabel (UI strings only). Executed via the
CoreApp shell tree + tokens + pattern library — not a one-off styling pass.
- **Tests:** chat-primary default; ephemeral renders as modal dialog + dismiss;
  canvas reachable as drawer with tiles intact; relabel UI-only (zero internal
  identifier diffs). Evals `chat-primary-default-001`,
  `ephemeral-renders-as-modal-dialog-001`, `conversations-relabel-ui-only-001`.
- **Implemented:** `/workspace` now declares `data-layout-mode="chat-primary"` and
  `data-canvas-drawer` state. Desktop CSS uses a two-column Conversations + Chat
  grid, removes the visible split resizer, and renders the canvas as a fixed
  right-side drawer when opened. The chat header and AppBar tile-count chip open/
  close the drawer; canvas destination selections and destination URLs open the
  drawer automatically. User-facing thread labels in the rail and switcher now say
  **Conversations**, **New conversation**, and **Copy conversation id** while DOM
  IDs, event names, data attributes, modules, settings, and database/thread
  internals remain unchanged. Ephemeral surfaces continue through the shared modal
  semantics established in M7/M8.
- **Verified:** warning-as-error compile; targeted workspace/responsive/
  accessibility tests; renderer/pattern/tile-inspector component tests. Coverage
  proves chat-primary shell state, closed-by-default canvas drawer, drawer opening
  from chat/header/destination paths, Conversations relabel, retained internal
  thread selectors, and modal semantics for ephemerals.
- **Drift note:** The renderer still emits the historical split-resizer node after
  chat for compatibility with the existing `WorkspaceRenderer` recursion, but M9 CSS
  keeps the resizer hidden and removes the desktop split affordance. Removing the
  node itself can be folded into M12 cleanup if no older shell tests depend on it.

### M10: Operator Panels (Intents / Settings-Models / Surface-Policy)
The three operator panels as catalog components on the design system, rendering the
v0.56 DTOs through `Actions.Runner.run/3`. Intents: coverage, source badges, slot
counts, eval/gate status, review queue, edit/disable/promote (ADR 0071 gate).
Settings/Models: recommendation matrix, status, diagnostics, bounded inventories
(raw rows require an explicit affordance + surface policy). Surface-Policy: the M11
policy editor.
- **Tests:** panels render from action DTOs (no store reads / no CLI scraping); no
  secret/endpoint leak; gated promotion mutates via action; regressing promotion
  rejected. Evals `intents-panel-v056-dto-parity-001`,
  `intents-panel-gated-promotion-001`, `models-panel-v056-dto-redaction-001`.
- **Implemented:** added `intents_panel`, `models_panel`, and `surface_policy_panel`
  catalog atoms plus `workspace:intents`, `workspace:models`, and
  `workspace:surface_policy` launcher destinations. CoreApp exposes the panels as
  selected workspace tools. The Intents panel reads `intent_coverage`,
  `intent_list_descriptors`, `intent_list_review`, and `intent_eval_run` through
  registered actions, and its edit/disable/enable/promote
  affordances dispatch only to registered descriptor actions through
  `Actions.Runner.run/3`. The Models panel reads `model_doctor`,
  `list_provider_profiles`, and `list_model_profiles`, with profile inventories
  hidden behind an explicit bounded-row affordance. The Surface Policy panel was
  added as the catalog destination for the M11 policy editor.
- **Verified:** warning-as-error compile; focused surface/catalog/renderer tests;
  v0.56 intent DTO/security eval tests; settings action redaction tests. Coverage
  proves catalog registration, workspace destination routing, action-backed panel
  rendering, no secret/API-key/endpoint URL leak, and cross-app intent-eval fixture
  lookup from the web app.
- **Drift note:** M10 intentionally left the `surface_policy.*` namespace and write
  actions to M11; M11 now owns that operator-managed policy layer.

### M11: Operator-Managed Surface Policy Layer
A `surface_policy.*` Settings Central namespace + a read/update action pair + redacted
DTOs + integration points used by agent-facing read actions before rendering. Governs,
per surface/action: which may request `operator_report` vs `assistant_summary`, the
redaction/display profile, row/count bounds, and the explicit affordance required.
Now applies across the *uniform* surfaces (M1–M4). Security Central stays final
authority; policy cannot make `:internal` actions routable, lower floors, bypass
confirmation, or grant egress.
- **Tests:** raw reports require explicit affordance; NL turns stay bounded; policy
  edits audited + fail-closed; policy grants no authority; web/TUI/CLI parity proves
  one policy DTO. Evals `surface-policy-raw-report-affordance-001`,
  `surface-policy-settings-central-001`, `surface-policy-no-authority-grant-001`.
- **Implemented:** added `AllbertAssist.SurfacePolicy`, the Settings Central
  `surface_policy.*` namespace, `surface_policy_read`, and
  `surface_policy_update`. The policy DTO reports defaults, configured
  surface/action rows, effective report mode, redaction profile, row bounds, and
  raw-report affordance requirements. `list_settings`, `list_provider_profiles`,
  `list_model_profiles`, and `list_channels` now consult the policy before
  rendering and bound their returned DTO rows. CLI/Mix operator reports pass the
  explicit affordance; LiveView model inventories and the Surface Policy panel use
  action-backed DTOs through `Actions.Runner.run/3`.
- **Verified:** warning-as-error compile; focused Settings schema, surface-policy
  action, registry, settings/profile/channel action, and web renderer tests.
  Coverage proves raw reports downgrade without explicit affordance, policy writes
  audit through Settings Central, invalid edits fail closed, internal policy
  actions are not public tools, and the web panel renders the one policy DTO.
- **Drift note:** M11 implements report shape and row bounds only. Security Central
  remains the authority boundary; surface policy cannot expose internal actions,
  change permissions, bypass confirmation, or grant egress.

### M12: Redundancy Consolidation
Consolidate the 8 helper families into shared modules — `AllbertAssist.Maps`
(`field/3`, `get_any/2`), `AllbertAssist.Serialization` (`stringify_keys/1`),
`AllbertAssist.Validation` (`clamp_limit/4`), `AllbertAssist.Settings.Helpers`
(`setting_bool/2`) — plus the M1/M4 renderer/error/context consolidations. Verify-
before-removing the dead candidates (duplicate `sha256/1` copies,
`redact_release_output/1`, stale `context/0` post-ADR-0070).
- **Tests:** consolidated helpers pass each call site's prior behavior; no public-API
  change. Eval `redundancy-consolidation-no-regression-001`.
- **Implemented:** added the shared `AllbertAssist.Maps`,
  `AllbertAssist.Serialization`, `AllbertAssist.Validation`, and
  `AllbertAssist.Settings.Helpers` modules. Migrated the duplicated mixed-key
  `field`/`get_any` behavior in the surface renderer, M11 surface-policy path,
  intent operator supports, MCP registry/connect-spec/tool-discovery surfaces,
  and the web operator panel support. Migrated repeated deep `stringify_keys`
  helpers in workflows, Plan/Build runtime, result readback, and MCP schema
  normalization. Migrated repeated bounded-limit helpers in MCP registry/tool
  discovery and the Settings Central boolean lookup in the web panel.
- **Verified:** warning-as-error compile plus focused helper, renderer, intent
  operator, MCP registry/connect/discovery, workflow/Plan-Build, public-protocol
  readback/schema, and web renderer tests. Coverage proves the shared helpers keep
  mixed atom/string-key DTO behavior, bounded limits, recursive key
  stringification, and Settings DTO boolean lookup stable.
- **Drift note:** M12 avoided broad mechanical rewrites of domain-specific or
  test-local helpers. The `sha256` and release-output redaction helpers remain
  where they are actively used for artifact/release evidence domains; stale
  `context/0` candidates were not removed unless already covered by M4 shared
  context work.

### M13: Evals + `release.v058` Lane
`:v058` rows (above) + a `release.v058` lane bundling disposable migration, the
surface-parity units, settings-no-bypass, the design-system conformance units, panel
DTO units, surface-policy units, and a release-home secret scan.
- **Verify:** `mix allbert.test release.v058` exists after M13, is deterministic, and
  is reproducible. Absence of the lane before M13 is an implementation gap, not an
  operator-validation step.
- **Implemented:** added the `:v058` security-eval inventory rows and
  `AllbertAssist.Security.V058SurfaceConsistencyEvalTest`; exposed
  `mix allbert.test release.v058`; and bundled disposable migration, surface
  contract units, Settings Central guard/schema units, web catalog/design-system
  units, v0.56 operator-panel DTO and surface-policy units, helper-consolidation
  regression units, eval inventory/task usage checks, and the release-home secret
  scan.
- **Verified:** warning-as-error compile and focused M13 eval/task tests passed;
  `mix allbert.test release.v058` passed and wrote redacted release evidence under
  the disposable release home. The lane remains the authoritative M13 handoff
  gate before M13.1 remediation and must pass again before M14 manual operator
  validation.

### M13.1: Audit Remediation (point milestone)
Output of the **first post-implementation audit** (six parallel static auditors, one
per pillar/milestone group, plus a live Chrome-driven UI pass on `localhost:4000`).
The spine is sound — single renderer, shared context/helpers, settings enforcement +
CI guard, redundancy consolidation, the `release.v058` gate, clean compile/docs, and
full milestone coverage are real; **no security/authority BLOCKER**. The audit found
uniformity leaking at the edges: **1 BLOCKER, 8 MAJOR, ~10 MINOR/accepted**. This
point milestone fixes the BLOCKER + MAJORs in code so M14 validates remediated code.
**Blocks M14.** Operator decisions (2026-06-25): the eval and surface-policy gaps
are fixed in code, not by tightening prose. The second-pass audit then found an
operator-reachable intent-task crash, an R2-C pattern-library narrowing, and two
too-narrow drift guards; M13.1E fixed those. The third-pass audit found one
remaining MAJOR consistency item: drawer/table shared components were defined and
tested, but the production stateful catalog atoms still duplicated their contract
and only stamped matching pattern markers. M13.1F fixes that before the fourth-pass
implementation audit.
The fourth-pass audit verified M13.1F as genuine, then found that M14 operator
validation still depended on ambiguous handoff prose: hardcoded port `4000` could
hit a stale server, `/objectives/:id` had no deterministic setup, the approval
handoff and gated-promotion browser checks lacked exact triggers/expected output,
and event evidence relied on the TUI's recent-events window. M13.1G fixes those
before M14.
The v0.59 list below is planned release scope, not unresolved v0.58 remediation.

M13.1 is implemented in this order so each fix has a clear rejoin point:

| Point | Scope | Done criteria | Gate |
|---|---|---|---|
| M13.1A | Contract/security remediation | **Complete 2026-06-25.** R1-A through R1-D implemented; ADR 0070/0073 wording reflects the exposure and event-recording decisions. | Focused settings/profile, public-protocol event, and settings-no-bypass tests passed; `mix allbert.test release.v058` passed. |
| M13.1B | Design-system/catalog remediation | **Complete 2026-06-25.** `/` no longer bypasses the shell/token contract; raw `btn` usage in touched operator surfaces moves through the variant registry; ADR 0074/web guide state shipped vs deferred patterns honestly. | Focused web design-system/accessibility tests passed; Chrome smoke passed for `/`, `/workspace`, `/jobs`, and `/objectives/:id`. |
| M13.1C | Eval/policy hardening | **Complete 2026-06-25.** `:v058` eval now has behavioral assertions for renderer no-leak, rejection audit events, profile DTO redaction, and panel no-authority; surface policy covers all named operator-panel reads. | Focused eval, surface-policy, intent/model, TUI, settings, and web renderer tests passed; `mix allbert.test release.v058` passed and wrote evidence to `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v058/p0-8194/home/release_evidence/v058/release-v058-1782414643.json`. |
| M13.1D | Drift review and rejoin | **Complete 2026-06-25.** Plan, request-flow, ADR 0073, developer docs, and operator evidence expectations agree; M14 hard stop is removed because M13.1A-C passed. | `git diff --check`, `MIX_ENV=test mix allbert.test docs`, warning-as-error compile, and Credo passed. |
| M13.1E | Second-pass audit remediation | **Complete 2026-06-25.** Operator-reachable `mix allbert.intent` rejected-result crashes are fixed and covered by `release.v058`; shared status/error/loading/drawer/table-list patterns are implemented without redesigning layout; Settings/web drift guards are widened beyond touched-file allowlists; ACP sibling rejections and MCP tool pre-dispatch denials persist rejected events. | Focused intent, public-protocol, settings-guard, design-token, and shared-pattern tests passed; warning-as-error compile, Credo, docs, and `mix allbert.test release.v058` passed with evidence at `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v058/p0-12997/home/release_evidence/v058/release-v058-1782425652.json`. |
| M13.1F | Third-pass pattern-contract closure | **Complete 2026-06-25.** Root-safe shared drawer/table class/attrs helpers are implemented and consumed by both stateless pattern components and stateful `UtilityDrawer`, `Table`, `Row`, and `Column` atoms while preserving literal LiveComponent roots. Renderer parity tests prove the live atoms share one contract. The core `System.get_env` guard now flags unknown production `ALLBERT_*` reads unless explicitly classified as infrastructure; the LiveView offline banner routes through `status_callout` and the static offline shell mirrors the same marker; ADR 0073/web-design-system/request-flow wording is exact through M13.1F. | Focused pattern/renderer parity tests, settings-guard tests, and offline/banner tests passed; `git diff --check`, `MIX_ENV=test mix allbert.test docs`, warning-as-error compile, Credo, and `mix allbert.test release.v058` passed with evidence at `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v058/p0-13378/home/release_evidence/v058/release-v058-1782430687.json`. Fourth-pass Chrome smoke on current checkout verified this closure and led to M13.1G's operator-validation precision patch. |
| M13.1G | Fourth-pass operator-validation precision closure | **Complete 2026-06-25.** M14 S0-S6 now use an explicit `V058_PORT`/`V058_BASE_URL`, fail fast on stale listening servers, seed deterministic Objective detail and `list_channels` review-descriptor fixtures, name the exact approval-handoff prompt and gated-promotion DOM target/expected text, verify no generated descriptor mutation, bind MCP/OpenAI smokes to the selected port, and export bounded event evidence through the registered `operator_events` action. `mix allbert.intent` help now documents the mutation/reindex/optimize subcommands it already implements. | Focused intent task tests, docs gate, warning-as-error compile, Credo, `release.v058`, and fresh Chrome smoke on a non-default current-checkout server passed; evidence at `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v058/p0-13380/home/release_evidence/v058/release-v058-1782434923.json`. |

- **R1 contract/security:**
  - **Implemented 2026-06-25:** provider/model list DTOs are source-redacted while
    runtime `resolve_*_profile` DTOs retain internal endpoint/secret-ref fields;
    `list_provider_profiles`/`list_model_profiles` use the ADR 0070 assistant-safe
    `:agent` carve-out; ACP prompt/session/permission/unsupported-method failures
    and MCP resource/tool denials record rejected events; web production code no
    longer calls `Settings.get/1` directly and the Credo guard flags broader web
    Settings/runtime-config bypasses.
  - **R1-A (load-bearing):** redact provider/model profile DTOs at source — drop
    `base_url`/`api_key_ref`/`provider_base_url`/`provider_api_key_ref` before return
    (`apps/allbert_assist/lib/allbert_assist/settings.ex:136-137,153-154`;
    `apps/allbert_assist/lib/allbert_assist/actions/settings/list_provider_profiles.ex:40` /
    `apps/allbert_assist/lib/allbert_assist/actions/settings/list_model_profiles.ex:40`).
    No secret is painted today (template renders
    `endpoint_kind`/`credential_status`; `ExposureFilter` blocks public surfaces),
    but the contract is that the DTO itself is redacted — raw refs sit in socket
    assigns where a future render/inspect leaks them.
  - **R1-B:** reconcile `exposure: :agent` on those two reads
    (`apps/allbert_assist/lib/allbert_assist/actions/settings/list_provider_profiles.ex:6`,
    `apps/allbert_assist/lib/allbert_assist/actions/settings/list_model_profiles.ex:6`) —
    preferred remediation is an explicit ADR 0070 carve-out that is valid only
    after R1-A makes the agent-routable DTO secret-free. If implementation needs
    raw operator fields, split them into `:internal` operator-read variants.
  - **R1-C:** record pre-dispatch protocol rejections — ACP guard `else`
    (`apps/allbert_assist/lib/allbert_assist/public_protocol/acp/server.ex:136-143`)
    and MCP `read_resource`
    (`apps/allbert_assist/lib/allbert_assist/public_protocol/mcp/runtime.ex:124-141`)
    originally emitted no `record_rejection`; M13.1A closed those, and M13.1E
    extends the proof to ACP sibling methods and MCP `tool_not_exposed`.
  - **R1-D:** remove residual direct web Settings reads (`layouts.ex:225`,
    `apps/allbert_assist_web/lib/allbert_assist_web/workspace/components/template_create.ex:368,389`);
    route via the resolved-settings snapshot / a read-only action; confirm the M5
    Credo guard covers the `Settings.get/1` class.
- **R2 design-system:**
  - **Implemented 2026-06-25:** `/` remains a thin landing page but now uses
    `Layouts.operator_shell/1`, emits the operator shell `data-*` contract, uses
    token-backed classes instead of hex/slate drift, and routes buttons through
    `Patterns.button_class!/1`. Raw `btn` sites in the M13.1B-touched operator
    surfaces (`operator_panels.ex`, `settings_central.ex`, `plan_build.ex`,
    `onboarding.ex`, `template_create.ex`, `layouts.ex`, and
    `core_components.ex`) now use the variant registry or catalog button atoms.
    Chrome recheck found and fixed the `/jobs` job-card action layout by adding
    a catalog action-row children class. M13.1E implements the broader shared
    loading/error/status/drawer/table-list pattern baseline without a UX layout
    redesign; ADR 0074 and the web guide now name that baseline as shipped.
  - **R2-A (BLOCKER):** bring the home page `/` onto catalog + `operator_shell`
    (`apps/allbert_assist_web/lib/allbert_assist_web/controllers/page_html/home.html.heex:1-39`)
    — live-confirmed bypass: no shell/catalog, hex `#e8f0f7`/`#3b5b7a`,
    `text-slate-*`, raw `btn`, no shell data contract. Fold into the catalog or,
    if a thin landing page remains, replace hex/slate/`btn` with tokens +
    `Patterns.button_class!/1` / catalog button output and emit the shell `data-*`
    contract.
  - **R2-B:** adopt the variant registry across ~40 raw `btn` sites
    (`operator_panels.ex`, `settings_central.ex`, `plan_build.ex`, `onboarding.ex`,
    `template_create.ex:279-296`) via `Patterns.button_class!/1` / the `:button`
    atom; retire/relabel `core_components.ex:94-119` `<.button>`. Live-confirmed
    `btn btn-secondary` in the shell.
  - **R2-C (M13.1E implemented; M13.1F closed):**
    `Patterns.status_callout/1`, `Patterns.error_callout/1`, and
    `Patterns.loading_state/1` own the shared callout/loading baseline and are
    adopted by Jobs notices, Objective response/error callouts, workspace thread/
    approval/loading states, and tile conflict banners while keeping existing
    layout structure. `Patterns.drawer_shell/1`, `Patterns.table_list/1`,
    `Patterns.table_row/1`, and `Patterns.table_column/1` define and test the
    drawer/table baseline. M13.1F adds shared attrs/classes/contract helpers,
    wires production stateful `UtilityDrawer`, `Table`, `Row`, and `Column` atoms
    through those helpers while preserving static root tags, and adds tests that
    fail if the stateless and stateful contracts drift.
- **R3 eval/policy (code):**
  - **Implemented 2026-06-25:** `v058_surface_consistency_eval_test.exs` now keeps
    the exact-set EvalInventory checks and adds behavioral assertions for
    `Surface.Renderer` payload no-leak, `EventRecorder.record_rejection/2`
    `surface_id` audit rows, provider/model profile source redaction, and
    operator-panel no-authority. The focused M13.1C test set passed, and
    `mix allbert.test release.v058` passed with evidence at
    `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v058/p0-8194/home/release_evidence/v058/release-v058-1782414643.json`.
  - **R3-A:** the `:v058` eval module is no longer inventory-only; it directly
    asserts the contracts that were only described by rows before M13.1C.
  - **R3-B:** `intent_coverage`, `intent_list_descriptors`,
    `intent_list_review`, and `model_doctor` now call
    `SurfacePolicy.report_policy/3`, report policy metadata, keep summaries
    fail-closed by default, and require `surface_policy_affordance` for raw
    operator reports. CLI/TUI/LiveView policy defaults name those reads; Mix,
    TUI slash commands, and web panels pass the explicit operator affordance.
- **Conformance-matrix correction:** `/` home-page row is now a thin-landing pass
  after R2-A. Surface-policy coverage now reflects the actual M13.1C read set.
- **v0.59 planned scope (not unresolved M13.1 remediation):** `field/3` ~128-copy
  long tail; `limit/1` clamp folding; `"local"`/`"web-local"` identity literals
  (`workspace_live.ex:54-57`; "User local" in `/jobs`); Objectives-nav→`/workspace`
  wart; `data-theme=null` on `<html>` theming check; `ErrorExtraction` unit test;
  bespoke `response_error/1`
  (`allbert.voice.local.ex:100-102`, `marketplace/panels/catalog.ex:190-191`);
  `stringify_keys` struct passthrough / `Maps.get_any` falsy semantics (latent);
  stale `CHANGELOG.md:296`.
- **M13.1F implementation:** root-safe consolidation was chosen over a v0.59
  scope cut. `Patterns` exposes canonical drawer/table classes and attrs, the
  stateful catalog atoms consume those helpers, and existing renderer DOM IDs/
  events stay stable. The Settings Central guard rejects unknown production
  `System.get_env("ALLBERT_*")` reads unless explicitly classified as
  infrastructure. The LiveView offline banner routes through the shared
  status-callout contract and the static offline shell mirrors the marker. ADR
  0073's stale M13.1A-D status line and pre-dispatch rejection prose are fixed.
  Reviewed residual minors remain accepted for M14 with current rationale:
  `set_active_model_profile` stays `:agent` but is permission-gated by Security
  Central; duplicated `setting_value/3` helpers are snapshot/context-local and not
  a release blocker; the render-mode enum remains fail-closed.
- **M13.1G implementation:** the fourth-pass audit found no remaining code
  consolidation blocker, but the manual-validation handoff was still too loose.
  The request-flow now names a non-default validation port, checks for stale
  listeners before Phoenix starts, seeds the Objective/detail and gated-promotion
  fixtures with exact `mix run` code, gives the approval-handoff prompt and DOM
  selector expected by Chrome validation, binds public-protocol smoke URLs to the
  chosen port, and captures event evidence through `operator_events` with
  `limit: 50`. `mix allbert.intent` help documents the implemented mutation and
  maintenance subcommands used by the validation.
- **Gate:** M13.1E adds `allbert_intent_test.exs` to `release.v058`, broadens the
  Settings/web drift guards, and adds ACP/MCP rejected-event assertions. M13.1F
  reran focused pattern/renderer/settings/offline tests, warning-as-error compile,
  Credo, docs, and `release.v058`. M13.1G reruns focused intent/docs/static gates,
  `release.v058`, and a fresh Chrome smoke on a current checkout before commit.
  Any operator-facing flow a fix touches is re-validated live with Chrome before
  M14 manual validation, not by paper review.

### M14: Operator Validation (manual gate)
A real operator runs the fully-fleshed S0–S6 validation in `v0.58-request-flow.md`:
deterministic gate, disposable home, Phoenix start, **browser pass** (chat-primary,
modals, canvas drawer, the three panels, design-system conformance, screenshots),
**cross-surface parity** (same operation → same DTO + event across web/TUI/CLI/MCP),
and **settings-no-bypass proof**. Durable redacted evidence; sanitized closeout note.
**Depends on M13.1; blocks M15.**

### M15: Closeout
Flip ADR 0073/0074 → Accepted; record ADR 0016/0030/0070 amendments + the ADR
0004/0031 note + the ADR 0024 revision; version → `0.58.0`; CHANGELOG; reconcile
roadmap, vision, and agent-context-map. Depends on M14. Tag held out of the closeout
commit per project convention.

## Settings Central Keys (new in v0.58)
| Key | Default | Purpose |
|---|---|---|
| `runtime.trace_default` | `disabled` | Trace default — **key already exists**; M5 removes the `ALLBERT_TRACE_ENABLED` env-var precedence so this key is authoritative. |
| `runtime.trace_recent_entries_limit` | `5` | Workspace recent-trace render bound. |
| `browser.driver.host_resolver_rules` | `nil` | Browser host-resolver rules (replaces the env bypass). |
| `intent.router_decisive_confidence` | `0.8` | Stage-2 decisive-confidence override (was hardcoded). |
| `active_memory.internal_candidate_limit` | `1_000` | Candidate scan bound (was hardcoded). |
| `active_memory.excluded_sample_limit` | `5` | Excluded-sample bound (was hardcoded). |
| `artifacts.ingestion_timeout_ms` | `15_000` | Artifact ingestion timeout (was hardcoded). |
| `surface_policy.*` | (per ADR 0073/M11) | Per-surface/action report mode, redaction/display profile, bounds, affordance. |

## Security / Eval Rows (`:v058`)
Surface consistency:
- `surface-renderer-unified-parity-001`, `surface-event-audit-parity-001`,
  `web-reads-action-backed-001`, `web-identity-resolved-001`,
  `surface-invocation-shared-001`.
Settings enforcement:
- `settings-no-bypass-001` (no operator-tunable config read outside Settings Central;
  CI guard fails on a seeded ad-hoc read).
Web design system:
- `design-tokens-global-001`, `component-variant-registry-001`,
  `pattern-library-a11y-001`, `all-pages-catalog-shell-001`,
  `chat-primary-default-001`, `ephemeral-renders-as-modal-dialog-001`,
  `conversations-relabel-ui-only-001`, `workspace-shell-validates-against-catalog-001`,
  `fragment-emission-hmac-validated-001`, `launcher-selection-view-only-001`,
  `mobile-single-column-and-reduced-motion-001`.
Operator panels + surface policy:
- `intents-panel-v056-dto-parity-001`, `intents-panel-gated-promotion-001`,
  `models-panel-v056-dto-redaction-001`, `surface-policy-raw-report-affordance-001`,
  `surface-policy-settings-central-001`, `surface-policy-no-authority-grant-001`.
Redundancy:
- `redundancy-consolidation-no-regression-001`.

## Documentation Deliverables
- ADR 0073 + 0074 accepted; ADR 0016/0030/0070 amendments + ADR 0004/0031 note + ADR
  0024 revision recorded.
- Developer guides: `docs/developer/surface-contract.md` (the methodology +
  conformance matrix) and `docs/developer/web-design-system.md` (tokens, variants,
  patterns, shell).
- Operator guide: `docs/operator/workspace.md` updated for chat-primary,
  Conversations, panels, and surface policy.
- Roadmap / vision / agent-context-map reconciliation.

## Non-Goals
- No rebuild of the canvas/ephemeral substrate; no model-generated UI.
- No internal `Thread`→`Session` rename; no DB/schema rename; no new routes.
- No descriptor-YAML control of report shape or authority.
- No settings *migration* tooling (v0.59, ADR 0046); v0.58 governs the access path,
  not schema versioning.
- No central action param-contract enforcement (v0.59 M7, ADR 0065); v0.58 makes the
  invocation path uniform so v0.59 can enforce on it.
- No new user-facing capability or authority.

## Definition Of Done
v0.58 is done when:
- every surface (web, TUI, channels, Mix, public protocol, Pi-mode) conforms to ADR
  0073: one `Surface.Renderer`, uniform event/audit by `surface_id`,
  `Identity.resolve`-based identity, spine-only reads, operator reads through the
  registered-action layer, and the shared invocation helpers — the conformance matrix
  is all ✓;
- no operator-tunable config is read outside Settings Central; the 6 known bypasses
  are fixed and the CI guard fails on ad-hoc reads;
- the web is a documented design system: global token scales, a prop-driven variant
  registry, a shared accessible pattern library, one app shell, and the catalog as
  the rendering boundary for every page (Jobs/Objectives folded in);
- `/workspace` renders chat-primary with modal ephemerals, a canvas drawer, and the
  "Conversations" relabel (UI-only, zero internal churn), executed on the design
  system;
- the Intents, Settings/Models, and Surface-Policy panels render v0.56 DTOs through
  registered actions with no secret/endpoint leak and no new authority; the
  operator-managed surface-policy layer ships (Settings keys + actions + DTOs) and
  governs report shape across the uniform surfaces;
- the redundant helper families are consolidated with no public-API change and no
  regression; dead candidates are verified and removed or kept with a note;
- `:v058` evals + `release.v058` + the full release gate pass, plus the M14 operator
  validation (browser + cross-surface parity + settings-no-bypass) with durable
  evidence;
- ADR 0073/0074 Accepted; ADR 0016/0030/0070 amendments, the ADR 0004/0031 note, and
  the ADR 0024 revision recorded; version is `0.58.0`; roadmap, vision,
  agent-context-map agree.
