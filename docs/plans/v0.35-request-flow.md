# Allbert v0.35 Request Flow - User Theming And Layout Overrides

Status: planned.

This document describes operator-visible behavior for v0.35. Implementation
details live in `docs/plans/v0.35-plan.md`.

## Source Documents

- `docs/plans/v0.35-plan.md`
- `docs/plans/v0.33-plan.md`
- `docs/plans/v0.33-request-flow.md`
- `docs/plans/v0.32-plan.md`
- `docs/plans/v0.32-request-flow.md`
- `docs/plans/v0.31-plan.md`
- `docs/plans/v0.31-request-flow.md`
- `docs/adr/0024-app-ui-contribution-and-workspace-zones.md`
- `docs/adr/0025-user-theming-and-override-security.md`

## Pre-Flight

Use a disposable Allbert Home and open `/workspace`:

```sh
export SMOKE_HOME=$(mktemp -d /tmp/allbert-v034-smoke.XXXXXX)
ALLBERT_HOME="$SMOKE_HOME" mix ecto.migrate
ALLBERT_HOME="$SMOKE_HOME" mix phx.server
```

v0.35 assumes v0.31 consolidated runtime paths/settings fragments, v0.32 made
`/workspace` the only operator home with Settings Central inside the workspace
utility drawer, and v0.33 added app handoff/clarification surfaces that theme
overrides must keep readable and reachable.

## Flow 1: Theme Settings Are Visible In Workspace

Operator opens Settings Central from the `/workspace` utility drawer.

Expected behavior:

1. Theme and layout settings are listed with defaults.
2. `workspace.theme.active` can be set to a theme name or cleared.
3. Snippets are disabled by default.
4. Layout override is disabled by default.
5. All writes use existing Settings Central action/audit behavior.

## Flow 2: Token Theme Retints Workspace

Operator writes `<ALLBERT_HOME>/themes/midnight.yaml` and sets
`workspace.theme.active=midnight`.

Expected behavior:

1. `/theme/user.css` serves a CSS variable block.
2. `/workspace` loads the stylesheet after `app.css`.
3. Colors/density/radii visibly change on reload with no asset rebuild.
4. Chrome desktop and narrow verification show no overflow or illegible
   contrast regression.

Failure behavior:

- Missing or invalid token files fall back to defaults per-key with bounded
  warnings.

## Flow 3: Invalid Token File Falls Back Safely

Operator selects a malformed theme file.

Expected behavior:

1. The workspace still loads.
2. Invalid keys/values are ignored.
3. Defaults fill missing values.
4. Operator-visible diagnostics name the rejected keys without exposing
   secrets or file contents unnecessarily.

## Flow 4: CSS Snippet Applies Only When Enabled

Operator writes `<ALLBERT_HOME>/themes/snippets/compact.css`.

Expected behavior:

1. With `workspace.theme.snippets_enabled=false`, no snippet CSS is served.
2. With snippets enabled and `compact.css` listed in
   `workspace.theme.enabled_snippets`, sanitized CSS is served.
3. The snippet loads after token CSS and can adjust local presentation.

Failure behavior:

- Unknown snippet names, path traversal, non-CSS files, and missing files are
  rejected with bounded diagnostics.

## Flow 5: Unsafe CSS Is Stripped Or Rejected

Operator enables a snippet containing `@import`, `url()`, `image-set()`, or a
remote font/source construct.

Expected behavior:

1. `@import` is rejected.
2. `url()` and `image-set()` are stripped.
3. Remote font/source constructs are stripped.
4. The served CSS is empty if the file is entirely unsafe.
5. The workspace remains usable.
6. CSP blocks remote style/image fetches.

## Flow 6: Layout Override Reorders Workspace

Operator writes `<ALLBERT_HOME>/workspace/layout.yaml` and sets
`workspace.layout.override_enabled=true`.

Expected behavior:

1. Valid zone ordering/hiding is reflected in `/workspace`.
2. Valid panel pins appear in allowed zones.
3. Unknown zones, unknown panels, or invalid placements are ignored per-key
   with warnings.
4. Layout data cannot create routes, components, action bindings, or
   permissions.

Failure behavior:

- Invalid layout YAML falls back to the default v0.32 layout.

## DOM Ids And Events

Stable handles for tests and Chrome manual verification:

| Surface | Handle | Expected use |
|---|---|---|
| Theme stylesheet | `link[href^="/theme/user.css"]` | Token stylesheet loaded after app CSS |
| Snippet stylesheet | `link[href^="/theme/snippets/"]` | Sanitized snippet stylesheet |
| Workspace shell | `#workspace-shell` | Visual verification target |
| Settings panel | `#workspace-settings-panel` | Theme setting controls |
| Theme diagnostics | `#workspace-theme-diagnostics` | Bounded warnings |
| Zone | `#workspace-zone-<zone>` | Layout override verification |
| Panel | `#workspace-panel-<surface-id>` | Panel pin verification |

## Smoke Checklist

- Theme settings appear inside `/workspace`.
- Token theme retints with no rebuild.
- Invalid token file falls back safely.
- Safe snippet applies only when explicitly enabled.
- Unsafe snippet constructs are stripped/rejected and diagnostics appear.
- CSP header is present.
- Layout override reorders/hides zones and pins panels.
- Invalid layout data falls back safely.
- Chrome verification covers desktop and narrow widths.

## Milestone User Testing

### M1-M2 Theme Roots And Tokens

1. Confirm theme roots exist in a disposable Allbert Home.
2. Select a valid token theme.
3. Reload `/workspace`; confirm visible retinting.
4. Select an invalid theme; confirm fallback.

### M3-M4 CSP And Snippets

1. Confirm CSP headers on `/workspace` and theme routes.
2. Enable a safe snippet; confirm presentation changes.
3. Enable unsafe snippet; confirm strip/reject behavior and warnings.

### M5 Layout Override

1. Enable valid layout YAML; confirm zone order/hide/pin behavior.
2. Add invalid keys; confirm per-key warnings and safe fallback.

### M6 Closeout

1. Run focused tests and full gate.
2. Run Chrome desktop and narrow verification.
3. Update README, roadmap, ADR 0025, agent-context map, and release notes.

## Milestone Update Log

- (planned) entries added per milestone at implementation time.
