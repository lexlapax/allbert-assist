# Allbert v1.2.0 Zero-Click First Run

Request flow: `docs/plans/v1.2-request-flow.md`
ADRs: two new ADRs ship with this release — ADR 0087 (zero-click first run
and detection-based enablement — the consent ADR, binding on M1–M3) and
ADR 0088 (model catalog, chooser, and fallback/degradation policy — binding
on M4/M5). ADR 0078 (First-Model Path) and ADR 0069 (Guided Onboarding)
carry v1.2 amendments recorded 2026-07-24 — their substance stands; only the
enablement point and the gate direction move. Consumes ADR 0072
(recommendation matrix + `model_doctor`), ADR 0061 (router model tiers),
ADR 0051 (`model_preferences.*` authority shape), ADR 0047 (doctor
envelope), ADR 0075 (personas), ADR 0031 (Settings Central), ADR 0006
(Security Central), and the v1.0 public-contract freeze (`release.v1` stays
green).

Status: **implementation-ready after the M0 decision barrier below.** v1.1.0
is shipped and its triad is archived (stable closeout HEAD `e7d8ec7a`,
2026-07-26), so the predecessor barrier is satisfied. BUILD MUST start with
M0 and may not enter M1 until the raw-setting presence contract, usable-model
selection rule, hosted-provider priority, cross-platform curated-model bakeoff
protocol, and numerical performance budgets are recorded in Build Progress.
M0 re-verifies every Current Code State anchor at build-start HEAD.

Source: roadmap ladder item 6 (1.2 flagship) and the future-features
entries: Zero-Click First Run (`future-features.md:542-583`, Must/L,
flagship), Model Chooser / Catalog (`:922-932`, enabler), Model
Fallback / Degradation Policy (`:864-884`, enabler — required by the
zero-click detect states). Operator decisions of 2026-07-24 locked the
consent posture (auto-enable on any detected configured provider), the
additive-only migration posture for 1.2–1.4, and the flagship-focused
validation contract (Locked Decisions below).

Implementation-readiness review 2026-07-26: the flagship is unambiguously
**Zero-Click First Run — install, open, chat**. Catalog/chooser, fallback, and
step-addressable onboarding are direct enablers, not co-flagships. The review
compared Hermes Agent, OpenClaw, and IronClaw and added the M0/M4 competitive
and local-model evidence work below; no competitor feature is imported merely
for parity. The current `llama3.2:3b` default stays unchanged until the
cross-platform bakeoff earns a replacement.

## Build Progress

(Empty until build start. Entries land here per the v1.1 precedent: each
milestone implement → focused test → drift-check → doc update → commit
`v1.2 M<n> <small title>`; root-caused drift fixes; measured claims cite the
test-metrics store; intake candidates held for operator disposition, never
written to future-features unilaterally.)

## Purpose

Ship the 1.2 flagship: install, open, chat. First run auto-detects an
already-provisioned model provider, auto-selects it local-first, enables
direct answers with an audited disclosure, and never dead-ends when nothing
is provisioned. Onboarding survives as an optional, step-addressable
customization surface, and the model surface gains a real catalog/chooser
and an opt-in fallback policy.

1. **Zero-click chat-ready.** A fresh Home with any reachable provisioned
   provider (configured local endpoint, localhost Ollama with a usable
   model, or a hosted key) answers the operator's **first question with the
   model, zero prior clicks**, on every surface — web workspace, packaged
   `allbert tui`, dev `mix allbert.tui`, `allbert ask`, bare `allbert` —
   with the one-time disclosure visible (ADR 0087 §1–§3).
2. **Honest empty state, no dead ends.** A fresh Home with nothing
   provisioned still opens a working chat: the deterministic fallback
   answers, exactly one primary repair CTA per detect state is presented
   (one-click curated pull / guided install / BYOK), and no wizard wall or
   TUI refusal exists anywhere (ADR 0087 §4).
3. **Onboarding is optional and step-addressable.** Every wizard step is
   directly enterable at any time, including after completion; re-entry and
   rewind never re-gate chat or the TUI; `onboarding_complete` stops gating
   anything user-facing (ADR 0087 §5, ADR 0069 amendment).
4. **A real model catalog and chooser.** One read-only catalog (curated
   local + live runtime inventory + configured profiles + verified hosted
   metadata) rendered by web/TUI/CLI/onboarding through registered read-only
   actions; selection and pulls write only through existing Settings actions
   (ADR 0088 §1–§2).
5. **Fallback with an egress line.** Selection-time local-first ordering is
   always on; runtime failover is default-OFF opt-in, local→hosted is
   double-gated, at most one failover per turn, every event audited, and the
   whole-chain-failed message stays honest (ADR 0088 §3).
6. **The consent line is redefined deliberately, not incidentally.**
   ADR 0087 Accepted; the DIT-2-class acceptance criteria replaced; an
   operator-stored `false` is never overridden; detection never provisions
   (no installs, pulls, key writes) and performs no hosted egress.

## Expected Inputs

- Stable v1.1.0 shipped and archived (build-start precondition). It carries
  the fan-out substrate, `release.v11` gate, behavioral `:v11` eval-row
  pattern with the meta-contract (M12.5), the FV-style focused operator
  validation convention (M12.11), gate-owned root isolation (M12.10.1), and
  the corrected caller/capability census discipline (M12.6).
- The first-run substrate: wizard state machine + go-signal
  (`onboarding.ex`), detection chain (`cli/first_run.ex`,
  `first_model/ollama.ex`, `settings/model_doctor.ex`), web auto-open
  policy, TUI readiness guard, persona seeds — all anchored below.
- The model substrate: `Settings.Models.for/2` + `candidates_for/2`,
  `model_profiles.*`/`model_preferences.*`/`providers.*` schema,
  ReqLLM/`ModelRuntime.model_spec/1`, `model_doctor` (ADR 0072).
- Gate machinery: `@release_v11_steps` composition precedent
  (`allbert.test.ex:5883`), EvalInventory milestone enum
  (`eval_inventory.ex:58` — `:v12` appends here), the v11 sweep
  meta-contract pattern (`v11_sweep_eval_test.exs:61-68`), docs-gate active
  plan list (`allbert.test.ex:212-219` — the v1.2 triad must be added
  there in the same change that creates it).
- Metrics-store baselines re-recorded at M0 (decide-turn, boot-to-ready,
  gate walls); v1.1 M0 figures are superseded by then.

## Current Code State

Verified 2026-07-24 at v1.1 M12.13 HEAD (recon pass; file:line anchors).
M0 re-verifies every anchor at build-start HEAD and records the drift table.

**Two different chat blocks exist today.**
Web/CLI ask is soft-blocked: `intent.direct_answer_model_enabled` defaults
`false` (`settings/schema.ex:735-740`, safe-write list `:53`, defaults tree
`:3629`) and `direct_answer` returns the deterministic `:model_disabled`
fallback (`actions/intent/direct_answer.ex:96-101`, text at `:312-317`).
Packaged TUI is hard-blocked: `CLI.Tui.readiness_guard/1`
(`cli/tui.ex:69-101`) refuses to start unless
`FirstRun.detect_details().state == :product_ready`. **Dev `mix allbert.tui`
(`plugins/allbert.tui/lib/mix/tasks/allbert.tui.ex`) has no guard at all** —
packaged and dev TUI diverge.

**Detection already runs; v1.2 changes what is done with it.**
`FirstRun.detect_details/1` (`cli/first_run.ex:71-96`) resolves six product
states and six model states (`:35-49`). The probe chain
(`first_model_state/1`, `:127-157`): configured local provider first
(`configured_local_probe/0` `:251-261` — `model_preferences.primary` →
`ModelDoctor.diagnose/2`, accepts `endpoint_kind: :local_endpoint`; the
v1.0.5 M8.4 WSL2 rule), then localhost Ollama discovery
(`default_ollama_probe/0` `:243-246`; `Ollama.probe/1`
`first_model/ollama.ex:87-100` — binary / `GET /api/version` / curated tag
in `GET /api/tags`, loopback-enforced `:73-80`), then BYOK env sniff
(`byok_ready?/0` `:232-237` — ANTHROPIC/OPENAI/OPENROUTER/GOOGLE/GEMINI key
vars). Curated model: `first_model.curated_model` default `llama3.2:3b`,
floor `first_model.curated_floor_gb` 8 (`schema.ex:960-972`, defaults
`:3667-3669`; `Ollama.curated_model/0` `ollama.ex:27-47`). Hardware floor:
`first_model/hardware.ex`. Guarded wrapper + readiness mapping:
`Onboarding.safe_first_model_state/0` (`onboarding.ex:500-516`, test
override `:first_model_state_override`), `readiness_label/1` (`:480-493` —
`local_ready|byok_ready→:ready`).

**The wizard owns enablement today.** Steps/tracks `onboarding.ex:31-33`
(`welcome track_select model_path profile_select profile_review
health_check first_chat optional_connect`; QuickStart drops
`optional_connect`, `:469-470`). `wizard_advance/3` (`:260-293`) completes
only when `first_chat_ready?/1` (`:324-329` — readiness `:ready` AND the
consent key true; "single source of truth… derived, never stored").
`maybe_enable_model_answer/2` (`:303-316`) flips
`intent.direct_answer_model_enabled` + `intent.model_assist_enabled` on
readiness `:ready` at steps `model_path|first_chat|optional_connect` —
the ADR 0078 M8.5 enablement point v1.2 supersedes. `wizard_rewind/2`
(`:342-372`) truncates `wizard_done` and clears `onboarding_complete` but
never revokes enablement (`:335-337`) — under today's gating a rewind
re-triggers web auto-open and the TUI block. State lives in the Home marker
`onboarding.json`, not Settings (`cli/first_run.ex:33`, atomic write
`:301-310`, corrupt-marker guard `:285-297`). Personas seed the same keys
(`priv/personas/*.yaml`; applied via confirmation-gated
`apply_persona_profile`, review at `apply_persona_profile.ex:98-102`;
selection recorded in the marker, `onboarding.ex:158-168`).

**Web first-run surfaces.**
`Workspace.FirstRun` auto-open policy
(`workspace/first_run.ex:15` `@auto_open_states
[:onboarding_incomplete, :profile_unreviewed, :first_model_not_ready]`,
destination `:41-49`), consumed at `workspace_live.ex:1382-1407`. Wizard
canvas incl. Start QuickStart/Advanced buttons
(`workspace/components/onboarding.ex:345-366`), go-signal banner
(`:334-343`), per-step rewind (`:368-400`, event `:86-88`). Chat renders
exactly one first-run entry point (`components/chat.ex:736-800`,
v1.0 R12). ModelsPanel = inventory/repair, not a chooser
(`operator_panels.ex:372+`; pull via `start_async` `:383-460`). Pull
progress plumbing: signal `signals.ex:19`
(`allbert.workspace.first_model.pull.progress`), forwarded at
`workspace_live.ex:1604-1612`.

**CLI surfaces.** Bare `allbert` dispatches `first_run/0`
(`cli.ex:244`, `:390-433` incl. repair suffixes). Terminal wizard:
`cli/areas/onboarding.ex` (routes `:136-188`, interactive `:200`, provider
key entry `:290`) + `mix/tasks/allbert.onboard.ex`. Boot ordering hazards
detection inherits: `:req` must be started before probing (`cli.ex:76`,
`cli/tui.ex:39,61-64` `ensure_http_started`) and settings must be read
fresh-process (`cli/tui.ex:34-38`, the v1.0.5 RC.2 lesson).

**Model configuration and resolution.**
Namespaces in `settings/schema.ex`: `providers.*` (`:123-126`, defaults
`:3687-3716`, secret refs like `secret://providers/openai/api_key`),
`model_profiles.*` (`:148-155`, defaults `:3720-3794` — `local`,
`coding_local`, `pi_coding_local`, `fast`, `anthropic_fast`,
`openrouter_fast`, `coding`, `embedding_local`, `router_local`,
`router_escalation_local`), `model_preferences.*` (`:82-84`, defaults
`:3671+` — `primary: "local"`, `tasks.direct_answer: ["local"]`).
Resolution: `Settings.Models.for/2` (`settings/models.ex:31`, task→
capability map `:13-16`), ranked `candidates_for/2` (`:50`),
`primary_fallback/2` (`:143-147`). Direct answer resolves ONE profile
(`direct_answer.ex:104`) and any provider error collapses to the static
`{:model_unavailable, _}` text (`:126,327-329`) — **text chat has no
multi-candidate retry**; only image/voice consume `candidates_for/2`
(`generate_image.ex:48`, `transcribe_voice.ex:43`,
`synthesize_voice.ex:42`). ReqLLM seams: `req_llm_answerer.ex` (injectable
via `answerer/0`, `direct_answer.ex:347-351`),
`ModelRuntime.model_spec/1` (`settings/model_runtime.ex:14-24`).

**No catalog exists.** `llm_db` is transitive-only via ReqLLM 1.17.1
(`mix.lock:45,68`; zero direct usage). `ProviderCatalog.known_capabilities/0`
reads settings, not an external catalog (`settings/provider_catalog.ex:66`).
`set_active_model_profile` writes the legacy `intent.model_profile` alias
(`set_active_model_profile.ex:50-60`) — v1.2 leaves the aliases untouched
(additive-only; removal stays with the 1.5/1.6 migration-runner cluster).

**Tests and lanes today.** `onboarding_test.exs` (28 tests, DataCase
`async: false`, no lane moduletag — M0 adjudicates its lane),
`first_run_test.exs` (16, `app_env_serial`), `first_model_test.exs` (18,
`:first_model_path`, gate row at `allbert.test.ex:4698`), web
`workspace_onboarding_test.exs` (11) + `first_run_test.exs` (4), CLI
onboarding tests, and three onboarding security evals.

## Locked Decisions

Decisions 1–3 are operator-locked (2026-07-24 planning review); the rest are
derived from those three plus code evidence, recorded for veto at plan
review.

1. **Auto-enable on any detected configured provider (operator).**
   Local-first strict order: configured local endpoint → localhost Ollama →
   hosted key presence. Detection is enablement; provisioning is consent.
   ADR 0087 binds the semantics, disclosure, and guardrails.
2. **Additive-only release (operator, applies to 1.2–1.4).** No non-additive
   migration; the Settings Runtime Migration Runner stays on the 1.5/1.6
   train; legacy `intent.*model_profile` aliases remain untouched. Each
   milestone's acceptance includes "no non-additive migration introduced".
3. **Validation is flagship-focused FV rows (operator).** The v1.1
   OV-00..OV-12 → FV-01..FV-04 correction is the standing convention:
   few feature rows, opened only after one uninterrupted authoritative
   release gate at the pushed candidate SHA; no validation-only product
   capability may ship (M12.11 principle).
4. **Sticky explicit-false (derived, v1.1 LD32 pattern).** Auto-enablement
   fires only when the consent key is absent (schema default). An
   operator-stored `false` is never overridden by detection; disabling once
   means disabled until the operator re-enables.
5. **Disclosure, not confirmation (derived, ADR 0087 §3).** One-time durable
   marker per surface; text generated from the audit provenance
   (`enabled_by: detection`, profile, provider class); hosted selection
   names the provider and the egress fact before the first message is sent.
6. **Detection never provisions and never performs hosted egress (derived).**
   No installs, no pulls, no key writes from detection; hosted detection is
   key presence only; model pulls stay one-click confirmation-gated.
7. **TUI guard inversion with dev/packaged parity (derived).** The TUI
   always starts; non-ready detect states render as in-TUI guidance.
   `mix allbert.tui` and packaged `allbert tui` adopt identical posture —
   the current divergence closes.
8. **`onboarding_complete` is decoupled from availability (derived).** It
   remains a wizard-progress marker only. Rewind/step re-entry never
   re-gates chat, never re-triggers auto-open, never re-blocks the TUI.
   The ADR 0069 false-complete rule stands: the wizard still cannot claim
   complete while readiness is non-ready.
9. **Detection stays off the hot turn path (derived).** Boot-time +
   event-driven (settings change, explicit re-detect); results cached
   through the existing `detect_details` caching seam (M7.9 precedent).
   M6 carries a measured budget: no regression to the decide-turn baseline
   and a bounded boot-to-ready cost, both cited from the metrics store.
10. **Catalog per ADR 0088 §1 (derived).** Four merged sources; `llm_db`
    fitness is an M4 verification with the result recorded either way;
    exposure only via registered read-only actions; the chooser writes only
    through existing settings actions and safe-write keys.
11. **Fallback split per ADR 0088 §3 (derived).** Selection-time ordering
    always on; runtime failover default OFF
    (`models.fallback.enabled=false`), local→hosted double-gated
    (`models.fallback.allow_local_to_hosted=false`), at most one failover
    per turn (`models.fallback.max_failovers_per_turn` default 1, clamp
    1..2), every event traced + audited, reply metadata names a non-primary
    answering profile.
12. **Gate = `release.v12` stacked on `release.v11` (derived).**
    `@release_v12_steps = @release_v11_steps ++ @v12_focused_steps` — the
    v105-on-v104 stacking precedent; v1.1's fan-out contracts are one
    release old and stay enforced; the added wall cost is minutes. The
    docs-gate active-plan list (`allbert.test.ex:212-219`) gains the v1.2
    triad in the same commit that starts the build.
13. **Behavioral eval rows from day one (derived, M12.5 lesson).** The
    `:v12` rows ship with the v11-style meta-contract (exact id set,
    distinct assert sets, production-boundary execution, no source-grep
    proofs) as a red-first deliverable of M7 — and the two flagship
    acceptance scenarios (zero-click answer; empty-state repair path) land
    as ordinary automated regressions in M1/M2, before any UI milestone,
    so operator validation cannot be the first end-to-end execution
    (M12.12 lesson).
14. **DIT-2-class acceptance criteria are replaced, not patched (derived,
    ADR 0087 §6).** The plan's M9 documentation deliverable rewrites the
    first-run acceptance rows; stale criteria may not survive in
    `docs/validation/` or operator guides.
15. **Fresh-Home fixtures are the validation substrate (derived, M12.9
    lesson).** Every automated acceptance scenario and every FV row runs
    against a disposable Home; nothing touches `~/.allbert`; no cloned
    state weakens clean-install evidence; runbooks avoid `set -e`/`exit`.
16. **Raw presence is the consent fact (readiness review).** Effective
    `Settings.get` values are default-merged and cannot distinguish absent
    from explicitly stored `false`. M1 adds one Store-owned atomic
    compare-and-write operation that reads the raw user-settings map, checks
    `Schema.get_dotted(user_settings, key)`, and writes under the same
    `StoreLock`; `nil` may auto-enable, stored `false` is sticky, stored
    `true` is idempotent. No caller performs a read-then-write sequence.
17. **M1 owns a minimal usable-model selector (readiness review).** First-run
    cannot depend on the M4 chooser. M1 selects: configured doctor-healthy
    local profile; otherwise the curated pulled tag; otherwise the first
    pulled text-generation model that passes a deterministic compatibility
    predicate recorded at M0. M4 reuses this predicate in the catalog rather
    than replacing it. If that predicate cannot be made trustworthy, M1 is
    deliberately curated-tag-only and `detected_needs_model` remains honest.
18. **Hosted detection is configured-but-unverified (readiness review).** Key
    presence performs no probe and makes no reachability claim. Hosted
    priority is existing explicit `model_preferences.primary`, then
    task-specific configured order, then a stable provider order recorded at
    M0; never environment enumeration order. A bad/expired key may enable the
    configured path but its first failed turn returns the honest unavailable
    state and repair CTA, never a false `provider_ready` claim.
19. **Disclosure precedes hosted transport atomically (readiness review).**
    Every entrypoint, including one-shot `allbert ask`, durably records the
    pending disclosure, renders/acknowledges it, and only then admits hosted
    inference. A crash before acknowledgement causes disclosure to repeat,
    not egress to occur first; the durable marker is consumed only after
    render acknowledgement. Local inference does not require an egress ack.
20. **Fallback classification is adapter-owned (readiness review).** M5
    records a table mapping concrete ReqLLM/provider/streaming results to
    `definitive | ambiguous | partial`; unknown shapes default to `partial`
    (no retry). Provider adapters normalize errors before the executor decides;
    the executor never parses exception strings.
21. **Budgets are fixed at M0 (readiness review).** M0 records numerical
    cross-platform cold/warm boot-to-ready, time-to-first-token, peak-memory,
    and decide-turn regression thresholds before M1. M6 proves those fixed
    thresholds; it may not choose a favorable bound after implementation.
22. **Curated-model change is evidence-gated (operator confirmed
    2026-07-26).** The candidate set includes `llama3.2:3b` (control), Qwen 3
    4B instruct/non-thinking, `qwen3.5:4b` (direct successor), and
    `qwen3.5:9b` (higher-floor quality challenger). Exact tags/digests are
    reverified at M0; first-run trials force non-thinking behavior while
    thinking variants remain catalog options.
    Thinking variants are catalog options, not the zero-click default. Test
    macOS arm64, Linux x64/arm64, and WSL2 where the release supports them;
    record download bytes/time, cold/warm TTFT, tokens/sec, peak RSS, direct-
    answer quality, tool/JSON conformance, malformed-call rate, multilingual
    behavior, context behavior, license, and exact public Ollama tag/digest.
    Promotion requires material Allbert-suite improvement without materially
    regressing first-answer latency. Raising the current 8 GB floor is allowed
    when the quality/tool-reliability gain is recorded and the operator accepts
    the reach tradeoff. A raised floor must produce an explicit consumer /
    prosumer disposition: either retain a lower-floor consumer curated default
    and recommend the winner as the prosumer profile, or promote the winner and
    prove the existing `below_floor` BYOK/repair path on excluded machines. No
    machine is silently reclassified as ready. Changing the default or tier
    updates every schema/catalog/doc/test occurrence as one M4 commit.
23. **Competitive bar, not parity scope (readiness review, 2026-07-26).** Hermes sets
    the bar for model choice, learned skills/memory, and delegated work;
    OpenClaw for gateway/channel activation and remote availability; IronClaw
    for packaged security/privacy positioning. v1.2 answers only the shared
    activation gap. Allbert differentiates through Security Central,
    registered actions, durable Objectives, audited settings, and one runtime
    spine; no autonomous skill/memory expansion or new channel scope enters
    this release.

## Operator-Visible Win

Install Allbert, open it, and ask a question — it answers. No wizard, no
toggle, no "Start QuickStart". If you already run Ollama, it finds it; if
you configured a provider key, it uses that (and says so, once, with the
revert command). If you have nothing, the chat still opens, answers
honestly, and shows exactly one button that fixes it. Want to customize?
Open onboarding anytime, jump straight to any step. Browse models in a real
catalog instead of typing tags — and if you opt in, Allbert survives a dead
provider by falling back down a chain you configured, never silently
falling into a paid one.

## Competitive Readiness Bar (research refreshed 2026-07-26)

- **Hermes Agent:** one-command/desktop activation, `hermes model`, broad
  provider choice, delegated subagents, scheduled work, and a visible learning
  loop across bounded memory and agent-managed skills. Source:
  <https://hermes-agent.nousresearch.com/docs/>.
- **OpenClaw:** a single self-hosted gateway connecting many chat channels to
  sessions, memory, agents, web control, and mobile nodes; its product promise
  is reachability from where the operator already chats. Source:
  <https://docs.openclaw.ai/>.
- **IronClaw:** packaged cross-platform Agent OS positioning centered on
  privacy, encrypted local credentials, onboarding, model status, and
  extensibility. Source: <https://github.com/nearai/ironclaw> and
  <https://docs.ironclaw.com/>.

The 1.2 answer is narrower and testable: Allbert removes the setup toll before
first value while retaining a stronger explicit-authority story. Success is
not feature-count parity; it is a first useful answer with fewer decisions,
honest repair when no model exists, and no weakening of the action/security
spine.

## Planned Interfaces

- `AllbertAssist.FirstRun.Enablement` (name final at M1) — the
  detection→enablement engine: consumes the existing
  `FirstRun.detect_details/1` chain, applies the ADR 0087 §1 ordering,
  performs the audited safe-write pair + profile selection when the consent
  key is absent, records provenance, and exposes
  `enablement_state/1 :: :auto_enabled | :sticky_disabled | :needs_model |
  :nothing_detected | :below_floor` for surfaces. Idempotent; no hosted
  egress; one atomic audited settings transaction.
- `Settings.Store.put_user_setting_if_absent/3` (name final at M1) — one
  StoreLock-owned raw-presence compare-and-write primitive; returns
  `:written | {:present, value}` and never treats a schema default as an
  operator value. Enablement uses it for the consent key and performs the
  paired assist/profile writes in the same audited transaction or rolls back.
- `FirstRun.UsableModel` (name final at M1) — deterministic compatibility
  predicate and local selection order shared first by detection and later by
  `Models.Catalog`; no catalog/UI dependency in M1.
- Disclosure surfaces: pending/acknowledged marker helper (Home marker file, per
  surface) + renderers — web chat preface, TUI banner line, CLI first-run
  paragraph. Text derives from the audit provenance record. Hosted paths use
  a durable pending→acknowledged state machine: transport admission requires
  acknowledged disclosure; crashes fail closed and may repeat disclosure.
- `AllbertAssist.Models.Catalog` — `list/1` merged rows (source, id,
  size, capabilities, floor, purposes, status), backed by the curated
  catalog file (`priv/model_catalog/*.yaml` or equivalent), Ollama tags,
  configured profiles + doctor status, and (conditional) `llm_db` hosted
  metadata. Registered read-only actions: `list_model_catalog`
  (`exposure: :internal`, `permission: :read_only`); `model_doctor`
  unchanged.
- Fallback: `Settings.Models.candidates_for/2` extended to text
  generation; a bounded failover executor inside the direct-answer path
  (classification: definitive advances, ambiguous advances once, possible
  partial-answer never silently retried); trace + audit events
  (`Runtime.Audit` existing kinds or an additive `:model_fallback` kind —
  M5 decides against the audit-writer precedent). Provider adapters emit a
  typed normalized result; unknown/partially streamed outcomes classify as
  `partial` and are never retried.
- New Settings Central keys (additive fragments, ADR 0031):
  `models.fallback.enabled` (false), `models.fallback.allow_local_to_hosted`
  (false), `models.fallback.max_failovers_per_turn` (1, clamp 1..2),
  `first_run.disclosure_shown.*` only if the marker cannot carry it (prefer
  the Home marker; Settings is not a per-surface flag store).
- Changed policy surfaces: `Workspace.FirstRun` auto-open states (auto-open
  only for repair states, never as a chat gate), `CLI.Tui.readiness_guard`
  inversion + parity for `mix allbert.tui`, chat first-run CTA states per
  the ADR 0087 §4 matrix.
- `mix allbert.test release.v12` — the release gate
  (`@release_v11_steps ++ @v12_focused_steps`).

## Code Seams

**S1 — detection→enablement core (M1, ADR 0087 §1–§2).** New enablement
module consuming `FirstRun.detect_details/1`; ordering configured-local →
localhost → hosted-key; StoreLock-owned atomic raw-presence check/write
(LD16, never effective/default-merged presence); M1-local usable-model
predicate and deterministic hosted-provider priority (LD17–LD18); audited writes of the
existing pair `intent.direct_answer_model_enabled` +
`intent.model_assist_enabled` plus `model_preferences.primary` when unset;
provenance into the audit trail. Wire at boot for web
(`application`/workspace mount path), TUI start, CLI dispatch — behind the
existing `ensure_http_started`/fresh-settings ordering. The two flagship
acceptance scenarios land here as automated regressions against a fresh
temporary Home: (a) provisioned fixture → first `submit_user_input` answer
is model-backed with zero writes beyond the audited pair; (b) empty fixture
→ deterministic fallback + repair CTA state exposed. `sticky_disabled`
proven red-first.

**S2 — surface unblocking + disclosure (M2, ADR 0087 §3–§4).** Invert
`CLI.Tui.readiness_guard` (always start; in-TUI guidance per detect state);
add the same posture to `mix allbert.tui`; change
`Workspace.FirstRun.@auto_open_states` semantics (repair-state auto-open
only; never gates chat); chat CTA per detect state
(`components/chat.ex:736-800` seam — one primary CTA, matrix-driven);
disclosure renderers (web preface, TUI banner via the adapter banner seam,
CLI paragraph) off the durable pending→acknowledged marker; hosted transport
admission is impossible before acknowledgement, including `allbert ask`;
`onboarding_complete` decoupling
(marker stays, consumers stop reading it for availability).

**S3 — step-addressable onboarding (M3, ADR 0087 §5).** Generalize
`wizard_rewind` into direct step entry at any time incl. post-completion
(web step list buttons already exist at `onboarding.ex:368-400` — extend to
completed state; CLI `onboard <step>` routes); re-entry/rewind never
re-gates (depends on S2's decoupling); ADR 0069 false-complete rule kept as
a permanent regression; persona/QuickStart flip sites unchanged and
idempotent against an already-auto-enabled Home.

**S4 — catalog + chooser (M4, ADR 0088 §1–§2).** `Models.Catalog` service +
curated catalog file + `list_model_catalog` action; the M4-entry
verification of `llm_db` (accuracy/footprint/license — recorded in Build
Progress either way, curated-file hosted section as the fallback); web
ModelsPanel upgraded to the chooser (browse by ADR 0072 purpose,
size/capability/floor + doctor status inline, select→existing settings
actions, pull→existing confirmation-gated pull with the live progress
plumbing); TUI/CLI equivalents on the same action; onboarding `model_path`
step re-pointed at the catalog rows.

**S5 — fallback/degradation (M5, ADR 0088 §3).** Extend `candidates_for/2`
consumption to the direct-answer path behind `models.fallback.enabled`;
typed failure normalization table per concrete provider/ReqLLM/streaming
shape (unknown→partial/no retry); the local→hosted
double gate; per-turn failover bound; trace + audit emission; reply
metadata naming a non-primary answerer; whole-chain-failed honest text.
ADR 0088 flips Accepted here (chooser proven at M4 + the egress-gate denial
row green at M5).

**S6 — eval rows + release gate (M7, LD13).** `:v12` enum entry FIRST
(`eval_inventory.ex:58`), then behavioral rows (request flow §G) with the
meta-contract sweep (`v12_sweep_eval_test.exs` mirroring
`v11_sweep_eval_test.exs:61-68` — exact ids, distinct assert sets,
production-boundary execution); gate wiring
`@release_v12_steps = @release_v11_steps ++ @v12_focused_steps` with
runner/evidence clone (`release_evidence/v12/`); docs-gate active-plan list
updated.

## Development Lane Annotation

- **Parallel workstreams:** WS-A (S1+S2 — the enablement spine and surface
  posture; serial within itself); WS-B (S4 catalog/chooser — independent of
  S1 until the onboarding `model_path` re-point); WS-C (S5 fallback —
  independent of S1/S4 until reply-metadata rendering). S3 rides after S2.
- **Serial barriers:** M0 before everything. S1 before S2 (surfaces render
  enablement states). S2 before S3 (decoupling first). S4 before S5's
  chooser-visible chain edits, but S5's executor can start against
  `model_preferences.tasks.*` directly. M7 after all feature milestones;
  M8 (dependency refresh) after churn settles; M9 last, tag operator-held.
- **Focused tests/gates:** per-milestone commands below; `release.v1` and
  `release.v11` after every contract-adjacent milestone (M1–M5);
  `inventory --check-tags` after every new test file (new suites take a
  primary lane; detection/enablement suites are `app_env_serial` unless
  their audit proves narrower; `onboarding_test.exs`'s missing lane tag is
  adjudicated at M0); no `mix | tail` pipes; every timing claim cites the
  metrics store.
- **Full-precommit timing:** once at the M7 barrier, again at M9 RC
  assembly.
- **External smokes:** none required for M1–M3 (local-only); M4/M5 require
  a live local Ollama spot-check (catalog live-inventory row + a real
  pulled-model selection) and — only if the operator has a configured
  hosted key — one live hosted-detection disclosure check. Provider
  credential failures record as BLOCKED, never converted to PASS.
- **Rejoin points:** after M2, an integrated fresh-Home matrix run (all
  detect states × web/TUI/CLI) before M3; docs/criteria reconciliation at
  M9 before tagging; triad archive + roadmap turnover only after published
  artifact validation.

### Focused command matrix

Commands run from the owner app directory (root `mix test` does not accept
`apps/…` paths). Every run uses an owned temporary Allbert Home.

| Milestone | Primary lane | Binding focused command |
|---|---|---|
| M0 | mixed | `MIX_ENV=test mix allbert.test metrics && cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/cli/first_run_test.exs test/allbert_assist/onboarding_test.exs` (record baselines; adjudicate onboarding lane) |
| M1 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/first_run/enablement_test.exs test/allbert_assist/cli/first_run_test.exs test/allbert_assist/onboarding_test.exs` |
| M2 | `app_env_serial` + `liveview_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/cli/tui_guard_test.exs ../../plugins/allbert.tui/test && cd ../allbert_assist_web && MIX_ENV=test mix test test/allbert_assist_web/workspace/first_run_test.exs test/allbert_assist_web/live/workspace/workspace_onboarding_test.exs` |
| M3 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/onboarding_test.exs test/allbert_assist/cli/areas/onboarding_test.exs && cd ../allbert_assist_web && MIX_ENV=test mix test test/allbert_assist_web/live/workspace/workspace_onboarding_test.exs` |
| M4 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/models/catalog_test.exs test/allbert_assist/actions/models_catalog_action_test.exs && cd ../allbert_assist_web && MIX_ENV=test mix test test/allbert_assist_web/live/workspace/workspace_models_chooser_test.exs` |
| M5 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/settings/models_fallback_test.exs test/allbert_assist/actions/intent/direct_answer_test.exs` |
| M6 | mixed | integrated fresh-Home matrix suite + `MIX_ENV=test mix allbert.test fast-local` |
| M7 | `security_eval_serial` | `MIX_ENV=test mix allbert.test release.v12` (repo root, first roll target) |
| M8 | mixed | `MIX_ENV=test mix allbert.test release.v12 && MIX_ENV=test mix allbert.test release.v1` after every applied wave |
| M9 | mixed | `MIX_ENV=test mix precommit && MIX_ENV=test mix allbert.test release && MIX_ENV=test mix allbert.test release.v1 && MIX_ENV=test mix allbert.test release.v12` |

## Security Posture

This release moves a consent line and is planned like it:

- **The only relaxation is ADR 0087's**: detection of an already-provisioned
  provider enables direct answers without a wizard step. Compensating
  controls are structural: sticky explicit-false, audited provenance,
  one-time disclosure naming provider + egress class, strict local-first
  ordering, and zero provisioning/egress in detection itself. Gate-bound
  `:v12` rows enforce each (request flow §G).
- **Nothing else grants.** Catalog reads are `permission: :read_only`
  registered actions; chooser writes ride existing settings actions and
  safe-write keys; pulls stay confirmation-gated; fallback never crosses
  local→hosted without the double gate; `Security Central`, the vault
  model, and provider key custody are untouched.
- **No new egress class:** detection probes only loopback/configured-local
  endpoints; hosted "detection" is key presence; catalog hosted metadata is
  static; the only new egress possibility (hosted failover) is double-gated
  and audited.
- Secrets remain encrypted/redacted end-to-end; disclosure and audit text
  never include key material or raw endpoints (doctor envelope rules).

## Non-Goals

- Removing the wizard, the QuickStart/Advanced tracks, or the persona
  system — they persist as the customization surface.
- Removing or migrating legacy `intent.*model_profile` aliases (1.5/1.6
  migration-runner cluster; additive-only rule).
- OAuth-authenticated hosted providers (1.4/1.5 enabler train); the catalog
  lists what exists, it does not add provider auth.
- Bundling a model or model runtime into the artifact (ADR 0078 stands).
- Auto-pull, auto-install, or any zero-click provisioning.
- Multi-user/remote first-run semantics; mobile-ready web (rides 1.4).
- Per-role fast/capable/thinking profile schema (v1.4, ADR 0090 scope).

## Milestones

Execution modes: `[gate]` gate-proved-contract, `[smoke]` scripted host
smoke, `[browser]` real-browser manual, `[model]` real-egress/local-model
manual, `[fv]` focused operator validation row.

- **M0: Recon verify + readiness decisions + baseline.** `[gate]` Start from shipped stable
  v1.1.0. Re-verify every Current Code State anchor at HEAD (drift table in
  Build Progress); record the fresh-Home behavior matrix per surface as the
  before-state; record decide-turn / boot walls from the metrics store;
  adjudicate the `onboarding_test.exs` lane; verify `Notify`-style fragment
  registration is not needed (consent keys already exist); confirm
  `llm_db` version shipped in the lock and stage the M4 verification
  questions; record LD16's atomic raw-presence API contract, LD17's usable-
  model predicate or curated-only disposition, LD18's stable hosted priority,
  and LD21's numerical budgets. Define the cross-platform model-bakeoff
  protocol from LD22 with exact supported host rows, 8/16/32 GB cohorts, and
  promotion/tier thresholds;
  verify public Ollama tags/digests and licenses at execution time. *Acceptance:*
  anchor drift table + baseline matrix + lane adjudication + five readiness
  decisions committed before M1; no curated default changes in M0.
- **M1: Detection→enablement core (S1, ADR 0087 §1–§2).** `[gate]`
  Enablement module with strict ordering, absent-key-only writes, audited
  provenance, sticky-false red-first, idempotency (double boot, wizard
  re-run, persona apply on an auto-enabled Home); the two flagship
  acceptance scenarios as automated fresh-Home regressions; boot ordering
  hazards covered (`:req` not started → detection defers without crash;
  fresh-process settings read). *Acceptance:* scenarios green; sticky-false,
  concurrent explicit-disable race, and no-egress proofs green; no
  non-additive migration; `release.v1` +
  `release.v11` green.
- **M2: Surface unblocking + disclosure (S2, ADR 0087 §3–§4).** `[gate]`
  TUI guard inversion + dev/packaged parity; web auto-open policy change;
  chat CTA matrix; disclosure on all three surfaces from the one-time
  marker, text derived from provenance; `onboarding_complete` decoupled.
  *Acceptance:* detect-state × surface matrix suite green; disclosure
  once-only proof (restart does not repeat); rewind no longer re-gates;
  `release.v1` + `release.v11` green.
- **M3: Step-addressable onboarding (S3, ADR 0087 §5).** `[gate]` Direct
  step entry any time incl. post-completion on web + CLI; ADR 0069
  false-complete permanent regression retained; flip-site idempotency.
  *Acceptance:* focused suites green; wizard completion still requires the
  go-signal; no consumer of `onboarding_complete` gates availability.
- **M4: Model catalog + chooser (S4, ADR 0088 §1–§2).** `[gate]`+`[model]`
  Catalog service + curated file + `list_model_catalog`; the recorded
  `llm_db` verification decision; web chooser + TUI/CLI lists + onboarding
  `model_path` re-point; selection/pull only through existing actions.
  Live local Ollama spot-check (inventory row appears; selection of a real
  pulled model answers). Execute the LD22 bakeoff on macOS arm64, Linux
  x64/arm64, and WSL2 supported rows; retain `llama3.2:3b` unless the fixed
  promotion/tier rule passes, otherwise change every default/catalog/doc/test
  reference atomically. Any floor increase includes the operator disposition
  and a real below-floor acceptance row. *Acceptance:* catalog merge suite green (all four
  sources incl. absent-source degradation); chooser write-path proof (no
  writes outside Settings actions); pull remains confirmation-gated;
  `release.v1` + `release.v11` green.
- **M5: Fallback/degradation policy (S5, ADR 0088 §3).** `[gate]`
  Selection-ordering extension to text; opt-in failover with
  classification, per-turn bound, local→hosted double gate red-first,
  trace+audit emission, honest whole-chain-failed text, reply metadata on
  non-primary answers. ADR 0088 flips Accepted. *Acceptance:* default-off
  proof (provider failure with keys absent behaves exactly as today);
  egress-gate denial row green; audit rows verified; image/voice callers
  unchanged.
- **M6: Cross-surface acceptance + performance guard.** `[gate]`+`[browser]`
  Integrated fresh-Home matrix (all §4 detect states × web/TUI/CLI) as one
  suite; measured detection budget vs the M0 baselines (boot-to-ready delta
  and unchanged decide-turn, cited from the metrics store) against the fixed
  M0 numerical thresholds; selected curated-model cold/warm TTFT and peak-RSS
  rows remain inside the M0 hardware floor; an in-app
  browser walkthrough of the zero-click and empty-state paths on an
  isolated Home. *Acceptance:* matrix green; budgets met and recorded;
  walkthrough logged with zero console errors.
- **M7: Eval rows + release.v12 gate (S6).** `[gate]` `:v12` enum FIRST;
  behavioral rows per request flow §G with the meta-contract sweep;
  `@release_v12_steps = @release_v11_steps ++ @v12_focused_steps` with
  evidence under `release_evidence/v12/`; docs-gate active-plan list
  updated. *Acceptance:* `release.v12` green first roll target; every new
  authority/deny path bound to a gate step; sweep meta-contract enforces
  distinct assert sets and production-boundary execution.
- **M8: Dependency refresh (standing rule).** `[gate]` Three-root
  `hex.outdated` review table, dedupe, bounded applies in own commits,
  dispositions recorded (named carry-items: `req_llm` line, the
  PLV `~> 1.1.0` pin, `llm_db` if adopted at M4). *Acceptance:* table
  complete; scoped suites + `release.v12` + `release.v1` green after every
  wave; M7 evidence superseded by the post-refresh run.
- **M9: Docs, RC, focused validation, binary release + closeout.**
  `[gate]`+`[smoke]`+`[browser]`+`[model]`+`[fv]` Purpose-bar check against
  every outcome (deferrals need operator sign-off); DIT-2-class criteria
  replaced across `docs/validation/` and operator guides; CHANGELOG
  `## v1.2.0` + version lockstep (three `mix.exs`, SW `CACHE_NAME` + gzip);
  `mix precommit` + authoritative `mix allbert.test release` + `release.v1`
  + `release.v12` at the RC SHA; **one uninterrupted authoritative gate at
  the pushed candidate SHA before FV opens**; FV-01..FV-04 per request flow
  §H executed and recorded; operator-held annotated tag `v1.2.0` (no
  `[skip-artifacts]`); CI publish + cosign; tap fill; install rehearsal;
  post-tag archive/roadmap administration. *Acceptance:* Definition of Done
  complete; ledger recorded; triad archived only after published-artifact
  validation.

## Validation Design (the v1.1 churn guardrails, binding)

The v1.1 M12.4–M12.13 corrective chain is the negative example this plan is
built against. Binding rules, each carried by a named milestone above:

1. **No validation-only product capability** (M12.11): nothing ships in
   `lib/` whose sole purpose is producing validation evidence. FV rows use
   the product as an operator would.
2. **The flagship scenarios are automated first** (M12.12): zero-click and
   empty-state land as fresh-Home regressions in M1/M2; FV re-executes what
   CI already proves, live.
3. **Behavioral eval rows with the meta-contract** (M12.5): no structural
   presence checks, no source-grep proofs, no shared assert triples.
4. **Capability wiring is census-proved, not grep-inferred** (M12.6): the
   surface matrix suite enumerates web/TUI-packaged/TUI-dev/CLI explicitly.
5. **Disposable Homes only** (M12.9): no FV row touches `~/.allbert`; no
   cloned state; runbooks are paste-executable without `set -e`/`exit`.
6. **FV opens once, after one green uninterrupted authoritative gate at the
   pushed candidate SHA**; the corrective loop is fixed: fix → focused →
   `mix precommit` → `release.v1`/`release.v12` → authoritative gate →
   reopen FV. No renumbering, no ad-hoc rows.

## Documentation Deliverables

- ADR 0087, ADR 0088 (Proposed → Accepted at their proving milestones);
  ADR 0078/0069 amendments already recorded at planning time.
- `docs/plans/v1.2-request-flow.md` maintained as implementation changes.
- Operator docs: first-run/onboarding guide rewritten for the zero-click
  posture; model catalog/chooser guide; fallback policy reference
  (settings, egress gate, audit locations); `model-recommendations.md`
  (ADR 0072) cross-linked to the catalog.
- `docs/validation/`: DIT-2-class criteria replaced per LD14.
- Developer docs: test-strategy lane additions for the new suites;
  detection/enablement module docs.
- `CHANGELOG.md` `## v1.2.0`; roadmap ladder + future-features lifecycle
  updates at closeout (operator-owned).
- Triad archived to `docs/plans/archives/` after artifact validation.

## Definition Of Done

- Detection→enablement shipped with strict local-first ordering,
  absent-key-only writes, audited provenance, sticky explicit-false, and
  zero provisioning/hosted egress — `[gate-proved-contract]`.
- All surfaces chat-ready per the ADR 0087 §4 matrix; TUI guard inverted
  with dev/packaged parity; disclosure once-only on web/TUI/CLI —
  `[gate-proved-contract]` + `[browser]`.
- Onboarding optional + step-addressable; completion decoupled from
  availability; ADR 0069 false-complete regression retained —
  `[gate-proved-contract]`.
- Catalog + chooser live on all surfaces through read-only actions;
  selection/pull only via existing settings/confirmation paths; `llm_db`
  verification recorded — `[gate-proved-contract]` + `[model]`.
- Fallback policy: default-off proof, egress double gate, per-turn bound,
  audit trail, honest degradation floor — `[gate-proved-contract]`.
- ADR 0087 and ADR 0088 Accepted; DIT-2-class criteria replaced —
  docs + `[gate-proved-contract]`.
- `:v12` behavioral rows + meta-contract sweep green;
  `mix allbert.test release`, `release.v1`, `release.v12` green at the
  tagged commit — `[gate-proved-contract]`.
- FV-01..FV-04 executed on disposable Homes at the candidate SHA and
  recorded — `[operator-attested]`.
- v1.2.0 published as a binary release: cosign-signed GitHub Latest, tap
  filled, install rehearsal recorded — `[operator-attested-smoke]`.
- Dependency disposition table complete; docs deliverables + closeout
  administration complete.
