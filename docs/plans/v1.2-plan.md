# Allbert v1.2.0 Zero-Click First Run

Request flow: `docs/plans/v1.2-request-flow.md`
ADRs: two new ADRs ship with this release — ADR 0087 (zero-click first run
and detection-based enablement — the consent ADR, binding on M1a–M3) and
ADR 0088 (model catalog, chooser, and fallback/degradation policy — binding
on M4/M5). ADR 0078 (First-Model Path) and ADR 0069 (Guided Onboarding)
carry v1.2 amendments recorded 2026-07-24 — their substance stands; only the
enablement point and the gate direction move. ADR 0016 (channel adapter
boundary and identity mapping) and ADR 0067 (TUI terminal channel) carry the
2026-07-27 LD26 launcher amendment. Consumes ADR 0072
(recommendation matrix + `model_doctor`), ADR 0061 (router model tiers),
ADR 0051 (`model_preferences.*` authority shape), ADR 0047 (doctor
envelope), ADR 0075 (personas), ADR 0031 (Settings Central), ADR 0006
(Security Central), and the v1.0 public-contract freeze (`release.v1` stays
green).

Status: **implementation complete through M8; M9 corrective operator FV is
complete on both required hosts. M9.1 operator-documentation convergence was
accepted by the operator on 2026-07-27 at pushed commit `e8c9ae84`; the final
release barrier is admitted. Attempts 1--5 each exposed and remediated one
bounded static or stale contract defect. Attempt 5 passed precommit,
`release.v1`, corrected `release.v12`, and the aggregate through high coverage,
external runtime, and security before one pre-v1.2 web proof still expected the
retired first-model suggestion card. Its focused test-only correction is green
and must be pushed before attempt 6 starts. FV-01..FV-03 passed on macOS at their recorded
clean pushed candidates with zero non-doc drift through current clean pushed
candidate `416f753f74417611f5e9385d20d656fc43326b56`; all three Linux rows passed
directly at that candidate, and Serenity's ordinary Ollama service was restored
after the isolated rows. The one complete post-FV sequence -- precommit,
`release.v1`, corrected `release.v12`, and uninterrupted aggregate release gate
-- remains pending.** v1.1.0
is shipped and its triad is archived (stable closeout HEAD `e7d8ec7a`,
2026-07-26), so the predecessor barrier is satisfied. BUILD MUST start with
M0 and may not enter M1a until the raw-setting presence contract, usable-model
selection rule, hosted-provider priority, cross-platform curated-model bakeoff
protocol, and numerical performance budgets are recorded in Build Progress.
M0 re-verifies every Current Code State anchor at build-start HEAD.

Source: roadmap ladder item 6 (1.2 flagship) and the future-features
entries: Zero-Click First Run (`future-features.md:542-583`, Must/L,
flagship), Model Chooser / Catalog (`:922-932`, enabler), Model
Fallback / Degradation Policy (`:864-884`, enabler — required by the
zero-click detect states). Operator decisions of 2026-07-24 locked the
consent posture (auto-enable on any detected configured provider), the
additive-only migration posture for 1.2–1.4, and the flagship-focused
validation contract (Locked Decisions below).

Third implementation-readiness pass 2026-07-26 (post-v1.1 closeout): anchors
re-verified at stable HEAD; the detect-state derivation table (request flow
§D.0), the multi-key raw-presence primitive (LD16), per-key stickiness across
the whole write set (LD4), and the hosted-vs-repair split (LD24) were added
because M1a/M1b could not have been executed unambiguously without them.

Implementation-readiness review 2026-07-26: the flagship is unambiguously
**Zero-Click First Run — install, open, chat**. Catalog/chooser, fallback, and
step-addressable onboarding are direct enablers, not co-flagships. The review
compared Hermes Agent, OpenClaw, and IronClaw and added the M0/M4 competitive
and local-model evidence work below; no competitor feature is imported merely
for parity. The current `llama3.2:3b` default stays unchanged until the
cross-platform bakeoff earns a replacement.

## Build Progress

### M0 — recon verify + baseline (2026-07-26)

- **Build anchor and drift.** Implementation starts from pushed
  `a4ae7ed3` on the v1.1.0 one-connection/WAL/`:immediate` topology. Every
  Current Code State symbol/path anchor resolves at HEAD. The only actionable
  drift was in `@docs_active_plan_files`: two archived v1.1 paths remained;
  M0 removed them. Line-only movement was accepted without changing scope.
  `classify_model_probe/3` still projects
  `model_ready → local_ready`, `model_missing → model_missing` above the
  floor (otherwise `below_hardware_floor`), `unhealthy → runtime_unhealthy`,
  and missing runtime to `byok_ready | runtime_missing`; request-flow §D.0
  is therefore the binding M1b replacement rather than a description of
  behavior already shipped.
- **Before-state matrix.** Web workspace, `allbert ask`, and bare `allbert`
  reach chat but return the deterministic `model_disabled` answer because
  the merged consent default is false. Packaged `allbert tui` refuses every
  state except `product_ready`; dev `mix allbert.tui` starts without that
  guard. A fresh Home with local ready, local missing-model, unhealthy local,
  below-floor local, hosted-key-only, or nothing configured does not
  atomically select and enable a usable profile on any surface. This is the
  M1b/M2 before-state; operator validation is not used to prove its repair.
- **Lane adjudication.** `onboarding_test.exs` owns primary lane `db_serial`:
  it uses `AllbertAssist.DataCase, async: false`, whose test-strategy default
  is `db_serial`. No extra module tag is required. `first_run_test.exs`
  remains `app_env_serial`.
- **M1a contracts.** `Store.put_user_settings_if_absent/2` takes the closed
  three-key map, acquires `StoreLock` once, reads raw user settings once,
  validates the merged absent subset once, persists once, emits one audit
  row per applied key and one transaction envelope with applied/preserved
  keys, and rolls back all writes/audits on failure. The usable-local
  predicate is a doctor-healthy configured text profile, else the exact
  curated pulled tag. M1a confirmed the originally considered arbitrary-
  pulled-model fallback cannot select atomically without also creating a
  profile outside LD16's closed three-key write set, so the readiness
  review's explicit curated-only disposition applies until M4 owns catalog
  profile creation/selection. Hosted priority is explicit
  `model_preferences.primary`, then configured text-task order, then the
  stable order `openai, anthropic, google, openrouter`; unknown providers
  sort lexically after that list. Presence is configured-but-unverified.
  A later corrective invariant binds this three-key decision atomically: if
  consent became explicit `false` or a different primary appeared before the
  Store lock, no absent key is applied.
- **Performance baseline and fixed budgets.** Fresh `bench-decide`
  (`decide-v1`, 30 turns) at the build anchor recorded **446.2 ms mean,
  359.3 ms p50, 614.9 ms max**; M6 passes at mean ≤490.8 ms and p50
  ≤395.2 ms (10% bounds), with no detection call on a turn stack. The latest
  stable clean prepush high-coverage record is 971 s / 2,422 tests; release
  lane walls are core 378 s / 2,088, external 700 s / 617, security 363 s /
  362, web 2,363 s / 316, StockSage 152 s / 198, plugins 8 s / 35, and
  Dialyzer 13 s. Boot-to-ready gets a dedicated disposable-Home measure in
  M6 because the metrics schema has no semantic boot-to-ready row today:
  five cold and ten warm samples per supported primary host, with p50
  **≤1,000 ms cold and ≤250 ms warm**, and detection's incremental p50
  **≤100 ms** over a no-detection control. Curated-model TTFT must be
  **≤2.0 s warm and ≤6.0 s cold** at the supported hardware floor; peak RSS
  must stay **≤75% of physical RAM**. A candidate may replace the control
  only with ≥15% aggregate quality/tool-conformance improvement, malformed
  calls no worse than control, and TTFT/tokens-per-second no worse by >10%.
- **Bakeoff protocol frozen.** Required rows are macOS arm64 and Linux x64;
  Linux arm64 and WSL2 are supported rows that may be `BLOCKED`. Each runs
  8/16/32 GB cohorts where hardware exists, three cold pulls/runs and ten
  warm runs, the same direct-answer/tool/JSON/multilingual/context corpus,
  and records exact tag/digest, bytes/time, TTFT, tokens/s, RSS, quality,
  malformed-call rate, and license. Public candidates verified at M0 are
  `llama3.2:3b` (`a80c4f17acd5`, 2.0 GB, Llama 3.2 Community License),
  `qwen3:4b-instruct` (`0edcdef34593`, 2.5 GB, Apache-2.0),
  `qwen3.5:4b` (`2a654d98e6fb`, 3.4 GB, Apache-2.0), and
  `qwen3.5:9b` (`6488c96fa5fa`, 6.6 GB, Apache-2.0). Qwen first-run trials
  disable thinking; thinking remains catalog-only. Digests are evidence
  pins, not permanent floating-tag assumptions, and are rechecked by M4.
- **Validation.** Metrics summary regenerated (4,468 records before this
  milestone's new rows). Focused first-run/onboarding baseline: 43 tests,
  0 failures, seed 583525, 15.4 s; post-remediation rerun: 43 tests,
  0 failures, seed 911507, 15.1 s. `mix allbert.test docs` and
  `git diff --check` passed. No curated default changed and no scope was
  deferred.

### M1a — atomic enablement spine (2026-07-26)

- **Implemented.** `Settings.Store.put_user_settings_if_absent/2` now owns
  the closed three-key compare-and-write. It holds one non-reentrant
  `StoreLock`, pins one fragment composition, reads raw settings once,
  partitions absent versus present values, validates the whole prospective
  merge, and writes the absent subset once. Explicit `false` is preserved;
  a second invocation is a no-op. Successful audited writes append exactly
  one row per applied key and one `settings.transaction` provenance envelope
  listing applied and preserved keys. Validation failure writes no setting
  or audit record.
- **Selector.** `FirstRun.UsableModel` is local-preferred and side-effect
  free: configured local profiles require a healthy Model Doctor result;
  the bounded fallback is the exact curated pulled tag; hosted candidates
  use secret presence only and are marked `configured_unverified`. Ordering
  is explicit primary, direct-answer task order, then stable provider order
  `openai, anthropic, google, openrouter`, with unknown providers lexical and
  last. It performs no hosted probe or inference.
- **Drift and remediation.** The first `release.v1` pass found two new strict-
  Credo findings (one excess nesting level and one unaliased nested test
  module); both were remediated before the gate was rerun. M1a also resolved
  LD17 to curated-only: arbitrary pulled tags cannot become
  `model_preferences.primary` without creating a profile, which is outside
  the atomic primitive's closed write set. No schema/migration or public
  contract changed.
- **Evidence.** Focused selector/store suite: 9 tests, 0 failures, including
  ten concurrent auto-enable/explicit-disable races. Post-remediation
  `release.v1` passed (strict Credo clean, Dialyzer 0 findings, frozen/product
  sweeps 31 tests); `release.v11` passed (authority 13, fan-out 249, SQLite
  topology 5, channel authority 79, web operator 32; all zero failures).
  `git diff --check` passed.

### M1b — detection projection + flagship core (2026-07-26)

- **Implemented.** `FirstRun.Enablement` projects all six existing model
  states against hosted presence, local-preferred and availability-first.
  Raw consent `false` short-circuits to `sticky_disabled`; consent `true`
  with no usable selection projects `enabled_unavailable`; absent consent
  invokes M1a's one atomic write. A temporary boot Task runs after Settings
  supervision in non-test environments and performs the probe/write in a
  fresh process. If `:req` is not started, boot reconciliation returns
  `{:deferred, :req_not_started}` without a write or crash.
  Automatic ordering applies only when the primary is raw-absent. A usable
  raw-explicit primary is binding; an unavailable exact primary projects
  `enabled_unavailable` rather than enabling or disclosing another route.
- **Flagship core.** Fresh-Home regressions now prove the first direct answer
  is model-backed for both a ready local profile and an already-configured
  hosted profile, with no prior enablement click. The nothing-provisioned
  row remains the bounded honest fallback. Hosted presence assembly does not
  call Model Doctor or a hosted endpoint. Audit rows now render the
  `enabled_by`, profile, provider, and provider-class provenance that drove
  the write.
- **Drift and remediation.** The plan's phrase “projection landed in
  `classify_model_probe/3`” was structurally impossible without collapsing
  the existing six-state API: that classifier has no hosted-presence axis.
  The tested projection therefore lands immediately beside it in
  `FirstRun.Enablement`, preserving the six states and implementing the
  binding twelve-cell §D.0 table. The hosted flagship test then exposed a
  real integration defect: the legacy direct-answer preference list
  shadowed a newly selected hosted primary with default `local`. Models now
  prioritizes `model_preferences.primary` for direct answers only when that
  key is raw-explicit; schema-default behavior and its diagnostics are
  unchanged. The first release-gate pass also found three static issues
  (two alias-order/reference findings and an incomplete Dialyzer return
  spec); all were remediated and the full gates rerun.
- **Evidence.** Post-remediation focused suite: 43 tests, 0 failures, seed
  895278, including all twelve cells, sticky/unavailable states, idempotency,
  no-egress and boot-defer proofs, legacy model-preference behavior, and the
  three flagship core rows. `release.v1` passed with strict Credo clean and
  Dialyzer 0 findings. `release.v11` passed: authority 13, fan-out 249,
  SQLite topology 5, channel authority 79, web operator 32; zero failures.
  No migration was added and no scope was deferred.

### M2 — surface unblocking + disclosure (2026-07-26)

- **Implemented.** Packaged and development TUI entry now share a nonblocking
  readiness policy; unavailable models produce guidance while chat remains
  usable. Web no longer auto-opens onboarding merely because onboarding or
  profile setup is incomplete, and the detect-state presentation matrix owns
  one primary repair action across web, TUI, and CLI. A durable per-surface
  disclosure marker is written before auto-enablement: TUI and CLI acknowledge
  only after a successful output write, while web requires the exact random
  delivery handle mounted with the disclosure before hosted transport is
  admitted. Failed, absent, stale, or forged acknowledgement remains pending.
- **Drift and remediation.** Fail-closed ordering was made stricter than the
  prose diagram: the pending marker is persisted before the consent/settings
  write so a crash cannot enable hosted egress without a disclosure obligation.
  The first static gate found over-broad Dialyzer contracts in the new
  disclosure and presentation modules; their state and selection types were
  narrowed. The first `release.v11` pass then found that adding a third TUI
  banner output displaced a frozen channel test's confirmation event. The
  guidance now extends the existing second banner line, preserving the public
  output-event shape without hiding the repair action.
- **Evidence.** Core disclosure/presentation/enablement suite: 18 tests, 0
  failures, seed 862806. Web first-run/onboarding suite: 15 tests, 0 failures;
  TUI/disclosure focused suite: 7 tests, 0 failures, seed 52728. The exact web
  delivery-handle, write-failure persistence, once-only restart behavior,
  nonblocking TUI guard, and one-CTA matrix are covered. Post-remediation
  `release.v1` passed with strict Credo clean and Dialyzer 0 findings;
  `release.v11` passed authority 13, fan-out 249, SQLite topology 5, channel
  authority 79, and web operator 32 with zero failures. No migration or scope
  deferral was introduced.

### M3 — step-addressable onboarding (2026-07-26)

- **Implemented.** `wizard_enter/2` opens any of the eight canonical steps,
  including an off-track step and any step after completion, without truncating
  completed history or clearing `onboarding_complete`. Web renders direct-entry
  controls for every step and CLI accepts `allbert onboard STEP`. Rewind remains
  the distinct progress-editing operation. Wizard completion now depends on a
  genuinely ready model path alone, not the direct-answer consent key.
- **Sticky-disable UX.** Advancing `model_path` preserves a raw explicit `false`,
  records the decline, and offers one durable re-enable prompt. CLI names the
  explicit `re-enable-model` command; web provides the equivalent button. The
  offer is claimed only by a connected web render, remains visible for that
  component mount, and does not recur after restart. Re-enable uses audited
  Settings Central writes for both intent keys.
- **Drift and remediation.** Focused tests first exposed two routing/lifecycle
  assumptions: the generic CLI step route shadowed single-word repair commands,
  and disconnected LiveView rendering consumed the durable offer before the
  connected UI could show it. Specific command clauses now retain precedence,
  and only the connected component claims the offer. An older web assertion that
  consent gated the readiness signal was updated to the accepted ADR 0087
  semantics. Source census confirmed remaining `onboarding_complete` reads
  select wizard/progress presentation only; chat and TUI availability no longer
  depend on them.
- **Evidence.** Post-remediation focused core/CLI suite: 51 tests, 0 failures,
  seed 356995; focused web onboarding/first-run suite: 17 tests, 0 failures,
  same seed. The exact connected-render regression passed alone, seed 32258.
  `release.v1` passed with strict Credo clean and Dialyzer 0 findings;
  `release.v11` passed authority 13, fan-out 249, SQLite topology 5, channel
  authority 79, and web operator 32 with zero failures. No migration or scope
  deferral was introduced.

### M4 — model catalog + chooser (2026-07-26)

- **Implemented.** `Models.Catalog` merges a versioned shipped catalog,
  one bounded localhost Ollama inventory read, configured model profiles, and
  the shipped hosted-metadata fallback into a common redacted row shape.
  `list_model_catalog` is a registered read-only action with purpose filtering;
  listing is proven write-free. Web Models and onboarding `model_path`, CLI
  `allbert admin models catalog [PURPOSE]`, and TUI `/catalog` render that action.
  Configured-profile selection dispatches the existing
  `set_active_model_profile` action; unpulled local rows remain descriptive and
  retain the existing confirmation-gated pull path.
- **`llm_db` disposition.** The transitive 2026.7.2 snapshot is Apache-2.0 and
  current, but is 6.1 MB and direct invocation is outside Allbert's declared
  dependency contract (Dialyzer reports the call unknown). Per ADR 0088's
  conditional rule, M4 rejected direct coupling and uses the small shipped
  hosted section. No new dependency was added.
- **Qwen bakeoff and default decision.** Real macOS arm64 Ollama rows used exact
  pulled digests from M0 and 12 warm runs per model over JSON, tool-shaped JSON,
  Spanish, and context-recall prompts with thinking disabled. Control
  `llama3.2:3b`: 8/12 (66.7%), 740.6 ms mean, 144.5 tok/s. Qwen 3 4B Instruct:
  12/12 (100%), 811.4 ms, 121.0 tok/s. Qwen 3.5 4B: 10/12 (83.3%), 956.5 ms,
  106.2 tok/s. Qwen 3.5 9B: 8/12 (66.7%), 956.8 ms, 74.9 tok/s. Qwen 3 4B
  clears the quality threshold and stays within the 10% latency bound, but its
  16.2% throughput regression breaches the frozen 10% promotion limit.
  Therefore `llama3.2:3b` remains the curated default and the 8 GB floor does
  not rise. Linux x64 is `BLOCKED` by operator disposition allowing one primary
  row to be blocked; Linux arm64 and WSL2 are also `BLOCKED` because those hosts
  are unavailable. No cross-platform promotion is possible.
- **External smoke.** A disposable Home at
  `/private/tmp/allbert-v12-m4-smoke` listed the real pulled Llama and Qwen rows,
  selected `local` through the audited Settings action, enabled direct answers
  through Settings Central, and answered `391` through `direct_answer`; trace
  `2026-07-27T02-05-29Z-completed-direct-answer-019fa151-e202-771a-ac35--10306.md`
  was emitted under that disposable Home.
- **Drift and remediation.** Focused testing found a map-normalization pipeline
  applied per tuple, optional catalog keys omitted from hosted rows, and
  pre-start web onboarding lacking direct step buttons. Those were remediated
  with one common row shape and always-visible direct-entry controls. The first
  release gate additionally found non-stable HEEx whitespace and the rejected
  `llm_db` call; interpolated strings and the hosted fallback resolved both.
- **Evidence.** Post-remediation catalog/CLI/TUI suite: 6 tests, 0 failures,
  seed 483464; catalog-only rerun: 3 tests, 0 failures, seed 549799. Web
  onboarding/models suite: 14 tests, 0 failures, seed 683600; exact shared
  catalog row: 1 test, 0 failures, seed 403256. Lane reconciliation: 561 files,
  zero unclassified/double-counted. `release.v1` passed with strict Credo clean
  and Dialyzer 0 findings; `release.v11` passed authority 13, fan-out 249,
  SQLite topology 5, channel authority 79, and web operator 32 with zero
  failures. No migration or scope deferral was introduced.

### M5 — fallback/degradation policy (2026-07-26)

- **Implemented.** Text direct-answer resolution is local-first unless the
  operator explicitly selected a primary; remaining failover candidates are
  local-first. Runtime fallback is separately opt-in and remains default off.
  ReqLLM adapters normalize concrete request status/cause structs into
  definitive, ambiguous, or partial failures; unknown and possible-partial
  outcomes never retry. A turn makes at most one failover even when the
  additive setting is raised to its bounded maximum of two. Local-to-hosted
  fallback additionally requires `models.fallback.allow_local_to_hosted=true`.
- **Observability and degradation.** Successful non-primary answers name the
  failed and answering profiles, classification, provider-call count, and
  audit path in reply/trace metadata. Every decision appends a redacted row
  under `settings/audit/model_fallback/`; exhausted chains return deterministic
  text naming only the attempted profiles. `models.catalog.version=1` and the
  three bounded fallback settings now ship through Settings Central.
- **External smoke.** A disposable Home at
  `/private/tmp/allbert-v12-m5-smoke` used missing local model
  `allbert-missing-model-v12` followed by real pulled `llama3.2:3b`. The first
  call classified definitive, the second answered exactly `M5 fallback smoke
  passed`, and trace
  `2026-07-27T02-38-33Z-completed-direct-answer-019fa170-23ca-7a5a-b902--11074.md`
  recorded call count 2, failed `local`, answering `fast`, and the matching
  audit path. The audit row records `model_fallback.answered` with no prompt,
  endpoint, or secret.
- **Drift and remediation.** The first ordering implementation applied
  local-first to every capability and broke the established image retry chain.
  M5 narrowed the change to text `direct_answer`; image and voice retain their
  exact pre-M5 ordering. ADR 0088 now makes explicit-selected-primary the
  precedence exception required by M1's hosted selection contract. Current
  ReqLLM documentation and installed source confirmed HTTP 401/403/404 and
  wrapped transport-cause fields; the adapter matches structs, never strings.
- **Evidence.** Post-remediation fallback/direct-answer plus model/image/voice
  suite: 41 tests, 0 failures, seed 773763. The pre-remediation `release.v1`
  and `release.v11` gates were green but were discarded. Their final
  post-remediation reruns passed: `release.v1` had strict Credo clean,
  Dialyzer 0 findings, and 31 frozen/product tests green; `release.v11` had
  authority 13, fan-out 249, SQLite topology 5, channel authority 79, and web
  operator 32 green. ADR 0088 is now Accepted. No migration or scope deferral
  was introduced.

### M6 — cross-surface acceptance + performance guard (2026-07-26)

- **Integrated acceptance.** One fresh-Home regression now derives all twelve
  request-flow §D.0 cells plus `sticky_disabled` and `enabled_unavailable`,
  then asserts the common web/CLI/TUI presentation contract, web repair
  destination, and packaged/dev TUI guard parity. The development Mix task now
  executes the packaged launcher's non-gating readiness guard instead of
  maintaining a bypass.
- **Boot barrier and performance.** Measurement exposed the temporary M1b boot
  Task competing with the rest of application startup and delaying readiness.
  It was replaced by a synchronous, one-shot OTP child-init barrier, and boot
  detection now reuses its local selection rather than probing twice. Five
  cold disposable-Home reconciliation samples were 658.4, 504.2, 579.8,
  574.5, and 615.5 ms (579.8 ms p50, budget ≤1,000 ms). Ten warm samples had
  an 89.9 ms p50 (budget ≤250 ms and detection increment ≤100 ms).
  `bench-decide` recorded 439.9 ms mean and 354.5 ms p50, both better than the
  M0 446.2/359.3 ms baseline and inside its fixed 10% bounds.
- **Model/runtime measurements.** Real macOS arm64 Ollama `llama3.2:3b` TTFT
  was 2.844 s cold p50 over three runs and 0.201 s warm p50 over ten runs,
  inside the 6.0/2.0 s budgets. The model server used 16,768,560 KiB on a
  64-GiB host (about 24.4%, below the 75% ceiling). Linux x64 remains the
  operator-disposed `BLOCKED` primary row recorded in M4; Linux arm64 and WSL2
  remain unavailable, so M6 does not manufacture cross-platform evidence.
- **Browser and drift remediation.** An automated browser walkthrough against
  disposable real Homes proved a ready local model answers the neutral first
  question with zero setup clicks, while an unreachable configured local
  endpoint offers guided repair and returns the explicit side-effect-free
  unavailable-model response. Both paths had zero browser-console errors. The
  first `fast-local` roll found an M2 disclosure using raw slate classes and an
  M5 settings test classified in two serial lanes; the shared semantic callout
  class and one `app_env_serial` lane resolved both without changing behavior.
- **Evidence.** Warning-free compile and the focused first-run/cross-surface
  suite passed (20 core tests and 2 web tests, seed 488159). Focused drift
  regressions passed (8 core and 5 web tests, seed 373025). The final
  `fast-local` gate passed strict Credo and all eight partitions: core 234,
  signal 2, Discord 5, Matrix 2, Slack 5, StockSage 28, web 39, and WhatsApp 4,
  all with zero failures. No migration, public-contract change, default-model
  promotion, or scope deferral was introduced.

### M7 — eval rows + release.v12 gate (2026-07-26)

- **Behavioral bindings.** The eval milestone enum gained `:v12` before its
  original thirteen request-flow §G rows. LD26's M9 correction adds a
  fourteenth launcher/adapter identity-boundary row to the replacement gate;
  that corrected row count is not retroactive M7 evidence. `V12SweepEvalTest` executes the production
  Enablement, Settings Store, disclosure, registered chooser action,
  DirectAnswer fallback, and TUI guard boundaries. Its meta-contract enforces
  the exact id set, three assertions per row, distinct assertion sets, and the
  owning test module; every row calls `AssertBinding.check!/2` after its
  behavioral assertions.
- **Derived gate.** `mix allbert.test release.v12` is exactly the unchanged
  `@release_v11_steps` prefix plus four focused steps: v1.2 authority sweep,
  enablement/onboarding, catalog/fallback, and web first-run projections. Each
  step records normal test metrics and redacted JSON evidence under
  `release_evidence/v12/`. The help surface and active release documentation
  name the new command. The docs active-plan list already contained the v1.2
  plan/request-flow after M0's stale-v1.1 correction, so no further list edit
  was necessary.
- **First roll and drift.** The first authoritative roll passed without
  remediation: Hex audit, migration, formatting, warning-free forced compile,
  strict Credo, Dialyzer (0 findings), v1 freeze 12, v1 product 15, v1.1
  authority 13, fan-out 249, topology 5, channels 79, and web operator 32.
  The original new steps passed authority 13, enablement/onboarding 76,
  catalog/fallback 26, and web first-run 26 tests, all with zero failures.
  Evidence was emitted as `release-v12-1785123759.json` in the gate's
  disposable Home. The pre-gate focused task/eval suite passed 44 tests, seed
  359105. No schema, public-contract, dependency, or scope change was made.

### M8 — upstream dependency refresh (2026-07-26)

- **Three-root review and dispositions.** Live `hex.outdated` was run at the
  umbrella, core, and web roots, followed by an umbrella `--all` transitive
  review. The deduplicated result is:

  | Dependency | Before → after | Disposition |
  | --- | --- | --- |
  | Bandit | 1.12.1 → 1.12.3 | applied; bounded patch includes the two published HTTP/WebSocket security fixes |
  | Igniter | 0.8.2 → 0.8.3 | applied; bounded dev/test patch |
  | `llm_db` | 2026.7.2 → 2026.7.3 | applied as ReqLLM's transitive data snapshot; M4's no-direct-coupling decision is unchanged |
  | `ex_ast`, `glob_ex`, `zoi` | 0.13.0→0.13.1, 0.1.11→0.1.12, 0.18.6→0.18.7 | solver-selected compatible transitive patches |
  | IDNA | 6.1.1 → 7.1.0 | applied in a second wave; both JSV and Swoosh requirements explicitly admit 7.x |
  | ReqLLM | 1.17.1 | retained; current latest and `jido_ai ~> 1.12` is satisfied |
  | Phoenix LiveView | 1.1.28 (latest 1.2.7) | carried; update is outside the deliberate web `~> 1.1.0` pin and is a separate breaking-line migration |

  All other direct and transitive packages were current. Core/web duplicate
  Bandit rows resolve through the one umbrella lockfile and were applied once.
- **Scoped proof and compile drift.** Wave one passed 30 core
  model/security tests and 52 HTTP/public-protocol web tests. Wave two passed
  18 core provider/security tests and the same 52 web tests. Allbert's forced
  warning-as-error compile was clean after each wave. A clean dependency
  rebuild reprinted Jido Action's existing warnings for its absent optional Lua
  package; Allbert does not use that optional path, so M8 did not add an unused
  runtime dependency merely to silence upstream optional-code analysis.
- **Authoritative gates.** After wave one, `release.v12` passed every inherited
  and v1.2 step with evidence `release-v12-1785124935.json`, followed by green
  standalone `release.v1` evidence `release-v1-1785125889.json`. After the
  final IDNA wave, the superseding `release.v12` run again passed (Dialyzer 0;
  v1 freeze 12, product 15, v1.1 authority 13/fan-out 249/topology 5/channels
  79/web 32, v1.2 authority 13/enablement 76/catalog 26/web 26) with evidence
  `release-v12-1785126086.json`; standalone `release.v1` then passed with
  evidence `release-v1-1785127029.json`. No manifest constraint, application
  code, schema, public contract, or release scope changed.

### M9 — release-candidate preparation (2026-07-26)

- **Purpose-bar reconciliation.** All six Purpose outcomes are present with no
  deferral: zero-click model-backed first questions are automated and live-
  smoked; empty Homes keep working chat plus one repair action; onboarding is
  optional and step-addressable; the four-source catalog/chooser is shared by
  all surfaces; fallback is default-off and egress-double-gated; and ADR 0087's
  consent change is accepted, audited, sticky-false, and behaviorally bound.
  The Linux x64 model-promotion row remains the previously operator-disposed
  `BLOCKED` evidence row, not a release-scope cut; it cannot promote Qwen.
- **Candidate docs and version.** The three Mix applications and workspace
  service-worker cache now identify 1.2.0. CHANGELOG carries a pre-publication
  v1.2.0 section; operator onboarding/workspace/model guidance describes chat-
  first detection, optional onboarding, the shared catalog, and the fallback
  egress line. Historical DIT-2 evidence is retained as history but explicitly
  superseded by v1.2's zero-click and honest-empty-state FV criteria. ADR 0087
  and ADR 0088 are Accepted in their files and index.
- **Validation boundary.** FV-01..FV-03, tagging, artifact publication, tap
  fill, install rehearsal, roadmap/backlog lifecycle changes, and triad archive
  remained intentionally unexecuted at initial RC preparation. Under the final
  operator timing, each correction in the active macOS/Linux FV loop runs only
  directly affected focused tests plus format, docs, and diff checks as
  applicable, then is documented, committed, pushed, and verified clean and
  upstream-equal before invalidated FV rows resume. Precommit, `release.v1`,
  corrected `release.v12`, broad lanes, and the aggregate do not run inside that
  loop. After FV-01..FV-03 pass on both required hosts at the final candidate,
  run one fresh sequence: precommit, `release.v1`, corrected `release.v12`, and
  one uninterrupted `mix allbert.test release`. Any failure is remediated with
  the same focused loop, only invalidated FV rows repeat, and the final sequence
  restarts from the beginning before tag/publication.
- **Candidate-gate drift and remediation.** The first authoritative gate at
  candidate `20465437` stopped in `high_coverage_fast_local`: the new
  agent-exposed `list_model_catalog` descriptor had focused action, catalog,
  surface, and v1.2 coverage but no positive row in the general intent corpus.
  Added `model-catalog-001` (`show the model catalog`), advanced the committed
  corpus count to 297, and reproduced the failed seed against the complete
  corpus-contract file: 7 tests, 0 failures. This is test-coverage drift, not a
  Purpose deferral; all exact-SHA candidate gates must be repeated after the
  remediation commit is pushed.
- **Second candidate-gate drift.** The repeated authoritative gate cleared the
  corrected corpus partition, then found the same M4 action absent from the
  canonical stable-order registry assertion. Added `list_model_catalog` beside
  the existing model read actions in that exhaustive inventory. This was a
  stale expected list, not runtime ordering or scope drift; the focused registry
  and corpus suites must pass before another remediation commit and full
  exact-SHA gate restart.
- **External-runtime gate drift.** After high coverage passed, the authoritative
  external-runtime lane found two more stale release pins: the IntentAgent's
  allowed core action set omitted `list_model_catalog`, and the pre-publication
  CHANGELOG did not carry the exact formula-lag contract required while 1.1.0
  remains packaged Latest. Added the action to the Jido surface inventory and
  recorded the binding 1.1.0 → 1.2.0 tap-fill/repository-sync wording. Both are
  release-test expectation drift; neither changes runtime behavior or release
  scope. Focused reproduction precedes another exact-SHA gate restart.
- **Security-eval inheritance drift.** The authoritative security lane then
  exposed two historical assertions that v1.2 documentation had not kept
  additive: ADR 0069's amended status no longer contained the frozen exact
  `Status: Accepted (v0.63)` line, and the v0.64 repairability row still expected
  the TUI to enforce the onboarding wall that v1.2 intentionally removes.
  Restored the frozen ADR status as its own line and amended the historical row
  to prove repair guidance remains available while `readiness_guard/1` returns
  `:ok` without leaking a raw probe atom. This aligns inherited security
  evidence with the flagship rather than weakening its authority assertions.
- **FV-01 identity-admission failure (2026-07-27).** FV opened at pushed
  candidate `2e096750eab8e1ff96c0ddfee2197cd44cff55cb`. In a fresh disposable
  Home, model detection and local disclosure succeeded, but the first normal
  question produced no answer and `/settings get` reported terminal profile
  `default` was unmapped. The durable channel event proves the turn was rejected
  with `reason: :not_mapped`; it never reached Runtime or Ollama. This is a real
  flagship FAIL, not operator error: the runbook promised zero-click TUI chat
  while neither launcher established the existing identity-map prerequisite,
  and ordinary-turn rejection was swallowed. FV execution was closed. The
  corrective slice is LD26: amend ADRs 0016/0067/0087; add a dedicated atomic
  Settings bootstrap; route packaged and development launchers through one
  pre-adapter seam; render
  bounded normal-turn identity rejection; add focused, concurrency,
  security-eval, and cross-surface regressions; and repair FV-01's
  packaged/development and macOS/Linux commands. Under the then-binding timing,
  FV-01 could reopen only after focused tests, docs/drift review, and
  `mix precommit` passed; the correction was committed and pushed; the tree was
  clean and HEAD equaled its upstream; and `release.v1` plus `release.v12`
  passed at that exact replacement SHA. Those prerequisites
  passed at `27a700593112e4419cb4908debc5c2f3dc23d053`, proving the product
  correction; that historical candidate does not by itself open FV at the
  docs-amendment successor. The aggregate release gate is deliberately post-FV.
- **Corrective source smoke (non-gate, 2026-07-27).** Development
  `mix allbert.tui` used disposable Home
  `/tmp/allbert-v12-ld26-smoke.q1CNwH`: the first question answered Paris, a
  restart remained answer-ready, and launcher audit counts stayed exactly one
  each for `channels.tui.enabled`, `channels.tui.identity_map`, and the Settings
  transaction. A raw-explicit channel `false` produced the bounded refusal and
  re-enable guidance before Adapter startup. This is useful implementation
  evidence only; it did not by itself replace the focused/static/precommit or
  exact-SHA versioned gates, and therefore did not by itself open FV.
- **LD26 corrective implementation and precommit barrier (2026-07-27).** The
  shared launcher now calls `Settings.Store.prepare_local_tui_launch/1` after
  AppBootstrap readiness and before Adapter initialization. The exact-purpose
  transaction persists raw-absent channel activation and the built-in identity
  map together, honors the effective child profile, serializes concurrent
  launch/disable/map writes, reports audit diagnostics, and leaves raw-explicit
  disable and every raw-present identity map authoritative. Packaged launcher
  failures now exit visibly through `CLI.Tui.launch!/1`; ordinary unmapped and
  disabled turns render bounded rejections. A production-path regression writes
  a real TUI turn as canonical `local`, stops the Adapter, and mounts the web
  workspace against that eligible thread. Consolidated core correction tests
  passed 51/51 (seed 588526), the Store/authority rejoin passed 26/26 (seed
  764623), and the focused web rejoin passed 18/18 (seed 394882). Lane inventory
  reconciled 566 files with zero unclassified or double-counted files; adding
  the missing `models_fallback_test.exs` lane tag and the new regressions
  produced a clean 3,602-row manifest. Drift review also removed one nonexistent
  `list_model_catalog_test.exs` argument from `release.v12`; the real Registry
  and Runner contract remains covered by `models/catalog_test.exs`. Docs,
  formatting, compile-with-warnings-as-errors, Hex audit, and strict Credo are
  green. The first precommit attempt stopped only on three alias-order findings;
  those were remediated and the complete restarted `mix precommit` passed. The
  correction was committed and pushed as candidate
  `bd55806c6e2319ebfbf6e6fa1607fcf228e09a63`. At that exact SHA,
  `release.v1` passed and corrected `release.v12` passed every inherited and
  v1.2 lane, including 15/15 authority, 96/96 enablement/onboarding, 26/26
  catalog/fallback, and 28/28 web-first-run tests. The first aggregate release
  attempt then exposed one stale `unicode_util_compat` entry when its
  `deps.unlock --unused` drift check changed `mix.lock`; the run was stopped
  before accepting any aggregate result. The unused entry is removed as the
  drift remediation. That invalidates `bd55806c` as the final replacement
  candidate; its versioned-gate results remain historical rather than FV-open
  evidence.
- **Replacement candidate and final gate timing (2026-07-27).** The lock-only
  remediation was committed and pushed as clean candidate
  `27a700593112e4419cb4908debc5c2f3dc23d053`. The corrective
  focused/static/precommit barrier remained green, and exact-SHA `release.v1`
  and corrected `release.v12` both passed, so the product correction met the FV
  barrier; at that historical point, no FV row had passed. The then-binding
  timing required a docs-amendment successor to repeat those exact-SHA
  versioned gates; the later operator timing below supersedes that requirement
  for the active FV remediation loop. An aggregate release run was
  already in flight when the operator moved that gate after FV: it passed the
  static/high-coverage phases, external-runtime 620 tests with 0 failures and
  12 skipped, and security 377 tests with 0 failures, then was operator-stopped
  during the full-web phase. That partial run is explicitly **non-acceptance**.
  During active remediation, run only directly affected focused tests plus
  format/docs/diff checks, document, commit, and push before repeating
  invalidated rows on macOS and Linux. Start the full final barrier only after
  all three FV rows pass on both hosts: precommit, `release.v1`, corrected
  `release.v12`, then one uninterrupted aggregate. If that barrier fails,
  remediate it, rerun only any FV rows invalidated by the correction, and
  restart the complete barrier; a partial or failed run is never acceptance.
- **FV pass, command audit, and product remediation (2026-07-27).** FV-01
  passed at clean pushed candidate
  `887805e194a5f5ef10df9de4f8c60b417cbe7dce`: the first TUI question was
  model-backed, local disclosure appeared once, model/channel settings were
  true, and one `default → local` mapping survived restart. FV-02 was halted
  before a product decision because `script ... mix phx.server &` suspended
  its background PTY. The resulting row-by-row audit also found three product
  gaps that fixtures had hidden: a fresh env-provided hosted key was blocked by
  the schema-default provider `false`; source `mix allbert.ask` did not render
  the pending disclosure before runtime transport; and a successful web model
  pull neither re-entered Enablement nor refreshed the repair presentation in
  the same LiveView. `Settings.ProviderEligibility` now gives first-run
  selection and runtime resolution one raw-presence rule: raw explicit
  provider `false` remains sticky, while raw-absent credentialed providers may
  use a vaulted/env key without persisting provider enablement or probing the
  host. The Mix task now renders/acknowledges disclosure before submit. Web
  mount uses read-only `Enablement.preview/2`, chat renders one persistent
  primary repair CTA plus optional onboarding, and the confirmation-gated pull
  action performs event-driven enablement for the exact configured pulled
  profile; the parent LiveView refreshes disclosure and removes the CTA without
  a restart. Request-flow §H now uses unique per-attempt evidence, source-only
  pre-tag commands, one product-default Ollama endpoint, direct background
  processes with bounded readiness and exact-PID cleanup, failure-preserving
  output capture, executable assertions, and macOS/Linux variants. The final
  focused correction also made `GOOGLE_API_KEY` an alias for the seeded Gemini
  provider, moved CLI disclosure ahead of voice transcription, and atomically
  abstains if consent or the selected primary changes before the enablement
  Store lock. Focused corrective verification passed **118 tests, 0 failures**:
  95 directly affected core/Mix tests, 5 exact web scenarios, the 3-row cross-
  surface matrix, and 15 v1.2 security evals. Per operator direction, no aggregate, release,
  precommit, or broad lane ran; those remain deferred until FV-01..FV-03 pass
  on both macOS and Linux, when the final barrier runs once from the beginning.
- **Cross-platform FV closeout (2026-07-27).** macOS FV-01, FV-02, and FV-03
  passed at clean pushed candidates `6a9ef6ea`, `6e131b6f`, and `7ef541af`
  respectively; each has a zero non-doc bridge through clean pushed candidate
  `416f753f`. Linux FV-01..FV-03 then all passed directly at `416f753f` on
  Serenity. The Linux flagship TUI answered Paris on the first question and
  retained its one disclosure, true model/channel settings, durable
  `default -> local` map, and 1/1/1 launcher audit counts. The delegated web row
  showed bounded fallback, exactly one repair CTA, an explicit real
  `llama3.2:3b` pull into an initially empty isolated store, and a same-session
  Paris answer with zero Chrome warnings/errors; both owned processes and ports
  were stopped. The hosted row disclosed OpenAI before the real Paris answer,
  kept explicit `false` sticky across two fresh processes, recorded exactly one
  detection transaction, and passed the raw-key absence scan. Serenity's
  systemd Ollama service was then restored active with its ordinary model
  inventory intact. No aggregate, broad lane, precommit, or versioned release
  gate ran during this validation loop.

### M9.1 — operator documentation convergence (2026-07-27)

- **Information architecture.** Audited all 35 Markdown files under
  `docs/operator/`. Added `quickstart.md` as the unique install → open → chat
  entry, rebuilt the operator index as a complete task map, routed the project
  README and docs index to Quickstart, and added a Quickstart route to every
  deep guide. Install, onboarding, workspace, and TUI now agree that onboarding
  is optional, first chat never has a wizard/model gate, explicit disabled
  state remains sticky, provisioning is confirmed, and only one runtime may
  own an Allbert Home.
- **Current operator commands.** Replaced package-capable Mix examples with
  `allbert` / `allbert admin` across everyday TUI, channels, MCP, public HTTP,
  memory, artifacts, research/media settings, workflows, marketplace,
  self-improvement, dynamic integration, export/import, settings diagnostics,
  and security. Source-only sandbox, plugin helper, generator, stdio-server,
  and release-validation commands are now labelled as such. Historical TUI
  validation was removed from the daily guide and routed to archived request
  flows.
- **Qwen decision made operator-visible.** The model guide now records the four
  exact M4 rows and their disposition. `llama3.2:3b` remains the 8 GB
  cross-platform default; `qwen3:4b-instruct` is the macOS-evidenced opt-in
  recommendation for tool/JSON conformance; `qwen3.5:9b` remains a 16 GB
  reasoning/catalog option because it tied the Llama quality score and ran at
  materially lower throughput.
- **Audit remediation.** The TUI `/catalog` route existed but its generic
  renderer exposed only the action count message. `list_model_catalog` now
  emits a bounded redacted row listing through `surface_payload`; focused
  catalog/TUI regressions prove the Llama and Qwen rows, identity boundary,
  write-free action, and intended agent exposure. This is a supporting catalog
  correction outside the operator-validated primary first-run scenarios.
  Per the operator's primary-feature-only FV decision, no FV row is invalidated;
  supporting behavior is proved by automated tests and will rejoin at the final
  release gates.
- **Evidence.** Targeted formatter check passed. Focused catalog/TUI suite:
  5 tests, 0 failures, seed 79028. `mix allbert.test docs`, repository-relative
  file-link and heading-anchor checks, complete-index/Quickstart-route checks,
  and `git diff --check` passed. No precommit, versioned gate, broad lane, or
  aggregate ran. The operator accepted M9.1 on 2026-07-27 at pushed commit
  `e8c9ae84`; this acceptance admits, but does not itself start, the final
  release barrier.
- **Post-M9.1 operator intake disposition.** The operator confirmed that the
  packaged TUI should become a thin client of the running daemon rather than
  require the service stop/restart dance. This is parked in
  `future-features.md` as the foundational Daemon-Backed TUI / Thin Terminal
  Client, slotted for the first post-1.2 readiness review. It is not a late
  v1.2 scope addition, does not invalidate completed FV, and requires explicit
  promotion into a versioned plan plus an attach/session-protocol ADR before
  implementation.
- **Final-barrier attempt 1 and bounded remediation.** The final barrier started
  from clean pushed `a2163d8b`. Hex audit, warnings-as-errors compile, and
  formatter passed; precommit then stopped at strict Credo before any
  `release.v1`, `release.v12`, or aggregate invocation. Credo reported four
  nested-module test aliases, one production alias-order issue, and one depth
  finding in the atomic raw-absent settings apply branch. Remediation aliases
  the test modules, orders the production aliases, and extracts that exact
  apply branch into a private helper without changing its condition, write,
  validation, audit, pin refresh, or return shape. The directly affected
  store/enablement/flagship/model suite passed 56 tests with 0 failures (seed
  896321), and strict Credo then reported no issues. This structural correction
  does not invalidate FV: all primary behavior is unchanged and its exact
  automated paths are green. After docs/diff checks and a clean pushed
  remediation candidate, the entire four-step final barrier restarts at
  precommit.
- **Final-barrier attempt 2 and bounded remediation.** At clean pushed
  `d35d69d0`, precommit, `release.v1`, and corrected `release.v12` passed. The
  uninterrupted aggregate then stopped in its first high-coverage phase at
  `db_serial` partition 4: one CLI contract test still required the
  persistent-service sentence in `docs/operator/onboarding.md`, although M9.1
  moved service installation and foreground-repair guidance to the canonical
  install guide. The correction repoints that documentation contract assertion
  to `docs/operator/install.md` and its current `allbert serve` wording. It
  changes no runtime or primary-feature behavior, so no FV row is invalidated.
  The directly affected CLI suite passed 16 tests with 0 failures (seed 19652);
  targeted format, docs, and diff checks passed. After a clean pushed
  candidate, the entire four-step final barrier restarts at precommit.
- **Final-barrier attempt 3 and bounded remediation.** At clean pushed
  `5dcbaa34`, precommit, `release.v1`, and corrected `release.v12` passed. The
  aggregate passed static and high-coverage phases, then stopped in
  `core_external_runtime_serial` with 622 tests, 1 failure, and 12 skips. The
  M9.1 MCP guide now correctly uses packaged
  `allbert admin settings ...` examples, but its documentation-contract test
  adapter accepted only the development `mix allbert.settings ...` syntax and
  rejected the example before Settings Central executed it. The correction
  teaches that test adapter both command envelopes while retaining one
  Settings Central execution path. It changes no runtime or primary-feature
  behavior, so no FV row is invalidated. The directly affected integration
  documentation-contract test passed (1 test, 0 failures, seed 130153), and
  targeted format, docs, and diff checks passed. After a clean pushed
  candidate, the entire four-step final barrier restarts at precommit.
- **Final-barrier attempt 4 and bounded remediation.** At clean pushed
  `cf3ce741`, precommit, `release.v1`, and corrected `release.v12` passed. The
  aggregate passed static, high-coverage, and external-runtime phases, then
  stopped in `core_security_eval_serial` with 377 tests and 1 failure. The
  v0.64 package-first security eval still required three pre-M9.1 sentences in
  `docs/operator/onboarding.md`; M9.1 made Quickstart the package-first entry,
  moved service posture to the install guide, and isolated source commands in
  the contributor README section. The correction proves those same three
  security properties at their canonical owners: onboarding routes to
  Quickstart, Quickstart installs without Mix, the README labels Mix as source
  contributor work, and the install guide keeps foreground serve diagnostic.
  This is a test-only documentation-contract correction with no runtime or
  primary-feature behavior change, so no FV row is invalidated. The focused
  v0.64 security-eval suite passed 6 tests with 0 failures (seed 716275), and
  targeted format, docs, and diff checks passed. After a clean pushed
  candidate, the entire four-step final barrier restarts at precommit.
- **Pre-attempt 5 Quickstart repair-path clarification.** Final operator review
  found that Quickstart promised a primary repair action when Ollama was absent
  but did not name the **Install local runtime** control or distinguish absence
  from an installed-but-unhealthy service. The guide now names the control,
  the supported macOS/Linux installer sources, the explicit confirmation, and
  the existing-runtime repair path. The package-first security eval now locks
  that guidance. Its focused suite passed 6 tests with 0 failures (seed
  716275), and the docs gate and diff check passed. This docs/support-test
  correction changes no runtime or primary-feature behavior, so it invalidates
  no FV row. Push the correction, then start final-barrier attempt 5 at
  precommit.
- **Final-barrier attempt 5 and bounded remediation.** At clean pushed
  `f661fa18`, precommit, `release.v1`, and corrected `release.v12` passed. The
  aggregate passed static/high coverage (421 seconds), external-runtime serial
  (622 tests, 0 failures, 12 skips; 682 seconds), and security-eval serial (377
  tests, 0 failures; 356 seconds), then full web stopped after 2,606 seconds
  with 327 tests and exactly 1 failure. The v0.61 redesigned-surface proof still
  expected the old guided-onboarding `first_model_detect` suggestion, although
  v1.2 runs detection automatically and renders its one repair CTA through
  `FirstRun.Presentation`; production already exposed only the three ordinary
  view-only read DTOs. The correction updates that historical test comment and
  expected list without touching production code. Its focused file passed 2
  tests with 0 failures (seed 501814). This changes no runtime or primary
  behavior, so it invalidates no FV row. After docs/format/diff checks and a
  clean pushed candidate, restart the entire four-stage barrier at precommit.

## Purpose

Ship the 1.2 flagship: install, open, chat. First run auto-detects an
already-provisioned model provider, auto-selects it local-preferred among
usable candidates, enables
direct answers with an audited disclosure, and never dead-ends when nothing
is provisioned. Onboarding survives as an optional, step-addressable
customization surface, and the model surface gains a real catalog/chooser
and an opt-in fallback policy.

1. **Zero-click chat-ready.** A fresh Home with any reachable provisioned
   provider (configured local endpoint, localhost Ollama with a usable
   model, or a configured hosted profile whose key reaches the provider)
   answers the operator's **first question with the
   model, zero prior clicks**, on every surface — web workspace, packaged
   `allbert tui`, dev `mix allbert.tui`, `allbert ask`, bare `allbert` —
   with the one-time disclosure visible (ADR 0087 §1–§3). A fresh local TUI
   launcher establishes the built-in profile's ordinary Settings identity map
   before adapter startup; no mapping command is a first-question prerequisite.
2. **Honest empty state, no dead ends.** A fresh Home with nothing
   provisioned still opens a working chat: the deterministic fallback
   answers, exactly one primary repair CTA per detect state is presented
   (one-click curated pull / guided install / BYOK), and no wizard wall or
   TUI refusal exists anywhere (ADR 0087 §4).
3. **Onboarding is optional and step-addressable.** Every wizard step is
   directly enterable at any time, including after completion; re-entry and
   rewind never re-gate chat or the TUI; `onboarding_complete` stops gating
   anything user-facing (ADR 0087 §5, ADR 0069 amendment).
4. **A real model catalog and chooser.** One read-only catalog (curated
   local + live runtime inventory + configured profiles + verified hosted
   metadata) rendered by web/TUI/CLI/onboarding through registered read-only
   actions; selection and pulls write only through existing Settings actions
   (ADR 0088 §1–§2).
5. **Fallback with an egress line.** Selection-time local-first ordering is
   always on; runtime failover is default-OFF opt-in, local→hosted is
   double-gated, at most one failover per turn, every event audited, and the
   whole-chain-failed message stays honest (ADR 0088 §3).
6. **The consent line is redefined deliberately, not incidentally.**
   ADR 0087 Accepted; the DIT-2-class acceptance criteria replaced; an
   operator-stored `false` is never overridden; detection never provisions
   (no installs, pulls, key writes) and performs no hosted egress.

## Expected Inputs

- Stable v1.1.0 shipped and archived (build-start precondition). It carries
  the fan-out substrate, `release.v11` gate, behavioral `:v11` eval-row
  pattern with the meta-contract (M12.5), the FV-style focused operator
  validation convention (M12.11), gate-owned root isolation (M12.10.1), and
  the corrected caller/capability census discipline (M12.6).
- v1.1 outputs that landed AFTER this plan's 2026-07-24 recon stamp
  (M12.14–M12.22) and are binding on v1.2:
  (a) the one-connection / WAL / `:immediate` SQLite topology for development
  and packaged production (`config/runtime.exs:47-52`, `config/dev.exs:31`;
  the `POOL_SIZE` escape hatch is removed) — M0's boot-to-ready and
  decide-turn baselines are measured on this topology and M6's fixed budgets
  are judged against it, never against pre-M12.19 figures;
  (b) `AllbertAssist.Database.TransientError` as the closed transient-database
  classifier — reuse it rather than adding a second policy; LD20's provider
  normalization table is its provider-side analogue, not a replacement;
  (c) the M12.20 disposable-Home production-profile integration gate step,
  which boots real persistence without SQL Sandbox — v1.2's fresh-Home matrix
  stacks onto it and does not add a parallel harness;
  (d) the M12.20 source census rejecting unreviewed `Repo.transaction`,
  checkout, and streaming connection ownership — any new v1.2 code taking Repo
  connection ownership is added to the approved inventory in the same commit
  that introduces it.
- The first-run substrate: wizard state machine + go-signal
  (`onboarding.ex`), detection chain (`cli/first_run.ex`,
  `first_model/ollama.ex`, `settings/model_doctor.ex`), web auto-open
  policy, TUI readiness guard, persona seeds — all anchored below.
- The model substrate: `Settings.Models.for/2` + `candidates_for/2`,
  `model_profiles.*`/`model_preferences.*`/`providers.*` schema,
  ReqLLM/`ModelRuntime.model_spec/1`, `model_doctor` (ADR 0072).
- Gate machinery: `@release_v11_steps` composition precedent
  (`allbert.test.ex:5937`), EvalInventory milestone enum
  (`eval_inventory.ex:58` — `:v12` appends here), the v11 sweep
  meta-contract pattern (`v11_sweep_eval_test.exs:61-68`), docs-gate active
  plan list (`@docs_active_plan_files`, `allbert.test.ex:213-226`). The v1.2
  triad is ALREADY listed there; M0 verifies presence rather than adding it,
  and separately removes the two archived v1.1 triad entries that the v1.1
  closeout left behind (the check is basename-in-README, so the stale rows
  are inert today but wrong).
- Metrics-store baselines re-recorded at M0 (decide-turn, boot-to-ready,
  gate walls); v1.1 M0 figures are superseded by then.

## Current Code State

Verified 2026-07-24 at v1.1 M12.13 HEAD (recon pass; file:line anchors),
spot-re-verified 2026-07-26 at stable v1.1.0 closeout HEAD in the third
readiness pass: every anchor below still resolves within ±2 lines except the
web component paths, corrected in place. M0 still re-verifies every anchor at
build-start HEAD and records the drift table.

**Two different chat blocks exist today.**
Web/CLI ask is soft-blocked: `intent.direct_answer_model_enabled` defaults
`false` (`settings/schema.ex:735-740`, safe-write list `:53`, defaults tree
`:3629`) and `direct_answer` returns the deterministic `:model_disabled`
fallback (`actions/intent/direct_answer.ex:96-101`, text at `:312-317`).
Packaged TUI is hard-blocked: `CLI.Tui.readiness_guard/1`
(`cli/tui.ex:69-101`) refuses to start unless
`FirstRun.detect_details().state == :product_ready`. **Dev `mix allbert.tui`
(`plugins/allbert.tui/lib/mix/tasks/allbert.tui.ex`) has no guard at all** —
packaged and dev TUI diverge.

**Detection already runs; v1.2 changes what is done with it.**
`FirstRun.detect_details/1` (`cli/first_run.ex:71-96`) resolves six product
states and six model states (`:35-49`). The probe chain
(`first_model_state/1`, `:127-157`): configured local provider first
(`configured_local_probe/0` `:251-261` — `model_preferences.primary` →
`ModelDoctor.diagnose/2`, accepts `endpoint_kind: :local_endpoint`; the
v1.0.5 M8.4 WSL2 rule), then localhost Ollama discovery
(`default_ollama_probe/0` `:243-246`; `Ollama.probe/1`
`first_model/ollama.ex:87-100` — binary / `GET /api/version` / curated tag
in `GET /api/tags`, loopback-enforced `:73-80`), then BYOK env sniff
(`byok_ready?/0` `:232-237` — ANTHROPIC/OPENAI/OPENROUTER/GOOGLE/GEMINI key
vars). **`classify_model_probe/3` (`:151-158`) consults BYOK on exactly one
branch (`:missing`).** A reachable-but-unhealthy runtime returns
`:runtime_unhealthy` and a below-floor host returns `:below_hardware_floor`
without ever testing key presence, so a machine with a broken local runtime
and a valid hosted key is NOT `byok_ready` today. LD24 and request-flow §D.0
define what M1 changes here; the six `model_state` values (`:42-49`) stay the
only detection vocabulary — v1.2 adds no seventh.
Curated model: `first_model.curated_model` default `llama3.2:3b`,
floor `first_model.curated_floor_gb` 8 (`schema.ex:960-972`, defaults
`:3667-3669`; `Ollama.curated_model/0` `ollama.ex:27-47`). Hardware floor:
`first_model/hardware.ex`. Guarded wrapper + readiness mapping:
`Onboarding.safe_first_model_state/0` (`onboarding.ex:500-516`, test
override `:first_model_state_override`), `readiness_label/1` (`:480-493` —
`local_ready|byok_ready→:ready`).

**The wizard owns enablement today.** Steps/tracks `onboarding.ex:31-33`
(`welcome track_select model_path profile_select profile_review
health_check first_chat optional_connect`; QuickStart drops
`optional_connect`, `:469-470`). `wizard_advance/3` (`:260-293`) completes
only when `first_chat_ready?/1` (`:324-329` — readiness `:ready` AND the
consent key true; "single source of truth… derived, never stored").
`maybe_enable_model_answer/2` (`:303-316`) flips
`intent.direct_answer_model_enabled` + `intent.model_assist_enabled` on
readiness `:ready` at steps `model_path|first_chat|optional_connect` —
the ADR 0078 M8.5 enablement point v1.2 supersedes. `wizard_rewind/2`
(`:342-372`) truncates `wizard_done` and clears `onboarding_complete` but
never revokes enablement (`:335-337`) — under today's gating a rewind
re-triggers web auto-open and the TUI block. State lives in the Home marker
`onboarding.json`, not Settings (`cli/first_run.ex:33`, atomic write
`:301-310`, corrupt-marker guard `:285-297`). Personas seed the same keys
(`priv/personas/*.yaml`; applied via confirmation-gated
`apply_persona_profile`, review at `apply_persona_profile.ex:98-102`;
selection recorded in the marker, `onboarding.ex:158-168`).

**Web first-run surfaces.**
`Workspace.FirstRun` auto-open policy
(`workspace/first_run.ex:15` `@auto_open_states
[:onboarding_incomplete, :profile_unreviewed, :first_model_not_ready]`,
destination `:41-49`), consumed at `workspace_live.ex:1382-1407`. Wizard
canvas incl. Start QuickStart/Advanced buttons
(`workspace/components/onboarding.ex:345-366`), go-signal banner
(`:334-343`), per-step rewind (`:368-400`, event `:86-88`). Chat renders
exactly one first-run entry point
(`workspace/components/chat.ex:745,:776-793`, v1.0 R12 — the file moved under
`workspace/` in the v1.0.2 web split; the pre-split path no longer exists).
ModelsPanel = inventory/repair, not a chooser
(`workspace/components/operator_panels.ex:372+`; pull via `start_async`
`:383-460`). Pull
progress plumbing: signal `signals.ex:19`
(`allbert.workspace.first_model.pull.progress`), forwarded at
`workspace_live.ex:1604-1612`.

**CLI surfaces.** Bare `allbert` dispatches `first_run/0`
(`cli.ex:244`, `:390-433` incl. repair suffixes). Terminal wizard:
`cli/areas/onboarding.ex` (routes `:136-188`, interactive `:200`, provider
key entry `:290`) + `mix/tasks/allbert.onboard.ex`. Boot ordering hazards
detection inherits: `:req` must be started before probing (`cli.ex:76`,
`cli/tui.ex:39,61-64` `ensure_http_started`) and settings must be read
fresh-process (`cli/tui.ex:34-38`, the v1.0.5 RC.2 lesson).

**Model configuration and resolution.**
Namespaces in `settings/schema.ex`: `providers.*` (`:123-126`, defaults
`:3687-3716`, secret refs like `secret://providers/openai/api_key`),
`model_profiles.*` (`:148-155`, defaults `:3720-3794` — `local`,
`coding_local`, `pi_coding_local`, `fast`, `anthropic_fast`,
`openrouter_fast`, `coding`, `embedding_local`, `router_local`,
`router_escalation_local`), `model_preferences.*` (`:82-84`, defaults
`:3671+` — `primary: "local"`, `tasks.direct_answer: ["local"]`).
Resolution: `Settings.Models.for/2` (`settings/models.ex:31`, task→
capability map `:13-16`), ranked `candidates_for/2` (`:50`),
`primary_fallback/2` (`:143-147`). Direct answer resolves ONE profile
(`direct_answer.ex:104`) and any provider error collapses to the static
`{:model_unavailable, _}` text (`:126,327-329`) — **text chat has no
multi-candidate retry**; only image/voice consume `candidates_for/2`
(`generate_image.ex:48`, `transcribe_voice.ex:43`,
`synthesize_voice.ex:42`). ReqLLM seams: `req_llm_answerer.ex` (injectable
via `answerer/0`, `direct_answer.ex:347-351`),
`ModelRuntime.model_spec/1` (`settings/model_runtime.ex:14-24`).

**No catalog exists.** `llm_db` is transitive-only via ReqLLM 1.17.1
(`mix.lock:45,68`; zero direct usage). `ProviderCatalog.known_capabilities/0`
reads settings, not an external catalog (`settings/provider_catalog.ex:66`).
`set_active_model_profile` writes the legacy `intent.model_profile` alias
(`set_active_model_profile.ex:50-60`) — v1.2 leaves the aliases untouched
(additive-only; removal stays with the 1.5/1.6 migration-runner cluster).

**Tests and lanes today.** `onboarding_test.exs` (28 tests, DataCase
`async: false`, no lane moduletag — M0 adjudicates its lane),
`first_run_test.exs` (16, `app_env_serial`), `first_model_test.exs` (18,
`:first_model_path`, gate row at `allbert.test.ex:4698`), web
`workspace_onboarding_test.exs` (11) + `first_run_test.exs` (4), CLI
onboarding tests, and three onboarding security evals.

## Locked Decisions

Decisions 1–3 are operator-locked (2026-07-24 planning review); the rest are
derived from those three plus code evidence, recorded for veto at plan
review.

1. **Auto-enable on any detected configured provider (operator).**
   Availability-first, local-preferred order: a healthy configured local
   endpoint → healthy localhost Ollama → configured hosted key. An unusable
   local rung (`model_missing`, `runtime_unhealthy`, or
   `below_hardware_floor`) does not block a configured hosted rung; hosted
   selection carries the pre-egress disclosure and local repair remains a
   secondary suggestion. Detection is enablement; provisioning is consent.
   This automatic order applies when `model_preferences.primary` is raw-
   absent. A raw-explicit primary is binding: enablement must select and
   disclose that exact profile when usable, or abstain as
   `enabled_unavailable`; it never enables against one profile while runtime
   resolution preserves another.
   ADR 0087 and request-flow §D.0 bind the projection and guardrails.
2. **Additive-only release (operator, applies to 1.2–1.4).** No non-additive
   migration; the Settings Runtime Migration Runner stays on the 1.5/1.6
   train; legacy `intent.*model_profile` aliases remain untouched. Each
   milestone's acceptance includes "no non-additive migration introduced".
3. **Validation is flagship-focused FV rows (operator).** The v1.1
   OV-00..OV-12 correction is the standing convention: FV-01..FV-03 cover
   only the primary Zero-Click First Run feature set (local first answer,
   honest empty-state repair, hosted first answer + sticky revert). During the
   active macOS/Linux loop, a correction runs only directly affected focused
   tests plus format/docs/diff checks as applicable, then is documented,
   committed, and pushed before invalidated FV rows repeat. Precommit,
   `release.v1`, corrected `release.v12`, broad lanes, and the aggregate wait
   until all three FV rows pass on both required hosts; they then run once, in
   that order, as the final release barrier. Onboarding
   navigation, catalog/chooser, fallback, and the
   model bakeoff are supporting scope and MUST be proven by automated suites
   or scripted machine-verifiable external smokes, never operator FV. No
   validation-only product capability may ship (M12.11 principle).
4. **Sticky explicit-false (derived, v1.1 LD32 pattern).** Auto-enablement
   fires only when the consent key is absent (schema default). An
   operator-stored `false` is never overridden by detection; disabling once
   means disabled until the operator re-enables. **Stickiness is per-key
   across the whole write set (third readiness pass):**
   `intent.direct_answer_model_enabled` gates whether enablement runs at all;
   `intent.model_assist_enabled` and `model_preferences.primary` are each
   written only when raw-absent. An explicitly stored value on ANY of the
   three is preserved — a one-key stickiness rule would let detection
   overwrite an operator's stored `model_assist_enabled: false`, which is the
   same defect LD16 exists to prevent, one key over. If consent becomes false
   or a different primary appears before the locked write, the whole subset is
   rejected to keep selection, disclosure, and runtime routing aligned. The
   provenance audit row records which keys were written and which were already
   present.
5. **Disclosure, not confirmation (derived, ADR 0087 §3).** One-time durable
   marker per surface; text generated from the audit provenance
   (`enabled_by: detection`, profile, provider class); hosted selection
   names the provider and the egress fact before the first message is sent.
6. **Detection never provisions and never performs hosted egress (derived).**
   No installs, no pulls, no key writes from detection; hosted detection is
   key presence only; model pulls stay one-click confirmation-gated.
7. **TUI guard inversion with dev/packaged parity (derived).** The TUI
   always starts; non-ready detect states render as in-TUI guidance.
   `mix allbert.tui` and packaged `allbert tui` adopt identical posture —
   the current divergence closes.
8. **`onboarding_complete` is decoupled from availability (derived).** It
   remains a wizard-progress marker only. Rewind/step re-entry never
   re-gates chat, never re-triggers auto-open, never re-blocks the TUI.
   The ADR 0069 false-complete rule stands: the wizard still cannot claim
   complete while readiness is non-ready. **Completion is decoupled from
   consent (operator, 2026-07-26):** `first_chat_ready?/1`
   (`onboarding.ex:325-327`) today requires readiness `:ready` AND the
   consent key `true`, so a sticky-disabled Home could never finish the
   wizard — a permanent dead end of exactly the class Purpose #2 removes.
   M3 completes the wizard on readiness `:ready` alone, records the model
   step as explicitly declined, and surfaces a ONE-TIME "re-enable model
   answers?" affordance on that step which never nags again. The ADR 0069
   false-complete rule is preserved in its real sense: completion still
   requires a genuinely ready model path, not a consent flag.
9. **Detection stays off the hot turn path (derived).** Boot-time +
   event-driven (settings change, explicit re-detect); results cached
   through the existing `detect_details` caching seam (M7.9 precedent).
   M6 carries a measured budget: no regression to the decide-turn baseline
   and a bounded boot-to-ready cost, both cited from the metrics store.
10. **Catalog per ADR 0088 §1 (derived).** Four merged sources; `llm_db`
    fitness is an M4 verification with the result recorded either way;
    exposure only via registered read-only actions; the chooser writes only
    through existing settings actions and safe-write keys.
11. **Fallback split per ADR 0088 §3 (derived).** Selection-time ordering
    always on; runtime failover default OFF
    (`models.fallback.enabled=false`), local→hosted double-gated
    (`models.fallback.allow_local_to_hosted=false`), at most one failover
    per turn (`models.fallback.max_failovers_per_turn` default 1, clamp
    1..2), every event traced + audited, reply metadata names a non-primary
    answering profile.
12. **Gate = `release.v12` stacked on `release.v11` (derived).**
    `@release_v12_steps = @release_v11_steps ++ @v12_focused_steps` — the
    v105-on-v104 stacking precedent; v1.1's fan-out contracts are one
    release old and stay enforced; the added wall cost is minutes. The
    docs-gate active-plan list (`allbert.test.ex:212-219`) gains the v1.2
    triad in the same commit that starts the build.
13. **Behavioral eval rows from day one (derived, M12.5 lesson).** The
    `:v12` rows ship with the v11-style meta-contract (exact id set,
    distinct assert sets, production-boundary execution, no source-grep
    proofs) as a red-first deliverable of M7 — and the two flagship
   acceptance scenarios (zero-click answer; empty-state repair path) land
   as ordinary automated regressions in M1b/M2, before any UI milestone,
    so operator validation cannot be the first end-to-end execution
    (M12.12 lesson).
14. **DIT-2-class acceptance criteria are replaced, not patched (derived,
    ADR 0087 §6).** The plan's M9 documentation deliverable rewrites the
    first-run acceptance rows; stale criteria may not survive in
    `docs/validation/` or operator guides.
15. **Fresh-Home fixtures are the validation substrate (derived, M12.9
    lesson).** Every automated acceptance scenario and every FV row runs
    against a disposable Home; nothing touches `~/.allbert`; no cloned
    state weakens clean-install evidence; runbooks avoid `set -e`/`exit`.
16. **Raw presence is the consent fact, and the primitive is multi-key
    (readiness review; corrected third pass).** Effective `Settings.get`
    values are default-merged and cannot distinguish absent from explicitly
    stored `false` — `Store.put_user_setting/3` demonstrates the trap
    directly, computing `old_value` from the merged map (`store.ex:167`).
    M1a adds ONE Store-owned atomic compare-and-write operation that reads the
    raw user-settings map, checks `Schema.get_dotted(user_settings, key)`,
    and writes under the same `StoreLock`; `nil` may auto-enable, stored
    `false` is sticky, stored `true` is idempotent. No caller performs a
    read-then-write sequence.
    **The primitive takes a key→value MAP, not one key.** Enablement writes
    three keys and the plan requires atomic application of the raw-absent
    subset; `StoreLock.with_lock/2`
    opens a fresh connection and issues `BEGIN EXCLUSIVE`
    (`store_lock.ex:29-40`), so it is NOT reentrant — composing the operation
    out of three per-key writes either self-deadlocks for the 5 s timeout or
    produces three non-atomic commits that can strand
    `direct_answer_model_enabled: true` beside `model_assist_enabled: false`.
    One lock, one raw read, absent-key filter, one merged validation, one
    write, one audit row per applied key, and one transaction provenance
    envelope listing applied and preserved keys. Validation failure writes
    none of the absent subset.
17. **M1a owns a minimal usable-model selector (readiness review).** First-run
    cannot depend on the M4 chooser. M1a selects: configured doctor-healthy
    local profile; otherwise the curated pulled tag; otherwise the first
    pulled text-generation model that passes a deterministic compatibility
    predicate recorded at M0. M4 reuses this predicate in the catalog rather
    than replacing it. If that predicate cannot be made trustworthy, M1a is
    deliberately curated-tag-only and `detected_needs_model` remains honest.
18. **Hosted detection is configured-but-unverified (readiness review).** Key
    presence performs no probe and makes no reachability claim. Hosted
    priority is existing explicit `model_preferences.primary`, then
    task-specific configured order, then a stable provider order recorded at
    M0; never environment enumeration order. A bad/expired key may enable the
    configured path but its first failed turn returns the honest unavailable
    state and repair CTA, never a false `provider_ready` claim.
19. **Disclosure precedes hosted transport atomically (readiness review).**
    Every entrypoint, including one-shot `allbert ask`, durably records the
    pending disclosure, renders/acknowledges it, and only then admits hosted
    inference. A crash before acknowledgement causes disclosure to repeat,
    not egress to occur first; the durable marker is consumed only after
    render acknowledgement. Local inference does not require an egress ack.
20. **Fallback classification is adapter-owned (readiness review).** M5
    records a table mapping concrete ReqLLM/provider/streaming results to
    `definitive | ambiguous | partial`; unknown shapes default to `partial`
    (no retry). Provider adapters normalize errors before the executor decides;
    the executor never parses exception strings.
21. **Budgets are fixed at M0 (readiness review).** M0 records numerical
    cross-platform cold/warm boot-to-ready, time-to-first-token, peak-memory,
    and decide-turn regression thresholds before M1. M6 proves those fixed
    thresholds; it may not choose a favorable bound after implementation.
22. **Curated-model change is evidence-gated (operator confirmed
    2026-07-26).** The candidate set includes `llama3.2:3b` (control), Qwen 3
    4B instruct/non-thinking, `qwen3.5:4b` (direct successor), and
    `qwen3.5:9b` (higher-floor quality challenger). Exact tags/digests are
    reverified at M0; first-run trials force non-thinking behavior while
    thinking variants remain catalog options.
    Thinking variants are catalog options, not the zero-click default. A
    candidate tag that does not exist publicly at M0 records `BLOCKED` and is
    dropped from the bakeoff; it is never silently substituted with a
    nearest-name tag. Test
    macOS arm64, Linux x64/arm64, and WSL2 where the release supports them;
    record download bytes/time, cold/warm TTFT, tokens/sec, peak RSS, direct-
    answer quality, tool/JSON conformance, malformed-call rate, multilingual
    behavior, context behavior, license, and exact public Ollama tag/digest.
    **Promotion targets macOS arm64 AND Linux x64 measured. One of those two
    primary rows may record `BLOCKED` only with explicit operator disposition
    naming the unavailable host and evidence gap; Linux arm64 and WSL2 may
    also record `BLOCKED` with their gaps named in Build Progress and in the
    CHANGELOG entry.**
    Promotion requires material Allbert-suite improvement without materially
    regressing first-answer latency. Raising the current 8 GB floor is allowed
    when the quality/tool-reliability gain is recorded and the operator accepts
    the reach tradeoff. A raised floor must produce an explicit consumer /
    prosumer disposition: either retain a lower-floor consumer curated default
    and recommend the winner as the prosumer profile, or promote the winner and
    prove the existing `below_floor` BYOK/repair path on excluded machines. No
    machine is silently reclassified as ready. Changing the default or tier
    updates every schema/catalog/doc/test occurrence as one M4 commit.
23. **Competitive bar, not parity scope (readiness review, 2026-07-26).** Hermes sets
    the bar for model choice, learned skills/memory, and delegated work;
    OpenClaw for gateway/channel activation and remote availability; IronClaw
    for packaged security/privacy positioning. v1.2 answers only the shared
    activation gap. Allbert differentiates through Security Central,
    registered actions, durable Objectives, audited settings, and one runtime
    spine; no autonomous skill/memory expansion or new channel scope enters
    this release.
24. **Availability-first with local preference (operator, 2026-07-26).** With
    no raw-explicit primary, a healthy local provider always wins. A usable
    raw-explicit primary is the binding operator exception. When the local runtime is present but
    unusable (`model_missing`, `runtime_unhealthy`, or
    `below_hardware_floor`) and a hosted key is configured, Allbert
    auto-enables the hosted profile so the first question still receives a
    model answer. The disclosure names the provider and egress before the
    send; the relevant local pull/restart/hardware guidance is secondary and
    never blocks chat. Without a hosted key, the same states remain honest
    repair states with one primary CTA. Request-flow §D.0 is the binding
    derivation table and every cell is automated in M1b/M6.
25. **Auto-enablement has no global off switch (operator, 2026-07-26).** The
    per-Home sticky explicit-false (LD4) is the only control. v1.2 adds no
    `first_run.auto_enable.*` policy key; a fleet/packaged policy lever is out
    of scope and is recorded in Non-Goals so a later release can revisit it
    deliberately rather than by drift.
26. **Explicit local TUI launch atomically prepares activation and ordinary
    identity admission (operator correction, 2026-07-27).** Packaged
    `allbert tui` and development `mix allbert.tui` share one pre-adapter
    bootstrap. Under one Settings lock and one validated absent-subset write, a
    fresh built-in-profile launch persists raw-absent
    `channels.tui.enabled=true` and the normal list-shaped
    `channels.tui.identity_map` `default → local` entry together. The effective
    child profile, including a launcher child-option override, decides whether
    the identity default is eligible; a custom effective profile may activate
    a raw-absent channel but never receives an invented mapping. Raw-present
    identity state is sticky in every form, including an empty list, a custom
    map, or a disabled entry. Raw-present `channels.tui.enabled=false` is an
    intentional operator settings action—not an onboarding outcome: optional
    onboarding remains available and never stores this shutdown on the
    operator's behalf. The explicit shutdown blocks before Adapter startup,
    writes nothing, and prints source/package re-enable guidance. Concurrent launch
    converges on one audited transaction; later launches preserve the durable
    state. The Adapter has no fallback and still resolves both turns and slash
    commands through `Identity.resolve/3`; generic adapter/supervisor startup
    remains unmapped. An unmapped or disabled normal turn renders a bounded
    rejection instead of appearing to hang. TUI `default` and web `web-local`
    independently resolve to canonical user `local`; the TUI mapping grants no
    web authentication and creates no automatic cross-surface thread
    continuation.

## Operator-Visible Win

Install Allbert, open it, and ask a question — it answers. No wizard, no
toggle, no "Start QuickStart". If you already run Ollama, it finds it; if
you configured a provider key, it uses that (and says so, once, with the
revert command). If you have nothing, the chat still opens, answers
honestly, and shows exactly one button that fixes it. Want to customize?
Open onboarding anytime, jump straight to any step. Browse models in a real
catalog instead of typing tags — and if you opt in, Allbert survives a dead
provider by falling back down a chain you configured, never silently
falling into a paid one.

## Competitive Readiness Bar (research refreshed 2026-07-26)

- **Hermes Agent:** one-command/desktop activation, `hermes model`, broad
  provider choice, delegated subagents, scheduled work, and a visible learning
  loop across bounded memory and agent-managed skills. Source:
  <https://hermes-agent.nousresearch.com/docs/>.
- **OpenClaw:** a single self-hosted gateway connecting many chat channels to
  sessions, memory, agents, web control, and mobile nodes; its product promise
  is reachability from where the operator already chats. Source:
  <https://docs.openclaw.ai/>.
- **IronClaw:** packaged cross-platform Agent OS positioning centered on
  privacy, encrypted local credentials, onboarding, model status, and
  extensibility. Source: <https://github.com/nearai/ironclaw> and
  <https://docs.ironclaw.com/>.

The 1.2 answer is narrower and testable: Allbert removes the setup toll before
first value while retaining a stronger explicit-authority story. Success is
not feature-count parity; it is a first useful answer with fewer decisions,
honest repair when no model exists, and no weakening of the action/security
spine.

## Planned Interfaces

- `AllbertAssist.FirstRun.Enablement` (name final at M1) — the
  detection→enablement engine: consumes the existing
  `FirstRun.detect_details/1` chain, applies the ADR 0087 §1 ordering,
  performs the audited safe-write pair + profile selection when the consent
  key is absent, records provenance, and exposes
  `enablement_state/1 :: :auto_enabled | :sticky_disabled | :needs_model |
  :nothing_detected | :below_floor | :enabled_unavailable` for surfaces.
  Idempotent; no hosted egress; one atomic audited settings transaction.
  `:enabled_unavailable` covers the most common real-world dead end — a Home
  auto-enabled against a local runtime that has since disappeared, where the
  consent key is `true` but every turn returns `{:model_unavailable, _}`. It
  renders the honest unavailable text plus the same repair CTA as
  `:nothing_detected`, and never re-shows the disclosure. States derive from
  the six existing `model_state` values per request-flow §D.0; this enum is a
  presentation projection, not a seventh detection state.
  It also covers an unusable exact raw-explicit primary and a primary that
  changed between selection and the locked enablement write.
- `Settings.Store.put_user_settings_if_absent/2` (name final at M1) — one
  StoreLock-owned raw-presence compare-and-write primitive over a key→value
  MAP (LD16: single-key cannot atomically apply the absent subset under a non-reentrant
  lock). Inside one `StoreLock.with_lock/2` + `Fragments.with_composition/1`
  — the existing `store.ex:158-162` shape — it reads raw `user_settings`
  once, tests each key with `Schema.get_dotted(user_settings, key)`, applies
  only absent keys, validates the merged result once, writes once, and emits
  one audit row per applied key plus one transaction provenance envelope
  listing written and preserved keys. Returns
  `{:ok, %{disposition: :applied | :explicitly_disabled | :selection_changed,
  written: [key], present: %{key => value}}}`. A validation failure writes
  nothing; the two abstention dispositions also write none of the subset.
  Never treats a schema default as an operator value. No
  caller may nest `StoreLock` or rebuild this out of per-key writes.
  **Write layer:** enablement is system-initiated, so it writes through
  `Settings.Store` (which audits) rather than a registered action (which is
  the agent-facing boundary). It is not a new privileged capability: it writes
  only the three named keys, only when raw-absent, only from the boot/detect
  path, and M1 proves no other key is reachable through it.
- `Settings.Store.prepare_local_tui_launch/1` — a separate exact-purpose LD26
  transaction. It owns raw presence, effective-profile, and explicit-disable
  checks under one lock; its closed write set is
  `channels.tui.enabled=true` plus, only for the built-in effective profile,
  the ordinary list-shaped `default → local` map. It validates and persists the
  absent subset once, emits per-key and transaction audits only for applied
  keys, and cannot write any other key. It does not widen the model-enablement
  primitive's closed three-key authority.
- `FirstRun.UsableModel` (name final at M1) — deterministic compatibility
  predicate and local selection order shared first by detection and later by
  `Models.Catalog`; no catalog/UI dependency in M1.
- Disclosure surfaces: pending/acknowledged marker helper (Home marker file, per
  surface) + renderers — web chat preface, TUI banner line, CLI first-run
  paragraph. Text derives from the audit provenance record. Hosted paths use
  a durable pending→acknowledged state machine: transport admission requires
  acknowledged disclosure; crashes fail closed and may repeat disclosure.
  Per-surface acknowledgement mechanics (v1.1 delivery-ack precedent):
  **web** acknowledges an exact mounted delivery handle bound to the LiveView
  socket — absent, stale, or forged leaves the disclosure `pending` and hosted
  transport inadmissible; a reconnect re-renders and re-acknowledges rather
  than assuming the prior mount delivered. **TUI/CLI** acknowledge after a
  successful write to the output stream, whether or not it is a tty — a
  redirected `allbert ask "x" > out.txt` is acknowledged when the write
  succeeds, matching v1.1's "after every kickoff line is written" rule.
  Per-surface marker keys are updated read-modify-write through the existing
  atomic marker path (`cli/first_run.ex:301-310`); a lost update under
  concurrent first boot of two surfaces may only REPEAT a disclosure, never
  suppress one.
- `AllbertAssist.Models.Catalog` — `list/1` merged rows (source, id,
  size, capabilities, floor, purposes, status), backed by the curated
  catalog file (`priv/model_catalog/*.yaml` or equivalent), Ollama tags,
  configured profiles + doctor status, and (conditional) `llm_db` hosted
  metadata. Registered read-only actions: `list_model_catalog`
  (`exposure: :internal`, `permission: :read_only`); `model_doctor`
  unchanged.
- Fallback: `Settings.Models.candidates_for/2` extended to text
  generation; a bounded failover executor inside the direct-answer path
  (classification: definitive advances, ambiguous advances once, possible
  partial-answer never silently retried); trace + audit events
  (`Runtime.Audit` existing kinds or an additive `:model_fallback` kind —
  M5 decides against the audit-writer precedent). Provider adapters emit a
  typed normalized result; unknown/partially streamed outcomes classify as
  `partial` and are never retried.
- New Settings Central keys (additive fragments, ADR 0031):
  `models.fallback.enabled` (false), `models.fallback.allow_local_to_hosted`
  (false), `models.fallback.max_failovers_per_turn` (1, clamp 1..2),
  `first_run.disclosure_shown.*` only if the marker cannot carry it (prefer
  the Home marker; Settings is not a per-surface flag store).
- Changed policy surfaces: `Workspace.FirstRun` auto-open states (auto-open
  only for repair states, never as a chat gate), `CLI.Tui.readiness_guard`
  inversion + parity for `mix allbert.tui`, chat first-run CTA states per
  the ADR 0087 §4 matrix.
- `mix allbert.test release.v12` — the release gate
  (`@release_v11_steps ++ @v12_focused_steps`).

## Code Seams

**S1a/S1b — atomic enablement spine + detection projection (M1a/M1b,
ADR 0087 §1–§2).** New enablement
module consuming `FirstRun.detect_details/1`; ordering configured-local →
localhost → hosted-key, with LD24's availability-first projection applied
inside `classify_model_probe/3` (`cli/first_run.ex:151-158`) so every unusable
local state reaches a configured hosted rung; the request-flow §D.0 table is the binding spec and
every one of its twelve cells gets a regression; StoreLock-owned atomic
multi-key raw-presence check/write
(LD16, never effective/default-merged presence); M1a-local usable-model
predicate and deterministic hosted-provider priority (LD17–LD18); audited writes of the
existing pair `intent.direct_answer_model_enabled` +
`intent.model_assist_enabled` plus `model_preferences.primary` when unset;
provenance into the audit trail. Wire at boot for web
(`application`/workspace mount path), TUI start, CLI dispatch — behind the
existing `ensure_http_started`/fresh-settings ordering. The two flagship
acceptance scenarios land here as automated regressions against a fresh
temporary Home: (a) provisioned fixture → first `submit_user_input` answer
is model-backed with zero writes beyond the audited pair; (b) empty fixture
→ deterministic fallback + repair CTA state exposed. `sticky_disabled`
proven red-first.

**S2 — surface unblocking + disclosure (M2, ADR 0087 §3–§4).** Invert
`CLI.Tui.readiness_guard` (always start; in-TUI guidance per detect state);
add the same posture to `mix allbert.tui`; change
`Workspace.FirstRun.@auto_open_states` semantics (repair-state auto-open
only; never gates chat); chat CTA per detect state
(`workspace/components/chat.ex:745,:776-793` seam — one primary CTA,
matrix-driven);
disclosure renderers (web preface, TUI banner via the adapter banner seam,
CLI paragraph) off the durable pending→acknowledged marker; hosted transport
admission is impossible before acknowledgement, including `allbert ask`;
`onboarding_complete` decoupling
(marker stays, consumers stop reading it for availability).

**S3 — step-addressable onboarding (M3, ADR 0087 §5).** Generalize
`wizard_rewind` into direct step entry at any time incl. post-completion
(web step list buttons already exist at `onboarding.ex:368-400` — extend to
completed state; CLI `onboard <step>` routes); re-entry/rewind never
re-gates (depends on S2's decoupling); ADR 0069 false-complete rule kept as
a permanent regression; persona/QuickStart flip sites unchanged and
idempotent against an already-auto-enabled Home. Per LD8, `first_chat_ready?/1`
(`onboarding.ex:325-327`) drops its consent-key conjunct so a sticky-disabled
Home can complete the wizard; the model step records an explicit decline and
carries a one-time re-enable affordance. Red-first: a sticky-disabled fresh
Home reaches `onboarding_complete` and the affordance appears exactly once
across restarts.

**S4 — catalog + chooser (M4, ADR 0088 §1–§2).** `Models.Catalog` service +
curated catalog file + `list_model_catalog` action; the M4-entry
verification of `llm_db` (accuracy/footprint/license — recorded in Build
Progress either way, curated-file hosted section as the fallback); web
ModelsPanel upgraded to the chooser (browse by ADR 0072 purpose,
size/capability/floor + doctor status inline, select→existing settings
actions, pull→existing confirmation-gated pull with the live progress
plumbing); TUI/CLI equivalents on the same action; onboarding `model_path`
step re-pointed at the catalog rows.

**S5 — fallback/degradation (M5, ADR 0088 §3).** Extend `candidates_for/2`
consumption to the direct-answer path behind `models.fallback.enabled`;
typed failure normalization table per concrete provider/ReqLLM/streaming
shape (unknown→partial/no retry); the local→hosted
double gate; per-turn failover bound; trace + audit emission; reply
metadata naming a non-primary answerer; whole-chain-failed honest text.
ADR 0088 flips Accepted here (chooser proven at M4 + the egress-gate denial
row green at M5).

**S6 — eval rows + release gate (M7, LD13).** `:v12` enum entry FIRST
(`eval_inventory.ex:58`), then behavioral rows (request flow §G) with the
meta-contract sweep (`v12_sweep_eval_test.exs` mirroring
`v11_sweep_eval_test.exs:61-68` — exact ids, distinct assert sets,
production-boundary execution); gate wiring
`@release_v12_steps = @release_v11_steps ++ @v12_focused_steps` with
runner/evidence clone (`release_evidence/v12/`); docs-gate active-plan list
updated.

## Development Lane Annotation

- **Parallel workstreams:** WS-A (S1a+S1b+S2 — the atomic enablement spine,
  detection projection, and surface
  posture; serial within itself); WS-B (S4 catalog/chooser — independent of
  S1 until the onboarding `model_path` re-point); WS-C (S5 fallback —
  independent of S1/S4 until reply-metadata rendering). S3 rides after S2.
- **Serial barriers:** M0 before everything. M1a before M1b; their rejoin is
  the complete S1 contract and blocks S2 (surfaces render enablement states).
  S2 before S3 (decoupling first). S4 before S5's
  chooser-visible chain edits, but S5's executor can start against
  `model_preferences.tasks.*` directly. M7 after all feature milestones;
  M8 (dependency refresh) after churn settles; M9 last, tag operator-held.
- **Focused tests/gates:** per-milestone commands below; `release.v1` and
  `release.v11` after every contract-adjacent milestone (M1a–M5);
  `inventory --check-tags` after every new test file (new suites take a
  primary lane; detection/enablement suites are `app_env_serial` unless
  their audit proves narrower; `onboarding_test.exs`'s missing lane tag is
  adjudicated at M0); no `mix | tail` pipes; every timing claim cites the
  metrics store.
- **Full-precommit timing:** once at the historical M7 barrier, then once at
  M9 only after FV-01..FV-03 pass on both required hosts **and M9.1 operator
  documentation convergence is accepted**; never inside the active FV
  remediation or M9.1 docs-only loop.
- **External smokes:** none required for M1a–M3. M4/M5 own scripted,
  machine-verifiable smokes: live local Ollama catalog inventory + real pulled
  model selection/answer; the structured Qwen bakeoff; and a bounded real
  local failover smoke. Hosted disclosure belongs to flagship FV-03, not the
  supporting-feature smokes. A required primary bakeoff platform may record
  `BLOCKED` only with explicit operator disposition; other credential or host
  failures record `BLOCKED`, never PASS.
- **Rejoin points:** after M2, an integrated fresh-Home matrix run (all
  detect states × web/TUI/CLI) before M3; docs/criteria reconciliation at
  M9.1 before the final release gates; triad archive + roadmap turnover only
  after published artifact validation.

### Focused command matrix

Commands run from the owner app directory (root `mix test` does not accept
`apps/…` paths). Every run uses an owned temporary Allbert Home.

| Milestone | Primary lane | Binding focused command |
|---|---|---|
| M0 | mixed | `MIX_ENV=test mix allbert.test metrics && cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/cli/first_run_test.exs test/allbert_assist/onboarding_test.exs` (record baselines; adjudicate onboarding lane) |
| M1a | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/settings/store_put_if_absent_test.exs test/allbert_assist/first_run/usable_model_test.exs` |
| M1b | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/first_run/enablement_test.exs test/allbert_assist/first_run/enablement/boot_worker_test.exs test/allbert_assist/first_run/flagship_core_test.exs test/allbert_assist/cli/first_run_test.exs test/allbert_assist/onboarding_test.exs` |
| M2 / LD26 correction | `app_env_serial` + `liveview_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/first_run/disclosure_test.exs test/allbert_assist/first_run/presentation_test.exs test/allbert_assist/settings/store_tui_identity_bootstrap_test.exs test/allbert_assist/cli/tui_test.exs test/allbert_assist/channels/tui_test.exs test/mix/tasks/allbert_tui_test.exs && cd ../allbert_assist_web && MIX_ENV=test mix test test/allbert_assist_web/v12/cross_surface_first_run_test.exs test/allbert_assist_web/workspace/first_run_test.exs test/allbert_assist_web/live/workspace/workspace_onboarding_test.exs` |
| M3 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/onboarding_test.exs test/allbert_assist/cli/areas/onboarding_test.exs && cd ../allbert_assist_web && MIX_ENV=test mix test test/allbert_assist_web/live/workspace/workspace_onboarding_test.exs` |
| M4 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/models/catalog_test.exs test/allbert_assist/settings/model_preferences_test.exs test/allbert_assist/actions/settings_actions_test.exs && cd ../allbert_assist_web && MIX_ENV=test mix test test/allbert_assist_web/live/workspace/workspace_onboarding_test.exs` |
| M5 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/settings/models_fallback_test.exs test/allbert_assist/actions/intent/direct_answer_test.exs` |
| M6 | mixed | integrated fresh-Home matrix suite + `MIX_ENV=test mix allbert.test fast-local` |
| M7 | `security_eval_serial` | `MIX_ENV=test mix allbert.test release.v12` (repo root, first roll target) |
| M8 | mixed | `MIX_ENV=test mix allbert.test release.v12 && MIX_ENV=test mix allbert.test release.v1` after every applied wave |
| M9.1 operator docs | docs + targeted support regression | complete `docs/operator/` inventory and current-behavior audit → directly affected catalog/TUI tests if audit exposes product drift → targeted format check → `MIX_ENV=test mix allbert.test docs` → repository-relative Markdown file/anchor check → `git diff --check`; no precommit, versioned gate, broad lane, or aggregate |
| M9 validation/release barrier | mixed | During macOS/Linux FV remediation: directly affected focused suites → `MIX_ENV=test mix format --check-formatted` → `MIX_ENV=test mix allbert.test docs` → `git diff --check` → document/remediate and rerun invalidated checks → commit + push → verify clean `HEAD == @{upstream}` → rerun only invalidated FV rows on both hosts. Do not run precommit, versioned gates, broad lanes, or aggregate in that loop. After FV-01..03 pass on both hosts: one fresh `MIX_ENV=test mix precommit` → `MIX_ENV=test mix allbert.test release.v1` → `MIX_ENV=test mix allbert.test release.v12` → one uninterrupted `MIX_ENV=test mix allbert.test release`; a failure returns to focused remediation, repeats only invalidated FV, and restarts this entire final barrier → tag/release only after it is green |

## Security Posture

This release moves a consent line and is planned like it:

- **The only relaxation is ADR 0087's**: detection of an already-provisioned
  provider enables direct answers without a wizard step. Compensating
  controls are structural: sticky explicit-false, audited provenance,
  one-time disclosure naming provider + egress class, automatic usable-local-
  first ordering with a binding raw-explicit-primary exception, and zero
  provisioning/egress in detection itself. Gate-bound
  `:v12` rows enforce each (request flow §G).
- **Nothing else grants.** Catalog reads are `permission: :read_only`
  registered actions; chooser writes ride existing settings actions and
  safe-write keys; pulls stay confirmation-gated; fallback never crosses
  local→hosted without the double gate; `Security Central`, the vault
  model, and provider key custody are untouched.
- **No new egress class:** detection probes only loopback/configured-local
  endpoints; hosted "detection" is key presence; catalog hosted metadata is
  static; the only new egress possibility (hosted failover) is double-gated
  and audited.
- Secrets remain encrypted/redacted end-to-end; disclosure and audit text
  never include key material or raw endpoints (doctor envelope rules).

## Non-Goals

- Removing the wizard, the QuickStart/Advanced tracks, or the persona
  system — they persist as the customization surface.
- Removing or migrating legacy `intent.*model_profile` aliases (1.5/1.6
  migration-runner cluster; additive-only rule).
- OAuth-authenticated hosted providers (1.4/1.5 enabler train); the catalog
  lists what exists, it does not add provider auth.
- Bundling a model or model runtime into the artifact (ADR 0078 stands).
- Auto-pull, auto-install, or any zero-click provisioning.
- A global/fleet policy switch disabling detection-based enablement (LD25).
  Per-Home sticky explicit-false is the only control in 1.2; a
  `first_run.auto_enable.*` policy key is a deliberate later decision, not an
  omission.
- Multi-user/remote first-run semantics; mobile-ready web (rides 1.4).
- Per-role fast/capable/thinking profile schema (v1.4, ADR 0090 scope).

## Milestones

Execution modes: `[gate]` deterministic automated contract, `[external-smoke]`
scripted real-runtime/provider check with machine PASS criteria, `[fv]`
focused operator validation limited to the flagship.

- **M0: Recon verify + readiness decisions + baseline.** `[gate]` Start from shipped stable
  v1.1.0. Re-verify every Current Code State anchor at HEAD (drift table in
  Build Progress); record the fresh-Home behavior matrix per surface as the
  before-state; record decide-turn / boot walls from the metrics store;
  adjudicate the `onboarding_test.exs` lane; verify `Notify`-style fragment
  registration is not needed (consent keys already exist); confirm
  `llm_db` version shipped in the lock and stage the M4 verification
  questions; record LD16's atomic raw-presence API contract, LD17's usable-
  model predicate or curated-only disposition, LD18's stable hosted priority,
  and LD21's numerical budgets. Define the cross-platform model-bakeoff
  protocol from LD22 with exact supported host rows, 8/16/32 GB cohorts, and
  promotion/tier thresholds;
  verify public Ollama tags/digests and licenses at execution time; confirm the
  v1.2 triad is present in `@docs_active_plan_files` and remove the two
  archived v1.1 triad entries left there by the v1.1 closeout; re-verify the
  §D.0 derivation table against `classify_model_probe/3` at HEAD.
  *Acceptance:*
  anchor drift table + baseline matrix + lane adjudication + five readiness
  decisions committed before M1a; no curated default changes in M0; baselines
  recorded on the M12.19 one-connection topology and labelled as such so M6
  cannot compare against pre-M12.19 figures.
- **M1a: Atomic enablement spine (S1a, ADR 0087 §1–§2).** `[gate]`
  Multi-key raw-absence primitive; absent-subset validation/commit under one
  lock; per-key audit rows plus one transaction provenance envelope;
  validation-failure rollback; concurrent explicit-disable race; per-key
  stickiness; usable-model selector; stable hosted priority. *Acceptance:*
  injected validation failure writes nothing; stored values on any of the
  three keys survive; double invocation is idempotent; no key outside the
  closed write set is reachable; `release.v1` + `release.v11` green.
- **M1b: Detection projection + flagship core (S1b, ADR 0087 §1–§2).** `[gate]`
  Enablement module with availability-first local-preferred ordering, audited
  provenance, sticky-false red-first, idempotency (double boot, wizard
  re-run, persona apply on an auto-enabled Home); the two flagship
  acceptance scenarios as automated fresh-Home regressions; boot ordering
  hazards covered (`:req` not started → detection defers without crash;
  fresh-process settings read); LD24's availability-first projection landed in
  `classify_model_probe/3` with all twelve §D.0 cells regressed.
  *Acceptance:* scenarios green; no-egress detection proof green; all three
  unusable-local-with-key cells auto-enable hosted while raw-primary-absent
  local-ready still wins; a usable raw-explicit primary is binding and an
  unavailable exact primary abstains; no-key variants remain repair states; `sticky_disabled` and
  `enabled_unavailable` green; no
  non-additive migration; `release.v1` +
  `release.v11` green.
- **M2: Surface unblocking + disclosure (S2, ADR 0087 §3–§4).** `[gate]`
  TUI guard inversion + dev/packaged parity, including LD26's shared local
  launcher activation/identity preparation before Adapter initialization; web auto-open
  policy change;
  chat CTA matrix; disclosure on all three surfaces from the one-time
  marker, text derived from provenance; `onboarding_complete` decoupled.
  *Acceptance:* detect-state × surface matrix suite green; a fresh TUI's first
  normal and identity-requiring slash turns resolve as canonical `local` with
  no setup command; raw-absent channel activation and default identity are one
  atomic write; effective custom profiles and raw-present empty/custom/disabled
  identity state remain authoritative; raw-explicit channel `false` blocks
  before Adapter startup with re-enable guidance; and generic Adapter denial
  remains authoritative. Disclosure and launcher-preparation once-only proofs
  show restart repeats neither audit; rewind no longer re-gates;
  `release.v1` + `release.v11` green.
- **M3: Step-addressable onboarding (S3, ADR 0087 §5).** `[gate]` Direct
  step entry any time incl. post-completion on web + CLI; ADR 0069
  false-complete permanent regression retained; flip-site idempotency.
  *Acceptance:* focused suites green; wizard completion still requires a
  genuinely ready model path (readiness `:ready`) but no longer the consent
  key (LD8); the sticky-disabled completion path and the one-time re-enable
  affordance are green; no consumer of `onboarding_complete` gates
  availability.
- **M4: Model catalog + chooser (S4, ADR 0088 §1–§2).** `[gate]`+`[external-smoke]`
  Catalog service + curated file + `list_model_catalog`; the recorded
  `llm_db` verification decision; web chooser + TUI/CLI lists + onboarding
  `model_path` re-point; selection/pull only through existing actions.
  Live local Ollama spot-check (inventory row appears; selection of a real
  pulled model answers). Execute the LD22 bakeoff on macOS arm64, Linux
  x64/arm64, and WSL2 supported rows. macOS arm64 and Linux x64 are the
  primary promotion rows; one may be `BLOCKED` only with explicit operator
  disposition, while the other two may record `BLOCKED` with the gap named.
  retain `llama3.2:3b` unless the fixed
  promotion/tier rule passes, otherwise change every default/catalog/doc/test
  reference atomically. Any floor increase includes the operator disposition
  and a real below-floor acceptance row — and, per LD24, a below-floor host
  with a hosted key auto-enables hosted rather than dead-ending.
  The catalog/select/answer smoke and bakeoff emit structured evidence and
  calculate thresholds without operator judgment. *Acceptance:* catalog merge suite green (all four
  sources incl. absent-source degradation); chooser write-path proof (no
  writes outside Settings actions); pull remains confirmation-gated;
  `release.v1` + `release.v11` green.
- **M5: Fallback/degradation policy (S5, ADR 0088 §3).** `[gate]`+`[external-smoke]`
  Selection-ordering extension to text; opt-in failover with
  classification, per-turn bound, local→hosted double gate red-first,
  trace+audit emission, honest whole-chain-failed text, reply metadata on
  non-primary answers. ADR 0088 flips Accepted. *Acceptance:* default-off
  proof (provider failure with keys absent behaves exactly as today);
  egress-gate denial row green; audit rows verified; image/voice callers
  unchanged; scripted local failover smoke emits provider-call count,
  answering profile, trace, and audit evidence.
- **M6: Cross-surface acceptance + performance guard.** `[gate]`
  Integrated fresh-Home matrix (every request-flow §D row, derived per §D.0,
  × web/TUI/CLI) as one
  suite; measured detection budget vs the M0 baselines (boot-to-ready delta
  and unchanged decide-turn, cited from the metrics store) against the fixed
  M0 numerical thresholds; selected curated-model cold/warm TTFT and peak-RSS
  rows remain inside the M0 hardware floor; automated browser coverage runs
  the zero-click and empty-state paths on an isolated Home and asserts zero
  console errors. *Acceptance:* matrix and browser suite green; budgets met
  and recorded.
- **M7: Eval rows + release.v12 gate (S6).** `[gate]` `:v12` enum FIRST;
  behavioral rows per request flow §G with the meta-contract sweep;
  `@release_v12_steps = @release_v11_steps ++ @v12_focused_steps` with
  evidence under `release_evidence/v12/`; docs-gate active-plan list
  updated. *Acceptance:* original thirteen-row `release.v12` green first-roll
  target; LD26's corrected fourteen-row gate must pass at the M9 replacement
  candidate; every new
  authority/deny path bound to a gate step; sweep meta-contract enforces
  distinct assert sets and production-boundary execution.
- **M8: Dependency refresh (standing rule).** `[gate]` Three-root
  `hex.outdated` review table, dedupe, bounded applies in own commits,
  dispositions recorded (named carry-items: `req_llm` line, the
  PLV `~> 1.1.0` pin, `llm_db` if adopted at M4). *Acceptance:* table
  complete; scoped suites + `release.v12` + `release.v1` green after every
  wave; M7 evidence superseded by the post-refresh run.
- **M9: Docs, RC, focused validation, binary release + closeout.**
  `[gate]`+`[external-smoke]`+`[fv]` Purpose-bar check against
  every outcome (deferrals need operator sign-off); DIT-2-class criteria
  replaced across `docs/validation/` and operator guides; CHANGELOG
  `## v1.2.0` + version lockstep (three `mix.exs`, SW `CACHE_NAME` + gzip);
  execute and record request-flow §H on macOS and Linux. During remediation,
  run only directly affected focused tests plus format/docs/diff checks as
  applicable, document the result, commit and push, verify a clean
  upstream-equal candidate, and repeat only invalidated FV rows. After all
  three rows pass on both required hosts **and M9.1 is accepted**, run one
  fresh final barrier:
  `mix precommit`, `release.v1`, corrected `release.v12`, then **one
  uninterrupted authoritative `mix allbert.test release` before tag/release**;
  operator-held annotated tag
  `v1.2.0` (no
  `[skip-artifacts]`); CI publish + cosign; tap fill; install rehearsal;
  post-tag archive/roadmap administration. *Acceptance:* Definition of Done
  complete; ledger recorded; triad archived only after published-artifact
  validation.
- **M9.1: Operator documentation convergence (mandatory pre-gate barrier).**
  `[gate]` Runs after both-host FV and before any final precommit or release
  gate. Audit every file under `docs/operator/` against current code, accepted
  ADRs, and the v1.2 request flow. Add `docs/operator/quickstart.md` as the
  canonical install → open → chat path; make onboarding explicitly optional;
  explain the detected-local, detected-hosted, and honest-empty first-run
  outcomes; distinguish packaged `allbert` commands from source-only `mix`
  commands; retain task-focused deep guides for every shipped operator
  capability; rebuild `docs/operator/README.md` as the complete information
  architecture; and route the project `README.md` plus `docs/README.md` to the
  Quickstart. Remove stale claims that a wizard, identity-map command, model
  pull, or hosted probe is required before first chat. Preserve explicit
  confirmation, sticky-disable, one-live-runtime-per-Home, egress disclosure,
  and no-silent-provisioning boundaries. *Acceptance:* every operator document
  is inventoried and linked from the operator index; Quickstart is the unique
  first-run entry; current v1.2 behavior and commands agree across Quickstart,
  install, onboarding, workspace, TUI, models, security, and the README family;
  historical release-validation detail is clearly separated from everyday
  operator instructions; the docs gate, repository-relative link check, and
  `git diff --check` are green. No precommit, versioned gate, broad lane, or
  aggregate may start until this milestone is documented, committed, pushed,
  and accepted.

## Validation Design (the v1.1 churn guardrails, binding)

The v1.1 M12.4–M12.13 corrective chain is the negative example this plan is
built against. Binding rules, each carried by a named milestone above:

1. **No validation-only product capability** (M12.11): nothing ships in
   `lib/` whose sole purpose is producing validation evidence. FV rows use
   the product as an operator would.
2. **The flagship scenarios are automated first** (M12.12): zero-click and
   empty-state land as fresh-Home regressions in M1b/M2; FV re-executes what
   CI already proves, live.
3. **Behavioral eval rows with the meta-contract** (M12.5): no structural
   presence checks, no source-grep proofs, no shared assert triples.
4. **Capability wiring is census-proved, not grep-inferred** (M12.6): the
   surface matrix suite enumerates web/TUI-packaged/TUI-dev/CLI explicitly.
5. **Disposable Homes only** (M12.9): no FV row touches `~/.allbert`; no
   cloned state; runbooks are paste-executable without `set -e`/`exit`.
6. **The active FV remediation loop is focused-only; the final barrier follows
   both-host success.** A correction runs directly affected focused tests plus
   format/docs/diff checks as applicable, is documented, committed, pushed, and
   verified clean and upstream-equal before only invalidated FV rows repeat on
   macOS and Linux. Do not run precommit, versioned gates, broad lanes, or the
   aggregate inside that loop. After all three rows pass on both hosts, run one
   fresh sequence: precommit → `release.v1` → corrected `release.v12` → full
   aggregate. A failure returns to focused remediation, repeats only invalidated
   FV rows, and restarts the entire final barrier; tag/release requires a
   complete green sequence. No renumbering, no ad-hoc rows.
7. **Supporting scope is machine-proved.** Onboarding navigation,
   catalog/chooser, fallback, and model-candidate evaluation have no FV row.
   Their automated suites and scripted external-smoke records are release
   evidence and must be green (or carry an allowed operator-disposed BLOCKED
   host row) before the final release barrier can admit tag/release.

## Documentation Deliverables

- ADR 0087, ADR 0088 (Proposed → Accepted at their proving milestones);
  ADR 0078/0069 amendments already recorded at planning time.
- `docs/plans/v1.2-request-flow.md` maintained as implementation changes.
- Operator docs: first-run/onboarding guide rewritten for the zero-click
  posture; model catalog/chooser guide; fallback policy reference
  (settings, egress gate, audit locations); `model-recommendations.md`
  (ADR 0072) cross-linked to the catalog.
- `docs/validation/`: DIT-2-class criteria replaced per LD14.
- Developer docs: test-strategy lane additions for the new suites;
  detection/enablement module docs.
- `CHANGELOG.md` `## v1.2.0`; roadmap ladder + future-features lifecycle
  updates at closeout (operator-owned).
- Triad archived to `docs/plans/archives/` after artifact validation.

## Definition Of Done

- Detection→enablement shipped with availability-first, local-preferred ordering,
  absent-key-only writes, audited provenance, sticky explicit-false, and
  zero provisioning or hosted probe during detection; hosted inference is
  admitted only after disclosure acknowledgement — `[gate-proved-contract]`.
- All model/readiness states chat-ready per the ADR 0087 §4 matrix; TUI guard
  inverted with dev/packaged parity and a raw-absent-only audited local
  activation/identity preparation; raw-explicit channel disable blocks with
  re-enable guidance; generic Adapter denial, effective-profile selection, and
  explicit identity-map stickiness stay intact; disclosure once-only on
  web/TUI/CLI —
  `[gate-proved-contract]`.
- Onboarding optional + step-addressable; completion decoupled from
  availability; ADR 0069 false-complete regression retained —
  `[gate-proved-contract]`.
- Catalog + chooser live on all surfaces through read-only actions;
  selection/pull only via existing settings/confirmation paths; `llm_db`
  verification recorded — `[gate-proved-contract]` + `[external-smoke]`.
- Fallback policy: default-off proof, egress double gate, per-turn bound,
  audit trail, honest degradation floor — `[gate-proved-contract]` +
  `[external-smoke]`.
- ADR 0087 and ADR 0088 Accepted; DIT-2-class criteria replaced —
  docs + `[gate-proved-contract]`.
- all three FV rows passed on macOS and Linux at the clean pushed
  primary-feature candidate, with only recorded docs/support bridges afterward,
  followed by one green final sequence of `mix precommit`,
  `release.v1`, corrected `release.v12`, and uninterrupted
  `mix allbert.test release` before tag/release — `[gate-proved-contract]`.
  The product correction's versioned barrier was historically green at
  `27a70059`; it does not replace the final post-FV sequence, which remains
  pending.
- FV-01..FV-03 executed on disposable Homes on both macOS and Linux at the
  candidate SHA and recorded — `[operator-attested]`.
- v1.2.0 published as a binary release: cosign-signed GitHub Latest, tap
  filled, install rehearsal recorded — `[operator-attested-smoke]`.
- Dependency disposition table complete; docs deliverables + closeout
  administration complete.
