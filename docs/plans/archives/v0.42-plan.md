# Allbert v0.42 Tool Discovery + MCP-First Integration Pack 1

Request flow: `docs/plans/archives/v0.42-request-flow.md`

ADRs: `docs/adr/0048-tool-discovery-and-discovered-server-trust.md` (binding),
`docs/adr/0039-mcp-first-native-plugin-second-integrations.md` (binding).
Consumed: `docs/adr/0038-mcp-client-trust-tier.md`,
`docs/adr/0011-confirmed-external-capability-adapters.md`,
`docs/adr/0012-resource-access-security-posture.md`,
`docs/adr/0013-uri-first-resource-identity.md`,
`docs/adr/0021-intent-objective-capability-and-advisory-boundary.md`,
`docs/adr/0047-provider-doctor-contract.md`,
`docs/adr/0017-allbert-plugin-contract.md`,
`docs/adr/0033-capability-gap-acquisition-and-trust-tiers.md`.

Status: implemented; fourth-pass closing remediation milestones are tracked
below as M12-M18 before release validation.

Source: `docs/archives/version-1.0-planning-03.md`, post-v0.37 planning pass;
discovery track added in the post-v0.40 planning pass.

## Third-Pass Readiness Sweep

Status: implementation-ready after this documentation pass; code work starts at
M1 by accepting ADR 0048 and confirming the registry-source contract below.

This version is special because it is the first post-v0.41 feature release to
exercise the new development-lane contract while also expanding Allbert's
operator-visible capability surface through external registries, configured MCP
servers, workspace panels, and a native reference plugin. The readiness bar is
therefore higher than "the milestones are plausible": the plan must pin the
trust transitions, persistence surfaces, fixture strategy, serial resources, and
drift handoff before implementation begins.

Third-pass result:

- **Vision fit.** v0.42 keeps the Allbert kernel small: discovery is a
  registered-action family, remote metadata is advisory only, external egress
  reuses `External.HttpPolicy`, connect authority remains a confirmation, and
  workspace panels consume the existing MCP client instead of adding provider
  SDKs to core.
- **Roadmap fit.** v0.42 correctly follows v0.40 MCP client and v0.41
  development-lane work. The discovery gate is useful but not freeze-blocking
  for v1.0 because registry coverage and ranking quality are ecosystem-
  dependent; the connect boundary is the release-critical part.
- **Past-plan fit.** v0.40's first-server matrix is binding: resource-backed
  reads may use remembered grants, but read-like MCP tools remain per-call
  confirmed. v0.41's lane annotations are binding for every milestone below.
- **Future-plan fit.** v0.43 browser research inherits the same "remote content
  is not authority" posture; v0.45 marketplace-lite remains the code-bearing
  discovery path and v0.47 self-improvement may mine v0.42 traces only as inert
  suggestion inputs.
- **ADR fit.** No new ADR is needed if ADR 0048 is amended by M1 to reflect the
  registry-source contract and ADR 0039 is read as "MCP-first with host-owned
  panels." Add a new ADR only if implementation adds code-bearing discovery,
  silent/remembered connect approval, a new registry trust root, or a new
  provider-specific core dependency.

Readiness guardrails:

- M1 must lock whether the optional keyed subregistry source is in scope for
  code or parked. The official MCP Registry is the only required remote source.
- M3/M4/M5 must introduce the discovery persistence substrate before any
  workspace suggestion surface or baseline-hash recheck relies on it.
- M6 must record the per-integration resource-vs-tool inventory and decide the
  calendar read model before panel implementation starts.
- Each milestone must update this plan's request-flow handoff and record the
  focused gate evidence named in the development-lane table.

This release has two tracks in one version. The **discovery track** (M1-M5)
ships first because it is the foundational primitive: a `find_tools` capability
search that fans out to local tools and to internet MCP registries, plus the
confirmation-gated connect gate by which a discovered server becomes real. The
**integration track** (M6-M11) is the original v0.42 scope — calendar, mail, and
GitHub as **MCP-server-configured workspace panels** plus a single `notes/files`
**native reference plugin** — and it now becomes a clean consumer of `find_tools`
(an unconfigured panel can discover and connect a server through the gate rather
than assuming hand configuration). Native plugin variants for
calendar/mail/GitHub remain post-1.0 follow-on and ship only when MCP coverage
proves insufficient for a specific workspace surface or memory-namespace need.

## Purpose

Make Allbert useful for ordinary personal work without growing
provider-specific dependencies in core, and give Allbert a general,
authority-bounded way to answer "is there a tool for this, and if not, can I get
one?"

The discovery track adds `find_tools`: when the operator needs a capability,
Allbert searches its own registered actions, skills, and already-connected MCP
servers (`find_local_tools`) and, in parallel, searches internet MCP registries
(`find_mcp_tools`), returning one ranked list of candidates. A candidate that is
not already usable is connected only through an explicit, confirmation-gated
connect gate that shows the operator the exact run command or remote URL. This is
the reusable primitive the intent engine, the objective runtime (ADR 0033), and
operator queries can all call.

The integration track makes calendar, mail, GitHub, and notes/files arrive as
MCP-configured workspace panels driven by the v0.40 MCP client. The `notes/files`
integration additionally ships as a small native reference plugin to give plugin
authors a concrete starter scaffold.

## Research Grounding

The v0.42 design is grounded in:

- **The official MCP Registry** (`registry.modelcontextprotocol.io`): read API
  frozen at v0.1, defining `GET /v0.1/servers` (params: `search` = name
  substring only, `updated_since`, cursor pagination, `version`), per-version
  endpoints, and a `server.json` schema (`name` reverse-DNS, `description`,
  `version`, `repository`, `packages[]` with `registryType`/`identifier`/
  `transport`/`runtimeHint`/`runtimeArguments`/`environmentVariables`/
  `fileSha256`, and `remotes[]` with `type`/`url`/`headers`). It is **preview
  with no durability guarantee** and performs **no cryptographic payload
  validation**, so Allbert treats it as a non-durable cache and does its own
  verification.
- **Subregistries / aggregators**: third parties (PulseMCP, Glama, Smithery)
  re-implement the same Generic Registry API and add the search/enrichment the
  official registry lacks (semantic search, popularity, security scans). The
  third-pass doc sweep found that PulseMCP's current subregistry API requires
  keyed headers, so v0.42 no longer assumes a no-auth aggregator. The official
  registry is the required remote source; PulseMCP is an optional keyed provider
  only when the operator configures the needed secret refs. Other semantic /
  API-keyed sources (Smithery) remain parked.
- **The cascade / progressive-disclosure pattern** (Anthropic Tool Search,
  Composio tool-calling guide): defer loading tool definitions, search for a
  capability, expand only matching candidates, then act. v0.42 retargets this
  from "pick among tools I already have" to "find a *server* I might add," so the
  terminal step is a human-gated connect, not a tool call.
- **MCP discovery/connection threats** (MCP security best practices, security
  research): SSRF during discovery (the client fetches server-controlled URLs),
  malicious `npx`/`uvx` start commands, tool-poisoning / prompt-injection via
  tool descriptions, registry name-squatting, and rug-pull (post-approval
  definition changes). Mitigations: route every discovery fetch through
  `External.HttpPolicy`; treat all server-supplied text as untrusted metadata;
  mandate pre-configuration consent showing the exact command; hash tool
  definitions at connect and re-verify on reconnect.
- **The Allbert plugin/app contract** for the `notes/files` reference plugin:
  ADR 0017 (plugin contract), ADR 0027 (app/surface contract and memory namespace
  declaration), ADR 0033 (capability vocabulary), the v0.33 intent descriptor and
  handoff substrate, and the v0.32/v0.34/v0.35 workspace shell.

Implementation consequence: discovery adds new read-only actions, one
confirmation-gated connect action, a discovery settings namespace, a tool-source
port, and a passive workspace surface — but no new network primitive (reuses
`External.HttpPolicy`), no new secret store, no new runtime (reuses
`AllbertAssist.Jobs` for scanning), and no new authority root.

## find_tools Port And ToolCandidate Contract

`find_tools` is an orchestrator action that fans out in parallel to adapters
behind a tool-source port and returns one ranked, deduplicated candidate list.

```text
find_tools(need)                       # orchestrator; parallel fan-out + merge + rank
├── find_local_tools(need)             # read-only, no network, no confirmation
│     • Actions.Registry.capabilities/0      (registered actions)
│     • Skills.Registry.list/1               (skills)
│     • configured MCP servers' tools/list   (already connected)
└── find_mcp_tools(need)               # internet cascade, SSRF-guarded, read-only
      • discover_servers → fetch_server_manifest → evaluate_server
      • across official MCP Registry + optional keyed PulseMCP,
        behind a registry provider port
```

Source-port contract (`AllbertAssist.Tools.SourcePort` behaviour):

- `search(need :: String.t(), opts :: map()) :: {:ok, [ToolCandidate.t()]} | {:error, term()}`
- adapters: `AllbertAssist.Tools.Source.Local`, `AllbertAssist.Tools.Source.McpRegistry`
  (the registry adapter itself fans out to registry-provider adapters
  `AllbertAssist.Mcp.Registry.Official` and `AllbertAssist.Mcp.Registry.PulseMcp`
  behind `AllbertAssist.Mcp.Registry.Provider`).

Normalized `ToolCandidate`:

```elixir
%{
  id: String.t(),                   # stable provider/name/version/manifest digest key
  name: String.t(),                 # action name, skill name, or server.json name
  description: String.t(),          # untrusted when source is :remote_mcp
  source: :local_action | :local_skill | :configured_mcp | :remote_mcp,
  usable_now?: boolean(),           # local => true; remote_mcp => false
  requires: :none | :connect_confirmation,
  provenance: map(),                # namespace match, verified/official, signing
  signals: map()                    # popularity, recency, security scan, registry id
}
```

The `usable_now?` / `requires` pair is the enforcement seam: a `:remote_mcp`
candidate is inert descriptive metadata until it passes the connect gate. Tool
descriptions from `:remote_mcp` candidates are surfaced verbatim but never alter
agent behavior (ADR 0038 §2; ADR 0048 §2).

## Discovery Persistence And Evaluation Reports

v0.42 adds a small `AllbertAssist.Tools.Discovery` context backed by SQLite so
the connect gate, background scan, workspace surface, and doctor recheck share
one source of truth instead of passing transient maps between actions.

Persistence contracts:

- `tool_discovery_candidates`: `candidate_id`, `source`, `provider`, `name`,
  `version`, `manifest_hash`, `tool_definition_hash`, `description`,
  `provenance`, `signals`, `evaluation`, `status`, `first_seen_at`,
  `last_seen_at`, `expires_at`, `user_id`. Background scans upsert into this
  table and the passive surface reads from it.
- `mcp_server_trust_records`: `server_id`, `candidate_id`, `name`, `version`,
  `provider`, `manifest_hash`, `tool_definition_hash`, `consent_id`,
  `connected_at`, `last_verified_at`, `status`, `diagnostics`. The connect gate
  writes this on approval; `mcp_doctor_server` compares current tool definitions
  to this baseline and marks rug-pull diagnostics without silently trusting the
  new shape.

`EvaluationReport` is persisted as trace-safe structured data:

```elixir
%{
  candidate_id: String.t(),
  provider: :official | :pulsemcp,
  provenance_level: :unknown | :claimed | :verified | :official,
  danger_flags: [atom()],
  health_status: :unknown | :ok | :degraded | :blocked,
  manifest_hash: String.t(),
  tool_definition_hash: String.t() | nil,
  required_secret_refs: [String.t()],
  diagnostics: [map()]
}
```

The persisted report is still advisory. It ranks candidates and informs consent;
it never grants permission, creates a Resource Access grant, or skips operator
confirmation.

## Discovery Settings (`mcp.discovery.*`)

Add the `mcp.discovery.*` namespace to `AllbertAssist.Settings.Schema`.
Discovery is off by default; the background scan is a paused job by default.

```yaml
mcp:
  discovery:
    enabled: false                 # opt-in master switch for find_mcp_tools
    sources:
      official:
        enabled: true              # registry.modelcontextprotocol.io (no auth)
      pulsemcp:
        enabled: false             # optional keyed subregistry
        api_key_ref: null          # secret://mcp/discovery/pulsemcp_api_key
        tenant_ref: null           # secret://mcp/discovery/pulsemcp_tenant_id
    scan:
      schedule: "paused"           # operator_approved cron; job created paused
      max_results: 25
    registry_allowlist: []         # optional reverse-DNS namespace pins
    registry_denylist: []
    auto_connect: false            # invariant: must stay false (connect is confirmed)
```

Safe-write keys: `mcp.discovery.enabled`, `mcp.discovery.sources.*.enabled`,
`mcp.discovery.sources.pulsemcp.api_key_ref`,
`mcp.discovery.sources.pulsemcp.tenant_ref`,
`mcp.discovery.scan.schedule`, `mcp.discovery.scan.max_results`,
`mcp.discovery.registry_allowlist`, `mcp.discovery.registry_denylist`.
`mcp.discovery.auto_connect` is read-only and validated to remain `false`.

The official registry needs no secret refs. Optional PulseMCP uses
`secret://mcp/discovery/pulsemcp_api_key` and
`secret://mcp/discovery/pulsemcp_tenant_id` if enabled; missing refs disable
that provider and must not fail the whole discovery turn. A future Smithery
adapter would add `secret://mcp/discovery/smithery_key` and is parked.

The connect gate writes the v0.40 `mcp.servers.*` namespace (ADR 0038 §1); no new
server-config shape is introduced.

## Discovery Permission And Operation Classes

Add to `AllbertAssist.Security.Policy` (following the v0.40 MCP precedent):

- `:tool_discovery` — safety floor `:allowed`, default decision `:allowed`.
  Gates `find_mcp_tools` registry egress. Read-only; creates no grants. (Local
  fan-out needs no new authority.)
- `:mcp_server_connect` — safety floor `:needs_confirmation`, default decision
  `:needs_confirmation`. Gates `mcp_server_connect`. Settings can tighten to
  `:denied` but can never loosen below confirmation.

Use `AllbertAssist.Actions.Capability` execution mode `:mcp_discovery` for
read-only discovery actions (the v0.40 MCP action family already uses this
mode), and add `:mcp_server_connect` for the confirmation-gated connect action
if it is not already present. No new
`OperationClass` is required: discovery search touches no Resource Access
identity, and the connect gate writes settings + creates a confirmation.

## Action Schemas And Capability Metadata

All actions register through `AllbertAssist.Actions.Registry` and run through
`AllbertAssist.Actions.Runner.run/3`.

| Action | Permission | Execution mode | Exposure | Notes |
|---|---|---|---|---|
| `find_tools` | `:read_only` | `:mcp_discovery` | `:internal` | Orchestrates local + mcp adapters in parallel; merges + ranks `ToolCandidate`s. |
| `find_local_tools` | `:read_only` | `:mcp_discovery` | `:internal` | Inventories actions, skills, configured-MCP tools. No network. |
| `find_mcp_tools` | `:tool_discovery` | `:mcp_discovery` | `:internal` | Registry cascade; egress via `External.HttpPolicy`. |
| `mcp_fetch_server_manifest` | `:tool_discovery` | `:mcp_discovery` | `:internal` | Normalizes `server.json`; surfaces the exact run command / remote URL. |
| `mcp_evaluate_server` | `:tool_discovery` | `:mcp_discovery` | `:internal` | Provenance + danger-flag + bounded health probe (extends doctor) + baseline tool-def hash. |
| `mcp_server_connect` | `:mcp_server_connect` | `:mcp_server_connect` | `:internal`, resumable | Pre-config consent → write `mcp.servers.<id>` → persist baseline hash. Never auto-connects. |

Action schemas:

| Action | Required params | Optional params |
|---|---|---|
| `find_tools` | `query` | `need` alias, `sources`, `limit` |
| `find_local_tools` | `query` | `need` alias, `limit` |
| `find_mcp_tools` | `query` | `need` alias, `sources`, `limit` |
| `mcp_fetch_server_manifest` | `name` | `version`, `source` |
| `mcp_evaluate_server` | `name` | `version`, `source` |
| `mcp_server_connect` | `candidate_id` | `server_id`, `enable_on_connect` |

`name`/`server_id` use `[A-Za-z0-9_.\-]+`. Resolved run commands/argv, remote
URLs, env, and headers are shown untruncated in the connect consent but redacted
to names/counts in traces and audits; secret values are never rendered.

Integration-track actions (M9 `notes/files` plugin):

| Action | Required params | Mode | Notes |
|---|---|---|---|
| `SearchNotes` (`search_notes`) | `query` | `:read_only` | Reads notes via Resource Access file scope. |
| `ReadNote` (`read_note`) | `path` | `:read_only` | Reads one note via Resource Access. |
| `WriteNote` (`write_note`) | `path`, `content` | confirmation-gated | Writes confirmation-gated via Resource Access. |

## Expected Inputs

- **v0.40 MCP client integration**, accepted through operator use: the
  `mcp_list_tools`, `mcp_list_resources`, `mcp_read_resource`, and
  `mcp_call_tool` actions, `mcp_doctor_server`, the `mcp.servers.*` settings
  namespace, the `secret://mcp/...` refs, the `mcp://` resource identity, and the
  grant-gated resource-read / confirmation-gated tool-call model. v0.40 shipped
  the first-server validation matrix (see "v0.40 Capability Matrix Dependency").
- **`External.HttpClient` / `HttpPolicy`** (ADR 0011/0047): SSRF blocking, host
  rules, bounded timeouts/bodies, redirect denial, redaction — reused for every
  discovery fetch.
- **`AllbertAssist.Actions.Registry`** (`capabilities/0`, `capability/1`) and
  **`AllbertAssist.Skills.Registry`** (`list/1`) for `find_local_tools`.
- **`AllbertAssist.Jobs`** (`create_job/1`, `pause_job/1`, `resume_job/1`,
  `due_jobs/2`, `claim_due_job/2`; `target_type: "registered_action"`,
  `status: "paused"`) for the background scan.
- **`AllbertAssist.Surface`** / `App.SurfaceProvider` (`:canvas_panels` /
  `:utility_drawer` zones, `visible_when: :operator_opened`) for the Discovery
  Suggestions surface and the integration panels.
- **`AllbertAssist.Confirmations`** (`create/2`, `resolve/4`,
  `Confirmations.ResourceMetadata`) for the connect consent.
- **Settings Central** secrets and the `Settings.Fragment` substrate.
- v0.27 app/surface contract and memory namespace declaration; v0.33 app intent
  descriptor and handoff substrate; v0.32/v0.34/v0.35 workspace shell.
- ADR 0048 and ADR 0039 accepted at M1 before implementation closeout.

## Operator-Visible Win

An operator can ask "find me a tool for X" (or "find an MCP server for
calendar") and get a ranked list mixing tools Allbert already has with internet
MCP servers it could add. Connecting a discovered server always shows the exact
command/URL and requires explicit approval; nothing connects silently. With the
opt-in background scan enabled, suggestions accumulate on a passive Discovery
Suggestions panel the operator reviews on their own time.

An operator can also ask for an agenda, mail summary, GitHub overview, or notes
search from `/workspace` or a supported channel. Each integration renders a
workspace summary panel driven by configured MCP tools and resources; an
unconfigured panel can offer to discover and connect a server through the gate.
The operator approves any effectful action through the normal Approval Handoff
path.

A plugin author opening `./plugins/allbert.notes_files/` sees a complete,
minimal worked example of the plugin contract: app behaviour, surface provider,
panel declarations, memory namespace, intent descriptor, registered actions, and
Settings.Fragment — small enough to read in one sitting.

## Planned Interfaces

Discovery:

- `find_tools` / `find_local_tools` / `find_mcp_tools` actions and the
  `AllbertAssist.Tools.SourcePort` + `AllbertAssist.Mcp.Registry.Provider` ports
  with the official adapter required and PulseMCP optional/keyed.
- `mcp_fetch_server_manifest`, `mcp_evaluate_server`, `mcp_server_connect`
  actions under `AllbertAssist.Actions.Mcp.*` and `AllbertAssist.Mcp.Registry.*`.
- `AllbertAssist.Tools.Discovery` persistence for candidates, suggestions,
  evaluation reports, and connected-server baseline hashes.
- Discovery Suggestions workspace panel (passive, `visible_when:
  :operator_opened`).
- `mix allbert.tools find "<need>"`; `mix allbert.mcp discover "<need>"`,
  `mix allbert.mcp connect --candidate-id <id>` or
  `mix allbert.mcp connect <unique-name>`, `mix allbert.mcp scan
  enable|pause|resume|run-once`.
- Intent routing for "find a tool/server for X".

Integration:

- Workspace summary panels (one per integration) under the existing
  `:canvas_panels` zone.
- Intent descriptors per integration so v0.33 handoff works (e.g., "show me
  today's agenda" → calendar panel).
- `./plugins/allbert.notes_files/`: native reference plugin.
  - `AllbertNotesFiles.Plugin` (plugin contract).
  - `AllbertNotesFiles.App` (app + SurfaceProvider contract).
  - Memory namespace declaration: `notes_files`.
  - Intent descriptor: `search_notes`, `read_note`.
  - Registered actions: `SearchNotes`, `ReadNote`, `WriteNote` (writes
    confirmation-gated).
  - Workspace panel: notes list + note detail.
  - Settings.Fragment for notes-files config.
  - Skill pack with two example skills.
- MCP-configured panels for calendar/mail/GitHub (no native plugin in v0.42):
  - MCP server `mcp.servers.calendar` (operator points at e.g. Google
    Calendar MCP server).
  - MCP server `mcp.servers.mail` (operator-chosen mail MCP server).
  - MCP server `mcp.servers.github` (official GitHub MCP server).
  - Each panel renders through the v0.40 actions: `mcp_list_tools` /
    `mcp_list_resources` for discovery, `mcp_read_resource` for summary content
    (gated by a remembered Resource Access grant the operator approves once per
    `mcp://` server scope), and `mcp_call_tool` for effectful actions
    (confirmation-gated). Panels never call MCP servers directly; every effect
    goes through `Actions.Runner.run/3`, Security Central, and confirmations.

## v0.40 Capability Matrix Dependency

v0.40 M5 **shipped** the first-server validation matrix (see
`docs/plans/archives/v0.40-plan.md` M5 implementation notes). v0.42 consumes it directly
rather than re-deriving it; the resolved implications are:

| Integration | Required panel data | v0.40 shipped exposure finding | v0.42 resolution |
|---|---|---|---|
| Calendar | upcoming events, event detail, create/update event | **mostly tools** | Agenda reads are **per-call-confirmed tool calls**, or the panel shows an empty/"configure a resource-exposing server" state. v0.42 does **not** promise prompt-free agenda refresh. (Decided in M7.) |
| Mail | inbox/message summaries, message detail, reply/send | mixed resources and tools | Header/detail summaries use `mcp_read_resource` (remembered grant) when the configured server exposes them; otherwise per-call-confirmed. Sends/replies are confirmed tool calls. |
| GitHub | PR/issue/review summaries, repo/readme detail, comments | mixed resources and tools | Prefer resource reads for inspectable artifacts; mutations and searches stay behind confirmed tool calls. |
| In-repo HTTP / stdio mocks | doctor/list/read/call coverage | resources and tools | CI exercises the canonical grant-gated read and confirmed tool-call paths. |

If a real or mock MCP server exposes essential summary-panel data only through
tools, v0.42 keeps that interaction operator-triggered with per-call
confirmation, parks that panel detail, or amends ADR 0038 before implementation.
It must not treat `:mcp_resource_read` grants as authority for `mcp_call_tool`.

## Why notes/files Is The Native Reference

`notes/files` is the cheapest integration to implement natively (just file IO
plus a tiny workspace panel) and the most relatable for plugin authors
(everyone has notes). StockSage remains the depth reference for financial
workflows, native specialist agents, Python bridge integration, and parity
runs; `notes/files` is the shallow reference that proves the contract is
small enough to use as a starter scaffold.

The `notes/files` plugin is also the only native plugin in v0.42 because the
notes/files use case benefits most from a native memory namespace (operators
want notes promoted into Allbert memory) and a custom workspace panel
(MCP-only would lose the markdown-render affordance).

## Why Other Integrations Stay MCP-Only

- **Calendar**: MCP servers for Google Calendar, CalDAV, and Microsoft Graph
  exist or are emerging. Native plugin would lock Allbert into one provider.
- **Mail**: v0.16 email channel already covers the mail-as-channel surface.
  Mail-as-app via MCP avoids duplicating IMAP/SMTP code.
- **GitHub**: official GitHub MCP server is comprehensive. Native plugin
  would replicate GitHub API client surface in core.

Each can graduate to a native plugin in post-1.0 follow-on releases if a
workspace surface, memory namespace, or intent descriptor need proves the MCP
coverage insufficient. Those releases are not freeze-blocking.

## Security Posture

Discovery:

- `find_mcp_tools` and the manifest/evaluate actions are read-only; every
  registry/server fetch routes through `External.HttpPolicy` (SSRF, host rules,
  bounded timeout/body, redirect denial, redaction). The official registry is a
  non-durable cache: discovery degrades to local-only when it is unreachable.
- Server-supplied text (descriptions, tool schemas) is untrusted metadata and
  never alters agent behavior (ADR 0038 §2).
- `mcp_evaluate_server` flags dangerous run-command patterns (`sudo`, `rm -rf`,
  network egress baked into argv, paths outside expected dirs) and scores
  provenance (namespace match, verified/official, signing); these are advisory
  inputs to the consent, never auto-approve.
- `mcp_server_connect` is the single dangerous transition: confirmation-gated
  (`:mcp_server_connect`, floor `:needs_confirmation`), shows the exact
  untruncated command/URL + required secret refs, writes `mcp.servers.<id>` only
  on approval, and records a tool-definition baseline hash. Reconnect /
  `mcp_doctor_server` re-verifies the hash; a change forces re-consent.
- Background scanning is opt-in (`mcp.discovery.enabled: false` default), a
  paused-by-default `Jobs` job, and writes to a passive surface only — no
  unprompted messaging, no scan-initiated connect.

Integration:

- Integrations are plugin-shaped (not core dependencies). MCP-configured panels
  inherit the v0.40 MCP client trust tier (per ADR 0038). Native `notes/files`
  plugin uses Resource Access for file IO and Security Central for
  permission/confirmation. The notes namespace uses the v0.27 namespace
  declaration contract; v0.47 may suggest memory promotion candidates but never
  auto-promotes.

## Non-Goals

Discovery:

- No auto-connect and no remembered/silent connect approval.
- No proactive push messaging about discovery results (passive surface only).
- No capability-gap-triggered remote acquisition in v0.42 (ADR 0033 objectives
  may consult `find_local_tools`; gap-triggered discovery is parked).
- No semantic registry source in v0.42. PulseMCP is optional and keyed only if
  M1 keeps it in scope with Settings secret refs; Smithery and other keyed
  semantic providers are parked.
- No code-bearing plugin install through discovery — MCP servers only.
- No community trust scoring or registry-moderation authority.

Integration:

- No native plugin for calendar, mail, or GitHub in v0.42 (post-1.0
  follow-on if needed).
- No core dependency on Google APIs, GitHub APIs, or mail provider SDKs.
- No silent credential reuse across unrelated integrations.
- No automatic memory promotion from notes or mail.
- No hosted multi-user authorization model.
- No replacement of StockSage as the depth reference app.

## Development-Lane Annotations

Per ADR 0049 §6, every milestone carries a development-lane annotation. v0.42 is
back-filled here as the worked example (the contract was introduced in v0.41). All
implementation milestones rejoin through the full release gate, which must
reproduce the v0.40 regression oracle green set (see `docs/developer/test-strategy.md`).

| M | Parallel workstreams | Serial barriers | Gate evidence + rejoin |
|---|---|---|---|
| M1 | ADR acceptance; `mcp.discovery.*` schema; port/`ToolCandidate` contract; optional PulseMCP secret-ref decision | `Security.Policy` + `Settings.Schema` shared modules; app-env/schema cache tests stay serial | focused: settings/policy/capability/normalizer tests, `mix compile --warnings-as-errors`; rejoin: ADR 0048/0039 text, schema, and policy green before M2 |
| M2 | local source adapter; orchestrator; `mix allbert.tools`; no-network tests | `Actions.Registry` registration and any configured-MCP mock process ownership | focused: `find_tools`/`find_local_tools` pure tests, registry tests; rejoin: local candidates registered, zero transport calls asserted |
| M3 | official adapter; optional keyed PulseMCP adapter if M1 keeps it; evaluate; mock-registry fixtures | registry-provider registration; `External.HttpPolicy`; discovery persistence migration; HTTP stub ownership | focused: adapter parse, pagination, keyed-provider disabled/missing-secret, SSRF/redirect/timeout fixtures; lane: db_serial for persistence + no live net; rejoin: mocks and persistence green |
| M4 | consent metadata; settings-write path; baseline trust record | `Confirmations`, `Settings`, `Tools.Discovery`, and `:mcp_server_connect` policy are serial; confirmation resume path is shared | focused: consent + baseline-hash + denial/fail-closed tests; lane: confirmation/db_serial; rejoin: approved/denied connect green |
| M5 | suggestions panel; scan `Jobs` job; intent routing | `Jobs` scheduled/named process, `IntentAgent`/`Engine`/`Ranker`, `Surface` registration, persisted suggestion reads | focused: scan lifecycle + intent + panel render; lanes: db_serial + liveview_serial; rejoin: no-proactive-message asserted, suggestions visible |
| M6 | docs + per-integration inventory/config fixtures | none beyond settings schema validation | docs + schema-validate fixture; rejoin: config examples validate and calendar read model is locked before M7 |
| M7 | calendar panel; mail panel | `Surface` registration; MCP Resource Access grant flow; confirmation for tool-only/effectful paths | focused: panel render (mocks) + create-event/reply confirmation; lanes: liveview_serial + resource_access/db_serial; rejoin: calendar/mail green |
| M8 | github panel; intent | `Surface` registration; MCP Resource Access grant flow; confirmation for comments/mutations | focused: GitHub panel render + comment confirmation; lanes: liveview_serial + resource_access/db_serial; rejoin: GitHub green |
| M9 | plugin modules; skill pack; settings fragment; panel | plugin registration, memory namespace declaration, Resource Access file IO, `write_note` confirmation | focused: plugin load/actions/settings/namespace tests; lanes: home_fs_serial + security_eval; rejoin: no-auto-promote/isolation green |
| M10 | per-integration descriptors | `IntentAgent`/`Engine`/`Ranker` shared ranking behavior | focused: intent routing and descriptor-proposes-not-authorizes tests; rejoin: routing green |
| M11 | docs reconciliation; eval inventory; release evidence | `EvalInventory`, security eval lane, external smokes, release gate | full v0.42 security_eval lane, focused v0.39-v0.40 regression tests, `mix allbert.test release`; rejoin: roadmap/request-flow/docs match implementation |

## Implementation Order

### Milestone 1: Discovery Contract, Settings, Permissions, ADRs

Status: implemented.

Implementation tasks:

- Accept `docs/adr/0048-tool-discovery-and-discovered-server-trust.md` and
  `docs/adr/0039-mcp-first-native-plugin-second-integrations.md`.
- Add the `AllbertAssist.Tools.SourcePort` behaviour and the
  `AllbertAssist.Mcp.Registry.Provider` behaviour with the `ToolCandidate`
  contract (struct + normalizer).
- Add `mcp.discovery.*` to `AllbertAssist.Settings.Schema` with defaults, schema
  entries, safe-write keys, and validation (including `auto_connect` pinned to
  `false`, official enabled by default, and optional PulseMCP disabled unless
  its secret refs are configured).
- Add `:tool_discovery` (floor `:allowed`) and `:mcp_server_connect` (floor
  `:needs_confirmation`) to `AllbertAssist.Security.Policy`
  (`@permission_settings`, `@default_decisions`, `safety_floor/1`,
  `@known_permissions`, `reason/5`).
- Reuse `:mcp_discovery` for read-only discovery execution mode and add
  `:mcp_server_connect` to `AllbertAssist.Actions.Capability`.
- Add `docs/plans/archives/v0.42-request-flow.md` planned-flow sections for discovery and
  mark unimplemented behavior clearly.

Acceptance criteria:

- `mcp.discovery.*` resolves to the documented defaults; `enabled` is `false`,
  official source is enabled, optional PulseMCP source is disabled, and
  `auto_connect` is `false` and rejects any write that sets it `true`.
- `Security.Policy.permission_classes/0` includes `:tool_discovery` and
  `:mcp_server_connect`; `safety_floor(:mcp_server_connect)` is
  `:needs_confirmation` and settings cannot loosen it.
- The `ToolCandidate` normalizer enforces `usable_now? == false` and
  `requires == :connect_confirmation` for `source: :remote_mcp`.

Automated tests:

- Schema validation tests for each `mcp.discovery.*` key, including
  `auto_connect` tighten-only-to-false enforcement and PulseMCP missing-secret
  disabled behavior.
- Policy tests for both classes: default decision, safety floor,
  floor-cannot-be-loosened, `reason/5` text.
- Capability metadata tests for the new execution modes.
- `ToolCandidate` normalizer tests for the local/remote invariants.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-m1-home
rm -rf "$ALLBERT_HOME"
mix run --no-start -e 'IO.inspect(AllbertAssist.Settings.get("mcp.discovery.enabled")); IO.inspect(AllbertAssist.Settings.put("mcp.discovery.auto_connect", true, %{actor: "local", channel: :test})); IO.inspect(Enum.member?(AllbertAssist.Security.Policy.permission_classes(), :mcp_server_connect)); IO.inspect(AllbertAssist.Security.Policy.safety_floor(:mcp_server_connect))'
```

Expected behavior:

- `mcp.discovery.enabled` defaults to `false`.
- the `auto_connect: true` write is rejected with a read-only/validation error.
- `:mcp_server_connect` is a known permission class with floor
  `:needs_confirmation`.

Request-flow handoff:

- Update `v0.42-request-flow.md` with the discovery settings/permission/port
  contract and the `ToolCandidate` shape.

M1 validation evidence:

- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m1_raw_home DATABASE_PATH=/private/tmp/allbert_v042_m1_raw_db/allbert_test.db mix ecto.migrate.allbert --quiet`
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m1_raw_home DATABASE_PATH=/private/tmp/allbert_v042_m1_raw_db/allbert_test.db mix allbert.test.raw test/allbert_assist/settings_test.exs test/allbert_assist/security/permission_gate_test.exs test/allbert_assist/security/security_central_test.exs test/allbert_assist/action_test.exs test/allbert_assist/tools/tool_candidate_test.exs` — 67 tests, 0 failures.
- `MIX_ENV=test mix compile --warnings-as-errors`

### Milestone 2: `find_local_tools` And The `find_tools` Orchestrator

Status: implemented.

Implementation tasks:

- Add `AllbertAssist.Tools.Source.Local` implementing `SourcePort.search/2` over
  `Actions.Registry.capabilities/0`, `Skills.Registry.list/1`, and the
  `tools/list` of MCP servers already configured/enabled in `mcp.servers.*`.
- Add the `find_local_tools` action (`AllbertAssist.Actions.Tools.FindLocalTools`,
  `:read_only`, `:internal`).
- Add the `find_tools` orchestrator action
  (`AllbertAssist.Actions.Tools.FindTools`, `:read_only`, `:internal`) that runs
  enabled sources in parallel, merges, dedupes by `{source, name}`, and ranks
  (local first by relevance; remote ranked in M3).
- Register both actions; add `Mix.Tasks.Allbert.Tools` with a `find` subcommand.
- In M2, `find_tools` runs the local source only; the mcp source is a
  fail-closed placeholder until M3.

Acceptance criteria:

- `find_local_tools` returns `ToolCandidate`s for matching registered actions,
  skills, and connected-MCP tools, all `usable_now?: true, requires: :none`.
- `find_tools` returns merged, deduped local results and performs no network I/O
  in M2.
- No discovery action creates a grant or authorizes anything.

Automated tests:

- `find_local_tools` matches by capability/keyword across actions, skills, and a
  configured mock MCP server's tools.
- `find_tools` parallel-merge/dedup/rank tests; assert zero transport calls in
  M2.
- Capability/exposure tests (`:read_only`, `:internal`).

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-m2-home
rm -rf "$ALLBERT_HOME"
mix allbert.tools find "settings"
mix allbert.ask --trace "what tools do I have for working with settings?"
```

Expected behavior:

- `mix allbert.tools find` lists local candidates (e.g. settings actions) with
  `usable_now?: true`.
- the runtime prompt routes to `find_local_tools`/`find_tools` and lists local
  tools; no network call is made.

Request-flow handoff:

- Update `v0.42-request-flow.md` with the local fan-out and `find_tools`
  orchestration flow.

M2 validation evidence:

- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m2_raw_home2 DATABASE_PATH=/private/tmp/allbert_v042_m2_raw_db2/allbert_test.db mix ecto.migrate.allbert --quiet`
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m2_raw_home2 DATABASE_PATH=/private/tmp/allbert_v042_m2_raw_db2/allbert_test.db mix allbert.test.raw test/allbert_assist/tools/finder_test.exs test/allbert_assist/actions/tools_actions_test.exs test/allbert_assist/actions/registry_test.exs test/mix/tasks/allbert_tools_test.exs test/allbert_assist/tools/tool_candidate_test.exs test/allbert_assist/agents/intent_agent_test.exs` — 49 tests, 0 failures.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m2_raw_home2 DATABASE_PATH=/private/tmp/allbert_v042_m2_raw_db2/allbert_test.db mix allbert.tools find settings` — listed local usable candidates including `list_settings`.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m2_raw_home2 DATABASE_PATH=/private/tmp/allbert_v042_m2_raw_db2/allbert_test.db mix allbert.ask --trace "what tools do I have for working with settings?"` — routed to `find_tools` and completed.
- `MIX_ENV=test mix compile --warnings-as-errors`

### Milestone 3: `find_mcp_tools` Registry Cascade

Status: implemented.

Implementation tasks:

- Add `AllbertAssist.Mcp.Registry.Provider` adapters
  `AllbertAssist.Mcp.Registry.Official` and
  `AllbertAssist.Mcp.Registry.PulseMcp`. The current official registry docs
  expose `GET /v0.1/servers` with `limit`, `cursor`, and `updated_since`; there
  is no documented `search` parameter, so the adapter fetches a bounded
  cursor-paginated page window and applies local query matching. PulseMCP's
  public beta API is unauthenticated, but Allbert still honors the v0.42 settings
  contract by skipping that source unless the configured MCP secret refs resolve.
  Both providers route egress through `External.HttpClient`/`HttpPolicy`.
- Add `AllbertAssist.Tools.Discovery` migrations/context for candidates,
  evaluation reports, suggestions, and baseline trust records before the
  background scan or connect gate depend on persisted state. Implemented via
  `tool_discovery_candidates`, `tool_discovery_evaluation_reports`,
  `tool_discovery_suggestions`, and `tool_discovery_baseline_trust_records`.
- Add `AllbertAssist.Tools.Source.McpRegistry` fanning out to enabled providers,
  normalizing to `ToolCandidate` (`source: :remote_mcp`, `usable_now?: false`),
  merging + ranking by relevance, provenance, and popularity.
- Add the `find_mcp_tools` action (`AllbertAssist.Actions.Mcp.FindTools`,
  permission `:tool_discovery`) and the
  `mcp_fetch_server_manifest` and `mcp_evaluate_server` actions.
- `mcp_evaluate_server` scores provenance, flags dangerous run commands, runs a
  bounded health probe (extends `mcp_doctor_server`), and computes the
  tool-definition baseline hash.
- Wire `find_mcp_tools` into the `find_tools` orchestrator.
- Add deterministic in-repo mock-registry fixtures (official-shape and optional
  keyed-provider shape) under `test/support`; no live network in CI. Degrade to
  local-only when a source is unreachable.

Acceptance criteria:

- `find_mcp_tools` returns ranked `:remote_mcp` candidates from the mock
  registries, all `usable_now?: false, requires: :connect_confirmation`, and
  upserts remote candidates into `AllbertAssist.Tools.Discovery`.
- All registry/server egress is subject to `HttpPolicy`; an unreachable source
  yields a degraded (local-only or partial) result, not a failed turn.
- A missing optional PulseMCP secret disables that provider without failing the
  official-registry search.
- `mcp_evaluate_server` returns an `EvaluationReport` with provenance level,
  dangerous-command flags, health status, and a stable tool-definition hash.
- Server-supplied descriptions are surfaced as metadata and never executed or
  treated as authority.

Automated tests:

- Registry adapter parse tests against mock `server.json` (official + optional
  keyed-provider shapes), including cursor pagination and name-substring search.
- Missing-secret test for the optional keyed provider.
- SSRF/timeout/redirect-bound tests for registry egress (reuse external
  fixtures), including a server-controlled URL pointing at a private IP.
- `mcp_evaluate_server` provenance/danger-flag/hash tests, including a
  dangerous-argv fixture.
- Persistence tests for candidate upsert, expiry, and stable `candidate_id`.
- Degraded-mode test: an unreachable source returns partial/local-only results.

Validation evidence:

- `MIX_ENV=test mix compile --warnings-as-errors` — clean.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m3_raw_home2 DATABASE_PATH=/private/tmp/allbert_v042_m3_raw_db2/allbert_test.db mix ecto.migrate.allbert --quiet` — clean.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m3_raw_home2 DATABASE_PATH=/private/tmp/allbert_v042_m3_raw_db2/allbert_test.db mix allbert.test.raw apps/allbert_assist/test/allbert_assist/mcp/registry/official_test.exs apps/allbert_assist/test/allbert_assist/mcp/registry/pulse_mcp_test.exs apps/allbert_assist/test/allbert_assist/tools/discovery_test.exs apps/allbert_assist/test/allbert_assist/tools/mcp_registry_source_test.exs apps/allbert_assist/test/allbert_assist/actions/mcp_discovery_actions_test.exs apps/allbert_assist/test/allbert_assist/tools/finder_test.exs apps/allbert_assist/test/allbert_assist/actions/tools_actions_test.exs apps/allbert_assist/test/allbert_assist/actions/registry_test.exs` — 28 tests, 0 failures.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m3_raw_home2 DATABASE_PATH=/private/tmp/allbert_v042_m3_raw_db2/allbert_test.db mix allbert.tools find settings` — 24 local candidates, discovery disabled by default, no remote egress.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-m3-home
rm -rf "$ALLBERT_HOME"
mix allbert.settings set mcp.discovery.enabled true
# mock registries are served by the test harness
mix allbert.mcp discover "calendar"
mix allbert.tools find "calendar"
```

Expected behavior:

- `mix allbert.mcp discover` lists remote candidates with provenance/popularity
  signals, all marked not-yet-usable.
- `mix allbert.tools find` interleaves local and remote candidates with a clear
  usable-now vs requires-connect distinction; traces redact server hosts.

Request-flow handoff:

- Update `v0.42-request-flow.md` with the discover → fetch-manifest → evaluate
  cascade and the registry provider port.

### Milestone 4: The Connect Gate

Status: implemented.

Implementation notes:

- Added `AllbertAssist.Actions.Mcp.ConnectServer`
  (`mcp_server_connect`, permission `:mcp_server_connect`, execution mode
  `:mcp_server_connect`, internal/resumable/confirmation-required). The action
  resolves a persisted `candidate_id`, builds a pre-configuration consent with
  the exact stdio command/argv or HTTP/SSE URL, required secret refs, transport,
  and evaluation report, and writes nothing before approval.
- Added `AllbertAssist.Mcp.ConnectSpec` to derive a settings-ready server config
  from descriptive registry metadata. Stdio npm packages become exact
  `npx -y <package>@<version>` commands; PyPI packages become exact `uvx`
  commands; HTTP/SSE transports preserve the exact URL/auth-ref shape.
- On approval, `mcp_server_connect` writes `mcp.servers.<id>` through Settings
  Central (`enabled: false` unless `enable_on_connect: true`) and persists a
  durable `mcp_server_trust_records` baseline through
  `AllbertAssist.Mcp.ServerTrust`.
- `mcp_doctor_server` re-verifies an approved server's live `tools/list` hash
  against the trust baseline and emits `:tool_definition_changed` when the tool
  definition set changes. If no baseline is present, or the trust store is
  unavailable to a non-DB test harness, doctor behavior remains the existing
  reachability/discovery summary.
- Invalid or missing manifests fail closed with no confirmation and no settings
  write.

Acceptance criteria:

- A discovered candidate cannot be configured without an approved consent; the
  consent shows the exact command/URL untruncated.
- On approval, `mcp.servers.<id>` is written and a baseline trust record is
  stored; on denial nothing is written.
- A subsequent doctor run with a changed tool-definition set reports a rug-pull
  diagnostic and does not silently re-trust.
- The connect consent and audits redact secret values (names/counts only).

Automated tests:

- Consent-required test: connect with no approval writes nothing; with approval
  writes `mcp.servers.<id>`.
- Consent content test: exact command present untruncated; secrets shown as
  refs/names only.
- Baseline trust-record persist + rug-pull re-verification test.
- Denial/fail-closed test for invalid/missing manifest.

Validation evidence:

- `MIX_ENV=test mix compile --warnings-as-errors` — clean.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m4_raw_home DATABASE_PATH=/private/tmp/allbert_v042_m4_raw_db/allbert_test.db mix ecto.migrate.allbert --quiet` — clean.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m4_raw_home DATABASE_PATH=/private/tmp/allbert_v042_m4_raw_db/allbert_test.db mix allbert.test.raw apps/allbert_assist/test/allbert_assist/actions/mcp_connect_actions_test.exs apps/allbert_assist/test/allbert_assist/actions/mcp_discovery_actions_test.exs apps/allbert_assist/test/allbert_assist/actions/mcp_actions_test.exs apps/allbert_assist/test/allbert_assist/actions/confirmations_actions_test.exs apps/allbert_assist/test/allbert_assist/actions/registry_test.exs apps/allbert_assist/test/allbert_assist/tools/discovery_test.exs apps/allbert_assist/test/allbert_assist/tools/mcp_registry_source_test.exs` — 34 tests, 0 failures.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-m4-home
rm -rf "$ALLBERT_HOME"
mix allbert.settings set mcp.discovery.enabled true
mix allbert.mcp discover "calendar"
mix allbert.mcp connect --candidate-id "<id from discover>"   # deny first
mix allbert.mcp connect "io.example/calendar"                  # approve if name is unique
mix allbert.mcp doctor calendar
rg -i 'token|secret' "$ALLBERT_HOME/settings" "$ALLBERT_HOME/memory/traces" || true
```

Expected behavior:

- the first connect is denied/pending at consent and writes nothing.
- the approved connect writes `mcp.servers.<id>` and records a baseline hash; the
  consent showed the exact command/URL.
- doctor reports reachability and (if tool defs changed) a rug-pull diagnostic.
- the final `rg` prints no matches.

Request-flow handoff:

- Update `v0.42-request-flow.md` with the connect consent flow, settings write,
  and baseline-hash re-verification.

### Milestone 5: Background Scan, Suggestions Surface, Intent Routing

Status: implemented.

Implementation notes:

- Added `AllbertAssist.Tools.Discovery.Scan` as the managed scan facade around
  the `AllbertAssist.Jobs` job named `mcp-discovery-scan`. `enable/1` turns on
  `mcp.discovery.enabled` and creates/refreshed the job as `paused`;
  `resume/1` and `run_once/2` fail closed while discovery is disabled.
- Added `Discovery.list_suggestions/1` and the passive
  `AllbertAssist.Workspace.DiscoverySuggestions` surface. The panel reads only
  pending suggestions by default, keeps dismissed/denied records auditable in
  the store, and uses short stable DOM node ids so long remote candidate ids
  remain data, not invalid surface identifiers.
- Added the `workspace:discover` destination and Chrome/LiveView connect
  affordance routing. The button calls `mcp_server_connect(candidate_id: ...)`
  and surfaces the M4 approval handoff; it never writes or enables a server
  before approval.
- Updated the web surface-provider merge so the host-owned `CoreApp` provider is
  always canonical for Allbert workspace panels, even when registry-provided
  static Allbert surfaces are present.
- Extended `Jobs.Runner.run_now/2` to accept a narrow `:action_context` overlay,
  preserving production behavior while allowing deterministic registry stubs in
  scan tests.
- Added `mix allbert.mcp discover`, `mix allbert.mcp connect`, and
  `mix allbert.mcp scan enable|pause|resume|run-once` CLI surfaces.
- Extended deterministic intent coverage so "find me an MCP server for X"
  routes to `find_tools`.

Implementation tasks:

- Add a background discovery scan as an `AllbertAssist.Jobs` job
  (`target_type: "registered_action"` running `find_mcp_tools` over configured
  `scan` settings), created `status: "paused"` and gated by
  `mcp.discovery.enabled`.
- Add the passive "Discovery Suggestions" workspace surface
  (`AllbertAssist.Surface` panel, `:utility_drawer`/`:canvas_panels`,
  `visible_when: :operator_opened`) rendering accumulated persisted
  `:remote_mcp` candidates with a "review / connect" affordance routing to
  `mcp_server_connect(candidate_id: ...)`.
- Add `mix allbert.mcp scan enable|pause|resume|run-once`.
- Add deterministic intent routing in `AllbertAssist.Agents.IntentAgent` /
  `Intent.Engine` / `Intent.Ranker` for "find a tool/server for X" → `find_tools`.
- No unprompted messaging; scans never connect.

Acceptance criteria:

- The scan job exists `paused` by default and runs only after the operator
  enables discovery and resumes it.
- Scan results populate the passive surface; Allbert sends no proactive message.
- "find a tool/server for X" phrasing routes to `find_tools`.
- A connect from the suggestions surface goes through the M4 consent gate.
- Expired or denied candidates are hidden from the default suggestions view but
  remain auditable in the discovery store.

Automated tests:

- Job lifecycle tests (created paused; resume schedules; run produces
  suggestions).
- Surface render test: suggestions appear; connect affordance routes to
  `mcp_server_connect`; no proactive-message side effect asserted.
- Intent routing tests for discovery phrasing.
- CLI scan lifecycle and fail-closed tests.
- LiveView route test for the Discover destination and approval handoff.

Validation evidence:

- `MIX_ENV=test mix compile --warnings-as-errors` — clean.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m5_raw_home3 DATABASE_PATH=/private/tmp/allbert_v042_m5_raw_db3/allbert_test.db mix do ecto.migrate.allbert --quiet + allbert.test.raw apps/allbert_assist/test/allbert_assist/tools/discovery_scan_test.exs apps/allbert_assist/test/allbert_assist/workspace/discovery_suggestions_test.exs apps/allbert_assist/test/allbert_assist/tools/discovery_test.exs apps/allbert_assist/test/allbert_assist/agents/intent_agent_test.exs apps/allbert_assist/test/allbert_assist/jobs_test.exs apps/allbert_assist/test/mix/tasks/allbert_jobs_test.exs apps/allbert_assist/test/mix/tasks/allbert_mcp_test.exs apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs apps/allbert_assist/test/allbert_assist/workspace/catalog_test.exs` — 137 tests, 0 failures.
- Chrome extension browser validation against
  `http://localhost:4042/workspace?destination=workspace:discover` with a seeded
  pending MCP suggestion — Discovery panel rendered the suggestion and Connect
  opened an `mcp_server_connect` approval dialog without auto-connecting.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-m5-home
rm -rf "$ALLBERT_HOME"
mix allbert.settings set mcp.discovery.enabled true
mix allbert.mcp scan run-once
mix allbert.ask --trace "find me an MCP server for github"
PORT=4041 mix phx.server   # open /workspace, view the Discovery Suggestions panel
```

Expected behavior:

- `scan run-once` records suggestions to the passive surface without messaging
  the operator.
- the prompt routes to `find_tools` and returns mixed local/remote candidates.
- the workspace panel lists suggestions with a connect affordance that triggers
  the consent gate.

Request-flow handoff:

- Update `v0.42-request-flow.md` with the scan-job, suggestions-surface, and
  intent-routing flows.

### Milestone 6: Integration Capability Inventory

Status: implemented.

Implementation notes:

- Added the v0.42 integration capability inventory to
  `docs/operator/mcp-servers.md`, covering calendar, mail, GitHub, and
  notes/files.
- Locked the calendar read model for M7: agenda refresh is resource-backed only
  when the configured calendar MCP server exposes read-only resources; otherwise
  the panel must use an operator-triggered confirmed tool call or a clear
  empty/configure state.
- Added operator-guide config examples for `mcp.servers.calendar`,
  `mcp.servers.mail`, and `mcp.servers.github` using the shipped
  JSON-aware `mix allbert.settings set` input path.
- Added a documentation fixture test that extracts those exact operator-guide
  examples and runs each `mix allbert.settings set` command through
  `Mix.Tasks.Allbert.Settings`, proving the examples parse through Settings
  Central.
- Confirmed notes/files stays on the existing native file Resource Access and
  file confirmation path; no MCP grant or new permission class is introduced.

Implementation tasks:

- Produce the integration capability inventory: for calendar, mail, and GitHub,
  which MCP server(s), which `mcp.servers.<id>` config shape, which v0.40 actions
  drive each panel, and which data is resource-backed vs tool-only, referencing
  v0.40's shipped matrix (see "v0.40 Capability Matrix Dependency").
- Lock the calendar read model before M7: resource-backed agenda refresh only
  when the configured server exposes resources; otherwise the panel uses
  operator-triggered per-call-confirmed tool reads or a clear empty state.
- Add per-integration `mcp.servers.calendar/mail/github` config examples to the
  operator guide that match the implemented input path (v0.40 JSON-aware
  `mix allbert.settings set`).
- Confirm `notes/files` file IO maps to existing Resource Access file scopes and
  the existing file permission/confirmation path (no new permission class).

Acceptance criteria:

- The inventory names, per integration, the server, config keys, driving v0.40
  actions, and resource-vs-tool exposure with the resulting panel UX.
- Operator-guide config examples parse through the implemented settings input
  path.
- Calendar has no undecided exposure tension left for M7.

Automated tests:

- Documentation/fixture test asserting each integration's config example is
  valid against the `mcp.servers.*` schema.

Validation evidence:

- `MIX_ENV=test mix compile --warnings-as-errors` — clean.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m6_raw_home DATABASE_PATH=/private/tmp/allbert_v042_m6_raw_db/allbert_test.db mix do ecto.migrate.allbert --quiet + allbert.test.raw apps/allbert_assist/test/allbert_assist/integrations/capability_inventory_test.exs apps/allbert_assist/test/mix/tasks/allbert_settings_test.exs apps/allbert_assist/test/allbert_assist/settings_test.exs apps/allbert_assist/test/allbert_assist/mcp/client_test.exs apps/allbert_assist/test/allbert_assist/actions/mcp_actions_test.exs` — 49 tests, 0 failures.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-m6-home
rm -rf "$ALLBERT_HOME"
mix allbert.settings set mcp.servers.calendar.transport streamable_http
mix allbert.settings set mcp.servers.calendar.base_url https://example-calendar-mcp.invalid/mcp/
mix allbert.mcp doctor calendar || true
```

Expected behavior:

- the calendar server config writes validate against the schema.
- doctor returns a redacted reachability envelope (or a clear unreachable
  result against the invalid host).

Request-flow handoff:

- Update `v0.42-request-flow.md` with the per-integration config + driving-action
  map.

### Milestone 7: MCP-Configured Calendar And Mail Panels

Status: implemented.

Implementation tasks:

- Add calendar and mail workspace summary panels under `:canvas_panels`, driven
  by the v0.40 MCP actions (`mcp_list_tools`/`mcp_list_resources` discovery,
  `mcp_read_resource` for resource-backed summaries, `mcp_call_tool` for effects).
- Implement the M6 calendar read-model decision: agenda reads are resource-
  backed with a remembered grant only when the configured server exposes
  resources; otherwise the panel uses operator-triggered per-call-confirmed tool
  calls or a "configure a resource-exposing calendar server" empty state. The
  panel must not imply prompt-free agenda refresh when the server is tool-only.
- For mail, use `mcp_read_resource` (remembered grant) for header/detail
  summaries when available; sends/replies are confirmed tool calls.
- Offer a "discover & connect a server" affordance (M4 gate) when the integration
  is unconfigured.
- Panels never call MCP servers directly.

Acceptance criteria:

- With a configured (mock) calendar server, the agenda panel renders and its read
  path matches the decided model (resource-grant or per-call-confirmed); no false
  prompt-free promise.
- With a configured (mock) mail server, the inbox summary renders via
  `mcp_read_resource` when exposed and via confirmed tool calls otherwise.
- An unconfigured panel shows an empty state with a discover/connect affordance.
- One effectful flow per panel (create event / reply) routes through Approval
  Handoff.

Automated tests:

- Panel render tests against the mock servers for resource-backed and tool-only
  shapes.
- Effectful-flow confirmation tests (create event, reply).
- Empty-state + discover-affordance test for an unconfigured panel.

Implementation notes:

- Added `AllbertAssist.Workspace.McpIntegrationPanels`, a host-owned surface
  builder for `workspace:calendar` and `workspace:mail`. The panels render under
  `:canvas_panels` and use only `Actions.Runner.run/3` for
  `mcp_list_resources`, `mcp_list_tools`, `mcp_read_resource`, and
  `mcp_call_tool`; they do not call the MCP client or transport directly.
- Calendar and mail resource summaries are read during panel refresh only when a
  matching `mcp://` Resource Access grant already exists. Without a remembered
  grant, the panel shows an approval action instead of creating a confirmation
  during render.
- Tool-only calendar and mail summary paths render explicit per-call-confirmed
  actions. Calendar never presents tool-only agenda refresh as prompt-free.
- Unconfigured panels show a discover action (`find_tools`) plus an explicit
  link to the Discovery Suggestions destination, where connect still routes
  through the M4 `mcp_server_connect` consent gate.
- Added LiveView event plumbing for `run_mcp_integration_action` and
  `discover_mcp_integration`; create-event and reply affordances route through
  Approval Handoff before any MCP transport call.

Validation evidence:

- `MIX_ENV=test mix compile --warnings-as-errors` — clean.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m7_raw_home DATABASE_PATH=/private/tmp/allbert_v042_m7_raw_db/allbert_test.db mix do ecto.migrate.allbert --quiet + allbert.test.raw apps/allbert_assist/test/allbert_assist/workspace/mcp_integration_panels_test.exs apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs` — 63 tests, 0 failures.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m7_verify_home DATABASE_PATH=/private/tmp/allbert_v042_m7_verify_db/allbert_test.db mix do ecto.migrate.allbert --quiet + allbert.test.raw apps/allbert_assist/test/allbert_assist/workspace/mcp_integration_panels_test.exs apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs:152 apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs:173 apps/allbert_assist/test/allbert_assist/workspace/catalog_test.exs apps/allbert_assist/test/allbert_assist/integrations/capability_inventory_test.exs` — 20 tests, 0 failures.
- Chrome extension browser validation on `PORT=4043`: verified
  `workspace:calendar` renders `Server calendar`, `Confirmed Agenda Read`, and
  `Create Event`; clicking `Create Event` showed Approval Handoff for
  `mcp_call_tool`. Verified `workspace:mail` renders `Server mail`,
  `Confirmed Inbox Summary`, and `Reply`; clicking `Reply` showed Approval
  Handoff for `mcp_call_tool` / `reply_message`.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-m7-home
rm -rf "$ALLBERT_HOME"
# configure mock calendar + mail servers via the harness
mix allbert.ask --trace "show me today's agenda"
mix allbert.ask --trace "summarize my inbox"
PORT=4041 mix phx.server   # view calendar + mail panels
```

Expected behavior:

- agenda and inbox panels render from configured MCP tools/resources per the
  decided read model.
- an effectful action routes through Approval Handoff; traces leak no secrets.

Request-flow handoff:

- Update `v0.42-request-flow.md` with the calendar/mail panel flows and the
  decided calendar read model.

### Milestone 8: MCP-Configured GitHub Panel

Status: implemented.

Implementation tasks:

- Add the GitHub workspace summary panel under `:canvas_panels`, driven by the
  v0.40 MCP actions: resource reads for PR/issue/readme artifacts; confirmed tool
  calls for comments/mutations and searches.
- Offer the discover/connect affordance when unconfigured.

Acceptance criteria:

- With a configured (mock) GitHub server, the panel renders PR/issue summaries
  via resource reads where exposed; comments/mutations are confirmed tool calls.
- The unconfigured panel shows the discover/connect affordance.

Automated tests:

- GitHub panel render tests (resource-backed summaries) and confirmed-mutation
  test (comment).

Implementation notes:

- Extended `AllbertAssist.Workspace.McpIntegrationPanels` with the
  `workspace:github` panel using the same host-owned, MCP-action-only pattern as
  M7 calendar/mail. The panel lists configured server resources/tools through
  `mcp_list_resources` and `mcp_list_tools`, never by calling the MCP client or
  transport directly.
- Resource-backed GitHub summaries render through `mcp_read_resource` only when a
  remembered `mcp://` Resource Access grant already applies. Without an exposed
  resource or grant, the panel shows a read/search `mcp_call_tool` affordance
  with `needs_confirmation` status.
- GitHub comments and compatible mutation/comment tools route through
  `mcp_call_tool` and Approval Handoff. The shipped panel recognizes
  `create_issue_comment`, `add_issue_comment`,
  `create_pull_request_review_comment`, `comment_on_issue`, and
  `create_comment` as effectful comment affordances.
- Added the `workspace:github` destination to the workspace catalog and
  destination rail; unconfigured GitHub shows the same discover/connect
  affordance as calendar/mail, backed by `find_tools` and the M4 connect gate.

Validation evidence:

- `MIX_ENV=test mix compile --warnings-as-errors` — clean.
- `MIX_ENV=test ALLBERT_HOME=/private/tmp/allbert_v042_m8_verify_home DATABASE_PATH=/private/tmp/allbert_v042_m8_verify_db/allbert_test.db mix do ecto.migrate.allbert --quiet + allbert.test.raw apps/allbert_assist/test/allbert_assist/workspace/mcp_integration_panels_test.exs apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs:194 apps/allbert_assist/test/allbert_assist/workspace/catalog_test.exs apps/allbert_assist/test/allbert_assist/integrations/capability_inventory_test.exs` — 21 tests, 0 failures.
- `git diff --check` and `mix format --check-formatted` — clean.
- Chrome extension browser validation on `PORT=4044`: verified
  `workspace:github` renders `Server github`, `Confirmed GitHub Summary`, and
  `Comment`; activating `Comment` showed Approval Handoff for `mcp_call_tool` /
  `create_issue_comment`.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-m8-home
rm -rf "$ALLBERT_HOME"
# configure mock github server via the harness
mix allbert.ask --trace "list my open PRs"
PORT=4041 mix phx.server   # view github panel
```

Expected behavior:

- the GitHub panel renders PR/issue summaries; a comment routes through Approval
  Handoff.

Request-flow handoff:

- Update `v0.42-request-flow.md` with the GitHub panel flow.

### Milestone 9: `notes/files` Native Reference Plugin

Status: implemented.

Implementation tasks:

- Created `./plugins/allbert.notes_files/` implementing the full plugin contract:
  `AllbertNotesFiles.Plugin` (ADR 0017), `AllbertNotesFiles.App` (+
  `SurfaceProvider`), the `notes_files` memory namespace declaration (v0.27), the
  `search_notes`/`read_note` intent descriptor (v0.33), the registered actions
  `SearchNotes`/`ReadNote`/`WriteNote` (writes confirmation-gated), a notes
  list + detail workspace panel, a `Settings.Fragment`, and a two-skill skill
  pack.
- File IO routes through Resource Access refs; no auto-promotion to memory.

Implementation notes:

- `AllbertNotesFiles.Plugin` is a shipped plugin discovered through normal
  source-tree plugin discovery and compiled through the reviewed shipped-plugin
  path.
- `AllbertNotesFiles.App` registers the `:notes_files` app id, a non-writable
  `:notes_files` memory namespace, app-scoped settings
  `apps.notes_files.notes_root` / `apps.notes_files.max_results`, and workspace
  list/detail panels available at `app:notes_files`.
- `search_notes` and `read_note` use `:read_only` and emit `file://`
  `read_local_path` Resource Access refs. `write_note` uses the dedicated
  `:notes_file_write` permission, has a `needs_confirmation` safety floor, emits
  `write_local_path` refs, and resumes only after durable confirmation approval.
- Approval resume support is intentionally action-specific (`write_note`) rather
  than a generic plugin write bypass.

Acceptance criteria:

- The plugin loads through normal plugin discovery; its surfaces/actions/settings
  register without granting authority by registration.
- `search_notes`/`read_note` work read-only; `write_note` is confirmation-gated.
- The notes namespace never auto-promotes; promotion remains a separate confirmed
  action.
- The plugin is small enough to read in one sitting and is documented as the
  reference scaffold.

Automated tests:

- Plugin load/registration test (surfaces, actions, namespace, settings
  fragment).
- Action tests: read paths read-only; `write_note` requires confirmation.
- Namespace isolation + no-auto-promote test.

Validation evidence:

- `MIX_ENV=test mix compile --warnings-as-errors` — clean.
- `MIX_ENV=test mix test ../../plugins/allbert.notes_files/test` — 9 tests,
  0 failures.
- `MIX_ENV=test mix test ../../plugins/allbert.notes_files/test test/allbert_assist/security/security_central_test.exs test/allbert_assist/settings_test.exs`
  — 54 tests, 0 failures.
- `MIX_ENV=test mix test test/allbert_assist_web/live/workspace_live_test.exs`
  from `apps/allbert_assist_web` — 59 tests, 0 failures.
- Chrome extension browser validation on `PORT=4044`: verified
  `app:notes_files` renders `Notes/files`, `onboarding.md`, `Onboarding`, and
  the seeded note preview/body from `/private/tmp/allbert_v042_m9_browser_home`.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-m9-home
rm -rf "$ALLBERT_HOME"
mix allbert.ask --trace "find notes about onboarding"
mix allbert.ask --trace "write a note titled scratch with body hello"   # confirm
PORT=4041 mix phx.server   # view notes_files panel
```

Expected behavior:

- notes search returns read-only results; the note write routes through Approval
  Handoff before writing.
- the notes panel renders list + detail; nothing auto-promotes to memory.

Request-flow handoff:

- Update `v0.42-request-flow.md` with the notes_files plugin flow and reference-
  scaffold cross-link.

### Milestone 10: Intent Descriptors Per Integration

Status: implemented.

Implementation tasks:

- Registered intent descriptors so v0.33 handoff routes "show agenda" →
  calendar, "summarize inbox" → mail, "list my PRs" → GitHub, and "find notes
  about X" → notes_files, each proposing handoff (not authorizing execution).

Implementation notes:

- `AllbertAssist.App.CoreApp` now contributes inert descriptors for the
  host-owned MCP integration panels. Each descriptor points at a read-only,
  agent-exposed panel action (`open_calendar_panel`, `open_mail_panel`,
  `open_github_panel`) and carries a workspace destination
  (`workspace:calendar`, `workspace:mail`, `workspace:github`).
- Intent handoffs can now carry an optional `destination`. Accepting a
  destination handoff opens that workspace destination and dismisses the
  handoff; it does not set active app context, run an MCP action, or resubmit
  the prompt.
- `allbert.notes_files` continues to own the notes descriptor path through its
  plugin app. `find notes about X` proposes a `notes_files/search_notes`
  handoff and remains read-only unless a later confirmed write action is chosen.

Acceptance criteria:

- Each phrasing routes to the right integration panel/action via the v0.33
  descriptor; descriptors propose, never authorize.

Automated tests:

- Intent routing tests for each integration phrase; descriptor-proposes-not-
  authorizes assertion.

Validation evidence:

- `MIX_ENV=test mix compile --warnings-as-errors` — clean.
- `MIX_ENV=test mix test test/allbert_assist/intent/descriptor_test.exs test/allbert_assist/intent/handoff_test.exs test/allbert_assist/intent/engine_test.exs`
  — 28 tests, 0 failures.
- `MIX_ENV=test mix test test/allbert_assist_web/live/workspace_live_test.exs`
  from `apps/allbert_assist_web` — 60 tests, 0 failures.
- Chrome extension browser validation on `PORT=4044`: asked
  "show me today's agenda", verified the `Open Calendar?` handoff, accepted it,
  and verified navigation to `workspace:calendar` with the calendar panel
  rendered and the handoff dismissed.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-m10-home
rm -rf "$ALLBERT_HOME"
mix allbert.ask --trace "show me today's agenda"
mix allbert.ask --trace "summarize my inbox"
mix allbert.ask --trace "list my open PRs"
mix allbert.ask --trace "find notes about onboarding"
```

Expected behavior:

- each prompt routes to the corresponding integration via its intent descriptor.

Request-flow handoff:

- Update `v0.42-request-flow.md` with the per-integration intent-descriptor
  routing.

### Milestone 11: Security Evals, Docs, Closeout

Status: implemented.

Evidence:

- Added milestone `:v042` discovery/integration rows to
  `AllbertAssist.SecurityFixtures.EvalInventory` and executable coverage in
  `AllbertAssist.Security.V042DiscoveryIntegrationEvalTest`.
- Focused M11 eval command:
  `MIX_ENV=test mix test test/security/v042_discovery_integration_eval_test.exs test/security/security_eval_case_test.exs`
  passed (`11 tests, 0 failures`).
- Release gate components passed after closeout fixes:
  `mix precommit` (`1200` core tests, `112` web tests, `197` StockSage tests,
  `11` Telegram/Email/Notes-files tests, all `0 failures`) and `mix dialyzer`
  (`0 errors`).
- Version metadata is now `0.42.2`; README, CHANGELOG, roadmap, request-flow,
  future-features, vision, agent-context-map, developer MCP notes, and
  security-hardening were reconciled to the implemented v0.42 closeout scope.

Implementation tasks:

- Add discovery + integration rows to
  `AllbertAssist.SecurityFixtures.EvalInventory` (milestone `:v042`) and the
  corresponding eval tests:
  - discovery: `mcp-discovery-ssrf-001`,
    `mcp-discovery-tool-poisoning-inert-001`,
    `mcp-discovery-rug-pull-detection-001`,
    `mcp-discovery-supply-chain-command-flag-001`,
    `mcp-discovery-server-impersonation-001`,
    `mcp-discovery-consent-before-connect-001`,
    `mcp-discovery-registry-unavailable-degrades-001`,
    `mcp-discovery-schema-not-authority-001`.
  - integration: `integration-core-dependency-deny-001`,
    `integration-credential-scope-001`, `integration-resource-grant-001`,
    `integration-memory-no-auto-promote-001`,
    `integration-mcp-native-boundary-001`,
    `notes-files-reference-plugin-action-boundary-001`,
    `notes-files-namespace-isolation-001`.
- Write/extend operator + developer docs (see Documentation Deliverables).
- Reconcile `docs/plans/roadmap.md`, `docs/plans/future-features.md`,
  `docs/plans/allbert-jido-vision.md`,
  `docs/developer/agent-context-map.md`, and
  `docs/operator/security-hardening.md`.
- Bump version metadata; update README and CHANGELOG.

Acceptance criteria:

- All discovery + integration evals pass; the SSRF, schema-not-authority,
  consent-before-connect, and rug-pull fixtures assert the documented floors and
  zero unintended connects/transport calls.
- Operator and developer guides exist and match the shipped actions.
- Roadmap, future-features, vision, ADR 0048/0039/0038, agent-context-map, and
  security-hardening agree on the discovery + integration scope and the v0.42 →
  v0.43 handoff.
- Release gate passes.

Automated tests:

- The full v0.42 eval suite.
- Existing v0.39/v0.39b/v0.40 suites still pass (no regression in onboarding,
  doctor, active memory, MCP client, or the intent surface).

Operator closeout test:

```sh
export ALLBERT_HOME=/tmp/allbert-v042-closeout-home
rm -rf "$ALLBERT_HOME"
mix allbert.settings set mcp.discovery.enabled true
mix allbert.mcp discover "calendar"
mix allbert.mcp connect --candidate-id "<id from discover>"   # approve
mix allbert.ask --trace "find me an MCP server for github"
mix allbert.ask --trace "show me today's agenda"
mix allbert.ask --trace "find notes about onboarding"
rg -i 'token|secret' "$ALLBERT_HOME/settings" "$ALLBERT_HOME/memory/traces" || true
PORT=4041 mix phx.server   # /workspace: suggestions + integration panels
```

Expected behavior:

- discovery/connect run through consent; integration panels render; notes_files
  works as the reference plugin.
- traces/audits are inspectable and redacted; the final `rg` prints no matches.

Request-flow handoff:

- Mark `v0.42-request-flow.md` as the final implemented flow; confirm roadmap
  points to v0.43 as the next milestone.

### Milestone 12: Closing Remediation - Discovery Permission Boundary

Status: implemented in 0.42.1.

Source: `v0.42-remediation-01.md` R1; post-implementation audit pass 2 P0.

Problem:

- The unified `find_tools` action is `:read_only`, but its default source fanout
  includes `McpRegistry`. When `mcp.discovery.enabled=true` and
  `permissions.tool_discovery=denied`, unified `find_tools` can still perform
  remote registry egress. ADR 0048 binds remote discovery to
  `:tool_discovery`; local fanout may stay read-only.

Implementation tasks:

- Keep the action-level permission for `find_tools` as `:read_only`.
- Before including `AllbertAssist.Tools.Source.McpRegistry`, evaluate
  `:tool_discovery` through `Security.PermissionGate`.
- When `:tool_discovery` is denied, return local candidates only plus a bounded
  diagnostic for the skipped MCP registry source.
- Preserve the direct `find_mcp_tools` action's existing `:tool_discovery`
  gate.
- Update operator/developer docs to describe unified `find_tools` as a local
  fanout plus a conditional remote branch.

Acceptance criteria:

- `mcp.discovery.enabled=true` plus `permissions.tool_discovery=denied` yields
  no remote registry HTTP call from unified `find_tools`.
- Local action, skill, and already-configured MCP candidates still return.
- `find_mcp_tools` remains denied through its direct permission path.
- A security eval row covers the bypass.

Automated tests:

- `find_tools` action test for denied remote discovery.
- Finder/source diagnostic-shape test.
- CLI/intent coverage for paths that invoke unified `find_tools`.
- New eval row `mcp-discovery-permission-boundary-001`.

Lane annotation:

- `security_eval` for the new eval row; `pure_async` for action/source tests.
- Rejoin gate: focused tests green before M13 starts.

### Milestone 13: Closing Remediation - Live Connected-Server Trust Baseline

Status: implemented in 0.42.1.

Source: `v0.42-remediation-01.md` R2; post-implementation audit pass 2 P0.

Problem:

- Connect currently stores a manifest-derived `tool_definition_hash`.
  `mcp_doctor_server` later compares that value to a live `tools/list` hash.
  Registry manifests often omit `tools`, so an unchanged server can false
  positive as a rug-pull, while manifest and live-tool claims are conflated.

Implementation tasks:

- Split the trust concepts:
  - `manifest_definition_hash`: descriptive registry/evaluation metadata.
  - `connected_tool_definition_hash`: live `tools/list` hash used by doctor.
  - `baseline_status`: `live_captured`, `pending_live_verification`, or
    `stale`.
- Add a migration for existing `mcp_server_trust_records`: carry the old hash
  into `manifest_definition_hash`, leave `connected_tool_definition_hash` null,
  and mark existing rows `pending_live_verification`.
- On approved connect with `enable_on_connect=true`, write settings, initialize
  the server, call live `tools/list` once, and store the live baseline when it
  succeeds.
- If the server is disabled, missing credentials, or live capture fails, store a
  pending baseline.
- On first successful doctor for a pending baseline, capture the live hash and
  emit an informational diagnostic unless an operator decision requires a
  second explicit confirmation.
- Amend ADR 0048 to record the live-baseline rule.

Acceptance criteria:

- A manifest without `tools` and an unchanged live server does not produce a
  rug-pull diagnostic.
- Changed live tools after baseline capture still produce
  `tool_definition_changed`.
- Disabled or credential-missing connects store explicit pending-baseline
  status.
- Consent and doctor outputs label manifest hashes as registry metadata, not
  the trust authority.

Automated tests:

- Migration test for existing records.
- Connect tests for immediate live baseline and pending baseline.
- Doctor tests for pending capture, no false positive, and changed live tools.
- Rewritten `mcp-discovery-rug-pull-detection-001` plus new
  `mcp-discovery-rug-pull-no-false-positive-001`.

Lane annotation:

- `db_serial` for migration/trust-record writes.
- `security_eval` for rug-pull evals.
- Rejoin gate: focused connect, doctor, migration, and eval tests green before
  M14.

### Milestone 14: Closing Remediation - Public Contract Reconciliation

Status: implemented in 0.42.1.

Source: `v0.42-remediation-01.md` R4.

Problem:

- The plan/request-flow use `need` and examples such as
  `mix allbert.mcp connect <name>`, while the implementation uses `query` and
  requires `candidate_id`.

Implementation tasks:

- Keep `query` and `candidate_id` canonical.
- Accept `need` as an alias for `query` in `find_tools`, `find_local_tools`, and
  `find_mcp_tools`.
- Add explicit `mix allbert.mcp connect --candidate-id ID`.
- Allow bare connect input to resolve exact candidate id first, then exact
  candidate name if unique; ambiguous names return a disambiguation list.
- Update request-flow and operator docs so examples run as written.

Acceptance criteria:

- `find_tools(%{need: "calendar"})` and
  `find_tools(%{query: "calendar"})` produce equivalent results.
- `mix allbert.mcp connect "io.example/calendar"` works when exactly one
  candidate has that name.
- `mix allbert.mcp connect --candidate-id <id>` remains the unambiguous form.
- Duplicate names are handled without connecting the wrong server.

Lane annotation:

- `pure_async` for action/CLI tests and doc edits.
- Rejoin gate: request-flow operator commands work in a disposable
  `ALLBERT_HOME`.

### Milestone 15: Closing Remediation - Reference Plugin And Local Resilience

Status: implemented in 0.42.1.

Source: `v0.42-remediation-01.md` R8, R10, and R9 when small enough not to
delay M12/M13.

Implementation tasks:

- Rewrite the notes/files reference `SKILL.md` files to use canonical flat
  skill metadata keys:
  `allowed-tools`, `metadata.allbert.kind`, `metadata.allbert.actions`,
  `metadata.allbert.permissions`, `metadata.allbert.confirmation`,
  `metadata.allbert.memory-effects`, and `metadata.allbert.trace-effects`.
- Remove the noncanonical top-level `capability:` frontmatter.
- Add plugin tests that load the reference skills and assert no
  `unknown_frontmatter_field` or `unknown_allbert_metadata` warnings.
- Change `Tools.Source.Local` so a failing skills registry degrades with a
  diagnostic instead of crashing direct local discovery.
- Reject discovered HTTP/SSE URLs with userinfo or credential-shaped query
  parameters (`token`, `api_key`, `key`, `secret`, `password`, `bearer`,
  `access_token`, `auth`) before persisting them into settings, if the change
  remains contained.

Acceptance criteria:

- Notes/files skills validate against registered actions and permissions.
- Direct `find_local_tools` still returns local action/configured-MCP
  candidates if skill listing fails.
- Registry-provided credential-looking endpoint URLs are rejected with a clear
  diagnostic and are not persisted as `base_url`.

Lane annotation:

- `pure_async` for skill/frontmatter and local-source tests.
- Optional `security_eval` row for credential-shaped URL rejection.
- Rejoin gate: focused plugin, local source, and `ConnectSpec` tests green.

### Milestone 16: Closing Remediation - Honest Integration Effects

Status: implemented in 0.42.2.

Source: `v0.42-remediation-01.md` R3.

Problem:

- Calendar, mail, and GitHub effect buttons prove the confirmation route, but
  they currently submit placeholder arguments. The product language should
  either ship real minimal workflows or be downgraded to "confirmed tool-call
  skeleton."

Default decision:

- Build minimal per-integration forms rather than downgrading the release claim.

Implementation tasks:

- Calendar: form for summary and start/end or a natural-language event body.
- Mail: form for target message/thread id plus reply body.
- GitHub: form for target issue/PR plus comment body.
- Confirmation summaries show domain arguments, not only `tool_name`.
- Approving each confirmation calls MCP `tools/call` with the submitted
  arguments.

Acceptance criteria:

- One realistic create-event, reply, and comment flow reaches confirmation and
  post-approval tool invocation with the submitted arguments.
- Chrome-extension browser validation covers changed workspace UI and Approval
  Handoff behavior.

Verification:

- `MIX_ENV=test mix test test/allbert_assist/workspace/mcp_integration_panels_test.exs`
  passed: 7 tests, 0 failures.
- `MIX_ENV=test mix test test/allbert_assist_web/live/workspace_live_test.exs`
  passed: 60 tests, 0 failures.
- Chrome-extension validation covered calendar/mail/GitHub MCP effect forms and
  calendar Approval Handoff submission.

Lane annotation:

- `liveview_serial` for workspace UI tests and browser validation.
- Rejoin gate: focused panel/LiveView tests green and Chrome validation
  recorded.

### Milestone 17: Closing Remediation - Deterministic v0.42 Release Smoke

Status: implemented in 0.42.2.

Source: `v0.42-remediation-01.md` R5.

Implementation tasks:

- Add a deterministic `mix allbert.test release.v042` gate, or an equivalent
  scoped release command, that runs without external network.
- Exercise local+remote discovery, denied connect, approved connect, live
  baseline doctor pass, changed-baseline doctor failure, scan suggestions,
  one read and one confirmed effect per integration, notes/files read and
  confirmed write, and secret grep of settings/traces.
- Write redacted closeout evidence under
  `<ALLBERT_HOME>/release_evidence/v042/`.

Acceptance criteria:

- The gate runs in CI and produces reproducible evidence.
- Request-flow operator validation points at this deterministic gate instead of
  relying only on a manual command list.

Verification:

- `mix allbert.test release.v042` passed with 59 core v0.42 tests, 10
  notes/files tests, 60 workspace LiveView tests, and 0 failures.
- Secret scan passed and redacted evidence was written under
  `<ALLBERT_HOME>/release_evidence/v042/`.

Lane annotation:

- `home_fs_serial` for release evidence writes.
- Rejoin gate: the release smoke runs green before final 0.42 validation.

### Milestone 18: Closing Remediation - Deferred Schema, Identity, And Docs

Status: v0.43 M1 carry-forward unless required by M13.

Source: `v0.42-remediation-01.md` R6, R7, R11, R13, R14, R15, R16.

Implementation tasks:

- Expand discovery persistence only where needed by M13; otherwise fold the
  full documented schema reconciliation into v0.43 M1.
- Keep the current logical `ToolCandidate.id` stable and add an immutable
  `candidate_version_id` / manifest snapshot for exact consent history in
  v0.43 M1.
- Add `docs/developer/mcp-servers-schema.md` and wire operator examples to that
  schema-document pattern.
- Add GitHub resource-read and `read_note` intent-routing coverage if not
  already subsumed by M16/M17.
- Reconcile `docs/operator/mcp-servers.md`, `docs/developer/mcp-client.md`,
  notes/files Resource Access docs, and the v0.43 source line.

Acceptance criteria:

- Any claim left in v0.42 docs is either implemented by M12-M17 or explicitly
  moved to v0.43 M1.
- v0.43 starts from a clean handoff with no hidden P0/P1 v0.42 debt.

## Documentation Deliverables

- Operator discovery guide: enabling discovery, the scan job, reviewing
  suggestions, and the connect consent (`docs/operator/mcp-servers.md` update).
- Operator integration setup guide (how to point at each MCP server).
- Developer notes for the `find_tools` source port, registry provider port, and
  discovery actions (`docs/developer/mcp-client.md` update).
- Developer guide using `./plugins/allbert.notes_files/` as the worked
  reference example (`docs/developer/how-to-create-an-allbert-app.md` update).
- Security-hardening notes for discovery threat surfaces (SSRF-on-discovery,
  tool-poisoning, rug-pull, supply-chain command flagging, pre-config consent)
  plus credentials, grants, and memory namespace isolation.
- Agent context map entries for the discovery action class and the integration
  class.
- Future-features reconciliation: park the discovery deferred remainder
  (capability-gap-triggered discovery, semantic/Smithery sources, additional
  keyed registries beyond an explicitly configured PulseMCP source, auto-connect,
  community trust scoring) and keep native integration plugin variants parked.

## Definition Of Done

v0.42 is done when:

- `find_tools` fans out to `find_local_tools` and `find_mcp_tools` and returns one
  ranked, normalized candidate list distinguishing usable-now local tools from
  connect-required remote MCP servers;
- a discovered MCP server can be connected only through the confirmation-gated
  `mcp_server_connect` consent that shows the exact command/URL, writes
  `mcp.servers.<id>`, and records a tool-definition baseline hash, with reconnect
  re-verification; nothing auto-connects;
- background discovery is opt-in, paused-by-default, and writes only to a passive
  Discovery Suggestions surface with no unprompted messaging;
- each of calendar/mail/GitHub has a usable MCP-configured workspace panel driven
  by an operator-configured MCP server (or a clear empty state with a
  discover/connect affordance), with the calendar read model decided;
- `./plugins/allbert.notes_files/` is a working native plugin a developer can
  copy as a starting point for their own plugin;
- at least one effectful affordance per integration routes through registered
  actions + confirmation (create event, reply, comment, write note);
- focused tests/evals prove discovery fetches stay within `External.HttpPolicy`,
  server metadata never becomes authority, connect requires consent, rug-pulls
  are detected, no integration bypasses Allbert's action boundary, and no
  integration auto-promotes content into markdown memory;
- `docs/plans/archives/v0.42-request-flow.md` matches the implementation; roadmap,
  future-features, vision, ADRs, operator/developer guides, agent-context-map,
  and security-hardening reflect the shipped state.
