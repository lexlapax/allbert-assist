# Allbert v0.66 Request Flow - Product RC & No-Docs Validation

Status: **released — tagged `v0.66.0` (2026-07-11), GitHub Latest** (built M1–M11 from the
implementation-readiness-deepened triad; inputs v0.64.3 and v0.65.0 released before it).
Verification is two-layer — a deterministic `release.v066` gate for
contract/routing/boundary invariants plus operator-attested evidence for the
install/browser/model/cross-platform claims; eval rows below are tagged accordingly.

Implementation details live in `docs/plans/archives/v0.66-plan.md`.

## Source Documents

- `docs/plans/archives/v0.66-plan.md`
- `docs/plans/archives/v0.59-plan.md` through `docs/plans/archives/v0.63-plan.md`
- `docs/plans/archives/v0.60b-plan.md`
- `docs/plans/archives/v0.61b-plan.md`
- `docs/plans/archives/v0.64-plan.md`
- `docs/plans/archives/v0.65-plan.md`
- `docs/adr/0046-settings-schema-migration-policy.md`
- `docs/adr/0065-central-action-param-contract-enforcement.md`
- `docs/adr/0069-operator-onboarding-flow.md`
- `docs/adr/0074-web-design-system-and-ux-language.md`
- `docs/adr/0075-user-category-settings-profiles.md`
- `docs/adr/0076-packaging-distribution-and-unified-cli.md`
- `docs/adr/0077-product-experience-design-and-information-architecture.md`
  (v0.60 product-experience design the validated releases implement)
- `docs/adr/0078-first-model-path.md` (v0.60 First-Model-Path decision)
- `docs/adr/0079-visual-design-language-and-art-direction.md`
- `docs/adr/0080-navigation-consolidation-and-workspace-shell-presentation.md`
- `docs/plans/roadmap.md` `## v0.66: Product RC & No-Docs Validation`

## Inputs

v0.64 closes trusted install and repairable first-run. v0.65 makes local files,
notes, and reviewed agent memory the primary launch integration. v0.66 validates
those paths together with every already-implemented advanced surface before the
v1.0 freeze.

## Main User/Operator Flows

- Operator installs Allbert from the packaged path on a clean host.
- Operator starts the persistent service and sees runtime/web/channels health go green.
- Operator completes QuickStart onboarding on the consumer-default path: guided local
  runtime setup if needed, then a one-click in-app curated-local-model download (no manual
  model CLI, no API key), and reaches a first useful chat. The advanced BYOK/custom path
  is validated as a regression surface.
- Operator's natural non-developer prompts route sensibly (no mis-route to a disabled or
  demo capability), and the web workspace passes a non-developer usability audit.
- Operator connects a local notes/files root, searches/reads a note, confirms a
  write, reviews an agent-memory candidate, and recalls it later.
- Operator inspects security/confirmation/event state through web, CLI, and TUI
  surfaces.
- Operator exercises existing advanced surfaces — remote channels, MCP, browser
  research, plan approval, public protocols, and export/import — as regression
  blockers, even though they are not the default first-run path.
- Operator exports/imports or upgrades an Allbert Home and verifies behavior.
- Operator uninstalls service/binary without deleting user data unless requested.
- Maintainer runs final docs, warning, drift, secret-scan, and `release.v066`
  gates.

## Data Flow

No new runtime data flow is introduced. The RC validates the v0.61-v0.65 product
capability releases integrated on the v0.60 product-experience design. It records
redacted evidence from the packaged binary, daemon, web workspace, onboarding,
settings/profile, trusted install, local knowledge/memory, TUI, advanced
surfaces, export/import, and uninstall paths. Evidence must use disposable homes
or explicitly declared test homes, never a real operator home.

## Authority Invariants

No new capability or authority. The RC validates that every product path remains
behind the same action/settings/security spine and that packaged entry points,
onboarding profiles, and web affordances grant nothing by themselves.

## Handles / CLI Surfaces

- Packaged install/uninstall commands.
- Persistent service start/stop, `/health`, `allbert onboard`, `allbert tui`,
  `allbert admin ...`; foreground `allbert serve` remains a diagnostic fallback.
- Web `/`, `/workspace`, `/jobs`, `/objectives`, onboarding, settings/model, and
  operator panels.
- Export/import or upgrade commands.
- Secret/evidence leak scan.
- `mix allbert.test release.v066` deterministic gate.

## Security Eval Rows

Only gate-bound or mixed contract rows are registered in `EvalInventory` (`:v066`
milestone). Each registered row must have a concrete owning `test_module`, `≥3 assert:`
atoms, and a matching `AssertBinding.check!/2` in `v066_sweep_eval_test.exs`.
`release.v066` fails if any registered row is prose-only or unbound. These rows assert
capability exposure, permission/confirmation floors, routing decisions, and boundary
contracts — not live browser/model/provider behavior.

Gate-bound / mixed rows:

- `product-rc-consumer-default-oneclick-model-no-key-first-chat-001` — M7 `[gate]`
  routing/state contract + `[attested]` first useful model chat.
- `product-rc-conversational-routing-no-misroute-001` — M7 `[gate]`.
- `product-rc-web-smoke-no-console-error-001` — M3 `[gate]` LiveView render/dispatch
  contract + `[attested]` real-browser console/layout smoke.
- `product-rc-cli-tui-no-mix-needed-001` — M4 `[gate]` CLI dispatch contract +
  `[attested]` packaged-binary CLI/TUI smoke.
- `product-rc-profile-no-authority-regression-001` — M8 `[gate]`.
- `product-rc-packaging-no-authority-regression-001` — M8 `[gate]`.
- `product-rc-local-files-notes-memory-policy-bounded-001` — M5/M8 `[gate]`.
- `product-rc-advanced-surfaces-no-regression-001` — M6 `[gate]` contract +
  `[attested]` live advanced-surface evidence.
- `product-rc-export-import-upgrade-001` — M9 `[gate]` dry-run + `[attested]` real
  upgrade/import behavior.
- `product-rc-evidence-secret-scan-001` — M11 `[gate]`.
- `product-rc-v1-handoff-current-001` — M11 `[gate]`.

Attested-only evidence ids are not `EvalInventory` rows:

- `product-rc-install-serve-onboard-local-knowledge-first-chat-001` — M1/M5
  `[attested]`.
- `product-rc-web-usability-audit-item11-001` — M3 `[attested]` human audit.
- `product-rc-uninstall-preserves-home-001` — M9 `[attested]` host smoke.
- `product-rc-no-docs-validation-001` — M3 `[attested]` fresh non-developer walk.

Attested-only and mixed-row evidence is recorded under `docs/validation/v0.66/` at
closeout, cross-referencing the same id where a mixed row has both a gate contract and
operator evidence.

## Smoke Checklist

- Install binary on a clean host/home.
- Start the persistent service; verify runtime/web/channels health.
- Complete web QuickStart onboarding via guided local runtime setup if needed and the
  consumer-default one-click local-model download from an empty model cache (no manual
  model CLI, no API key) to first chat; terminal onboarding smoke.
- Run the natural-prompt routing set and confirm no mis-route to a disabled/demo
  capability; run the non-developer web-UX usability audit against the item-11 bar.
- Recheck the v0.63 onboarding command contract: bare resume,
  `--quickstart`, `--advanced`, confirmed `--reset`, and
  canonical `--non-interactive --authorize` plus the deprecated
  `--accept-risk` alias warning.
- Use browser control to verify `/`, `/workspace`, `/jobs`, `/objectives`,
  onboarding, settings/model, and operator panels.
- Use grouped CLI/TUI admin reads without raw `mix`.
- Exercise the local files/notes/memory launch path.
- Exercise implemented advanced surfaces as regression blockers:
  `mix allbert.test external-smoke -- browser_research`,
  `mix allbert.test external-smoke -- browser_research_delegate`, every configured
  channel selector, the v0.51 MCP/OpenAI-compatible/ACP command set, the v0.44
  Plan/Build workflow approval smoke, and export/import.
- Export/import or upgrade a Home; verify behavior and redaction.
- Uninstall service/binary; verify Home preservation unless explicit data removal
  is requested.
- Run leak scans, docs gate, warning gate, and `release.v066`.

## Operator Validation

Status: released. Observed v0.66 evidence is recorded in
`docs/validation/v0.66/README.md`. The command block below is retained as the host
walkthrough template for carried-forward v1.0 DIT rows; it is not a claim that every
operator-attested row passed during v0.66.

```sh
export ALLBERT_HOME="$(mktemp -d /tmp/allbert-v066.XXXXXX)"
# Choose the packaged service path for the host:
#   macOS/Homebrew: brew services start allbert
#   Linux service:  systemctl --user start allbert
curl -fsS http://localhost:4000/health
allbert admin health
allbert onboard --quickstart
allbert onboard
allbert onboard --advanced
allbert ask "hello from v0.66 validation"
allbert admin security status
mix allbert.test release.v066
```

Expected behavior:

- install, service start, onboarding, local files/notes/memory, first chat, inspection,
  implemented-surface regression smoke, export/import or upgrade, and uninstall
  are either recorded as PASS in the evidence matrix or carried forward as explicit
  v1.0 DIT blockers;
- no-docs validation of the primary launch path remains a carried-forward v1.0 DIT
  blocker unless a fresh true non-developer walkthrough records a PASS; the
  one-click download-from-empty step is a hard freeze blocker, not satisfied by a
  preloaded/local-ready model;
- web and CLI/TUI surfaces require no raw `mix` for operator work;
- no secret appears in logs, evidence, output, docs, or screenshots;
- `release.v066`, docs, warning, and drift checks pass.

## Docs Closeout Checklist

RC-specific guides (the new artifacts):

- Product RC operator validation guide exists.
- No-docs launch-path validation guide exists.
- Browser-control smoke guide exists.
- Cross-platform install/uninstall validation notes exist.
- Upgrade/export/import evidence guide exists.
- Secret/evidence leak-scan commands exist.
- Item-11 usability-audit report exists (`docs/validation/v0.66/`).
- Attested evidence matrix exists in `docs/validation/v0.66/`, with one row per
  attested-only or mixed evidence id and a current commit/artifact/home reference.
- v1.0 handoff note is current and links the 17-item acceptance matrix.

Full doc-surface consistency sweep (M10 — the whole developer + operator + index +
getting-started surface, not just the RC guides):

- `README.md` trimmed under target with no per-release history (release detail links to
  CHANGELOG/roadmap).
- Operator + developer docs version-pin-swept: no hardcoded older-version currency
  stamps, no stale gate/version references lagging the shipped release.
- All active indexes reconciled — `docs/README.md`, `docs/operator/README.md`,
  `docs/developer/README.md`, `docs/plans/README.md`, and a new `docs/design/README.md` —
  active docs linked, no active-doc orphans (incl. `local-knowledge.md` in the operator
  index). Archives, samples, generated release evidence, screenshots/binary design assets,
  and older historical plans are excluded unless an active index links them as current
  guidance.
- Start-Here entry sequence agrees across `README.md`, `docs/README.md`, and
  `docs/operator/README.md`, and surfaces local-knowledge.
- The docs staleness/index-completeness check runs as a `release.v066` step and passes
  (drift is gate-enforced, not a one-time manual sweep).
