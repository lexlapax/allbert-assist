# Allbert v0.55 Request Flow - Channel Parity + TUI/Terminal Channel

Status: in progress (authored Phase B, R4). M0/M1 landed on 2026-06-21:
direct runtime `owl`, `AllbertAssist.Channels.ChannelParity`,
`mix allbert.channels --parity`, and Matrix generic outbound through
`Channels.Outbound`. M2 landed on 2026-06-21: `plugins/allbert.tui`,
`mix allbert.tui`, and the runtime `model_payload` / `surface_payload` seam are
implemented. M3 landed on 2026-06-21: terminal approval handoff rendering and
typed `ALLBERT:...:<id>` callback resolution are implemented. M4 landed on
2026-06-21 with the `release.v055` deterministic gate and docs. A post-M4 audit
correction now routes the live `mix allbert.tui` prompt through the
descriptor-derived `Channels.Supervisor` child instead of a separate unsupervised
adapter. A manual-validation punchlist correction also makes
`mix allbert.settings set channels.tui.identity_map '[...]'` parse the
list-shaped channel identity-map JSON and keeps non-interactive TUI children quiet
during ordinary `mix` inspection tasks. M5 manual validation and M6 release
closeout remain pending.

Implementation details live in `docs/plans/v0.55-plan.md`.

## Source Documents
- `docs/plans/v0.55-plan.md`
- `docs/adr/0067-tui-terminal-channel.md` (Proposed; accept in v0.55)
- `docs/adr/0016-channel-adapter-boundary-and-identity-mapping.md`
- `docs/adr/0057-cross-channel-conversation-threading.md`
- `docs/adr/0029-typed-runtime-response-contracts.md` and
  `docs/adr/0030-unified-surface-catalog-renderer-and-extension-registry.md`
  (v0.55 split-payload amendments)
- Templates: `plugins/allbert.telegram` (adapter), `plugins/allbert.email`
  (typed-command renderer); `apps/allbert_assist/lib/allbert_assist/channels.ex`,
  `channels/supervisor.ex`
- Upstream hard prerequisite: v0.52 M0 (`primitives:` field,
  `Approval.Handoff.render/2`, registry validator) plus ADR 0057
  (`threading:` descriptors, `Conversations.ChannelThread`). If missing, stop
  and reconcile v0.52/v0.53 sequencing before v0.55 implementation.

## Locked Decisions
Recorded in `v0.55-plan.md`. TUI is a delivery adapter with no extra authority;
library = `owl`; CLI not promoted (TUI is the first-class terminal channel);
TUI primitives `[:typed_command, :list]`; identity via the existing list-shaped
`channels.tui.identity_map`; parity matrix machine-derived from descriptors. The
mix-free operator console (in-TUI slash-commands + `mix allbert.channels status`)
is deferred to v0.55.1 (`docs/plans/v0.55b-plan.md`, ADR 0070); v0.55 ships the
basic `mix allbert.tui` launcher only.

## TUI Data Flow

The TUI inbound path mirrors the Telegram adapter (`plugins/allbert.telegram`):
when launched by `mix allbert.tui`, the descriptor-derived child under
`Channels.Supervisor` reads terminal input, normalizes it, resolves identity, and
submits through the same `Runtime.submit_user_input/1` seam every channel uses —
the only TUI-specific pieces are the input loop (`Owl.IO.input`), the descriptor's
`:tui_session` strategy, and the scrollback/live-block render surface.

```text
terminal input (Owl.IO.input loop, supervised GenServer)
  -> normalize (trim, drop empty; in-TUI slash-command routing is v0.55.1)
  -> Channels.Identity.resolve (list-shaped channels.tui.identity_map;
     terminal profile "default" unmapped -> {:error, :not_mapped})
  -> Channels.derive_session_id (descriptor session_strategy {:tui_session, prefix: "ch_tui_"}
     through the existing generic strategy branch unless implementation proves otherwise)
  -> Runtime.submit_user_input(%{channel: "tui", user_id, session_id, ...})
  -> Conversations.ChannelThread lookup/link/message-ref recording
  -> channel_events create/update (dedupe by (channel, external_event_id)
     for inbound/callback rows;
     redacted/truncated summary, never raw payload)
  -> response appended as scrollback lines
     + a single Owl.LiveScreen live block for the active prompt / streaming line
  -> approval -> Approval.Handoff.render/2 -> :list / :typed_command in terminal
  -> typed "ALLBERT:APPROVE:<id>" -> Actions.Runner.run("approve_confirmation", ...)
```

The split-payload seam (ADR 0067 extending ADR 0029/0030) adds named typed-response
fields `model_payload` and `surface_payload`. `surface_payload` defaults to
`model_payload` for existing callers. The runtime stores/threads only
`model_payload`; terminal renderers draw `surface_payload`, which may contain
framing, ANSI, paging, and the `LiveScreen` live block. Terminal chrome never
leaks into conversation history.
This is the foundation the v0.57 Pi-mode coding surface builds on.

## Parity Matrix (dimensions, machine-derived)
Per channel (web, Telegram, email, CLI label, Discord, Slack, Matrix, WhatsApp,
Signal, TUI): approval primitives declared; attachments posture; streaming
(none — turn-complete in v0.55); identity mapping; approval rendering; outbound
delivery; release/live-use status. Canonical prose in the ADR 0016 amendment;
derived via `ChannelParity` / `mix allbert.channels --parity`.

## Authority Invariants
- TUI calls only `Runtime.submit_user_input/1` + `Actions.Runner.run/3`; no direct
  Security Central / confirmation-store access (ADR 0016).
- Unmapped terminal identity is rejected; never implicitly `"local"`.
- Approval primitives never embed targets/commands/URLs; confirmations resolve
  only through the durable store via the Runner.
- `channel_events` summaries redacted/truncated; no raw payloads.
- TUI crash isolated under `:one_for_one`.

## Handles
- `mix allbert.channels --parity` (matrix report).
- `mix allbert.tui` (basic launcher: boots the app + the owl input loop;
  persistent terminal session, no slash-commands).
- `plugins/allbert.tui` adapter; `channels.tui.*` settings.

Deferred to v0.55.1 (`docs/plans/v0.55b-request-flow.md`): the mix-free operator
console — the in-session TUI slash-commands `/status`, `/confirmations`, `/events`,
`/channels`, `/settings get`, `/help`, and `mix allbert.channels status`.

## M2 Implementation Checkpoint (2026-06-21)

The M2 adapter is now a shipped plugin descriptor, so `mix allbert.channels list`,
`show tui`, and `--parity` report the terminal channel from registry truth. The
basic `mix allbert.tui` launcher starts the descriptor-derived supervised TUI
child under `Channels.Supervisor` with launcher-only interactive opts
(`auto_input?: true`, `live_screen?: true`). Ordinary `mix` settings/channel
inspection tasks may start the supervised descriptor child when
`channels.tui.enabled` is true, but that child stays non-interactive and quiet.

Focused M2 invariants now covered by tests:

- descriptor contract: provider `"terminal"`, `:local` trust class,
  `[:typed_command, :list]`, `threading: :rich`, and
  `{:tui_session, prefix: "ch_tui_"}`;
- inbound mapped terminal input creates a `channel_events` row, calls
  `Runtime.submit_user_input/1`, records `ChannelThread` refs, and renders
  `surface_payload`;
- replaying the same terminal `external_event_id` returns duplicate without a
  second runtime submission;
- unmapped terminal identity is rejected before runtime submission;
- runtime persistence stores `model_payload`, never terminal-only
  `surface_payload` framing.

## M3 Implementation Checkpoint (2026-06-21)

The TUI now renders approval handoffs without adding a new authority path. The
renderer asks `Approval.Handoff.render/2` for terminal-supported primitives and
prints:

- exact typed commands: `ALLBERT:APPROVE:<id>`, `ALLBERT:DENY:<id>`,
  `ALLBERT:SHOW:<id>`;
- numbered approve/deny/show options with the same typed commands;
- no button, link, target URL, or executable target command affordances.

The adapter parses typed approval commands before normal runtime submission. A
typed command becomes a `channel_events` callback row and resolves through
`AllbertAssist.Channels.ConfirmationCallback`, which re-resolves the terminal
identity map and verifies the pending confirmation belongs to the same local
user and origin channel. Wrong-channel confirmations are rejected and stay
pending; typed approval text is never sent to `Runtime.submit_user_input/1`.

## Security / Eval Rows (`:v055`)
See `v0.55-plan.md`: matrix-matches-descriptors; TUI dedupe; identity-map reject;
crash isolation; approval-primitive-honor (`:list`/`:typed_command` only);
confirmation resolve; no-authority; redaction; split-payload contract/defaulting;
and direct Owl runtime dependency. These rows are executable in
`apps/allbert_assist/test/security/v055_tui_channel_eval_test.exs`.

## Smoke Checklist
- `mix allbert.channels --parity` matches the registry.
- `mix allbert.channels list` / `show tui` reports the TUI channel configuration
  summary plus sibling channels without secrets. It is **not** the v0.55 event or
  payload inspector; semantic event/payload inspection lands in the deterministic
  gate and the v0.55.1 console.
- TUI: input drives a full turn with `channel_events` dedupe; unmapped identity
  rejected; typed approval resolves the confirmation; TUI crash leaves Telegram up.

## Operator Validation
- `mix allbert.test release.v055` (deterministic; simulated terminal input). This
  gate covers the `:v055` parity/dedupe/identity/crash/approval/redaction eval rows
  plus split-payload/Owl runtime dependency checks with a fake input source — no
  live terminal session required. The command prints a fresh `release.v055
  evidence:` path every run; the operator must save the path from the current
  run, not reuse a historical path from this document.

## Operator-Validation Punchlist (v0.55, persistent TUI session)

Run A1–A5 against the **persistent TUI session** to confirm the warm-process
behaviour the deterministic gate (`mix allbert.test release.v055`, simulated input)
cannot exercise: the live `owl` input loop, the scrollback/`LiveScreen` render
surface, and the model-facing/surface payload split. The deterministic gate now
exercises descriptor-derived crash/restart under `Channels.Supervisor`; M5 records
that evidence alongside the live terminal observations. Unlike the v0.54 router
punchlist — where every step was a fresh
cold `mix allbert.ask` against a shared `ALLBERT_HOME` — **this punchlist runs
inside one warm BEAM**: a single supervised `mix allbert.tui` session holds the
node, the `Channels.Supervisor` tree, and the SQLite store open for the whole run.
After Step 3, no step restarts the BEAM or reopens the DB except the A2
identity-reload/relaunch check when required by the shipped settings-read
behavior; every other check is typed at the live prompt or read from a second
shell with an existing `mix` task against that same validation home. (The
in-TUI slash-command inspection surface is v0.55.1 —
`docs/plans/v0.55b-request-flow.md`.)

The TUI adapter plus the basic `mix allbert.tui` launcher is built in **M2**
(`plugins/allbert.tui`) and its approval rendering in **M3**; these validation
**instructions** are authored in **M4** (Evals + Docs). The punchlist below is
**run as the M5 operator-validation gate**, and a failing run blocks the **M6**
release closeout / version tag. The mix-free operator console (in-TUI
slash-commands + `mix allbert.channels status`) is v0.55.1 —
`docs/plans/v0.55b-plan.md`.

### Step 0: Project + Manual Home Preflight

Open **Shell 1** in the real `allbert-assist` project root on `main`, not in a
coding-agent worktree. Run:

```bash
git checkout main
git pull --ff-only origin main
git status -sb
git log -1 --oneline --decorate
```

Expected: `git status -sb` shows `## main...origin/main` with no modified files,
and the latest commit includes the current v0.55 manual-validation punchlist
correction. Stop if the branch is not `main`, the pull is not fast-forward, or the
worktree is dirty for reasons you do not understand.

Still in **Shell 1**, create a fresh manual validation home. Do not reuse an old
`ALLBERT_HOME` from an earlier failed attempt. Also keep gate-owned temporary
homes so printed evidence paths remain readable after the gate exits.

```bash
export ALLBERT_TEST_KEEP_TMP=1
export ALLBERT_HOME="$(mktemp -d /tmp/allbert-v055-manual.XXXXXX)"
printf 'ALLBERT_TEST_KEEP_TMP=%s\n' "$ALLBERT_TEST_KEEP_TMP"
printf 'V055_MANUAL_HOME=%s\n' "$ALLBERT_HOME"
```

Open **Shell 2** in the same project root on `main`. Paste the exact path printed
by Shell 1:

```bash
export ALLBERT_TEST_KEEP_TMP=1
export ALLBERT_HOME="<paste V055_MANUAL_HOME from Shell 1>"
printf 'ALLBERT_TEST_KEEP_TMP=%s\n' "$ALLBERT_TEST_KEEP_TMP"
printf 'V055_MANUAL_HOME=%s\n' "$ALLBERT_HOME"
test -d "$ALLBERT_HOME" && echo "manual home ok"
git status -sb
```

Expected: both shells print the same `V055_MANUAL_HOME`, Shell 2 prints
`manual home ok`, both shells print `ALLBERT_TEST_KEEP_TMP=1`, and Shell 2 is
also on clean `main`. Keep both shells open for the rest of validation.

### Step 1: Deterministic Release Gate

In **Shell 1**, run the deterministic gate before the live terminal:

```bash
ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v055
```

Pass criteria: exit 0. Copy the printed `release.v055 evidence:` path.
`ALLBERT_TEST_KEEP_TMP=1` is required because the release gate creates its own
gate-owned temporary home; without it, the printed evidence path may be removed
when the Mix task exits.

In **Shell 1** and **Shell 2**, set the same variable using that copied path:

```bash
export V055_RELEASE_EVIDENCE="<paste printed release.v055 evidence path>"
test -f "$V055_RELEASE_EVIDENCE" && echo "release evidence ok"
```

Expected: both shells print `release evidence ok`. A1, A2, A4, and A5 cite rows
from this evidence file. The deterministic gate may create its own temporary test
home internally; that does not replace the manual `ALLBERT_HOME` prepared in
Step 0.

### Step 2: Migrate + Configure TUI

In **Shell 1**, migrate the fresh manual home:

```bash
mix ecto.migrate.allbert --quiet
```

Configure the default terminal identity before enabling the channel:

```bash
mix allbert.settings set channels.tui.identity_map '[{"external_user_id":"default","user_id":"local","enabled":true}]'
mix allbert.settings get channels.tui.identity_map
```

Expected: the `get` output includes one entry with
`"external_user_id" => "default"`, `"user_id" => "local"`, and `"enabled" => true`.

Enable and inspect the channel:

```bash
mix allbert.settings set channels.tui.enabled true
mix allbert.channels show tui
mix allbert.channels --parity
```

Expected for `show tui`: channel `tui`, provider `terminal`, `Enabled: true`,
`Identities: 1`, no secrets. Expected for parity: a row for `tui` with provider
`terminal`, primitives `typed_command+list`, threading `rich`,
identity `channels.tui.identity_map`, approval `typed_command+list`, attachments
`none`, streaming `turn_complete`, outbound `none`, release `released`, and live
`true`. These inspection tasks must not print the `Allbert TUI (default)` banner
or start an input prompt.

### Step 3: Launch The Persistent TUI

In **Shell 1**, launch the live TUI:

```bash
mix allbert.tui
```

Expected on launch: a banner identifying the `"tui"` channel + provider
`"terminal"`, the resolved local profile, and a live `LiveScreen` prompt. This is
the basic launcher — it boots the app and the owl input loop only. The A-checks
below read state from **Shell 2** using existing `mix` tasks
(`mix allbert.channels show tui` / `list`, `mix allbert.confirmations list`) with
the same `ALLBERT_HOME`. The in-TUI slash-command inspection surface (`/status`,
`/events`, `/confirmations`, `/channels`, `/settings get`, `/help`) lands in
v0.55.1. In v0.55, only `/quit` and `/exit` are valid lifecycle commands.

### Step 4: A1 Full Turn + Dedupe Evidence

In **Shell 1**, at the live prompt, type:

```text
> remind me what timezone meetings default to
```

Expected: the response renders as scrollback above the live prompt; the prompt
returns; the same `mix allbert.tui` process remains open.

In **Shell 2**, run:

```bash
mix allbert.channels show tui
```

Expected: `Enabled: true`, `Identities: 1`, and no secrets. This is only a
secret-free channel summary, not an event-row inspector.

Do **not** prove dedupe by retyping the same text. A normal re-submit is a new
terminal event and should receive a fresh `external_event_id`. Dedupe is by
`(channel, external_event_id)` for inbound/callback rows, so the duplicate proof
comes from the deterministic `release.v055` evidence captured in Step 1.

Optional evidence spot-check from **Shell 2**:

```bash
python3 -m json.tool "$V055_RELEASE_EVIDENCE" | rg 'tui-inbound-turn-dedupe-001|status'
```

Pass criteria: the live turn completes and renders as scrollback; the first submit
is backed by release evidence showing exactly one processed inbound
`channel_events` row; the replay evidence shows that the same `external_event_id`
produces **no second runtime submission** and no second processed inbound row.
Record: live scrollback/prompt behavior plus the
`tui-inbound-turn-dedupe-001` row from `$V055_RELEASE_EVIDENCE`.

### Step 5: A2 Identity-Map Rejection

The shipped v0.55 TUI reads settings at session start. Use this explicit relaunch
path; A2 is the only relaunch exception in the warm-session punchlist.

In **Shell 1**, exit the TUI:

```text
> /quit
```

In **Shell 2**, empty the identity map:

```bash
mix allbert.settings set channels.tui.identity_map '[]'
mix allbert.settings get channels.tui.identity_map
```

Expected: `channels.tui.identity_map=[]`.

In **Shell 1**, relaunch the same manual home:

```bash
mix allbert.tui
```

At the live prompt in **Shell 1**, type:

```text
> hello with no identity mapping
```

Expected: no assistant response is produced for the unmapped input. The row-level
proof that the redacted event records `status: rejected` carrying
`{:error, :not_mapped}`, with no `Runtime.submit_user_input/1` call, no thread,
and no model turn, comes from the `tui-identity-map-001` row in
`$V055_RELEASE_EVIDENCE`.

In **Shell 1**, exit again:

```text
> /quit
```

In **Shell 2**, restore the identity map:

```bash
mix allbert.settings set channels.tui.identity_map '[{"external_user_id":"default","user_id":"local","enabled":true}]'
mix allbert.settings get channels.tui.identity_map
```

Expected: the restored map again contains one enabled `default` -> `local` entry.

In **Shell 1**, relaunch for the remaining checks:

```bash
mix allbert.tui
```

Pass criteria: the unmapped session produced no assistant response, the identity
map was restored, and the TUI is live again with the mapped profile. Record: the
visible no-response behavior plus the `tui-identity-map-001` row from
`$V055_RELEASE_EVIDENCE`.

### Step 6: A3 Approval Primitive Render + Resolve

In **Shell 1**, trigger a confirmation:

```text
> create a note titled v055 with body hello from the tui
```

Expected: the terminal renders a `needs_confirmation` handoff using only a
numbered `:list` plus typed commands. It must not render buttons, links, target
URLs, or executable target commands. The visible handoff should include exact
commands like:

```text
Status: needs_confirmation
Approval Handoff:
  ALLBERT:APPROVE:<CONF_ID>
  ALLBERT:DENY:<CONF_ID>
  ALLBERT:SHOW:<CONF_ID>
  Confirmation: <CONF_ID> target=write_note status=pending
```

Copy the displayed `<CONF_ID>`. In **Shell 1**, approve it by typing the exact
command:

```text
> ALLBERT:APPROVE:<CONF_ID>
```

In **Shell 2**, inspect confirmations:

```bash
mix allbert.confirmations list
```

Pass criteria: the typed `ALLBERT:APPROVE:<id>` routes through
`Actions.Runner.run("approve_confirmation", ...)` (the same Runner path Telegram
uses — the TUI holds no direct confirmation-store access), the confirmation
resolves, and the `write_note` side effect runs. The in-session approval result
line and `mix allbert.confirmations list` show `<CONF_ID>` moved from pending to
resolved/approved. If the prompt does not produce a confirmation, stop and record
that as a validation failure; do not invent a passing confirmation through a
private store call. Record: rendered primitives, copied `<CONF_ID>`, approval
command, and resolved confirmation output.

### Step 7: A4 Crash-Isolation Evidence

In **Shell 2**, capture the secret-free channel summary:

```bash
mix allbert.channels list
```

Expected: output includes `tui` and sibling channels, with no secrets.

Do not try to kill the live TUI GenServer from a plain second-shell `mix` task;
that starts a different BEAM and is not a valid crash-isolation test. v0.55 has no
operator-facing in-node kill command. The required process-kill proof is the
`tui-crash-isolation-001` row in `$V055_RELEASE_EVIDENCE`; it starts the
descriptor-derived TUI child with the same supervised-launcher wait loop used by
`mix allbert.tui`, kills it, observes `:one_for_one` restart, and confirms the
sibling worker stays alive.

Optional evidence spot-check from **Shell 2**:

```bash
python3 -m json.tool "$V055_RELEASE_EVIDENCE" | rg 'tui-crash-isolation-001|one_for_one|passed'
```

Pass criteria: deterministic A4 evidence shows killing the descriptor-derived TUI
child does **not** take down siblings and the supervisor restarts it under
`:one_for_one`; the live `mix allbert.tui` session is launched through
`Channels.Supervisor` and remains one warm prompt. Record:
`tui-crash-isolation-001` from `$V055_RELEASE_EVIDENCE`, live prompt presence, and
the `mix allbert.channels list` output.

### Step 8: A5 Split-Payload Seam

In **Shell 1**, at the live prompt, type:

```text
> show me a long response that pages and renders styled output
```

Expected: the terminal visibly renders the response in the TUI surface and returns
to the live prompt.

The model/surface payload proof is not available through `mix allbert.channels
show tui`; that task is only a channel summary in v0.55. The proof comes from
`split-payload-contract-001` and `split-payload-defaulting-001` in
`$V055_RELEASE_EVIDENCE`.

Optional evidence spot-check from **Shell 2**:

```bash
python3 -m json.tool "$V055_RELEASE_EVIDENCE" | rg 'split-payload-contract-001|split-payload-defaulting-001|passed'
```

Pass criteria: `model_payload` recorded for the turn (the thread message and any
model-facing event summary) contains only the conversational content — **no** ANSI
escape sequences, paging markers, prompt framing, or `LiveScreen` live-block
artifacts. `surface_payload` may be richly framed; the stored/threaded
`model_payload` is clean. Record: rendered terminal output and the two split
payload rows from `$V055_RELEASE_EVIDENCE`.

A second-shell `mix` read against the running node (`mix allbert.channels show
tui` / `list`, `mix allbert.confirmations list`) is read-only channel or
confirmation summary evidence only. If the SQLite store is busy, retry the read
after the live turn finishes; do not make second-shell reads the authority for
row-count or payload checks in v0.55.

### Step 9: Warm-TUI Sign-Off

In **Shell 1**, exit the TUI:

```text
> /quit
```

In **Shell 2**, run the final parity check:

```bash
mix allbert.channels --parity
```

Pass criteria: A1–A5 passed; the only TUI relaunch was the A2 identity-reload
check; `mix allbert.test release.v055` was green; `$V055_RELEASE_EVIDENCE` exists;
and final parity still matches the registry. Record the manual validation home
path from `$ALLBERT_HOME` and the release evidence path from
`$V055_RELEASE_EVIDENCE`.

## Matrix Outbound Real-Provider Smoke

The Matrix generic-outbound check (v0.54-deferred gap, closed in M1) is **not**
part of the warm-TUI punchlist: it requires a configured Matrix homeserver plus
credentials and is run as a separate real-provider smoke. Confirm that a generic
outbound to a **mapped Matrix target** now delivers rather than degrading with
`:outbound_not_implemented`. This validates the v0.54-deferred Matrix
`deliver_outbound/3` that M1 closes through `Channels.Outbound`.

Run this only from a shell that has the same real-provider Matrix settings,
secrets, allowlists, and identity mapping the operator intends to validate. A
missing homeserver, token, room allowlist, or mapped Matrix target is a provider
configuration blocker, not a passing v0.55 result.

### Step 10: Matrix Real-Provider Smoke

In **Shell 2** or a new shell from the project root on `main`, keep the manual
`ALLBERT_HOME` exported if it contains the intended Matrix provider settings. If
the Matrix settings live in a different operator home, explicitly export that
home and record the path. Keep the smoke-owned temp home so the printed evidence
path remains readable:

```bash
export ALLBERT_TEST_KEEP_TMP=1
printf 'ALLBERT_TEST_KEEP_TMP=%s\n' "$ALLBERT_TEST_KEEP_TMP"
printf 'MATRIX_SMOKE_HOME=%s\n' "$ALLBERT_HOME"
```

Run the smoke:

```bash
ALLBERT_TEST_KEEP_TMP=1 mix allbert.test external-smoke -- matrix
```

Pass criteria for the command itself: exit 0 and a printed
`external-smoke-matrix evidence` path.

Copy the printed path and inspect it:

```bash
export V055_MATRIX_SMOKE_EVIDENCE="<paste printed external-smoke-matrix evidence path>"
test -f "$V055_MATRIX_SMOKE_EVIDENCE" && echo "matrix evidence ok"
python3 -m json.tool "$V055_MATRIX_SMOKE_EVIDENCE" | rg '"status": "passed"|"matrix"|"m.room.message"|send_channel_message|Channels.Outbound'
```

Expected: the evidence exists, includes Matrix delivery context, shows
`"status": "passed"`, and exercises the `send_channel_message` →
`Channels.Outbound` → Matrix `deliver_outbound/3` boundary rather than a direct
Matrix client send.

Manual fallback, if the smoke selector itself is blocked but the provider is
configured, must use the same action boundary and not an ad-hoc adapter call. In a
live Allbert prompt, send:

```text
> send a matrix message to <MAPPED_MATRIX_TARGET> saying v055 outbound check
```

The router should choose `send_channel_message` with slots
`channel="matrix"`, `target="<MAPPED_MATRIX_TARGET>"`, and
`body="v055 outbound check"`. Approve the durable confirmation. If a future CLI
helper is added, it must still call the same `send_channel_message` action and
`Channels.Outbound` boundary.

Pass criteria: the message is delivered to the Matrix target; the call **never**
returns `{:error, :outbound_not_implemented}` and is **not** the v0.54 graceful-
degrade for mapped Matrix targets. Evidence: delivery confirmation (or an outbound
`channel_events` row for the Matrix channel) with no `:outbound_not_implemented`.
If provider state blocks this step, record the exact blocker and do not mark the
Matrix real-provider smoke complete.

## Doc Drift Checks Before M6

The current docs gate is necessary but not sufficient: `mix allbert.test docs`
wraps `git diff --check` and catches whitespace/conflict-marker drift, not
semantic release drift. Before M6, explicitly scan and reconcile:

- command references: `mix allbert.channels --parity`, `mix allbert.channels
  list` / `show tui`, and `mix allbert.tui` exist in code or are still clearly
  marked as v0.55 implementation scope; `show tui` is documented only as a
  channel summary, not an event/payload inspector; `mix allbert.channels status`
  and the TUI slash commands are deferred to v0.55.1
  (`docs/plans/v0.55b-plan.md`), not v0.55;
- identity-map examples use the list-shaped channel contract, never
  `{"default":"local"}`;
- ADR 0016, ADR 0067, ADR 0029, ADR 0030, roadmap, and agent-context-map agree on
  the split-payload fields and the no-token/diff-streaming-in-v0.55 boundary;
- `CHANGELOG.md`, version metadata, and this plan say `0.55.0` only at closeout;
- `docs/developer/channel-parity.md` and `docs/operator/tui-channel.md` exist and
  match the final command surface.
