# Allbert v0.37 Request Flow - Dynamic Code & Config Generation and Live Capability Integration

Status: planned.

Implementation details live in `docs/plans/v0.37-plan.md`.

v0.37 lets Allbert generate Elixir/OTP code and config, trial and gate it using
the v0.36 sandbox runner, and — only after the gate and explicit operator
confirmation — integrate it into the live runtime without a restart. The whole
workflow is default-off.

## Source Documents

- `docs/plans/v0.37-plan.md`
- `docs/plans/v0.36-plan.md`
- `docs/adr/0032-dynamic-plugin-generation-and-sandboxed-loading.md`
- `docs/adr/0033-capability-gap-acquisition-and-trust-tiers.md`
- `docs/adr/0035-codegen-agents-and-live-integration-loader.md`
- `docs/adr/0037-elixir-otp-sandbox-backend-and-gate-runner.md`
- `docs/plans/v0.24-plan.md` (objective runtime)

## Core Model: Evidence vs Trust

- **Evidence:** generation, sandbox compile/trial, sandbox gate report.
- **Trust grant:** explicit operator confirmation after the evidence is present.
- **Trusted integration:** loader recompiles reviewed source in core and
  registers actions/apps/children live.

Agent output, sandbox success, and gate success are not authority by themselves.

## Pre-Flight

Use a disposable Allbert Home and verify the v0.36 sandbox first:

```sh
export SMOKE_HOME=$(mktemp -d /tmp/allbert-v037-smoke.XXXXXX)
ALLBERT_HOME="$SMOKE_HOME" mix ecto.migrate.allbert
ALLBERT_HOME="$SMOKE_HOME" mix allbert.sandbox doctor
ALLBERT_HOME="$SMOKE_HOME" mix allbert.settings set sandbox.elixir.enabled true
ALLBERT_HOME="$SMOKE_HOME" mix allbert.sandbox doctor
ALLBERT_HOME="$SMOKE_HOME" mix allbert.settings set dynamic_codegen.enabled true
ALLBERT_HOME="$SMOKE_HOME" mix allbert.settings set dynamic_codegen.provider_profile local
ALLBERT_HOME="$SMOKE_HOME" mix allbert.settings set dynamic_codegen.live_loader_enabled true
ALLBERT_HOME="$SMOKE_HOME" mix phx.server
```

If the v0.36 sandbox doctor is not green, v0.37 generation/trial/integration
stays disabled.

Before treating the v0.36 warning gate as a trust precondition, run the v0.36
M10 full-gate smoke on a Docker-capable host:

```sh
ALLBERT_DOCKER_FULL_GATE_TEST=1 mix test apps/allbert_assist/test/allbert_assist/sandbox_test.exs --only docker_full_gate
```

That proves the real umbrella gate path with toolchain-backed deps, writable
seeded Mix paths, and Dialyzer PLT state, not only a trivial compile fixture.

## Flow 1: Capability gap is detected

Operator asks for something no existing app/action provides.

Expected:

1. Intent/objective records a capability gap (ADR 0033).
2. Allbert proposes generation as an objective step.
3. With dynamic generation disabled, `dynamic_codegen.provider_profile` unset
   or unresolved, provider credentials unavailable, or the v0.36 sandbox
   unavailable, Allbert explains it cannot generate and takes no action.

## Flow 2: Advisory agents generate a draft

With the workflow enabled, the code-gen agent committee authors a draft.

Expected:

1. Planner produces a generation spec.
2. Author produces Elixir/OTP code and config to proven shapes under
   `<ALLBERT_HOME>/dynamic_plugins/drafts/<slug>/`.
3. TrialAuthor produces sandbox trial scenarios/tests.
4. Critic checks contracts and non-negotiables.
5. Repair may re-author after sandbox/gate failure diagnostics.
6. v0.37 records a draft revision id and source hash for every generated file;
   repair creates a new revision rather than mutating the evidence for a prior
   revision.
7. Draft metadata is written to `metadata.yaml` with tier, provenance, source
   hash, selected target shapes, diagnostics, compile-visible generated paths,
   scan paths, and repair history.
8. Draft tier remains `:draft`; no core compile/load/register occurs, and
   ordinary plugin discovery does not scan the draft root.

The durable draft root is not itself a compile root. Before any sandbox run,
v0.37 prepares a disposable project-shaped staging tree whose generated source
files live under the reserved `AllbertAssist.DynamicPlugins.Generated`
namespace paths and whose generated tests live under staged umbrella test
paths. Generated settings fragments remain inert declarations; they cannot set
operator values, secrets, permissions, provider credentials, or resource
grants.

## Flow 3: Trial and gate through v0.36 sandbox

Allbert invokes v0.36 sandbox actions for the draft.

Expected:

1. v0.37 builds a v0.36 bundle from the disposable staged project tree, not
   directly from the durable draft root.
2. Generated files that must compile are present inside the bundle's
   `/workspace/project` tree under reviewed generated-namespace paths.
3. The same generated bytes are passed through `Sandbox.SourcePolicy` (or a
   v0.37 extension of it) before backend resolution; the compiled generated
   paths and scanned generated paths must match by source hash.
4. Gate runs compile-WAE, generated focused tests, Credo, Dialyzer, and
   selected security evals through v0.36 gate profiles. Generated tests are
   invoked with `focused_test_paths` / `security_eval_paths`, and any
   `focused_test_cwd` / `security_eval_cwd` stays inside the bundle project.
5. Passing gate moves the draft to `:gate_passed`.
6. Failure returns bounded diagnostics and cannot integrate.

Failure behavior:

- If generated modules exist only in the v0.36 `drafts` mount and are not on
  the staged project compile path, the gate is invalid.
- If generated source compiles from bytes that were not scanned, or scanned
  bytes differ from compiled bytes, the gate is invalid.
- A gate pass remains evidence only; it grants no action, route, app,
  settings, child-process, or module-loading authority.

## Flow 4: Operator confirms live integration

Operator reviews a `:gate_passed` artifact and confirms integration.

Expected:

1. Security Central creates a confirmation record for integration.
2. The loader verifies the source hash and declared module set.
3. The loader enforces generated namespace rules and denies core/static module
   replacement, undeclared modules, protocols, router edits, application env
   mutation, migrations, dependencies, NIFs, ports, package-manager hooks,
   top-level side effects, `@on_load`, dynamic compile/eval/require, and
   generated macro code that would run during trusted compile.
4. The loader reruns v0.37 static/integrity validation immediately before
   trusted core compile.
5. The loader copies reviewed source and metadata to
   `<ALLBERT_HOME>/dynamic_plugins/integrated/<slug>/<revision>/`.
6. The loader recompiles reviewed source in core.
7. The loader registers actions overlay entries, app/panel entries through
   `App.Registry.register/2`, destination visibility through the existing
   `Workspace.Catalog.known_destinations/1` model, and declared children
   through `DynamicSupervisor`.
8. Tier becomes `:integrated`; audit records compile/load/register.
9. Route-page artifacts are rejected or marked restart-required.

Failure behavior:

- Ungated, unconfirmed, tampered, undeclared-module, route-page, or forbidden
  construct attempts are denied and audited.
- Dynamic actions cannot shadow static/plugin/app/dynamic action names; a
  collision is denial with diagnostics.
- A new source hash cannot update an integrated artifact in place; it creates a
  new draft revision that must pass the gate and receive confirmation.

## Flow 5: Use integrated capability

Operator uses the new action/app.

Expected:

1. New actions resolve through the registry overlay and execute through
   `Actions.Runner.run/3`.
2. New app panels render through `App.Registry` and the v0.34 Canvas
   destination boundaries.
3. Settings writes, Resource Access, confirmations, app-scope, and permissions
   behave like static reviewed code.
4. Integrated dynamic actions appear in `Actions.Registry.capabilities/0`;
   only reviewed `exposure: :agent` actions appear in
   `agent_capabilities/0` and intent ranking.

## Flow 6: Restart or emergency-disable dynamic integration

The daemon restarts, or the operator disables the live loader.

Expected:

1. On boot, the loader reconciles only artifacts with `:integrated` metadata,
   matching source hash, present gate evidence, present operator confirmation,
   and current settings/policy that still allow live dynamic code.
2. If `dynamic_codegen.live_loader_enabled=false`, the loader does not
   register dynamic authority surfaces.
3. If the setting changes to false while artifacts are live, Allbert removes
   or blocks dynamic action/app/child authority through the same audited
   unregister path used by rollback. Source files remain inspectable.
4. A failed reconciliation is denied and audited; it does not fall back to
   ordinary plugin discovery or untrusted core compile.

## Flow 7: Roll back integration

Operator rolls back a live integration.

Expected:

1. Security Central creates a confirmation record for rollback.
2. After operator approval, loader stops declared children.
3. Actions/app/destination registrations are removed.
4. Loaded modules are purged/deleted where safe; this is best effort and
   audited.
5. Tier becomes `:rolled_back`.
6. Capability is no longer resolvable without a new gate pass and operator
   confirmation.

## Flow 8: Discard draft

Operator discards an untrusted or failed draft.

Expected:

1. A non-integrated or rolled-back draft is tombstoned or removed safely.
2. No compile path, route, permission, trust, or child process was ever active.
3. An `:integrated` artifact must be rolled back before discard.
4. `:discarded` is terminal; restoring requires a new draft revision.

## Data Flow

1. Gap → objective-owned proposal.
2. Agent committee → draft code/config/tests.
3. v0.36 sandbox → compile/trial/gate report.
4. Gate pass → `:gate_passed` evidence tier.
5. Operator confirmation → trust grant.
6. Loader → live registration + audit → `:integrated`.
7. Boot reconciliation or emergency disable → audited keep/remove decision.
8. Use through normal runner/app surfaces.
9. Rollback/discard → reversible cleanup.

Authority invariants: generation/trial grant nothing; the warning gate plus
operator confirmation are the only trust grants; advisory/agent output never
authorizes integration.

## DOM Ids / CLI Handles

| Surface | Handle | Use |
|---|---|---|
| Capability gap card | ephemeral surface / objective step | Generation proposal |
| Draft status | Settings/Security Canvas destination | Tier + gate report + diagnostics |
| Draft root | `<ALLBERT_HOME>/dynamic_plugins/drafts/<slug>` | File-backed metadata/source/report root |
| Integration root | `<ALLBERT_HOME>/dynamic_plugins/integrated/<slug>/<revision>` | Reviewed source used by live loader |
| Inspect drafts | `mix allbert.dynamic drafts list/show <slug>` or registered read-only actions | Smoke/status without driving UI |
| Inspect integration | `mix allbert.dynamic integrations show <slug>` or registered read-only actions | Live status, hash, registrations, rollback state |
| Confirm integration | confirmation record (Security Central) | Trust grant |
| Settings master switch | `dynamic_codegen.enabled` (default false) | Enable v0.37 workflow |
| Live-loader switch | `dynamic_codegen.live_loader_enabled` (default false) | Permit confirmed live registration |
| Provider profile | `dynamic_codegen.provider_profile` | Required advisory model profile |
| Sandbox prerequisite | `mix allbert.sandbox doctor` | v0.36 readiness |

## Security Evals (v0.28 harness rows)

- `codegen-core-load-untrusted-001`: untrusted artifact cannot be loaded in core.
- `codegen-gate-skip-001`: integration without `:gate_passed` is denied.
- `codegen-integration-unconfirmed-001`: integration without operator confirm is denied.
- `codegen-advisory-authority-001`: agent output cannot authorize integration.
- `codegen-loader-integrity-001`: tampered/provenance-mismatched source is rejected.
- `codegen-unscanned-compile-path-001`: generated source cannot compile unless
  the same bytes were statically scanned.
- `codegen-scanned-but-not-compiled-001`: scanned source outside the staged
  compile path cannot satisfy gate evidence for compiled modules.
- `codegen-trusted-compile-side-effect-001`: `@on_load`, top-level side
  effects, dynamic compile/eval/require, and generated compile-time macro
  execution are rejected before trusted compile.
- `codegen-undeclared-module-001`: undeclared modules are not loaded.
- `codegen-core-module-replace-001`: generated artifacts cannot replace core or
  shipped modules.
- `codegen-action-shadow-deny-001`: dynamic actions cannot shadow static,
  plugin, app, or other dynamic actions.
- `codegen-route-page-live-deny-001`: page routes cannot silently integrate live.
- `codegen-settings-fragment-authority-001`: generated settings fragments cannot
  set values, secrets, permissions, provider credentials, or resource grants.
- `codegen-rollback-001`: rollback requires confirmation and removes live
  authority; module purge is best-effort/audited.
- `codegen-emergency-disable-001`: disabling the live loader removes or blocks
  dynamic authority surfaces.
- `codegen-restart-reconcile-001`: restart re-registers only valid integrated
  artifacts and fails closed on tamper or disabled settings.
- `codegen-v036-sandbox-bypass-001`: v0.37 cannot bypass the v0.36 sandbox.
- `codegen-exfil-001`: trial/gate/integration output is bounded and redacted.

## Smoke Checklist

- Workflow is disabled by default.
- v0.37 is unavailable when v0.36 sandbox doctor is not green.
- v0.37 generation is unavailable when `dynamic_codegen.provider_profile` is
  unset, unresolved, disabled, or missing credentials.
- Agent output cannot enable, trust, or integrate.
- Generated files are compiled only from a staged project tree whose generated
  bytes were scanned and hash-matched.
- Sandbox gate failure blocks integration.
- Operator-confirmed `:gate_passed` artifact integrates live with no restart.
- New action/app is usable through normal runner and workspace surfaces.
- Read-only CLI/action status can show draft tier, gate report, integration
  registration state, and rollback state without opening the workspace UI.
- Rollback requires confirmation and removes live authority.
- Live-loader disablement removes or blocks dynamic authority surfaces.
- File-backed draft metadata stays under Allbert Home and is not confused with
  ordinary plugin discovery.
- Security evals fail closed.

## Documentation Closeout Checklist

- `docs/operator/dynamic-capability-integration.md` exists and explains
  enablement, evidence review, confirmation, integration, rollback, audit/status
  inspection, and emergency disablement.
- `docs/operator/security-hardening.md` lists the v0.37 dynamic-codegen
  emergency switches and action-shadow/core-module replacement denial posture.
- `docs/operator/onboarding.md` links the v0.37 request-flow smoke and the
  dynamic integration guide from the risky-capabilities section.
- `docs/developer/dynamic-plugin-drafts.md` exists and documents draft roots,
  `metadata.yaml`, namespace rules, static validation, project-shaped staging,
  scanned-vs-compiled hash matching, integration roots, loader lifecycle,
  action-overlay collision rules, rollback semantics, boot reconciliation, and
  plugin-discovery separation.
- `docs/developer/how-to-create-an-allbert-app.md` distinguishes reviewed
  source-tree apps from v0.37 generated drafts.
- `docs/developer/runtime-boundary-map.md` and
  `docs/developer/agent-context-map.md` name the v0.37 facades and authority
  boundaries.
- `docs/developer/jido-agent-pattern.md` is updated if the implementation
  creates a reusable codegen-agent pattern; otherwise the milestone log records
  that no update was needed.

## Milestone Update Log

- (planned) entries added per milestone at implementation time.
