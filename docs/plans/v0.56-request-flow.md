# Allbert v0.56 Request Flow - Channel Parity + TUI/Terminal Channel

Status: planned — **STUB** (2026-06-09 restructure, Phase A). Authored in Phase B (R4).

Implementation details live in `docs/plans/v0.56-plan.md`.

## Source Documents
- `docs/plans/v0.56-plan.md`
- `docs/adr/0016-*` (channel adapter boundary, identity mapping, approval primitives)
- Existing channel plugins: `plugins/allbert.telegram`, `plugins/allbert.email`

## To be authored in Phase B
- The channel capability/parity matrix (per-channel primitives/attachments/
  streaming/identity/approvals).
- The TUI/terminal channel flow under the ADR 0016 contract (inbound normalize →
  `Runtime.submit_user_input` → dedupe → Approval Handoff → redaction).
- Authority invariants: the TUI is a delivery adapter only; no extra authority.
- Security eval rows + smoke checklist + `release.v056`.
