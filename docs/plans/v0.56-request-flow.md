# Allbert v0.56 Request Flow - Channel Parity + TUI/Terminal Channel

Status: planned (authored Phase B, R4).

Implementation details live in `docs/plans/v0.56-plan.md`.

## Source Documents
- `docs/plans/v0.56-plan.md`
- `docs/adr/0016-channel-adapter-boundary-and-identity-mapping.md`
- Templates: `plugins/allbert.telegram` (adapter), `plugins/allbert.email`
  (typed-command renderer); `apps/allbert_assist/lib/allbert_assist/channels.ex`,
  `channels/supervisor.ex`
- Upstream: v0.52 M0 (`primitives:` field, `Approval.Handoff.render/2`, registry
  validator)

## Locked Decisions
Recorded in `v0.56-plan.md`. TUI is a delivery adapter with no extra authority;
library = `owl`; CLI not promoted (TUI is the first-class terminal channel);
TUI primitives `[:typed_command, :list]`; identity via `channels.tui.identity_map`;
parity matrix machine-derived from descriptors.

## TUI Data Flow
```text
terminal input (owl IO.input loop, supervised GenServer)
  -> normalize -> Identity.resolve (channels.tui.identity_map)
  -> Channels.derive_session_id ({:tui_session, ...})
  -> Runtime.submit_user_input(%{channel: "tui", user_id, session_id, ...})
  -> Channels.create_event/update_event (dedupe, redacted summary)
  -> render response (owl LiveScreen) ; approval -> Approval.Handoff.render/2
     -> :list / :typed_command rendered in terminal
  -> typed "ALLBERT:APPROVE:<id>" -> Actions.Runner.run("approve_confirmation", ...)
```

## Parity Matrix (dimensions, machine-derived)
Per channel (web, Telegram, email, CLI label, Discord, Slack, mobile, TUI):
approval primitives declared; attachments posture; streaming (none — turn-complete);
identity mapping; approval rendering. Canonical prose in the ADR 0016 amendment;
derived via `ChannelParity` / `mix allbert.channels --parity`.

## Authority Invariants
- TUI calls only `Runtime.submit_user_input/1` + `Actions.Runner.run/3`; no direct
  Security Central / confirmation-store access (ADR 0016).
- Unmapped terminal identity is rejected; never implicitly `"local"`.
- Approval primitives never embed targets/commands/URLs; confirmations resolve
  only through the durable store via the Runner.
- `channel_events` summaries redacted/truncated; no raw payloads.
- TUI crash isolated under `:one_for_one`.

## Handles
- `mix allbert.channels --parity` (matrix report); `plugins/allbert.tui` adapter;
  `channels.tui.*` settings.

## Security / Eval Rows (`:v056`)
See `v0.56-plan.md`: matrix-matches-descriptors; TUI dedupe; identity-map reject;
crash isolation; approval-primitive-honor (`:list`/`:typed_command` only);
confirmation resolve; no-authority; redaction.

## Smoke Checklist
- `mix allbert.channels --parity` matches the registry.
- TUI: input drives a full turn with `channel_events` dedupe; unmapped identity
  rejected; typed approval resolves the confirmation; TUI crash leaves Telegram up.

## Operator Validation
- `mix allbert.test release.v056` (deterministic; simulated terminal input).
- Opt-in interactive-terminal smoke before tag where the harness allows.
