# v0.12.2 - Tracing and replay

Status: Draft, implementation-ready

## Summary

v0.12.1 made Allbert legible in the moment: operators can see what the runtime is doing right now through `ActivitySnapshot`, spinners, stuck hints, and channel-native activity surfaces. v0.12.2 makes Allbert legible after the fact. When an operator asks "what happened?", "why did skill X activate?", "did the model retry?", or "which tool ran for 12 seconds?", v0.12.2 produces a structured, persisted, replayable answer.

The release introduces a span-based session trace aligned with the OpenTelemetry GenAI semantic conventions. The same kernel transitions that emit `ActivityUpdate` in v0.12.1 also emit `Span` begin/end events, captured to `~/.allbert/sessions/<id>/trace.jsonl` and surfaced through `/trace`, `allbert-cli trace`, and a Telegram trace summary. An optional file-based OTLP exporter lets advanced operators ship traces into their own observability stack (Datadog, Langfuse, Jaeger, Grafana) without coupling the daemon to a remote endpoint.

The default capture posture is `capture_messages = true`. Allbert is a personal assistant; the operator owns their own data and gets the most value when traces include actual prompts, responses, tool args, and tool results so replay is meaningful. Privacy guards remain unconditional: secrets are always redacted, the disk cap is bounded, retention is finite, and the operator can lower capture fidelity per field or disable tracing entirely through `/settings`.

The release includes an additive protocol bump to `4` so daemon and channels can carry span events alongside existing v3 activity messages, plus a setup wizard step and a safe default-write upgrade path so both fresh and existing profiles get tracing without manual TOML editing.

## Goals

- Capture every kernel decision and runtime call as a structured span with parent-child relationships: turn, prepare-context, classify-intent, route-skill, invoke-agent, execute-tool, chat, run-validation, run-script, wait-approval, wait-input, finalize.
- Persist spans per session at `~/.allbert/sessions/<id>/trace.jsonl`, append-only, line-oriented, with disk-cap-aware rotation and crash recovery for in-flight spans.
- Align span names and attributes with the OpenTelemetry GenAI semantic conventions so traces are portable to external observability tools.
- Default `capture_messages = true` so operators get full prompt/response replay; allow per-field policy (`capture` / `summary` / `drop`) for finer privacy control.
- Always redact secrets at write time, regardless of `capture_messages`, using a hardened pattern allowlist plus pluggable redactor seam.
- Make traces useful in the same channels operators already live in: `allbert-cli trace`, TUI `/trace`, Telegram `/trace last`.
- Provide an opt-in file-based OTLP-JSON exporter so operators with their own observability stacks can ship traces without coupling Allbert to remote endpoints.
- Fold tracing posture into the v0.12.1 settings hub so capture fidelity, retention, and disk cap are inspectable and changeable from `/settings`.
- Fold tracing into onboarding/bootstrap so fresh profiles answer the question explicitly during setup and existing profiles get safe defaults written automatically.
- Reuse v0.12.1 boundaries: same kernel transitions, same redaction posture, same channel capability gating; do not invent a parallel observability stack.

## Non-goals

- No network OTLP exporter. v0.12.2 ships only file-based OTLP-JSON export. Network exporters can land in a later release once policy on outbound traffic is decided.
- No automatic anomaly detection, eval scheduling, golden-set replay, or annotation queue. These are observability-platform features, not in scope here.
- No retroactive trace synthesis. Sessions that ran before v0.12.2 will not gain spans; tracing starts forward.
- No cross-session aggregation analytics in v0.12.2. Each session's trace stands alone.
- No tracing of pre-daemon process startup (the setup wizard before the kernel starts has its own logging path).
- No new approval kinds. Tracing inherits existing approval/inbox behavior.
- No structured error trait migration; v0.12.1's deferral remains in force.
- No span sampling beyond the `capture_messages` flag and per-field redaction policy in v0.12.2.
- No automatic redaction of model output text. Operators who want output redaction must lower `provider_payloads` to `summary` or `drop`.
- No removal of existing telemetry, cost log, daemon log, or session-scoped trace text files. They remain alongside the new span trace.

## What-if audit findings

| Scenario | Today | v0.12.2 response |
| --- | --- | --- |
| Operator asks "what happened in my last turn?" | No durable record beyond cost/approval; kernel events are gone | `/trace` or `allbert-cli trace show` renders the span tree of the last session |
| Operator asks "why did `memory-curator` activate?" | Tier-1/2/3 events render to TUI but are not persisted | `route_skill` spans capture eligibility, score, decision, and parent turn |
| Operator asks "did the model retry?" | No record of retries | `chat` span includes retry attempts as span events with reason and elapsed |
| Operator asks "which tool ran for 12 seconds?" | `ToolCall`/`ToolResult` events have no duration | `execute_tool` span has `started_at`, `ended_at`, `duration_ms` |
| Sub-agent delegation chain | Lost after the turn | Nested `invoke_agent` spans with parent-child IDs |
| Operator wants to ship traces to external observability | No path | `allbert-cli trace export <session> --otlp-json` writes OTLP-JSON to a configured directory |
| Privacy-conscious operator | All text inline; no per-field policy | `capture_messages = false` plus per-field redaction policy lets operator pick the level |
| Disk fills with traces | Cost log can grow but no trace artifacts existed | Per-session and total disk caps with rotation and GC |
| Daemon crashes mid-turn | In-flight kernel events lost | In-flight span snapshots in `current_spans/`; on restart, marked truncated rather than lost |
| Trace contains a secret | Daemon log already redacts but kernel events do not | Span attributes pass through a redactor at write time; secrets are always removed |
| Operator runs setup on a fresh profile | No tracing exists | Setup wizard explains tracing, asks for capture posture, writes defaults |
| Operator upgrades an existing v0.12.1 profile | New `[trace]` section missing | Daemon writes safe defaults on first start, logs an informational note, no manual TOML edit |
| Operator wants to disable tracing entirely | N/A | `[trace] enabled = false` or `/settings set trace.enabled false` stops persistence; activity surfaces continue working |
| Operator wants to lower fidelity | N/A | `/settings set trace.capture_messages false` flips per-field policy to `summary` for prompts/responses/tool args |
| Trace file grows beyond cap | N/A | Rotation archives oldest spans to `trace.<n>.jsonl.gz`; oldest archive evicted past total cap |
| External tool wants to ingest traces | N/A | OTLP-JSON file format under `otel_export_dir/<session>.json` |

## Public interfaces

### Config

```toml
[trace]
enabled = true                  # set to false to disable persistence entirely
capture_messages = true         # include prompt/response text and tool args/results
session_disk_cap_mb = 50        # per-session cap; oldest spans archived/evicted past cap
total_disk_cap_mb = 2048        # total cap across all sessions; oldest sessions GCed past cap
retention_days = 30             # sessions older than this are GCed by `trace gc`
otel_export_dir = ""            # empty disables OTLP-JSON export; otherwise dir under ALLBERT_HOME
otel_service_name = "allbert"

[trace.redaction]
secrets = "always"              # "always" only; setting "never" is rejected with operator-visible error
tool_args = "capture"           # "capture" | "summary" | "drop"
tool_results = "capture"        # "capture" | "summary" | "drop"
provider_payloads = "capture"   # "capture" | "summary" | "drop"
```

When `capture_messages = false`, the loader coerces `tool_args`, `tool_results`, and `provider_payloads` from `capture` to `summary` for that load. The override does not rewrite the file; the operator still sees their original choice in `/settings`. `capture_messages = true` means the per-field policy applies as written.

`session_disk_cap_mb` clamps to `[5, 500]`; `total_disk_cap_mb` clamps to `[100, 51200]`; `retention_days` clamps to `[1, 365]`. Out-of-range values warn and clamp at config load. `otel_export_dir` must stay inside `ALLBERT_HOME`; absolute paths or path escapes are rejected with an operator-visible error.

`secrets = "never"` is intentionally rejected. Secret redaction is unconditional, and removing it would break the privacy contract documented in operator docs and the upgrade note.

### CLI surfaces

| Command | Behavior |
| --- | --- |
| `allbert-cli trace show [<session>]` | Render span tree for the given session, or the last session if omitted |
| `allbert-cli trace tail [<session>]` | Follow new spans as they end |
| `allbert-cli trace list [--limit <n>]` | List sessions that have trace data with span count, duration, and last-touched timestamp |
| `allbert-cli trace show-span <span-id>` | Render one span with full attributes and events |
| `allbert-cli trace export <session> [--format otlp-json] [--out <path>]` | Write OTel-aligned export; default format `otlp-json` |
| `allbert-cli trace gc [--dry-run]` | Apply retention and disk-cap policy; `--dry-run` prints what would be removed |
| `allbert-cli trace --help` | Includes EXAMPLES block per v0.12.1 help convention |

### TUI slash commands

| Command | Behavior |
| --- | --- |
| `/trace` | Alias for `/trace show last` |
| `/trace show [<session>]` | Render span tree inline |
| `/trace show-span <span-id>` | Render one span detail |
| `/trace tail` | Show recent spans of the active session |
| `/trace export [--otlp-json]` | Trigger an export and print the output path |
| `/trace settings` | Alias for `/settings show trace` |

Unknown trace subcommands remain local TUI errors (matching v0.12.1 unknown-slash-command behavior) and must not be sent as model turns.

### Telegram

| Command | Behavior |
| --- | --- |
| `/trace last` | Reply with a compact, redacted summary of the last session's span tree (counts, top-level durations, terminal phase) |
| `/trace span <span-id>` | Reply with one span's compact summary |

Telegram trace responses must respect the same channel capability gating as approvals: if markdown is not supported, fall back to plain-text bullet output. Raw prompt/response text and tool payloads must not be inlined into Telegram replies regardless of `capture_messages`. Telegram users see structural information; full content stays on local disk.

### Daemon protocol

Bump `PROTOCOL_VERSION` from `3` to `4` with additive span messages and fields.

Add:

- `SpanKind`: `Internal`, `Client`, `Server`, `Producer`, `Consumer` (mirrors OTel).
- `SpanStatus`: `Ok`, `Error { message: String }`.
- `SpanEvent`: `{ timestamp, name, attributes }`.
- `Span`: `{ id, parent_id, session_id, trace_id, name, kind, started_at, ended_at, duration_ms, status, attributes, events }`.
- `ServerMessage::SpanEvent(Span)` for in-flight broadcast (optional; subscribers opt in).
- `ClientMessage::TraceShow { session_id }` and `ServerMessage::TraceSpans(Vec<Span>)` for direct CLI/channel reads.
- `ClientMessage::TraceList` and `ServerMessage::TraceSessions(Vec<TraceSessionSummary>)`.

Backward compatibility:

- A v3 client connecting to a v4 daemon must continue working. The daemon must omit unknown-to-v3 messages when serializing for a v3 peer and must not gate v3 messages on v4 features.
- A v4 client connecting to a v3 daemon must surface a clear version mismatch with operator-facing remediation (`upgrade the daemon to v0.12.2 to use trace surfaces`) and must not synthesize spans locally.
- M0 must include a roundtrip test using a v3 envelope against the v4 deserializer.

### Settings hub additions

The v0.12.1 settings registry gains a `trace` group:

- `trace.enabled`
- `trace.capture_messages`
- `trace.session_disk_cap_mb`
- `trace.total_disk_cap_mb`
- `trace.retention_days`
- `trace.otel_export_dir`
- `trace.otel_service_name`
- `trace.redaction.tool_args`
- `trace.redaction.tool_results`
- `trace.redaction.provider_payloads`

`trace.redaction.secrets` is intentionally not exposed as a settable key; its only valid value is `always`. The settings registry must list it as read-only with an explanatory note so operators discover the contract without being able to weaken it.

### Bootstrap and onboarding

Setup wizard adds a "Trace and replay" step after the memory step and before the self-improvement step. The step must:

1. Briefly explain what tracing captures (decisions, routing, tool calls, model calls, durations) and how it differs from live activity.
2. Ask whether to enable tracing (default `yes`).
3. Ask whether to capture full prompt and response text (`capture_messages` default `yes`), with one-paragraph explanation that secrets are always redacted, retention is bounded, and the operator can change this later via `/settings`.
4. Show the storage path, disk caps, and retention defaults; allow override.
5. Print a summary line at the end naming `/settings show trace` and `allbert-cli trace --help` for later inspection.

The setup state file (v0.12.1's `setup.state_path`) gains a `trace` block so `allbert-cli setup --resume` resumes the trace step correctly.

Existing-profile upgrade behavior:

- On daemon start, if the loaded `config.toml` has no `[trace]` section, the daemon writes the default block atomically (preserving unrelated TOML comments where practical) and emits an informational message to `daemon.log`: `Trace defaults installed (capture_messages=true, retention_days=30). Use \`/settings show trace\` or \`allbert-cli trace --help\` to inspect.`
- Existing profiles do not get a setup-wizard re-run.
- Default-write must be idempotent and must skip if any partial `[trace]` block exists; the operator's partial choice wins.
- The default-write is logged but not surfaced as an inbox approval; this is configuration scaffolding, not a behavior change requiring operator review.

## Implementation design

### Span model

Add the shared span types in `allbert-proto`:

```rust
struct Span {
    id: String,
    parent_id: Option<String>,
    session_id: String,
    trace_id: String,
    name: String,
    kind: SpanKind,
    started_at: DateTime<Utc>,
    ended_at: Option<DateTime<Utc>>,
    duration_ms: Option<u64>,
    status: SpanStatus,
    attributes: BTreeMap<String, AttributeValue>,
    events: Vec<SpanEvent>,
}

enum SpanKind { Internal, Client, Server, Producer, Consumer }
enum SpanStatus { Ok, Error { message: String } }

struct SpanEvent {
    timestamp: DateTime<Utc>,
    name: String,
    attributes: BTreeMap<String, AttributeValue>,
}

enum AttributeValue {
    String(String),
    Int(i64),
    Float(f64),
    Bool(bool),
    StringArray(Vec<String>),
    IntArray(Vec<i64>),
}
```

`trace_id` is per top-level turn; `session_id` is the cross-turn session identifier. A turn span is the root of one trace; sub-agent spans inherit the same `trace_id` with a chain of `parent_id` references.

### OTel attribute alignment

Attribute keys follow OTel GenAI conventions where they exist and use the `allbert.*` prefix for assistant-specific attributes:

- `gen_ai.operation.name`: e.g., `chat`, `invoke_agent`, `execute_tool`, `route_skill`.
- `gen_ai.system`: provider name (`anthropic`, `openai`, `gemini`, `ollama`, `openrouter`).
- `gen_ai.request.model`: model id.
- `gen_ai.usage.input_tokens`, `gen_ai.usage.output_tokens`, `gen_ai.usage.cache_read_tokens`, `gen_ai.usage.cache_create_tokens`.
- `gen_ai.agent.name`: for `invoke_agent` spans, names the agent invoked.
- `allbert.session.id`, `allbert.channel`, `allbert.skill.name`, `allbert.skill.tier`, `allbert.tool.name`, `allbert.approval.id`.

Unknown attributes are tolerated by readers; new attributes can land additively.

### Capture points

| Span name | When emitted | Typical children |
| --- | --- | --- |
| `turn` | Top-level user input → final response | All below |
| `prepare_context` | Bootstrap, memory load, telemetry assembly | none |
| `classify_intent` | Intent/router pass | none |
| `route_skill` | Per skill considered/eligible (one span per candidate, with eligibility outcome attribute) | none |
| `invoke_agent` | Agent or sub-agent invocation | nested spans |
| `chat` (`gen_ai.operation.name=chat`) | Provider call (one per attempt; retries are sibling spans or events on the parent depending on adapter) | none (events for streaming chunks if `capture_messages = true` and adapter supports) |
| `execute_tool` | Tool call (kernel-driven) | none |
| `run_validation` | Tier A or other validation command | none |
| `run_script` | Script (Lua) execution | none |
| `wait_approval` | Approval gate open → resolved | none |
| `wait_input` | Input request open → resolved | none |
| `finalize` | Memory staging, cost persist, cleanup | none |

The same kernel transitions that emit `ActivityUpdate` in v0.12.1 emit `SpanBegin`/`SpanEnd`. The wiring is shared; only the destinations differ. v0.12.1 sends activity to channels live; v0.12.2 also writes spans to the trace sink.

### Persistence

- Each session has a directory `~/.allbert/sessions/<session_id>/`.
- Spans land in `trace.jsonl` (current file) as one JSON span per line, written when the span ends.
- In-flight span snapshots live in `current_spans/<span_id>.json`. On daemon restart, any leftover snapshot is finalized with `status = Error { message: "truncated_at_restart" }` and `ended_at = now()` plus a span event marking the restart, then appended to `trace.jsonl` and removed from `current_spans/`.
- When `trace.jsonl` exceeds `session_disk_cap_mb`, the file is rotated to `trace.<n>.jsonl.gz` (gzip) and a fresh `trace.jsonl` started. Rotation runs synchronously inside the writer to avoid races.
- When a session's archived files exceed the per-session cap, the oldest archive is evicted.
- When the total trace footprint exceeds `total_disk_cap_mb`, oldest sessions are removed in full (entire session directory's trace artifacts; non-trace artifacts are not touched).
- GC runs on demand via `allbert-cli trace gc` and as a maintenance job per existing job scheduling. GC must be safe to interrupt and idempotent.

### Redaction

Secret detection runs on every attribute value at write time:

- Built-in patterns: AWS keys (`AKIA[0-9A-Z]{16}`), generic API keys (`(api[_-]?key|token|secret)\s*[:=]\s*['"]?[A-Za-z0-9_\-]{20,}['"]?`), JWTs, env-style assignments matching common credential names, OpenAI/Anthropic key prefixes (`sk-`, `sk-ant-`).
- Detected secrets are replaced with `<redacted:secret>` in span attributes before persistence.
- The redactor is wrapped behind a `SecretRedactor` trait so the pattern set can extend without re-architecting.

`tool_args`, `tool_results`, and `provider_payloads` policies apply after secret redaction:

- `capture`: full content with secret redaction applied.
- `summary`: first 200 chars of the stringified value plus `total_len_chars` and `total_size_bytes`.
- `drop`: field omitted entirely; span retains a `<field>_dropped: true` marker so readers know data was suppressed by policy.

`capture_messages = false` overrides the loaded policy to `summary` for `tool_args`, `tool_results`, and `provider_payloads` for that boot. The override is logged once at startup so operators understand why their TOML choice was downgraded.

### TUI rendering

Span tree renders as an indented ASCII tree:

```text
turn 12.4s [ok]
├── prepare_context 0.8s
├── classify_intent 0.2s
├── route_skill memory-curator 0.0s [eligible]
├── route_skill skill-author 0.0s [not_eligible: no_trigger_match]
├── invoke_agent kernel 11.0s
│   ├── chat anthropic claude-... 8.4s [in:1240, out:642]
│   ├── execute_tool read_file 0.1s
│   └── chat anthropic claude-... 2.4s [in:1882, out:198]
└── finalize 0.4s
```

Color: green for `ok`, red for `Error`, dim for non-eligible routing decisions. Compact terminals collapse middle nodes with `…` and surface root-level summary plus errors. `/trace show-span` renders the full attribute map for one span in pretty key/value form.

### Cross-cutting with v0.12.1

- Same emission boundaries: every kernel transition that produces an `ActivityUpdate` also produces a span begin/end. The kernel exposes both through one combined hook so adding new boundaries is one edit, not two.
- Same redaction posture: v0.12.1 already declares activity must not include raw prompts, secrets, full tool inputs, or provider payloads. v0.12.2's redactor is the implementation; v0.12.1's declarations stay the contract.
- Same settings hub: v0.12.1 ships the registry; v0.12.2 adds the `trace` group keys.
- Same async loop and protocol seam: v0.12.1 bumps protocol to `3`; v0.12.2 bumps to `4` additively.

### OTLP-JSON export

`OtlpJsonExporter` writes OTel-aligned JSON to `otel_export_dir/<session_id>.json`. The format follows the OTLP/JSON specification for `ResourceSpans`:

- `resource.attributes`: includes `service.name = otel_service_name`, `service.version = "0.12.2"`, and `allbert.host` when known.
- `scope_spans[0].scope.name`: `allbert-kernel`.
- `scope_spans[0].spans`: array of OTel-shaped spans with `traceId`, `spanId`, `parentSpanId`, `name`, `kind`, `startTimeUnixNano`, `endTimeUnixNano`, `attributes`, `status`, `events`.

Operators wire their own pipeline: file watcher, periodic upload, or batched ingestion into Datadog, Langfuse, Jaeger, or any OTLP-compatible backend. Allbert does not push to a network endpoint in v0.12.2.

### Tests

- Unit: span lifecycle, parent-child propagation, secret redactor coverage, capture-policy coercion, OTLP-JSON exporter shape.
- Integration: full turn produces expected span tree; sub-agent invocation produces nested spans; provider retry produces retry events; tool call duration captured.
- Persistence: rotation, archive, eviction, GC, crash-recovery for in-flight spans.
- Backcompat: v3 envelope deserializes against v4 reader; v4 envelope older than expected fields tolerated.
- Privacy regression: prompts containing common secret patterns produce redacted attributes; `capture_messages = false` produces `summary` for sensitive fields; trace.redaction.secrets cannot be set to `never`.
- Setup: setup wizard step writes expected defaults, accepts overrides, and updates setup state file.
- Upgrade: existing profile without `[trace]` section gets defaults installed atomically and logs the informational message.
- Telegram: `/trace last` reply shape; structural data only, no inline prompt/response text.

## Milestones

### M0 - Span model and protocol

Scope:

- Add `Span`, `SpanKind`, `SpanStatus`, `SpanEvent`, `AttributeValue` in `allbert-proto`.
- Bump `PROTOCOL_VERSION` to `4`.
- Add `ServerMessage::SpanEvent`, `ClientMessage::TraceShow`/`TraceList`, and `ServerMessage::TraceSpans`/`TraceSessions`.
- Define `TraceSessionSummary` shape.
- Add v3-against-v4 backcompat tests.

Tests:

- Roundtrip serialization/deserialization for span types.
- Backcompat: v3 envelope decodes against v4 deserializer with no panic and no synthesized spans.
- Backcompat: v4 envelope omits trace fields when serializing for a declared v3 peer.
- Span-event ordering test (events sorted by timestamp).

Validation after M0:

```bash
cargo fmt --check
env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings
env -u RUSTC_WRAPPER cargo test -q proto
```

Commit after validation with a scoped M0 commit.

### M1 - Kernel emission and persistence

Scope:

- Add `TracingHooks` trait wrapping the existing kernel emission boundary.
- Hook every capture point: `turn`, `prepare_context`, `classify_intent`, `route_skill`, `invoke_agent`, `chat`, `execute_tool`, `run_validation`, `run_script`, `wait_approval`, `wait_input`, `finalize`.
- Add `TraceWriter` trait and `JsonlTraceWriter` implementation: append on span end, line-buffered, atomic file replace on rotation.
- Add `current_spans/` snapshot on span begin; remove on span end.
- Add crash-recovery sweep on daemon start: finalize stale snapshots with `truncated_at_restart`.
- Add disk-cap-aware rotation and archive (`gzip`) per `session_disk_cap_mb`.
- Add per-session archive eviction past cap.
- Wire daemon to the writer; activity emission and span emission share one kernel hook.

Tests:

- Span lifecycle test for each capture point.
- Parent-child relationship test for sub-agent invocation.
- Provider retry produces retry events on the parent `chat` span.
- Tool call captures `started_at`, `ended_at`, `duration_ms`.
- Crash recovery: leftover `current_spans/` snapshot finalized on next boot.
- Rotation test: file exceeds cap → archived, fresh file started.
- Archive eviction past per-session cap.

Validation after M1:

```bash
cargo fmt --check
env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings
env -u RUSTC_WRAPPER cargo test -q tracing
env -u RUSTC_WRAPPER cargo test -q kernel
env -u RUSTC_WRAPPER cargo test -q daemon
```

Commit after validation with a scoped M1 commit.

### M2 - Redaction and capture policy

Scope:

- Add `SecretRedactor` trait with default pattern allowlist (AWS, generic API key, JWT, env-style assignments, common provider key prefixes).
- Apply redactor to all attribute values before persistence.
- Add per-field policy resolution (`capture` / `summary` / `drop`) for `tool_args`, `tool_results`, `provider_payloads`.
- Add `capture_messages = false` override that coerces these three fields to `summary` at load time.
- Reject `trace.redaction.secrets = "never"` at config load with an operator-visible error.
- Add per-load coercion log so operators see why their per-field choices were downgraded.

Tests:

- Secret-redaction coverage for each built-in pattern, including position-in-the-middle of larger strings.
- Per-field policy coverage: `capture` keeps full content (with secret redaction), `summary` truncates with length metadata, `drop` omits and adds `<field>_dropped: true`.
- `capture_messages = false` coerces to `summary` regardless of file value.
- `secrets = "never"` rejected.
- Privacy regression: known prompt patterns containing simulated secrets produce redacted span attributes.

Validation after M2:

```bash
cargo fmt --check
env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings
env -u RUSTC_WRAPPER cargo test -q tracing
env -u RUSTC_WRAPPER cargo test -q redaction
env -u RUSTC_WRAPPER cargo test -q config
```

Commit after validation with a scoped M2 commit.

### M3 - CLI trace surfaces

Scope:

- Add `allbert-cli trace show|tail|list|show-span|export|gc`.
- Add `OtlpJsonExporter` writing OTLP-JSON `ResourceSpans` per session.
- Add `EXAMPLES:` blocks for `trace`, `trace show`, `trace export`, `trace gc`.
- Reuse the v0.12.1 shared rendering helpers for span-tree output.
- Add `allbert-cli trace --help` description and subcommand `about` text.

Tests:

- CLI test for each subcommand on a fixture trace file.
- OTLP-JSON export shape test against the OTel schema fixture.
- GC dry-run prints planned removals; GC live removes per policy.
- Help-text snapshot tests for trace command tree.

Validation after M3:

```bash
cargo fmt --check
env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings
env -u RUSTC_WRAPPER cargo test -q cli
env -u RUSTC_WRAPPER cargo test -q tracing
env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- trace --help
env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- trace show --help
env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- trace export --help
```

Commit after validation with a scoped M3 commit.

### M4 - TUI and Telegram surfaces

Scope:

- Add TUI `/trace`, `/trace show`, `/trace show-span`, `/trace tail`, `/trace export`, `/trace settings`.
- Add span-tree renderer with capacity-aware collapsing for narrow terminals.
- Add Telegram `/trace last` and `/trace span <id>` returning structural-only summaries.
- Ensure Telegram never inlines raw prompts, responses, tool args, or tool results regardless of `capture_messages`.
- Update `/help` grouping in the v0.12.1 command hierarchy to include `trace` under Status group.

Tests:

- TUI `TestBackend` snapshots for span tree (idle, in-flight, error, narrow-terminal).
- Slash-command tests for each `/trace` form.
- Telegram render tests for compact span summary.
- Regression: Telegram trace replies do not contain inline prompt/response text or full tool payloads.
- `/help` includes the trace group.

Validation after M4:

```bash
cargo fmt --check
env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings
env -u RUSTC_WRAPPER cargo test -q tui
env -u RUSTC_WRAPPER cargo test -q telegram
env -u RUSTC_WRAPPER cargo test -q tracing
```

Commit after validation with a scoped M4 commit.

### M5 - Onboarding and bootstrap

Scope:

- Add the "Trace and replay" setup wizard step after memory and before self-improvement.
- Persist trace answers in the v0.12.1 setup state file under a `trace` block.
- Make `allbert-cli setup --resume` resume the trace step correctly.
- Add safe default-write on daemon start when `[trace]` is missing from `config.toml`.
- Make default-write atomic, idempotent, and skip if any partial `[trace]` block exists.
- Emit `daemon.log` informational message on default-write.
- Add `trace` group to the v0.12.1 settings registry with `trace.redaction.secrets` exposed read-only.
- Update setup summary to name `/settings show trace` and `allbert-cli trace --help`.

Tests:

- Setup wizard test for the trace step: defaults, override, summary line.
- Setup resume test: interrupted setup picks up from the trace step.
- Default-write test: missing `[trace]` block → defaults installed, comments preserved where practical.
- Default-write idempotency: rerunning daemon with full or partial `[trace]` block does not overwrite operator choices.
- Settings registry test: `trace.redaction.secrets` is read-only and rejects writes; other trace keys are settable.
- Settings registry test: `capture_messages = false` coercion is documented in `/settings show trace.capture_messages`.

Validation after M5:

```bash
cargo fmt --check
env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings
env -u RUSTC_WRAPPER cargo test -q setup
env -u RUSTC_WRAPPER cargo test -q settings
env -u RUSTC_WRAPPER cargo test -q config
env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- setup --help
env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- settings show trace
```

Commit after validation with a scoped M5 commit.

### M6 - Privacy and policy hardening

Scope:

- Expand `SecretRedactor` pattern coverage based on a curated list of provider/SDK key shapes.
- Add `allbert-cli trace verify-redaction <session>` (debug/test-only) that scans a session trace for known patterns and reports any leaks; default-disabled in release builds.
- Add operator docs for trace privacy posture: what is captured, what is always redacted, how to lower fidelity, how retention works.
- Draft and accept ADR (proposed `0081`) for trace artifact envelope, redaction posture, and OTel alignment commitment.

Tests:

- Pattern coverage test for each added secret form.
- `verify-redaction` test on a fixture trace with planted patterns reports zero leaks after the redactor runs.
- Regression: secret redaction cannot be disabled by config, env var, or CLI flag.

Validation after M6:

```bash
cargo fmt --check
env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings
env -u RUSTC_WRAPPER cargo test -q redaction
env -u RUSTC_WRAPPER cargo test -q tracing
```

Commit after validation with a scoped M6 commit.

### M7 - Release closeout and readiness review

Scope:

- Bump crates and lockfile package entries to `0.12.2`.
- Update `CHANGELOG.md` with v0.12.2.
- Add an upgrade note under `docs/notes/v0.12.2-upgrade-<date>.md` covering trace defaults, `capture_messages = true`, retention/disk caps, OTLP-JSON export, and bootstrap default-write.
- Update [docs/operator/tui.md](../operator/tui.md), [docs/operator/telemetry.md](../operator/telemetry.md), and add [docs/operator/tracing.md](../operator/tracing.md) covering posture, commands, redaction, retention, and OTLP export.
- Update [roadmap.md](roadmap.md) and [vision.md](../vision.md) only if release sequencing or stated product posture changes beyond this plan.
- Run a release readiness review confirming v0.12 and v0.12.1 invariants remain true: no automatic patch install, no binary auto-swap, no Lua auto-enable, no hidden config mutation beyond the documented `[trace]` default-write, no concurrent turn queue, no cancel.

Validation after M7:

```bash
cargo fmt --check
env -u RUSTC_WRAPPER cargo clippy --workspace --all-targets -- -D warnings
env -u RUSTC_WRAPPER cargo test -q
env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- --help
env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- trace --help
env -u RUSTC_WRAPPER cargo run -q -p allbert-cli -- settings show trace
```

Additional release validation:

```bash
git diff --check
# Run the local Markdown link checker.
# Run a temp-home daemon smoke confirming default `[trace]` write, capture_messages=true, and `trace show` on a generated session.
```

Commit after validation with the final v0.12.2 release commit and tag only after the readiness review is clean.

## End-user release plan

Release notes must explain:

- Allbert now persists a structured trace of every session at `~/.allbert/sessions/<id>/trace.jsonl`.
- The default capture posture is `capture_messages = true`: full prompt and response text, tool args, and tool results are captured for replay value.
- Secrets are always redacted at write time. The `trace.redaction.secrets = "never"` value is intentionally rejected.
- Operators can lower fidelity per field (`tool_args`, `tool_results`, `provider_payloads`) to `summary` or `drop`, or set `trace.capture_messages = false` to coerce the per-field policy in one switch.
- `allbert-cli trace show|tail|list|show-span|export|gc` covers inspection, tail, single-span detail, OTLP-JSON export, and disk-cap GC.
- TUI `/trace` renders the current session's span tree inline; `/trace show-span` shows one span in detail.
- Telegram `/trace last` returns a compact, redacted summary; full content stays on local disk.
- An optional file-based OTLP-JSON exporter writes per-session `ResourceSpans` for ingestion into Datadog, Langfuse, Jaeger, Grafana, or any OTLP-compatible backend.
- Setup now includes a "Trace and replay" step. Existing profiles get `[trace]` defaults installed automatically on first daemon start with an informational note.
- Disk caps and retention are bounded; `allbert-cli trace gc` enforces them.
- The protocol bumps from `3` to `4`; v3 clients connecting to v4 daemons keep working, and v4 clients connecting to v3 daemons surface a clear version-mismatch remediation.

Operator acceptance checklist:

- Fresh setup ends with the trace step accepted, defaults written, and `/settings show trace` reflecting the choices.
- Existing profile starts the daemon and sees `[trace]` defaults installed in `config.toml` with the informational log line.
- A turn produces a span tree visible in `/trace show` and `allbert-cli trace show`.
- Sub-agent invocations show as nested `invoke_agent` spans.
- Tool calls show with `duration_ms` populated.
- Provider retry shows as retry events on the parent `chat` span.
- Setting `capture_messages = false` coerces tool-arg/result/provider-payload fields to `summary` and logs the coercion.
- `trace.redaction.secrets = "never"` is rejected with operator-visible remediation.
- A session trace exceeding the per-session cap rotates and archives without losing trailing spans.
- `allbert-cli trace export <session> --format otlp-json` writes a valid OTLP-JSON document.
- `allbert-cli trace gc` removes sessions past `retention_days` and trims to `total_disk_cap_mb` without touching non-trace artifacts.
- A simulated daemon crash mid-turn finalizes the in-flight span on next boot rather than losing it.
- Telegram `/trace last` replies with structural data only and does not inline prompt/response text.
- Tier A validation passes with zero clippy warnings.

## Deferred work

- Network OTLP exporter (HTTP/gRPC).
- Live TUI replay panel rebuilt from spans (interactive scrub timeline).
- Automatic anomaly detection and quality regression alerting.
- Annotation queue for human review of traces (Langfuse-style).
- Eval/golden-set replay scheduling.
- Cross-session aggregation analytics and dashboards.
- Span sampling policies beyond `capture_messages` and per-field redaction.
- Distributed tracing across multiple Allbert hosts.
- Native Datadog/Langfuse/Jaeger push integrations.
- Trace-driven cost attribution dashboards.

## ADRs

Proposed `0081`: Trace artifact envelope, redaction posture, and OTel alignment commitment. The ADR captures: span schema and on-disk format; OTel GenAI semantic conventions as the alignment target; secret redaction as unconditional; per-field capture policy; bootstrap default-write contract; protocol bump and backcompat rules.
