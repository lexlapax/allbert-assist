# Allbert v1.0.2 Test Suite Speed & Isolation (Phase 1) + Binary Catch-Up

Request flow: `docs/plans/v1.0.2-request-flow.md`
ADR: two new ADRs ship with this release — ADR 0082 (registry injection seams for
test isolation — binding on the M2/M3 build) and ADR 0081 (Tier-2 → Tier-1
promotion process — a docs deliverable, M6). Consumes ADR 0049 (test strategy),
superseded ADR 0050 only as dependency history, ADR 0015/0031 (the
registration↔Settings-schema coupling this work
must NOT weaken), and the v1.0 public-contract freeze (`release.v1` stays green).

Status: **implementation-ready after second-pass documentation repair — planned
2026-07-15 against shipped v1.0.1 code (`ea7b4219`).** Current Code State and
Code Seams were rechecked against current main; release ordering, registry
side effects, bounded conversion scope, dependency policy, and binary operator
proof are decision-complete. Not started.

Source: roadmap ladder item 2 (`docs/plans/roadmap.md`) and the operator scope
decisions of 2026-07-15: **full test-isolation phase 1 including the web split**,
plus the v0.58 cleanup tails A+B, the Tier-2→Tier-1 promotion ADR, and the queued
dependency scope-outs (ecto pair, swoosh, tailwind wrapper, jsv — all four
recon-verdicted "apply"). This is a **BINARY point release** — the first binary
tag since `v1.0.0` — and it carries the standing obligation from the v1.0.1
`[skip-artifacts]` ship: **the v1.0.1 fixes reach the packaged artifact line with
this release** (tap jumps 1.0.0 → 1.0.2; §F–§I packaged validations re-run against
the published artifact).

## Build Progress

Implementation started 2026-07-15. Each milestone: implement → focused test →
drift-check → doc update → commit (`v1.0.2 M<n> <small title>`).

- **M0 (done):** preflight executed UNPIPED: `mix allbert.test inventory
  --check-tags` exits **1** with **173 findings** — 66 no-primary-tag, 42
  mis-tagged (checker expectation ≠ file tag), 65 multi-tagged. Correction
  recorded: the checker fails correctly; the third-pass "exits 0" claim was
  the `mix | tail` exit-masking trap (both probes were piped) — the very
  gotcha this plan exists to retire. No checker fail-mode work needed. Raw
  census stands as recon'd (415/46/36 files). Full finding log preserved as
  the M0 artifact (scratch: m0-preflight.log; classes reproduced in M1's
  worklist).

## Purpose

Retire the test suite's structural debt in one deliberate phase, and put the
1.0.1 fixes in packaged users' hands:

1. **De-flake the documented residue.** Five order/selection-dependent failures
   are individually root-caused (see Current Code State); the v1.0.1 build tripped
   over four of them repeatedly. Each becomes deterministically green solo AND in
   batch, red-first where the fix has a testable seam.
2. **Shrink the serial surface.** Virtually the whole core suite (~291
   explicit-serial files + 127 DataCase + 28 SecurityEvalCase vs 21 `pure_async`)
   is serial-within-a-VM — the dominant wall-clock cost and the flake source. Add
   the missing registry injection seam (ADR 0082), then convert the
   seam-unblocked and trivially-isolatable files to `pure_async`/partitionable
   lanes, lane by lane.
3. **Split the web long pole.** `WorkspaceLiveTest` alone is ~256.6s — ~83% of web
   wall-clock — and the only web `external_runtime_serial` file. Split it into
   partitionable `liveview_serial` files per the v0.41 M9 deferral's "own safety
   story" requirement.
4. **Clear the queued tails**: v0.58 cleanup tails A+B, the Tier-2→Tier-1
   promotion ADR, and the four scoped-out dependency updates.
5. **Binary ship**: `release.v102` gate, 1.0.2 bump, tag WITHOUT
   `[skip-artifacts]`, CI publish + cosign, tap fill (1.0.0 → 1.0.2), install
   rehearsal, and the packaged §F–§I catch-up attestations.

## Expected Inputs

- v1.0.1 shipped as a source/docs tag (`v1.0.1` → `1b03d7a4`, `[skip-artifacts]`);
  `v1.0.0` is still GitHub Latest; `homebrew/allbert.rb:13` still pins `1.0.0`
  with `v1.0.0` URLs — v1.0.2 is the first tap fill since 1.0.0. The version-less
  `latest` aliases resolve via GitHub Latest, so the 1.0.0 → 1.0.2 jump is clean
  (assetless v1.0.1 never entered the aliasing path).
- The `release.v1` freeze gate (unchanged; must stay green) and the `release.v101`
  gate as composition precedent.
- The lane taxonomy and gate machinery: `docs/developer/test-strategy.md:541-611`
  (nine lanes, one primary tag per file, reconciliation rule at `:587`),
  `apps/allbert_assist/lib/mix/tasks/allbert.test.ex` (fast-local `:403`,
  prepush `:392`, serial-core `:507`, partition runner `:528`; security/external
  lanes force `--partitions 1` at `:521`).
- The standing dependency-refresh working rule (roadmap Working Rules) with the
  four queued scope-outs recon-verdicted "apply in 1.0.2".
- Recorded timing baselines (all pre-date ~230 new core files — M0 re-baselines):
  release ~778s at v0.45.1 (core 364s / web 305s / stocksage 83s); fast-local
  quick 22.94s; high-coverage fast-local 393.79s; web `--web-lanes` 55.44s;
  `WorkspaceLiveTest` 256.6s (M9) / 265.0s (M1).

## Current Code State

Verified 2026-07-15 on `ea7b4219`.

**Lane census (grown ~2x since the M1 baseline).** 415 core / 46 web / 36
stocksage test files. Core explicit lane tags: `app_env_serial` 117,
`external_runtime_serial` 74, `home_fs_serial` 49, `global_process_serial` 29,
`security_eval_serial` 18 (+28 via `SecurityEvalCase`), `pure_async` **21**,
plus 127 `DataCase` (→ `db_serial`). Web: 35 `ConnCase` (→ `liveview_serial`),
3 `pure_async`, 1 `app_env_serial`, and exactly ONE `external_runtime_serial`
file — `workspace_live_test.exs`, tagged via
`use AllbertAssistWeb.ConnCase, async: false, lane: :external_runtime_serial`
(`:2`), not a `@moduletag`.

**The five documented residue failures — distinct root causes.** Residue (a) is
an unowned Home marker, (b) is eager runner resolution in the Runtime singleton,
and (c)–(e) are global registry/descriptor dependence. For the latter,
`Actions.Registry.agent_modules/0` / `internal_capabilities/0` compute the action
surface from global App/Plugin registries and descriptors flow through
`Extensions.Registry`; solo-vs-batch contents flip assertions without an owned
baseline.

| # | Test | Missing/leaked state | Minimal fix |
|---|---|---|---|
| a | `workspace_live_test.exs:1053` | `CLI.FirstRun.detect()`'s first gate is `home_initialized?` = `File.exists?(<home>/db/allbert.sqlite3)` (`cli/first_run.ex:172-175`); the test seeds onboarding markers but never the db file → solo `:home_missing` → `default_destination` nil (`workspace/first_run.ex:44-49`) → models panel never opens; batch predecessors leave the file | own the home: `put_env(:allbert_assist, Paths, home: tmp)` + create `<tmp>/db/allbert.sqlite3`; restore in `on_exit` |
| b | `v055_tui_channel_eval_test.exs:347` (assert `:374`) | setup put_envs an `agent_runner` returning `"v0.55 model: …"` (`:72-83`) but the running `Runtime` singleton resolved its runner at boot — solo persists `"ok"` | lazy-resolve `agent_runner` at call time OR thread a `runtime:`/`agent_runner:` param through `start_tui_adapter/1` (`:418`) |
| c | `intent_agent_test.exs:62` | `Registry.agent_modules()` reads global registries; setup (`:19-60`) isolates Memory/Paths/Settings/Audit but never the action registry → solo surface matches none of the 8 allowed sets (`:141-154`) | seed a deterministic registry baseline in setup (`PluginRegistry.clear()` + register the expected set + `AppRegistry` reset; restore in `on_exit`) |
| d | `corpus_completeness_test.exs:89`/`:138` | same global-registry dependence (`set_notes_root` positive / `restore_database_backup` negative rows); file also has **no primary `@moduletag`** (reconciliation gap) | seed registry baseline in setup + add `@moduletag :global_process_serial` |
| e | `golden_set_test.exs:44` (anchor `research-001`) | setup seeds stocksage/notes_files/browser descriptors via `ProviderPreconditions` (`:20-22`) but not research; `v046_research_delegate_eval_test.exs:53` does `AppRegistry.clear()` and leaves the global without `AllbertResearch.App` in batches | add `ProviderPreconditions.ensure_research_descriptors!()` (new sibling in `test/support/provider_preconditions.ex:13-38`) |

The "~20 order-dependent full-`mix test` flakes" is an operator-known estimate;
these five are the individually documented backlog. The "3 intent-battery flakes"
(`archives/v1.0-plan.md:478-479`) are a distinct named subset.

**The seam gap (the load-bearing finding).** `App.Registry` and `Plugin.Registry`
public functions already accept `server:` and `start_link/1` accepts `name:`, but
their `server/1` helpers are private and each private instance also needs a unique
named ETS `table_name:`. Registration currently clears the shared Settings-schema
cache and emits global registration signals, so private reads alone are not
isolated. `ActionsOverlay.agent_modules(server \\ __MODULE__)`
at `dynamic_plugins/actions_overlay.ex:43`; `Extensions.Registry` read fns take
opts at `extensions/registry.ex:80-113`) — **but the production READ paths do not
thread a server param**: `Actions.Registry.agent_modules/0` (`:502`),
`internal_capabilities/0` (`:528`), `plugin_actions/0` (`:674`);
`Intent.Engine` global reads (`intent/engine.ex:816`, `:904`, `:1263`, `:1432`);
`Skills.Registry` (`skills/registry.ex:279-299`). Until the thread-through lands,
private per-test registries are invisible to the code under test, forcing the
clear+register+restore global-mutation pattern — which is exactly what makes
those files serial and order-dependent.

**Convertibility of the prime serial candidates:** `intent/engine_test.exs`
(db_serial + registry, 44.8s hotspot), `skills/registry_test.exs`
(app_env_serial), `actions/browser_actions_test.exs` (home_fs_serial) — all three
convertible ONLY after the seam. `v043`/`v046` security evals and the browser
delegate smoke **stay serial by policy** (`test-strategy.md:597`; external lane
forces `--partitions 1`).

**The web split.** v0.41 M9 explicitly deferred it: "Leave web
`external_runtime_serial` files out of fast-local until a later plan can split or
passivate the runtime-heavy flows with its own safety story"
(`archives/v0.41-plan.md:579-580`; reinforced `test-strategy.md:536-539`).
`WorkspaceLiveTest` is that entire lane: 256.6s of ~307s web wall-clock.

**Dependency scope-outs (all recon-verdicted APPLY; full analysis in the
request-flow's refresh table):**
- **ecto pair = a four-package move**: ecto_sqlite3 0.22.0 pins `ecto_sql ~> 3.13`
  (why hex.outdated says "not possible"); 0.24.x flips to `~> 3.14`, pulling
  ecto 3.14.1 + decimal 2.4.1→3.1.1. The two ecto-3.14 behavioral traps
  (query-opts raise; `:preload_order`) have ZERO call sites (swept); no adapter
  option/journal changes in ecto_sqlite3 0.23–0.24.1. Watch: `mix deps.update
  ecto_sqlite3` may unlock exqlite 0.36→0.38 (vendored SQLite bump) — take it
  deliberately or re-pin, decided at the lock diff.
- **swoosh 1.26.3**: email channel uses raw gen_smtp
  (`plugins/allbert.email/.../smtp_client.ex:9-10`), NOT Swoosh; Swoosh surface is
  the Local/Test mailer scaffold + dev MailboxPreview. Gap includes the
  CVE-2026-54893 fix (unused MsGraph adapter). Near-zero risk.
- **tailwind wrapper 0.5.1**: Allbert already pins Tailwind CSS **4.1.12**
  (`config/config.exs:55-63`, v4 CSS-first config, no tailwind.config.js) — the
  wrapper bump changes only the download/run shim; compiled CSS is byte-identical;
  the v0.60b visual language cannot shift. Validation = CSS byte-diff, not
  screenshots.
- **jsv 0.21.2**: DIRECT dep (`apps/allbert_assist/mix.exs:88`, `~> 0.19` already
  permits 0.21) validating **workflow YAML** (`workflows/schema.ex:50`,
  `workflows/validator.ex:73-95`) — NOT MCP (hermes_mcp validates with peri).
  0.21's breaking item (Httpc resolver security defaults) touches only remote
  $ref resolution, unused. Operator decision: apply now.
- Standing-review notes: req_llm locked 1.13.0 vs 1.17.1; exqlite 0.36.0 vs
  0.38.0; phoenix_live_view 1.2 deliberately pinned out (`~> 1.1.0` at
  `apps/allbert_assist_web/mix.exs:52`) — its review is a future item.

**Release mechanics.** Gate precedent: gates never carry a predecessor's bespoke
focused steps (`@release_v066_steps` re-declares rather than appending v065's;
v101's three real focused test files already run in fast-local/web lanes; the SW
lockstep + gunzip guards ride `v1_version_consistency` inside `@release_v1_steps`)
→ `@release_v102_steps = @release_v1_steps ++ @v102_focused_steps`. Version bump
surface: three `mix.exs:7` lines (now `"1.0.1"`), `workspace-sw.js:1` CACHE_NAME +
regenerated `.gz` — all guard-enforced by `version_consistency_test.exs`
(`:15/:20/:37/:52`). CI: publish gated on linux-rehearsal
(`release-artifacts.yml:174`), cosign a hard step (`:205-212`).

## Locked Decisions

1. **Binary release; Latest moves to 1.0.2.** Tag WITHOUT `[skip-artifacts]`;
   full CI build → cosign → publish; manual tap fill (1.0.0 → 1.0.2); the
   packaged §F–§I re-attestations against the PUBLISHED artifact are the v1.0.1
   artifact-line catch-up proof and part of this release's Definition of Done.
2. **Per-test isolation, never invariant weakening.** The ADR 0015/0031
   registration↔Settings-schema coupling stands; every de-flake seeds or owns
   state per test. Production code gains injection SEAMS (default = the global
   singleton) — behavior in production is unchanged, proven by the freeze sweep,
   Dialyzer, and identical default return values, ordering, diagnostics,
   provenance, and authority decisions (ADR 0082 binds this).
3. **Seam before conversion.** M2's complete registry thread-through and
   side-effect-isolated fixture land before any dependent M3 lane demotion.
   Files that cannot isolate cleanly stay in their serial lane — no tag changes
   without the isolation actually done (the reconciliation rule stays honest).
4. **Security-eval and external-runtime lanes stay serial by policy.** No
   promotion of `security_eval_serial`/`external_runtime_serial` files; their
   fix (where needed) is solo-determinism via seeding, not lane demotion.
5. **The web split has a safety story** (the v0.41 M9 condition): a split
   `WorkspaceLiveTest` file may carry `liveview_serial` only if it runs with
   `server: false` semantics, owns its home/settings state, and touches no
   runtime singleton that the partition runner doesn't own — the checklist is
   codified in test-strategy.md as part of M4, and every split file passes
   `inventory --check-tags` + the solo/batch determinism proof.
6. **De-flake is red-first where a seam exists**: each residue fix demonstrates
   the failure solo on the pre-fix code, then green solo AND green in the batch
   combination that previously flipped it (both orders recorded in Build
   Progress).
7. **Measurable acceptance, honest scope.** Acceptance = the five named residues
   deterministic; the seam landed; wave-1 conversions + the web split measured
   against the M0 re-baseline. The "~20" full-`mix test` estimate is re-measured
   with an exact prior-failing batch matrix and 20 recorded full-suite seeds,
   and recorded at M8 — reducing it is the goal; eliminating every unknown
   order-dependence is NOT promised by phase 1 (remainder stays on the track's
   future-features entry with the new count).
8. **Dependency refresh per the standing rule**: review-always (three-root
   `hex.outdated`, dedup, disposition table), apply-bounded (the four verdicted
   items in two waves: swoosh+tailwind+jsv, then the ecto pair with the exqlite
   lock-diff decision recorded), own commit(s).
9. **ADR 0081 (Tier-2→Tier-1 promotion) is a docs deliverable** — it changes no
   code and no current tier assignment; it defines the process (criteria,
   freeze-sweep row addition, versioning interplay) for future promotions.
10. **No new EvalInventory rows / no `:v102` milestone atom.** Nothing here adds
    authority. `@release_v102_steps = @release_v1_steps ++ @v102_focused_steps`
    (precedent: v066/v101 — no predecessor bespoke steps).

## Operator-Visible Win

Packaged users finally get the v1.0.1 fixes: `brew upgrade allbert` / the curl
installer move 1.0.0 → 1.0.2 (TUI launches, browser research works end-to-end
behind one consent, channel-send routes correctly, CSS is content-hashed). For
the operator-as-developer: the daily gates get faster and honest — the five tests
that flaked through the entire 1.0.1 build are deterministic, the biggest web
file no longer serializes the release web phase, and the measured gate times are
re-baselined and recorded so future regressions are visible.

## Planned Interfaces

- `mix allbert.test release.v102` — the v1.0.2 release gate.
- No new product interfaces. The registry seam parameters are internal
  (`opts`/`server:` with global defaults), not operator-facing.

## Code Seams

**S1 — residue de-flake (M1).** The five fixes exactly as tabled in Current Code
State: (a) owned home + db file marker in `workspace_live_test.exs:1053`'s setup
(gate: `cli/first_run.ex:172-175`); (b) lazy/injected `agent_runner` via
`start_tui_adapter/1` (`v055_tui_channel_eval_test.exs:418`); (c)+(d) registry
baseline seeding in setup (clear + register expected set + restore) and
`@moduletag :global_process_serial` added to `corpus_completeness_test.exs`;
(e) `ensure_research_descriptors!()` added to
`test/support/provider_preconditions.ex` and called in `golden_set_test.exs`
setup.

**S2 — registry injection seam (M2, ADR 0082).** Thread one internal context,
`app: [server: ...], plugin: [server: ...], actions_overlay: ...`, through every
read needed by the named targets: Actions module/name/capability/metadata,
diagnostic, provenance, agent and internal wrappers; Intent action, descriptor,
surface, normalization and active-app reads; Skills app/plugin roots;
Extensions.Registry's existing nested option shape; and capability provenance
lookups. A reusable fixture owns unique process names and ETS table names.
Internal-only `side_effects: false` fixture registration suppresses global signals
and shared Settings-schema cache invalidation while production defaults remain
unchanged. Prove identical default values, order, diagnostics, provenance and
authority decisions with focused suites, a concurrent two-context negative test,
static gates, Dialyzer, `release.v1`, and the authoritative release gate.

**S3 — lane conversions wave 1 (M3).** Audit exactly three mandatory candidates:
`intent/engine_test.exs`, `skills/registry_test.exs`, and
`actions/browser_actions_test.exs`. Convert only a file whose complete registry,
DB, app-env, home, process, HTTP-stub and filesystem ownership audit passes.
`intent/engine_test.exs` remains `db_serial` or becomes `db_partition_safe` unless
DB ownership is genuinely removed; registry isolation never implies `pure_async`.
Record every outcome, old/new lane and ownership rationale. Beyond the three
mandatory candidates, **opportunistic demotions are permitted where the full
ownership audit is trivially clean** (the file already owns everything and only
its tag is conservative) — recorded per file, no quota, no seam work beyond M2's;
anything needing new isolation machinery is a backlog observation for later
phases, not added scope. (Third-pass note: this restores the bounded sweep the
operator's "full first phase" selection asked for; the second pass's hard
three-file cap under-delivered it.)

**S4 — web split (M4).** Split `workspace_live_test.exs` (2,000+ lines, 88
tests, 256.6s) into cohesive `liveview_serial` files: shell/nav, chat/turn,
onboarding, confirmations/approvals, canvas/tiles, Settings Central, and
destinations. Each satisfies Locked Decision 5. Keep only tests that boot or
depend on the live Runtime singleton, external browser/endpoint processes, or
other shared OS resources in a small enumerated `external_runtime_serial` file.
Fold split files into `fast-local --web-lanes`, codify the checklist in
`test-strategy.md`, require no split file to exceed 25% of the post-split web
release phase, and require that phase to measurably improve from M0. Correctness
and honest lane classification remain the hard gates.

**S5 — v0.58 tails (M5).** Current-code re-verification leaves seven work
families. Tail A: 262 private `field` definitions across apps/plugins; repeated
limit clamps (including tools sources/actions and memory list); `"local"` /
`"web-local"` identity constants in Workspace/Jobs; and Objectives links that
still leave the consolidated workspace shell. Tail B: add the missing
`data-theme=null` regression assertion; replace bespoke `response_error/1` in
the marketplace catalog and voice-local panel with `Actions.ErrorExtraction`;
and fix/test `Maps.get_any/3` falsy-value semantics. The previously listed
ErrorExtraction unit-test gap is already closed by `actions/helper_test.exs`;
the stale CHANGELOG line is no longer present, so neither is v1.0.2 work.
Focused gates: helper/maps tests, marketplace and voice panel tests, page/theme
tests, workspace navigation tests, plus focused tests for each mechanically
consolidated helper family.

**S6 — ADR 0081 (M6).** New `docs/adr/0081-tier2-to-tier1-promotion-process.md`:
when a stabilizing Tier-2 contract earns promotion (criteria: released across ≥N
minors unchanged, exercised by gate-bound rows, no pending redesign intake), the
process (an ADR records the promotion; the `:v1` freeze sweep gains the
exact-name row in the same change; Tier-1 changes thereafter require a major),
and the inverse (Tier-2 stays additive; demotion is not a thing — removal needs
a major). Reconcile `docs/developer/public-contract-freeze.md`.

**S7 — dependency refresh (M7).** Wave 1: `mix deps.update swoosh tailwind jsv`
+ scoped suites + the tailwind CSS byte-diff (build `assets` before/after —
expect zero delta). Wave 2: `mix deps.update ecto_sql ecto_sqlite3` → inspect
the lock diff (expect ecto 3.14.1 + decimal 3.1.1). Accept exqlite 0.38.x only
when selected by that coordinated update and migration, concurrency,
backup/restore, full-core, and packaged-smoke gates all pass; otherwise retain
the existing exqlite lock and record the scoped-out disposition → migration round-trip + full core
suite. Review-always table for everything else (req_llm/exqlite drift noted;
PLV 1.2 pinned out by design). Own commit per wave.

**S8 — final measurement, docs, and release candidate.** Add the usage line,
dispatch, and exact `@v102_focused_steps` using the existing release runner's
map shape (`cwd: :root, executable: "mix", args: [...]`):
`v102_lane_reconciliation` uses
`args: ["allbert.test", "inventory", "--check-tags"]`; five separately
named solo steps (`v102_residue_workspace`, `v102_residue_tui`,
`v102_residue_intent_agent`, `v102_residue_corpus`, `v102_residue_golden`); one
`v102_residue_batch_matrix` step running both recorded prior-failing orders; one
`v102_web_lanes` step invoking
`mix allbert.test fast-local --web-lanes --partitions 4`; and dependency
workflow/migration/backup-restore steps not already present in the authoritative
release gate. Then
`@release_v102_steps @release_v1_steps ++ @v102_focused_steps`; runner +
step fn clones with `release_evidence/v102/`. Version bump commit: three
`mix.exs` → `"1.0.2"`, SW `CACHE_NAME` → `v1.0.2` + `gzip -kf9` regeneration
(guards enforce). CHANGELOG `## v1.0.2`. The release-candidate commit contains
the final timing/census and 20-seed flake tables, future-features dispositions,
accepted ADRs, reconciled developer docs, and green `mix allbert.test release`,
`release.v1`, and `release.v102`.

**S9 — binary publication and administrative closeout.** Push parity → annotated
tag `v1.0.2` (NO `[skip-artifacts]`) → CI publish (Linux rehearsal gates publish;
cosign hard) → manual tap fill → install rehearsal → packaged §F–§I
re-attestations → evidence ledger. Only after published-artifact validation,
archive the triad and mark the roadmap shipped in a post-tag docs-administration
commit; that commit is not missing tagged-product content.

## Development Lane Annotation

- **Parallel workstreams:** WS-A (M1 residue fixes — five independent test
  files + one support helper); WS-B (M5 tails — disjoint from test files);
  WS-C (M6 ADR 0081 — docs only); WS-D (M7 wave 1 deps — lockfile+scoped
  suites). These four can interleave.
- **Serial barriers:** M0 records the initial inventory failure, M1 corrects its
  missing tag, and the authoritative baseline is then captured before M2 — all
  timing claims derive from that post-reconciliation baseline.
  M2 (seam) strictly before M3 conversions (Locked Decision 3). M3 before M4's
  fast-local fold (the web split reuses the demotion checklist). M7 wave 2
  (ecto) after WS-A/M3 land (a full-suite validation run should test the final
  test topology, not churn twice). M8 last: gate wiring → full precommit →
  bump → all three release gates. M9 alone owns the operator-held tag,
  publication, packaged attestations, and administrative closeout.
- **Focused tests/gates:** per-file solo + batch runs for every de-flake and
  demotion; `mix allbert.test inventory --check-tags` after every lane change;
  fast-local after M3/M4 with timings recorded; no `mix | tail` pipes.
- **Full-precommit timing:** once at the M8 barrier (plus the M7 wave-2 full
  core suite).
- **External smokes:** none until M9's packaged attestations (which require the
  PUBLISHED artifact).
- **Rejoin points:** test-strategy.md census + lane tables re-recorded at the M0
  baseline and M8 final measurement; drift review + docs reconciliation at M8
  before tagging; triad archives only after M9 artifact validation.

## Security Posture

No authority, permission, signal, settings, or protocol changes. The registry
seams are read-path parameters defaulting to production globals (ADR 0082
explicitly forbids using them to bypass registration authority); the freeze
sweep and v066 product-RC sweep re-run inside `release.v102`. Dependency wave 2
touches the persistence layer — validated by migration round-trip + the full
core suite; swoosh's gap includes a CVE fix (unused adapter, taken as hygiene).

## Non-Goals

- Eliminating ALL unknown order-dependence in full `mix test` — phase 1 fixes
  the documented five, lands the seam, converts what isolates cleanly, and
  re-measures; the remainder (with its new count) stays on the future-features
  track entry.
- Converting `security_eval_serial` / `external_runtime_serial` files (policy
  lanes — Locked Decision 4).
- phoenix_live_view 1.2, req 0.7+, and the deliberately pinned constraints —
  future refresh items.
- WSL2 DIT-1 leg — operator re-deferred 2026-07-15 (no Windows host).
- Intent-pipeline refinements (opportunistic ladder item) — only if a de-flake
  demands one.

## Milestones

Execution modes: `[gate]` gate-proved-contract, `[smoke]` scripted host smoke
against a built binary, `[browser]` real-browser manual, `[host]`
cross-platform manual, `[model]` real-egress model manual.

- **M0: Census preflight — EXECUTED at build start (2026-07-15).** `[gate]`
  `mix allbert.test inventory --check-tags` run UNPIPED: **exit 1 with 173
  findings** — 66 files with no primary lane tag (incl. two added in v1.0.1),
  **42 mis-tagged** (checker-expected lane ≠ the file's tag), and **65
  multi-tagged** (more than one primary lane tag). Correction to the
  third-pass note: the checker DOES fail on findings — the earlier "exits 0"
  observations were the documented `mix | tail` exit-masking trap (both probe
  runs were piped); no checker fail-mode work is needed, and the
  `v102_lane_reconciliation` gate step is already meaningful. The full finding
  list is the M0 artifact. *Acceptance (met):* the 173-finding preflight log
  and raw census recorded in Build Progress.
- **M1: Residue de-flake + tag reconciliation (S1).** `[gate]` The five
  documented failures fixed per the root-cause table, each red-first solo where
  reproducible, then green solo AND green in the previously-flipping batch
  (both orders in Build Progress). Then resolve ALL 173 reconciliation
  findings: untagged files take the checker-suggested lane unless the file's
  own state audit contradicts it (adjudications recorded); multi-tagged files
  keep exactly one primary lane (the strongest resource class, per the
  taxonomy's reconciliation rule); mis-tagged files are adjudicated per file —
  the checker's expectation derives from template/content heuristics and may
  itself be wrong; where the file's tag is right, record why and align the
  file so the checker agrees (or fix the heuristic if it is systematically
  off — recorded either way). Then capture the authoritative baseline: lane
  census; fast-local quick and high-coverage; prepush; `release.v1`; and
  per-lane partition timings in test-strategy.md. *Acceptance:* all five
  residue files pass solo, in their prior failing combinations, and in their
  full lanes; `inventory --check-tags` exits 0 with ZERO findings; the
  post-reconciliation M0 baseline table is committed before M2.
- **M2: Registry injection seam (S2, ADR 0082).** `[gate]` Land the complete
  context, side-effect-isolated fixture, and concurrent cross-contamination
  proof; accept ADR 0082 in that same commit. *Acceptance:* focused
  App/Plugin/Extensions/Actions/Intent/Skills suites, compile -Werror, Credo,
  Dialyzer, `release.v1`, and the authoritative release gate are green; default
  returns, order, diagnostics, provenance, and authority behavior are identical.
- **M3: Lane conversions wave 1 (S3).** `[gate]` Complete the resource audit for
  exactly engine_test, skills/registry_test, and browser_actions_test; convert
  only those proven safe. *Acceptance:* all three have a recorded outcome,
  old/new lane and ownership rationale; DB-bound tests stay DB-classified;
  inventory reconciles; converted files are green solo/batch/full-lane. Other
  candidates are backlog observations, not added scope.
- **M4: Web split (S4).** `[gate]` `WorkspaceLiveTest` split into
  `liveview_serial` files passing the Locked Decision 5 safety checklist
  (codified in test-strategy.md); runtime-heavy remainder stays
  `external_runtime_serial` and is enumerated; split files fold into
  `--web-lanes`. *Acceptance:* web release-phase and `--web-lanes` timings vs
  M0 recorded; no split file exceeds 25% of post-split web phase and that phase
  measurably improves — the remaining external file is a small enumerated set,
  not 83% of web wall-clock; all
  split files green solo and partitioned; residue (a) stays green in its new
  home.
- **M5: v0.58 cleanup tails A+B (S5).** `[gate]` Close the seven currently
  verified work families; do not recreate the already-present ErrorExtraction
  unit test or a no-longer-stale CHANGELOG edit. Fix `Maps.get_any/3` falsy
  semantics red-first. *Acceptance:* every family has a current anchor and named
  focused suite; no behavior changes beyond the documented corrections.
- **M6: ADR 0081 Tier-2→Tier-1 promotion process (S6).** docs-only.
  *Acceptance:* ADR Accepted; `public-contract-freeze.md` reconciled;
  `mix allbert.test docs` green.
- **M7: Dependency refresh (S7).** `[gate]` Standing-rule refresh: three-root
  `hex.outdated` review table; wave 1 (swoosh, tailwind wrapper, jsv) with the
  CSS byte-diff proof and scoped suites; wave 2 (ecto_sql + ecto_sqlite3 with
  ecto/decimal rides and the exqlite decision recorded) with migration
  round-trip + full core suite. Own commit per wave. *Acceptance:* disposition
  table complete (applied/scoped-out/blocked per entry incl. req_llm/exqlite/
  PLV notes); all validation suites green; CSS byte-diff zero.
- **M8: Final measurement, docs, and release candidate (S8).** `[gate]` Run 20
  full-suite seeds with `mix test --seed <recorded-seed>` from a clean owned test
  home; classify every failure as reproduced residue, new order dependence,
  deterministic failure, or environment failure, and isolate-reproduce each
  product failure. **Cost note (third pass): a monolithic full suite has
  historically run 13–22 min, so 20 seeds is a 4.5–7.5 hour campaign — run it
  backgrounded/overnight with seeds pre-recorded; it is a one-time measurement,
  not a repeatable gate step, which is why it lives in M8 and NOT inside
  `@release_v102_steps`.** Record final lane/timing tables against M0 and write surviving
  work to future-features. Accept ADRs, reconcile test/freeze docs, bump 1.0.2 +
  SW/gzip, add CHANGELOG, and run `mix precommit`, authoritative
  `mix allbert.test release`, `release.v1`, and exact `release.v102`. *Acceptance:*
  the release-candidate commit contains every tagged-product/document deliverable
  and all three release gates are green with evidence.
- **M9: Binary publication and closeout (S9).** `[smoke]`+`[host]`+`[browser]`+
  `[model]` Prove push parity; cut the operator-held annotated tag without
  `[skip-artifacts]`; verify CI's three tarballs, mandatory SHA256SUMS cosign
  bundle and Latest status; fill/audit the tap; run install rehearsal and
  published-artifact §F–§I plus spot smokes. *Acceptance:* ledger complete, tap
  and curl resolve 1.0.2, packaged catch-up passes. Then archive the triad and
  mark the roadmap shipped in a post-tag docs-administration commit.

## Documentation Deliverables

- ADR 0081 (promotion process) and ADR 0082 (registry injection seams).
- `docs/developer/test-strategy.md`: post-reconciliation M0 baseline census +
  timings; the web-split safety checklist; M3/M4 lane-table updates; M8 final
  measurements and 20-seed flake table.
- `CHANGELOG.md` `## v1.0.2` entry.
- `docs/plans/roadmap.md` ladder item 2 updated at closeout (shipped; artifact
  catch-up obligation discharged); `docs/plans/future-features.md` — the shipped
  entries removed per the backlog lifecycle rule (Test Suite Speed & Isolation
  updates in place with the phase-1 remainder; the folded web-split entry, both
  v0.58 tails, and the promotion-ADR entry removed), the remainder re-parked
  with fresh counts.
- This triad archived to `docs/plans/archives/` at closeout.

## Definition Of Done

- The five residue tests deterministic solo + batch — `[gate-proved-contract]`
  (`v102_residue_solo` steps).
- Complete registry context and side-effect-isolated fixture landed; default
  values/order/diagnostics/provenance/authority unchanged; ADR 0082 Accepted —
  `[gate-proved-contract]` (focused suites + negative concurrency proof + static
  gates + Dialyzer + freeze and authoritative release gates).
- Wave-1 conversions + web split done with `inventory --check-tags` reconciling
  and timings recorded vs the M0 baseline — `[gate-proved-contract]` +
  measurement evidence.
- v0.58 tails A+B closed; ADR 0081 Accepted — `[gate-proved-contract]` / docs.
- Dependency refresh: four applies validated (CSS byte-diff zero; migration
  round-trip green; disposition table complete) — `[gate-proved-contract]`.
- authoritative `mix allbert.test release` + `release.v1` + `release.v102` GREEN
  at the tagged commit —
  `[gate-proved-contract]`.
- v1.0.2 published as a BINARY release: cosign-signed GitHub Latest, tap at
  1.0.2, install rehearsal recorded — `[operator-attested-smoke]`.
- Packaged §F–§I re-attested against the published artifact (the v1.0.1
  artifact-line catch-up) — `[operator-attested-smoke]`.
- M8 measurement table + future-features turnover present in the tagged commit —
  `[gate-proved-contract]` / docs.
