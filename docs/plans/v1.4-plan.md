# Allbert v1.4.0 Adaptive Usage Profiling

Request flow: `docs/plans/v1.4-request-flow.md`
ADRs: one new ADR ships with this release — ADR 0090 (adaptive usage
profiling and confirmed customization — binding on M1–M6), plus the
ADR 0084 amendment of 2026-07-24 (additive `:suggestion` notification
kind, quiet hours, per-class rate limit — binding on M6). Amends the
ADR 0045 boundary deliberately (suggestions stay inert; the confirmed
application path is new). Consumes ADR 0089 (v1.3 memory — same episodic
sources, shared propose/review grammar), ADR 0088 (catalog — per-role
profiles read it), ADR 0072/0061 (model substrate), ADR 0008 (durable
confirmations), ADR 0080/0074 (shell + design system — the mobile rider),
ADR 0031/0006, and the v1.0 freeze (`release.v1` stays green).

Status: **implementation-ready plan; BUILD MUST NOT START until v1.3.0 is
tagged, published, and its triad archived.** M0 re-verifies every Current
Code State anchor at build-start HEAD.

Source: roadmap ladder item 8 (1.4 flagship: stages a/b/c — usage memory,
distill/suggest, confirmed customizations + effectiveness) with the
operator-confirmed riders: Per-Role Fast/Capable/Thinking Model Profiles
(`future-features.md:934-944`), Proactive Notifications Policy
(`:418-441`, scoped as the suggestion-delivery stage), and — operator
decision 2026-07-24 — Mobile-Ready Web UI/UX **stage 1 only**
(`:663-691`, with Dynamic Mobile Breakpoints `:694-704` folded in as its
enabler). Operator decisions 2026-07-24: additive-only 1.2–1.4;
flagship-focused FV validation; mobile stage 1 rides here.

## Build Progress

(Empty until build start; entries per the standing convention — measured
claims cite the metrics store; intake candidates held for operator
disposition.)

## Purpose

Ship the 1.4 flagship: Allbert profiles its own usage, proposes concrete
improvements with evidence, applies exactly what the operator confirms —
one click, one durable confirmation — and reports whether each applied
change actually helped. Per-role model profiles and proactive suggestion
delivery ride the same loop; the web workspace becomes genuinely usable
on a phone.

1. **A usage-signal store that always works.** Additive, local, redacted
   per-turn usage events (decision outcome, action outcome class, model
   profile, surface, timing bands — never bodies or secrets), joined
   with the existing durable sources; bounded retention (ADR 0090 §1).
2. **Distill/suggest as a managed job.** Periodic, bounded, zero-egress
   profiling produces inert suggestion cards carrying evidence, a
   concrete proposed change, and a predicted benefit; the dead
   `dismissed` status gains its writer and the approve-resume gap is
   fixed (ADR 0090 §2).
3. **One-click CONFIRMED customization.** `apply_suggested_customization`
   is `confirmation: :required` and safety-floor-pinned; changes are
   limited to the shipped allowlist (never permissions/security/egress/
   credentials/fallback gates); the confirmation shows the exact
   before→after diff; application is audited (ADR 0090 §3).
4. **Effectiveness feedback.** Applied changes link suggestion →
   confirmation → audit; the next distill measures the delta and the
   panel shows it; revert is one confirmed click (ADR 0090 §5).
5. **Per-role model profiles.** `model_roles.fast|capable|thinking`
   formalized as additive fragments over the ADR 0088 catalog; profiling
   may propose remaps only through the confirmed path (ADR 0090 §4).
6. **Proactive suggestion delivery, default OFF.** The `:suggestion`
   kind on the ADR 0084 boundary with separate opt-in, quiet hours, and
   a per-class rate limit (ADR 0084 amendment).
7. **Mobile-ready web, stage 1.** Breakpoint token roles in the design
   system + phone-form-factor usability for the primary surfaces —
   chat, objectives, settings, memory — at 390 px with no horizontal
   scroll; stages 2–4 explicitly out (rider).

## Expected Inputs

- Stable v1.3.0 shipped and archived (build-start precondition) — the
  memory substrate, `release.v13`, the propose/review grammar operators
  already know, and the shipped Search Central conversation API. v1.4 consumes
  those contracts; it does not reopen v1.3's Search ownership decision.
- v1.2's catalog/chooser (`Models.Catalog`, ADR 0088) and fallback
  policy; the v1.1 notify boundary + ledger + consent-offer pattern.
- The v0.47 suggestion/draft machinery, confirmations spine, jobs
  substrate, and usage sources — anchored below.
- Gate machinery: `@release_v13_steps` composition, `:v14` enum slot,
  behavioral sweep meta-contract, docs-gate active-plan list.
- Metrics-store baselines re-recorded at M0.

## Current Code State

Verified 2026-07-24 at v1.1 M12.13 HEAD (recon pass). M0 re-verifies at
build-start HEAD (v1.2/v1.3 will have landed by then — their deltas are
themselves anchors to re-verify).

**v0.47 suggestion pipeline (ADR 0045) — alive, passive.**
`SelfImprovement.TraceIndex.index/1` over `memory/traces/**/*.md`
(`trace_index.ex:47,80,116`; gates `self_improvement.enabled` +
`.trace_index.enabled` `:86-96`; four pattern types `:236-308`).
`Discovery.discover/2` (`discovery.ex:31,67,118-122`) maps pattern →
suggestion type and persists via `tool_discovery_suggestions`
(`tools/discovery/suggestion.ex:42`; statuses
`pending|accepted|dismissed|expired` `:16`; provenance
`self_improvement` `:29`; `draft_id` link). Draft artifacts under
`drafts/*` (`drafts/store.ex:20,741-784`; tiers `draft|discarded|promoted`
`:29`; `live_authority` hardwired false `:281`). Generation is on-demand
only (`discover_patterns`, read-only, `discover_patterns.ex:33`) — **no
job schedules it**. Promotion: six `promote_*_draft` actions →
`Confirmations.create` → resume (`promotion_action.ex:37-68`;
`approve_confirmation.ex:1371`) — **`@self_improvement_action_names`
(`approve_confirmation.ex:63-68`) omits `promote_template_draft` and
`promote_capability_gap_draft`** (resume gap; v1.4 fixes). Web panel is
display-only for self-improvement cards — **no accept/promote handler**
(`workspace/discovery_suggestions.ex:123`; only MCP candidates have a
button `:110-120`). **The `dismissed` status has no writer** (verified).

**Usage signals — rich, not queryable.** Intent decisions are transient
(`intent/router/outcome.ex:16`; no persistence in the router); they
surface only in trace markdown (`trace.ex:944,956`), the per-turn
`action_log` map on assistant messages (`runtime.ex:1140,1159-1172` —
status, actions, decision, resource_access, diagnostics), and
`scheduled_job_runs.decision`. Traces gated by `runtime.trace_default`
(`trace.ex:29`) — sparse if never enabled; token estimate is a word
count (`trace.ex:158`). Durable structured sources:
`conversation_threads/messages` (`conversations/thread.ex:16`,
`message.ex:19` — role, content, `action_log`, trace_id),
`objective_events` (`objectives/event.ex:40-47`), jobs runs
(`jobs/run.ex:18`), privileged markdown audits (`runtime/audit.ex:67` —
six kinds incl. `:channel_notify`). Security decisions are NOT a durable
log (`security/audit.ex:16`; reconstruction via `security/review.ex:49`).
No retention/pruning anywhere.

**The settings-write trap (the load-bearing fact for §3).**
`permissions.settings_write` defaults `:allowed`
(`security/policy.ex:79`); `configure_channel_setting`
(`configure_channel_setting.ex:13-19`) and `update_setting`
(`update_setting.ex:32`) are `confirmation: :not_required`; the safety
floor forces `:needs_confirmation` only for named actions
`migrate_secrets` (`policy.ex:412-413`) and `apply_persona_profile`
(`policy.ex:419-420`) — **the precedent v1.4's
`apply_suggested_customization` pin follows**. Settings write path:
`Settings.put/3` (`settings.ex:59-73`) → validate
(`schema.ex:4571`) → store; fragments registry
(`settings/fragments.ex:24-31,74` — "fragments do not grant write").
Confirmations: durable YAML + markdown audit (`confirmations/store.ex:
22-28`); record fields incl. `operator_resolution` with
`target_status/target_result/target_resumed?`
(`record.ex:18-41,138-164`) — the closest existing outcome record for
the effectiveness loop.

**Per-role groundwork.** ADR 0061 tiers are embedding/router/escalation
(NOT fast/capable/thinking): `intent.router_embedding_profile` /
`router_model_profile` (`router_local` = llama3.1:8b) /
`router_escalation_profile`. `fast` is the only schema-default profile
of the alias family (`schema.ex:3744`); `capable/thinking` exist only in
`priv/provider_catalog/models.json:111-177` and the codegen alias map
(`dynamic_plugins/codegen/llm.ex:127-136`); preference lists reference
them without schema defaults (`schema.ex:3675,3679`). Selection sites:
embedder (`req_llm_embedder.ex:17,59`), disambiguator
(`req_llm_disambiguator.ex:36,135` — optimizer rejects non-local
`:399-413`), decomposer (`req_llm_proposer.ex:48-49` — reuses the router
profile), coding (`coding/config.ex:29,155-160` — `coding.model_profile`
default `pi_coding_local`), classifier (`classifier.ex:101`), direct
answer (`direct_answer.ex:104`), primary chat (`models.ex:102,133-138`).
**Steering is deterministic — no model role exists there.** Legacy
`intent.model_profile`/`intent.direct_answer_model_profile` are compat
aliases (`settings.ex:15-16`; normalizers `store.ex:251,262`; specs
`schema.ex:671,741`); `set_active_model_profile` still writes the legacy
key (`set_active_model_profile.ex:50-60`); removal is
migration-runner-blocked (per ADR 0046) and stays out (additive-only).

**Notify boundary (ADR 0084).** `channels/notify.ex:24` `@kinds
[:status, :completion, :confirmation_request, :consent_offer]`; ordered
gate `authorize_and_send/5` (`:119-138`); Security consult `:228-238`
(`permissions.channel_autonomous_notify`, `schema.ex:2891-2900`);
fragment `settings_schema/2` (`notify.ex:27-49` — enabled/level/
min_interval; **no kinds key, no quiet hours**; M0 verifies the fragment
wiring — no lib caller of `settings_schema/2` was found though defaults
are live per `notify_test.exs:251-263`). Ledger
(`notify_delivery.ex:12`; states `:8`; kinds `:9`; unique delivery_key
`:75`; attempts ≤2 `:65`). Throttle: `:status` only (`notify.ex:
210-226`). Consent offer once-only via ledger
(`notify.ex:64-106`) + exact typed command + identity re-proof
(`notify_consent_callback.ex:10-42`) — the reusable opt-in template.
Adding `:suggestion` touches: `notify.ex:24,193-208,210,310-319`;
`notify_delivery.ex:9`; a producer; tests; ADR text (the enumerated
v1.4 amendment).

**Jobs substrate.** Targets `runtime_prompt` | `registered_action`
(`jobs/runner.ex:69-82`); schedules on TimeZoneInfo
(`jobs/schedule.ex:6,62-85`); **`jobs.default_state` defaults
`"paused"`** (`jobs.ex:434-437`); the ReviewCadence managed-job pattern
(`memory/review_cadence.ex:1-16`) is the distill-cadence template; by
build start v1.3's consolidation job is the second exemplar.

**Mobile baseline.** Tailwind v4 CSS-first (no config file;
`config/config.exs:54-63`; single 3,417-line `app.css` holding all
`--allbert-*` tokens); **exactly one static breakpoint** — 768px /
767.98px (13 media queries; mobile shellbar `app.css:820-825,993-1022`;
desktop grid vs `data-mobile-tab` stack `:1325-1401`; sticky composer
`:1393`; outliers 760px `:3232`, `pointer: coarse` `:3301`). Viewport
meta present (`root.html.heex:5`). Shell per ADR 0080
(`workspace_live.ex:799-895` — `#workspace-shell`,
`data-layout-mode`/`data-mobile-tab`, `workspace_mobile_tabs/0`
`:2850`); read-only setting `workspace.mobile.breakpoint_px` = 768.
Known gaps: no breakpoint roles / nav overflow behavior
(`docs/design/design-system-gap-analysis.md:31-71`); Dynamic Mobile
Breakpoints deferred since v0.26. Responsive pin test:
`workspace/responsive_test.exs` (`pure_async`, pins 768/767.98).

**Tests and lanes.** Suggestions/drafts (`app_env_serial`), settings
(`external_runtime_serial`), confirmations (`app_env_serial`), notify
(`app_env_serial`), jobs (`pure_async` schedule + agent tests), web
(`liveview_serial` default; responsive + tokens `pure_async`).

## Locked Decisions

1–3 operator-locked (2026-07-24); the rest derived for veto at review.

1. **Mobile stage 1 rides v1.4 as non-flagship scope (operator).**
   Stage 1 + Dynamic-Breakpoints fold-in only; stages 2–4 (API surface,
   remote auth, native shell) explicitly out; the rider may not displace
   flagship milestones — if squeeze occurs, the deferral proposal goes
   to the operator (no unilateral deferral).
2. **Additive-only release (operator, standing).** Usage store, roles
   fragments, suggestion types, notify keys — all additive; legacy
   aliases untouched; no migration-runner pull-forward.
3. **Flagship-focused FV validation (operator, standing).** Disposable
   Homes; FV opens once after one green uninterrupted authoritative gate
   at the pushed candidate SHA; no validation-only product capability.
4. **Confirmed customization is structurally forced (derived, ADR 0090
   §3).** `apply_suggested_customization` is `confirmation: :required`
   AND safety-floor-pinned by action name; the allowlist ships in code,
   reviewed, and categorically excludes permissions/security/egress/
   credentials/fallback gates; the confirmation renders the exact
   before→after diff.
5. **Suggestions stay inert (derived, ADR 0045 posture).** Accept-click
   creates a confirmation; nothing applies without approval; dismissed
   gains its writer; the resume-gap bug is fixed in M2.
6. **The usage store is additive, local, redacted, bounded (derived,
   ADR 0090 §1).** Never bodies/prompts/secrets (schema-level, eval-
   bound); cap + age retention keys; always-on (it is telemetry-free
   local bookkeeping, not tracing) with a kill switch
   `profiling.usage_events.enabled` default `true` — operator veto at
   plan review.
7. **Distill posture (derived).** Managed job, ReviewCadence pattern,
   `profiling.distill.*` keys, explicit activation (paused-default
   gotcha), zero-egress, local-model assist only under ADR 0089 LD-R5
   bounds; default cadence weekly, kill switch honored; quality floor
   measured on real fixtures (no synthetic-template evidence — the
   v1.1 M12.8 lesson).
8. **Per-role profiles are a naming layer (derived, ADR 0090 §4).**
   `model_roles.<role>.profile` fragments resolve through existing
   `Settings.Models`; no parallel resolver; purpose keys unchanged;
   chooser renders roles; remaps only via the confirmed path.
9. **`:suggestion` notify kind per the ADR 0084 amendment (derived).**
   Separate opt-in default OFF; quiet hours + per-day rate limit
   additive keys; ledger/throttle/audit unchanged; notification is never
   an approval channel.
10. **Effectiveness is measured, displayed, never auto-acted (derived,
    ADR 0090 §5).** Suggestion → confirmation → audit linkage; next
    distill computes deltas; revert is one confirmed click; no
    automatic rollback.
11. **Gate = `release.v14` stacked on `release.v13` (derived).**
    `@release_v14_steps = @release_v13_steps ++ @v14_focused_steps`;
    evidence `release_evidence/v14/`; docs-gate list updated at build
    start.
12. **Behavioral evals + automated flagship loop first (derived,
    standing).** `:v14` enum first; rows per request flow §G with the
    meta-contract; the seeded-usage → distill → confirm → apply →
    measure loop is a CI regression before FV exists.
13. **Mobile stage 1 acceptance is concrete (derived).** Breakpoint
    token roles land in the design system doc + tokens; at 390×844:
    chat (composer usable, sticky, no horizontal scroll), objectives
    tree, settings central, memory review all operable with
    ≥44px touch targets; the 768 pin test evolves rather than breaks;
    no page-local CSS systems (ADR 0074 rule).
14. **Fresh/disposable Homes for all scenarios (derived, standing).**

## Operator-Visible Win

Allbert starts noticing how you actually use it — and instead of silently
changing anything, it shows you cards: "your quick lookups wait on a slow
model — map the fast role to llama3.2:3b (evidence: 40 turns, p50 9s)".
One click, one confirmation showing exactly what changes, done — and two
weeks later the card says whether it worked, with a one-click revert.
Opt a channel in and the card headline can reach you there — never at
night, never more than a few a day. And the whole workspace finally works
one-handed on your phone.

## Planned Interfaces

- `AllbertAssist.Profiling.UsageEvents` — additive table + writer at turn
  close (decision kind/action/confidence band, outcome class, profile,
  surface, timing bands; redaction-proven); retention keys
  `profiling.usage_events.{enabled,max_rows,max_age_days}`; bounded
  read API for distill.
- `AllbertAssist.Profiling.Distill` — registered action
  (`distill_usage_profile`, `exposure: :internal`) + managed job
  (`profiling.distill.{enabled,cadence}` via the ReviewCadence pattern);
  emits additive suggestion types through the existing suggestion store
  with evidence payloads; run summaries redacted.
- `apply_suggested_customization` — registered action,
  `confirmation: :required`, safety-floor-pinned; customization
  allowlist module (shipped, reviewed list of safe-write keys);
  before→after diff in the confirmation; audit linkage fields
  (suggestion_id, confirmation_id) additive on the suggestion record;
  `revert_suggested_customization` through the same path.
- Suggestion lifecycle completion: `dismiss_suggestion` writer; resume
  list fix for the two missing promote actions; web panel gains
  accept/dismiss/apply-flow handlers (render-and-dispatch only); CLI
  `mix allbert.profiling suggestions|apply|dismiss`.
- `model_roles.{fast,capable,thinking}.profile` fragments + resolution
  aliases in `Settings.Models`; chooser role rows; role-remap suggestion
  type.
- Notify: `:suggestion` kind + `suggestions_enabled` (false),
  `quiet_hours`, `suggestion_max_per_day` keys per the ADR 0084
  amendment; producer wired from the distill run's completion.
- Mobile stage 1: breakpoint token roles (design-system tokens + doc),
  phone-form-factor passes over chat/objectives/settings/memory,
  evolved responsive pin tests.
- `mix allbert.test release.v14` — the release gate.

## Code Seams

**S1 — usage-signal store (M1).** Additive migration + writer invoked at
the existing turn-close seam (where `action_log` is assembled,
`runtime.ex:1140-1172` — same data, structured); schema-level redaction
(no free-text columns beyond enumerated bands/names); retention sweep
through existing prune surfaces; bounded read API. Red-first: the
redaction eval row and a write-per-turn regression.

**S2 — distill/suggest (M2).** Distill action reading S1 + joined
sources (conversations `action_log`, objectives, jobs runs, traces where
enabled — and v1.3 memory only as provenance, never content into
cards); aggregate computations (turn-time bands per role/profile,
fallback rate, steering-correction rate, repeated-prompt classes —
extending the four TraceIndex pattern types); card emission with
evidence; managed job + kill switch; quality floor on real seeded
fixtures; `dismissed` writer + resume-gap fix; zero-egress proof.

**S3 — confirmed customization (M3).** The action + floor pin
(`policy.ex` safety-floor clause, `apply_persona_profile` precedent) +
allowlist module + diff-rendering confirmation + audit linkage +
one-click web flow (panel handlers dispatch through `Runner.run/3`
only) + CLI; adversarial red-first: apply without approval, floor
survives contract drift, non-allowlisted key denied.

**S4 — effectiveness loop (M4).** Linkage fields; delta computation in
the next distill (before/after windows on the touched dimension); panel
outcome line; `revert_suggested_customization`; the measured-loop CI
regression (seeded fixture → apply → synthetic post-window → delta
recorded).

**S5 — per-role profiles (M5).** Fragments + `Settings.Models` alias
resolution (purpose keys may reference `role:fast` etc. without
breaking existing values); chooser/catalog role rows (ADR 0088 seam);
role-remap suggestion type through S3; legacy aliases untouched proof.

**S6 — suggestion notify (M6, ADR 0084 amendment).** Kind + keys +
producer + quiet-hours/rate-limit enforcement in the ordered gate;
ledger kind extension; per-channel matrix (email completion-shaped or
absent — recorded); live smoke on one configured channel; consent-offer
reuse decision recorded.

**S7 — mobile stage 1 (M7, rider).** Breakpoint token roles (tokens +
design-system doc + gap-analysis closure for the named items);
phone-form-factor passes on the four primary surfaces within ADR
0074/0080 (no page-local CSS); evolved pin tests; 390×844 browser
walkthrough.

**S8 — eval rows + gate (M8).** `:v14` enum FIRST; behavioral rows
(request flow §G) + meta-contract sweep;
`@release_v14_steps = @release_v13_steps ++ @v14_focused_steps`;
evidence clone; docs-gate list.

## Development Lane Annotation

- **Parallel workstreams:** WS-A (S1→S2→S3→S4 — the flagship spine,
  serial); WS-B (S5 roles — independent until the remap suggestion type
  joins S3); WS-C (S6 notify — independent after S2 defines the card
  shape); WS-D (S7 mobile — fully independent; may run from M1).
- **Serial barriers:** M0 first. S1 before S2 before S3 before S4. S5's
  suggestion type after S3. S6 after S2. M8 after features; M9 after
  churn; M10 last, tag operator-held.
- **Focused tests/gates:** per-milestone commands below; `release.v1` +
  `release.v13` after every contract-adjacent milestone; `inventory
  --check-tags` on every new file; usage-store suites `db_serial`;
  distill suites `app_env_serial`; notify additions `app_env_serial`;
  mobile suites `pure_async`/`liveview_serial`; metrics citations for
  every claim.
- **Full-precommit timing:** at the M8 barrier and M10 RC assembly.
- **External smokes:** one configured live channel for the `:suggestion`
  kind at M6 (credential failure = BLOCKED); local-model spot-check if
  distill uses assist (M2); no other egress exists.
- **Rejoin points:** after M4, an integrated loop run (seed → distill →
  card → confirm → apply → measure) before S5/S6 consume it; docs
  reconciliation at M10; archive after artifact validation.

### Focused command matrix

Owner-app directories; owned temporary Homes.

| Milestone | Primary lane | Binding focused command |
|---|---|---|
| M0 | mixed | `MIX_ENV=test mix allbert.test metrics && cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/self_improvement test/allbert_assist/channels/notify_test.exs` (baselines; fragment-wiring + dead-status + resume-gap verification) |
| M1 | `db_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/profiling/usage_events_test.exs test/allbert_assist/runtime/persistence_test.exs` |
| M2 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/profiling/distill_test.exs test/allbert_assist/profiling/distill_corpus_test.exs test/allbert_assist/self_improvement` |
| M3 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/actions/profiling/apply_suggested_customization_test.exs test/allbert_assist/security/policy_test.exs test/allbert_assist/confirmations_test.exs` |
| M4 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/profiling/effectiveness_test.exs test/allbert_assist/profiling/loop_regression_test.exs` |
| M5 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/settings/model_roles_test.exs test/allbert_assist/settings/models_test.exs` |
| M6 | `app_env_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/channels/notify_test.exs test/allbert_assist/channels/notify_suggestion_test.exs` |
| M7 | `pure_async`/`liveview_serial` | `cd apps/allbert_assist_web && MIX_ENV=test mix test test/allbert_assist_web/workspace/responsive_test.exs test/allbert_assist_web/workspace/design_system_tokens_test.exs test/allbert_assist_web/live/workspace` |
| M8 | `security_eval_serial` | `MIX_ENV=test mix allbert.test release.v14` (repo root, first roll target) |
| M9 | mixed | `MIX_ENV=test mix allbert.test release.v14 && MIX_ENV=test mix allbert.test release.v1` after every applied wave |
| M10 | mixed | `MIX_ENV=test mix precommit && MIX_ENV=test mix allbert.test release && MIX_ENV=test mix allbert.test release.v1 && MIX_ENV=test mix allbert.test release.v14` |

## Security Posture

This release adds one carefully-fenced application path and one
notification kind:

- **Confirmed customization is the only new effectful path.** Forced
  confirmation (contract + floor pin), shipped allowlist excluding
  permissions/security/egress/credentials/fallback gates, exact-diff
  confirmations, full audit linkage — gate-bound denial rows for each.
- **Suggestions and profiles grant nothing.** Cards are inert; the usage
  store is bookkeeping (no bodies/secrets, schema-proven); distill is
  zero-egress; ADR 0045's advisory posture holds everywhere else.
- **`:suggestion` notify is default OFF** on the single ADR 0084
  boundary with quiet hours and a per-day cap; a notification can never
  approve anything.
- **Role remaps ride the spine**: settings actions + confirmed path
  only; the fallback egress gates from ADR 0088 are not remappable via
  suggestions (allowlist exclusion).
- Mobile stage 1 is presentation-only; no authority content.

## Non-Goals

- Automatic application of any suggestion; automatic rollback.
- Learned/trained profiling models (System Memory Distillation parked).
- Proactive notification classes beyond `:suggestion`.
- Legacy `intent.*model_profile` removal (migration-runner train);
  OAuth hosted providers (1.5 train; catalog lists, this plan does not
  auth).
- Mobile stages 2–4 (API surface, remote auth, native shell).
- New telemetry/egress of any kind; usage data never leaves the machine.
- Retention changes to conversations/traces beyond the usage store's
  own bounds.

## Milestones

Modes: `[gate]`, `[model]` (local), `[channel]` (live configured),
`[browser]`, `[fv]`.

- **M0: Recon verify + baseline.** `[gate]` Anchor drift table (incl.
  v1.2/v1.3 deltas); notify fragment wiring verified; dead `dismissed`
  status + resume gap confirmed at HEAD; trace-gate state; usage
  baselines (decide bands, fallback/steering rates) recorded from the
  metrics store + a seeded sample. *Acceptance:* tables committed
  before M1.
- **M1: Usage-signal store (S1).** `[gate]` Additive table + turn-close
  writer + retention + bounded reads; redaction proven red-first;
  per-turn write regression; kill switch. *Acceptance:* no bodies/
  secrets by schema + eval row; write cost within the M0 decide budget;
  `release.v1` + `release.v13` green.
- **M2: Distill/suggest (S2).** `[gate]`+`[model]` Aggregates + card
  emission with evidence; managed job + kill switch; quality floor on
  real fixtures recorded; `dismissed` writer + resume-gap fix;
  zero-egress proof. *Acceptance:* floor met; cards carry evidence +
  concrete change + predicted benefit; v0.47 suites green unchanged.
- **M3: Confirmed customization (S3).** `[gate]` Action + floor pin +
  allowlist + diff confirmation + audit linkage + web one-click flow +
  CLI. *Acceptance:* apply-without-approval denied; floor survives
  contract drift (adversarial test); non-allowlisted key denied;
  every application fully audited; `release.v1` + `release.v13` green.
- **M4: Effectiveness loop (S4).** `[gate]` Linkage, deltas, panel
  outcome line, confirmed revert; the full-loop CI regression green.
  *Acceptance:* measured loop recorded in Build Progress; revert
  proven; ADR 0090 flips Accepted here.
- **M5: Per-role model profiles (S5).** `[gate]` Fragments + alias
  resolution + chooser rows + remap suggestion type via S3; legacy
  aliases untouched proof. *Acceptance:* role resolution parity (no
  existing purpose key changes behavior); remap only via confirmed
  path.
- **M6: Suggestion notify (S6).** `[gate]`+`[channel]` Kind + keys +
  producer + quiet hours + rate limit at the ordered gate; ledger
  extension; per-channel matrix recorded; live smoke on one configured
  channel. *Acceptance:* default-off, quiet-hours, and rate-limit
  suppression rows green; never-an-approval-channel proof; live
  delivery attested or BLOCKED recorded.
- **M7: Mobile-ready web stage 1 (S7, rider).** `[gate]`+`[browser]`
  Breakpoint token roles; phone passes on chat/objectives/settings/
  memory; evolved pin tests; 390×844 walkthrough with zero console
  errors. *Acceptance:* LD13 criteria met; no page-local CSS; gap-
  analysis items closed or operator-dispositioned.
- **M8: Eval rows + release.v14 gate (S8).** `[gate]` `:v14` enum
  first; behavioral rows + meta-contract; gate wiring + evidence clone;
  docs-gate list. *Acceptance:* `release.v14` green first roll target.
- **M9: Dependency refresh (standing).** `[gate]` Review table, bounded
  applies, dispositions; post-refresh gates supersede M8 evidence.
- **M10: Docs, RC, FV, binary release + closeout.**
  `[gate]`+`[browser]`+`[channel]`+`[fv]` Purpose-bar check; operator
  docs (profiling guide: what is collected and never collected, cards,
  confirmed apply, effectiveness, roles, notify opt-in, mobile);
  CHANGELOG + lockstep; RC gates; one uninterrupted authoritative gate
  at the pushed candidate SHA; FV-01..FV-04 (request flow §H);
  operator-held tag `v1.4.0`; CI/cosign/tap/install rehearsal; archive
  + roadmap administration (incl. mobile stages 2–4 re-park).

## Validation Design (churn guardrails, binding)

The standing v1.1-lessons contract, instantiated: no validation-only
product capability; the flagship loop is a CI regression (M4) before FV
exists; behavioral eval rows + meta-contract; measured claims on real
fixtures only; explicit surface census (web panel, CLI, notify channel,
mobile viewport); disposable Homes, no `set -e`; FV opens once after the
green authoritative gate at the candidate SHA; fixed corrective loop.

## Documentation Deliverables

- ADR 0090 (Accepted at M4); ADR 0084 amendment verified in place;
  ADR 0045 cross-reference note at M3.
- `docs/plans/v1.4-request-flow.md` maintained as implementation
  changes.
- Operator docs: profiling guide (collection boundaries, cards,
  confirmed apply + allowlist, effectiveness, revert), model-roles
  reference (with ADR 0072 matrix cross-link), notify suggestion
  opt-in/quiet-hours, mobile usage note.
- Developer docs: usage-store schema + lanes; design-system breakpoint
  roles; test-strategy updates.
- `CHANGELOG.md` `## v1.4.0`; roadmap + future-features lifecycle at
  closeout (operator-owned; mobile stages 2–4 and any intake candidates
  re-parked/surfaced explicitly).
- Triad archived after artifact validation.

## Definition Of Done

- Usage store live, redacted, bounded, kill-switchable —
  `[gate-proved-contract]`.
- Distill produces evidenced cards on cadence with the measured quality
  floor; zero egress — `[gate-proved-contract]` + `[model]`.
- Confirmed customization: forced confirmation + allowlist + diff +
  audit; adversarial denials green — `[gate-proved-contract]`.
- Effectiveness measured, displayed, revertable; full loop as CI
  regression; ADR 0090 Accepted — `[gate-proved-contract]`.
- Per-role profiles resolve as a pure naming layer; remaps confirmed-
  path-only — `[gate-proved-contract]`.
- `:suggestion` notify default OFF with quiet hours + rate cap on the
  single boundary; live smoke attested — `[gate-proved-contract]` +
  `[channel]`.
- Mobile stage 1 criteria met at 390×844 with evolved pin tests —
  `[gate-proved-contract]` + `[browser]`.
- `:v14` behavioral rows + meta-contract; `mix allbert.test release`,
  `release.v1`, `release.v14` green at the tagged commit —
  `[gate-proved-contract]`.
- FV-01..FV-04 attested at one candidate SHA on disposable Homes —
  `[operator-attested]`.
- v1.4.0 published as a binary release (cosign, tap, install
  rehearsal); dependency table complete; docs + closeout done.
