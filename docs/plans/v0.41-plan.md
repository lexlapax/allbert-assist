# Allbert v0.41 Developer Velocity And Parallel Test Methodology

Developer flow: `docs/plans/v0.41-request-flow.md`

ADR: `docs/adr/0049-development-gates-and-test-parallelization.md`

Developer guide: `docs/developer/test-strategy.md`

Status: released as v0.41.0.

Source: post-v0.40 closeout. The v0.40 full `mix precommit` gate took roughly
22 minutes in local development, with most time in synchronous ExUnit lanes. The
next feature milestone (v0.42 Tool Discovery + MCP-First Integration Pack 1) is
deferred until Allbert has an explicit development gate and test-parallelization
methodology — and until the existing suite is migrated onto it.

## Purpose

Make Allbert's development loop match its Elixir/OTP ambition: parallel when
resources are isolated, explicit and serial when they are not, and still strict
enough that release gates remain authoritative.

This release does two things. First, it defines the methodology and developer
substrate: a gate matrix, a test-lane taxonomy by resource ownership, an
isolation contract, and a milestone parallelization-annotation contract that all
future plans must follow. Second — because the methodology only pays off once the
suite uses it — v0.41 **executes the full migration of the existing test suite
onto the new lanes and gates**, leaving a clean slate for development going
forward. It adds no operator-facing assistant capability; operator features
resume in v0.42.

The migration is the dominant work of this release: it touches essentially every
test in the repository (see "Scope Of The Test Migration"). It is staged,
reversible, and validated at every step against the v0.40 regression oracle
below, so the suite never loses coverage and the release bar never drops.

## Second-Pass Readiness Sweep

This plan is implementation-ready for **M1 only**. M1-M5 deliberately form the
methodology lock before any test migration begins; M6-M9 remain blocked until
M1 records the authoritative benchmark/oracle and M2-M5 freeze the taxonomy,
isolation, gate, and sequencing contracts.

Sweep result:

- **Vision fit.** v0.41 is not an assistant capability release. It is the
  development-system counterpart of the Elixir/OTP/Jido vision: resource
  ownership, supervised concurrency, explicit serial chokepoints, and observable
  evidence applied to the way humans and agents build Allbert.
- **Roadmap fit.** v0.42 and later add more surface area and external
  integrations; v0.41 is correctly inserted before them so the repo can absorb
  that work without making `mix precommit` the only feedback loop.
- **Past-plan fit.** The v0.40 MCP closeout provides the regression oracle and
  the first real pressure signal: strict gates are passing, but the monolithic
  local loop is too slow for the next integration arc.
- **Future-plan fit.** v0.42 is already back-filled as the worked example for
  milestone development-lane annotations. Later plans may be back-filled at the
  start of their implementation, but v0.42 must remain annotated before v0.41
  closeout.
- **ADR fit.** ADR 0049 is sufficient for the current design: gate matrix,
  lane taxonomy, async eligibility, SQLite partitioning, plan annotations, full
  suite migration, and benchmark-driven reordering. No new ADR is required
  before M1. Add or amend an ADR only if implementation introduces a new
  persistent gate runner, CI topology, external test scheduler, or changes the
  release-gate semantics.

Readiness guardrails:

- M1 must resolve the exact current-main oracle commit, green test counts, and
  wall-clock baseline before M6 implementation begins.
- M6-M9 must record benchmark artifacts in `docs/developer/test-strategy.md`
  after every milestone/batch and apply the reorder rule when the measured
  improvement is ineffective.
- Production assistant behavior is out of scope. v0.41 may touch Mix aliases,
  test support, test configuration, docs, and tests; any production-runtime
  change must be justified as test isolation infrastructure and called out in
  the milestone evidence.

## v0.40 Regression Oracle And Fallback Gate

The v0.40 closeout commit on current `main` (`f81d13d`, "v0.40 M6 MCP security
closeout") is the regression oracle for this entire release. Its full
`mix precommit` runs green: 1146 core + 107 web + 197 + 2 plugin/channel tests,
0 failures, 2 skipped.

This is baked into the migration:

- **Oracle.** Every implementation milestone and every migration batch must
  reproduce that exact green result through the full release gate before the
  batch is accepted. "Faster" is never accepted in exchange for fewer passing
  tests.
- **Fallback gate.** The monolithic v0.40-style serial `mix precommit` stays
  runnable throughout the migration. Development never loses a known-good gate:
  if the new lanes are mid-flight or suspect, the v0.40 serial precommit is the
  authoritative fallback.
- **Flake triage.** When a newly parallel/partitioned lane flakes, re-run the
  suspect module under the serial fallback. If it passes serially, the failure is
  a parallelism/ownership defect (fix the ownership contract or move the module
  back to serial); if it fails serially too, it is a real regression and blocks.

## Efficiency Benchmark And Reorder Loop

Efficiency is measured, not assumed. The release is justified by a wall-clock
win, so each step is gated on a benchmark, and the milestone order is adaptive:
if a milestone does not improve efficiency effectively, the remaining milestones
are re-sequenced to attack the measured hotspots first.

The benchmark (run on the machine context recorded in M1, so deltas compare):

- `release` gate wall-clock, with test counts, to prove coverage equals the
  v0.40 oracle (the correctness ceiling; this number is allowed to stay flat).
- `fast-local` gate wall-clock — the primary efficiency metric.
- per-lane wall-clock plus `mix test --slowest-modules N` and `--slowest N` to
  locate the dominant hotspot.

Each benchmark record must include: commit SHA, milestone/batch id, date,
machine context (OS, CPU/core count, Elixir/Erlang versions, and whether caches
were warm or cold), exact command(s), wall-clock time, test counts, failures,
skips, lane counts, top slowest modules/tests, and a short interpretation. The
records live in `docs/developer/test-strategy.md` so the next agent can compare
without reconstructing terminal history.

Cadence:

- **Before implementation (M1):** establish the benchmark harness and record the
  BEFORE baseline (the v0.40-equivalent serial run plus the per-lane breakdown).
- **After each implementation milestone (M6-M9):** re-run the benchmark and record
  the delta versus the previous milestone and versus the M1 baseline. Planning
  milestones (M2-M5) change no code and reuse the M1 baseline.
- **After done (M9 closeout):** final benchmark versus the M1 baseline; confirm
  the target is met or document the gap.

Reorder rule:

- M1 sets the overall wall-clock target and each implementation milestone's
  planned share of it.
- A milestone is **effective** if its benchmark reduces `fast-local` wall-clock by
  at least its planned share, or measurably shrinks the hotspot it targeted.
- If a milestone is not effective — or the latest `--slowest-modules` report shows
  the dominant hotspot is a lane not scheduled next — **re-sequence the remaining
  milestones/batches to attack the measured hotspots first**, record the reorder
  and its rationale in `docs/developer/test-strategy.md`, and continue. A
  planned share must be an explicit wall-clock or percentage target; "should be
  faster" is not a target.
- Reordering optimizes efficiency only within the correctness constraint: every
  reordered batch still reproduces the v0.40 oracle green set. Efficiency is never
  bought with coverage.

## Current Baseline

Observed during v0.40 M6 closeout (preliminary; M1 replaces these with an
authoritative census):

- `mix precommit` passed but took roughly 22 minutes wall-clock, and is a
  monolithic serial sequence (compile, format, Credo, then four separate `mix
  test` invocations for core, web, StockSage, and telegram/email). It does not
  run Dialyzer.
- The core app suite reported 1146 tests, 0 failures, 2 skipped; web 107; plugin/
  channel 197 and 2 — across ≈233 `*_test.exs` files (≈180 core, 16 web, 37
  plugin).
- The suite is conservative, not untagged: async is almost always explicit
  (≈195 of 196 app test files declare it) but skewed serial — about 160
  `async: false` modules versus 35 `async: true`.
- Heavy serial-resource use: ≈623 `Application.put_env` sites in tests, temporary
  Allbert Home roots, ≈21 named-process spawns, `Ecto.Adapters.SQL.Sandbox` in
  `:manual` mode (`shared: not tags[:async]`), and filesystem cleanup.
- Partition-readiness is partial, not greenfield: `config/test.exs` already
  derives `DATABASE_PATH` from env vars. `MIX_TEST_PARTITION` is referenced only
  in a comment and is not yet wired.

These numbers are starting evidence, not final truth. M1 produces the
authoritative census and records exact commands, machine context, and timing.

## Scope Of The Test Migration

The implementation reaches essentially every test (≈233 files / ≈1,450 tests;
≈160 `async: false` promotion candidates). To keep that tractable and reviewable,
the migration is **classified at the case-template boundary, not per file**:

- **Template defaults carry the lane.** Each shared case template
  (`AllbertAssist.DataCase`, web `ConnCase`, `SecurityEvalCase`, channel/plugin
  cases) sets its default lane tag and `async` default in one place. A handful of
  template edits classify the majority of files. (`SecurityEvalCase` already
  takes `async:` as an option, so this extends an existing pattern.)
- **Per-file edits only for exceptions.** Promotions (e.g. a `DataCase` test that
  is actually pure → `pure_async`) and the plain-`ExUnit.Case` tail (files using
  no shared template) get an explicit `@moduletag` override. The async flip is
  then a small, reviewable, ownership-proof diff per promoted module — not a
  blanket rewrite of 233 files.
- **Reconciliation is the exit criterion.** Lane test-counts must sum to the full
  suite total: zero unclassified files, no file double-counted. A file touching
  multiple serial resources records a primary lane plus secondary blockers and is
  excluded from async if any blocker is present.

## Design Principles

- Async eligibility is proven by owned resources, not guessed from test speed.
- Two concurrency axes: pure tests run async in one VM (`--max-cases`); every
  VM-global lane (DB, app env, Allbert Home, named processes, LiveView)
  parallelizes across OS partitions (`--partitions`), serial within a partition,
  with per-partition roots. SQLite is single-writer, so DB tests stay serial
  inside a partition; the speedup is partition count, not async-within-partition.
- `Application.put_env`, global process names, shared registry names, Req/Mox
  style global stubs, and shared filesystem roots are serial resources unless a
  test helper provides unique ownership and cleanup.
- Security evals, external runtime smokes, Docker/gate runs, and browser/LiveView
  flows are explicit lanes. They are not hidden inside a vague "precommit."
- Full release precommit remains authoritative, and the v0.40 green set is the
  regression oracle. Faster local gates add feedback; they do not weaken the
  release bar.
- Implementation-ready plans must annotate concurrency. Milestones name
  parallel workstreams, serial barriers, focused gates, external smokes, and the
  rejoin point where evidence is checked before commit or release closeout.

## Planned Gate Matrix

Future implementation defines these gates as named commands or Mix aliases:

| Gate | Purpose | Expected shape |
| --- | --- | --- |
| `docs` | Docs-only verification | `git diff --check` plus link/reference checks. |
| `focused` | Milestone-local feedback | Explicit test file list named in the milestone plan. |
| `fast-local` | Daily local confidence | Format, Credo, compile warning gate, pure async tests, partition-safe fast lanes. |
| `serial-core` | VM-global lanes | DB/home/app-env/process/LiveView lanes run serial within a partition, parallel across OS partitions (N from cores); security evals stay single-VM serial. |
| `release` | Authoritative handoff gate | All lanes plus Dialyzer and security evals. Equivalent or stronger than current `mix precommit`. |
| `external-smoke` | Opt-in local integrations | Docker, real MCP servers, provider endpoints, browser drivers, and other machine-dependent checks. |

The `release` gate adds Dialyzer, which today's `mix precommit` does not run; it
is a superset of the v0.40 precommit, never a subset.

## Implementation-Plan Parallelization Contract

After v0.41, a plan is not implementation-ready merely because each milestone
has scope and acceptance criteria. Each milestone must also include a development
lane annotation:

- **Parallel workstreams**: docs, schemas/migrations, pure modules, UI surface
  work, focused tests, browser/manual validation, or external smoke preparation
  that can proceed independently.
- **Serial barriers**: SQLite/Repo ownership, app env mutation, shared Allbert
  Home or filesystem roots, named processes/supervisors, LiveView/Repo process
  trees, security eval harnesses, external runtimes, and release evidence.
- **Gate evidence**: focused test files, static checks, serial lane command,
  external smoke command, and whether full release precommit is required before
  the milestone commit or only at release closeout.
- **Rejoin point**: where parallel branches must be integrated, request-flow
  docs updated, drift checked against the plan, and gate evidence reviewed.

Plan audits must call out missing annotations as readiness gaps. This is the
planning-side analogue of test-lane classification: parallelism is allowed when
resource ownership and ordering are explicit. The contract applies to plans
authored or audited after v0.41; existing downstream plans (v0.42 onward) get
their annotations back-filled at the start of their own implementation, and v0.42
is back-filled now as the worked example.

This plan dogfoods the contract: every milestone below carries a Lane annotation.

## Milestones

M1-M5 are the methodology/design pass (docs + measurement, no test edits).
M6-M9 are the implementation pass that migrates the suite. The split keeps the
"design the contract" work reviewable before any test is touched. Each of M6-M9
ends with the efficiency benchmark and the reorder check (see "Efficiency
Benchmark And Reorder Loop"): a milestone that does not yield effective
improvement triggers re-sequencing of the remaining batches.

### M1: Baseline Measurement And Test Inventory

Status: complete on 2026-05-29. The first benchmark attempt failed before
Allbert tests because local Elixir 1.19.5 / Erlang OTP 29.0.1 could not compile
`:memento` 0.5.0. The operator accepted the current-toolchain compatibility
path. ADR 0050 records the resulting Jido stack update and local
`vendor/memento` override. The full v0.40-style release oracle now passes on the
current toolchain (`mix precommit`, `real 1221.01`). M1 also records the
fast-local-equivalent baseline, slowest reports, file-level inventory in
`docs/developer/v0.41-test-inventory.csv`, targets, and planned shares.

Work:

- Record the v0.40 regression oracle: the exact `mix precommit` command, machine
  context, wall-clock, and the green test counts at commit `f81d13d`.
- Establish the efficiency benchmark harness and record the BEFORE baseline:
  release + fast-local-equivalent wall-clock, per-lane breakdown, and
  `--slowest-modules`/`--slowest` reports on a recorded machine context.
- Set the overall wall-clock target and each implementation milestone's planned
  share of it.
- Produce a test inventory with file path, app/plugin owner, case template,
  `async` value, tags, approximate timing, and detected resource classes.
- Record current contention classes: SQLite sandbox, `Application` env, Allbert
  Home/filesystem roots, named processes/supervisors, global HTTP stubs, LiveView
  ownership, external runtime smokes.

Acceptance:

- `docs/developer/test-strategy.md` has a current baseline table and the recorded
  v0.40 oracle.
- The inventory identifies which files are candidates for `pure_async`,
  `db_serial`, `db_partition_safe`, `app_env_serial`, `home_fs_serial`,
  `global_process_serial`, `external_runtime_serial`, `liveview_serial`, and
  `security_eval_serial`, and the count of files reachable by template default
  vs. needing per-file classification.
- The BEFORE benchmark, the overall wall-clock target, and each implementation
  milestone's planned efficiency share are recorded.

Lane annotation: parallel — docs + measurement only. Serial barriers — none
(read-only measurement; running the suite uses the existing serial gate).
Gate evidence — docs gate plus recorded benchmark/oracle commands and results.
Rejoin — inventory + oracle recorded in test-strategy.md before M2.

### M2: Test Taxonomy And Ownership Rules

Status: complete on 2026-05-29. The lane taxonomy, primary tag names,
case-template defaults, override path, and secondary-blocker rule are frozen in
`docs/developer/test-strategy.md`.

Work:

- Finalize the lane taxonomy in `docs/developer/test-strategy.md`.
- Define async-safe requirements: no shared database writes, no global app env
  mutation, unique tmp roots, unique process names, local-only stubs, and
  deterministic cleanup.
- Define serial-only requirements and how to document exceptions.
- Define the tagging convention: the exact `@moduletag` keys/values per lane, the
  case-template → default-lane mapping, the secondary-blocker rule, and how lanes
  map to `--only`/`--exclude` gate filters.
- Define the milestone planning annotation format for parallel workstreams,
  serial barriers, gate evidence, and rejoin points.

Acceptance:

- Every test can be classified without an implementer inventing new labels.
- Case templates (`DataCase`, `ConnCase`, security eval case, plain
  `ExUnit.Case`) have documented default lane expectations and a documented
  override path.
- Future milestone plans can be audited for implementation readiness using the
  same annotation format.

Lane annotation: parallel — docs only. Serial barriers — none. Gate evidence —
docs gate. Rejoin — taxonomy + tagging convention frozen in test-strategy.md
before M6 begins tagging.

### M3: Isolation Contract

Status: complete on 2026-05-29. The partition/root ownership contract is frozen
in `docs/developer/test-strategy.md`, including per-partition `ALLBERT_HOME`,
`ALLBERT_HOME_DIR`, `DATABASE_PATH`, derived runtime roots, process naming,
cleanup limits, and the requirement that partition helpers run both Allbert and
StockSage migrations.

Work:

- Specify per-test and per-partition ownership for `ALLBERT_HOME`,
  `ALLBERT_HOME_DIR`, `DATABASE_PATH`, Settings Central roots, secrets roots,
  memory roots, sandbox roots, and tmp roots, building on the existing
  `config/test.exs` env-derivation.
- Specify process naming rules: no global test name unless the test is serial;
  async tests use unique names or supervised anonymous processes.
- Specify cleanup rules for filesystem roots, ports, supervised processes,
  registry entries, and global env.

Acceptance:

- The contract is strong enough to support OS-process partitioning with
  `MIX_TEST_PARTITION`.
- The contract forbids tests from writing to a real operator home.

Lane annotation: parallel — docs only. Serial barriers — none. Gate evidence —
docs gate. Rejoin — isolation contract complete before M6 builds the helpers.

### M4: Gate Matrix Design

Status: complete on 2026-05-29. The command names and sequencing are frozen as
`mix allbert.test docs`, `focused`, `fast-local`, `serial-core`, `release`, and
`external-smoke`; M6 implements these names without changing the release
semantics.

Work:

- Design future command names and sequencing for docs, focused, fast-local,
  serial-core, release, and external-smoke gates.
- Decide which gates may run in parallel OS processes and which must be serial.
- Document how agents choose a focused gate per milestone and when full release
  precommit is mandatory, and that the release gate is a superset of v0.40
  precommit plus Dialyzer.
- Update agent/developer guidance so planning passes require milestone-level
  parallelization annotations before implementation starts.

Acceptance:

- `DEVELOPMENT.md` and `AGENTS.md` describe the matrix and keep the
  no-known-failing-gate handoff rule.
- `DEVELOPMENT.md`, `AGENTS.md`, and
  `docs/developer/agent-context-map.md` route plan-readiness audits to the
  development-lane annotation contract.
- `docs/plans/v0.41-request-flow.md` shows exact planned command shapes.

Lane annotation: parallel — docs only. Serial barriers — none. Gate evidence —
docs gate. Rejoin — DEVELOPMENT.md/AGENTS.md/context-map agree on the matrix
before implementation.

### M5: Migration Plan And Sequencing

Status: complete on 2026-05-29. M1 slowest evidence adjusts the implementation
priority: after M6/M7, attack high-cost core intent/runtime and StockSage
objective/action partition-safety before lower-cost filesystem-only cleanup, with
LiveView partitioning/tuning in M9. The batch order and rollback/reorder rule are
frozen in `docs/developer/test-strategy.md`.

Work:

- Fix the batch order: template-default classification first, then the measured
  core intent/runtime partition hotspots, then StockSage objective/action
  partition hotspots, then low-risk pure cleanup/promotions only if the
  benchmark preserves or improves, then LiveView/process-heavy tests; security
  evals and external smokes stay serial.
- Define batch sizing, the per-batch green-vs-oracle requirement, the flake-rerun
  budget, and per-batch rollback to serial.
- Define the reconciliation check (lane counts sum to the full suite total).

Acceptance:

- The migration order reduces risk while following the benchmark: classify first,
  attack the measured core and StockSage hotspots, defer low-value pure cleanup
  when it does not improve the gate, then handle LiveView/process-heavy tests.
- Rollback is documented: any flaky lane can be moved back to serial without
  weakening release coverage, validated by the v0.40 fallback gate.

Lane annotation: parallel — docs only. Serial barriers — none. Gate evidence —
docs gate. Rejoin — sequencing + rollback frozen before M6.

### M6: Gate Matrix And Isolation Helpers Implementation

Status: complete on 2026-05-29. M6 implements the developer gate command
surface in `Mix.Tasks.Allbert.Test`: docs, inventory, focused, fast-local,
serial-core, release, and explicit opt-in external smokes. It also wires
`MIX_TEST_PARTITION` into test config, creates owned temp `ALLBERT_HOME` /
`DATABASE_PATH` roots, runs migrate-only setup for raw shards, and fixes the web
test alias so fresh web databases include StockSage plugin migrations. No tests
are retagged or promoted in M6.

Work:

- Implement the gate Mix aliases/scripts for docs, focused, fast-local,
  serial-core, release, and external-smoke, preserving v0.40 precommit coverage.
- Implement the unique-home/owned-root test helper and the lane-agnostic
  per-partition `DATABASE_PATH`/`ALLBERT_HOME` derivation + per-partition
  migration, wiring `MIX_TEST_PARTITION` so any VM-global lane can partition, not
  only DB.
- Keep the existing monolithic precommit available as the fallback gate.

Acceptance:

- `mix allbert.test release` reproduces the v0.40 oracle green set (same tests,
  0 failures) and additionally runs Dialyzer.
- A partition smoke run (`--partitions N` with per-partition roots) creates,
  migrates, and tears down isolated databases/homes with no cross-talk.
- No test is yet re-tagged or promoted; behavior is unchanged from v0.40 except
  gate ergonomics.
- Benchmark after M6: `fast-local` wall-clock is measurably below the full
  release gate (the gate-ergonomics win), recorded versus the M1 baseline.

M6 evidence:

- `env MIX_ENV=test mix compile --warnings-as-errors` passed.
- `env MIX_ENV=test mix allbert.test inventory --output /private/tmp/v041-m6-inventory-2.csv`
  matched `docs/developer/v0.41-test-inventory.csv`.
- `env MIX_ENV=test mix allbert.test partition-smoke --partitions 2` created
  separate owned homes/databases for partitions 1 and 2 and cleaned them up.
- `/usr/bin/time -p env MIX_ENV=test mix allbert.test fast-local` passed at
  `real 23.56` seconds, down from the M1 34.46s fast-local-equivalent
  sequential baseline.
- A fresh web DB focused test at
  `apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs:1576`
  passed, proving the web test alias now applies StockSage plugin migrations.
- `/usr/bin/time -p env MIX_ENV=test mix allbert.test release` passed at
  `real 1268.73` seconds, preserving the v0.40 precommit oracle and adding a
  green Dialyzer phase.

Lane annotation: parallel — alias/script authoring and helper modules can be
drafted alongside the partition infra. Serial barriers — the gate definitions and
partition DB/home infra (touch global test config). Gate evidence — full release
gate must equal the v0.40 oracle; partition smoke; `mix format`/`credo`/compile;
M6 benchmark record. Rejoin — release gate green vs oracle before M7 begins
tagging.

### M7: Case-Template Lane Classification

Status: complete on 2026-05-29. Reconciliation is enforced by
`mix allbert.test inventory --check-tags`; every test file has exactly one
primary lane and lane filters select the expected files. The benchmark did not
improve `fast-local`, so M8 is re-sequenced toward the measured core/StockSage
hotspots before low-value pure cleanup.

Work:

- Apply default lane tags and `async` defaults at the case-template boundary
  (`DataCase`, web `ConnCase`, `SecurityEvalCase`, channel/plugin cases) per the
  M2 tagging convention.
- Add per-file `@moduletag` lanes only for the plain-`ExUnit.Case` tail and
  explicit overrides; record secondary blockers.
- Produce the reconciliation report: every file has exactly one primary lane;
  lane counts sum to the suite total.

Acceptance:

- Reconciliation passes: zero unclassified files, no double-counting.
- The full release gate still reproduces the v0.40 oracle green set (tagging is
  metadata; behavior unchanged).
- `mix test --only <lane>` / `--exclude <lane>` select the intended files.
- Benchmark after M7 recorded versus M6; if classification did not improve
  `fast-local`, the reorder check re-prioritizes the M8 batches before promotion.

Lane annotation: parallel — template edits per app (core/web/plugins) can proceed
independently; the plain-ExUnit tail classification can proceed in parallel.
Serial barriers — the shared case templates (changing them affects every
dependent module); the reconciliation count. Gate evidence — release gate green
vs oracle; lane-selection spot checks; reconciliation report; M7 benchmark
record. Rejoin — full suite green and reconciled before M8.

### M8: Async Promotion And Ownership Retrofits

Status: complete on 2026-05-29. M8a adds
`mix allbert.test fast-local --core-lanes`, keeps the quick pure-lane default,
accepts empty `--only` partition shards as green, and proves partitioned core
DB/app-env/home/process lanes under the M8 high-coverage local-gate target. M8b
adds `--stocksage-lanes`, exact plugin file selection, and StockSage
DB/app-env/process lane partitioning. The M8c pure-cleanup batch is a no-op
because no measured regression justified low-value pure churn.

Work:

- Migrate modules in the post-M7 reordered batch order, re-prioritized by the
  latest `--slowest-modules` report per the reorder rule (attack the dominant
  hotspot first): first core intent/runtime DB/app-env/process partitioning,
  then StockSage objective/action partitioning, then low-risk pure cleanup only
  when it has a measured target and preserves or improves the measured gate.
- Each batch: run the affected lane (including async/partition reruns for flake
  detection) and the full release gate; keep security evals and external smokes
  serial.
- Use the v0.40 serial fallback to triage any flake before accepting a batch.

Acceptance:

- Each accepted batch reproduces the v0.40 oracle green set with zero net-new
  flakes across the flake-rerun budget.
- Any lane that flakes is rolled back to serial and its blocker recorded, without
  losing coverage.
- `fast-local` high-coverage wall-clock drops measurably versus the v0.40 serial
  precommit while the quick default remains stable.
- Each batch records a benchmark delta; a batch that does not improve
  `fast-local` effectively triggers re-sequencing of the remaining batches toward
  the measured hotspot.

Lane annotation: parallel — independent batches by lane/app may proceed
concurrently once their ownership helpers exist. Serial barriers — shared
Repo/Sandbox, app env, shared homes, named processes, LiveView/Repo trees,
security evals (all stay serial); the per-batch rejoin. Gate evidence —
per-batch affected-lane reruns + full release gate green vs oracle;
flake-rerun budget; benchmark record after each batch. Rejoin — release gate
green vs oracle after each batch; no batch handed off with a known failing/flaky
lane.

### M9: Acceptance, Metrics, Rollback, And Clean-Slate Closeout

Status: complete on 2026-05-29. M9 adds
`mix allbert.test fast-local --web-lanes`, runs web `liveview_serial`
partitions from the web app working directory, records the after-done benchmark,
and documents `WorkspaceLiveTest` as the remaining release-only web long pole
because it stays `external_runtime_serial`.

Work:

- Record final metrics: fast-local and release wall-clock versus the v0.40
  baseline, lane distribution, and flake counts.
- Confirm the clean slate: every test classified, gate matrix live, the
  parallelization-annotation contract enforced for new plans, and v0.42 annotated.
- Finalize `docs/developer/test-strategy.md`, `DEVELOPMENT.md`, `AGENTS.md`,
  roadmap, README, and agent context map.
- Leave web `external_runtime_serial` files out of fast-local until a later plan
  can split or passivate the runtime-heavy flows with its own safety story.

Acceptance:

- The release can be judged with objective evidence: faster fast-local wall time,
  release gate coverage equal to or greater than the v0.40 oracle, no weakened
  coverage, no hidden external dependencies, and no increase in flaky failures.
- The wall-clock target set from the M1 baseline is met, with any remaining
  release-only bottleneck documented with a next-step note.

Lane annotation: parallel — docs/metrics write-up. Serial barriers — final full
release gate. Gate evidence — full release gate green vs oracle + slowest-module
report + lane summary + after-done benchmark. Rejoin — release closeout; roadmap
points to v0.42.

## Documentation Deliverables

- ADR 0049 accepted.
- `docs/developer/test-strategy.md` added and finalized (taxonomy, tagging
  convention, isolation contract, migration order, rollback, v0.40 oracle).
- `DEVELOPMENT.md`, `AGENTS.md`, roadmap, README, and agent context map updated.
- Future-plan readiness guidance added for milestone-level parallel workstreams,
  serial barriers, gate evidence, and rejoin points.
- v0.42 milestones back-filled with development-lane annotations as the worked
  example of the contract.
- During the v0.41 insertion pass, the then-planned v0.41-v0.50 milestones
  shifted forward by one slot to v0.42-v0.51.
  Later v1.0 planning moved Channel Pack 1 from v0.44 to v0.49; this note
  describes the earlier v0.41 insertion only.

## Verification

Docs-only verification for the planning milestones (M1-M5):

```sh
git diff --check
rg "v0\\.41|v0\\.42|v0\\.50b|v0\\.51" docs README.md DEVELOPMENT.md AGENTS.md
```

Implementation verification for M6-M9: each milestone/batch runs the gate
appropriate to its lane annotation and must reproduce the v0.40 oracle green set
through the full release gate before acceptance; the release finishes with the
full release gate plus Dialyzer.

Each implementation milestone also records the efficiency benchmark (release +
fast-local wall-clock, per-lane breakdown, `--slowest-modules`) and its delta
versus the prior milestone and the M1 baseline; an ineffective delta triggers the
reorder rule. The after-done benchmark at closeout is compared to the M1 baseline
to confirm the target.

## Definition Of Done

v0.41 is done when Allbert has an accepted development-gate ADR, an explicit test
strategy, a measured baseline with the recorded v0.40 oracle, a decision-complete
isolation contract, the gate matrix and isolation helpers implemented, and **the
existing test suite migrated onto the lanes** — every test classified, proven-safe
modules promoted to async/partition-safe, security evals and external smokes kept
serial — with the full release gate reproducing the v0.40 green set (plus
Dialyzer), the M1 efficiency target met (or the gap documented) as proven by the
after-done benchmark versus the M1 baseline, zero net-new flakes, and v0.42
carrying development-lane annotations. The clean slate: development going forward chooses a
gate by lane, and every new plan is auditable against the parallelization
contract, without weakening the release bar.
