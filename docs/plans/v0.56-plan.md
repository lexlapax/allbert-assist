# Allbert v0.56 Channel Parity + TUI/Terminal Channel

Request flow: `docs/plans/v0.56-request-flow.md`

ADR: `docs/adr/0016-channel-adapter-boundary-and-identity-mapping.md` (v0.56
amendment: channel parity matrix as a first-class artifact + the TUI/terminal
channel).

Status: planned. NEW in the 2026-06-09 restructure; authored Phase B (research R4).

> **Upstream dependency:** v0.56 builds on the v0.52 M0 channel-primitive
> substrate (`primitives:` descriptor field, `AllbertAssist.Approval.Handoff.render/2`,
> the `Plugin.Registry` validator). Those are **not in the codebase yet** (verified
> by grep — owned by v0.52 M0). The Readiness Sweep must confirm v0.52 shipped
> them; if not, v0.56 M0 absorbs that work.

## Purpose

Two goals:
1. **Channel parity** — make the cross-channel capability matrix explicit and
   machine-checkable (which approval primitives `{:list,:button,:typed_command,
   :link}`, attachments, streaming, identity mapping, and approval rendering each
   channel supports), and close the gaps.
2. **A proper TUI/terminal channel** — a persistent supervised terminal channel
   that is a real ADR 0016 channel (descriptor + `channel_events` dedupe +
   identity mapping + Approval Handoff + redaction), distinct from the one-shot
   `mix allbert.ask` label.

## Readiness Sweep
- **Contract fit.** Channels are delivery adapters around
  `Runtime.submit_user_input/1` + registered actions (ADR 0016 §boundary); they
  do NOT own intent/actions/Security Central/Resource Access/confirmation storage/
  conversation history. The TUI is just another adapter under
  `Channels.Supervisor` (`:one_for_one`).
- **Reuse fit.** `plugins/allbert.telegram` is the adapter template
  (`channels/0` descriptor → supervised GenServer → normalize → `Identity.resolve`
  → `Channels.derive_session_id` → `Runtime.submit_user_input` → `channel_events`).
  Email's `ALLBERT:APPROVE:<id>` renderer is the `:typed_command` template.
- **Upstream check.** Confirm v0.52 M0 delivered `primitives:` + `Approval.Handoff`
  + registry validation; absorb into M0 if not.
- **Library fit.** `owl` is already a (transitive) dep; pure-Elixir; its
  `LiveScreen`/`IO.input`/`capture_stdio` primitives fit a persistent terminal
  loop. `ratatouille` is dormant (0.5.1/2020) and pulls the `ex_termbox` NIF.

## Locked Decisions
| # | Decision | Chosen value | Trade-off if changed |
|---|---|---|---|
| 1 | TUI authority | The TUI is a delivery adapter with **no authority beyond any other channel** (ADR 0016). | A privileged channel breaks the boundary. |
| 2 | TUI library | **`owl`** (maintained, pure-Elixir, already a dep) over `ratatouille` (dormant, `ex_termbox` NIF). Promote owl transitive→direct. | ratatouille adds a native-build + unmaintained dependency for a layout feature a chat channel doesn't need. |
| 3 | CLI promotion | Do **not** promote `mix allbert.ask` (stays a non-channel developer label, `channel: :cli`). The TUI is the first-class persistent terminal channel (`channel_id: "tui"`, provider `"terminal"`). | Promoting the one-shot task duplicates the TUI and muddles the label. |
| 4 | TUI primitives | Declare `primitives: [:typed_command, :list]` (numbered options + `ALLBERT:APPROVE:<id>`); **not** `:button`/`:link` (no GUI affordance / no in-terminal workspace URL). `:list` is the mandatory universal fallback. | Declaring `:button`/`:link` would advertise affordances the terminal can't honor. |
| 5 | TUI session/identity | Maps to local `user_id` via `channels.tui.identity_map` (default `"local"`); new `session_strategy` (`{:tui_session, prefix: "ch_tui_"}`) via `Channels.derive_session_id`. | Implicit `"local"` claim violates ADR 0016 identity mapping. |
| 6 | Parity matrix home | Canonical prose table in the ADR 0016 amendment + machine-derived from descriptors (`ChannelParity` / `mix allbert.channels --parity`) + `docs/developer/channel-parity.md`. | A prose-only matrix drifts from the registry truth. |

## Milestones

### M0: Locked Decisions + Dependency + Matrix Substrate
Promote `owl` to a direct dep; verify the v0.52 M0 `primitives:`/`Approval.Handoff`/
registry-validator substrate exists (absorb if not); define the parity-matrix data
model + `ChannelParity` introspection over `PluginRegistry.registered_channels()`.

### M1: Parity Matrix + Gap Closure
Build the explicit matrix (primitives, attachments, streaming, identity mapping,
approval rendering per channel). Ensure every shipped descriptor declares
`primitives:`; document CLI/LiveView as local surfaces (declared-but-not-plugin)
consistently; record a canonical "streaming: not supported (turn-complete)" cell
and per-channel attachment posture. Add `mix allbert.channels --parity` / report.

### M2: TUI Channel Adapter
New shipped plugin `plugins/allbert.tui`: `channels/0` descriptor
(`channel_id: "tui"`, adapter module, `child_spec`, `settings_prefix:
"channels.tui"`, `identity_map_key`, `session_strategy`, `primitives:`). Supervised
GenServer adapter under `Channels.Supervisor`: `owl` input loop → normalize →
`Identity.resolve` → `derive_session_id` → `Runtime.submit_user_input` →
`channel_events` create/update → render response (`LiveScreen`). Settings schema +
enabled flag.

### M3: Approval-Primitive Rendering In TUI
TUI `render_approval_handoff/1` consumes `{primitive_kind, payload}` from
`Approval.Handoff.render/2`; renders `:list` (numbered approve/deny/show) and
`:typed_command` (`ALLBERT:APPROVE:<id>`); resolves via
`Actions.Runner.run("approve_confirmation"|"deny_confirmation"|"show_confirmation")`
like Telegram. No primitive embeds targets/commands/URLs (ADR 0016).

### M4: Evals, Docs, Closeout
`:v056` security/parity eval lane; `release.v056` deterministic gate; opt-in
interactive-terminal smoke where the harness allows; ADR 0016 v0.56 amendment
accepted; docs (request-flow, roadmap §v0.56, vision, agent-context-map, operator
TUI guide, `docs/developer/channel-parity.md`); version/CHANGELOG closeout + full
release gate.

## Security / Eval Rows (`:v056`)
- `channel-parity-matrix-matches-descriptors-001` (report == registry truth)
- `tui-inbound-turn-dedupe-001` (duplicate terminal submit → one `channel_events`,
  one runtime submit)
- `tui-identity-map-001` (unmapped → rejected, no runtime submission)
- `tui-crash-isolation-001` (`:one_for_one`: kill TUI, Telegram survives)
- `approval-primitive-honor-tui-001` (selector returns `:list`/`:typed_command`,
  never `:button`/`:link`)
- `tui-confirmation-resolve-001` (typed `ALLBERT:APPROVE:<id>` resolves the durable
  confirmation via the Runner, isolated from other channels)
- `tui-no-authority-001` (cannot bypass Security Central / set active app / grant
  resource access)
- `tui-redaction-001` (`channel_events` summaries redacted/truncated)

## Documentation Deliverables
- `docs/developer/channel-parity.md` (the matrix) + ADR 0016 v0.56 amendment.
- Operator TUI/terminal-channel setup guide.
- Roadmap/vision/agent-context-map v0.56 reconciliation.

## Non-Goals
- No CLI (`mix allbert.ask`) promotion to a channel.
- No `:button`/`:link` primitives in the terminal.
- No new authority for any channel; channels stay delivery adapters.
- No streaming contract change (records the current turn-complete reality).

## Definition Of Done
v0.56 is done when:
- an explicit, machine-derived channel capability/parity matrix exists and matches
  the registry (`mix allbert.channels --parity` + `docs/developer/channel-parity.md`),
  with every shipped descriptor declaring `primitives:`;
- a persistent TUI/terminal channel (`plugins/allbert.tui`) runs under
  `Channels.Supervisor`, normalizes terminal input through the channel substrate
  (identity map, session derivation, `channel_events` dedupe), and renders
  responses + `:list`/`:typed_command` approvals, resolving confirmations via the
  Runner;
- the TUI carries no authority beyond any other channel (eval rows pass), and a
  TUI crash is isolated;
- `:v056` evals + `release.v056` + the full release gate pass;
- ADR 0016 v0.56 amendment, roadmap, vision, and agent-context-map agree.
