# Allbert v0.45 Request Flow - Marketplace Lite

Status: planned.

Implementation details live in `docs/plans/v0.45-plan.md`.

## Source Documents

- `docs/plans/v0.45-plan.md`
- `docs/adr/0043-marketplace-lite-trust-tier.md`
- `docs/plans/v0.38-plan.md`
- `docs/plans/future-features.md`

## Main User/Operator Flows

- Operator browses reviewed skills/templates.
- Operator inspects provenance, version, hash, and trust metadata.
- Operator installs a reviewed skill into Allbert Home as disabled/untrusted.
- Operator views reviewed-source plugin index metadata without installing code.
- Operator sees marketplace templates as `workspace:create` catalog metadata.

## Data Flow

Marketplace metadata is fetched or read as reviewed index data, normalized into
local metadata, and written only to Allbert Home marketplace/cache locations.
Installed skills remain disabled/untrusted until a separate operator action
changes their state.

## Authority Invariants

- Marketplace metadata is descriptive only.
- Marketplace install does not enable skills, actions, plugins, templates, or
  permissions.
- Code-bearing remote plugins are not installed.
- Template catalog metadata does not execute templates.
- Any enablement still uses registered actions, Security Central,
  confirmations, traces, and audits.

## Handles / CLI Surfaces

Planned handles only; exact names may change during implementation:

- Marketplace list/search/detail surfaces.
- Skill install action that writes disabled/untrusted skill state.
- `workspace:create` template catalog metadata.

## Security Eval Rows

- `marketplace-skill-disabled-default-001`
- `marketplace-code-plugin-deny-001`
- `marketplace-provenance-hash-001`
- `marketplace-template-metadata-no-execute-001`
- `marketplace-permission-grant-deny-001`

## Smoke Checklist

- Discover reviewed skill metadata.
- Install a skill and confirm it is disabled/untrusted.
- Inspect reviewed-source plugin metadata without code install.
- Confirm template metadata appears without template execution.
- Confirm invalid provenance/hash metadata is rejected or marked unusable.

## Operator Validation

Status: planned. Commands are illustrative until the milestone lands; verify and
replace with observed output at closeout.

```sh
export ALLBERT_HOME="$(mktemp -d /tmp/allbert-v045.XXXXXX)"
export ALLBERT_TRACE_ENABLED=true
mix allbert.ask --trace "show the reviewed skill and template catalog"
# Install one reviewed skill, then confirm its state.
mix allbert.skills list
```

Expected behavior:

- catalog entries show provenance, version, hash, and trust metadata.
- an installed skill lands disabled and untrusted; install grants no execution
  authority, and invalid provenance/hash is rejected or marked unusable.
- reviewed-source plugin index metadata is viewable without installing code; no
  code-bearing remote plugin is installed.
- template catalog metadata appears without executing templates.

## Docs Closeout Checklist

- Operator marketplace-lite guide exists.
- Developer metadata notes exist.
- Security-hardening docs cover marketplace-lite threats.
