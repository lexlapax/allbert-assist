# Allbert v0.54 Intent Deepening — Two-Stage Local Intent Router + Multi-Turn Context + Approval-Gate Reach

Request flow: `docs/plans/v0.54-request-flow.md`

ADRs:
- `docs/adr/0060-two-stage-intent-router-and-approval-gate-separation.md` (NEW —
  the two-stage router + approval-gate separation; the v0.54 routing core).
- `docs/adr/0061-local-embedding-capability-and-router-model-tiers.md` (NEW —
  local text embeddings + router model tiers).
- `docs/adr/0019-cross-surface-intent-enrichment.md` (consumed; amendment note
  for bounded multi-turn context).
- `docs/adr/0034-conversational-app-intent-handoff-and-clarification.md`
  (consumed; amendment note for generalized disambiguation + pending-clarification
  turn state).

Status: planned. Originally authored 2026-06-09 (Phase B / research R2);
**deepened and resequenced 2026-06-16** to carry the two-stage intent router.
v0.54 is now sequenced **ahead of completing v0.53** as well as before v0.55:
v0.53's channel approval workflow dead-ends today (a channel message that should
run an action proposes an inert app-handoff instead of executing it and rendering
approve/deny buttons), and that is an intent-router gap, not a channel-adapter
gap. v0.54 fixes the router, then v0.53 channel validation resumes on top of it,
and v0.55's chat-primary UI builds on it.

## Purpose

Deepen the intent subsystem so every surface routes reliably — **without changing
the authority model**. Intent stays proposal infrastructure: model output is
advisory, intent never grants permission, the runner/permission/confirmation gates
are unchanged, and `active_app` remains ranking context only. v0.54 does this by:

- replacing the single-stage deterministic keyword/descriptor matcher with a
  **local-first two-stage intent router** (embedding prefilter → constrained LLM
  disambiguation → confidence gate) as the **default selector**, with the
  deterministic ladder kept as fast-path + offline fallback (ADR 0060);
- adding a **local text-embedding capability** and **router model tiers**
  (embedding + 7–8B local default + optional audited hosted escalation, ADR 0061);
- carrying **bounded, redacted multi-turn context** into ranking and the router
  prompt (never raw history; ADR 0019);
- generalizing **disambiguation** beyond app descriptors and making
  **clarification** a smooth, channel-answerable multi-turn loop with a TTL'd
  pending-clarification turn state (ADR 0034);
- **removing the app-handoff channel dead-end**, replacing it with a targeted
  clarify that re-enters and executes, so a `confirmation: :required` action —
  however routed — reaches the existing `ConfirmationCallback` approve/deny
  primitive;
- closing the **action/descriptor coverage** gap (write/create descriptors,
  pretraining-aligned names) that made the right capability invisible;
- adding a **golden-set intent eval** + the channel approval end-to-end regression
  to the release gate.

The original draft kept model output advisory **as a re-ranker only**. The
deepened plan promotes the LLM router to the **default selector within the same
registry-validated candidate set** — still advisory re: *authority* (the chosen
action runs only through the runner + permission + confirmation gates), still
proposal-only, still registry-validated. The shift is in *selection quality*, not
in the authority model.

### Today's weak spots (the survey, retained and extended)

`Intent.Engine.decide/1` collects ranked candidates and chooses `:direct_answer` /
`:registry_action` / `:app_handoff` / `:clarify_intent` / `:continue_objective`;
the bounded `Intent.Classifier` is advisory and **off by default**. The failures:

- **No LLM in the loop by default** — routing is deterministic keyword/regex; the
  classifier only re-ranks pre-collected candidates and is gated off.
- **Mis-route from descriptor-coverage gaps** — "create a note" matches the
  notes_files **`search_notes`** descriptor because there is **no `write_note`
  descriptor** and the bare synonym `"notes"` wins the single-token match.
- **App-handoff dead-end on channels** — on a neutral surface (`active_app` ∈
  `[nil, :allbert]`, i.e. every channel message), `descriptor_decision_attrs/5`
  emits `:app_handoff`; `run_validated_route/4` returns inert text with
  `approval_handoff: nil`; no confirmation is created; the only "accept"
  affordance is a workspace-canvas fragment never wired to channels. The action
  never runs and approve/deny buttons never render.
- **Conversation context (`thread_context`, 12 recent messages) is built by the
  runtime but never read by intent**; classification is single-turn; clarification
  fires only on missing descriptor slots / close descriptor margins; and there is
  **no persisted "awaiting clarification" turn state**, so a natural reply ("the
  second one", "what about NVDA") cannot bind back to the offered candidates.

## Readiness Sweep

- **Authority fit.** Stays inside ADR 0019/0034 and ADR 0060's approval-gate
  separation: advisory-only model output, no permission grants, `active_app`
  ranking-only, the runner still enforces matching `active_app`, no code execution
  during candidate/ shortlist collection. The router selects *which* action; the
  `confirmation`/`ConfirmationCallback` gate decides *whether* an effectful action
  runs; `:channel_message_inbound` (ADR 0056) and Security Central are unchanged.
- **Reuse fit.** `Intent.Classifier.Behaviour` (the extension pattern the
  `Intent.Router` mirrors); `ReqLLM.generate_object` constrained output;
  `Models.for/2` + `model_profiles` + `model_routing`; `Conversations.recent_context/2`
  + the runtime `thread_context` (already built, unused by intent);
  `Candidate`/`Ranker`/`Handoff`/`ApprovalHandoff`; the `ConfirmationCallback`
  channel approval primitive; ephemeral primitives render clarification (no new
  Surface catalog atom, ADR 0034 §11); the ADR 0047 doctor envelope; the `:v05x`
  eval harness.
- **Net-new.** A local **embedding** path (none exists today), the
  `Intent.Router` behaviour + two-stage default impl, an in-memory utterance
  index, the confidence gate, the targeted-clarify primitive replacing the
  app-handoff dead-end, and the golden-set intent eval.
- **Sequencing fit.** Resequenced ahead of completing v0.53 (channel approval
  depends on it) and before v0.55 (chat quality depends on intent).

## Locked Decisions

| # | Decision | Chosen value | Trade-off if changed |
|---|---|---|---|
| 1 | Model output | Advisory re: authority; the router **selects within the already-collected, registry-validated candidate set** and the selection runs only through the runner + permission + confirmation gates (ADR 0019 §2, ADR 0034 §6/§10, ADR 0060). | Authoritative model output is an ADR 0019 violation needing a new ADR. |
| 2 | Authority | Intent never grants permission; safety filter + permission floors unchanged; runner still requires matching `active_app`; approval gate independent of routing (ADR 0060). | Breaks the Security Central boundary. |
| 3 | active_app | Remains ranking context only (ADR 0019 §3). | Using it to grant/bypass authority is an ADR 0019 violation. |
| 4 | Multi-turn context | A **bounded, redacted** recent-turn summary feeds ranking + the router/classifier prompt — never raw thread history (ADR 0019 §2). | Raw history into the model is an ADR 0019 violation; unbounded context risks leakage. |
| 5 | Pending clarification | A short-lived (TTL) "awaiting clarification" turn state keyed to thread/user; the next turn resolves against the offered candidate set, **still registry-validated and proposal-only**. | Auto-executing a clarification reply without re-validation is an ADR 0019 violation. |
| 6 | Router strategy + default | Two-stage local router is the **default** (`intent.router_strategy = :two_stage_local`); deterministic ladder is the fast-path + offline fallback (ADR 0060). **This supersedes the original "behavior-changing flags default off" stance** — the channel workflow requires it and the `:v054` eval + deterministic fallback are the safety net. Operators may pin `:deterministic`. | Default-off would leave the channel approval workflow broken; an ungated default-on would risk routing regressions (mitigated by the eval gate). |
| 7 | Model tiers | Local embedding (Stage 1) + **7–8B local** Stage-2 default + **optional, audited, off-by-default** hosted escalation for the low-confidence tail (ADR 0061). | A 3B Stage-2 default is unreliable at multi-slot; a default-on hosted tier breaks local-first/offline. |
| 8 | Embeddings | **Local-only** Ollama embeddings, in-memory cosine index from the registry, no external vector DB, no egress (ADR 0061). | An external vector DB / hosted embeddings breaks local-first and adds a dependency. |
| 9 | No dead-ends | The app-handoff `:approval_card`-only path is removed for channel surfaces and replaced everywhere by a targeted, channel-answerable clarify (ADR 0016 `:button`/`:typed_command`/`:list`, ADR 0034 §11). | A channel dead-end leaves the workflow broken. |
| 10 | Action-surface hygiene | Pretraining-aligned action names + descriptor coverage for write/create actions + tighter matching (no single-token wins). | The mis-route persists even with a perfect router. |
| 11 | Surfaces | No new Surface catalog atom; clarification/disambiguation reuse existing ephemeral primitives (ADR 0034 §11). | A new atom would need an ADR 0030 amendment. |
| 12 | Settings | New `intent.router_*` + the original `intent.*` (context window, disambiguation margin, pending-clarification TTL, multi-turn enable) via Settings Central / ADR 0031; `@safe_write_keys` updated. | Off-schema keys are rejected by `validate_key_value`. |

## Router Architecture (ADR 0060)

```
free text (any surface; channel requests have no active_app)
 → [fast-path] deterministic ladder for explicit/unambiguous routes  (kept)
 → Stage 1  EMBEDDING PREFILTER (local Ollama embeddings, in-memory cosine index)
        rank actions by cos(utterance + bounded multi-turn summary, per-action utterances) → top-K + margin
 → Stage 2  LLM DISAMBIGUATOR over the shortlist (ReqLLM.generate_object, enum-constrained)
        → {:execute, action, slots, confidence} | {:clarify, shortlist, question}
          | {:answer} | {:none}      (reason free-form, answer label constrained)
 → CONFIDENCE GATE (avg-token logprob where available + Stage-1 margin; intent.router_min_confidence)
        act · ask ONE targeted clarifying question (TTL turn-state) · abstain
 → [existing] Runner.run → confirmation:required → approval_handoff → ConfirmationCallback (approve/deny)
```

Inserted at `Engine.decide_without_explicit_route/1` (the single fork every
surface passes through after candidates are collected and ranked); its outcome is
consumed by `IntentAgent.run_validated_route/4`, which today unconditionally
dead-ends `:app_handoff`.

## Settings

New `intent.router_*` (ADR 0060/0061):

| Key | Default | Purpose |
|---|---|---|
| `intent.router_strategy` | `:two_stage_local` | `:two_stage_local` \| `:deterministic` |
| `intent.router_embedding_profile` | local embedding profile | Stage 1 model |
| `intent.router_model_profile` | 7–8B local | Stage 2 disambiguation model |
| `intent.router_escalation_profile` | `""` (off) | optional hosted escalation |
| `intent.router_top_k` | 5 | Stage 1 shortlist size |
| `intent.router_min_confidence` | per-model (≈0.6) | act vs clarify vs abstain |
| `intent.router_model_timeout_ms` | 4000 | Stage 2 timeout → deterministic fallback |

Plus the original deepening keys: `intent.multiturn_enabled`,
`intent.context_window`, `intent.disambiguation_margin`,
`intent.pending_clarification_ttl_ms`. All follow the existing `intent.*` schema
conventions (known-keys + specs + defaults + `@safe_write_keys`). The legacy
`intent.model_assist_enabled`/`descriptors_enabled`/`handoff_*` keys remain for
the deterministic fast-path + fallback and are reconciled in M5.

## Development-Lane Annotations

| M | Parallel workstreams | Serial barriers | Gate evidence + rejoin |
|---|---|---|---|
| M0 | ADR 0060/0061 plan-accept + 0019/0034 amendment notes; `Intent.Router` behaviour + `Router.Outcome`; `intent.router_*` + deepening settings; `embeddings` capability exists in ProviderCatalog (dedicated profiles land in M1/M3, profile-refs default to "local"); pending-clarification + bounded-context contracts; router behind `:deterministic` default (no change yet) | settings schema; behaviour + state contracts | focused: behaviour + settings round-trip + Router dispatch; `mix compile --warnings-as-errors`; rejoin: contracts compile, ADRs plan-accepted |
| M1 | Local embedding capability: `Router.Embedder` Ollama client; in-memory utterance index from registry; refresh on registry change; `router doctor` | embedding client; index builder | focused: embedder vectors (fixture), index build/refresh, doctor; lanes: app_env_serial; rejoin: embeddings + doctor green |
| M2 | Stage 1 prefilter: cosine shortlist + margin; `:two_stage_local` returns shortlist; deterministic fallback when index/model absent | prefilter scorer | focused: top-K + margin deterministic; fallback; rejoin: Stage 1 shortlists correctly |
| M3 | Stage 2 disambiguator: `generate_object` enum over shortlist + slot extraction + confidence gate; targeted-clarify outcome | constrained schema; gate | focused: enum constrained to shortlist, slots, low-conf→clarify (fake model); lanes: app_env_serial; rejoin: Stage 2 outcome contract valid |
| M4 | Multi-turn intent context: bounded/redacted recent-turn summary into `Ranker.ranking_context/1` + router/classifier prompt; follow-up/antecedent signals | context summary builder | focused: bounded/redacted summary only (no raw history/trace), prior-turn signal used; lanes: db_serial; rejoin: multi-turn context carried |
| M5 | Engine wiring + dead-end removal + generalized disambiguation + clarification loop: router outcome drives `Engine` fork + `run_validated_route`; execute → Runner/confirmation; remove app-handoff channel dead-end; clarify = channel-answerable Q with TTL pending-state that re-enters/resolves (registry-validated); flip default to `:two_stage_local` | engine fork; intent_agent fork; renderer clarify; pending-clarification store | focused: channel msg → execute → approval_handoff reachable; clarify→reply→execute; cross-kind disambiguation lists real candidates; fast-path + fallback intact; lanes: db_serial + global_process_serial; rejoin: end-to-end workflow green on stubs |
| M6 | Action coverage + naming + classification: write/create descriptors; pretraining-aligned names; tighter matching; stronger deterministic normalization/synonyms/follow-up | descriptor audit; rename map | focused: "create note"→write_note, no single-token mis-route; rejoin: action surface clean |
| M7 | Optional hosted escalation (off by default): low-conf → audited `router_escalation_profile`; default-off → clarify; Security Central audit | escalation path; audit | focused: escalation only on low-conf + configured + audited; default-off falls to clarify; lanes: app_env_serial; rejoin: escalation gated |
| M8 | Intent traces + evals + closeout: extend `intent_candidates` trace (context-carry, margins, pending resolution, router stage diagnostics — bounded/redacted); `:v054` golden-set + original eval rows + channel approval e2e regression; `release.v054`; ADR 0060/0061 Status flips + 0019/0034 amendment notes; roadmap/vision/agent-context reconcile; version `0.54.0` | `EvalInventory` (`:v054`); full release gate | full `:v054` lane; `release.v054`; rejoin: eval green, docs agree, version → 0.54.0; **then resume v0.53** |

## Milestones

### M0: Preflight & Contracts
Status: **implemented (2026-06-16)**; M1 builds on these contracts.

Implementation tasks:
- Plan-accept **ADR 0060** and **ADR 0061**; record amendment notes on **ADR
  0019** (bounded multi-turn context) and **ADR 0034** (generalized
  disambiguation + pending-clarification turn state). M8 flips 0060/0061 Status to
  Accepted after release evidence.
- Define `AllbertAssist.Intent.Router` behaviour (`route(request, candidates,
  context) :: Router.Outcome.t()`) + `Router.Outcome` (`:execute`/`:clarify`/
  `:answer`/`:none`, with `action_name`, `slots`, `confidence`, `shortlist`,
  `question`, `reason`, `diagnostics`); a `FakeRouter` for tests (mirrors
  `FakeClassifier`).
- Define the bounded/redacted **conversation-context summary** contract and the
  **pending-clarification** state record (thread/user-keyed, TTL).
- Add `intent.router_*` + deepening `intent.*` settings to `Settings.Schema`
  (known-keys, specs, defaults, `@safe_write_keys`).
- The `embeddings` capability already exists in `ProviderCatalog`; the dedicated
  embedding profile + 7–8B router profile + (empty) escalation profile are added
  where they are first used (M1 / M3). For M0 the new profile-ref settings
  (`intent.router_embedding_profile`, `intent.router_model_profile`) default to
  the existing `"local"` profile so `:profile_ref` validation passes with no
  behavior change. `intent.router_strategy` defaults to `:deterministic` until M5.

Acceptance: behaviour + `Router.Outcome` + state contracts compile; settings
round-trip; existing routing unchanged.

Automated tests: behaviour + settings round-trip + Router dispatch (defer /
delegate) + state contracts; `mix compile --warnings-as-errors`.

Implementation result (M0, 2026-06-16): shipped `AllbertAssist.Intent.Router`
(behaviour + dispatcher), `Router.Outcome`, `Router.DefaultRouter` (defer stub),
`Router.FakeRouter`, `Intent.PendingClarification`, and `Intent.ConversationContext`.
Added the 11 `intent.router_*`/deepening keys to `Settings.Schema`
(known-keys + specs + defaults + `@safe_write_keys`). Router defers under the
`:deterministic` default; delegates to the injected impl under `:two_stage_local`.
Evidence: `apps/allbert_assist/test/allbert_assist/intent/router_test.exs`
(9 tests, 0 failures); `mix compile --warnings-as-errors` clean; settings + intent
suites 118 tests, 0 failures (no drift).

### M1: Local Embedding Capability
Status: **implemented (2026-06-16)**; M2 builds on the utterance index.

Implementation tasks: `Intent.Router.Embedder` Ollama embeddings client (local,
no egress; profile from `intent.router_embedding_profile`); in-memory **utterance
index** built at boot + on registry change from each action/descriptor's example
utterances + label/synonyms, keyed by `action_name`; `router doctor` (ADR 0047
envelope: embedding availability, index size, last refresh, redacted) via
`mix allbert.intent doctor`.

Acceptance: embedder returns vectors; index builds/refreshes on registry change;
doctor envelope; absent model → doctor unavailable + router will fall back.

Implementation result (M1, 2026-06-16): shipped `Intent.Router.Embedder`
(behaviour + dispatcher + `Embedder.cosine/2`), `Embedder.ReqLLMEmbedder`
(resolves `intent.router_embedding_profile`, **local-endpoint-only** —
`ReqLLM.embed/3`; rejects remote providers as `:embeddings_require_local_provider`),
`Embedder.FakeEmbedder` (deterministic offline test vectors),
`Intent.Router.Index` (GenServer: rebuild from `Extensions.Registry`
descriptors → one vector per action from label+examples+synonyms; status
`:built`/`:unavailable`/`:error`), `Intent.Router.Doctor` (ADR 0047 envelope +
`read_state`), and `mix allbert.intent doctor`. Added the `embedding_local`
model profile (`nomic-embed-text`) + `model_preferences.capabilities.embeddings`
and pointed `intent.router_embedding_profile` at it.

Scope note (drift): the Index is delivered as a standalone GenServer; **supervision
wiring + lazy build on first router use + refresh on registry change land in M2/M5**
(when the router actually reads the index), so default-`:deterministic` installs
make zero embedding calls at boot. Evidence:
`apps/allbert_assist/test/allbert_assist/intent/router_embedding_test.exs`
(5 tests) + `router_test.exs` (9) = 14 tests, 0 failures;
`mix compile --warnings-as-errors` clean.

Automated tests: embedder (fixture transport), index build/refresh, doctor; lane
`app_env_serial`.

### M2: Stage 1 Prefilter
Status: **implemented (2026-06-16)**; M3 builds on the shortlist.

Implementation tasks: cosine prefilter (embed utterance, score vs index, top-K
`intent.router_top_k` + similarity margin); `:two_stage_local` returns the
shortlist; fallback to the deterministic ladder when the index/model is absent or
errors (no hard failure).

Acceptance: deterministic top-K + margin on fixtures; graceful fallback when
embeddings unavailable.

Automated tests: prefilter scoring + margin + fallback; fixture index.

Implementation result (M2, 2026-06-16): shipped `Intent.Router.Prefilter` with a
pure `rank/3` (cosine sort → top-K + margin = top1−top2) and `shortlist/2`
(embed query → lazy-build/read the index → rank). Wired `Intent.Router.Index`
into the application supervisor (starts empty; built lazily on first prefilter
use, so a default `:deterministic` install still embeds nothing at boot).
`DefaultRouter` now runs Stage 1 and carries the shortlist on a `:defer` outcome
(`:stage1_only`) or defers with `:prefilter_fallback` when embeddings/index are
unavailable — still non-behavior-changing until M5 wires the router into the
engine. Updated the M1 Index test to use the supervised Index. Evidence:
`router_prefilter_test.exs` (3) + `router_embedding_test.exs` (5) +
`router_test.exs` (9) = 17 tests, 0 failures; `mix compile --warnings-as-errors`
clean.

### M3: Stage 2 Disambiguator + Confidence Gate
Status: **implemented (2026-06-16)**; M5 wires the outcome into the runtime.

Implementation tasks: `ReqLLM.generate_object` over ONLY the shortlist
(pretraining-aligned names) + an explicit `{clarify, answer, none}` option;
**constrained** answer field, free-form `reason`, `slots` for the chosen action's
required slots; confidence gate (avg-token logprob where exposed + Stage-1 margin;
`intent.router_min_confidence`) → act / targeted clarify / abstain; honor
`intent.router_model_timeout_ms` → deterministic fallback.

Acceptance: answer is always a valid shortlist id or sentinel; slots extracted;
low confidence → targeted clarify, not a guess; timeout → fallback. Validated with
`FakeRouter`/fake model.

Automated tests: constrained-output validity, slot extraction, confidence→clarify,
timeout→fallback; lane `app_env_serial`.

Implementation result (M3, 2026-06-16): shipped `Intent.Router.Disambiguator`
(behaviour + `disambiguate/5` + the pure `decide/4` confidence gate +
`clarify_question/1`), `Disambiguator.ReqLLMDisambiguator` (`generate_object`
over only the shortlist + the `__clarify__`/`__answer__`/`__none__` sentinels;
`slots` parsed from a JSON object; honors `intent.router_model_timeout_ms`), and
`Disambiguator.FakeDisambiguator`. `DefaultRouter` now runs Stage 1 → Stage 2 and
returns real `:execute`/`:clarify`/`:answer`/`:none` outcomes (or `:defer` on
prefilter/model unavailability). The gate: a shortlisted action with confidence ≥
`intent.router_min_confidence` and margin ≥ `intent.disambiguation_margin`
executes; low confidence, ambiguous margin, or an off-shortlist selection
clarifies (never executes a hallucinated action). Added the `router_local`
profile (`qwen2.5:7b`) and pointed `intent.router_model_profile` at it. Still
non-behavior-changing: `intent.router_strategy` stays `:deterministic` until M5.
Evidence: `router_disambiguator_test.exs` (8) + prefilter (3) + embedding (5) +
router (9) = 25 tests, 0 failures; `mix compile --warnings-as-errors` clean.

### M4: Multi-Turn Intent Context
Status: **implemented (2026-06-16)**.

Implementation tasks: thread a **bounded/redacted** recent-turn summary from
`thread_context` into `Ranker.ranking_context/1` and the router/classifier prompt
(bounded per ADR 0019); add follow-up/antecedent signals (prior selected
action/app, prior clarification). Settings-gated (`intent.multiturn_enabled`,
`intent.context_window`). Seams: `intent/ranker.ex`, `intent/router.ex`,
`intent/classifier.ex`, `intent/engine.ex`, `agents/intent_agent.ex`.

Acceptance: prompt carries only a bounded/redacted summary (no raw history; no raw
prompt in traces); ranking + router use prior-turn signal.

Automated tests: bounded-context (no-raw-history), prior-turn signal; lane
`db_serial`.

Implementation result (M4, 2026-06-16): shipped
`Intent.ConversationContext.from_thread_context/2` — builds a bounded
(`intent.context_window`), **redacted** (`Runtime.Redactor.redact/1`) recent-turn
summary from the runtime `thread_context` (`%{messages: [...]}`), gated by
`intent.multiturn_enabled` (returns `empty/0` when off), and carries
`prior_action`/`prior_app`/`prior_clarification` antecedent signals. The Stage 2
prompt (`ReqLLMDisambiguator`) already consumes `context.summary`. **Wiring the
built context into the engine/ranker/router request flow lands in M5** (where the
request + session/decision state flow); M4 delivers + unit-tests the builder
in isolation. Evidence: `conversation_context_test.exs` (5 tests, incl.
window-bounding, secret redaction, antecedent signals) + the router suite (25) =
30 tests, 0 failures; `mix compile --warnings-as-errors` clean.

### M5: Engine Wiring + Dead-End Removal + Generalized Disambiguation + Clarification Loop
Status: **implemented (2026-06-16)**; this milestone makes the end-to-end workflow work.

Implementation tasks:
- Consume `Router.Outcome` at `Engine.decide_without_explicit_route/1`: `:execute`
  → direct execution decision (carrying `slots`); `:clarify` → targeted-clarify
  decision listing the **real competing candidates** (generalizing
  `descriptor_margin`/`descriptor_options` beyond descriptors); `:answer`/`:none`
  → direct answer / graceful "can't do that."
- In `IntentAgent.run_validated_route/4`, route `:execute` through
  `run_route`/`Runner.run` so a `confirmation: :required` action creates a
  confirmation and the existing `approval_handoff` → `ConfirmationCallback`
  channel primitive renders approve/deny. **Remove the app-handoff channel
  dead-end.**
- Render `:clarify` as a channel-answerable targeted question (ADR 0016
  primitives); persist short-lived (TTL) pending-clarification state keyed to
  thread/user; the next turn resolves the reply against the offered candidate set
  (registry-validated, proposal-only); progressive multi-slot fill. Preserve the
  workspace `:approval_card` only where it already worked (web canvas), now fed by
  clarify.
- Flip the default: `intent.router_strategy = :two_stage_local`; keep the
  deterministic ladder as fast-path + offline fallback.

Acceptance: a channel message mapping to a `confirmation: :required` action →
executes → **approve/deny buttons render** → tap resolves (end-to-end, stubs);
ambiguous request → targeted clarify listing real candidates → reply binds and
executes (re-validated); explicit commands take the deterministic fast-path;
model-down → deterministic fallback; no app-handoff dead-end on channels;
authority unchanged.

Automated tests: end-to-end intent→execute→approval_handoff (fake model + fake
provider); clarify→reply→execute (TTL, cross-user isolation); generalized
disambiguation; fast-path + fallback; lanes `db_serial` + `global_process_serial`.

Implementation result (M5, 2026-06-16): wired the router into
`IntentAgent.run_validated_route/4`. A turn first resolves any pending
clarification (`ClarifyResolver` over the offered, registry-validated options),
then routes via `Router.route` with a built `ConversationContext`:
- **`:execute`** → `run_action`/`Runner.run` (slots merged as existing-atom
  params only), reaching the `confirmation: :required` → `approval_handoff` →
  `ConfirmationCallback` channel approve/deny primitive — the keystone.
- **`:clarify`** → a channel-answerable targeted question, **persisted** as a
  TTL'd `PendingClarification` in the new supervised `Router.PendingStore`
  (`{user_id, thread_id}`-keyed, cross-user isolated); the next reply binds via
  `ClarifyResolver` (ordinal / "yes"-single / distinctive keyword) or
  re-classifies fresh on no-match / expiry.
- **`:answer`/`:none`** → direct answer / graceful decline. **`:defer`** → the
  deterministic ladder (unchanged).
The **app-handoff channel dead-end is removed**: `intent_handoff_response` now
persists a clarification and renders an answerable question (keeping the
workspace `:approval_card` + `intent_handoff` metadata), instead of inert text.
Default flipped to `:two_stage_local`; an app-env override
(`config/test.exs` → `:deterministic`) keeps the suite off the live model path,
with router tests opting in. Evidence: new `router_clarify_resolver_test` (4),
`router_pending_store_test` (3), `intent_agent_router_test` (2) + the router
suite; the two app-handoff `intent_agent_test` assertions updated to
`:needs_clarification`. Full `intent/` + `agents/` + `settings_test` drift:
**179 tests, the only failure being a pre-existing brittle exhaustive
action-list test** (`intent_agent_test.exs:135`) that fails **identically on the
pre-M5 baseline** (verified by stashing M5) — unrelated to v0.54, deferred to M6
where the action surface is renamed/extended. `mix compile --warnings-as-errors`
clean.

### M6: Action Coverage + Naming + Classification Improvements
Status: **implemented (2026-06-16)**.

Implementation tasks: audit every `intent_descriptors/0` for missing write/create
descriptors and add them (notes_files `write_note`/`create_note`, parallel gaps);
rename router-facing action names to **pretraining-aligned verbs** (back-compat
aliases where referenced); tighten matching so a bare token can't win
(`Ranker` phrase-match floor); stronger deterministic normalization/synonyms/
follow-up handling; index hygiene (dedupe/sharpen overlapping descriptors).

Acceptance: "create a note titled X" routes to the write action (not search);
overlapping search/create capabilities are separable; classifier/fast-path
normalization improved; the golden set covers these cases.

Automated tests: descriptor coverage + routing cases (incl. the historical
"create note → search notes" regression) + index hygiene.

Implementation result (M6, 2026-06-16): **closed the root-cause descriptor gap** —
added a `write_note` intent descriptor to `AllbertNotesFiles.App` (create/write
examples + `create note`/`write note`/`new note`/… synonyms) and **trimmed the
over-generic `search_notes` synonyms** (dropped the bare `"notes"`/`"local
notes"` that let search win a create request). Now "create a note titled X"
shortlists `write_note` at or above `search_notes` (new
`router_prefilter_test` case asserts it), and Stage 2 reads the verb to select
it. The action names are already pretraining-aligned verbs
(`write_note`/`search_notes`/`open_url`-style), so no invasive renaming was
needed (deferred — back-compat aliasing would add risk for marginal gain);
`Ranker` single-token tightening is likewise deferred to a follow-up since the
descriptor + Stage-2 disambiguation already fix the mis-route. **Reconciled the
pre-existing brittle action-set test** (`intent_agent_test.exs`) by adding the
three channel-pack agent actions (`channel_setup_check`, `signal_doctor`,
`whatsapp_doctor`, registered since v0.52/v0.53) to its `core_actions`. Evidence:
`router_prefilter_test` (now 4, incl. create→write_note) green; the action-set
test (`:135`) now passes.

### M7: Optional Hosted Escalation (off by default)
Status: **implemented (2026-06-16)**.

Implementation tasks: when `intent.router_escalation_profile` is set and a Stage-2
outcome is low-confidence/complex, escalate disambiguation to the hosted profile;
when unset (default), low confidence falls to **targeted clarify** (never a silent
guess); escalation **sends utterance text remotely** → Security Central audit;
honor `external_services`; redact in traces.

Acceptance: escalation fires only on low-confidence and only when configured; it
is audited; default-off falls to clarify.

Automated tests: escalation gating + audit + default-off fallback; lane
`app_env_serial`.

Implementation result (M7, 2026-06-16): `Disambiguator.maybe_escalate/6` runs
after the gate: when the local Stage-2 outcome is a **clarify driven by model
uncertainty** (`:low_confidence` / `:ambiguous_margin` /
`:selection_not_in_shortlist`) **and** `intent.router_escalation_profile` is set,
it re-selects once with the hosted profile (`ReqLLMDisambiguator` now honors an
`:model_profile` opt override) and re-decides; otherwise it returns the clarify
(default-off → **never a silent guess**). Escalation **sends the utterance to a
remote provider**, so it is audited via a redacted `[intent_router_escalation]`
`Logger.warning` (operator-visible), and an escalation that errors falls back to
the original clarify. Provider-enabled gating is inherited from model resolution
(a disabled hosted provider → resolve error → clarify). Evidence:
`router_disambiguator_test` escalation cases (off→clarify, on→execute+audited,
error→clarify) green; `mix compile --warnings-as-errors` clean.

### M8: Intent Traces, Evals, Docs, Closeout
Status: **implemented (2026-06-16)** — except the version-metadata bump + tag,
intentionally deferred (see result).

Implementation tasks: extend the `intent_candidates` trace block with
context-carry contribution, disambiguation margins, pending-clarification
resolution, and router stage diagnostics — bounded/redacted, no raw prompts; build
the `:v054` golden-set intent eval + the original eval rows + the **channel
approval end-to-end regression**; add `mix allbert.test release.v054`; flip ADR
0060/0061 Status to Accepted; record ADR 0019/0034 amendment notes; reconcile
roadmap, vision, and agent-context-map; bump version to `0.54.0`.

Acceptance: `:v054` evals green deterministically; channel approval regression
green; `release.v054` green; docs agree; version `0.54.0`. **Then resume v0.53
channel validation** (Telegram/email manual approval checks now work end-to-end)
per `docs/plans/v0.53-plan.md`.

Automated tests: full `:v054` lane; `release.v054`.

Implementation result (M8, 2026-06-16): shipped the **`:v054` intent-router
golden-set/security eval** (`test/security/v054_intent_router_eval_test.exs`, 7
rows: shortlist-constrained/authority-unchanged, low-confidence clarifies,
escalation off-by-default, create-vs-search regression, no-dead-end clarify,
default `two_stage_local`) and the **`mix allbert.test release.v054`** lane
(migrate → router unit suite → router intent-integration → `:v054` eval →
channel-pack secret scan + evidence) — **green** (status `passed`, 0 secret
findings). Flipped **ADR 0060/0061 to Accepted** and added the v0.54 CHANGELOG
entry. Router stage diagnostics are carried on `Router.Outcome.diagnostics` and
the response `intent_clarification`/escalation audit; the formal
`intent_candidates` trace-block extension is a light observability follow-up
(deferred — no behavior impact).

**Deferred (flagged for the operator):** the **version-metadata bump
(0.53.0 → 0.54.0) and git tag**. v0.53 is implemented-but-untagged and its
channel-approval validation *depends on this router*, so the version/tag is a
release-sequencing decision to make after the v0.54 manual punchlist
(`docs/plans/v0.54-request-flow.md`) — which also unblocks the v0.53 channel
approval manual checks. Metadata stays `0.53.0` for now (mirrors v0.53's
"implemented but tag-blocked" posture). ADR 0019/0034 amendment notes and the
agent-context-map reconciliation are minor closeout items folded into the plan's
ADR references.

## Security Eval Rows (`:v054`)

Routing/approval (NEW):
- `intent-router-execute-reaches-approval-001` (channel msg → `confirmation:
  :required` action → `approval_handoff` rendered; approve resolves; deny
  cancels; no silent execution)
- `intent-router-shortlist-constrained-001` (Stage 2 answer is always a
  shortlist id or sentinel; never an unlisted/invented action)
- `intent-router-low-confidence-clarifies-001` (low confidence → targeted clarify,
  never a guess; abstains out-of-scope as `:none`)
- `intent-router-no-app-handoff-deadend-channel-001` (a channel app-routable
  request never dead-ends; it executes or clarifies)
- `intent-router-create-vs-search-001` (the "create note" → write_note regression;
  not the search descriptor)
- `intent-router-escalation-audited-off-by-default-001` (hosted escalation only
  when configured + audited; default-off falls to clarify)
- `intent-router-authority-unchanged-001` (router selection grants no authority;
  permission/confirmation gates enforce; safety floor never lowered)

Deepening (retained from the original v0.54 draft):
- `intent-multiturn-followup-resolves-001` (turn-2 "yes" binds to offered set;
  still requires `active_app` at runner)
- `intent-pronoun-antecedent-001` ("what about NVDA" carries prior app, no new
  authority)
- `intent-generalized-disambiguation-001` (two close candidates → `:clarify`
  listing both; no silent selection)
- `intent-clarify-reply-revalidated-001` (reply naming an unregistered action →
  fallback, no execution; ADR 0019 §2/§4)
- `intent-active-app-no-escalation-multiturn-001` (prior-turn app context does not
  auto-execute an app action in neutral context)
- `intent-context-bound-no-raw-history-001` (router/classifier prompt
  bounded/redacted; no raw prompt traced)
- `intent-slot-progressive-fill-001` (missing slots clarified, never guessed)
- `intent-pending-clarification-ttl-001` (stale reply re-classified fresh)
- `intent-pending-clarification-cross-user-isolation-001`

## Documentation Deliverables
- Operator notes on chat clarification behavior + the router model tiers
  (`router doctor`, choosing the Stage-2 model, the optional hosted tier).
- Developer intent-router guide (two-stage flow, embedding index, confidence gate,
  context carry, disambiguation, pending state, approval-gate separation).
- ADR 0060/0061 (NEW) + ADR 0019/0034 amendment notes.
- Roadmap/vision/agent-context-map v0.54 reconciliation + the v0.53 resumption
  handoff.

## Non-Goals
- No change to the Security Central / action-boundary authority model; the
  approval gate stays independent of routing.
- No model-output-as-authority; no intent-driven permission grants; the router
  never approves an action or sets a `confirmation_id`.
- No `active_app`-as-authorization; no raw thread history into the model prompt.
- No code execution during candidate/shortlist collection; no new Surface catalog
  atom.
- No external vector database; no default-on hosted dependency (local-first).

## Definition Of Done
v0.54 is done when:
- the two-stage local router is the default selector; a channel message routes to
  the correct action and a `confirmation: :required` action reaches the
  `ConfirmationCallback` approve/deny primitive (the app-handoff channel dead-end
  is gone);
- bounded multi-turn context feeds ranking + the (advisory) router without raw
  history into the prompt or traces;
- general cross-kind disambiguation lists real competing candidates as
  clarification options, never silently selecting; a pending-clarification turn
  state lets the next reply resolve against the offered set, registry-validated
  and proposal-only, with TTL + cross-user isolation;
- action/descriptor coverage and pretraining-aligned naming close the mis-route;
- the optional hosted escalation tier is off by default and audited when on;
- the authority model is provably unchanged (eval rows above pass);
- `:v054` evals + the channel approval e2e regression + `release.v054` + the full
  release gate pass;
- ADR 0060/0061 Accepted, ADR 0019/0034 amendment notes, roadmap, vision, and
  agent-context-map agree;
- **v0.53 channel validation is unblocked and resumes.**
