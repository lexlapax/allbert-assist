# Allbert v0.60 Request Flow — Product Experience Design

Status: validation complete and release-ready for `v0.60.0` on 2026-06-30.
Post-implementation remediation M8.1-M8.4 and M9.1-M9.6, operator validation
S0-S6, Chrome S4.5, and the `release.v060` gate passed before tag. The final
pre-validation audit found no blocker/major issues; M9.6 closed the zero-residual
polish list before operator validation. Design-first release inserted
2026-06-30 before the v0.61 Presentation Layer Overhaul. v0.60 ships **no new
authority, capability, or runtime behavior beyond a navigable walking skeleton**
— it is a design + scaffolding release whose deliverables are specifications,
two accepted ADRs (0077, 0078), and a placeholder-only walking skeleton. Security
Central is unchanged.

Implementation details live in `docs/plans/v0.60-plan.md`.

## Source Documents
- `docs/plans/v0.60-plan.md`
- `docs/adr/0077-product-experience-design-and-information-architecture.md`
  (Accepted (v0.60) — the unified journey, the IA / navigation / composition
  redesign, the "final UX layout redesign" ADR 0074 deferred)
- `docs/adr/0078-first-model-path.md` (Accepted (v0.60) — assisted-local vs
  managed-hosted vs honest-BYOK, the QuickStart "first useful chat" contract; the
  First-Model Path is realized across v0.61 (landing / empty-state), v0.62
  (packaging), and v0.63 (onboarding), not v0.62 alone)
- `docs/adr/0074-web-design-system-and-ux-language.md` (the v0.58 design-system
  baseline that explicitly deferred the layout redesign — §57 baseline-not-
  redesign, §71 deferred chat-primary re-layout — and receives the M7 v0.61 input)
- `docs/adr/0030-*` (unified surface catalog + renderer — the rendering boundary
  the walking skeleton renders through)
- `docs/adr/0073-cross-surface-contract.md`, `docs/adr/0075-user-category-settings-profiles.md`
  (the persona content M4 designs), `docs/adr/0076-packaging-distribution-and-unified-cli.md`
  (the entry points M5 designs), `docs/adr/0069-operator-onboarding-flow.md`
  (the onboarding M4 designs), `docs/adr/0072-recommended-model-profiles-per-purpose.md`
- v0.58 plan/request-flow (the design-system substrate) and v0.59 plan/request-flow
  (the hardened substrate this designs on top of)

## Milestones (consistent with the plan)
- **M1** Product Experience Spec — install → first-run → onboard → first-value →
  daily-use journey, written once for the technical-prosumer audience.
- **M2** Information Architecture & Navigation Model — sitemap, screen inventory,
  navigation model, workspace/screen composition → **ADR 0077**; the layout
  redesign ADR 0074 deferred.
- **M3** First-Model Path Decision — assisted-local default + BYOK fallback
  (managed-hosted rejected), detect + guided Ollama install (no bundling),
  QuickStart "first useful chat", packaging implications → **ADR 0078**.
- **M4** Onboarding Flow & Persona Model Design — two-track wizard UX + persona
  model (the ADR 0075 content); design input feeding v0.63.
- **M5** Entry-Point & CLI UX Design — command taxonomy, first-run detection,
  wizard launch; design input feeding v0.62.
- **M6** Walking Skeleton — navigable routes + nav shell + placeholder screens
  implementing the M2 IA; the gate artifact + v0.61 build scaffold.
- **M7** Design-System Gap Analysis — token/component/pattern gaps the overhaul
  needs; the ADR 0074 v0.61 input.
- **M8** Evals + `release.v060` gate.

## Locked Decisions
Recorded in `v0.60-plan.md`: design-first scope (specs + ADR 0077/0078 + a
placeholder walking skeleton, **no** presentation/packaging/onboarding/RC build);
the downstream build order is unchanged and correct, v0.60 only front-loads the
*design*; the freeze-trap (v1.0 freezes presentation contracts, so the deferred
redesign is designed in v0.60 and built in v0.61 before the freeze); exactly two
ADRs accepted (0077, 0078); the First-Model Path decided across assisted-local /
managed-hosted / honest-BYOK with a QuickStart contract; the walking skeleton is
navigable but placeholder-only with no live data and no authority; the persona /
onboarding / entry-point work is designed here and built in v0.62-v0.63; the gate
is real (format / compile / Credo / Dialyzer clean + ADRs Accepted + artifacts
present + skeleton navigable + nav/a11y smoke + docs gate).

## Cross-Surface Data / Decision Flow (the one path)
```text
DESIGN (no runtime behavior)
  M1 product-experience spec  (install -> first-run -> onboard -> first-value -> daily-use)
    -> first-value moment = "first useful chat"
    -> per-stage owning release map  (v0.61 / v0.62 / v0.63)

  M2 information architecture  (consumes M1)
    -> sitemap + screen inventory + navigation model + workspace/screen composition
    -> the layout redesign ADR 0074 deferred (chat-primary, ephemerals-as-modals)
    -> ADR 0077 Accepted (v0.60)
    -> feeds M6 (routes to render) and M7 (composition rules to gap-analyze)

  M3 first-model path  (consumes M1 first-value)
    -> decide assisted-local | managed-hosted | honest-BYOK
    -> QuickStart "first useful chat" contract
    -> packaging implications (handed to v0.62)
    -> ADR 0078 Accepted (v0.60)
    -> feeds M4 (QuickStart track) and M5 (first-run model-state check)

  M4 onboarding + persona design  (consumes M1, M3)  -> v0.63 input (ADR 0069 + ADR 0075 content)
  M5 entry-point + CLI UX design   (consumes M1, M3, M4)  -> v0.62 input (ADR 0076)
  M7 design-system gap analysis    (consumes M2, M6)  -> v0.61 input (ADR 0074 amendment)

WALKING SKELETON (the only running code v0.60 adds)
  M6 nav shell + placeholder screen per IA route
    -> render through the unified catalog inside one app shell (ADR 0030/0073/0074)
    -> route-specific composition zones render inside each placeholder route
    -> routes resolve; nav model exposed; keyboard-focus/high-contrast/reduced-motion hold
    -> NO live data, NO business-state read, NO effectful affordance, NO authority
    -> the v0.61 build scaffold + the M8 gate artifact

GATE
  M8 mix allbert.test release.v060
    -> format + compile-warnings-as-errors + Credo strict + Dialyzer
    -> design artifacts present + docs-gate clean
    -> ADR 0077 + ADR 0078 Accepted (v0.60)
    -> walking-skeleton routes resolve + nav/a11y smoke
    -> :v060 evals + handoff drift-check (v0.61/v0.62/v0.63 consumers)
    -> redacted evidence under release_evidence/v060/
```

## Authority Invariants
- No new capability, no new authority surface, no confirmation-floor change, no
  egress grant, no Settings key, no contract freeze. Security Central remains the
  only authority boundary.
- The walking skeleton renders through the unified catalog inside one app shell
  (ADR 0030/0073/0074). No model-generated or data-generated markup is introduced.
- Placeholder screens read no business state, compose no effectful affordance, and
  grant no authority or egress. They prove the IA is navigable and scaffold v0.61.
- The design milestones (M1-M5, M7) change documents only; they add no code path,
  no setting, and no seeded value. The persona / First-Model-Path / onboarding
  designs are inputs to v0.62-v0.63, not v0.60 implementations.
- Exactly two ADRs are accepted (0077, 0078); ADR 0074/0075/0069/0076 receive
  design inputs, not implementation.

## Security / Eval Rows (`:v060`)
See `v0.60-plan.md` for the one-line assertion per row. Design-artifact presence:
`product-experience-spec-present-001`, `information-architecture-spec-present-001`,
`first-model-path-design-present-001`, `onboarding-flow-design-present-001`,
`persona-model-design-present-001`, `entry-point-cli-ux-design-present-001`,
`design-system-gap-analysis-present-001`.
ADR acceptance: `adr-0077-accepted-001`, `adr-0078-first-model-path-accepted-001`.
Walking skeleton:
`walking-skeleton-routes-resolve-001`, `walking-skeleton-nav-shell-001`,
`walking-skeleton-a11y-smoke-001`. Design-only invariants:
`no-new-authority-design-only-001`, `rc-design-handoff-no-drift-001`,
`first-model-persona-cross-doc-coherence-001`, `persona-seed-preaudit-001`.

These 16 rows register in `apps/allbert_assist/test/support/security_fixtures/eval_inventory.ex`
as the `:v060` milestone-union addition, mirroring the `:v059` sweep plumbing, but
ownership is split across two `test_module`s. The **4** skeleton-facing rows — the
three `walking-skeleton-*` rows plus `no-new-authority-design-only-001` — are owned by
`AllbertAssistWeb.Skeleton.WalkingSkeletonTest`, which renders the skeleton and asserts
them. The other **12** rows — the 7 design-artifact presence rows, the 2 ADR
acceptance rows, `rc-design-handoff-no-drift-001`, and
`first-model-persona-cross-doc-coherence-001`, plus
`persona-seed-preaudit-001` — are owned by
`AllbertAssist.Security.V060SweepEvalTest` in
`apps/allbert_assist/test/security/v060_sweep_eval_test.exs`. The walking-skeleton
rows are the eval-side counterpart of the S4.5 browser pass: the browser pass produces
the human-observed evidence, the sweep asserts the rows are present and green.

## Smoke Checklist
- The M1 journey spec covers all five stages, names the first-value moment, and
  maps each stage to its owning downstream release.
- The M2 IA doc defines a sitemap, screen inventory, navigation model,
  workspace/screen composition rules, and preview-route manifest; ADR 0077 is
  Accepted (v0.60).
- ADR 0078 is Accepted (v0.60), names the chosen First-Model Path, defines the
  QuickStart "first useful chat" contract, and lists the v0.62 packaging
  implications.
- The M3 first-model path design doc is present and marked as the v0.62/v0.63
  handoff for QuickStart, BYOK fallback, and packaging implications.
- The M4 onboarding/persona design and the M5 entry-point/CLI design are present
  and marked as v0.63 and v0.62 design inputs respectively, with no v0.60 build.
- The M7 gap analysis enumerates token/component/pattern gaps mapped to composition
  rules / skeleton screens as the ADR 0074 v0.61 input.
- Every IA route resolves through the catalog/shell to a placeholder screen; the
  nav shell exposes the IA navigation model; each route renders a route-specific
  composition zone; keyboard focus / high-contrast / reduced-motion hold across
  all skeleton screens; no screen reads live data or grants authority.
- `mix allbert.test release.v060` is green and deterministic; format, compile,
  Credo, Dialyzer, docs, ADR-acceptance, and skeleton nav/a11y evidence are
  embedded; no presentation/packaging/onboarding/RC build scope leaked into v0.60.

## Operator / Reviewer Validation

Status: complete. Operator validation S0-S6 passed on 2026-06-30 using disposable
validation Home `/tmp/allbert-v060-web.uYIRMq` and retained redacted evidence
under `/Users/spuri/.allbert-release-evidence/v060`. This is a **design-review +
skeleton-navigation** validation, not a runtime capability validation: there is
no new authority to exercise. Stop at the first non-zero status, missing
artifact, un-accepted ADR, unresolved route, a11y failure, or persona pre-audit
drift if rerunning before tag.

### S0 - Task And Artifact Visibility
```sh
set -euo pipefail
export V060_EVIDENCE_DIR="$HOME/.allbert-release-evidence/v060"
mkdir -p "$V060_EVIDENCE_DIR"

MIX_ENV=test mix help allbert.test > "$V060_EVIDENCE_DIR/s0-allbert-test-help.txt"
rg 'release\.v060' "$V060_EVIDENCE_DIR/s0-allbert-test-help.txt"

ls docs/design/product-experience-spec.md \
   docs/design/information-architecture.md \
   docs/design/first-model-path.md \
   docs/design/onboarding-flow.md \
   docs/design/persona-model.md \
   docs/design/entry-point-cli-ux.md \
   docs/design/design-system-gap-analysis.md \
   > "$V060_EVIDENCE_DIR/s0-design-docs.txt"
ls docs/adr/0077-product-experience-design-and-information-architecture.md \
   docs/adr/0078-first-model-path.md \
   > "$V060_EVIDENCE_DIR/s0-adrs.txt"

printf 'PASS S0 v0.60 gate and design artifacts visible\n'
```
Expected output:
```text
release.v060
PASS S0 v0.60 gate and design artifacts visible
```
If `release.v060` or any v0.60 design doc / ADR is absent, stop: the checkout is
stale or the relevant milestone has not landed.

### S1 - Design-Artifact Presence + Docs Gate
```sh
set -euo pipefail
for f in product-experience-spec information-architecture first-model-path \
         onboarding-flow persona-model entry-point-cli-ux \
         design-system-gap-analysis; do
  test -s "docs/design/$f.md" || { echo "MISSING docs/design/$f.md"; exit 1; }
done

# M1 spec covers all five journey stages and names the first-value moment.
rg -i 'install' docs/design/product-experience-spec.md
rg -i 'first.run' docs/design/product-experience-spec.md
rg -i 'onboard' docs/design/product-experience-spec.md
rg -i 'first.value|first useful chat' docs/design/product-experience-spec.md
rg -i 'daily.use' docs/design/product-experience-spec.md

# M2 IA defines the four structural pieces.
rg -i 'sitemap' docs/design/information-architecture.md
rg -i 'screen inventory' docs/design/information-architecture.md
rg -i 'navigation model' docs/design/information-architecture.md
rg -i 'composition' docs/design/information-architecture.md
rg -i 'preview.route|route manifest' docs/design/information-architecture.md
rg '^## Preview Route Manifest$' docs/design/information-architecture.md
rg '^\| route_id \| preview_path \| title \| nav_group \| active_key \| catalog_components \|$' \
  docs/design/information-architecture.md
rg '\| /preview' docs/design/information-architecture.md

# M3 first-model design captures the handoff, not only the ADR decision.
rg -i 'quickstart|first useful chat' docs/design/first-model-path.md
rg -i 'byok|bring your own key|fallback' docs/design/first-model-path.md
rg -i 'packaging' docs/design/first-model-path.md

MIX_ENV=test mix allbert.test docs > "$V060_EVIDENCE_DIR/s1-docs-gate.txt" 2>&1
rg '^==> docs$' "$V060_EVIDENCE_DIR/s1-docs-gate.txt"
printf 'PASS S1 design artifacts present and docs-gate clean\n'
```
Expected output:
```text
==> docs
PASS S1 design artifacts present and docs-gate clean
```
Expected evidence: each `docs/design/*.md` deliverable exists and is non-empty;
the journey spec covers all five stages and names the first-value moment; the IA
doc defines the sitemap, screen inventory, navigation model, composition rules,
and parser-stable preview-route manifest; the first-model design doc names
QuickStart, fallback, and packaging implications; the docs gate is clean.

### S2 - ADR Acceptance
```sh
set -euo pipefail
rg -n 'Status:\s*Accepted \(v0\.60\)' \
  docs/adr/0077-product-experience-design-and-information-architecture.md
rg -n 'Status:\s*Accepted \(v0\.60\)' \
  docs/adr/0078-first-model-path.md

# ADR 0077 carries the IA / nav / composition redesign ADR 0074 deferred.
rg -i 'information architecture|navigation|composition|chat.primary|layout redesign' \
  docs/adr/0077-product-experience-design-and-information-architecture.md
printf 'PASS S2 ADR 0077 and ADR 0078 accepted at v0.60\n'
```
Expected output:
```text
Status: Accepted (v0.60)
Status: Accepted (v0.60)
PASS S2 ADR 0077 and ADR 0078 accepted at v0.60
```
Expected evidence: ADR 0077 and ADR 0078 both report `Accepted (v0.60)`; ADR 0077
contains the deferred ADR 0074 layout redesign as the IA / navigation /
composition design.

### S3 - First-Model Path Review
```sh
set -euo pipefail
rg -i 'assisted.local' docs/adr/0078-first-model-path.md
rg -i 'managed.hosted' docs/adr/0078-first-model-path.md
rg -i 'honest.byok|bring your own key|byok' docs/adr/0078-first-model-path.md
rg -i 'quickstart|first useful chat' docs/adr/0078-first-model-path.md
rg -i 'packaging' docs/adr/0078-first-model-path.md

rg -i 'quickstart|first useful chat' docs/design/first-model-path.md
printf 'PASS S3 first-model path decided with QuickStart and packaging implications\n'
```
Expected output:
```text
PASS S3 first-model path decided with QuickStart and packaging implications
```
Expected evidence: ADR 0078 evaluates assisted-local, managed-hosted, and
honest-BYOK; names the chosen path; defines the QuickStart "first useful chat"
contract; and enumerates the v0.62 packaging implications.

### S4 - Walking-Skeleton Navigation
```sh
set -euo pipefail
export V060_WEB_HOME="$(mktemp -d /tmp/allbert-v060-web.XXXXXX)"
export V060_WEB_PORT=4060
export V060_BASE_URL="http://127.0.0.1:${V060_WEB_PORT}"
export V060_EVIDENCE_DIR="${V060_EVIDENCE_DIR:-$HOME/.allbert-release-evidence/v060}"
export V060_SHOTS="$V060_EVIDENCE_DIR/screenshots"
mkdir -p "$V060_EVIDENCE_DIR" "$V060_SHOTS"

ALLBERT_HOME="$V060_WEB_HOME" ALLBERT_HOME_DIR="$V060_WEB_HOME" MIX_ENV=dev \
  mix run --no-start -e 'AllbertAssistWeb.Skeleton.RouteManifest.preview_paths() |> Enum.each(&IO.puts/1)' \
  > "$V060_EVIDENCE_DIR/s4-preview-routes.txt"
test -s "$V060_EVIDENCE_DIR/s4-preview-routes.txt"

ALLBERT_HOME="$V060_WEB_HOME" ALLBERT_HOME_DIR="$V060_WEB_HOME" MIX_ENV=dev \
  mix allbert.ecto.migrate --quiet > "$V060_EVIDENCE_DIR/s4-web-migrate.txt"
ALLBERT_HOME="$V060_WEB_HOME" ALLBERT_HOME_DIR="$V060_WEB_HOME" MIX_ENV=dev \
  PORT="$V060_WEB_PORT" mix phx.server > "$V060_EVIDENCE_DIR/s4-web-server.log" 2>&1 &
V060_WEB_PID="$!"
printf '%s\n' "$V060_WEB_PID" > "$V060_EVIDENCE_DIR/s4-web-server.pid"
sleep 8

# Resolve every flag-gated /preview/* IA route to a rendered placeholder screen
# (HTTP 200). The route set comes from the M2 IA preview-route manifest through
# AllbertAssistWeb.Skeleton.RouteManifest, not from a hand-maintained shell list.
while IFS= read -r route; do
  test -n "$route"
  slug="$(printf '%s' "$route" | sed 's#^/##' | tr '/' '-')"
  code="$(curl -s -o "$V060_EVIDENCE_DIR/s4-route-$slug.html" \
    -w '%{http_code}' "$V060_BASE_URL$route")"
  echo "route=$route http=$code"
  test "$code" = "200"
done < "$V060_EVIDENCE_DIR/s4-preview-routes.txt"

# Leave the server running: S4.5 reuses this same V060_WEB_HOME/server for the
# browser pass and tears it down at the end.
printf 'PASS S4 walking-skeleton routes resolve through the catalog/shell\n'
```
Expected output:
```text
route=/preview http=200
route=/preview/... http=200
PASS S4 walking-skeleton routes resolve through the catalog/shell
```
Expected evidence: every flag-gated `/preview/*` IA route resolves (HTTP 200) to a
placeholder screen rendered through `Layouts.operator_shell/1` + known catalog
components. This step proves only that the routes resolve; the
nav-shell-renders, IA-is-navigable, and no-authority invariants the nav shell is
meant to expose are **verified in the browser in S4.5**, not asserted from a curl
200 here. Keep `s4-preview-routes.txt`, `s4-route-*.html`, and `s4-web-server.log`
as sanitized evidence. If `AllbertAssistWeb.Skeleton.RouteManifest.preview_paths/0`
is absent or empty, stop: M6 has not supplied the machine-readable route manifest
that the gate depends on. The `:preview_routes` compile-env flag is enabled in
dev/test for v0.60 validation and remains off in production.

### S4.5 - Walking-Skeleton Browser Pass

Drive each flag-gated `/preview/*` IA route listed in
`$V060_EVIDENCE_DIR/s4-preview-routes.txt` in Chrome/browser control against the
already-running `phx.server` from S4 (`$V060_BASE_URL`, i.e.
`http://127.0.0.1:$V060_WEB_PORT`). This is the pass that actually *renders* the
walking skeleton — S4 only proved the routes return HTTP 200, and S5 only greps a
test's printed booleans. Here we confirm the nav shell renders, the placeholder
screens render through `Layouts.operator_shell/1` + the catalog, the IA is
navigable, no live data or authority leaks, and a11y/console hold under live render.

Run it one of two ways:

- **Automated evidence** — via the Chrome/browser-control tools available in the
  validation environment: `navigate` (drive to each `$V060_BASE_URL$route`),
  `read_page` / `find` (assert nav-shell structure and click nav links),
  `read_console_messages` (assert zero console errors), and `computer` (capture
  screenshots). Capture one screenshot per route plus one per a11y mode, and log a
  `PASS <route> <slug> Shot: <path>` line per invariant to
  `$V060_EVIDENCE_DIR/s4_5-browser-notes.md`.
- **Guided manual** — the operator validates the same invariants live in Chrome
  and still writes the same positive `PASS <route> <invariant> Shot: <path>` notes.
  A failure is recorded as `FAIL <route> <invariant> ...` and stops closeout.

Reuse the disposable `V060_WEB_HOME`/server and the `V060_BASE_URL` /
`V060_SHOTS` / `V060_EVIDENCE_DIR` env from S4. Initialize the notes file:

```sh
set -euo pipefail
: > "$V060_EVIDENCE_DIR/s4_5-browser-notes.md"
```

Iterate over the same manifest-derived routes from
`$V060_EVIDENCE_DIR/s4-preview-routes.txt`. For each `/preview/*` route, navigate
in Chrome and assert:

1. **Nav shell renders.** Expected: the global navigation shell is present, the
   active route is marked current (e.g. `aria-current`), and links to the other IA
   routes exist in the nav — the real check S4 only claims in prose. Shot:
   `$V060_SHOTS/NN-<route>.png`. The PASS note must emit the slug `nav-shell` (i.e.
   `PASS <route> nav-shell Shot: ...`) so the teardown grep succeeds.
2. **Placeholder screen renders through the shell + catalog.** Expected: the screen
   renders through `Layouts.operator_shell/1` + known catalog atoms — no stock-
   Phoenix scaffold, no raw error page, no unknown-component `Placeholder`
   fallback, no palette/raw-`btn` drift. Shot: `$V060_SHOTS/NN-<route>.png`. The
   PASS note must emit the slug `catalog-render` so the teardown grep succeeds.
3. **IA is navigable.** Expected: clicking a nav link to another IA route changes the
   route (`find` the link, click it) and the new placeholder screen renders — proves
   navigation, not just a per-URL HTTP 200. Shot:
   `$V060_SHOTS/NN-<route>-nav.png` (optional nav-traversal recording). The PASS note
   must emit the slug `nav-traversal` so the teardown grep succeeds.
4. **Route-specific composition depth.** Expected: each route renders a visible
   composition zone with `data-skeleton-composition-route="<route_id>"`,
   `data-skeleton-composition-zone="<active_key>"`, and
   `data-skeleton-composition-component="<manifest_component>"`, where the
   component value is declared by that route's manifest row. Onboarding and
   settings also render safe child catalog nodes from their manifest rows so the
   browser check can see more than a single zone wrapper; unsafe live panel atoms
   are represented by the inert `skeleton_placeholder` catalog component carrying
   the manifest component metadata. Shot:
   `$V060_SHOTS/NN-<route>.png`. The PASS note must emit the slug
   `composition-depth` so the teardown grep succeeds.
5. **No live data / no authority.** Expected: placeholder copy only — no business
   state, no effectful affordance (no Approve / Promote / Send control), consistent
   with `no-new-authority-design-only-001`. Shot: `$V060_SHOTS/NN-<route>.png`. The
   PASS note must emit the slug `no-authority` so the teardown grep succeeds.

**A11y / console smoke** (this is the real a11y check; S5's string-grep is no longer
presented as the validation): for each route, `read_console_messages` shows zero
errors — emit a PASS note with the slug `console-errors=0` so the teardown grep
succeeds. Verify keyboard focus by tabbing through the active nav link, sibling
nav links, and the main placeholder content; emit a PASS note with the slug
`keyboard-focus`.

For deterministic disposable-Home validation, switch visual modes through
Settings Central before reloading the route. These writes are validation-only
settings in `V060_WEB_HOME`; they are not new v0.60 settings. Keep the S4
`phx.server` process running while these commands run: each `mix run` uses the
same `ALLBERT_HOME` / `ALLBERT_HOME_DIR` and writes through Settings Central to
`$V060_WEB_HOME/settings.yml`. After each write, reload the current preview route
in Chrome before checking the root attributes.

```sh
# dark-mode token resolution
ALLBERT_HOME="$V060_WEB_HOME" ALLBERT_HOME_DIR="$V060_WEB_HOME" MIX_ENV=dev \
  mix run -e 'alias AllbertAssist.Settings; Settings.put("workspace.theme.mode", "dark", %{audit?: false}); Settings.put("workspace.accessibility.high_contrast", false, %{audit?: false}); Settings.put("workspace.accessibility.reduce_motion", false, %{audit?: false})'

# high-contrast token resolution
ALLBERT_HOME="$V060_WEB_HOME" ALLBERT_HOME_DIR="$V060_WEB_HOME" MIX_ENV=dev \
  mix run -e 'alias AllbertAssist.Settings; Settings.put("workspace.theme.mode", "light", %{audit?: false}); Settings.put("workspace.accessibility.high_contrast", true, %{audit?: false}); Settings.put("workspace.accessibility.reduce_motion", false, %{audit?: false})'

# reduced-motion token resolution
ALLBERT_HOME="$V060_WEB_HOME" ALLBERT_HOME_DIR="$V060_WEB_HOME" MIX_ENV=dev \
  mix run -e 'alias AllbertAssist.Settings; Settings.put("workspace.theme.mode", "system", %{audit?: false}); Settings.put("workspace.accessibility.high_contrast", false, %{audit?: false}); Settings.put("workspace.accessibility.reduce_motion", true, %{audit?: false})'
```

After each global mode switch, reload all 9 preview routes and confirm the
relevant root attributes and visual behavior on each route:
`html[data-theme="dark"]` for dark, `body[data-high-contrast="true"]` for high
contrast, and `body[data-reduce-motion="true"]` for reduced motion. Emit per-route
PASS notes with the slugs `dark`, `high-contrast`, and `reduced-motion` so the
teardown greps succeed. Shots: `$V060_SHOTS/NN-<route>-dark.png`,
`$V060_SHOTS/NN-<route>-high-contrast.png`,
`$V060_SHOTS/NN-<route>-reduced-motion.png`.

Each invariant gets an `Expected:` clause and a `Shot:` line as above. After the
browser check, verify evidence and tear down the server:

```sh
set -euo pipefail
while IFS= read -r route; do
  test -n "$route"
  for slug in nav-shell catalog-render nav-traversal no-authority \
              composition-depth console-errors=0 keyboard-focus dark high-contrast reduced-motion; do
    rg "^PASS ${route} ${slug} " "$V060_EVIDENCE_DIR/s4_5-browser-notes.md"
  done
done < "$V060_EVIDENCE_DIR/s4-preview-routes.txt"
! rg '^FAIL ' "$V060_EVIDENCE_DIR/s4_5-browser-notes.md"

kill "$V060_WEB_PID"
printf 'PASS S4.5 walking-skeleton browser pass: nav shell, navigable IA, a11y/console clean\n'
```

Expected: every `/preview/*` route renders its nav shell + catalog placeholder
through `operator_shell`; clicking a nav link navigates to a sibling route that
renders; each route has a route-specific composition zone; keyboard focus reaches
nav and main placeholder content; no placeholder exposes live data or an
effectful affordance; the console is error-free per route; dark mode /
high-contrast / reduced-motion apply. For
automated or guided-manual evidence the notes file carries one positive `PASS`
line per route and invariant, and no `FAIL` lines. Closeout caveat: durable
redacted evidence (the `s4_5-browser-notes.md` + screenshots) is retained outside
the repo — **do not commit raw screenshots**.

### S5 - Nav / A11y Smoke + Design-Only Invariant
```sh
set -euo pipefail
MIX_ENV=test mix test apps/allbert_assist_web/test --only v060_walking_skeleton \
  > "$V060_EVIDENCE_DIR/s5-skeleton.txt" 2>&1
rg '0 failures' "$V060_EVIDENCE_DIR/s5-skeleton.txt"
rg 'walking-skeleton-routes-resolve-001 status=pass route_count=9 composition_depth=true route_specific_components=9 composition_child_routes=9' \
  "$V060_EVIDENCE_DIR/s5-skeleton.txt"
rg 'walking-skeleton-nav-shell-001 status=pass' "$V060_EVIDENCE_DIR/s5-skeleton.txt"
# Eval-row check only: this asserts the a11y eval row registered and passed. The
# real a11y validation (keyboard focus works, contrast applies, motion collapses,
# console is error-free) is
# the S4.5 browser-observed pass, not this grep of the test's printed booleans.
rg 'walking-skeleton-a11y-smoke-001 status=pass keyboard_focus=true high_contrast=true reduced_motion=true' \
  "$V060_EVIDENCE_DIR/s5-skeleton.txt"
rg 'no-new-authority-design-only-001 status=pass live_data=false authority=none settings_keys=0' \
  "$V060_EVIDENCE_DIR/s5-skeleton.txt"

# Placeholder screens render no model-/data-generated markup and no effectful affordance.
! rg -i 'model.generated|data.generated' "$V060_EVIDENCE_DIR"/s4-route-*.html
printf 'PASS S5 nav/a11y smoke clean and design-only invariant held\n'
```
Expected output:
```text
0 failures
walking-skeleton-routes-resolve-001 status=pass route_count=9 composition_depth=true route_specific_components=9 composition_child_routes=9
walking-skeleton-nav-shell-001 status=pass
walking-skeleton-a11y-smoke-001 status=pass keyboard_focus=true high_contrast=true reduced_motion=true
no-new-authority-design-only-001 status=pass live_data=false authority=none settings_keys=0
PASS S5 nav/a11y smoke clean and design-only invariant held
```
Expected evidence: the `walking-skeleton-a11y-smoke-001` and
`no-new-authority-design-only-001` eval rows registered and passed, and placeholder
screens read no business state, render no effectful affordance, grant no authority,
add no Settings key, include a route-specific composition zone per route, and
render deeper child composition for every route through shared RouteManifest
metadata. The
a11y guarantee itself — keyboard focus, high-contrast, and reduced-motion holding
across every skeleton screen under live render — is observed in **S4.5's browser
pass**; this step confirms the eval rows are green, not that the string-grep is
the a11y validation.

### S6 - Gate And Handoff Drift
```sh
set -euo pipefail
MIX_ENV=test mix allbert.test release.v060 > "$V060_EVIDENCE_DIR/s6-release.txt"
rg '==> release\.v060 migrate' "$V060_EVIDENCE_DIR/s6-release.txt"
rg '==> release\.v060 format_check' "$V060_EVIDENCE_DIR/s6-release.txt"
rg '==> release\.v060 compile_warnings_as_errors' "$V060_EVIDENCE_DIR/s6-release.txt"
rg '==> release\.v060 credo_strict' "$V060_EVIDENCE_DIR/s6-release.txt"
rg '==> release\.v060 dialyzer' "$V060_EVIDENCE_DIR/s6-release.txt"
rg 'Total errors: 0' "$V060_EVIDENCE_DIR/s6-release.txt"
rg 'Skipped: 0' "$V060_EVIDENCE_DIR/s6-release.txt"
rg 'Unnecessary Skips: 0' "$V060_EVIDENCE_DIR/s6-release.txt"
rg '==> release\.v060 walking_skeleton_smoke' "$V060_EVIDENCE_DIR/s6-release.txt"
rg '==> release\.v060 v060_security_sweep' "$V060_EVIDENCE_DIR/s6-release.txt"
rg '==> release\.v060 docs_gate' "$V060_EVIDENCE_DIR/s6-release.txt"
rg 'release\.v060 evidence: ' "$V060_EVIDENCE_DIR/s6-release.txt"

V060_RELEASE_EVIDENCE="$(sed -n 's/^.*release\.v060 evidence: //p' "$V060_EVIDENCE_DIR/s6-release.txt" | tail -1)"
test -n "$V060_RELEASE_EVIDENCE"
test -f "$V060_RELEASE_EVIDENCE"
rg '"id": "walking_skeleton_smoke"' "$V060_RELEASE_EVIDENCE"
rg '"id": "v060_security_sweep"' "$V060_RELEASE_EVIDENCE"
rg '"id": "dialyzer"' "$V060_RELEASE_EVIDENCE"

# Handoff drift-check: v0.61 / v0.62 / v0.63 consumers named with concrete artifacts.
MIX_ENV=test mix test apps/allbert_assist/test/security/v060_sweep_eval_test.exs \
  --only rc_design_handoff > "$V060_EVIDENCE_DIR/s6-handoff.txt" 2>&1
rg 'rc-design-handoff-no-drift-001 consumer=v0\.61 output=information-architecture\+design-system-gap-analysis\+first-model-path status=no-drift' \
  "$V060_EVIDENCE_DIR/s6-handoff.txt"
rg 'rc-design-handoff-no-drift-001 consumer=v0\.62 output=entry-point-cli-ux\+first-model-path-packaging status=no-drift' \
  "$V060_EVIDENCE_DIR/s6-handoff.txt"
rg 'rc-design-handoff-no-drift-001 consumer=v0\.63 output=onboarding-flow\+persona-model\+first-model-path status=no-drift' \
  "$V060_EVIDENCE_DIR/s6-handoff.txt"
rg 'rc-design-handoff-no-drift-001 no-drift consumers=v0\.61,v0\.62,v0\.63' \
  "$V060_EVIDENCE_DIR/s6-handoff.txt"

# Cross-doc coherence: canonical first-model states and persona/FMP QuickStart boundary.
MIX_ENV=test mix test apps/allbert_assist/test/security/v060_sweep_eval_test.exs \
  --only v060_cross_doc_coherence > "$V060_EVIDENCE_DIR/s6-cross-doc-coherence.txt" 2>&1
rg 'first-model-persona-cross-doc-coherence-001 status=pass states=local_ready,runtime_missing,runtime_unhealthy,model_missing,below_hardware_floor,byok_ready,blocked persona_model_map=post-first-chat quickstart_extra_model_pulls=none daily_use=persona-uniform' \
  "$V060_EVIDENCE_DIR/s6-cross-doc-coherence.txt"

# Persona seed pre-audit: v0.63 starts from known safe-write keys, no v0.60 seed writes.
MIX_ENV=test mix test apps/allbert_assist/test/security/v060_sweep_eval_test.exs \
  --only v060_persona_seed_preaudit > "$V060_EVIDENCE_DIR/s6-persona-seed-preaudit.txt" 2>&1
rg 'persona-seed-preaudit-001 status=pass existing_safe_write_keys=17 exact_values=v0\.63 no_v060_seed_files=true' \
  "$V060_EVIDENCE_DIR/s6-persona-seed-preaudit.txt"
printf 'PASS S6 release.v060 gate green and design handoff/persona pre-audit drift-free\n'
```
Expected output:
```text
==> release.v060 migrate
==> release.v060 format_check
==> release.v060 compile_warnings_as_errors
==> release.v060 credo_strict
==> release.v060 dialyzer
Total errors: 0
Skipped: 0
Unnecessary Skips: 0
==> release.v060 walking_skeleton_smoke
==> release.v060 v060_security_sweep
==> release.v060 docs_gate
release.v060 evidence: <path>
rc-design-handoff-no-drift-001 consumer=v0.61 output=information-architecture+design-system-gap-analysis+first-model-path status=no-drift
rc-design-handoff-no-drift-001 consumer=v0.62 output=entry-point-cli-ux+first-model-path-packaging status=no-drift
rc-design-handoff-no-drift-001 consumer=v0.63 output=onboarding-flow+persona-model+first-model-path status=no-drift
rc-design-handoff-no-drift-001 no-drift consumers=v0.61,v0.62,v0.63
first-model-persona-cross-doc-coherence-001 status=pass states=local_ready,runtime_missing,runtime_unhealthy,model_missing,below_hardware_floor,byok_ready,blocked persona_model_map=post-first-chat quickstart_extra_model_pulls=none daily_use=persona-uniform
persona-seed-preaudit-001 status=pass existing_safe_write_keys=17 exact_values=v0.63 no_v060_seed_files=true
PASS S6 release.v060 gate green and design handoff/persona pre-audit drift-free
```
Expected evidence: `release.v060` exits 0, prints an evidence JSON path under the
disposable release Home, embeds all eight migrate / format / compile / Credo /
Dialyzer / walking-skeleton / v0.60 security-sweep / docs lane steps, Dialyzer
reports zero errors / skipped warnings / unnecessary skips from inside the release
lane, and the handoff drift-check enumerates the v0.61
/ v0.62 / v0.63 consumers with their concrete consumed design artifacts and pulls
no build scope into v0.60. The cross-doc coherence check confirms the canonical
first-model-state list, the persona / First-Model Path QuickStart boundary, and
the daily-use posture that personas remain setup seeds rather than runtime modes.
The persona pre-audit confirms candidate seeds are anchored to existing Settings
Central safe-write keys, exact values are deferred to v0.63, and v0.60 adds no
persona seed files or Settings writes. The design-artifact, ADR-acceptance,
cross-doc coherence, and persona pre-audit assertions are rows inside
`v060_security_sweep`, not separate release-runner steps. Record a sanitized
closeout note.

### Pass Criteria
- **Product-experience spec (M1):** the install → first-run → onboard →
  first-value → daily-use journey is written once for the technical-prosumer
  audience, names the first-value moment, and maps each stage to its owning
  downstream release.
- **Information architecture (M2):** the sitemap, screen inventory, navigation
  model, and workspace/screen composition are designed and accepted as ADR 0077
  (Accepted (v0.60)); the layout redesign ADR 0074 deferred is now designed.
- **First-model path (M3):** ADR 0078 (Accepted (v0.60)) decides across assisted-
  local / managed-hosted / honest-BYOK, defines the QuickStart "first useful chat"
  contract, and lists the v0.62 packaging implications.
- **Onboarding + persona design (M4):** the two-track wizard UX and the persona
  model (ADR 0075 content) are a complete v0.63 design input with no v0.60 build.
  M8.1 reconciles the persona model with the First-Model Path: persona
  `model_purpose_map` entries are post-first-chat seed recommendations, not
  QuickStart model-pull requirements. M9.2 pre-audits persona seed candidates
  against existing Settings Central safe-write keys without adding v0.60 seed
  files, Settings writes, or runtime persona behavior. M9.3 clarifies that
  personas are setup seeds only and daily-use remains persona-uniform unless the
  operator later changes Settings, configures a provider/channel, or confirms an
  action through the normal authority path.
- **Entry-point + CLI UX design (M5):** the command taxonomy, first-run detection,
  and wizard launch are a complete v0.62 design input with no v0.60 build.
- **Walking skeleton (M6):** every IA route resolves through the catalog/shell to a
  placeholder screen; every route renders a route-specific composition zone; the
  nav shell exposes the IA model; keyboard focus / high-contrast / reduced-motion
  hold; no live data; no authority; no model-/data-generated markup.
- **Design-system gap analysis (M7):** token / component / pattern gaps are
  enumerated, mapped to composition rules / skeleton screens, as the ADR 0074 v0.61
  input.
- **Gate / handoff (M8):** `mix allbert.test release.v060` is green and
  deterministic; durable redacted evidence is retained outside the repo; the
  handoff enumerates the v0.61 / v0.62 / v0.63 consumers; no build scope leaked
  into v0.60; the cross-doc coherence row holds the canonical first-model-state
  list and the M8.1 persona / First-Model Path boundary; the embedded Dialyzer
  step has zero errors / skipped warnings / unnecessary skips; Security Central
  is unchanged.

## Docs Closeout Checklist
- ADR 0077 Accepted (v0.60) — the unified journey + IA / navigation / composition
  redesign (the layout redesign ADR 0074 deferred).
- ADR 0078 Accepted (v0.60) — the First-Model Path + QuickStart contract + v0.62
  packaging implications.
- `docs/design/product-experience-spec.md`, `information-architecture.md`,
  `first-model-path.md`, `onboarding-flow.md`, `persona-model.md`,
  `entry-point-cli-ux.md`, and `design-system-gap-analysis.md` published.
- The walking skeleton is navigable, placeholder-only, and a11y-clean; it is the
  documented v0.61 build scaffold.
- The design handoff enumerates the v0.61 / v0.62 / v0.63 consumers with their
  concrete consumed design artifacts.
- The persona seed pre-audit names existing Settings Central safe-write keys and
  defers exact seeded values to v0.63 without adding v0.60 Settings writes.
- The first-model-state list remains the v0.60 design/test handoff contract; a
  downstream runtime source of truth belongs to v0.62/v0.63 and must not invent
  divergent labels.
- Roadmap, docs index, agent-context-map, and downstream v0.61-v0.64 docs agree on
  the v0.60 boundary (design + scaffolding only; presentation → v0.61, packaging →
  v0.62, onboarding/personas → v0.63, product RC → v0.64), while AGENTS.md stays
  compact and avoids release-history drift.
