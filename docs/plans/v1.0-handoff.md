# v1.0 Handoff — Acceptance Matrix

Status: current (authored at v0.66 M11). v0.66 is the integrated product release
candidate; v1.0 (roadmap item 67) is the stability release + tiered public-contract
freeze + non-developer product launch. This note is the handoff: the frozen set of
acceptance-matrix inputs v1.0 inherits from v0.66, and where each is proven.

v0.66 uses two-layer verification (`docs/plans/v0.66-plan.md` Locked Decision 1): a
deterministic `mix allbert.test release.v066` gate for contract/routing/boundary
invariants, plus operator-attested evidence (`docs/validation/v0.66/`) for the
install/browser/model/cross-platform claims the gate cannot run in a checkout.

## Acceptance matrix (17 inputs)

Each row is `[gate]` (proved by `release.v066`), `[attested]` (recorded in
`docs/validation/v0.66/`), or both. v1.0 must not freeze with any row regressed.

| # | Acceptance input | Layer | Where proven |
|---|---|---|---|
| 1 | Packaged install (curl) reaches a toolchain-free boot | [attested] | `linux_rehearsal.sh` — CI PASS (ubuntu-22.04) |
| 2 | Packaged install (Homebrew) reaches a toolchain-free boot | [gate]+[attested] | `artifact_smoke.sh` — macOS PASS |
| 3 | Cross-platform: macOS | [attested] | `m2-macos-artifact-smoke.log` — PASS |
| 4 | Cross-platform: Linux | [attested] | CI linux-rehearsal + linux-x64/arm64 builds PASS |
| 5 | Cross-platform: Windows/WSL2 | [attested] | PENDING-OPERATOR |
| 6 | Persistent service start + `/health` green | [attested] | `m2-macos-artifact-smoke.log` — PASS |
| 7 | Consumer-default QuickStart, keyless local model | [gate]+[attested] | `product-rc-consumer-default-oneclick-model-no-key-first-chat-001`; `m7-first-chat-keyless-local.log` |
| 8 | First useful chat with no API key | [attested] | `m7-first-chat-keyless-local.log` — PASS |
| 9 | Conversational routing, no mis-route to disabled/demo | [gate] | `product-rc-conversational-routing-no-misroute-001` |
| 10 | Local files/notes launch path bounded | [gate]+[attested] | `product-rc-local-files-notes-memory-policy-bounded-001`; `v066_product_rc.sh` core |
| 11 | Reviewed agent memory recall (`:kept`-only) | [gate]+[attested] | `v066_local_knowledge`; [model] recall PENDING-OPERATOR |
| 12 | Web workspace non-developer usability (item-11) | [attested] | `item11-usability-audit.md` — PASS, 1 v1.0 caveat |
| 13 | Grouped CLI/TUI operator flows, no raw `mix` | [gate]+[attested] | `product-rc-cli-tui-no-mix-needed-001`; `m4-packaged-cli-smoke.log` |
| 14 | Advanced surfaces no regression (channels/MCP/browser/plan-build) | [gate]+[attested] | `product-rc-advanced-surfaces-no-regression-001`; live PENDING-OPERATOR |
| 15 | Export/import + cross-version upgrade | [gate]+[attested] | `product-rc-export-import-upgrade-001`; real upgrade PENDING-OPERATOR |
| 16 | Uninstall preserves Home | [attested] | PENDING-OPERATOR (contract in `InstallPathTest`) |
| 17 | No raw secret in logs/output/evidence + `release.v066` green | [gate] | `product-rc-evidence-secret-scan-001`; `release.v066` |

## Contracts v1.0 freezes (roadmap item 67)

- Tier 1: Runtime, Actions/permissions, Plugin, App, Settings Central schema shape,
  Allbert Home layout, Channel adapter boundary, Resource Access URI/grants.
- Tier 2: SurfaceProvider, Surface DSL (additive-only carve-out), workspace
  canvas/ephemeral substrate (minus single-consumer components).

## Open items carried to v1.0

The `PENDING-OPERATOR` rows above (Linux/WSL2 installs, real cross-version upgrade,
host uninstall, live advanced-surface exercise, `[model]` recall-in-chat) are the
operator-attested evidence v1.0 must complete on real hosts before the freeze. The
one recorded usability caveat (ensure QuickStart always reaches the direct-answer
enable step) is tracked in `docs/validation/v0.66/item11-usability-audit.md`.

Canonical current-state sources: [CHANGELOG](../../CHANGELOG.md) and
[roadmap](roadmap.md) (`v1.0` = item 67).
