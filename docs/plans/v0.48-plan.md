# Allbert v0.48 Voice Modality And Provider Capabilities

Request flow: `docs/plans/v0.48-request-flow.md`

ADRs:
- `docs/adr/0051-provider-capability-preferences.md` (planned v0.48 M1).
- `docs/adr/0042-audio-image-and-media-resource-classes.md` (audio
  amendments accepted by M4).
- `docs/adr/0047-provider-doctor-contract.md` (v0.48 voice-doctor amendment).

Status: planned; implementation-ready after this doc-only readiness pass.

Source: current roadmap and vision, plus the v0.39 provider-control substrate.
The older archive citation is intentionally not authority for v0.48 because
the roadmap has since been resequenced.

## Purpose

v0.48 ships the first non-text modality without creating a second provider
system. Voice is the forcing function for a general provider capability and
operator preference layer:

1. providers and model profiles become capability-aware;
2. operators can set a global primary provider/profile and ranked
   per-capability preferences;
3. STT/TTS use the same Settings Central, doctor, secrets, and provider
   routing substrate as text generation;
4. audio resources and voice actions remain policy-bounded through Resource
   Access, Security Central, confirmations, traces, and audits.

v0.49 Vision And Image Generation consumes the provider capability substrate
landed here; it does not get a separate image-provider framework.

## Readiness Sweep

Status: complete after this doc pass.

- **Vision fit.** Voice remains an operator surface over the runtime. It does
  not create a voice runtime, a channel-owned STT/TTS path, or a microphone
  authority outside Resource Access.
- **Roadmap fit.** v0.48 follows v0.47b self-improvement and precedes v0.49
  vision. It lands the provider capability/preference substrate once, then
  voice proves it through STT/TTS.
- **Past-plan fit.** v0.39 provider control supplies the provider catalog,
  Settings Central profile resolution, onboarding, and doctor pattern. ADR
  0047 supplies the redacted doctor envelope. v0.43 browser supplies
  resource URI, operation-class, permission, trace, and workspace validation
  precedents. v0.45 marketplace and v0.47 self-improvement show the passive
  surface plus deterministic release-gate style.
- **Future-plan fit.** v0.49 adds `vision_input`, `image_generation`, and
  media rendering on the v0.48 capability layer. v0.50 channel plugins may
  forward voice/media references but do not own provider authority.
- **ADR fit.** ADR 0051 locks the provider capability/preference contract.
  ADR 0042 accepts audio-specific amendments for v0.48 while leaving image
  and screen details proposed for v0.49. ADR 0047 is amended for
  capability-aware doctors.

## Locked Decisions

These choices are binding for v0.48. Changing any value requires an explicit
re-decision in this plan and in the request-flow document before code lands.

| # | Decision | Chosen value | Trade-off if changed |
|---|---|---|---|
| 1 | Provider substrate | Reuse the existing provider/model profile framework. Voice providers are still providers. | A parallel voice-provider stack would duplicate secrets, doctor, onboarding, settings, traces, and future vision work. |
| 2 | Generalization | Make provider profiles capability-aware rather than adding voice-only STT/TTS keys. | Voice-only keys are faster, but v0.49 would repeat the same migration for images. |
| 3 | Preference shape | Use a global primary plus ranked preferences per task/capability. | A single profile per capability cannot degrade gracefully when a provider is disabled or not doctored. |
| 4 | Capability authority | Curated catalog defaults are seed data; doctor output may report availability but never grants permission. | Pure auto-detection is provider-fragile; pure catalog metadata cannot reflect live availability. |
| 5 | Compatibility | Keep `intent.model_profile` and `intent.direct_answer_model_profile` as compatibility aliases into the new preference layer. | Immediate migration would churn existing operator settings and focused tests. |
| 6 | Voice default posture | Local/offline or no-cloud-by-default where available; remote STT/TTS only after onboarding/settings choose it. | Cloud-key-required defaults would violate local-first expectations for first-run voice experiments. |
| 7 | CLI voice | `mix allbert.ask --voice AUDIO_FILE` transcribes a bounded audio file/fixture. CLI does not capture live microphone audio. | Live mic in CLI is platform-specific and hard to test deterministically. |
| 8 | Workspace mic | Live microphone capture is workspace-only and requires explicit per-session confirmation. | Sharing the CLI file path with live capture would blur authority and retention policy. |
| 9 | Cost visibility | v0.48 shows display-only provider/cost/usage metadata in action output and traces. Unified spend dashboards and enforcement remain parked. | A cost subsystem would expand v0.48 beyond the modality substrate. |
| 10 | Test lane | Deterministic fake STT/TTS providers are the release lane. Real provider smokes are optional and out of release authority. | Real provider tests are expensive, credentialed, and non-deterministic. |

## Capability And Preference Contract

v0.48 adds the following capability vocabulary:

```text
text_generation
speech_to_text
text_to_speech
vision_input
image_generation
video_input
streaming
embeddings
tool_use
```

The catalog and Settings Central model profile shape becomes:

```yaml
model_profiles:
  local:
    provider: local_ollama
    model: llama3.2:3b
    capabilities: [text_generation]
  voice_stt_fake:
    provider: fake_voice
    model: fixture-stt
    capabilities: [speech_to_text]
  voice_tts_fake:
    provider: fake_voice
    model: fixture-tts
    capabilities: [text_to_speech]

model_preferences:
  primary: local
  tasks:
    coding: [coding_local, coding, capable, local]
    direct_answer: [local, fast]
  capabilities:
    text_generation: [local, fast]
    speech_to_text: [voice_stt_fake]
    text_to_speech: [voice_tts_fake]
```

Selection is via a shared resolver, e.g. `Models.for/2`:

1. resolve task preference first when the caller asks for a task such as
   `:coding` or `:direct_answer`;
2. otherwise resolve a capability preference such as `:speech_to_text`;
3. skip disabled, missing, or incapable profiles;
4. fall back through the ranked list, then to `model_preferences.primary`
   only when the primary advertises the requested capability;
5. return diagnostics when no capable profile is available.

The resolver grants no authority. Action permissions, Resource Access, and
provider secrets remain separate boundaries.

## Settings

New safe-write settings:

- `model_preferences.primary`
- `model_preferences.tasks.<task>`
- `model_preferences.capabilities.<capability>`
- `model_profiles.*.capabilities`
- provider catalog additions for voice-capable providers and fake test
  providers
- `voice.enabled`
- `voice.audio.max_bytes`
- `voice.audio.max_duration_ms`
- `voice.audio.retention`
- `voice.trace.redact_audio`

Compatibility:

- reads of `intent.model_profile` map to `model_preferences.primary`;
- reads of `intent.direct_answer_model_profile` map to
  `model_preferences.tasks.direct_answer`;
- writes through existing actions continue to work until a later migration
  removes the legacy keys.

## Provider Doctor

`doctor_voice_provider` reuses ADR 0047's canonical redacted envelope. It adds
optional capability fields only:

- `capabilities_reported`
- `speech_to_text_supported`
- `text_to_speech_supported`
- `sample_rates_supported`
- `audio_formats_supported`
- `provider_usage_metadata_available`

The doctor is read-only and may probe only the configured provider/profile
host. It never accepts arbitrary URLs, raw audio paths, or model-output-chosen
probe targets.

## Audio Resource And Authority Model

v0.48 accepts the audio slice of ADR 0042:

- `mic://capture/<id>` identifies workspace microphone captures.
- CLI voice input uses `file://` Resource Access over a bounded local audio
  fixture/file, not live microphone capture.
- Audio content is default-off for retention and redacted from traces by
  default.
- Traces may record bounded metadata: resource URI, duration, byte size,
  MIME type, provider profile, transcript hash, and redaction status.
- Raw audio never appears in traces, audits, diagnostics, LiveView assigns, or
  CLI output.

New permission classes:

- `:microphone_capture` - floor `:needs_confirmation`
- `:voice_transcribe` - floor `:needs_confirmation` for remote providers,
  `:allowed` for fake/local fixture providers
- `:voice_synthesize` - floor `:needs_confirmation` for remote providers,
  `:allowed` for fake/local fixture providers

New operation classes and origins:

- origin kind `:audio_capture`
- origin kind `:audio_file`
- operation `:microphone_capture`
- operation `:voice_transcribe`
- operation `:voice_synthesize`

## Operator-Visible Win

After onboarding, an operator can say:

- "use local for most things";
- "use qwen coder for coding";
- "use this STT provider for voice transcription";
- "use this TTS provider for spoken output".

Then:

- `mix allbert.ask --voice ./fixtures/hello.wav` transcribes a file and sends
  the transcript through the normal runtime input path;
- workspace microphone capture creates a confirmation, records redacted audio
  metadata, then transcribes approved audio;
- TTS output is a registered action with visible provider/cost metadata;
- Telegram voice notes become transcribed channel input without giving the
  Telegram adapter provider authority.

## Planned Interfaces

- `AllbertAssist.Settings.ProviderCatalog` gains capability metadata.
- New provider preference resolver under Settings, e.g.
  `AllbertAssist.Settings.Models`.
- `doctor_voice_provider` action.
- `mix allbert.ask --voice AUDIO_FILE`.
- `transcribe_voice` action.
- `synthesize_voice` action.
- Workspace microphone surface/event path.
- `mic://capture/<id>` resource URI.
- Telegram voice-note ingestion path.
- `mix allbert.test release.v048`.

## Security Posture

- Voice input is input modality only; it does not change action authority.
- Provider/model metadata never grants permission or stores secrets.
- Microphone capture is confirmed per session.
- CLI voice accepts only explicit bounded audio files.
- Remote STT/TTS calls use provider settings and secrets through Settings
  Central.
- Audio retention is default-off; redacted metadata is the trace artifact.
- TTS is effectful and provider-backed; it reports display-only usage/cost
  metadata but does not enforce budgets in v0.48.
- Channel adapters never own STT/TTS authority.

## Non-Goals

- No always-on wake word in 1.0.
- No hidden cloud upload of audio.
- No durable audio retention by default.
- No channel permission bypass through voice.
- No Discord voice in v0.48.
- No unified cost dashboard or spend enforcement.
- No image, screenshot, or image-generation implementation; those are v0.49.
- No removal of legacy `intent.*model_profile` settings in v0.48.

## Development-Lane Annotations

Per ADR 0049. The deterministic `release.v048` lane uses fixture audio, fake
STT/TTS providers, and disposable `ALLBERT_HOME`; no external provider smoke
is required for release authority.

| M | Parallel workstreams | Serial barriers | Gate evidence + rejoin |
|---|---|---|---|
| M1 | ADR 0051 acceptance; catalog capability fields; settings schema defaults | Settings schema and catalog validation | focused provider catalog/settings tests; docs reconciled before M2 |
| M2 | preference resolver; compatibility aliases; onboarding preference flow | resolver precedence and diagnostics | focused resolver/onboarding tests; legacy `intent.*` tests still green |
| M3 | ADR 0047 amendment; voice doctor action; fake provider doctor | provider doctor envelope and redaction | focused doctor tests; no raw URLs/secrets/audio paths |
| M4 | ADR 0042 audio amendments; `mic://`; operation/permission classes; redaction policy | permission floors and trace redaction | resource URI, policy, operation-class, redaction tests |
| M5 | CLI `--voice AUDIO_FILE`; fake STT transcription action | CLI file bounds and Resource Access | CLI/action fixture tests; no live mic path |
| M6 | workspace mic surface; confirmation flow; redacted capture metadata | LiveView event and confirmation state | workspace focused tests plus browser/manual smoke if UI changes |
| M7 | TTS action; display-only usage/cost metadata; Telegram voice-note ingestion | channel adapter must not own provider authority | TTS action tests; Telegram fixture tests |
| M8 | security evals; operator/developer/security docs; `release.v048`; version/changelog closeout | `EvalInventory` milestone, release gate | security eval lane; `mix allbert.test release.v048`; full release gate before tag |

## Milestones

### M1: Provider Capability Metadata

Status: planned.

Implementation tasks:

- Accept ADR 0051.
- Add `capabilities` to shipped `model_profiles` and Settings schema.
- Add voice fake provider/profile seed entries for deterministic tests.
- Keep provider catalog seed data descriptive; Settings Central remains
  runtime authority.

Acceptance criteria:

- Catalog profiles validate known capabilities.
- Existing text-generation profiles advertise `text_generation`.
- No profile capability grants permission, enables a provider, or supplies a
  secret.

Automated tests: catalog schema validation; settings safe-write validation;
provider alias generation still works.

Request-flow handoff: document the capability packet shape.

### M2: Operator Preference Resolver

Status: planned.

Implementation tasks:

- Add ranked `model_preferences.primary`, `.tasks.*`, and `.capabilities.*`.
- Add resolver API, e.g. `Models.for/2`, with diagnostics for skipped
  disabled/missing/incapable profiles.
- Map legacy `intent.model_profile` and `intent.direct_answer_model_profile`
  into the new resolver.
- Extend onboarding so operators can set primary and voice defaults.

Acceptance criteria:

- `:direct_answer` and `:speech_to_text` resolve through the same helper.
- Fallback skips incapable or disabled profiles.
- Existing direct-answer and intent classifier behavior remains compatible.

Automated tests: resolver precedence, fallback diagnostics, compatibility
alias tests, onboarding preference tests.

Request-flow handoff: document preference resolution order.

### M3: Capability-Aware Provider Doctor

Status: planned.

Implementation tasks:

- Amend ADR 0047 for voice doctor optional fields.
- Add `doctor_voice_provider`.
- Keep the canonical redacted envelope and fixed diagnostic catalog.
- Add fake local STT/TTS doctor fixtures.

Acceptance criteria:

- Doctor output reports STT/TTS availability without raw provider bodies,
  full URLs, secrets, or raw audio paths.
- Doctor failure categories are stable enough for CLI/workspace rendering.

Automated tests: doctor success/failure fixtures; redaction tests; Settings
profile resolution tests.

Request-flow handoff: add doctor output shape and errors.

### M4: Audio Resource, Permission, And Redaction Substrate

Status: planned.

Implementation tasks:

- Accept v0.48 audio amendments in ADR 0042.
- Add `mic://capture/<id>` normalization and scope derivation.
- Add voice permission classes and operation/origin/scope kinds.
- Add audio redaction helpers for audio resource URIs and transcript metadata.
- Add `voice.*` Settings Central keys for bounds/retention/redaction.

Acceptance criteria:

- `mic://capture/<id>` is normalized and rejected when malformed.
- Audio permission floors match the locked decisions.
- Trace/audit/CLI/LiveView redaction never exposes raw audio or local paths
  outside allowed metadata.

Automated tests: ResourceURI tests, OperationClass tests, Security.Policy
tests, Redactor tests.

Request-flow handoff: document audio authority invariants.

### M5: CLI Voice File Transcription

Status: planned.

Implementation tasks:

- Add `mix allbert.ask --voice AUDIO_FILE`.
- Validate explicit local file Resource Access, type, size, and duration.
- Route to `transcribe_voice`, then submit the transcript as normal runtime
  text input.
- Record redacted trace metadata.

Acceptance criteria:

- CLI voice works with fixture audio and fake STT provider.
- CLI does not capture live microphone audio.
- Oversized, unsupported, or unauthorized files are denied before provider
  calls.

Automated tests: Mix task tests, transcription action fixture tests, trace
redaction tests.

Operator validation:

```sh
export ALLBERT_HOME="$(mktemp -d /tmp/allbert-v048-cli.XXXXXX)"
mix allbert.ask --voice test/fixtures/audio/hello.wav --trace
```

Request-flow handoff: replace illustrative command with observed output at
closeout.

### M6: Workspace Microphone Capture

Status: planned.

Implementation tasks:

- Add workspace microphone affordance.
- Confirm per-session microphone capture.
- Store only bounded transient capture metadata unless retention is explicitly
  enabled.
- Transcribe approved capture through `transcribe_voice`.

Acceptance criteria:

- Mic capture cannot start without confirmation.
- Denial writes no audio resource.
- Approved capture records redacted metadata and transcript handoff only.

Automated tests: LiveView event/confirmation tests, resource metadata tests.
Browser validation required for UI changes.

Request-flow handoff: document workspace mic state machine.

### M7: TTS And Telegram Voice Notes

Status: planned.

Implementation tasks:

- Add `synthesize_voice` registered action.
- Report display-only provider/cost/usage metadata.
- Parse Telegram voice-note updates into a voice ingestion path.
- Download/transcribe only through the shared voice action/provider boundary.

Acceptance criteria:

- TTS does not bypass provider preference or permission checks.
- Telegram voice notes become text input only after the STT action succeeds.
- Telegram adapter never owns STT/TTS authority.

Automated tests: TTS action tests, Telegram parser/adapter fixture tests,
provider diagnostics tests.

Request-flow handoff: document channel voice ingestion flow.

### M8: Security Evals, Docs, And Release Closeout

Status: planned.

Implementation tasks:

- Add v0.48 security eval rows to `EvalInventory`.
- Add deterministic `mix allbert.test release.v048`.
- Add operator voice guide, developer provider-capability guide, and
  security-hardening audio notes.
- Reconcile roadmap, vision, agent-context-map, future-features, README,
  CHANGELOG, and version metadata at closeout.

Security eval rows:

- `voice-provider-capability-no-authority-001`
- `voice-preference-fallback-capability-check-001`
- `voice-cli-file-bounds-001`
- `voice-mic-confirmation-001`
- `voice-audio-retention-default-off-001`
- `voice-trace-redaction-001`
- `voice-cloud-upload-policy-001`
- `voice-tts-cost-metadata-display-only-001`
- `voice-channel-authority-boundary-001`

Acceptance criteria:

- All evals pass on fake providers and fixture audio.
- `mix allbert.test release.v048` writes reproducible evidence.
- Full release gate remains green before release handoff.
- Docs agree that v0.49 consumes the v0.48 provider capability substrate.

Operator closeout test:

```sh
mix allbert.test release.v048
# evidence: <ALLBERT_HOME>/release_evidence/v048/release-v048-<ts>.json
```

#### M8 Closeout Discipline

Single-purpose closeout commit; complete only when every box is checked:

1. `mix.exs` version -> `0.48.0`.
2. child app mix versions -> `0.48.0`.
3. `AllbertAssist.App.CoreApp.version/0` -> `"0.48.0"`.
4. `CHANGELOG.md` `## v0.48.0` cites evals, `release.v048`, and full
   release-gate counts.
5. `mix allbert.test` help lists `release.v048`.
6. `docs/plans/v0.48-plan.md` status -> implemented and M1-M8 statuses
   updated.
7. `docs/plans/v0.48-request-flow.md` status -> implemented as `0.48.0`.
8. ADR 0051 accepted; ADR 0042 audio amendments accepted; ADR 0047 v0.48
   amendment accepted.
9. Roadmap, vision, agent-context-map, future-features, README, operator
   docs, developer docs, and security-hardening docs match implementation.
10. v0.49 plan/request-flow still point to provider capability substrate
    rather than redefining provider preferences.

## Documentation Deliverables

- Operator voice and provider-preference guide.
- Developer provider capability/preference guide.
- Security-hardening audio retention/redaction section.
- ADR 0051 provider capability/preferences.
- ADR 0042 audio amendments.
- ADR 0047 voice doctor amendment.
- Future-features reconciliation for cost dashboards and post-v0.48 voice
  follow-ons.

## Definition Of Done

v0.48 is done when:

- provider/model profiles carry capability metadata;
- operator preferences resolve through ranked per-task/per-capability lists;
- STT/TTS uses the shared provider resolver and doctor substrate;
- CLI voice transcribes an explicit audio file/fixture;
- workspace microphone capture requires confirmation;
- TTS is a registered action with display-only usage/cost metadata;
- Telegram voice notes become text input through the shared STT action;
- raw audio is bounded, default-off for retention, and absent from traces;
- all v0.48 evals and `release.v048` pass on fake providers and fixtures.
