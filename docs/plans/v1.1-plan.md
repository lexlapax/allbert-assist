# Allbert v1.1.0 Asynchronous Background Agent Fan-Out With In-Channel Steering

Request flow: `docs/plans/v1.1-request-flow.md`
ADRs: three new ADRs ship with this release — ADR 0083 (objectives parallel
child fan-out — binding on M1/M2), ADR 0084 (autonomous channel notification
authority — a NEW authority class, binding on M6), ADR 0085 (cooperative
cancellation and child-process kill — binding on M4). Consumes ADR 0021/0022
(delegate substrate), ADR 0016/0056/0059 (channel identity/trust), ADR 0063
(outbound compose boundary — the operator-initiated contrast), ADR 0029 (typed
response contracts — additive only), ADR 0041 (workflow YAML — fan-out does
NOT route through it), ADR 0058 (channel daemon supervision), ADR 0026
(runtime facades), and the v1.0 public-contract freeze (`release.v1` stays
green).

Status: **M12.1–M12.19 are implemented and FV-01 passed, but the final release
cascade is paused for approved post-implementation audit remediation M12.20.
The audit found that a failed Coordinator safety transition could leave a child
durably `running`, after which a separate join path could restart it without the
retry-safety or attempt-ceiling checks. M12.20 makes recovery fail closed, binds
the latest persistence/acknowledgement/decomposition regressions directly into
`release.v11`, and strengthens production-topology evidence before one final
clean-SHA cascade. The prior M12.19 implementation candidate `2892940e` passed
all 14 `release.v11` steps (`release-v11-1785038793.json`), and FV-01 against
documentation descendant `b74a3405` passed on 2026-07-26 in one fresh
disposable Home. That happy-path operator attestation stands; M12.20 is proved
by deterministic failure/recovery tests rather than a tenth full walkthrough.
v1.1 is not yet released.**
The first operator attempt showed that the validation work had drifted from the
v1.1 fan-out Purpose into a bespoke release-validation subsystem. M12.11 removes
that scaffolding, retains genuine product/audit fixes, and restores a concise
feature-focused operator walkthrough. Stable
v1.0.5 shipped on 2026-07-22 and closes
the binary-recovery chain that followed v1.0.3. The 2026-07-19 audit repaired
the cancellation substrate and public-protocol acknowledgement contracts;
M0 proved those assumptions at build-start HEAD before implementation began.
Current Code State anchors were verified 2026-07-18, including
a full read of the eight channel-adapter plugins under `plugins/` (the earlier
apps/-only recon missed them). M0 re-verifies anchors at build-start HEAD
(the predecessor requirement is now satisfied).

Source: roadmap ladder item 5 and the future-features flagship intake,
operator intake 2026-07-18), plus the merged 1.1-slice enablers
Mid-Action Interruption / In-Flight Kill and Child-Process Cancellation
Semantics (`future-features.md:164-185`). Operator decisions of 2026-07-18
locked the concurrency model, decomposition posture, report-back authority
class, and all-channels rollout (Locked Decisions below).

**Operator questions are resolved.**

1. **App-Registry Membership Check At Action
   Boundary is IN 1.1.** The future-features slice annotation (`Slice: 1.1`)
   wins over the roadmap's 1.5/1.6 enabler listing (roadmap reconciled in the
   same commit as this plan). It also fits the flagship's surface: fan-out
   dispatches actions from BACKGROUND contexts, so the boundary must stop
   trusting caller-supplied app identity before that surface widens. M0
   carries the entry's "verify first" (whether a later release already added
   a check); any remaining gap closes at the M2 pre-dispatch barrier. No
   other open questions: the remaining
   judgment calls (ack semantics, settings-pin policy, cancel confirmation
   class, status-update delivery posture — the edit-in-place fold-in
   superseded the earlier no-edit call, signal inbound wiring, next-turn
   report fallback) are settled with evidence as Locked Decisions 8–25.

## Build Progress

Implementation started 2026-07-22. Each milestone: implement → focused test →
drift-check → doc update → commit (`v1.1 M<n> <small title>`). Build entries
land here per the v1.0.2 precedent (root-caused drift fixes, measured
acceptance from the test-metrics store, intake candidates held for operator
disposition — never written to future-features unilaterally).

### M0 — recon verify + baseline (2026-07-22)

- **Build anchor:** implementation starts at `c6985b93`, four documentation
  commits after stable `v1.0.5` (`1d4d5634`). The Current Code State anchors
  remain structurally correct. Line drift since the planning snapshot is
  mechanical: Runtime `submit_user_input/1` remains at `runtime.ex:115`;
  `ChannelParity` still hardcodes `turn_complete` at `channel_parity.ex:98`;
  `Actions.Runner` performs release availability before its app-scope gate at
  `runner.ex:124,324`; Signal still exposes an unwired
  `Daemon.daemon_child_spec/2`; Email still lacks `deliver_outbound/3`.
- **Direct Runtime caller census:** eight remote/TUI adapters (Telegram,
  Email, Discord, Slack, Matrix, WhatsApp, Signal, TUI); WorkspaceLive;
  OpenAI controller; ACP server; Jobs runner; `mix allbert.ask`; the shared
  CLI ask facade; and three operator channel simulation paths. Tests call the
  same public boundary but are not production callers. M3 must cover every
  production family; the simulation paths inherit the CLI acknowledgement
  adapter rather than becoming a new continuation implementation.
- **Parity before-state:** CLI and LiveView are local surfaces; the eight
  remote adapters plus TUI are channel descriptors. Every row reports
  `streaming=turn_complete`. Outbound is implemented for Telegram, Discord,
  Matrix, Signal, Slack, and WhatsApp; absent for Email and TUI; local
  surfaces are not applicable. Signal and WhatsApp remain implemented but
  unreleased. The snapshot was generated against an isolated migrated
  `ALLBERT_HOME`; an initial unscoped invocation exposed that application-
  starting Mix tasks can otherwise select the default database, so every
  v1.1 test/smoke command is required to set a temporary home.
- **Lane topology:** v1.0.5 did not inherit a v1.0.3 conversion claim. The
  current test-strategy census is 504 files: `app_env_serial=116`,
  `db_serial=112`, `global_process_serial=32`, `liveview_serial=31`, and
  `home_fs_serial=20`; M1/M2 fan-out persistence stays `db_serial`, while
  supervision/Registry ownership tests stay `global_process_serial` unless a
  later ownership proof justifies reclassification.
- **Recorded performance baseline:** the stable product SHA records decide
  at 1,042.6 ms for the representative turn; the latest stable release high-
  coverage phase is 383 s / 1,852 tests; the latest passing prepush high-
  coverage record is 844 s / 2,160 tests. The earlier implementation-plan
  comparison points remain quick ~442 s, high ~883 s, prepush ~897 s; all
  later performance claims must cite fresh structured records rather than
  mixing those gate shapes.
- **Rollout/default baseline:** `objectives.fanout.rollout_mode` is absent
  repo-wide. M3 therefore adds the schema key with the planned neutral
  explicit behavior first and changes the absent-key release default only
  after its accuracy gate.
- **App-registry boundary finding:** a partial boundary already shipped.
  Registry resolution denies unknown actions and `Actions.Runner` checks an
  app-scoped action's capability `app_id` against `active_app`; router direct
  execution derives that scope only from the registry-selected capability.
  The remaining M2 gap is explicit membership re-proof of the capability's
  app in the active `App.Registry` context immediately before background
  dispatch, plus a regression proving stale/unregistered app capabilities
  cannot run.
- **M0 validation:** isolated parity generation passed; test-metrics summary
  regenerated from 2,350 structured records; `git diff --check` and the docs
  gate are the milestone commit gates. No implementation scope was deferred.

### M1 — objectives fan-out model + migration (2026-07-22)

- Added the additive objective fan-out schema exactly at S1: role/join,
  kickoff and report delivery state/digests, exact-origin locators, durable
  queue/attempt fields, and review reason. Parent lookup and partial unique
  receipt-digest indexes are migration-owned; changesets enforce the closed
  state vocabularies and surface uniqueness errors without leaking receipts.
- `Objectives.Fanout` now frames one parent and its ordered children in one
  Repo transaction, copies origin attribution, persists only a SHA-256 digest
  of each opaque receipt, and records lifecycle events. Receipt
  acknowledgement is compare-and-set, identity-bound, idempotent, and atomic
  with its event. Terminal join finalization persists the explicit reduction,
  issues the report receipt once, and produces a redaction-safe report that
  enumerates every child.
- Drift review found two implementation details that the prose implied but
  did not spell out: receipt uniqueness also needs changeset constraint
  mapping (otherwise SQLite raises), and delivery-state CAS plus its event
  must share a transaction. Both were remediated before the gate. The
  existing Event kind vocabulary was additively extended; the serialized
  Engine agent and plan/build execution path were not changed. ADR 0083
  remains Proposed until M3 as planned.
- Validation: fan-out/CAS/join and migration round-trip suites green; the
  combined Objectives + plan/build coexistence run passed 27 tests, 0
  failures; the final post-remediation focused run passed 16 tests, 0
  failures; `release.v1` passed including formatter, warnings-as-errors,
  Credo, Dialyzer, frozen-contract, product-RC, and docs phases. All commands
  used an isolated temporary Allbert Home.

### M2 — supervised run executor + registry + backpressure (2026-07-22)

- Added the global `Objectives.Runs.Registry`, bounded DynamicSupervisor,
  permanent fair Scheduler, temporary per-fan-out Coordinators and RunServers,
  and the public `Objectives.Lifecycle` facade. The Scheduler enforces distinct
  global/per-fan-out limits, FIFO within a fan-out, round-robin grants across
  fan-outs, observable queues, synchronous terminal cleanup, and signal-driven
  boot/live reconstruction without polling. Lifecycle runs the six planned
  operations with a fresh Settings Central pin at each boundary, dispatches
  through `Actions.Registry` + `Runner.run/3`, persists attempts/events and
  confirmation receipts, and checks a process-independent cooperative
  `CancelToken` between operations.
- Added the six `objectives.fanout.*` Settings Central keys with the locked
  defaults and clamps. This moves the schema definitions one milestone earlier
  than M0's “M3 adds” note because S2 consumes both concurrency limits and its
  acceptance requires decomposition-limit independence; M3 still owns rollout
  behavior and decomposition semantics. The limits remain independent:
  children 2..16, per-fan-out runs 1..8, global runs 1..32.
- **Shipped action retry-safety sweep:** reviewed read-only/idempotent execution
  modes are centrally classified safe; explicit safe/unsafe action declarations
  override the classification; every unreviewed/future mode stays unknown by
  default.

  | classification | reviewed basis | shipped actions | restart behavior |
  |---|---|---:|---|
  | `safe` | gate-derived reviewed read/diagnostic/preview/render modes plus explicit safe declarations | 94 | one automatic replay while the persisted attempt count is 1 |
  | `unsafe` | explicit declaration only | 0 | never automatic |
  | `unknown` | all other shipped modes/default | 131 | park `blocked/uncertain_effect` for explicit retry or skip |

  Counts were regenerated by the M12.8 capability-catalog gate (225 total),
  rather than retained as prose estimates.

- Closed the M0 App-Registry gap: Runner now re-proves the capability's
  expected app against the active registry context immediately before
  dispatch. An adversarial registry that removes membership between discovery
  and dispatch is denied as `unregistered_app` even when `active_app` matches.
- Crash/drift review proved and remediated four non-obvious failure classes:
  nested Settings defaults initially omitted the new keys; test teardown left
  a Coordinator holding a sandbox owner; boot reconciliation initially
  replayed unknown in-flight and confirmation-blocked work; and asynchronous
  finalization could retain Scheduler slots. Defaults, synchronous cleanup,
  status-aware reconciliation, and terminal slot release now close those
  classes. Safe work resumes once after RunServer/boot loss; unknown work parks;
  Coordinator restart reconnects live siblings; Scheduler restart reconstructs
  capacity without duplicate attempts; confirmation parking does not stop a
  sibling; and join truth remains derived from durable children.
- Lifecycle status/event writes and exhausted-retry failure/event writes are
  transactional, so a durable transition cannot exist without its audit event.
- Validation: binding + expanded focused suites passed (latest executor run 16
  tests, 0 failures; expanded action/settings/signal/objective regression green),
  lane inventory passed at 530 files with zero unclassified/double-counted
  files, formatter/warnings/Credo/Dialyzer passed, and `release.v1` passed.
  Structured contention evidence: four `db_serial` partitions passed
  160/160/160/168 tests in 77,523–113,121 ms with zero lock/ownership failures;
  the single `global_process_serial` lane passed 143 tests in 47,902 ms. New
  supervision cases measured 647–768 ms and the Scheduler suite 102 ms. All
  commands used owned temporary Allbert Homes. ADR 0083 remains Proposed until
  M3 proves delivery-before-start.

### M3 — Stage-0 decomposer + visible-list acknowledgement (2026-07-22)

- Added the advisory Stage-0 decomposer ahead of normal routing: deterministic
  enumerations/chains first, bounded ReqLLM structured proposal only for cheap
  plausible multi-task shapes, and fail-closed `:single` handling for commands,
  explicit one-task language, active steering, nested fan-out, malformed model
  output, and uncertainty. Overflow returns the full proposed list for operator
  narrowing; no child is silently lost.
- The delivery barrier is durable end to end. Framing returns a visible child
  list and restart-stable HMAC start receipt while children remain non-runnable.
  Successful render/transport/durable recording acknowledges through one
  idempotent Runtime boundary; failed transport moves kickoff delivery to
  `blocked` and retry reuses the receipt. Exact user, source channel/thread,
  origin digest, and receiver account are re-proved. Pending join reports render
  into the next turn without consumption and are consumed only after caller
  delivery succeeds.
- Drift review found that M1's random bearer receipts could not be reconstructed
  after a BEAM restart although only their digests were stored. M3 remediated
  this with a mode-0600 receipt secret under Allbert Home and deterministic HMAC
  receipts; the database still stores digests only. Review also found that an
  intent decision selecting a non-action surface could park one child forever;
  the lifecycle now persists an inert decision and falls back to the registered
  `direct_answer` action when no executable action exists. Execution continues
  through Registry, Security Central, and `Runner.run/3`.
- All production caller families are wired: TUI, web, Telegram, Email, Discord,
  Slack, Matrix, WhatsApp, Signal, shared CLI, `mix allbert.ask`, Jobs, channel
  simulation, ACP, and OpenAI. Remote transports/TUI/Jobs explicitly record
  failed delivery. OpenAI non-streaming and ACP hold outside
  `submit_user_input/1`; timeout returns kickoff with the eventual report still
  pending. OpenAI SSE now uses real chunked kickoff flush → acknowledgement →
  status → report → `[DONE]`. ACP uses an owner loop plus supervised prompt
  workers so stdin remains readable and `session/cancel` routes through the
  registered cancellation action. Successful in-band report delivery consumes
  its report receipt; uncertain/failed delivery does not.
- `confirm_before_start=true` now creates a durable confirmation included in
  the kickoff. Delivery acknowledgement alone does not start children; approval
  resumes the internal, registered, resumable `start_fanout` action, which
  re-proves ownership and acknowledged delivery before scheduling.
- Automatic-rollout evidence: the committed 200-row cross-surface corpus has
  50 fan-out positives, 100 ordinary single negatives, and 50 active-fan-out
  steering negatives across all 13 caller families. It measured precision
  **1.00**, recall **1.00**, and false-positive rate **0.00**; the existing
  golden/corpus suites remained green. The cheap single path measured
  330,682 µs for 10,000 calls (**33.068 µs mean**) in the isolated M3 run. The
  live ReqLLM/Ollama spot check (`llama3.2:3b`) returned two bounded independent
  tasks for a natural conjunction. Only after these gates did the absent-key
  schema default change to `automatic`; an explicitly stored `explicit` value
  remains unchanged and tested.
- Focused validation: app-side M3 gate 27 tests / 0 failures; dedicated caller
  matrix + corpus/ack gate 16 / 0; ACP 13 / 0; OpenAI 12 / 0; all channel/TUI
  adapter directories 16 / 0; Settings 48 / 0; action registry 11 / 0; existing
  intent corpus completeness returned green after documenting `start_fanout`
  as continuation-only. The post-refactor combined protocol/runtime rerun passed
  40 tests / 0 failures. Formatter, warnings-as-errors, strict Credo, and
  Dialyzer are green. The first fast-local run correctly caught one new source-
  inspection test carrying conflicting inferred/declared lanes; its primary
  lane was corrected to `global_process_serial`, the lane-packing contract
  passed 6 / 0, and the fresh full fast-local rerun passed all eight owners
  (294 tests / 0 failures; core 214 / 0). The authoritative `release.v1` rerun
  passed migration, formatting, compile, Credo, Dialyzer, v1 consistency/freeze,
  product-RC, and docs phases; its frozen/product sweeps passed 31 tests / 0
  failures. All runs used fresh temporary Allbert Homes. No release scope was
  deferred and no intake candidate was created.

### M4 — cancellation + interruption, all three tiers (2026-07-22)

- Added the application-level `Execution.ProcessOwners` DynamicSupervisor and
  unique `Execution.ProcessRegistry`. Each authorized host execution now runs
  under a temporary `ProcessOwner` independent of its fan-out RunServer. The
  owner uses the packaged erlexec argv protocol with an absolute executable,
  `{group, 0}`, `kill_group`, linked ownership, merged bounded output, cwd/env
  parity, and configurable TERM→grace→KILL. A separately supervised cleanup
  guard monitors owner loss, so the captured group and container cleanup hook
  are not abandoned if the owner dies. Startup/handshake failure is closed;
  there is no `System.cmd` fallback.
- `LocalRunner`, `SkillScriptRunner`, and the sandbox command/container path now
  converge on that owner for both explicit cancellation and timeout. Sandbox
  command specs carry an additive internal execution id so an objective cancel
  addresses the exact container/CLI execution; the existing named-container
  `rm -f` handle runs before group shutdown. A live local Docker proof launched
  `ubuntu:22.04`, timed out, and left no container row. The existing product
  release contains both packaged artifacts at
  `erlexec-2.3.4/.../exec-port` and `muontrap-1.8.0/priv/muontrap` and its
  binary version command passed.
- The packaged-substrate matrix selected erlexec for arbitrary scoped commands:
  current erlexec documentation and source confirm `run_link/2`, OS pid,
  process-group creation, monitoring, `stop_and_wait/2`, and signal APIs;
  MuonTrap remains appropriate for long-lived supervised daemons but exposes no
  arbitrary-command group handle; Docker/Podman keep their named-container
  stop handle. No new native helper was needed. Plain process groups honestly
  do not contain a descendant that deliberately creates a new session; this
  Darwin host has no `setsid` fixture, while configured cgroup/container and
  WSL2/native-Linux validation remain in the M12 cross-platform validation
  matrix rather than being claimed here.
- Added `cancel_objective_run`, an ownership-bound, no-confirmation safety
  action. It sets the run's Registry-carried atomics token, waits the Settings
  Central `execution.cancel.grace_ms` window (default 5 s), cancels any exact
  ProcessOwner, terminates an uncooperative RunServer, then persists objective
  cancellation, the reached tier, and `allbert.objectives.run.cancelled`.
  Terminal state is therefore recorded after cleanup. Parent cancellation
  covers every child and never targets a sibling execution group.
- Delegate dispatch now carries the same token in options and signal params,
  checks it before and after dispatch, and documents the additive developer
  contract. Both shipped families adopt natural-boundary checkpoints:
  `research.specialist` research/summarize commands and StockSage native
  specialist execution. Existing unconverted delegates still degrade to the
  supervised tier. Attached TUI Escape now offers cancellation for an active
  fan-out instead of incorrectly reporting that no turn is running; the reply
  remains the registered cancellation boundary.
- Drift review caught and remediated four issues: argv-mode erlexec needs an
  absolute executable; a missing executable otherwise appears as exit 1 rather
  than a failed handshake; owner loss needs cleanup ownership independent of
  the owner itself for container handles; and the flat rollout schema still
  said `explicit` while Settings' nested release default and M3 acceptance said
  `automatic`. The executable preflight, cleanup guard, internal execution-id
  propagation, and schema alignment close those gaps. The lane gate also found
  an external/global double classification; the owner protocol tests now use
  their actual `global_process_serial` primary lane, while descendant process
  fixtures remain `external_runtime_serial`. The authoritative manifest check
  then exposed two inherited M3 declarations plus the new delegate DataCase:
  both DataCase files now retain `db_serial`, the source-only OpenAI proof
  explicitly declares `pure_async`, and the regenerated inventory passes at
  538 files with zero unclassified or double-counted files.
- Validation: the named ProcessOwner/process-group/delegate suite passed 10 / 0,
  the expanded cancellation/delegate suite passed 6 / 0, the integrated M4 +
  TUI suite passed 38 / 0, and existing shell/skill/sandbox/settings/registry
  callers passed 110 / 0 (2 intentional skips). Descendant fixtures ignore
  TERM and prove parent/child/grandchild death after timeout and cancel, sibling
  survival, owner-loss cleanup, cooperative checkpointing, supervised
  escalation, OS-kill escalation, and fail-closed startup. The corrected
  fast-local gate passed all eight owners (294 / 0; core 214 / 0). Formatter,
  warnings-as-errors, strict Credo, Dialyzer, docs, and the full authoritative
  `release.v1` compatibility gate are green. All product/test runs used
  temporary Allbert Homes. ADR 0085 is Accepted; no scope was deferred and no
  intake candidate was created.

### M5 — channel parity/streaming contract renegotiation (2026-07-22)

- Channel descriptors now carry the additive `streaming:` declaration. Web and
  TUI declare `:live_region`; Telegram, Discord, Slack, Matrix, WhatsApp, and
  Signal declare `:progress_messages`; Email explicitly declares
  `:turn_complete`. CLI deliberately omits the field, proving the compatibility
  default remains `:turn_complete`.
- `ChannelParity` now derives its existing string-valued output from the
  declaration instead of hardcoding every row. `verify/1` rejects unknown modes
  and confines `:live_region` to local surfaces/TUI. This is capability truth,
  not report-back authority: ADR 0084's Settings/Security boundary remains
  default off and lands in M6.
- **Before/after matrix snapshot:** M0 recorded all ten descriptor rows as
  `turn_complete`. The isolated M5 regeneration records `cli` and `email` as
  `turn_complete`; `live_view` and `tui` as `live_region`; and the six remote
  message adapters as `progress_messages`. Outbound and release-availability
  columns are unchanged.
- Drift review found the historical v0.55 TUI security assertion and developer
  matrix still encoded turn-complete posture. Both now follow descriptor truth;
  no separate operator matrix exists to regenerate. The new focused parity suite
  independently supplies the shipped descriptors and proves derivation,
  absent-field defaulting, enum rejection, and the live-region locality rule.
  Its initial registry-backed fixture correctly failed lane reconciliation as a
  global-process test; an explicit descriptor-input seam removed that incidental
  state and the final test is genuinely `pure_async`.
- Validation: the focused parity suite passed 3 / 0; the parity + historical TUI
  security + channels Mix-task regression passed 22 / 0; lane reconciliation
  passed at 539 files with zero unclassified/double-counted files; and
  `release.v1` passed migration, format, warnings-as-errors, Credo, Dialyzer,
  frozen-contract, product-RC, and docs phases. All runs used isolated temporary
  Allbert Homes. No scope was deferred and no intake candidate was created.

### M6 — autonomous-notify authority + all-adapter delivery (2026-07-22)

- Added the default-OFF ADR 0084 authority boundary. `Channels.Notify` checks
  live-use, the channel grant, notification level, status throttle, Security
  Central policy, and exact origin/receiver/identity-map binding in that order,
  then dispatches only through `Channels.Outbound`. Payloads are redacted before
  transport. Suppressed, delivered, failed, and uncertain outcomes are persisted,
  signaled, and appended to the monthly redacted channel-notify audit under
  Allbert Home.
- Added the migration-owned delivery ledger with unique reservation keys and the
  `reserved → sending → delivered | failed | uncertain | suppressed` state
  machine. Completion/confirmation deduplicate terminal events; definitive
  failures receive at most one retry; uncertain provider acceptance is terminal
  for autonomous delivery. The ledger stores origin digests and operational
  receipts but no payload body, external identity, secret, or free-text address.
  Migration round-trip retains a pre-M6 objective row.
- Settings fragments now expose `autonomous_notify.enabled=false`, completion
  level, and a 30-second interval (5..600) for every channel; Email rejects the
  status level. The one-time kickoff offer is reserved durably per channel/user
  and marked only after caller delivery acknowledgement. Exact typed command and
  Telegram/Discord/Slack/WhatsApp buttons re-prove the current mapped identity,
  then invoke registered `configure_channel_setting` through Runner; arbitrary
  free text cannot enable the grant.
- The signal consumer converts run lifecycle, confirmation-block, and fan-out
  join events into status/confirmation/completion kinds. Successful autonomous
  completion acknowledges the durable report receipt; disabled or failed sends
  retain next-turn delivery. Pending reports render as compact parent/join/child
  summaries rather than injecting full report bodies into unrelated answers.
- Adapter work is complete: Email now sends threaded SMTP outbound replies;
  Telegram, Discord, Slack, Matrix, and WhatsApp carry native reply/thread hints;
  Signal mounts its MuonTrap daemon and adapter under a `rest_for_one` supervisor.
  The attended TUI uses the named ephemeral `Channels.TUI.Subscriptions` boundary,
  re-reads objective ownership before rendering, unregisters with the session,
  and persists no PID or callback. Web remains on its existing attended signal
  path and does not pass through autonomous Notify.
- Drift review caught five material gaps before acceptance: newly framed
  objectives initially lacked the persisted thread-ref id; room/thread refs did
  not bind the originating human; direct-message delivery reused stored targets
  instead of current identity maps; the planned TUI boundary was embedded in the
  adapter; and consent buttons rendered but their parsers accepted only
  confirmation ids. Thread linking now returns and persists the id, inbound refs
  carry a one-way identity digest, direct targets derive from current mappings,
  TUI subscription ownership is explicit, and all four button surfaces have
  adapter-level consent-loop proofs.
- Focused authority/ledger/migration/TUI/Email/Signal validation passed 16 tests;
  the four interactive adapter suites passed 79 tests; the authority plus plugin
  renderer suite passed 24 tests. Lane reconciliation passed at 541 files with
  zero unclassified/double-counted files. Formatter, warnings-as-errors, strict
  Credo, and Dialyzer are green. Live outbound smokes passed on configured
  Telegram and Email. Matrix failed pre-send with `M_UNKNOWN_TOKEN` (inactive
  token), Discord with HTTP 401, and Slack with `account_inactive`; these are
  operator credential-state failures, not converted to product passes. These
  rows remain release-environment diagnostics; the later M12.15 corrective
  disposition does not repeat them as manual feature acceptance. WhatsApp and Signal
  remain release-gated; their fixture transports and simulated adapter/daemon
  suites passed 25 tests, including the mounted-daemon JSON forwarding path.
  ADR 0084 remains Proposed until M7 proves
  edit/restart behavior. No scope was deferred and no intake candidate was
  created.

### M7 — capability-declared in-place status (2026-07-22)

- Added the second additive descriptor axis,
  `status_update_mode: :append_only | :edit_in_place`, defaulting absent
  declarations to append-only. Telegram, Discord, Slack, and Matrix declare
  edit-in-place; all other channel and local-surface rows remain append-only.
  `ChannelParity` now derives, prints, and validates the column, including an
  invalid-enum proof and an export check for every declaring adapter.
- Added transport edits without widening authority: `Channels.Outbound.edit/5`
  dispatches only after the same caller-side ADR 0084 boundary. Provider clients
  implement Telegram `editMessageText`, Discord message `PATCH`, Slack
  `chat.update`, and Matrix `m.replace` over the already policy-whitelisted PUT
  path. Fixture assertions bind method, path, target id/timestamp, replacement
  relation, and redacted authorization metadata.
- `Channels.Notify` now reads the latest delivered status receipt from the
  durable ledger. The first status appends; later statuses edit the same provider
  message after the throttle window. No in-memory cursor is authoritative, so a
  new process/BEAM uses the same id after restart. A definitive edit failure
  appends once, records `edit_fallback:<redacted reason>`, and makes the appended
  id the next edit target. An uncertain edit never appends or retries. Completion
  and confirmation-request kinds bypass edit dispatch and always create new
  history entries.
- Drift review found that M6 normalized Telegram/Discord receipt keys but not
  Slack `ts` or Matrix `event_id`; those channels would otherwise declare edit
  capability while appending forever after restart. Provider receipt
  normalization now persists all four shapes as strings (including Telegram's
  integer message id), with a dedicated ledger proof. The review also corrected
  nested fixture request-option forwarding in the Telegram/Discord/Slack
  adapters and made the shared Outbound moduledoc name both authority-gated
  callers rather than only ADR 0063 compose.
- Binding M7 edit/parity/provider suites passed 23 tests; the expanded four-
  adapter plus authority regression passed 95 tests. Lane reconciliation passed
  at 546 files with zero unclassified/double-counted files. Formatter,
  warnings-as-errors, strict Credo, and Dialyzer are green. The enhanced live
  Telegram smoke created and edited a real provider message successfully.
  Matrix remains blocked at `M_UNKNOWN_TOKEN`, Discord at HTTP 401, and Slack at
  `account_inactive`, all before edit dispatch; the M12 operator matrix retains
  those credential rows. ADR 0084 is Accepted. No scope was deferred and no
  intake candidate was created.

### M8 — steer-vs-new classifier + steering actions (2026-07-22)

- Added the stage-zero `Intent.Steering` classifier ahead of decomposition.
  Deterministic status/cancel/steer verbs win first; an optional injected model
  assist is advisory and cannot bypass ownership or ambiguous-target
  clarification. Numeric/ordinal and normalized-title references resolve only
  within active fan-outs owned by the current user and source thread. Unrelated
  turns return to the normal pipeline, and confirmation approval syntax/free
  text is never converted into steering authority.
- Added the registered `steer_objective_run` action and durable
  `steer_directive`/`steer_applied` events. Delivery uses the existing run
  Registry as a wake-up hint while the objective event stream remains truth;
  every lifecycle operation boundary reconciles unapplied directives, so
  restart cannot lose or duplicate a steer. Mutations are ownership-bound and
  terminal objectives reject them.
- Status responses aggregate durable child state. High-confidence cancellation
  continues through the M4 cooperative/supervised/OS action; missing or
  ambiguous mutation targets clarify instead of selecting the newest run.
  Parked pending confirmations are cancelled with
  `superseded_by_steer` before the directive event is committed.
- Drift review found the Registry canonical-name contract and updated its
  expected additive action. The first accuracy run also exposed incidental
  numerals in ambiguous corpus rows as ordinal references; the corpus now
  separates reference language from unrelated variants. The binding corpus is
  150 rows (30 per status/cancel/steer/new-request/clarify), macro accuracy is
  at least 0.90, cancel precision is 1.00, and approval false positives are
  zero. A live `llama3.2:3b` Ollama spot-check did not reliably follow the
  five-label contract (including incorrect status and new-request labels),
  validating the fail-closed posture: no model result grants authority and
  low-confidence/invalid output stays new-request or clarification. Focused
  steering tests passed 7 / 0; the expanded lifecycle, cancellation, Registry,
  and Runner suite passed 40 / 0 after the additive contract correction.
  Formatter, warnings-as-errors, strict Credo, Dialyzer, docs, and
  `release.v1` are green. No scope was deferred and no intake candidate was
  created.

### M9 — Web + TUI operator experience (2026-07-22)

- Extended the existing `show_objective` registered read with an additive
  child list for fan-out parents. ObjectiveLive now renders an accessible
  parent/child task tree with durable status/progress, explicit per-child
  steering forms, and cancellation controls. Every mutation dispatches
  `steer_objective_run` or `cancel_objective_run` through `Actions.Runner`;
  the LiveView owns no steering, settings, or objective semantics.
- Settings Central now explains the ADR 0084 boundary at the point of use:
  background report-back is off by default, grants delivery only (not tool or
  data authority), and Email is completion-only. Operators edit the existing
  `channels.<channel>.autonomous_notify.*` Settings Central rows. The TUI
  foundations landed with M4–M6 were re-audited: kickoff acknowledgement,
  attached live-region signals, steer-by-input, Escape cancellation offer,
  `/settings get`, ephemeral subscription registration, ownership re-proof,
  and terminate-time unsubscribe are all on the shared runtime/action spine.
- Drift review found ObjectiveLive assumed every event carried a non-null
  summary. `fanout_proposed` correctly omits one, so the view now renders a
  stable fallback instead of crashing. Focused ObjectiveLive tests passed
  5 / 0 and TUI/ephemeral-subscription tests passed 29 / 0.
- The required in-app browser walkthrough used an isolated migrated Allbert
  Home. It rendered the two-child tree, queued a real steering directive from
  the first child form, showed the durable success acknowledgement, rendered
  the Settings Central boundary copy, and recorded zero browser console
  errors on both pages. No scope was deferred and no intake candidate was
  created.

### M10 — eval rows + release.v11 gate (2026-07-22)

- Extended the eval-inventory milestone enum with `:v11` first, then added
  eleven concrete authority/deny rows covering durable fan-out, scheduler
  bounds, app membership, cancellation tiers, channel capability truth,
  default-off/exact-origin notify authority, in-place status, steering
  ownership, free-text non-approval, and surface dispatch. The owning sweep
  verifies exact inventory/routing and every row calls `AssertBinding.check!/2`.
- Added `mix allbert.test release.v11` as the unchanged `release.v1` prefix plus
  focused v1.1 authority, runtime/steering/cancellation, notify/edit/TUI, and
  web-operator steps. Its runner records structured metrics per step and writes
  redacted evidence beneath an isolated
  `ALLBERT_HOME/release_evidence/v11/`.
- Drift review found one nested-module Credo warning in the new sweep; aliasing
  the confirmation callback resolved it. Gate wiring/inventory tests passed
  41 / 0. The first roll correctly failed only that strict-Credo step while
  still exposing all later step results; after remediation, `release.v11`
  passed its complete 13-step chain, including 12 / 0 v1.1 evals, 17 / 0
  runtime, 14 / 0 channel, and 5 / 0 web focused tests. No scope was deferred
  and no intake candidate was created.

### M11 — dependency refresh (2026-07-22)

- Reconciled `mix hex.outdated` independently at the umbrella, core-app, and
  web-app roots, then deduplicated the results. All direct dependencies are
  current except the deliberately constrained Phoenix LiveView feature line.

  | Item | Before → after | Disposition | Evidence / reason |
  | --- | --- | --- | --- |
  | `req_llm` | 1.13.0 → 1.17.1 | applied | Bounded update also advanced compatible transitive `llm_db` 2026.5.2 → 2026.7.2 and `ex_ast` 0.12.10 → 0.13.0. Provider/runtime/coding suites and a real local Ollama `llama3.2:3b` text generation passed. |
  | `phoenix_live_view` | 1.1.28; latest 1.2.7 | scoped out | The web app intentionally requires `~> 1.1.0`; 1.2 is a new feature line with migration surface, not a bounded dependency refresh. No v1.1 Purpose outcome or acceptance criterion is deferred. |
  | `exqlite` | 0.39.0 | current | The resolved transitive version is already latest; no apply wave exists. |
  | Other direct dependencies | current | no change | All three root inventories agree after deduplication. |

- Drift review caught a stale IntentAgent action-surface assertion that omitted
  the M8 `cancel_objective_run` and `steer_objective_run` actions. The contract
  list was reconciled and the scoped provider/runtime/coding suite passed
  121 / 0. The local Ollama smoke returned the exact requested response through
  ReqLLM 1.17.1; its warning only identifies the local model as outside LLMDB's
  optional catalog, not a failed or degraded provider call.
- Final post-refresh `release.v11` and standalone `release.v1` runs supersede
  the M10 pre-refresh release evidence. No scope was deferred and no intake
  candidate was created.

### M12 — pre-validation release candidate (2026-07-22)

- Bumped the umbrella, core, and web versions to 1.1.0 in lockstep; bumped the
  workspace service-worker cache and regenerated its gzip. Added the v1.1.0
  pre-publication CHANGELOG entry and exact formula-lag contract. No tag,
  artifact publication, tap movement, plan archival, or shipped-status claim
  has occurred.
- Reconciled existing operator/developer docs for fan-out operation, steering,
  cancellation escalation, Settings Central bounds, default-OFF autonomous
  notify authority and recovery/audit location, onboarding, delegate-runner
  ownership, and test-lane/gate placement. The request-flow checklist now
  separates implementation evidence from the held warm-TUI, live-channel,
  cross-platform, binary, and closeout rows.
- **Purpose audit:** all six Purpose outcomes have implementation and automated
  contract evidence: kickoff delivery-before-start; durable supervised parallel
  children and honest join; contextual steering/cancellation; default-OFF
  exact-origin report-back across nine declared surfaces; descriptor-derived
  parity; and the shared Runtime continuation contract. Overall operator
  experience, configured-provider attestations, and packaged host containment
  remain validation evidence—not missing implementation—and are intentionally
  held for the post-audit matrix.
- RC drift review found and remediated four stale/incomplete cross-suite
  contracts: effectful replay disposition for the two contextual run actions;
  general intent-corpus exclusion for those contextual-only actions; Notify
  test placement in the application-environment lane; and temporary fan-out
  Coordinator teardown before SQL sandbox-owner exit. It also reconciled the
  new permission class in the stable-order assertion, the pre-publication
  formula wording, and three lock entries made unused by ReqLLM 1.17.1.
- Focused regressions passed (10 / 0 version/offline, 19 / 0 action/notify,
  6 / 0 lane packing, 7 / 0 corpus completeness, 38 / 0 fan-out teardown
  sequence, 35 / 0 permission/install). `mix precommit` passed. The final
  authoritative `mix allbert.test release` passed in 3,819 seconds: high
  coverage, 612 / 0 external-runtime, complete security evals, 302 / 0 web,
  198 / 0 StockSage, 35 / 0 channel plugins, and Dialyzer 0. Final
  `release.v11` and standalone `release.v1` also passed at the RC source.
- Real provider delivery passed for Telegram and Email. Matrix is blocked by
  `M_UNKNOWN_TOKEN`, Discord by HTTP 401, and Slack by `account_inactive`.
  WhatsApp/Signal lack live credentials and retain mandatory green simulated
  coverage. Telegram inbound validation was stopped after a second bot process
  held `getUpdates` (409 conflict); the warm overall operator matrix owns the
  reply/steer attestation. Local ReqLLM 1.17.1 + Ollama `llama3.2:3b` generation
  passed. These are validation dispositions, not scope deferrals.
- Remaining M12 work before validation: M12.5–M12.9 audit remediation and the
  final automated release rejoin. After that: warm TUI/Web operator validation, the complete
  channel matrix, Linux/WSL2/container packaged cancellation rows, annotated
  tag and binary CI/cosign publication, tap fill/install rehearsals, then
  roadmap/backlog administration and triad archival. No intake candidate was
  created.

### M12.1 — Elixir 1.20 clean-build portability remediation (2026-07-22)

- **Discovery:** an Arch Linux clean build at RC commit `659d6d09` with Elixir
  1.20.2 / OTP 29 failed `mix compile --force --warnings-as-errors`: 30 core
  warnings stopped compilation before the 5 web/plugin warnings already seen
  by the preceding non-failing forced build. The existing release evidence used
  an incremental Elixir 1.19.5 / OTP 29 compile and therefore did not prove a
  warning-free clean rebuild.
- **Scope:** repair all 35 project-owned type/dead-clause findings according to
  the intended contracts; add regressions for nil scope handling and JPEG
  segment-size matching; force recompilation in both the authoritative release
  static phase and the frozen `release.v1`/derived `release.v11` compile step.
  Dependency-owned optional-Lua warnings remain outside this project-owned
  source sweep and are evaluated separately by dependency disposition.
- **Implementation:** repaired all 35 project-owned findings, including the nil
  scope normalization bug and the JPEG variable-size match; regressions passed
  26 / 0 for resource references/image metadata and 5 / 0 for package-install
  status handling. The release, pre-push, `release.v1`, and derived
  `release.v11` compile phases now force recompilation under
  `--warnings-as-errors`, preventing an incremental build from masking this
  class of portability drift.
- **Local evidence:** the clean forced build is warning-free for project-owned
  sources on Elixir 1.19.5 / OTP 29. Formatter, strict Credo, `mix precommit`,
  `release.v1`, and `release.v11` passed; Dialyzer reported zero errors. The
  authoritative release gate passed in 3,701 seconds: high coverage, 613 / 0
  external-runtime with 12 skipped and 14 excluded, complete security evals,
  302 / 0 web, 198 / 0 StockSage, 35 / 0 channel plugins, and Dialyzer 0.
  Warning logs emitted by negative runtime tests are expected assertions and
  are not compiler-warning regressions.
- **Arch before/after evidence:** before remediation, Arch Linux at `659d6d09`
  on Elixir 1.20.2 / OTP 29 reported 30 core warnings and stopped before the 5
  already-observed web/plugin findings. After remediation, the operator pulled
  `dd03455042bb7dde2d36bb0a15b7281b7ab0173b`, ran `mix clean`, then
  `mix compile --force --warnings-as-errors`: 1,012 core and 57 web files
  compiled with zero warnings, both applications were generated, and
  `git status --short --branch` reported clean `main...origin/main`.
- **Disposition:** M12.1 is complete. Project-owned compiler warnings remain a
  release blocker on either environment. Expected negative-test runtime logs
  and dependency-owned optional-Lua warnings, if dependency recompilation
  emits them, retain their separate documented dispositions.

### M12.2 — eliminate vulnerable Hackney through offline time-zone migration (complete 2026-07-23)

- **Trigger and fixed-version boundary:** Hex 2.5's advisory output reports Hackney
  1.25.0 vulnerable to EEF-CVE-2026-47071 (high), EEF-CVE-2026-47076,
  EEF-CVE-2026-47075, and EEF-CVE-2026-47069. Hex/EEF mark every Hackney 1.x,
  2.x, and 3.x release affected by at least these findings; the common fixed
  floor is 4.0.1. No secure 1.x line exists, and the operator rejected a fork,
  vendored patch, or maintained backport.
- **Dependency/root-cause decision:** Allbert does not use Hackney as an HTTP
  client. It is mandatory only through direct `tzdata ~> 1.1`, whose Hackney
  constraint cannot resolve 4.x; Swoosh's Hackney support is optional while
  production is pinned to `Swoosh.ApiClient.Req`, channel email uses
  `gen_smtp`, and product HTTP uses Req/Mint. Replace direct `tzdata` with
  direct `time_zone_info ~> 0.7.15`, switch the single Jobs schedule database
  reference to `TimeZoneInfo.TimeZoneDatabase`, remove Tzdata configuration,
  and require Hackney plus its now-unused transitive tail to disappear from the
  resolved graph and lock. Do not add Hackney 4.x merely to retain an unused
  client.
- **Behavioral contract:** preserve packaged/offline named-zone validation,
  UTC conversion, daily/weekly/cron scheduling, DST spring gaps, DST fall
  ambiguities, and deterministic no-network startup. `TimeZoneInfo` ships IANA
  data and defaults updates to disabled; Allbert does not add an updater,
  writable time-zone data directory, network permission, or operator-managed
  database. Dependency refreshes remain the mechanism for shipping newer IANA
  data.
- **Workstreams and serial barriers:** W1 records the vulnerable before-state
  (`mix deps.tree`, lock, `mix hex.audit`) and adds red schedule/provider
  contracts. After that barrier, W2 changes the dependency/config/code seam and
  refreshes only the required lock entries. W3 runs dependency compilation and
  graph/audit proofs while W4 runs focused Jobs/settings/release-package
  behavior. They rejoin for drift review of Swoosh adapters, release contents,
  timezone semantics, stale Tzdata references, unexpected lock churn, and
  license/advisory changes. Any drift is remediated and the focused suites rerun
  before documentation and authoritative gates.
- **Focused and release gates:** prove valid/invalid IANA zones plus explicit
  America/Los_Angeles spring-gap and fall-ambiguity behavior; run Jobs schedule,
  scheduler, settings-timezone, email/Swoosh Req, and packaged/offline focused
  suites. Require `mix deps.tree` with no Hackney/Tzdata, a clean
  `mix hex.audit`, `mix deps.unlock --unused`, forced warnings-as-errors
  compilation, formatter, strict Credo, Dialyzer, `mix precommit`,
  `release.v1`, `release.v11`, and the authoritative release gate. Test and CI
  use disposable Allbert Homes; no live provider call is required for this
  dependency-only security boundary.
- **Documentation, commits, and rejoin:** this plan/request-flow commit lands
  before implementation. After implementation, record exact before/after
  versions, IANA data version, graph removals, advisory output, test counts,
  drift/remediation, and release timings here and in the request-flow row, then
  commit and push the bounded change. Only a green M12.2 reopens the
  post-implementation audit; any unresolved advisory, scheduling divergence,
  runtime download, or unplanned dependency churn blocks that rejoin. No v1.1
  Purpose outcome is deferred and no future-features intake entry is created.
- **Implementation and dependency evidence:** direct `tzdata ~> 1.1` became
  direct `time_zone_info ~> 0.7.15`; the lock advanced TimeZoneInfo 0.7.14 to
  0.7.15 with bundled IANA data 2026c. The resolved graph and top-level lock
  entries no longer contain Tzdata, Hackney 1.25.0, Certifi 2.15.0, Metrics
  1.0.1, Mimerl 1.5.0, ParseTrans 3.4.1, or SSL Verify Fun 1.1.7. Swoosh's
  lock metadata still describes Hackney as an optional adapter, but Hackney is
  not resolved or packaged. No replacement HTTP client, fork, patch, updater,
  writable data store, or runtime network path was added.
- **Behavior and drift evidence:** Jobs now uses
  `TimeZoneInfo.TimeZoneDatabase`; known/unknown IANA validation, the
  America/Los_Angeles 2026 spring gap, and the first occurrence of the 2026
  fall ambiguity are explicit contracts. The focused schedule suite passed
  11/0, the combined schedule/scheduler/golden suite passed 21/0, Jobs passed
  25/0, email passed 1/0, and the gate-topology TDD cycle moved from the
  expected 3 failures to 25/0. Drift review corrected the planned command name
  from nonexistent `mix deps.audit` to Hex 2.5's built-in `mix hex.audit`,
  upgraded the local Hex archive from 2.4.1 to 2.5.1, and made advisory
  auditing the first commit/prepush/release phase plus a setup and binary-
  release-CI step. M12.3 subsequently wrapped project-owned calls with an
  enforced Hex-version floor while retaining the raw binary-CI call.
  The first production package probe also exposed stale pre-migration
  directories under `_build/prod`; `MIX_ENV=prod mix clean --deps` followed by
  a clean rebuild remediated them rather than accepting a lock-only proof.
- **Release evidence:** forced warnings-as-errors compilation generated 1,026
  core and 59 web files with no warnings; `mix precommit`, `release.v1`, and
  `release.v11` passed. The authoritative `mix allbert.test release` passed all
  12 phases in 3,859 seconds: Hex advisory audit, compilation, unused-dependency
  check, format, Credo, partitioned local coverage, 613/0 external-runtime
  tests, 361/0 security evals, 302/0 web tests, 198/0 StockSage tests, 35/0
  channel-plugin tests, and Dialyzer with zero errors. M12.2 therefore clears
  the final remediation barrier and rejoins at post-implementation audit. The
  clean production release contains `time_zone_info-0.7.15` and none of the
  removed package directories; an offline release eval starts TimeZoneInfo and
  reports IANA 2026c.

### M12.3 — post-implementation audit remediation and validation handoff (complete 2026-07-23)

- **Audit trigger:** independent Arch Linux validation at M12.2 HEAD passed
  project-owned warnings-as-errors compilation, schedule tests, `release.v11`,
  the clean Hex 2.5.1 advisory audit, and a clean production package containing
  only TimeZoneInfo 0.7.15 with bundled IANA 2026c. A from-clean dependency
  compile also exposed Elixir 1.20 warnings owned by current upstream packages.
  The post-implementation audit found that local gates document Hex 2.5+ but do
  not enforce it; Hex 2.4 can return a false-green retirement-only audit.
- **Security remediation:** add one project-owned Mix task that rejects Hex
  versions below 2.5.0 before delegating to Hex's built-in advisory audit.
  Replace local setup, commit, prepush, release, and frozen v1/v11 gate calls
  with that wrapper. Binary CI retains raw `mix hex.audit` immediately after
  installing current Hex. Tests must prove old/missing/current Hex behavior,
  audit delegation, nonzero propagation, and exact gate topology.
- **Dependency-warning disposition:** inventory every Arch Elixir 1.20 warning
  by package/version, warning class, and production reachability. Direct
  dependencies are already at their latest compatible releases except the
  intentionally scoped-out Phoenix LiveView 1.2 feature line. Do not maintain
  dependency patches or widen v1.1 into a major dependency migration. Project
  warnings remain blockers; current dependency warnings require documented
  disposition plus focused production-path smoke, with WebSockex-backed
  Discord/Slack connectivity treated as the highest-risk live path.
- **Workstreams and barriers:** W1 adds red unit/topology tests for the audit
  wrapper. W2 reconciles the plan, request-flow, roadmap status, Arch package
  evidence, and warning inventory. After the red-test barrier, W3 implements
  and wires the wrapper while W4 checks direct/transitive version drift and
  production reachability. Rejoin for focused tests, from-clean graph/package
  proofs, warning classification, docs drift, and release-gate evidence.
- **Gates and rejoin:** require wrapper-focused tests, gate-topology tests,
  clean current-Hex audit, dependency graph/unused checks, warnings-as-errors
  project compilation, formatter, strict Credo, docs gate, `mix precommit`,
  `release.v1`, `release.v11`, and the authoritative release gate if production
  code changes. Record exact Arch and local evidence before the implementation
  commit. A green M12.3 is the serial barrier into the warm TUI/Web, channel,
  packaged-cancellation, and binary-publication operator matrix; it does not
  mark v1.1 shipped or cut any Purpose outcome.
- **Implementation:** added `mix allbert.hex_audit`, which compares the
  installed Hex version semantically against 2.5.0, fails with an actionable
  `mix local.hex --force` instruction when Hex is missing or old, and delegates
  the advisory query and exit status to Hex's built-in `mix hex.audit` in a
  child Mix process. Project-owned setup, commit, prepush, release, frozen-v1,
  and derived-v1.1 gates use the wrapper. Binary release CI intentionally
  retains raw `mix hex.audit` immediately after installing current Hex. The
  task is classified as an internal Mix-only CLI command, not a packaged
  operator surface.
- **TDD and drift remediation:** the wrapper began at 3 tests / 3 expected
  failures, then passed 3 / 0 for missing/old Hex, the 2.5.0 floor and newer
  releases, delegated arguments, and nonzero propagation. Gate-topology and
  CLI-census coverage joined at 37 / 0. Real execution initially exposed that
  in-process task lookup could not see Hex's archive task; delegation moved to
  a child Mix process. Review then removed a source-tree-derived working
  directory, corrected a Credo false positive around the literal task name,
  added the no-return contract Dialyzer required, and classified the task in
  the authoritative CLI census. Every correction was followed by its focused
  suite and the affected gate.
- **Arch and dependency-warning disposition:** independent Arch Linux evidence
  at M12.2 HEAD used Hex 2.5.1 and Elixir 1.20.2 / OTP 29: the advisory audit,
  11 schedule tests, `release.v11`, and a clean production package all passed;
  the package contained TimeZoneInfo 0.7.15/IANA 2026c and no Hackney/Tzdata.
  A from-clean dependency compile identified upstream Elixir 1.20 diagnostics,
  not project-source regressions. WebSockex 0.5.1 (Discord/Slack sockets) owns
  unpinned bitstring-size warnings and is the highest-risk reachable path;
  Jido AI 2.2.0 (generation), Hermes MCP 0.14.1 (MCP), Phoenix LiveView,
  Gettext, and DNSCluster own type/dead-code diagnostics on reachable paths;
  optional Lua/Postgrex/public-key branches own missing-optional-module or
  compile-time diagnostics. Compatible direct dependencies are current;
  Phoenix LiveView 1.2 is an intentionally out-of-scope feature-line upgrade.
  Allbert will neither patch nor fork these dependencies for v1.1. Project-
  owned warnings remain release blockers; upstream warnings remain documented
  compatibility debt and require their production-path/operator smokes.
- **Final gates and handoff:** real Hex 2.5.1 advisory execution reports no
  retired or security-advisory packages. `mix precommit`, `release.v1`, and
  `release.v11` pass; forced compilation generated 1,027 core and 59 web files
  without project warnings; Credo is clean and Dialyzer reports zero errors.
  After the CLI-census drift correction, the authoritative 12-phase release
  gate passed in 3,946 seconds: audit 2 s, compile 11 s, format 3 s, Credo 5 s,
  high coverage 406 s, external runtime 613 / 0 in 722 s (12 skipped), security
  evaluation 361 / 0 in 339 s, web 302 / 0 in 2,275 s, StockSage 198 / 0 in
  160 s, channel plugins 35 / 0 in 9 s, and Dialyzer 0 in 14 s. M12.3 clears
  the serial barrier into overall operator validation without deferring a
  Purpose outcome or creating a future-features intake candidate.

### M12.4 — packaged cancellation rehearsal (superseded and removed 2026-07-23)

- This milestone incorrectly turned release validation into a production
  capability by adding a registered action, CLI command, fixed process
  fixtures, confirmation rendering, and intent/test inventory solely to
  manufacture packaged-host proof. M12.11 removes that complete seam.
- The actual ADR 0085 cancellation substrate and its normal
  `ProcessOwner`, process-group, delegate-cancel, objective-cancel, and
  recovery tests remain the product proof. The unrelated UUID-based dynamic
  staging-root correction discovered during the abandoned gate run also
  remains because it fixes a real cross-VM collision.
### M12.5 — security-evidence integrity and ADR reconciliation (completed 2026-07-23)

- **Trigger and acceptance bar:** the final audit found that request-flow §I's
  eleven behavioral authority/deny rows do not exist. The implemented `:v11`
  rows use different ids, structural presence/source-string checks, `:allowed`
  throughout, and one identical generic AssertBinding triple. This does not
  satisfy M10's promise that every new authority/deny path is behaviorally
  bound. ADRs 0083–0085 are Accepted but absent from the ADR index while the
  closeout checkbox claims indexing is complete.
- **Implementation:** replace the current eleven inventory ids with the exact
  canonical §I ids and expected outcomes. Each owning test must execute the
  production boundary and bind distinct assertion atoms to its observed
  behavior; source greps, name presence, and `function_exported?/3` cannot be
  the decisive proof. Add a meta-contract rejecting reused generic binding
  sets across unrelated v1.1 rows. Index ADRs 0083–0085 and correct closeout
  status.
- **Workstreams/barriers:** W1 adds red exact-inventory, per-row behavior, and
  binding-specificity tests; W2 maps each row to existing focused fixtures.
  After the red barrier W3 implements missing scenarios while W4 reconciles
  ADR/request-flow terminology. Security evals remain the single
  `security_eval_serial` barrier. Rejoin after all eleven rows and their
  underlying notify, receipt, ownership, cancellation, and confirmation suites
  pass.
- **Gates/rejoin:** focused `v11_sweep_eval_test`, inventory/meta-tests, relevant
  production-boundary suites, docs gate, formatter, warnings-as-errors, Credo,
  Dialyzer, `release.v1`, and `release.v11`; document exact row-to-proof evidence,
  drift, and metrics before the milestone commit/push.
- **Build evidence:** the inventory now uses the exact eleven §I ids and expected
  outcomes, with eleven distinct three-assertion binding sets. The owning sweep
  executes durable fan-out confirmation, Settings-backed notify suppression,
  identity-mapped notify delivery, pre-transport redaction, objective ownership,
  kickoff/report receipt identity, exact origin-account binding, typed consent,
  and the packaged process-group cancellation proof. The intentional red barrier
  failed all 12 old tests after the canonical inventory replacement; the green
  sweep passes 12 / 0, and the joined notify/ack/steering/cancellation suites
  pass. The docs gate, warning-free compile, strict Credo, `mix precommit`,
  Dialyzer (zero errors), frozen `release.v1`, and derived `release.v11` pass.
  ADR index drift extended beyond the audit finding: ADR
  0086 also existed but was absent, so the current high-impact index now links
  ADRs 0083–0086 using their actual titles and filenames.

### M12.6 — acknowledgement capability and cancellation-setting convergence (completed 2026-07-23)

- **Trigger:** all current production caller families acknowledge delivery, but
  `Runtime.submit_user_input/1` cannot distinguish a caller that implements the
  contract. This misses Locked Decision 17's required absent-capability
  fail-closed `:single` behavior. Separately, `execution.cancel.grace_ms`
  controls the cooperative wait while ProcessOwner silently retains its own
  compiled TERM→KILL default unless a caller overrides it.
- **Implementation:** add a closed explicit delivery-ack capability to runtime
  requests. Automatic/explicit fan-out requires it; absent, false, malformed,
  or unsupported capability returns the existing single-turn path before
  framing. Wire and census all production callers, with a synthetic unadapted
  caller deny proof. Resolve the pinned Settings Central cancellation grace
  into normal ProcessOwner spawns while retaining explicit bounded test/proof
  overrides; do not introduce a second operator setting.
- **Workstreams/barriers:** W1 red-tests caller absence/malformed declarations
  and ProcessOwner grace resolution; W2 inventories every direct Runtime caller.
  W3 wires the runtime gate/callers, W4 converges cancellation settings. Rejoin
  through DB/runtime, app-env, and `external_runtime_serial` barriers; no caller
  family may be inferred from source grep alone.
- **Gates/rejoin:** Runtime acknowledgement/caller matrix, all channel adapters,
  CLI/TUI/web, Jobs, ACP/OpenAI, ProcessOwner, delegate/cancellation/M12.4 proof,
  packaged proof smoke, static gates, `release.v1`, `release.v11`, and Dialyzer.
- **Build evidence:** Runtime normalizes the exact closed string capability
  `fanout_delivery_ack_v1`; missing, boolean, atom-lookalike, malformed, and
  wrong-version values remain on the single-turn path before decomposition or
  framing. All nine channel surfaces, web, CLI and Mix ask, three simulations,
  Jobs, ACP, and OpenAI mappings declare the capability at request construction
  and retain their existing delivery acknowledgement/failure boundaries. The
  synthetic unadapted-caller matrix passes, as do the channel, CLI, Jobs,
  public-protocol, web, receipt, security, and process-owner focused suites.
  ProcessOwner now resolves `execution.cancel.grace_ms` once during spawn;
  explicit bounded proof/test overrides retain precedence. No setting, public
  protocol field, or authority class was added. Drift review found and fixed
  one formatter finding and two strict-Credo alias-order findings; the clean
  rerun passes `mix precommit`, Dialyzer with zero errors, frozen `release.v1`,
  and derived `release.v11`.

### M12.7 — objective-specific in-shell deep links (completed 2026-07-23)

- **Trigger:** ObjectiveLive supports one-objective rendering, but chat chips
  patch only to the generic `workspace:objectives` index. S8 explicitly requires
  every objective chip to restore the objective-specific in-shell destination.
- **Implementation:** extend the workspace objectives destination with a
  bounded objective parameter, point each named chat chip to its objective,
  retain the generic count/overflow index affordance, and fail safely for
  malformed, missing, or foreign ids. The LiveView remains render/dispatch only.
- **Workstreams/barriers:** W1 red component/path/reload/ownership tests; W2
  implements destination parsing and chip links; W3 updates browser evidence
  and accessibility labels. Rejoin at the LiveView/Repo serial barrier.
- **Gates/rejoin:** focused chat component, shell navigation, ObjectiveLive and
  ownership tests; web lane; browser chip→exact objective→thread walkthrough;
  `release.v1` and `release.v11` before commit/push.
- **Build evidence:** each named objective chip now patches to
  `workspace:objectives` with its exact `objective_id`; the count and overflow
  affordances remain generic. WorkspaceLive bounds the parameter to 160 bytes,
  re-proves the current user with `Objectives.get_objective/2`, and passes only
  an owned objective into the catalog renderer. The existing objective card then
  renders its title, objective text, status, and copyable id inside the shell.
  Missing, oversized, and foreign ids retain the objectives destination but
  render the generic index. Focused chip, reload/path, ownership, shell,
  ObjectiveLive, ObjectivesLive, catalog, renderer, and accessibility tests pass.
  `mix precommit`, Dialyzer (zero errors), frozen `release.v1`, and derived
  `release.v11` also pass.

### M12.8 — steering, decomposition, and catalog evidence hardening (completed 2026-07-23)

- **Trigger:** the 150-row steering gate is five repeated templates and proves
  aggregate accuracy plus cancel precision, not the required per-class macro
  precision/recall or real ambiguous multi-target cases. `adjust` was folded
  into `steer` without independent measurement. Build Progress retry-safety
  counts are stale, model-proposer proof is narrow, and redactor naming drifts.
- **Operator semantics:** `adjust` refines how a child pursues its existing goal
  (for example, add citations or shorten output); `steer` redirects what it is
  pursuing. Both execute through the same owned `steer_objective_run` action
  with identical authority and confirmation posture. The distinct label exists
  for useful acknowledgements, audit history, clarification, and independent
  accuracy measurement—it grants no new permission.
- **Implementation:** commit a reviewable corpus with at least 25 each for
  steer/adjust/cancel/status/new-request plus genuine ambiguous multi-target,
  unknown-target, confirmation-text, cross-thread, and cross-user negatives.
  Calculate per-class precision/recall and macro floors, cancel precision, and
  zero approval false positives. Add fixture-driven model proposer valid,
  malformed, low-confidence, over-limit, nested, and privileged-action cases.
  Replace prose census guesses with a gate-produced retry-safety inventory and
  reconcile the sanctioned Runtime redactor terminology.
- **Workstreams/barriers:** corpus/metrics and model fixtures proceed separately;
  classification behavior rejoins through the DB/global-process lanes, followed
  by intent regression and security evaluation. Live Ollama remains a smoke,
  never a deterministic gate.
- **Gates/rejoin:** steering/fan-out steering/decomposer/caller-family suites,
  intent corpus/golden regressions, security rows, live Ollama smoke when local,
  static/docs gates, `release.v1`, `release.v11`, and Dialyzer.
- **Build evidence:** `adjust` is now a distinct deterministic/model-assist
  classification and is measured separately, while `handle/1` dispatches it
  through the unchanged owned `steer_objective_run` action. The committed
  150-row corpus contains 25 each for status/cancel/steer/adjust, 25 ordinary
  new requests, and 25 ambiguous, unknown-target, confirmation-text, and
  unrelated negatives; all 150 texts are unique. Per-class precision/recall,
  macro precision/recall, cancel precision, and zero confirmation-approval
  false positives are gate assertions. Multiple ordinal targets now clarify
  instead of silently selecting the first. Cross-thread and cross-user targets
  remain untouched in the behavior suite. The model-proposer matrix covers
  valid, malformed, low-confidence-shaped, mixed-type, over-limit, nested, and
  privileged-looking task output; model text remains advisory. A local
  `llama3.2:3b` smoke returned the two requested task objects but also ignored
  the output-only instruction and drafted content, reinforcing (not replacing)
  the strict structured/bounded parser and fail-to-single contract. The
  capability-catalog gate generated `safe=94 unsafe=0 unknown=131 total=225`.
  Documentation now consistently names the sanctioned `Runtime.Redactor`.
  The 28-test intent/security rejoin, registry/decomposer gates, `mix precommit`,
  Dialyzer (zero errors), frozen `release.v1`, and derived `release.v11` pass.

### M12.9 — holistic audit rejoin and operator-validation handoff (candidate 2026-07-23)

- Re-audit every Purpose outcome, Locked Decision, ADR 0083–0085 boundary,
  request-flow claim, caller/surface matrix, and the seven mandatory findings
  against final code. Record dispositions; no scope deferral is implicit.
- Reconcile M12.4 evidence and retain the UUID dynamic-staging remediation found
  during release testing. Run focused gates first, then `mix precommit`,
  Dialyzer, `release.v1`, `release.v11`, a fresh production release with all
  three packaged cancellation modes, and one uninterrupted authoritative
  `mix allbert.test release` at the final SHA.
- Update Build Progress, request-flow, roadmap, exact numbered OV commands, and
  release evidence only from that SHA; commit/push the rejoin. Operator
  validation opens at OV-00 only after a clean worktree, upstream parity, and
  every automated gate above is green.
- **Holistic disposition:** every Purpose outcome remains implemented; no scope
  was deferred. ADR 0083 fan-out durability/authority, ADR 0084 default-off
  autonomous notify, and ADR 0085 owned cancellation remain behavior-bound.
  All nine channels plus CLI/web/Jobs/ACP/OpenAI declare and implement the
  delivery-ack contract. The seven audit findings close as: canonical security
  rows + ADR index (M12.5), explicit caller capability + one cancellation grace
  (M12.6), objective-specific owned shell links (M12.7), varied steering metrics
  with distinct adjust + bounded proposer + generated retry census + redactor
  terminology (M12.8), and this final evidence rejoin (M12.9). M12.4's dynamic
  staging UUID remediation remains present.
- **Candidate production evidence:** a fresh 1.1.0 production release assembled
  successfully with deterministic web assets and staged shipped plugins. Its
  packaged `cancel` and `timeout` proofs report PASS; macOS `session-escape`
  reports the documented `UNSUPPORTED/setsid_unavailable` boundary with cleanup
  complete. The source-focused, precommit, Dialyzer, frozen-v1, and derived-v1.1
  barriers were green in M12.5–M12.8. The only remaining automated acceptance
  action is one uninterrupted `mix allbert.test release` at the pushed M12.9
  candidate SHA; a failure re-blocks OV-00 and requires remediation/rejoin.
- **Candidate-gate drift:** the first authoritative run at `c519909b` stopped
  in high-coverage fast-local because the lane-manifest snapshot still allowed
  only the three onboarding dual-lane tests. M12.5 intentionally made
  `fanout-cancel-kill-scope-001` both `security_eval_serial` and
  `external_runtime_serial`; the focused sweep had passed, but the exact
  manifest contract had not been reconciled. The contract now names that one
  bounded fourth row explicitly. A redundant external-runtime module tag on
  the ProcessOwner DataCase was also removed; its DataCase lane declaration
  remains. Focused manifest and ProcessOwner gates must pass before a
  replacement candidate is pushed and the authoritative gate restarts.
- **Replacement-candidate drift:** the authoritative run at `b60ac9cd` passed
  high-coverage fast-local (including the reconciled four-row dual-lane
  manifest), 619 external-runtime tests, and 349 security evals before the
  full web suite found one stale pre-M12.7 navigation assertion. Production
  correctly patched a named owned objective to `workspace:objectives` with
  its bounded `objective_id`; the older shell test still expected only the
  generic destination. Reconcile that expectation with the M12.7 deep-link
  contract, run the focused shell test and precommit, then push a replacement
  candidate and restart the authoritative gate from its first phase.
- **Final candidate and first validation-Home correction:** replacement SHA
  `66820c46` passed the uninterrupted 12-phase authoritative release gate in
  3,708 seconds. The subsequent operator-runbook audit found that OV-00 backed
  up but then mutated the configured `~/.allbert`. OV-00 through OV-11 now run
  against an isolated Home; OV-12 uses a separate disposable Home per host.
  Initial operator execution then proved that cloning still imported stale
  database/settings/channel state and weakened clean-install evidence. The
  corrected OV-00 creates an empty permission-restricted Home, copies only the
  repository `.env` as a mode-600 credential input, re-pins every durable path
  override after sourcing it, and requires all migrations, settings, encrypted
  secrets, and identity maps to be created explicitly. The numbered rows pin
  process exclusivity, evidence paths, failure retention, success cleanup, and
  deletion guards; no runtime state is read from or copied out of the configured
  Home. A follow-up operator-shell audit found that `set -eu` and explicit
  `exit` paths could close the operator's terminal; a nested-shell mitigation
  was not reliable under the operator's terminal profile. The corrected matrix
  never enables `errexit`/`nounset` in an interactive shell, uses explicit
  PASS/STOP boundaries, and reserves fail-fast behavior for bounded OV-12
  subshell execution only.

### M12.10 — executable operator-validation harness (superseded and removed 2026-07-23)

- The shell harness, helper scripts, harness-only tests, state pointers,
  evidence choreography, and OV-00–OV-12 workflow are removed by M12.11.
  They duplicated automated release gates and obscured the v1.1 fan-out
  acceptance target.
### M12.10.1 — nested-gate root isolation (partially retained 2026-07-23)

- Stale-pointer recovery and harness restart behavior are removed with M12.10.
  The small automated-gate correction remains: every release-gate child
  process receives a gate-owned Home, database, and XDG roots while inherited
  Settings, Memory, Artifacts, plugin, and vault overrides are unset. Those
  subsystems cascade beneath the gate Home unless a test installs its own
  narrower root. This protects a real operator Home without defeating
  per-test isolation and is independent of manual validation.
### M12.11 — restore Purpose-focused validation and remove validation scaffolding (complete 2026-07-23)

- **Corrective trigger:** the seven commits from `5f1294a8` through
  `cf41cd64` centered on constructing and repairing OV-00–OV-12 rather than
  exercising the v1.1 flagship. A deeper audit also found M12.4 had added a
  registered production action, runtime service, CLI command, confirmation
  renderer, and fixtures whose sole purpose was packaged cancellation evidence.
  Validation infrastructure must not become a shipped product capability.
- **Remove:** delete the executable v1.1 harness, its three helper scripts and
  harness test suite; remove the `release_cancellation_proof` action,
  `CancellationProof` diagnostic service, `allbert admin cancellation-proof`
  CLI surface, confirmation formatting, registrations, intent fixture, and
  proof-only tests and their manifest rows; retire M12.4/M12.9/M12.10 operator
  procedures that exist only to drive those facilities. The service-worker
  gzip bytes are left untouched after verifying that their decompressed
  content is identical; another byte-only rewrite would create more noise.
- **Retain:** keep the ADR 0085 cancellation substrate and normal focused
  tests; M12.5 security bindings; M12.6 caller-capability propagation; M12.7
  objective links; M12.8 steering/decomposition corrections; M12.9 lane/link
  reconciliation; the UUID dynamic-staging isolation correction; and the
  schema-aware Settings CLI fix for numeric-looking strings. Keep one small
  gate-owned-root regression so automated tests cannot inherit real Settings,
  Memory, Artifact, database, plugin, vault, or XDG roots.
- **Operator acceptance:** replace OV-00–OV-12 with four feature rows: one
  disposable configured Home, one warm TUI fan-out/steer/cancel/join/restart
  scenario, the equivalent Web tree/control scenario, and one configured real
  edit-capable remote channel proving exact-origin reply steering plus
  autonomous-notify OFF and explicit opt-in ON. Automated `release.v11` remains
  authoritative for all-nine-surface parity, Email completion-only behavior,
  OpenAI/ACP continuation, confirmation security, and process-tree boundaries.
- **Barriers and rejoin:** focused Registry/CLI/cancellation/settings/gate-root/
  fan-out/steering tests, manifest reconciliation, shell/reference scans,
  formatter, warnings-as-errors, strict Credo, docs, precommit, `release.v1`,
  `release.v11`, and one final automated authoritative release gate. No harness,
  packaged proof, or broad manual platform revalidation remains. After the
  corrective commit is pushed, the operator starts the four-row fan-out
  walkthrough at its setup row.
- **Completion evidence:** removed more than 3,400 lines of validation-only product,
  scripts, tests, and runbook machinery; regenerated the 3,361-row test
  manifest; and left the real cancellation and fan-out implementation intact.
  Focused corrective coverage passed 81 / 0, the gate-root/notification
  regression passed 46 / 0, `mix precommit`, frozen `release.v1`, and derived
  `release.v11` passed. The final authoritative release gate passed in 3,765
  seconds: high coverage 382 s; external runtime 614 / 0 in 683 s (12
  intentional skips); security evaluations 349 / 0 in 316 s; Web 303 / 0 in
  2,194 s; StockSage 198 / 0 in 149 s; channel plugins 35 / 0 in 9 s; and
  Dialyzer zero errors in 12 s. Evidence:
  `release-2026-07-24T05_37_44Z.json`.

### M12.12 — deterministic flagship decomposition and result-bearing fan-in (complete 2026-07-24)

- **Corrective trigger:** the focused TUI walkthrough proved that fan-out starts
  three child runs concurrently, status is durable, steering is queued/applied,
  cancellation affects only its target, the join barrier is terminal, default-OFF
  notification waits for the next turn, and unrelated input stays a new request.
  It also exposed two release-blocking Purpose failures: the exact J01.1 prompt
  decomposes into a combined research/notes child, a summary child, and a bogus
  `report back` child; and the next-turn join renders only child titles/status
  glyphs even though the durable report already carries each child's
  `result_summary` and `review_reason`.
- **Scope:** repair only the v1.1 flagship contract. The exact acceptance prompt
  must frame research-homepage-title, notes-roots, and thread-summary as three
  children, with response choreography excluded from child work. Fan-in must
  enumerate every child with a bounded, redacted, truthful result or terminal
  reason. A steered child must report the result of its effective steered
  objective. TUI multiline input, partially typed-input interleaving, progress
  noise, and arrow-key rendering are explicitly outside this corrective
  milestone by operator direction; no validation script or product-only
  validation capability is added.
- **TDD workstreams and serial barriers:** W1 adds one public Decomposer
  regression for J01.1 and minimally repairs count-guided list decomposition.
  W2 adds Runtime-level next-turn report regressions for completed summaries and
  truthful non-success reasons, then minimally repairs rendering. W3 exercises
  public steering/cancellation APIs through terminal join and proves the report
  reflects the effective child result. W1 may proceed independently; W2 and W3
  rejoin at the Runtime/Fanout DB-serial barrier. Each slice runs red → green →
  refactor before the next slice.
- **Drift and validation rejoin:** re-read ADR 0083's enumerate-every-child rule,
  ADR 0084's default-OFF pending-report path, ADR 0085's truthful cancellation
  rule, the Purpose, all pending-report renderers, and §J after implementation.
  Reconcile any alternate public renderer that still omits results. Replace the
  race-prone J01 manual choreography with one local-model, one-line focused flow
  whose tasks are long enough to steer/cancel, while retaining the exact original
  J01.1 as an automated decomposition regression. Do not create or restore a
  validation harness.
- **Gates and handoff:** focused Decomposer, Fanout, FanoutSteering, Runtime
  fan-out acknowledgement/report, intent-routing, and public-protocol fan-out
  tests first; then formatter, warnings-as-errors, strict Credo, docs,
  `mix precommit`, Dialyzer, frozen `release.v1`, derived `release.v11`, and one
  final authoritative `mix allbert.test release` after drift remediation. Record
  final-SHA evidence in Build Progress, commit/push only changed files, and reopen
  operator validation only when every barrier is green.
- **Implementation and drift evidence:** the counted-list deterministic path now
  extracts the exact three J01.1 tasks and excludes report choreography. Steering
  updates the bounded child title alongside its effective objective. One shared
  Fanout detail rule selects completed results or non-success review reasons,
  redacts and bounds them to 500 characters, and is consumed by Runtime
  next-turn reports, autonomous channel completion, ACP, and OpenAI. Drift review
  found and closed the autonomous-notify title-only variant; ACP/OpenAI already
  emitted successful summaries but now gain the same non-success/fallback rule.
  The focused cross-surface rejoin passed 68 tests, 0 failures; formatter,
  warnings-as-errors, strict Credo, docs, precommit, Dialyzer (zero errors),
  frozen `release.v1`, and derived `release.v11` are green. At implementation
  SHA `68a8c451`, the uninterrupted authoritative release gate passed in 3,966
  seconds: high coverage 389 s; external runtime 614 / 0 in 730 s (12
  intentional skips); security evaluations 349 / 0 in 338 s; Web 303 / 0 in
  2,305 s; StockSage 198 / 0 in 159 s; channel plugins 35 / 0 in 9 s; and
  Dialyzer zero errors in 14 s. Evidence:
  `release-2026-07-24T15_35_37Z.json`. M12.12 clears the automated barrier and
  reopens the focused FV-01 operator walkthrough.
- **Arch Linux compiler observation:** an operator production build on Elixir
  1.20.2 / OTP 29 completed successfully, including the browser-runtime
  boundary, with no warnings from `allbert_assist` or `allbert_assist_web`.
  The reported warnings came from upstream dependencies under Elixir 1.20's
  stricter diagnostics. WebSockEx's unpinned bitstring-size variables and the
  deprecated `ParallelCompiler.async/1` / Mix `xref` forms are future compiler
  compatibility debt; optional Lua, Postgrex, and compile-order module warnings
  are expected for absent optional features. WebSockEx 0.5.1 is already the
  latest published release, so this milestone does not vendor a patch, suppress
  dependency warnings globally, or expand beyond the fan-out correction.
  Re-evaluate clean upstream upgrades, including the available LiveView 1.1
  patch line, in the next bounded dependency refresh.

### M12.13 — bounded lifecycle completion and effective live steering (complete 2026-07-24)

- **Corrective trigger and durable evidence:** the first post-M12.12 FV-01 run
  used a disposable Allbert Home and real local Ollama `llama3.2:3b`. Kickoff
  returned in 0.56–0.58 s, steering in 0.42 s, and durable status in 0.27 s,
  proving the asynchronous framing/control spine. Child
  `obj_7b29640e-4d74-438c-a75d-9ea85a256afb` nevertheless recorded 20
  `run_started` attempts after each execution reached `advance`; its long result
  exceeded the objective's 2,000-character durable-summary constraint,
  completion persistence left it `running`, and Coordinator's normal-terminal
  path bypassed the one-restart ceiling. It eventually failed
  `retry_exhausted: :missing_durable_observation`, and parent
  `fanout_110babc2-5faf-4be9-99a9-3d6b21343f8d` joined `partial` after about
  7.5 minutes. A directive received while child 1's proposal was in flight
  changed its objective/title but not its already-created `direct_answer`
  parameters, so execution used the old task and returned `No action was
  taken.` Premature `show the completed fan-out report` messages were routed to
  `preview_plan`, exposed `:missing_plan_source`, and consumed about 15 s each.
- **Acceptance boundary:** (1) redacted durable child summaries are bounded
  before persistence and a long successful result completes exactly once;
  (2) every worker terminal result is reconciled against durable state and no
  path can exceed ADR 0083's one automatic restart; (3) steering received
  during proposal causes the executable step to reflect the effective steered
  objective before authorization/execution; (4) an explicit fan-out-report
  request is a control message—while active it returns durable status, after
  join it returns the pending report, and it never enters an unrelated intent
  action or prints an internal atom; (5) unrelated foreground requests remain
  independent. `ALLBERT_TUI_LOG_LEVEL` continues to govern Logger only;
  attended fan-out lifecycle lines are product status, not logs. The retry fix
  must remove the pathological status flood without hiding required start,
  control, terminal, and join evidence.
- **TDD workstreams and serial barriers:** W1 (`db_serial`) adds a public
  Lifecycle/Coordinator regression whose action returns more than 2,000
  characters, then bounds completion storage and reconciles `run_terminal`
  errors through the retry-safety ceiling. It separately kills/fails terminal
  persistence and proves at most two attempts. W2 (`db_serial`) queues steering
  while proposal is blocked, releases proposal, and proves the action executed
  from the new objective and the joined report carries that result. W3
  (`db_serial` Runtime/TUI) exercises the report phrase before and after join
  and proves no `preview_plan` selection or raw reason. These workstreams may
  develop independently but rejoin serially across Repo, the global SignalBus,
  Scheduler, Registry, and named run supervisors. Each behavior lands as one
  red → green → refactor vertical slice.
- **Drift review and remediation:** inspect every `run_terminal`, `:DOWN`, boot
  reconciliation, and `maybe_join` path against ADR 0083 Decisions 3–5; inspect
  every steering boundary for stale steps/action parameters; inspect Runtime,
  OpenAI, ACP, autonomous notify, Web, and TUI pending-report consumers for the
  same control/report semantics. Preserve cancellation truth and default-OFF
  reporting. Do not add a validation harness, local dependency patch, or TUI
  line-editor work. Correct the existing §J commands so pasteable prompts do
  not contain `J01-*` labels, report requests occur only at the stated active or
  joined boundary, and PASS text explicitly caps starts at two only for a
  genuinely crashed safe child—ordinary successful children start once.
- **Gates and rejoin:** focused Lifecycle, RunServer/Coordinator supervision,
  Steering/Fanout, Runtime fan-out acknowledgement/report, intent routing, and
  TUI convergence tests first; then expanded cross-surface drift tests,
  formatter, warnings-as-errors, strict Credo, docs, `mix precommit`, Dialyzer,
  frozen `release.v1`, derived `release.v11`, and one uninterrupted
  `mix allbert.test release` at the final implementation SHA. Record metrics
  and evidence, update plan/request-flow/roadmap, commit and push only changed
  files, and reopen FV-01 only after every automated barrier is green.
- **Implementation and focused evidence:** Lifecycle now redacts and bounds the
  durable objective summary independently from its smaller event excerpt;
  Coordinator treats a normal worker exit without a durable terminal objective
  as a failed terminal observation and applies the existing one-restart ceiling.
  Steering supersedes stale proposal state and replans safe/read-only work in
  the same attempt; steering observed after a possibly effectful execution
  blocks for review rather than replaying the effect. Explicit fan-out report
  requests are deterministic controls before and after join, and semicolon
  decomposition strips the orchestration prefix from child 1. Red/green tests
  cover long results, failed terminal persistence, steering during proposal,
  steering during safe and possibly effectful execution, active/joined report
  control, and the exact operator decomposition prompt. The expanded rejoin is
  green at 105 core tests plus 2 web tests; warning-as-error compilation,
  strict Credo, formatter/diff checks, and Dialyzer are green. Precommit then
  detected the newly published HIGH Bandit fragmented-WebSocket advisory;
  the bounded upstream refresh locks Bandit 1.12.1 and its resolver-selected
  Plug Crypto 2.2.0, after which Hex audit is clean. Pre-push exposed a
  text-scanner false positive because the operator prompt names `GenServer`
  and `Agent`; an explicit audited `pure_async` adjudication keeps that inert
  prompt fixture in its real lane, its focused 6-test proof is green, and the
  committed test manifest is regenerated. The rerun pre-push is green across
  all core/plugin/web groups and serial partitions, as are frozen `release.v1`
  and derived `release.v11`. At committed implementation SHA `61569b68`, the
  uninterrupted authoritative release gate passed all 12 phases in 3,802
  seconds: Hex audit, static compile, unused-dependency check, formatting,
  Credo, high-coverage fast-local, 614 external-runtime tests (12 intentional
  skips), 349 security-evaluation tests, 303 web tests, 198 StockSage tests, 35
  channel-plugin tests, and Dialyzer. Evidence:
  `release_evidence/gates/release-2026-07-24T21_08_33Z.json`. FV-01 focused
  operator validation is therefore open; this gate did not tag, build, sign,
  or publish v1.1.

### M12.14 — cancellation-terminal ordering and join truth (complete 2026-07-24)

- **Corrective trigger and durable evidence:** FV-01 against candidate
  `a0090c44` used disposable Home `allbert-v11-fanout.wDTB9w`. Child
  `obj_07b09492-2e08-40aa-a6df-0f71b2b015b8` recorded attempt 1 at
  `22:24:30.090Z`, durable `cancelled` at `22:24:44.879Z`, supervised-tier
  cancellation at `22:24:45.384Z`, and an invalid attempt 2 at
  `22:24:45.408Z`. Parent `fanout_2a45419b-77c6-4288-8c31-dfd90fd814c4`
  joined partial at `22:24:47.709Z`; the restarted child continued after join,
  completed at `22:24:55.018Z`, and overwrote its durable status to
  `completed`. This is product failure, not presentation or validation-harness
  noise. No broader manual rows were run.
- **Root contract and acceptance boundary:** supervised cancellation currently
  terminates RunServer before `Objectives.cancel/3` commits the terminal row.
  Coordinator observes `:DOWN` while the child still appears `running` and
  schedules the ADR 0083 safe-crash retry; `Lifecycle.begin_attempt/1` then
  permits a terminal objective to return to `running`. Repair the public
  cancellation boundary so cancellation intent/state is durable before worker
  termination can trigger recovery. A child durably cancelled, completed,
  failed, abandoned, or blocked cannot begin or resume a run; cancellation
  emits one terminal outcome, never consumes crash-retry budget, and the join
  occurs only after all child terminal states are stable. No child may mutate
  after its parent joins. Preserve tier reporting and scoped process cleanup
  from ADR 0085.
- **TDD workstreams and serial barriers:** W1 (`db_serial`) is one tracer-bullet
  public `CancelObjectiveRun`/Coordinator integration: hold a run beyond the
  grace period, cancel it through the registered action, and prove one start,
  durable cancelled state, no restart, truthful partial join, and no later
  mutation. W2 (`db_serial`) exercises the Lifecycle public entrypoint against
  every terminal status and proves it rejects an additional attempt without
  incrementing `run_attempt_count`. W3 (`db_serial`) covers the cooperative,
  supervised, and coordinator-`:DOWN` interleavings plus rehydration so only a
  genuinely crashed safe running child receives attempt 2. These slices run
  serially around Repo, the global run Registry/Scheduler, and DynamicSupervisor;
  implementation proceeds one red → green slice at a time.
- **Drift review and rejoin:** inspect `Cancel.cancel/2`,
  `CancelObjectiveRun.cancel_all/3`, Lifecycle begin/cancel transitions,
  Coordinator `run_terminal`/`:DOWN`/rehydration/`maybe_join`, Fanout reduction,
  and every caller that directly cancels an objective. Confirm there is no
  second cancellation authority path or post-terminal reopen path. Rejoin with
  focused cancellation/lifecycle/supervision/fan-out tests, expanded runtime
  and surface convergence tests, warnings-as-errors, formatter, strict Credo,
  docs, Hex audit, precommit, Dialyzer, prepush, frozen `release.v1`, derived
  `release.v11`, and one uninterrupted authoritative release gate at the final
  committed implementation SHA. Record evidence in this plan/request-flow and
  reopen FV-01 only after every automated barrier is green. Do not add a
  validation harness or broaden into TUI line editing.
- **Implementation and focused proof:** cancellation now commits an
  ownership-scoped, idempotent `blocked`/`cancellation_requested` transition
  before process cleanup; terminal cancellation then records the bounded
  operator reason and explicitly wakes Coordinator reconciliation. Lifecycle
  accepts new attempts only from `open` or `running`, so blocked and terminal
  rows cannot reopen. Whole-parent cancellation processes the parent before
  active children and leaves already-terminal children untouched, avoiding a
  child-derived parent join racing the action itself. Red tests reproduced the
  terminal reopen, missing durable intent, duplicate intent event, untruthful
  reason, completed-child mutation, and mid-action parent-finalization races;
  the focused lifecycle/supervision/action suite and a 20-run stress repetition
  of the mixed completed/active whole-parent case are green.
- **Automated rejoin evidence so far:** warnings-as-errors compile, formatter,
  strict Credo, Hex audit, Dialyzer, and `mix precommit` are green. The dirty-tree
  pre-push gate at plan SHA `8b1ea951` passed in 946 seconds with 2,283 tests,
  0 failures, and 9 intentional exclusions. This is implementation evidence,
  not release acceptance. Commit the implementation next, then run frozen
  `release.v1`, derived `release.v11`, and the uninterrupted authoritative
  release gate at that exact clean implementation SHA before reopening FV-01.
- **Committed-SHA release closure:** implementation commit `32202415` is pushed
  to `origin/main`. Frozen `release.v1` passed with evidence
  `release_evidence/v1/release-v1-1784934999.json`; derived `release.v11`
  passed its 11 authority, 22 runtime-fan-out, 14 channel-authority, and 5
  web-operator tests with evidence
  `release_evidence/v11/release-v11-1784935062.json`. The uninterrupted
  authoritative release gate passed all 12 phases in 3,790 seconds: Hex audit,
  warning-free compile, unused-dependency check, formatter, Credo,
  high-coverage fast-local, 614 external-runtime tests (12 intentional skips),
  349 security-evaluation tests, 303 web tests, 198 StockSage tests, 35 channel
  plugin tests, and Dialyzer. Evidence:
  `release_evidence/gates/release-2026-07-24T23_19_57Z.json`. FV-01 is open
  against `32202415` with a new disposable Home; this did not tag, build, sign,
  or publish v1.1.

### M12.16 — pre-effect start recovery and lossless attended input (implemented; clean-SHA `release.v11` green)

- **Corrective trigger and durable evidence:** the first FV-01 run against
  pushed candidate `a8c4b25e` used disposable Home
  `allbert-v11-fanout.BtX898`. Parent
  `fanout_877322d9-d96b-4888-a760-1bb9d00768c2` was acknowledged, but all
  three children became `blocked` with `run_attempt_count=0` and no
  `run_started` event. Two review reasons contain SQLite `database is locked`
  at `BEGIN IMMEDIATE TRANSACTION`; the third contains a closed DBConnection
  after the same contention interval. That durable state proves no lifecycle
  attempt, action, or external effect began, so `uncertain_effect` is false.
  In the same session the operator typed the exact FV-01 steer immediately,
  but the persisted next turn lost its leading `change ` and was not accepted
  until roughly 76 seconds later. The TUI currently lets the Adapter write
  lifecycle output directly while `InputDriver` independently owns prompt and
  buffer state, and resets/ignores input between prompts; that split ownership
  explains the observed loss and delay. The Home already has WAL sidecars and
  Ecto SQLite3 enables WAL by default, so adding a WAL setting is not a
  remediation.
- **Operator-experience contract:** after the kickoff text is written, the TUI
  remains ready for a steer even while child-start/status lines arrive. One
  raw-terminal owner serializes prompts, typed characters, attended lifecycle
  lines, and redraw; an asynchronous line temporarily clears the visible input
  line, writes the lifecycle line, then redraws the exact prompt and complete
  in-progress buffer. Characters typed after submitting one turn but before
  the next prompt is rendered are retained as type-ahead, not discarded.
  Delivery remains synchronous at this boundary so a report receipt is
  acknowledged only after its stdout write succeeds.
- **Execution/recovery contract:** a child still durably `open` with attempt 0
  and no `run_started` event has not crossed an effect boundary. A transient
  SQLite/DBConnection failure while establishing that first attempt releases
  its Scheduler slot and re-enters the existing capped-backoff start path; it
  must not write `run_blocked`/`uncertain_effect`. Once `run_started` commits,
  the existing retry-safety and one-restart rules remain unchanged. Unknown
  programming failures remain crash-visible rather than being relabelled as
  transient database contention.
- **TDD and drift audit:** first reproduce (1) a started RunServer reporting a
  transient database failure while its child remains attempt 0 and then
  succeeding exactly once, and (2) lifecycle output interleaved with both an
  in-progress steering buffer and pre-prompt type-ahead. Make each test red
  before changing production behavior. Then census every raw-terminal output
  path and every Coordinator `run_terminal`/`:DOWN` recovery branch so no
  bypass retains the split ownership or the false uncertain-effect decision.
  Stress a real Repo-backed three-child concurrent start and retain the failed
  disposable Home read-only as diagnostic evidence.
- **Implementation and focused evidence:** Coordinator now distinguishes the
  durable pre-effect boundary (`open`, attempt 0) from post-start recovery and
  recognizes only known transient Exqlite/DBConnection shapes through nested
  result/exit reasons. Both a lifecycle-returned SQLite lock and a monitored
  DBConnection-closed exit requeue through independent capped exponential
  delay, then commit one `run_started`, no `run_blocked`, one successful join,
  and exactly one parent join event. `InputDriver` now buffers characters while
  the Adapter is between prompts and provides a synchronous attended-write
  boundary that serializes lifecycle output with raw input, clears the visible
  line, and redraws the exact prompt plus buffer. Adapter and output-mode live
  regions use that boundary; direct raw-terminal writes were removed. Exact
  red/green regressions pass 4 / 0. Complete TUI + supervision suites pass
  64 / 0; isolated fan-out, Lifecycle, Scheduler, and Runtime caller suites
  pass 21 / 0, 12 / 0, 5 / 0, and 20 / 0 respectively. An ad hoc seven-file
  raw ExUnit run passed 122 / 0 but emitted shared SignalBridge/SQL-sandbox
  teardown noise; it is diagnostic, not accepted gate evidence, and the
  project-owned serial gate remains authoritative for topology.
- **Static/no-loss evidence:** warnings-as-errors compile, formatter, strict
  Credo (1,396 source files, zero issues), docs, Hex advisory audit, and
  Dialyzer (zero errors/skips) pass. Manifest drift first named exactly the
  three new regression identities; regeneration now contains 3,482 rows, and
  live manifest plus lane checks pass with 550 files, zero unclassified, and
  zero double-counted. The implementation/docs candidate is committed before
  `release.v11`; its generated evidence is recorded externally at that exact
  clean SHA and folded into tracked closeout docs after FV-01 so an evidence-
  only edit does not invalidate the proved candidate.
- **Focused rejoin and release timing:** run the Coordinator/supervision,
  Lifecycle/fan-out, Runtime acknowledgement, and TUI suites; warnings-as-
  errors compile, formatter, strict Credo, and docs gate; then the derived
  `MIX_ENV=test mix allbert.test release.v11` gate at one clean committed SHA.
  Commit and push only the M12.16 code/tests/docs before asking for a new
  disposable-Home FV-01. The operator has explicitly deferred the much longer
  authoritative `mix allbert.test release` run until FV-01 passes or any
  resulting focused remediation closes. That changes sequencing, not the
  release acceptance bar: authoritative release, frozen-contract, pre-push,
  binary, and external-environment checks remain mandatory before closeout.

### M12.17 — non-blocking ordered attended turns (implemented 2026-07-25)

- **Corrective trigger and durable evidence:** pushed candidate `6b00b784`
  passed clean-SHA `release.v11`, then FV-01 used disposable Home
  `allbert-v11-fanout.vsruiV`. Parent
  `fanout_ac8e4369-9165-4f44-b600-86ab0925c082` was acknowledged at
  `00:00:50.291Z`; its kickoff channel event was processed at `00:00:50.345Z`,
  so kickoff delivery itself returned promptly. The exact steering channel
  event was inserted at `00:01:06.432Z` but was not processed until
  `00:01:23.103Z`, a 16.7-second foreground turn. While that synchronous
  Adapter call was occupied, the raw `InputDriver` retained a second line but
  the Adapter could not consume it. The database contains two distinct user
  messages—the exact steer and a later steer suffixed by the operator's
  responsiveness probe—so this is not one line executed twice or an
  InputDriver buffer-clear failure. All three children nevertheless completed
  on attempt 1, the parent joined once with `completed/success`, the attached
  report was delivered, and a later `2+2` turn answered `4`.
- **Root and operator-experience contract:** `handle_auto_input/2` currently
  calls the complete ordinary `handle_text_submission/3` pipeline inside the
  TUI Adapter GenServer. Any Repo, Runtime, provider, or SQLite wait therefore
  blocks the same mailbox that must consume the next raw input line and deliver
  fan-out lifecycle signals. In raw attended mode, submitting a line must
  immediately return a fresh prompt; the line enters a bounded FIFO, and
  exactly one ordinary non-coding turn executes at a time under supervision.
  Later input is retained in order, never run concurrently with the preceding
  turn, and lifecycle output continues through the single input-aware writer
  with exact prompt/buffer redraw. The existing synchronous `Adapter.submit/3`
  API, line-mode fallback, and Pi/coding-turn correction semantics stay
  unchanged.
- **Reliability and state handoff:** the worker uses the existing channel and
  Runtime pipeline, including delivery-before-start and exact receipt ACK; it
  does not invent a second fan-out or steering path. Before an acknowledged
  kickoff can start children, its attachment delta is handed back to the
  Adapter so lifecycle signals and subsequent steering resolve against the
  same durable parent. Worker completion or monitored failure clears only that
  turn and advances the FIFO; it cannot overwrite newer Adapter attachment or
  lifecycle state. Queue admission is bounded and over-capacity input receives
  an attended rejection instead of unbounded memory growth. Normal `/quit`
  ordering and raw-terminal restoration remain deterministic.
- **TDD, drift, and gates:** first add one public Adapter/InputDriver regression
  in which a foreground Runtime turn is deliberately held, a second complete
  line is accepted with a fresh prompt, an interleaved lifecycle line redraws
  correctly, and release of the first turn proves FIFO—not parallel—Runtime
  execution. Then implement the smallest supervised turn/queue seam and add
  crash/quit/overflow coverage only where the tracer exposes a real boundary.
  Census raw versus line-mode input, Pi-mode async work, every direct Adapter
  output path, fan-out attachment/clear paths, and TaskSupervisor shutdown so
  the correction cannot create a private durable loop or leaked worker.
- **Workstreams/barriers/rejoin:** evidence reconciliation and docs can proceed
  independently; Adapter/InputDriver behavior and the DB/global-process test
  lane are serial barriers. Run the exact regression, complete TUI suite,
  Runtime fan-out acknowledgement/caller and fan-out steering suites, then
  warnings-as-errors compile, formatter, strict Credo, docs, Hex audit,
  Dialyzer, manifest/no-loss checks, and clean-SHA `release.v11`. Commit/push
  the plan before code, then commit/push only implementation/tests/docs before
  a fresh disposable-Home FV-01.3. The authoritative release gate still runs
  only after FV-01 passes and remains mandatory before release closeout.
- **Implementation and drift closure:** raw attended input now admits each
  complete line immediately, returns a fresh prompt, and executes one ordinary
  turn at a time through `AllbertAssist.TaskSupervisor`; the Adapter continues
  consuming lifecycle signals and later input. Completion transfers only the
  coding/Pi fields the worker may own, while kickoff attachment state uses an
  exact synchronous handoff after successful output and before delivery
  acknowledgement. This avoids overwriting newer lifecycle state. A queued
  line captured during `/pi` entry is re-dispatched under Pi semantics rather
  than started as an ordinary turn. Monitored failure advances the FIFO, the
  32-line bound rejects excess input visibly, and adapter shutdown terminates
  the active worker and restores raw terminal ownership. Synchronous
  `Adapter.submit/3`, non-raw line input, and the existing Pi asynchronous turn
  path remain outside the new FIFO.
- **Measured rejoin:** the six-row focused slice passes 6 / 0; complete TUI
  passes 41 / 0 at seed `401981`; Runtime fan-out acknowledgement/callers plus
  intent/fan-out steering pass 34 / 0. Warning-as-error compile (1,028 core +
  59 web files), formatter, strict Credo (1,396 files), docs, Hex audit, and
  Dialyzer are clean. The regenerated manifest contains 3,486 rows and
  reconciles 550 files with no unclassified or double-counted file. Clean-SHA
  derived `release.v11` passes all 14 steps at pushed implementation candidate
  `28ee2d86`; evidence is `release-v11-1785027587.json`. The authoritative
  release gate remains intentionally sequenced after FV-01.

### M12.19 — serialized SQLite persistence and atomic inbound admission (implemented; release.v11 rejoin green 2026-07-25)

- **Corrective trigger and durable evidence:** FV-01 against documentation
  candidate `386a6650` used disposable Home `allbert-v11-fanout.i3bJfR`.
  Parent `fanout_6e625b4d-5f65-48fd-b1f4-dcc5f76cbe1b` joined once, but the
  first steering turn hit `%Exqlite.Error{message: "Database busy"}` while
  inserting `conversation_message_refs`. The steering user message committed
  at `03:01:33.601Z`, channel event 2 remained `received`, no inbound message
  reference or `steer_directive` event committed, and child 1 completed its
  original title. The attended worker then printed the exception and stack to
  the operator. The attached report's first receipt acknowledgement exhausted
  safely and remained pending; the next `status?` response redelivered it and
  committed the sole `report_delivered` event at `03:02:28.409Z`, after which
  `2 + 2` answered `4`. M12.18's isolation/fallback therefore worked; the
  release blocker is upstream SQLite admission contention and partial-write
  semantics. The same walkthrough also showed a second operator-facing
  blocker: after the first fan-out begins printing lifecycle output, the
  visible prompt can stop accepting or reflecting input for roughly five to
  ten seconds. M12.19 treats that as an independently measured responsiveness
  defect, not as an assumed consequence of the database correction.
- **Systemic root and environment drift:** WAL allows readers beside a writer
  but only one writer exists per database file. Operator development and
  packaged production currently create five Exqlite connections with a
  15-second busy timeout, manufacturing competing Allbert writers without
  write throughput. The automated test environment instead uses a large SQL
  Sandbox pool, so derived release did not exercise the operator topology.
  Runtime user-message persistence, channel-thread binding, and provider-
  message reference are separate commits; optional-reference code normalizes
  returned errors but not raised adapter exceptions. A census of all production
  `Repo.transaction` call sites also finds registered action execution,
  confirmation-file mutation, and objective signal publication inside SQLite
  transaction lifetimes. Raising timeouts or adding receipt-only retries would
  preserve the underlying lock amplification and unacceptable stalls.
- **Persistence architecture and operator contract:** Allbert's local SQLite
  Repo has one connection in development and packaged production; the
  production `POOL_SIZE` escape hatch is removed for v1.1 correctness. WAL is
  retained for reader/writer overlap, write transactions default to
  `:immediate`, and the busy timeout returns to a short bounded interval for
  genuinely foreign/second-process contention. Parallel agents, provider
  calls, actions, rendering, and BEAM scheduling remain parallel; only short
  durable database sections serialize. Every transaction is audited so no
  provider/action call, signal publication, confirmation/YAML/file operation,
  sleep, or process wait holds the sole Repo connection. Observable signals
  publish only after commit.
- **Atomic inbound admission:** one Runtime/Conversations persistence facade
  admits the canonical user message, thread timestamp, normalized channel-
  thread binding, and provider-message reference in one immediate transaction
  using stable ids and existing uniqueness constraints. A failed transaction
  leaves none of those rows rather than the partial state found in FV-01.
  Returned and raised known database failures normalize to one bounded typed
  result; unknown programming failures stay supervised/log-visible. Channel
  adapters retain their provider-event dedupe record and mark a failed
  admission definitively rather than stranding it at `received`. TUI shows one
  concise retryable persistence message without exception structs, stack
  frames, SQL, filesystem paths, or internal atoms; its Adapter and FIFO
  continue. No raw input is added to `channel_events` or logs.
- **Transaction-effect census and remediation boundary:** inspect every current
  core/Web/plugin `Repo.transaction` site, not only fan-out. Pure Repo reads/
  writes remain inside. `Actions.Runner.run/3`, objective/channel signal
  emission, Confirmation store operations, workspace/YAML/file writes, and
  other effects move before or after the durable phase with explicit recovery
  semantics. Any discovered path whose effect cannot safely separate from its
  transaction is recorded here and blocks M12.19; it is not silently exempted.
  Frozen v1 public contracts and Security Central authority remain unchanged.
- **Vertical TDD slices:** first reproduce the public behavior through one raw
  TUI session: three concurrent child persistence streams overlap a complete
  steering line, which must be admitted once, produce one `steer_directive`,
  avoid partial conversation/channel-ref state, keep the Adapter pid, and show
  no internal error. The raw InputDriver tracer records four distinct times:
  prompt display, byte/complete-line acceptance, next-prompt display, and
  Runtime start/result. Lifecycle output may interleave, but it must not cause
  a five-to-ten-second editing/admission freeze, and a slow ordered result must
  not suppress the next usable prompt. If SQLite serialization leaves that
  symptom, profile InputDriver, Adapter mailbox/printing, BEAM scheduler load,
  and local-provider handoff and remediate the responsible path before FV-01.
  Then prove atomic rollback on an injected busy exception; one stable retry/
  idempotency case; persistent contention producing a bounded failed event/
  operator message; joined-report acknowledgement queuing behind a held short
  write without reprint; and transaction-effect separation with slow injected
  Runner/signal/confirmation collaborators proving an unrelated Repo query is
  not held. Add a production-shaped non-Sandbox SQLite test with the one-
  connection/WAL/immediate options; test config may retain its isolated Sandbox
  pool but must prove the operator config separately.
- **Workstreams, barriers, drift, and rejoin:** docs/ADR/config-contract work
  can be reviewed independently, while Repo lifecycle, channel-event,
  transaction-effect, TUI/global process, and production-shaped SQLite tests
  are serial `db_serial`/`global_process_serial`/`app_env_serial` barriers.
  Rejoin at the exact public regression, complete TUI, Conversations/channel-
  threading/Runtime, Lifecycle/Steering/Fanout, Objective Commands, autonomous
  channels, ACP/OpenAI/Web receipt suites, and an explicit transaction-callsite
  census. Then run warning-as-error compile, formatter, strict Credo, docs,
  Hex audit, Dialyzer, manifest/no-loss checks, and clean pushed-SHA
  `MIX_ENV=test mix allbert.test release.v11`. Commit/push this plan before
  code; implement/test/drift-remediate/document/commit/push before a fresh
  disposable-Home FV-01. The long authoritative release remains after
  successful flagship validation by operator sequencing.
- **Implemented correction:** development and packaged production now use one
  Repo connection with WAL, immediate transactions, and a one-second foreign-
  contention timeout. `Conversations.admit_user_message/3` generates a stable
  message id before its bounded retry loop and commits the message, thread
  timestamp, channel-thread row, and inbound provider-message row atomically.
  A known raised SQLite/DBConnection contention error becomes
  `{:inbound_admission_failed, :persistence_unavailable}`; an invalid reference
  rolls the entire admission back. The TUI marks its dedupe event `failed`,
  renders `Allbert could not save that request. Nothing was started; retry
  it.`, keeps its Adapter/FIFO alive, and never prints the exception or stack.
  CLI, Web, ACP, OpenAI, and remote channel event records use the same bounded
  classification without changing frozen public response shapes.
- **Transaction/effect correction:** the reviewed production census contains
  20 explicit `Repo.transaction` call sites across nine files and is locked by
  a source-inventory regression. Registered objective action execution now has
  a committed selection phase, executes `Actions.Runner` outside Repo, then
  commits and publishes its result. Objective lifecycle signals, workspace
  ephemeral close events, and confirmation-file cancellation also occur only
  outside their durable transactions; other audited transaction hooks contain
  Repo state transitions only. The action-boundary regression observes
  `Repo.in_transaction?() == false` from the registered action itself.
- **Responsiveness correction:** the separate five-to-ten-second prompt stall
  was an Adapter-mailbox/output storm: every duplicate `run.progress` signal
  performed an ownership read and synchronous prompt redraw before a queued
  input line. TUI now displays at most the first progress line per child run
  attempt, reopens that claim on `steered`, `resumed`, or `blocked`, and bounds
  its in-session claim set at 128. Start, steer, block, completion,
  cancellation, join, and final report output remain unsuppressed. The raw
  InputDriver regression injects 25 duplicate progress events and proves one
  redraw plus a fresh prompt within 200 ms after complete-line admission while
  the prior ordered Runtime turn remains held.
- **Rejoin evidence:** the integrated Conversations/non-Sandbox SQLite/
  Objectives/Lifecycle/Steering/Ephemeral/TUI slice passes 129 / 0; remote
  channel plus ACP/OpenAI passes 148 / 0 and Web passes 10 / 0; the post-Credo
  objective/database refactor recheck passes 30 / 0. Warning-as-error compile,
  formatter, strict Credo, docs, Hex audit, and Dialyzer are clean. Test-lane
  reconciliation reports 552 files, zero unclassified, zero double-counts,
  and a 3,500-row manifest. Clean pushed implementation candidate `2892940e`
  passes all 14 `release.v11` steps: authority 12 / 0, Runtime/Fanout 198 / 0,
  channel authority/TUI 78 / 0, and Web/operator 29 / 0, alongside the frozen
  v1 and product-RC sweeps. Evidence is `release-v11-1785038793.json`. §J
  FV-01 is reopened against a fresh disposable Home; the authoritative release
  gate remains after operator flagship validation as explicitly sequenced.
- **Operator validation — PASS (2026-07-26):** documentation candidate
  `b74a3405` used fresh disposable Home `allbert-v11-fanout.gCzHGv`. One warm
  TUI started parent `fanout_66d9028f-0ad9-4587-9857-08aa9afb37de` with three
  independent children. The operator steered child
  `obj_5bf04059-81b4-4050-9171-ef84444ef013` once to the hospital-analogy task;
  one `run.steered` appeared, all three children completed, one joined report
  contained bounded result excerpts, and the same TUI answered `2 + 2` with
  `4` before a normal `/quit`. No five-to-ten-second input freeze, database
  exception, stack, partial admission, Adapter restart, or duplicate report was
  observed. The durable post-session projection reports parent `completed`,
  phase `joined`, join outcome `success`, delivery `delivered`, three completed
  children including the steered title, and exactly `fanout_proposed`,
  `fanout_acknowledged`, `fanout_joined`, and `report_delivered` parent events.
  FV-01 therefore passes; the authoritative release cascade is next.

### M12.18 — failure-isolated report delivery acknowledgement (implemented; focused rejoin green 2026-07-25)

- **Corrective trigger and durable evidence:** FV-01 against pushed candidate
  `c8c4598e` used disposable Home `allbert-v11-fanout.ZTRr0O`. Parent
  `fanout_dfb50759-8f24-4c4b-ada1-dd53281e1441` completed and joined once;
  its exact attached report printed, but `report_delivery_state` remained
  `pending` and no `report_delivered` event committed. While that report was
  arriving, the independent `what is 2+2?` channel event was inserted and its
  user conversation row persisted, but the event remained `received` and no
  assistant row followed. The TUI then printed its startup banner a second
  time. Together with the current synchronous
  `deliver_attached_fanout/2 -> Runtime.acknowledge_report_delivery/2` call in
  the Adapter mailbox, this is evidence of an acknowledgement failure escaping
  after stdout, terminating the Adapter and its unrelated attended worker.
  The operator submitted the independent turn before the narrow walkthrough's
  requested wait-for-join point, but that is valid product use and M12.17
  explicitly promises immediate admission plus ordered completion; the
  remediation must preserve that contract rather than constrain operator
  timing.
- **Systemic root and caller census:** Ecto's current transaction contract
  rolls back and bubbles an exception; `Repo.transaction/2` does not convert
  every database exception into `{:error, reason}`. The fan-out receipt facade
  nevertheless documents only identity/receipt errors, Scheduler and
  Coordinator carry separate transient SQLite/DBConnection classifiers, and
  attached TUI report acknowledgement runs inside the Adapter after output.
  Audit every production report-receipt caller—TUI attached and next-turn,
  Web browser acknowledgement, OpenAI, ACP, CLI/channel response families, and
  autonomous NotifyConsumer—and converge them on one Runtime contract. Direct
  Fanout receipt mutation outside that contract is removed unless it is a
  persistence-level test.
- **Architecture and operator-experience contract:** add one pure shared
  database-failure classifier for the known recursive DBConnection ownership/
  connection and Exqlite busy/locked shapes; Scheduler, Coordinator, and
  delivery acknowledgement reuse it. Receipt acknowledgement remains an
  idempotent compare-and-set and receives bounded retry only for that closed
  transient class. Identity mismatch, invalid receipt, and other returned
  errors do not retry; unknown programming exceptions remain crash-visible in
  their supervised worker rather than being relabelled as contention. After a
  complete TUI report write, the Adapter records an ephemeral in-session
  delivery claim and starts one unlinked temporary child under
  `AllbertAssist.TaskSupervisor`; duplicate joined signals cannot reprint the
  report while acknowledgement is pending. Success clears the task/attachment;
  bounded exhaustion or worker failure leaves the durable receipt `pending`,
  keeps the Adapter and unrelated turns alive, and prints at most one concise
  recovery warning. If the process or VM dies after stdout but before the
  durable transition, a later turn/session may honestly redeliver the pending
  report: at-least-once delivery is retained without a false `delivered` claim.
- **No validation-shaped subsystem:** this milestone adds no table, outbox,
  Settings key, permanent retry service, polling loop, terminal harness, or
  second fan-out path. The joined parent row remains the durable report outbox;
  the worker is bounded, temporary, and reconstructible. TUI output remains
  exactly once within one live session during transient retry, while restart
  behavior remains truthfully at least once. Other surfaces keep their native
  render/transport primitives and use the same normalized receipt result.
- **Vertical TDD slices:** first reproduce the public failure: hold an ordinary
  raw-TUI Runtime turn, print an attached joined report, inject a transient
  acknowledgement exception, and prove the same Adapter pid remains alive,
  the report prints once, later input is admitted, the held turn completes,
  and the receipt eventually becomes `delivered`. Then add one slice for a
  returned transient error and duplicate joined signal; one for bounded
  exhaustion/permanent or programming failure leaving `pending` with one
  warning and no Adapter restart; and one shared-classifier/Runtime slice that
  proves raised and exited known database shapes retry while identity errors
  and unknown failures do not. Finally re-run the existing failed-output,
  two-parent, next-turn, browser, OpenAI, ACP, and NotifyConsumer receipt rows
  to catch cross-surface drift.
- **Workstreams, serial barriers, and rejoin:** plan/request-flow/ADR wording
  and the pure classifier can be reviewed independently, but the shared Repo
  receipt transition, TaskSupervisor/Adapter ownership, app-env hooks, and
  SQLite tests are serial `db_serial`/`global_process_serial` barriers. Rejoin
  at the exact public regression, complete TUI suite, Runtime fan-out
  acknowledgement/caller matrix, Objectives fan-out + Scheduler/Coordinator
  recovery suites, NotifyConsumer, WorkspaceLive, OpenAI, and ACP receipt
  suites. Then run warnings-as-errors compile, formatter, strict Credo, docs,
  Hex audit, Dialyzer, manifest/no-loss checks, and clean-SHA
  `MIX_ENV=test mix allbert.test release.v11`. Commit/push the plan before
  code, then commit/push only implementation/tests/docs before a fresh
  disposable-Home FV-01. The authoritative release gate remains deliberately
  after successful flagship validation and mandatory before closeout.
- **Implementation and systemic drift closure:** one pure
  `Database.TransientError` classifier now owns the closed recursive
  DBConnection/SQLite policy used by receipt delivery, Scheduler rehydration,
  Coordinator join reconciliation, and pre-effect start recovery. Runtime's
  four-attempt 25/50/100 ms acknowledgement retry encloses only idempotent
  receipt persistence and its identity-bound lookup; fan-out scheduling stays
  outside the retry boundary. The production caller census has no direct
  report mutation outside Runtime. Attached TUI output records a bounded
  in-session claim, then starts a task linked only to the application
  TaskSupervisor; both active acknowledgement workers and retained suppression
  claims are capped at 32. Known raised, returned, and exited database failures
  retry; identity/permanent results do not; unknown exceptions/exits remain
  crash-visible in the temporary or supervised recovery worker. Exhaustion
  warns once, preserves `pending`, and does not restart the Adapter or interrupt
  its unrelated attended FIFO. No new table, setting, daemon, polling loop, or
  validation-only runtime seam landed.
- **Focused and static evidence:** the exact raw-TUI overlap plus complete TUI
  and pure acknowledgement policy pass 47 / 0 at seed `164416`; Runtime/Fanout/
  Scheduler/Coordinator pass 70 / 0 at seed `29985`; autonomous NotifyConsumer
  passes 21 / 0 at seed `496082`; ACP passes 15 / 0 at seed `415851`; Web/
  OpenAI pass 20 / 0 at seed `798914`; and the direct Runtime caller census
  passes 3 / 0 at seed `116063`. Warning-as-error compile, formatter, strict
  Credo (1,399 source files), docs, enforced Hex 2.5 advisory audit, and
  Dialyzer are clean. The live manifest contains 3,492 rows and reconciles 551
  files with zero unclassified or double-counted tests. Clean pushed-SHA
  `release.v11` passed all 14 steps at implementation candidate `516fc7b9`;
  evidence is `release-v11-1785033817.json`. FV-01 is ready against the
  documentation-only closeout descendant; the longer authoritative release
  gate remains after successful FV-01 by explicit operator sequencing.

### M12.15 — atomic fan-in truth, recovery, and report outbox (implemented; focused rejoin green)

- **Corrective trigger and durable evidence:** the third post-remediation FV-01
  run used disposable Home `allbert-v11-fanout.U90JZu`. Parent
  `fanout_cbcec720-0bdd-46e6-9745-4ef6bb0776ea` remained `open`, with
  `join_outcome=NULL`, `report_delivery_state=not_ready`, and no
  `fanout_joined` event after all three children durably completed once. The
  child event streams contain `run_started`, progress, and `run_completed`; the
  parent contains only `fanout_proposed` and `fanout_acknowledged`. This is a
  forbidden durable state and explains the missing joined signal and repeated
  "still running" report response. The exact initiating process event cannot be
  reconstructed because the pre-M12.15 Coordinator swallowed `finalize_join/1`
  failures and stops after any terminal reduction attempt. The same Home also
  contains roughly two dozen suppressed status-delivery rows per fan-out while
  autonomous notify was OFF. Those writes overlap the final child commit and
  make a deferred SQLite read-then-write finalization lock/snapshot failure the
  evidence-backed likely trigger, but not a proven exception because it was
  swallowed. That observability failure and write amplification are both part
  of this milestone.
- **Implementation closure:** `Objectives.Fanout.TerminalTransitions` is now
  the sole production authority for active and terminal fan-out child writes.
  It uses immediate SQLite transactions and compare-and-set predicates so a
  terminal child event, deterministic sibling reduction, parent terminal
  state, stable report receipt, `report_delivery_state=pending`, and the first
  `fanout_joined` event commit together. Completion, failure, cooperative and
  registered-action cancellation, retry exhaustion, stale abandonment, and
  confirmation resumption converge there. Signals are emitted only after the
  commit. Focused tests cover concurrent last-child writers, terminal/steer
  interleavings in both serial orders and concurrently, post-join immutability,
  historical stranded-parent repair, cancellation truth, and all four join
  outcomes. A fresh RunServer start failure now releases its scheduler slot and
  retries with bounded backoff before any effect exists; it cannot be mistaken
  for an uncertain-effect worker crash and parked forever. The authoritative
  release cascade remains the only open M12.15 barrier.
- **Root correction:** fan-in is a durable projection of child terminal state,
  not a best-effort Coordinator side effect. One public Objectives/Fanout
  terminal-transition boundary atomically persists a fan-out child's terminal
  state and event, reduces its siblings, and compare-and-set finalizes the
  parent plus its single join event/report receipt when that child makes the set
  terminal. Whichever terminal transition commits last closes the parent. A
  signal or process failure after commit may delay presentation, but cannot lose
  join or report truth. This refines ADR 0083 Decision 4; it does not add a new
  authority class.
- **State invariants:** (1) terminal children are immutable and cannot gain an
  attempt; (2) a parent cannot finalize while a child is non-terminal; (3) a
  committed terminal transition cannot leave all children terminal with the
  parent `not_ready`; (4) parent status/outcome is the deterministic reduction
  of its children; (5) one parent has exactly one join event and one report
  receipt, defended by parent CAS plus upgrade-safe insert/update guards that
  preserve legacy append-only history while preventing new duplicate
  `fanout_joined` events; (6) every acknowledged `not_ready` parent is live-reconciled whether
  or not any child is active; (7) boot and live recovery select the same durable
  states; (8) signals and Coordinators remain rebuildable projections; and (9)
  an authorized completion is delivered at most once while default-OFF
  completion remains durably available on the next turn.
- **Authoritative write-path migration:** normal Lifecycle completion/failure/
  cooperative cancellation, registered-action cancellation after process
  cleanup, private Engine cancel/continue paths, Coordinator retry exhaustion,
  and stale abandonment now converge on the terminal-transition boundary or
  reject fan-out children at the incompatible interactive-engine boundary.
  Parent cancellation records intent and lets the same child reduction derive
  its final outcome; it cannot independently terminalize the parent ahead of
  children. Generic `update_objective/2` must not be a production bypass for
  terminal fan-out children. The M12.14
  `Coordinator.durable_terminal/2` wake remains advisory or is removed; it is
  never durable join authority. Confirmation approval for a parked fan-out
  child resumes through Scheduler/RunServer/Lifecycle; it cannot rerun the
  action or terminalize the child through the generic confirmation/Engine
  path. Non-fan-out objectives retain their existing engine semantics.
- **Recovery contract:** reconciliation is idempotent and distinguishes
  `joined_now`, `already_joined`, `not_terminal`, permanent durable-parent
  absence, known transient database availability/ownership failure, and an
  unclassified fault. Joined truth stops normally. `:fanout_parent_not_found`
  is permanent for that process: the Coordinator frees Scheduler capacity and
  retires without a retry/log stream. Only `DBConnection` ownership/connection
  failures and message-qualified SQLite busy/locked errors receive bounded,
  redacted exponential backoff. Programming, schema, query, throw, and exit
  faults remain crash-visible; repeated Coordinator crashes retain their
  Scheduler backoff until a live Coordinator reports successful reconciliation.
  Scheduler startup prefetches the complete durable parent/child snapshot before
  installing Registry monitors, and both live recovery and the whole boot
  rehydration path contain transient database loss without restarting the top
  supervisor. An operator may briefly see `finalizing`/`recovering` during a
  database interruption; recovery self-heals, a rolled-back/deleted parent
  leaves no phantom worker, and unrelated Settings/channel registries stay
  available.
- **Projection and bounded-control closure:** `Fanout.parent_projection/1` is
  the canonical status/report view for Runtime, list/show actions, CLI, ACP,
  TUI, and web. It accepts joined truth only when the durable pending/delivered
  marker, deterministic report-receipt digest, join event, and child reduction
  agree; otherwise it exposes `recovering`/`finalizing` or `inconsistent` and
  wakes repair. Engine steering lookup, session cancellation, and other
  correctness-sensitive control paths use dedicated filtered Objectives
  queries rather than truncating through the operator list limit, so an older
  active fan-out is not hidden by newer rows. Scheduler rehydration monitors a
  Registry-proved live RunServer before Coordinator recovery and releases the
  reconstructed global slot if that process exits or disappears, preventing a
  restart-only capacity leak.
- **Report/outbox contract and operator experience:** the parent's transactional
  `report_delivery_state=pending` transition is the durable completion outbox.
  ADR 0084's existing delivery ledger still reserves a unique send before any
  remote transport. Disabled/completion-only status notifications are
  coalesced or rejected before per-progress delivery reservation while
  `Channels.Notify` remains the final send-time authority; default-OFF must not
  create a ledger/audit write for every internal progress signal. One bounded
  suppression decision preserves authority evidence and the completion fallback.
  `NotifyConsumer` treats the joined signal as a low-latency wakeup and also
  reconciles pending authorized completion work after its own or the application
  restart. It monitors SignalBus ownership, re-subscribes after a bus-only
  restart, and replays pending completion work after subscription recovery.
  Delivery-state recovery is explicit: `reserved` completion work (transport
  not begun) may resume; `sending` at process loss becomes `uncertain` and is
  never autonomously retried; `delivered` with a still-pending parent
  idempotently acknowledges the parent; and a definitive `failed` row receives
  only ADR 0084's bounded retry. Normal operators see one kickoff and one
  completion; restart never duplicates or loses that completion. Default-OFF
  remains silent until the next turn. Recovery is visible in durable
  status/audit evidence, not as startup spam. A historical all-terminal/open row
  may briefly render as finalizing/recovering, never indefinitely as ordinary
  running work.
- **Delivery and origin closure:** attended web/TUI delivery and next-turn
  responses never enter the autonomous-notification ledger. They acknowledge
  exactly the report receipt rendered or printed, and only after successful
  browser render/stdout write; multiple pending fan-outs are tracked and
  acknowledged independently. The Web kickoff uses an exact browser-mounted
  delivery handle rather than a server-side pre-render acknowledgement; the
  TUI kickoff propagates returned write errors, exceptions, and exits through
  the delivery tracker. A TUI delivery failure moves kickoff to `blocked`; an
  absent, stale, or forged Web mount acknowledgement leaves it `pending`. Both
  keep every child open. Remote autonomous delivery stays default OFF and
  reserves its unique ledger row before transport. Its persisted origin digest
  is deliberately stable over mutable provider metadata: it hashes channel,
  receiver-account reference, and provider-thread key, while the exact stored
  thread-ref row id and current identity map are re-read at send time. Metadata
  refresh may therefore proceed, but account/thread-key remapping suppresses
  delivery. Send-time settings, release availability, level, throttle, Security
  policy, and ownership are all re-authorized immediately before transport.
- **Steering and multi-parent closure:** directive recording re-reads the owned
  active child inside an immediate transaction. Non-cancel terminalization
  refuses while a durable unapplied directive exists, and Lifecycle rechecks at
  its final boundary, so either steering wins and is applied before completion
  or completion wins and the steer receives a terminal response—never an
  orphaned directive. Read-only status may aggregate several active parents;
  a mutating ordinal such as `cancel task 1` is ambiguous when more than one
  parent is active and must clarify until the operator names the parent/title or
  a specific child.
- **TDD vertical slices and development lanes:** W1 (`db_serial`) adds one public
  Lifecycle tracer bullet for ordinary three-child success and asserts durable
  parent pending state plus exactly one join event without calling
  `Fanout.finalize_join/1` in the test; then implements the atomic transition.
  W2 (`db_serial`) migrates one terminal source at a time—failure, cooperative
  cancel, registered-action cancel, retry exhaustion, stale abandonment, and
  confirmation approval/resumption—with a red invariant before each change.
  W3 (`global_process_serial` plus Repo)
  kills/drops Coordinator messages immediately around the last commit and
  proves live/boot repair. W4 (`db_serial`/`global_process_serial`) proves
  signal-loss and NotifyConsumer restart replay against the existing unique
  ledger. W5 (`db_serial`) covers back-to-back Runtime fan-outs and consistent
  status/report reads. These slices are serial at Repo, Scheduler, Registry,
  Runs.Supervisor, SignalBus, and NotifyConsumer ownership; only read-only docs
  and caller census may proceed independently. Each slice is red → green →
  refactor before the next. No external provider is required for deterministic
  implementation proof.
- **Additive migration:** add forward-only insert/update guards permitting at
  most one new `fanout_joined` objective event per parent, plus a partial index
  over the pending parent-report outbox. The transaction's parent-state CAS
  remains the primary idempotency mechanism. Triggers, rather than a partial
  unique index, let upgrades retain immutable legacy event history if an older
  Home already contains duplicates.
  Migration forward/rollback and upgrade of a pre-M12.15 database are
  focused-green, with no destructive rewrite of operator data.
- **Fault and interleaving matrix:** deliberately lose the final
  `run_terminal`; kill Coordinator before, during, and after the last terminal
  commit; restart Scheduler, NotifyConsumer, and the application projection;
  exercise concurrent last-child writers; prove permanent missing-parent
  retirement, transient `DBConnection` and SQLite-busy retry, full-snapshot
  boot retry without duplicate monitors, persistent-crash backoff, and ordinary
  programming-error crash visibility; require ACP and OpenAI join/timeout
  continuations to leave no per-parent Coordinator Registry entry;
  cover completion/failure/cancellation/retry-exhaustion/abandonment and a
  confirmation-blocked child approved back through Lifecycle; repair a
  fixture representing `U90JZu`; run repeated deterministic interleavings and
  assert the invariant set after every run. Assert a bounded number of
  status-delivery ledger/audit writes when notify is OFF or completion-only and
  zero status transport calls; enabled status still passes the send-time
  authority checks. Kill/restart NotifyConsumer and SignalBus independently at
  no-ledger, `reserved`, `sending`, `delivered-before-parent-ack`, and definitive
  `failed` completion boundaries; prove the documented resume/uncertain/ack/
  bounded-retry outcomes without a duplicate transport. Tests use temporary Allbert Homes
  and public Runtime/Lifecycle/action boundaries; manually mutating children and
  calling finalization is setup-only for the historical repair fixture, never
  acceptance of normal execution.
- **Systemic drift audit:** census every production terminal objective write,
  parent finalization caller, `run_terminal`/`:DOWN` branch, Scheduler recovery
  predicate, report/status consumer, and autonomous-notify delivery trigger.
  Reconcile TUI, Web, OpenAI, ACP, CLI/Jobs, and channel consumers to one durable
  parent projection. Any alternate fan-out terminal authority or lifecycle-
  bypassing test is fixed here rather than deferred to another FV remediation.
  TUI line editing, progress-line presentation, a validation harness, and broad
  platform revalidation remain out of scope.
- **Focused gates and serial rejoin:** run
  `objectives/fanout_test.exs`, `objectives/runs/supervision_test.exs`,
  `objectives/runs/scheduler_test.exs`, `runtime/fanout_ack_test.exs`,
  `channels/notify_test.exs`, `channels/notify_edit_test.exs`, cancellation and
  stale-objective suites, and the public-protocol fan-out suites through the
  project focused gate. Reconcile the test manifest/lane inventory, then run
  Hex audit, warnings-as-errors compile, formatter, strict Credo, docs,
  `mix precommit`, and Dialyzer before the candidate commit. At that one clean
  committed SHA, run derived `release.v11` with the new invariant/supervision/
  replay suites in its explicit inventory, frozen `release.v1`, pre-push, and
  one uninterrupted authoritative `mix allbert.test release`, then push the
  unchanged SHA. Generated release evidence and test-metric records are the
  pre-FV proof; exact paths/timings are reconciled into these tracked docs at
  post-FV closeout so an evidence-only edit cannot invalidate the proved SHA.
- **Focused-gate result:** the implemented focused matrix is green across the
  atomic fan-out/lifecycle/supervision/cancellation suites; Runtime receipt and
  pending-report paths; steering/Engine/confirmation ownership; Notify and TUI
  delivery; ACP/CLI continuation output; and web projection, ownership, and
  browser-render ACK paths. `release.v11` now appends four explicit steps to
  frozen v1: the 11-row authority sweep plus its inventory meta-contract, a
  consolidated core fan-out step, a channel/TUI authority step, and a web/
  OpenAI operator step. The first clean-candidate run passed expanded
  `release.v11` and frozen `release.v1`, then pre-push exposed two rejoin defects:
  `NotifyTest` owned `Paths.home` but not the higher-precedence Settings root,
  so partition neighbors could leak notify consent; and confirmation provenance
  treated absent `selected_action_module=nil` as a real atom/module, rejecting
  direct confirmation-gated actions. The fixture now owns/restores its exact
  Settings root, and the binder distinguishes absent provenance from a trusted
  module without weakening mismatch checks. Exact-seed Notify passed 21 / 0;
  confirmation plus memory-promotion regressions passed 14 / 0. The resulting
  replacement candidate again passed expanded `release.v11` and frozen
  `release.v1`; its pre-push serial lane then exposed one further shared seam as
  18 failures across four partitions: every ordinary file-backed approval was
  falling through to the legacy `Step.confirmation_id` lookup and therefore
  querying the Objectives Repo. That changed non-fan-out behavior and made an
  unrelated approval depend on SQL availability. The first remediation passed
  its 40 / 0 resolver matrix, 69 / 0 exact reproduction, and all four
  `app_env_serial` partitions (662 / 0), but final semantic review found that
  its version marker still conflated complete non-fan-out objective bindings
  with fan-out-child bindings. It also found a Plan/Build action path that
  discarded Runner fan-out context and one generated TUI test whose three
  runtime identities collapsed into one manifest row. Those findings supersede
  the first remediation evidence rather than being hidden by SQL sandbox
  ownership.

  The replacement contract writes `objective_binding_version: 2` plus an
  internally stamped `ordinary`, `objective`, or `fanout_child` kind. Ordinary
  incomplete/standalone records do not query Objectives; complete objective
  records are durably classified and cannot generic-resume a fan-out child; and
  fan-out-child approval requires exact child, step, parent, target-action, and
  owner agreement. Parent-present incomplete context is rejected before a
  prompt is written. The short-lived local version-1 transition shape remains
  readable and keeps complete-pair classification. Unversioned records written
  by pushed candidate `107a5ce2` keep their exact complete-provenance path;
  older unversioned records without complete provenance retain the unique-Step
  upgrade fallback. Stale/ambiguous approval leaves the record pending.
  Plan/Build now preserves Runner context, the static call-site audit allows
  only the two intentional context-free internal creators, and the TUI cases
  have three explicit test identities. The combined confirmation/fan-out/
  supervision/Plan-Build matrix passed 74 / 0 and the TUI matrix passed 35 / 0.
  The regenerated manifest now contains 3,469 rows; lane reconciliation reports
  550 files with zero unclassified or double-counted files. The final
  four-partition `app_env_serial` lane passed 665 / 0. Every clean-SHA barrier
  must now repeat; the authoritative release gate remains pending.

  The final truth audit found two additional decision-path edges before that
  candidate can close. First, an unversioned `107a5ce2` record with complete
  objective/step/origin provenance must use exact durable classification before
  the older unique-`Step.confirmation_id` fallback. Second, a target permission
  that changes to denied must not bypass fan-out classification: a valid parked
  child resolves denied and wakes Scheduler so Lifecycle cancels it and the
  parent can join, while tampered, stale, or ambiguous records remain pending
  and blocked. The corrected five-file confirmation/fan-out/supervision/
  Plan-Build matrix passed 77 / 0. The regenerated manifest contains 3,472
  rows and matches the live tree; lane reconciliation still reports 550 files
  with zero findings. The affected four-partition `app_env_serial` lane passed
  665 / 0 (163 + 166 + 142 + 194). The earlier 74 / 0 and 665 / 0 results above
  remain intermediate remediation evidence; these final counts are the
  pre-candidate rejoin. Static and clean-SHA barriers remain pending.

  Candidate `5601e67e` then passed expanded `release.v11`, frozen `release.v1`,
  and pre-push. Its authoritative release gate reached
  `external_runtime_serial` and failed 617 tests with 49 failures and 12
  intentional skips. Causal replay reduced that output to one ACP timeout test
  whose SQL rollback removed the durable parent while its Coordinator kept
  retrying `:fanout_parent_not_found`; the retry entered the next sandbox owner,
  crashed Scheduler/top supervision, and made later Sandbox/Settings processes
  unavailable. Evidence `release-2026-07-25T17_22_03Z` is failed diagnostic
  evidence, not acceptance. The replacement recovery slices pass 46 / 0 across
  Scheduler, Coordinator, and ACP; OpenAI passes 10 / 0 at seed `122487`; and
  the exact ACP → Sandbox → Settings replay passes 94 / 0 with two intentional
  skips at seed `19746`. The complete replacement `external_runtime_serial`
  lane passes 617 / 0 with 12 intentional skips at seed `97040` in 681.3
  seconds. The regenerated live manifest contains 3,479 rows. No
  ShippedRegistries, Settings, or Secrets patch was required because those
  failures were downstream process-loss symptoms.
- **Commit/push and validation barriers:** the docs-first M12.15 contract was
  committed and pushed as `a24df266`. The initial implementation candidate
  `107a5ce2` was also pushed, then superseded by the post-push pre-push and final
  semantic findings above. Additive local candidate `5601e67e` contains the
  confirmation correction and passed three clean-SHA barriers, but remains
  unpushed after the authoritative failure. The lifecycle containment and doc
  reconciliation become a second additive replacement commit; every automated
  barrier reruns on that unchanged clean SHA before either local commit is
  pushed. Implementation
  commits are coherent green rejoins, never knowingly broken intermediate
  states. Stage only files changed for M12.15. Operator validation remains
  stopped until the final implementation/docs commit is pushed and every
  automated barrier above passes. The replacement walkthrough
  validates only the flagship in one warm TUI session: parallel kickoff/
  execution, one in-channel steer, one honest
  result-bearing fan-in, attended exact-receipt output/ACK, and one post-session
  durable-state check. Automated tests own detached/default-OFF fallback,
  remote-channel authority, crash
  permutations, public protocols, restart recovery, and broad surface
  regression.

Third-pass readiness disposition (operator, 2026-07-18): the second-pass
freeze of OpenAI/ACP fan-out is RESCINDED and Locked Decision 17
(hold-until-join) is restored. The freeze had been argued against a
`202 + poll` wire change — a design never locked; hold-until-join keeps the
existing synchronous wire contract (the response completes with the join
report, timeout-bounded) and requires no Tier-1 change. No `202 + poll`
intake candidate remains.

Fourth-pass readiness disposition (operator, 2026-07-19): M4 reuses the
already-packaged `erlexec`/MuonTrap/container-runtime execution substrates
before adding native code; a new helper is allowed only when a named red test
proves a capability gap in every applicable existing substrate. For
non-streaming OpenAI/ACP, durable server recording of the kickoff is the
start precondition while the request holds for join; SSE uses successful
kickoff-event flush. This replaces the inconsistent "response commit, then
later complete the same response" wording without adding a wire shape.

Final readiness disposition (operator, 2026-07-22): retain OpenAI/ACP
hold-until-join and fund the required public-protocol continuation work;
pending next-turn reports use delivery receipts and acknowledge only after
delivery/durable recording; fan-outs persist an exact origin-thread-ref
locator and re-prove identity at send time; concurrency uses distinct global,
per-fan-out, and decomposition limits; superseded confirmations resolve as
`cancelled` with reason `superseded_by_steer`; standalone TUI quit performs a
bounded graceful wait then tiered cancellation/uncertain-effect parking; WSL2
must provide cooperative/supervised cancellation while descendant containment
remains Tier-2 best-effort. RunServer calls a new Objectives lifecycle facade,
and attached TUI status uses ephemeral session subscriptions rather than a
request-scoped sink.

### M12.20 — fail-closed run recovery and release-proof closure (planned 2026-07-26)

- **Audit trigger and release disposition:** static audit of candidate
  `41857507` found that `Coordinator.recover_run_failure/3` discards the result
  of the blocked/failed safety write, while `maybe_join/1` independently
  restarts every unmonitored durable `running` child. Boot reconciliation checks
  both `Lifecycle.retry_safety/1` and `run_attempt_count`; the post-worker path
  does not. A transient or permanent park/terminalization failure can therefore
  replay unknown/possibly-effectful work or exceed the single safe retry,
  violating Locked Decision 19 and ADR 0083. The operator accepted remediation
  before release; the authoritative cascade remains paused until the final
  corrective SHA is committed and pushed.
- **Locked recovery semantics:** boot recovery, worker-down recovery, terminal-
  persistence failure, and post-join reconciliation use one missing-run
  disposition. Only a `safe` child with no more than one persisted attempt may
  receive an execution grant. A failed `blocked/uncertain_effect` or exhausted-
  retry `failed` write retries that durable transition with bounded redacted
  backoff; it never retries the child. A pending unapplied steering directive is
  preserved and reconciled, but it cannot erase uncertainty about an earlier
  possibly-effectful attempt: after applying the operator's text, the child
  remains review-blocked rather than automatically executing. Permanent schema,
  programming, or compare-and-set errors remain crash/log visible while the
  child stays non-runnable. Successful start/transition clears its applicable
  retry state.
- **Vertical TDD and operator-visible acceptance:** first reproduce an unsafe
  worker crash whose park write returns a transient error and prove zero second
  execution; implement transition-only retry. Then reproduce an exhausted-safe
  failure whose terminal write fails and prove exactly two attempts; implement
  the shared recovery disposition. Add the pending-steer race and permanent-
  error cases, then clear pre-effect backoff after a successful start. Separately
  prove that TUI progress coalescing reopens on `run.started`, the actual attempt
  boundary. The attended-turn state handoff remains an explicit fail-closed
  whitelist; a named field list and regression prevent accidental whole-state
  merges without changing current behavior.
- **SQLite topology and runtime ownership boundary:** development and packaged
  production retain the M12.19 one-connection/WAL/immediate topology. Tests add
  one isolated disposable-Home production-profile integration step that boots
  actual Allbert persistence/fan-out code without SQL Sandbox; this is an
  automated gate, not an operator validation harness. The source census rejects
  unreviewed `Repo.transaction`, checkout, or streaming connection ownership.
  v1.1 documents one live Allbert runtime per Allbert Home: use daemon attach or
  a different Home for concurrent work. Generalizing the daemon writer-lock
  holder to every standalone runtime changes startup, migrations, attach, and
  all Mix tasks; it is an intake candidate pending operator disposition, not a
  last-minute v1.1 scope expansion.
- **Version-gate binding:** `release.v11` directly runs the SQLite topology,
  delivery acknowledgement, decomposer corpus, atomic conversation admission,
  and Runner-outside-transaction files in addition to its existing fan-out
  matrix. The decomposer corpus remains a deterministic orchestration/classifier
  contract; live provider quality is separate and was exercised by FV-01.
- **Workstreams, barriers, drift, and rejoin:** W1 is the serial
  `global_process_serial`/`db_serial` Coordinator TDD loop. W2 is the independent
  TUI coalescing/whitelist slice. W3 binds the five version-gate files and the
  production-profile SQLite step. W4 updates CHANGELOG, ADR 0083/0084 status,
  request-flow, roadmap, and the operator ownership note. Rejoin after focused
  Coordinator/Supervision/Steering/TUI/Conversations/Runner/topology tests,
  warning-as-error compile, formatter, strict Credo, docs/manifest/Hex audit,
  and Dialyzer. Commit and push the docs-first plan before code; after the
  implementation/docs commit is pushed and clean, run `release.v11`, pre-push,
  and one uninterrupted authoritative `mix allbert.test release` cascade. No
  repeat full FV-01 is required unless implementation drifts into its validated
  happy-path semantics.

## Purpose

Ship the 1.1 flagship: a prompt that decomposes into multiple tasks returns a
kickoff receipt, then fans out supervised background agents, streams their
status, joins, and reports back to the originating caller — while chat
channels stay open and mid-flight input is contextually routed as steering vs
a new request.

1. **Non-blocking fan-out turns with honest kickoff ordering.** A decomposable
   prompt returns a kickoff ack (the visible task list) in seconds; only after
   the caller confirms that it was delivered or durably recorded does work
   continue in a supervised OTP tree with durable state in Objectives.
   Interactive Runtime callers stop owning execution latency for decomposed
   work; OpenAI/ACP deliberately await through a separate bounded continuation.
2. **Parallel child-objectives with an honest join.** Child objectives under
   `parent_objective_id` execute concurrently under DynamicSupervisor with
   explicit backpressure; the join report always enumerates per-child
   outcomes; crash/restart rehydrates from Objectives (ADR 0083).
3. **In-channel steering.** Mid-flight input on the same thread is classified
   (advisory) as steer / adjust / cancel / status query / new request;
   effectful steering routes through registered actions and never lowers a
   confirmation class. Cancellation is real: cooperative cancel between
   operations, supervised shutdown, and OS child-process kill semantics
   (ADR 0085) — retiring the "operators can only wait out a long action" debt.
4. **Authorized report-back on every channel.** A NEW settings-governed
   authority class (default OFF, ADR 0084) lets Allbert autonomously deliver
   status/completion notifications per channel; without opt-in, reports wait
   for the next turn. All nine chat surfaces (tui, web, telegram, email,
   discord, slack, matrix, whatsapp, signal) get kickoff-ack,
   status/completion delivery per their primitives, and steer-by-reply.
5. **The parity contract tells the truth.** `streaming: "turn_complete"` stops
   being hardcoded; every channel declares its streaming capability and the
   parity matrix derives from declarations.
6. **One Runtime contract.** Broad automatic fan-out applies to every
   `Runtime.submit_user_input/1` caller. Chat, CLI, public-protocol, and Jobs
   callers acknowledge the kickoff through the same idempotent two-phase
   continuation boundary before children can execute.

## Expected Inputs

- Stable v1.0.5 shipped 2026-07-22. It carries the cumulative v1.0.1–v1.0.5
  source and packaged-recovery line, ADR 0082 test-isolation seams, the
  metrics store (`.test_metrics/runs.jsonl` + `mix allbert.test metrics`),
  and the turn-scoped settings pin (`Store.with_resolved_settings/1`). The
  v1.0.3 bounded conversion waves shipped no lane moves; M0 measures the
  actual v1.0.5 topology rather than assuming those conversions landed.
- The objectives substrate: durable objectives/steps/events
  (`objectives.ex`), the serialized engine agent, `parent_objective_id`,
  `source_channel`/`source_surface` attribution (v1.0.1), and the
  delegate-agent substrate (ADR 0021/0022, `AgentRegistry` +
  `:delegate_agent`).
- The channel substrate: eight plugin adapters + the web local surface, the
  `Channels.Outbound` boundary (ADR 0063), channel identity maps
  (ADR 0016/0056/0059), `ConfirmationCallback` typed commands, and the
  channel parity matrix.
- The gate machinery: `@release_v1_steps` prefix precedent
  (`allbert.test.ex:5526`, v101/v102 composition at `:5740/:5922`),
  EvalInventory + SecurityEvalCase (milestone enum at
  `test/support/security_fixtures/eval_inventory.ex:16-57` — extend it, the
  v1.0.2 M1 drift lesson), release-rehearsal runbook, release-artifacts CI.
- Recorded baselines from the metrics store: decide per-turn ~1,042ms
  post-M8.4; gate walls quick ~442s / high ~883s / prepush ~897s (v1.0.2 M8.4
  tables) — M0 re-baselines at HEAD.

## Current Code State

Originally verified 2026-07-18 on main (v1.0.2 M8 window). Stable v1.0.5 has
since shipped; M0 re-verifies every anchor at build HEAD before implementation.

**Turn model — synchronous everywhere, one existing async escape.**
`Runtime.submit_user_input/1` (`apps/allbert_assist/lib/allbert_assist/runtime.ex:115-138`)
runs the full turn inline via `run_agent_turn/2` (`:508` coding branch,
`:517` default) under `@default_timeout_ms 120_000` (`:44`). Every channel
adapter blocks its poll/webhook/gateway handler on this call for the turn
duration; none passes `timeout_ms`. The web workspace is the exception in
presentation only: `WorkspaceLive` wraps the same blocking call in
`start_async` (`workspace_live.ex:2566`, results at `handle_async :ask`
`:762-785`) and receives push updates via SignalBridge PubSub
(`:84` subscribe; `:718` `{:objective_event, _}`; `:722` `{:fragment, _}`).
The request map already carries a `stream_event_sink` field
(`runtime.ex:59`, threaded at `:486/:501`) — the TUI coding turn is its only
consumer today. It proves the LiveRegion renderer, but remains request-scoped;
v1.1 adds an ephemeral attached-session subscription instead of persisting or
reusing that sink for background runs.

**Settings pin — per-process, per-turn.** The whole intent turn runs under one
turn-scoped resolved-settings snapshot (`intent_agent.ex:170-184` wrapping
`respond_pinned`, `settings/store.ex:98-106` `with_resolved_settings/1`,
process-dictionary pin). The pin cannot cross process boundaries — background
run processes MUST establish their own (Locked Decision 9).

**Objectives — durable but strictly sequential.** The engine is ONE serialized
JidoBacked agent (`objectives/engine/agent.ex`, `use JidoBacked` at `:26`,
signal dispatch `timeout: :infinity` at `:160`) with signal routes for
frame/propose/evaluate/authorize/execute/observe/advance/cancel/continue/prune
(`:15-24`). One `current_step_id` per objective (`objectives/objective.ex:32`);
statuses `open running blocked completed cancelled failed abandoned`
(`objective.ex:11`); step statuses incl. `blocked`/`cancelled` and a
`confirmation_id` column (`objectives/step.ex:11,:30`). `parent_objective_id`
exists (`objective.ex:31`, migration `20260517000000_add_objectives.exs:17`)
and is consumed ONLY by plan/build runs (`plan_build.ex:42`,
`actions/plan_build/start_plan_run.ex:19`) — a fan-out discriminator column is
required for coexistence. `source_channel`/`source_surface` are plumbed at
create (`objectives.ex:82-84`, cast list `:35,:44,:46`) but NOTHING consumes
them for report-back (repo-wide grep: only unrelated e2ee threading uses).
Public API: `create_objective/1` (`objectives.ex:134`), `list_objectives/2`
(`:186`), `active_objectives/1` (`:220`), `create_step/1` (`:249`),
`transition_step/3` (`:282`), `create_event/1` (`:297`), `cancel/3` (`:111` —
no confirmation gate today), `continue/2` (`:122`).

**Delegation — blocking, monitored for eviction only.**
`AgentRegistry.dispatch/4` is a blocking `Jido.AgentServer.call/3`, default
5s (`objectives/agent_registry.ex:48-60`); registry monitors `:DOWN` solely to
evict dead entries (`:112-120`) — no join, no cancel, no status. The
`delegate_agent` action waits inline with default 180s, cap 900s
(`actions/objectives/delegate_agent.ex:43-44`, `delegate_timeout_ms/1`
`:155-159`).

**Intent — one Decision, active objectives already compete.** The engine emits
exactly one Decision through the two-stage router
(`intent/router/default_router.ex:17-24` → `InputGuard`/`Prefilter`/
`Disambiguator`; `prefilter.ex`, `disambiguator.ex`). A Stage-0 decomposer is
greenfield. Active objectives already join the candidate pool
(`intent/engine.ex:1223-1252` `objective_candidates/3`, up to 5
open/running/blocked) and win as `:continue_objective` decisions
(`engine.ex:273-291` `decision_attrs_for_candidate` for `kind: :objective`) —
this is the steer-vs-new seam to extend.

**Confirmations — channel-neutral records, no out-of-band delivery.**
`Confirmations.Origin` carries channel/thread/surface + `response_target`
(`confirmations/origin.ex:14-34`, `:29`) but nothing DELIVERS a
background-raised confirmation to a remote channel; approval-side plumbing
already exists channel-neutrally via typed commands
`ALLBERT:APPROVE|DENY|SHOW:<id>` (`channels/confirmation_callback.ex:15-33`)
and inline buttons on capable channels. Workspace `Emitters`
(`workspace/emitters.ex:35-147` incl. `objective_lifecycle/3` at `:119`) and
`Fragment` publication (`workspace/fragment.ex:324` via
`Jido.Signal.Bus.publish(AllbertAssist.SignalBus, ...)`;
bus started at `application.ex:34`) are workspace-scoped only.

**Channel parity — streaming hardcoded.** `channel_parity.ex:98` pins
`streaming: "turn_complete"` for every row ("until a later ADR changes that
contract" — this release's ADR 0084/M5). The `outbound` column is honest: it
live-checks `function_exported?(module, :deliver_outbound, 3)`
(`:141-155`). `Channels.Outbound.send/4` (`channels/outbound.ex:24-42`) is the
single operator-initiated boundary; the callback shape is
`deliver_outbound(target, body, opts) :: {:ok, map()} | {:error, term()}`
(`:21-22`), duck-typed (no adapter declares the behaviour).
`send_channel_message` (ADR 0063) is `confirmation: :required` and
identity-allowlist-gated before dispatch
(`actions/channels/send_channel_message.ex:17,:64-118,:149-160`).

**Per-adapter reality (full plugins/ read, 2026-07-18).** All eight adapters
are supervised GenServers (one child each under `Channels.Supervisor`,
`channels/supervisor.ex`); all block their inbound handler on
`Runtime.submit_user_input`; **none calls an edit API or typing indicator**
(verified: zero editMessageText / message-PATCH / chat.update / m.replace /
typing calls across all channel dirs):

| Channel | `deliver_outbound/3` | Transport | Inbound | Threading | Buttons | Status-update primitive available |
|---|---|---|---|---|---|---|
| telegram | `plugins/allbert.telegram/.../telegram/adapter.ex:915` | Bot API `sendMessage` (`client.ex:34`) | long-poll `:poll` loop (`adapter.ex:54`, 25s getUpdates, backoff ≤60s) | `reply_to_message_id` + `message_thread_id` (`:688`) | yes (inline keyboard + `answerCallbackQuery` `:823`) | post new message (edit API exists upstream, unused) |
| tui | **ABSENT** | local TTY | TTY loop (`adapter.ex:104,:112`), escape cancel (`:121,:173`) | `:rich` | typed commands | **richest**: async coding turn (`:437`), `stream_event_sink: self()` (`:447`), LiveRegion progressive render (`:186,:516`), live status line |
| email | **ABSENT** (inbound-triggered `deliver_reply` SMTP only, `adapter.ex:~466`) | gen_smtp | IMAP poll (`adapter.ex:47`, 60s) | `in_reply_to`/subject | no | threaded reply email only (digest-class) |
| discord | `plugins/allbert.discord/.../discord/adapter.ex:608` | REST `POST /channels/{id}/messages` (`client.ex:25`) | WebSocket gateway port (`adapter.ex:73,:115`) | native threads (`client.ex:77`) + `message_reference` | yes (interaction ack only, `gateway_port/real.ex:308`) | post new message |
| slack | `plugins/allbert.slack/.../slack/adapter.ex:650` | Web API `chat.postMessage` (`client.ex:45`) | Socket Mode WS (`adapter.ex:63,:79`) | `thread_ts` (`client.ex:272`) | yes | post new message (no `chat.update` in client) |
| matrix | `plugins/allbert.matrix/.../matrix/adapter.ex:601` (only adapter reading `opts`) | CS API `PUT .../send/m.room.message/{txn}` (`client.ex:22`); client whitelists GET/PUT send/sync ONLY (`client.ex:140-141`) | `/sync` long-poll (`adapter.ex:50,:122`, 30s) | `m.thread` relations (`:500-501`) | no (`:link` primitive) | post new message; `allowed_room_ids` outbound validation (`:630`) |
| whatsapp | `plugins/allbert.whatsapp/.../whatsapp/adapter.ex:793` (ReleaseAvailability-gated `:794`) | Cloud API `POST {phone_number_id}/messages` (`client.ex:28`) | HTTP webhook (`adapter.ex:32,:62`; controller `whatsapp_webhook_controller.ex:32`) | `context_message_id` reply chain | yes (`client.ex:42/:94`) | post new message |
| signal | `plugins/allbert.signal/.../signal/adapter.ex:526` (ReleaseAvailability-gated `:527`) | signal-cli JSON-RPC over UDS (`client.ex:17,:63`) | **GAP: daemon receiver not wired** — `Daemon.daemon_child_spec/2` (`daemon.ex:9-19`, MuonTrap per ADR 0058) has NO callers; `handle_notification` (`adapter.ex:54`) reachable only via `simulate_daemon_notification/2` (`:31`) | timestamp reply chain | no | post new message (after daemon wiring) |

**Cancellation/kill — the debt is real.** `Execution.LocalRunner` runs
`System.cmd` inside `Task.async` and `Task.shutdown(:brutal_kill)`s on timeout
(`execution/local_runner.ex:48-57,:59-70`) — the BEAM task dies but no OS pid
is captured and no process group is killed; the same shape exists in
`skill_script_runner.ex` and the sandbox command backends
(`sandbox/backends/command.ex`, `container_runner.ex`). The TUI escape path
cancels only the coding turn presentation. `CodingTurnSupervisor` is the
supervised-async template to generalize
(`coding/turn_supervisor.ex:145-159`: `Task.Supervisor.async_nolink` +
registration + await; `Registry` precedent `AllbertAssist.Coding.TurnRegistry`
at `application.ex:35`; `Task.Supervisor` at `:36`).

**Workflow YAML cannot fan out — by design.** ADR 0041 reserves
`parallel_steps`/`for_each` (`future-features.md:1099-1114`). v1.1 does not
route through it (Locked Decision 6).

**Eval/gate precedent.** EvalInventory milestone enum must be extended
(`eval_inventory.ex:16-57`; `:v066`/`:v1` were added late as a v1.0.2 M1
drift fix — do not repeat). Gate composition:
`@release_v11_steps = @release_v1_steps ++ @v11_focused_steps`
(`allbert.test.ex:5526/:5740/:5922` precedent); no predecessor bespoke steps.

## Locked Decisions

Decisions 1–4 are operator-locked (2026-07-18). Decisions 5–14 are derived
during planning from the locked four plus code evidence; each records its
rationale so the operator can veto at plan review. Decision 11 was
subsequently superseded by an operator fold-in (2026-07-18 intake walk) and
is now operator-locked. Decisions 15–32 are operator-locked readiness-audit
resolutions.

1. **Concurrency = parallel child-objectives (operator).** A fan-out frames
   CHILD objectives under `parent_objective_id`, each executed by a supervised
   run process (one global DynamicSupervisor, `restart: :temporary`,
   Registry-addressed per-run keys); join = `Process.monitor` + child status
   reduction into the parent; durable state lives in Objectives (no private
   goal loops — the OTP tree is the executor); crash/restart ⇒ rehydrate from
   Objectives. ADR 0083 binds the model.
2. **Decomposition = model proposes, visible list (operator).** A Stage-0
   decomposer MAY use the local model to propose the task list (the
   `propose_steps` precedent); the list is ALWAYS surfaced in-channel before
   fan-out starts (default-on ack); deterministic/explicit decomposition works
   without a model; decomposition output is advisory-only and grants nothing
   (AGENTS.md: `objective_id`/`step_id` are never authority).
3. **Report-back authority = settings-level autonomous notify (operator).**
   Per-channel operator settings (Settings Central, default OFF) authorize
   autonomous status/completion notifications on that channel. Security
   Central holds the boundary. This is a NEW authority class with its own ADR
   (0084). The `turn_complete` parity contract is renegotiated per channel
   with an explicit streaming capability field.
4. **Rollout = ALL channels at once; steering extends `:continue_objective`;
   cancellation ships all three tiers (operator).** Every adapter
   (tui/web/telegram/email/discord/slack/matrix/whatsapp/signal) gets
   kickoff-ack, status/completion delivery per its primitives, and
   steer-by-reply in 1.1. The steering classifier is advisory; effectful
   cancel/steer routes through registered actions + the confirmation floor
   (steering may never LOWER any action's confirmation class). Cancellation
   ships the merged enablers: cooperative cancel token between operations +
   supervised shutdown, AND OS child-process kill semantics
   (`future-features.md:164-185`). ADR 0085 binds cancellation.
5. **OTP pattern constraints (operator, baked into seams).** Supervised
   GenServer/Jido.Agent per the pragmatic-substrate rule with the choice
   documented in `@moduledoc`; never bare `Task.async` for steerable agents;
   monitors, not polling; Registry-addressed steering; signal-bus push status
   (no polling loops); Scheduler-owned global/per-fanout backpressure with a
   static DynamicSupervisor safety ceiling and explicit FIFO queue policy
   (refined by Locked Decisions 21/28: fair
   round-robin capacity grants ACROSS fan-outs, durable FIFO preserved
   WITHIN each fan-out); no GenStage/Flow; Security Central authority
   unchanged — a supervised process has no authority by virtue of being
   supervised.
6. **No workflow-YAML routing (derived, ADR 0041).** Fan-out is an
   objectives-native mechanism; `parallel_steps`/`for_each` stay reserved
   vocabulary. Rationale: concurrency semantics belong in the supervised
   runtime, not operator-authored YAML; the ADR 0041 operability rationale
   stands.
7. **Additive-only contract evolution (derived, freeze).** The runtime
   response gains only additive fields (`fanout` summary, `pending_reports`)
   per ADR 0029 Tier-2 rules; channel descriptors gain an optional
   `streaming:` field with the current behavior as default; `release.v1`
   stays green throughout. No non-additive migration: the objectives schema
   change is additive columns + index (the 1.5-horizon migration-runner
   cluster is not pulled forward).
8. **Ack semantics (derived from "default-on ack").** The kickoff ack — the
   decomposition list with child ids/titles — IS the fan-out turn's response,
   delivered synchronously on every channel; visibility cannot be disabled.
   Framing NEVER starts a run: only the caller's idempotent acknowledgement of
   successful delivery makes the queue runnable (Decision 15). A new setting
   `objectives.fanout.confirm_before_start` (default `false`) adds a durable
   confirmation after acknowledgement and before scheduling for operators who
   want a second hard gate.
9. **Settings-pin policy for background runs (derived).** Each run process
   establishes its OWN resolved-settings pin around each lifecycle operation
   (`Store.with_resolved_settings/1` around propose/evaluate/authorize/
   execute/observe/advance), because (a) the M8.4
   pin is process-dictionary-scoped and cannot cross into spawned processes,
   and (b) a fan-out may outlive the originating turn by hours — freezing the
   parent turn's snapshot for the whole run would make operator setting
   changes (e.g. disabling autonomous notify) invisible until restart.
   Semantics: deterministic within a step; operator changes take effect at
   the next step/notify boundary. Documented in the RunServer `@moduledoc`
   and tested red-first.
10. **Cancel needs no confirmation; steering never approves (derived).**
    `cancel` of your own fan-out/child is the safety action and follows the
    existing `Objectives.cancel/3` precedent (no confirmation today —
    `objectives.ex:111`); the OS-kill tier executes only against processes
    the run itself spawned (ADR 0085 scope rule). Steering directives are
    advisory reframes recorded durably; they may cause new effectful steps
    which confirm at their own actions, and free-text steering NEVER maps to
    confirmation approval — approvals stay on the typed-command/button path
    (`ConfirmationCallback`). Gate-bound eval rows enforce both.
11. **Capability-declared in-place status (operator fold-in, 2026-07-18
    intake walk — supersedes the earlier no-edit decision).** Adapters
    DECLARE their edit capability in the channel descriptor
    (`status_update_mode: :append_only | :edit_in_place`, default
    `:append_only` — additive, parallel to the S7 `streaming:` field). The
    four providers with a real edit primitive declare `:edit_in_place`:
    telegram (`editMessageText`), discord (message PATCH), slack
    (`chat.update`), matrix (`m.replace` relation). whatsapp (the Cloud API
    has no business-initiated edit), signal (partial/recent CLI support),
    and email stay `:append_only`; TUI/web are local surfaces. Where
    declared, `Channels.Notify` keeps ONE status message per fan-out per
    channel and EDITS it in place, with automatic append fallback on edit
    failure (audited). Completion reports and confirmation requests are
    ALWAYS new messages — never edits: an approval prompt must not replace
    history. Edits do not evade the min-interval throttle — an edit is a
    delivery for throttling purposes. M7 (S7b) carries the implementation.
12. **Signal inbound daemon is wired in-scope; live gating unchanged
    (derived).** "ALL channels" includes signal steer-by-reply, which is
    impossible while `Daemon.daemon_child_spec/2` has no caller — M6 wires
    the MuonTrap daemon receiver under the adapter (ADR 0058 pattern).
    WhatsApp/signal remain `ReleaseAvailability`-gated exactly as today; v1.1
    changes code paths, not release status — their validation runs simulated
    (webhook/notification fixtures) plus live where the operator has a
    configured live channel.
13. **Notify-OFF fallback = next-turn report (derived).** With autonomous
    notify OFF (the default), completed fan-outs deliver their join report as
    part of the user's NEXT turn on that thread (additive `pending_reports`
    response field rendered by every adapter) and are always visible in the
    web/TUI objectives surfaces. Attached live local surfaces (an open
    workspace LiveView, an attached TUI session) render push status without
    the ADR 0084 authority — the user is present and no unattended transport
    send occurs; ADR 0084 defines this boundary precisely.
14. **Fan-out discriminator (derived).** Additive columns `fanout_role`
    (`"parent"`/`"child"`/nil) and `join_policy` (default `"all_terminal"`)
    plus an index on `parent_objective_id` distinguish fan-out children from
    the existing plan/build `parent_objective_id` consumers
    (`plan_build.ex:42`) — the recon's "unused" claim was wrong; coexistence
    is explicit.
15. **Delivery-or-durable-record start across every Runtime caller (operator,
    clarified 2026-07-19).**
    Framing returns a normal additive response plus an opaque, identity-bound,
    single-use start receipt. Children remain durable `open` and cannot start
    until the caller acknowledges successful kickoff delivery or the
    protocol-specific durable equivalent: remote chat after transport success;
    Web after its exact browser-mounted delivery handle; TUI after every
    kickoff line is written; non-streaming HTTP after the kickoff is durably
    recorded server-side; SSE after the kickoff event is successfully flushed;
    CLI after output; Jobs after a durable kickoff event. The non-streaming
    request then holds for join without pretending an unflushed response was
    delivered. Duplicate acknowledgement is idempotent. An absent, stale, or
    forged Web handle leaves kickoff `pending`; a definitive TUI output failure
    records `blocked`. Neither starts a child. An operator retry/status turn
    re-renders the same kickoff and reuses the durable receipt before
    acknowledging it.
16. **Broad automatic eligibility (operator).** Stage 0 may fan out any prompt
    judged to contain two or more independent tasks; explicit "parallel" or
    "background" language is not required. Single-task, uncertain, unsupported,
    and nested-fan-out proposals return `:single`. If a valid decomposition
    exceeds `max_children_per_fanout`, Allbert clarifies before framing; it never drops,
    truncates, or silently merges tasks. Existing intent accuracy floors
    remain the regression bar. Rollout is independently controllable through
    `objectives.fanout.rollout_mode = "explicit" | "shadow" | "automatic"`
    (default `"explicit"` until M3's corpus and cross-surface gates pass,
    then the release default becomes `"automatic"`). `"shadow"` records the
    proposed decision and metrics but never frames children;
    `objectives.fanout.enabled=false` is the emergency kill switch.
17. **All Runtime callers participate (operator; restored third pass
    2026-07-18).** Auto-fan-out is not channel-only. Every direct caller of
    `Runtime.submit_user_input/1`, including `mix allbert.ask`, ACP/
    OpenAI-compatible controllers, and Jobs, implements the same delivery
    acknowledgement contract. A caller without an acknowledgement
    implementation fails closed to `:single`; it never starts unacknowledged
    work. **API presentation: OpenAI-compatible/ACP requests HOLD until
    join** — for non-streaming requests, durable server recording of the
    kickoff satisfies the start precondition and children may then run; the
    HTTP response
    completes with the join report, bounded by the request timeout
    (timeout ⇒ the kickoff returns and the report remains behind its
    unconsumed delivery receipt in `pending_reports`); SSE-streaming clients start only after the kickoff
    event flushes successfully, then receive status chunks and the report.
    Disconnect-before-record/flush never starts work; disconnect after
    durable recording follows the restart-safe pending-report path.
    Synchronous third-party clients get a complete
    answer in-band; interactive channels keep the async experience. No wire
    format changes (ADR 0029 respected).
18. **Multiple active fan-outs (operator).** An unqualified status query
    aggregates every active fan-out attributed to the thread/session. A
    mutating steer/cancel resolves an explicit child/fan-out reference; when
    more than one target remains plausible, Allbert clarifies instead of
    choosing the newest run. New requests continue through the normal pipeline.
19. **Crash recovery never guesses about external effects (operator).**
    Rehydration auto-resumes only a step whose retry safety/idempotency is
    proved by its action contract. If an effect may have committed before its
    observation became durable, the child becomes `blocked` with an
    `uncertain_effect` review reason; only an explicit retry or skip decision
    may advance it. Registered actions gain optional
    `retry_safety: :safe | :unsafe | :unknown`, default `:unknown`; only
    `:safe` may replay automatically. M2 sweeps the SHIPPED action catalog:
    read-only/idempotent actions (queries, lists, status reads) declare
    `:safe`; possibly-effectful actions declare `:unsafe` or stay `:unknown`;
    the sweep table is recorded in Build Progress so auto-resume is real,
    not vacuously absent.
20. **Durable queue, attempts, and join outcome (operator).** Queue position,
    per-child run-attempt count, delivery state/receipt identity, and
    `join_outcome` are reconstructible from Objectives; the Coordinator owns
    no irreplaceable loop state. `join_outcome` is additive
    (`success|partial|failed|cancelled`) and does not widen the frozen objective
    status enum: all completed → completed/success; any completed plus failed
    or cancelled → completed/partial; no completed and any failed →
    failed/failed; all cancelled → cancelled/cancelled.
21. **Fair global scheduling (operator).** A permanent supervised
    `Objectives.Runs.Scheduler` grants global capacity round-robin across
    active fan-outs while preserving durable FIFO order within each fan-out.
    It is signal/monitor-driven, reconstructs from Objectives, and restarts
    temporary Coordinators after coordinator-only crashes.
22. **Full child lifecycle (operator).** A framed child begins with its task,
    not executable steps. RunServer therefore drives propose → evaluate →
    authorize → execute → observe → advance using the existing Objectives
    helpers directly, without serializing through `Engine.Agent`; settings are
    pinned independently around each lifecycle operation.
23. **Standalone sessions pause honestly (operator).** Attached
    `allbert serve` sessions continue background work. Quitting a standalone
    TUI/CLI pauses at durable boundaries and says that work resumes on the
    next Allbert boot; it does not silently imply a daemon handoff. Resume is
    honest about interruption: a child whose in-flight operation declared
    `retry_safety: :safe` auto-resumes; otherwise it waits `blocked`/
    `uncertain_effect` with an explicit retry affordance, and the quit notice
    and next-boot status both say so (Locked Decision 19). Quit waits a
    bounded grace for the current operation to reach a durable boundary;
    after that it invokes tiered cancellation. An interrupted non-safe
    operation parks as `uncertain_effect` rather than being replayed.
24. **Restart-safe notify state and honest uncertainty (operator).** An
    additive delivery ledger persists delivery keys, provider message ids,
    throttle timestamps, attempts/errors, and one-time-offer state. Edit-capable
    channels resume the same status message after restart. If a provider may
    have accepted a send before receipt persistence, delivery becomes
    `uncertain`, autonomous retry is suppressed, and the report remains
    available through status/next-turn fallback with an audit warning.
25. **Parked-confirmation steering invalidates before replanning (operator).**
    Redirect/skip against a confirmation-blocked child durably resolves the
    old confirmation as `cancelled` with reason `superseded_by_steer` and the
    steering event id, records the directive, and replans under unchanged
    confirmation rules; the old approval can no longer execute.
26. **Pending-report delivery is acknowledged (operator).** Reading a
    pending report never consumes it. Runtime returns an opaque report receipt;
    callers acknowledge only after transport success, rendering/printing, or
    the public protocol's durable recording point. Failure/uncertainty reuses
    the same receipt; acknowledgement is identity-bound and idempotent.
27. **Origin addressing is exact, then re-proved (operator).** Framing stores
    the originating `thread_channel_refs` row id or stable digest plus channel,
    receiver-account reference, and local user. Notify loads that exact ref and
    re-proves its current identity mapping at send time. Missing, conflicting,
    or cross-user remapping suppresses delivery; stored free text is never an
    address.
28. **Three concurrency limits, one scheduler (operator).**
    `max_children_per_fanout` bounds decomposition size;
    `max_concurrent_runs_per_fanout` bounds one fan-out; and
    `max_concurrent_runs_global` is the Scheduler's fair global capacity.
    DynamicSupervisor uses a static safety ceiling and does not encode
    operator scheduling policy or count Coordinators as run slots.
29. **Public protocols fund a real continuation path (operator).** OpenAI
    non-streaming and ACP await join outside `Runtime.submit_user_input/1`
    through a fan-out continuation API; timeout returns the kickoff and keeps
    the report pending. OpenAI streaming uses real chunked SSE after kickoff
    flush. ACP prompt work is concurrent so `session/cancel` remains readable
    and routes to the run cancellation action.
30. **RunServer calls a lifecycle facade (operator).** A new
    `Objectives.Lifecycle` facade owns CAS transitions, transactions, events,
    retry/attempt policy, confirmation parking, Registry/Runner invocation,
    observe, and advance. RunServer never treats private Jido command modules
    as a public execution API.
31. **Attached TUI status is an ephemeral subscription (operator).** TUI
    attach registers a user/session/thread-scoped SignalBus subscriber and
    unregisters on exit. Pids/functions never enter Objectives; with no
    subscriber, durable status and report receipts remain for the next turn.
32. **Rollout default changes once, explicitly (operator).** M3 builds and
    tests with `explicit`; after its accuracy/cross-surface acceptance, the
    shipped schema default changes to `automatic` in the M3 commit. Existing
    operators who explicitly stored `explicit` remain explicit; absence of a
    stored key adopts the release default. M0 records whether the key already
    exists in the operator-neutral test baseline.

## Operator-Visible Win

Ask for a multi-part job on ANY channel — "research X, summarize Y, and draft
Z" from Telegram, the TUI, or the workspace — and Allbert answers in seconds
with the task list and starts working. The channel stays usable: ask for
status, redirect a task, or cancel one, in plain language, from the same
thread. Opt a channel into autonomous notify and completion reports arrive on
their own; leave it off and the report greets the next message. Long-running
actions finally die when told to — including their OS child processes.

## Planned Interfaces

- `AllbertAssist.Objectives.Fanout` — `frame/2` (decomposition → parent +
  children), `children/1`, `join_status/1`, `report/1` (redaction-safe join
  report).
- `AllbertAssist.Runtime` — the existing response gains an opaque
  `fanout_start_receipt`; an additive acknowledgement function records
  caller delivery idempotently and makes the durable child queue runnable.
  The receipt is a continuation handle, not authority. Exact additive facade:
  `acknowledge_fanout_start(receipt, delivery_context) :: :ok | {:error, term}`;
  `await_fanout(parent_id, user_id, timeout_ms) :: {:ok, report} |
  {:timeout, kickoff}`; `subscribe_fanout(parent_id, user_id, sink) ::
  {:ok, subscription_ref} | {:error, term}`; and
  `acknowledge_report_delivery(receipt, delivery_context) :: :ok |
  {:error, term}`. Public protocols await/stream outside
  `submit_user_input/1`; `pending_reports` entries carry the report receipts.
- `AllbertAssist.Objectives.Lifecycle` — the RunServer-facing domain facade
  for transactional/CAS lifecycle operations, registered Runner dispatch,
  confirmation parking, durable observation, and retry decisions. Private
  Engine command modules remain private.
- `AllbertAssist.Objectives.Runs.{Supervisor, Coordinator, RunServer,
  Scheduler, Registry, CancelToken}` — the supervised executor (S2);
  Scheduler owns fair global capacity grants and live reconciliation while
  Objectives remains durable truth.
- `AllbertAssist.Intent.Decomposer` — Stage-0 (S3): `propose/2` returning
  `{:fanout, [task]} | :single`, model-optional, advisory.
- `AllbertAssist.Channels.Notify` — the runtime-initiated notification
  boundary (S6, ADR 0084): `deliver(user_id, channel, kind, payload, opts)`;
  authority-checked (settings + Security policy + identity map + origin
  binding) before any adapter dispatch; delivery via the existing
  `deliver_outbound/3`.
- `AllbertAssist.Channels.NotifyConsentCallback` — parses only the exact typed
  command/button, re-proves channel/user identity, and invokes registered
  `configure_channel_setting` through Runner; free text cannot reach it.
- New registered actions: `steer_objective_run` (`confirmation: :not_required`,
  advisory reframe), `cancel_objective_run` (`confirmation: :not_required`, scope
  child|fanout, all three tiers of ADR 0085); `list_objectives`-family output
  extended with fan-out trees (additive).
- Registered action metadata gains optional
  `retry_safety: :safe | :unsafe | :unknown`, default `:unknown`; M2 sweeps
  the shipped catalog (Locked Decision 19).
- `AllbertAssist.Execution.{ProcessOwners, ProcessOwner, ProcessRegistry}` —
  DynamicSupervisor (`restart: :temporary`) + per-execution port owner +
  unique Registry keyed `{:execution, execution_id}`; application-tree
  sibling of the execution runners (S4/ADR 0085), serving fan-out AND
  non-fan-out executions alike.
- `AllbertAssist.Channels.Notify.Delivery` — additive operational ledger for
  idempotency/delivery key, local user/channel/origin identity, state,
  provider message id, throttle timestamp, attempt/error class, and
  one-time-offer state; it stores no payload, secret, raw external identity,
  or free-text transport address.
- `AllbertAssist.Channels.TUI.Subscriptions` — ephemeral attached-session
  registry/subscriber bridge keyed by local user/session/thread; it stores no
  durable execution state.
- `AllbertAssist.Runtime.Audit` gains `:channel_notify`, routed to a redacted
  append-only monthly markdown writer at
  `<ALLBERT_HOME>/channels/notify/audit/YYYY-MM.md`; `Paths` gains the
  corresponding root resolver and `ensure_home!/0` entry.
- New signals (full taxonomy in the request flow):
  `allbert.objectives.fanout.{proposed,started,joined}`,
  `allbert.objectives.run.{started,progress,blocked,steered,completed,failed,cancelled}`,
  `allbert.channels.notify.{delivered,suppressed,failed,uncertain}`.
- New Settings Central keys (schema fragments, ADR 0031):
  `objectives.fanout.enabled` (default `true`),
  `objectives.fanout.rollout_mode` (`"explicit" | "shadow" | "automatic"`;
  build default `"explicit"`, release default `"automatic"` only after M3
  gates pass),
  `objectives.fanout.max_concurrent_runs_per_fanout` (default 3, clamp 1..8),
  `objectives.fanout.max_concurrent_runs_global` (default 6, clamp 1..32),
  `objectives.fanout.max_children_per_fanout` (default 8, clamp 2..16),
  `objectives.fanout.confirm_before_start` (default `false`);
  `channels.<id>.autonomous_notify.enabled` (default `false`),
  `channels.<id>.autonomous_notify.level`
  (`"completion"` | `"status_and_completion"`, default `"completion"`),
  `channels.<id>.autonomous_notify.min_interval_seconds` (default 30,
  clamp 5..600). Email's level is forced `"completion"` (digest-class).
- Channel descriptor additive field `streaming:`
  (`:turn_complete | :progress_messages | :live_region`, default
  `:turn_complete`), consumed by `ChannelParity` (S7).
- Channel descriptor additive field `status_update_mode:`
  (`:append_only | :edit_in_place`, default `:append_only`), consumed by
  `ChannelParity` and `Channels.Notify` (S7b, Locked Decision 11).
- Runtime response additive fields: `fanout` (parent id, children, ack list,
  delivery state), `fanout_start_receipt`, and `pending_reports` entries with
  opaque `report_delivery_receipt` values (Locked Decisions 13/15/26).
- `mix allbert.test release.v11` — the release gate.

## Code Seams

**S1 — objectives fan-out model (M1, ADR 0083).** Additive migration
with exact objective columns: `fanout_role`, `join_policy`, `join_outcome`,
`kickoff_delivery_state`, `fanout_start_receipt_digest`,
`report_delivery_state`, `report_delivery_receipt_digest`,
`origin_thread_ref_id`, `origin_thread_ref_digest`,
`origin_receiver_account_ref`, `queue_position`, `run_attempt_count`, and
`review_reason`. Kickoff states are `pending | blocked | acknowledged |
cancelled`; report states are `not_ready | pending | delivered`. Add a
`parent_objective_id` lookup index and unique non-null receipt-digest indexes;
acknowledgements use compare-and-set transitions. The origin digest covers
channel + opaque receiver-account ref + provider-thread key and stores no
external user id or free-text address.
`Objectives.Fanout.frame/2` creates the parent + children in one transaction,
copying `source_channel`/`source_surface`/`source_thread_id`/`session_id` to
each child; `children/1` and `join_status/1` are pure reads;
`report/1` builds the enumerated per-child join report (redaction-safe via
`Runtime.Redactor`). Join reduction is explicit: all completed →
`completed/success`; any completed plus failed or cancelled →
`completed/partial`; no completed and any failed → `failed/failed`; all
cancelled → `cancelled/cancelled`. The report always enumerates. Objective
events record every transition. The serialized engine
agent is untouched except rehydration (S2); existing single-objective flows
prove identity via the objectives suites.

**S2 — supervised run executor (M2, ADR 0083).**
One global `Objectives.Runs.Supervisor` (DynamicSupervisor with a static safety
ceiling above the maximum Scheduler capacity) + `Objectives.Runs.Registry` (Elixir Registry,
unique keys) added to the application tree near the coding precedent
(`application.ex:35-36`). A permanent plain-GenServer `Runs.Scheduler`
(pragmatic-substrate choice documented in `@moduledoc`) grants slots fairly
round-robin across fan-outs, preserves each fan-out's durable FIFO order,
monitors Coordinators, and reconstructs/restarts them from Objectives without
polling. `Coordinator` (GenServer per active fan-out,
`restart: :temporary`): derives its FIFO queue from durable queue positions,
starts ≤ `max_concurrent_runs_per_fanout` subject to the Scheduler's
`max_concurrent_runs_global` grants, monitors runs, applies the bounded restart
policy from the persisted attempt count (one restart per child, then terminal
`failed`), performs join
reduction, emits `fanout.joined`. `RunServer` (plain GenServer — pragmatic
substrate: thin idle→running→cancelling lifecycle, Registry-addressed
steering mailbox; Jido.Agent's routing buys nothing here; documented in
`@moduledoc`): executes the child's full lifecycle
(propose → evaluate → authorize → `Actions.Runner.run/3` → observe → advance)
through `Objectives.Lifecycle` — NOT through the serialized engine agent or
private Jido command modules — pinning
settings around each lifecycle operation (Locked Decision 9), emitting
`run.*` signals on the
SignalBus, checking its `CancelToken` between operations, and parking on
`needs_confirmation` (step `confirmation_id`, status `blocked`) without
blocking siblings. Scheduler boot/live reconciliation uses the existing
rehydrate window to restart coordinators for parents with non-terminal
children. Only actions declaring `retry_safety: :safe` auto-resume;
`:unsafe`/`:unknown` and a possibly committed external
effect with no durable observation parks as `blocked`/`uncertain_effect`
until an explicit retry or skip.
Backpressure proof: queued children start only as slots free; forced
`Process.exit(pid, :kill)` → bounded restart → honest join; BEAM restart
mid-fan-out resumes.

**S3 — Stage-0 decomposer + ack (M3).** `Intent.Decomposer.propose/2` runs
BEFORE the router for eligible new turns (no typed command): deterministic
splitter first
(explicit enumerations, "then/and then" chains, numbered lists), model
proposal second (local model, `propose_steps`-style prompt, JSON list,
bounded by `max_children_per_fanout`). Any prompt judged to contain at least two
independent tasks may fan out; explicit parallel language is not required.
Single-task, uncertain, unsupported, and nested proposals return `:single`.
An otherwise valid proposal above `max_children_per_fanout` returns a clarification turn
showing the count and asking the operator to narrow or explicitly batch it;
no task is silently dropped or merged.
Latency guard: the model proposer is consulted ONLY when cheap deterministic
pre-signals (enumeration/conjunction/list shape) indicate plausible
multi-task; single-task prompts must not pay a model round-trip — M3
acceptance carries a measured decomposer-overhead budget on `:single` turns
(cited from the metrics store; the M8.4 −67% turn win is not given back).
Explicit single-task language ("as one task", "together", "don't split")
deterministically forces `:single` — the per-prompt opt-out mirror of the
broad eligibility rule; corpus rows cover both directions.
Decision path: a `:fanout` proposal frames via S1 and returns the kickoff ack
(list + child ids + steering hint, plus the REPORT DELIVERY MODE for this
channel — "reports will arrive here" when autonomous notify is on, or "I'll
report when you next message; enable auto-notify in settings to get it
pushed" when off — the operator always knows where the results will land)
plus a start receipt. It does NOT start S2.
Every Runtime caller acknowledges successful render, transport, CLI output,
or durable Job/public-protocol kickoff record through one idempotent boundary;
only that acknowledgement makes children runnable. Delivery failure leaves
the parent blocked for retry/cancel; operator retry/status re-renders and
reuses the receipt. OpenAI-compatible/ACP callers persist the kickoff, then
use `await_fanout/3` outside `submit_user_input/1` and HOLD until join per
Locked Decisions 17/29. Timeout returns the kickoff while the report remains
pending behind its report-delivery receipt. OpenAI SSE uses a real chunked
response: flush kickoff, acknowledge start, subscribe, emit throttled status,
then join + `[DONE]`; disconnect before flush never starts work. ACP prompt
execution is concurrent with the stdio reader so `session/cancel` remains
serviceable and targets the active run.
Concretely, OpenAIController switches the streaming branch from buffered
`send_resp/3` to `send_chunked/2` + checked `chunk/2` calls; a failed kickoff
chunk does not acknowledge start. `Acp.Server.serve_stdio/0` becomes an owner
loop: it keeps reading lines, starts prompt workers with the existing
`AllbertAssist.TaskSupervisor.async_nolink`, tracks request/session → worker +
fanout ids in process memory, and serializes worker updates back to stdout.
`session/cancel` cancels the mapped fan-out through the registered action;
server restart relies on durable Objectives/report receipts, not worker state.
On every subsequent turn, Runtime reads eligible `pending_reports` without
mutating their delivery state and returns an identity-bound report receipt.
Each caller acknowledges that receipt only after successful transport,
render/print, durable Job event, or public-protocol recording. Failed or
uncertain delivery reuses the same receipt; an atomic compare-and-set prevents
double consumption and cross-user/thread acknowledgement.
`confirm_before_start` adds a durable
confirmation before an acknowledged queue may run. The decomposer NEVER runs
on steering turns (S5 classifies against active fan-outs first).

**S4 — cancellation, all three tiers (M4, ADR 0085).**
`Objectives.Runs.CancelToken`: per-run token (Registry-keyed atomics/ETS
flag) checked by the RunServer between operations and offered to actions via
runner context (`context[:cancel_token]`, cooperative — long-running actions
opt in by checkpointing). Each OS execution has a supervised
`Execution.ProcessOwner` that owns the execution port independently of
RunServer: a new `Execution.ProcessOwners` DynamicSupervisor
(`restart: :temporary` children, application-tree sibling of the execution
runners — placed in `application.ex` near the coding supervisor precedent,
NOT under `Objectives.Runs.Supervisor`, because non-fan-out executions —
coding turns, direct commands — use it too) plus a unique
`Execution.ProcessRegistry` keyed `{:execution, execution_id}`. Cancellation
addresses that owner before terminating RunServer; execution control-channel
EOF kills its process group on owner/BEAM failure. Supervised shutdown then drains
RunServer to the last durable step. OS tier: `Execution.LocalRunner` (and
`skill_script_runner`, sandbox `command.ex`) move from bare `System.cmd` to
the existing packaged execution substrates: `erlexec` process groups
(`{group, 0}` + `kill_group`) for portable macOS/Linux execution, MuonTrap
with cgroups where configured for stronger Linux descendant containment, and
the container runtime's stop/kill handle for container execution. Each
provides a scoped handle owned by `ProcessOwner`, with kill semantics
SIGTERM → grace (config, default 5s) → SIGKILL; timeout paths
(`local_runner.ex:53-56`) use the same kill instead of orphaning. TUI escape
extends beyond coding turns: escape during an attached fan-out offers
cancel. `cancel_objective_run` action drives: cooperative flag → supervised
shutdown → OS kill, recording tier reached in the objective event.
M4 begins with a red-first capability matrix covering owner death, timeout,
ordinary child/grandchild cleanup, sibling isolation, output/env/cwd parity,
and packaged execution. A new C or Rust helper is prohibited unless a named
acceptance row fails against every applicable existing substrate and the
plan/ADR are amended with that evidence. Process-group scope is stated
honestly: it covers members that remain in the captured group; descendants
that create a new session can escape. Linux cgroups and containers provide
the stronger containment contract where available.
WSL2 must pass cooperative-token and supervised-shutdown proofs. Its Linux
artifact uses the same process-owner API, but descendant containment beyond
ordinary process-group members remains Tier-2 best-effort and is reported
explicitly rather than claimed equivalent to native Linux cgroups.
**Delegate cancel-token propagation (operator fold-in, 2026-07-18 intake
walk — supersedes the earlier out-of-scope note):** the `:delegate_agent`
contract gains the cooperative token — `AgentRegistry.dispatch/4` passes
`cancel_token` in the delegate signal payload; the delegate developer
contract (docs/developer/delegate-agents.md) documents checkpoint polling;
the two shipped consumers (StockSage specialists, research.specialist)
adopt checkpoints at their natural boundaries. A cancelled child's delegate
call now returns `:cancelled` at the next checkpoint instead of computing
to its timeout with a discarded result; unconverted third-party delegates
degrade to exactly the old tier-2 behavior (token unchecked ⇒ supervised
shutdown after grace) — additive, never breaking. ADR 0085's consequence
note updates in the same commit.

**S5 — steering (M8).** Extend the `:continue_objective` seam: fan-out
parents/children in the candidate pool carry fan-out metadata
(`engine.ex:1223-1252` collection + `:273-291` decision attrs). A new
`Intent.Steering` classifier (advisory: deterministic verb/reference
heuristics + optional local-model assist) refines a `:continue_objective`
decision targeting an active fan-out into
`{:steer | :adjust | :cancel | :status | :new_request}` with a child
reference resolution (ordinal, title fuzzy match, "the research one").
Unqualified `status` aggregates all active fan-outs in the thread/session
from durable state (no engine dispatch); `steer/adjust` →
`steer_objective_run` (directive recorded as an objective event, delivered
Registry-addressed to the RunServer, applied at the next step boundary —
never mid-operation); `cancel` → `cancel_objective_run`; `new_request` falls
through to the normal pipeline. A mutating request with more than one
plausible target clarifies; it never silently chooses the newest run. Free
text never approves confirmations
(Locked Decision 10; eval row). Misclassification cost is bounded: steer
directives are advisory and reversible; cancel asks the classifier for high
confidence or falls back to a clarify turn.
Steering a child parked on confirmation first resolves that confirmation as
`cancelled` with `superseded_by_steer` + steering event id through the durable
confirmation store, then records the directive and replans; the old callback
can no longer execute.

**S6 — autonomous notify + per-adapter delivery (M6, ADR 0084).**
`Channels.Notify` boundary: resolves the fan-out's origin
(`source_channel` + the exact persisted thread-ref id/digest and
receiver-account reference), re-derives the transport target from the current
identity map (never from stored free text), rejects missing/conflicting/
cross-user remaps, checks — in order — channel live-use,
`channels.<id>.autonomous_notify.enabled`, level, min-interval throttle
(coalescing statuses per fan-out), Security policy, then dispatches via the
adapter's `deliver_outbound/3` with a threading hint in `opts`. Every
decision (delivered/suppressed/failed/uncertain + reason) first updates the
additive delivery ledger and is then signaled. Redacted operator evidence
appends through `Runtime.Audit` kind `:channel_notify` to
`<ALLBERT_HOME>/channels/notify/audit/YYYY-MM.md`. The ledger state machine is
`reserved → sending → delivered | failed | uncertain | suppressed`; a unique
delivery key reserves before transport, definitive failures may receive the
one bounded retry, possible provider acceptance becomes `uncertain`, and an
audit-write failure is surfaced but never converts uncertainty into a retry.
Ledger rows are not automatically pruned in 1.1; future retention policy must
preserve terminal idempotency keys and requires an explicit operator decision.
The additive `channel_notify_deliveries` table stores `delivery_key` (unique),
fanout/child id, local user id, channel, origin-thread-ref id/digest,
notification kind, state, provider message id, throttle timestamp, attempt
count, error class, one-time-offer state, and timestamps; it stores no payload
body, external user id, secret, or free-text address.
Unique terminal-event keys deduplicate completion; stored provider message ids and throttle
timestamps restore edit/coalescing state after restart. A send that may have
succeeded without a persisted receipt becomes `uncertain`, is not
autonomously retried, and falls back to status/next-turn retrieval with an
audit warning.
Background-raised confirmations are a distinct notify kind
(`:confirmation_request`) rendered with the channel's approval primitives
(buttons where available, typed `ALLBERT:APPROVE:<id>` everywhere) — the
existing `ConfirmationCallback` path completes the loop untouched.
`pending_reports` rendering rule (all adapters): compact per-fan-out summary
lines (parent title + join outcome + per-child status glyphs), never the full
multi-child report prepended to an unrelated answer — the full report renders
on a status query or in the web/TUI objectives surfaces.
**One-time notify offer (operator, readiness audit 2026-07-18):** a remote
channel's FIRST fan-out appends a one-time interactive offer to the kickoff
ack — "reply `ALLBERT:NOTIFY:ON` to get reports pushed here" (buttons where
the channel has them). Accepting invokes registered
`configure_channel_setting` through Runner to write
`channels.<id>.autonomous_notify.enabled=true`; a dedicated callback re-proves
channel/user identity with the same rules as `ConfirmationCallback`. The
durable marker is written when the offer is successfully delivered, so
ignoring it, dismissing it, or answering it never causes a repeat. Free text still never
changes settings — only the typed command/button does.
Per-adapter work items (from the Current Code State table):
  - **telegram**: pass threading hint (`reply_to_message_id`/thread id) from
    `opts`; kickoff/status/completion render via existing renderer; buttons
    for confirmation notify. (Outbound exists — smallest delta.)
  - **email**: IMPLEMENT `deliver_outbound/3` via the existing `SmtpClient`
    reply path (closes the ADR 0063 gap for email as a side effect);
    completion-only level; threaded reply (`in_reply_to`) to the origin
    message.
  - **discord**: threading hint → `message_reference`/thread id on
    `create_message`.
  - **slack**: threading hint → `thread_ts` on `chat_post_message`.
  - **matrix**: threading hint → `m.thread` relation on `send_message`
    (client already PUT-capable; `allowed_room_ids` validation stands).
  - **whatsapp**: threading hint → `context_message_id`; stays
    ReleaseAvailability-gated.
  - **signal**: wire the MuonTrap daemon receiver
    (`Daemon.daemon_child_spec/2` → supervised under the adapter's plugin
    child spec per ADR 0058) so inbound/steer works; notify via existing
    `send_message`; stays ReleaseAvailability-gated.
  - **tui**: no `deliver_outbound` (correct — local surface): attached
    sessions register through `Channels.TUI.Subscriptions` and render
    SignalBus `run.*` events through LiveRegion; the request-scoped
    `stream_event_sink` remains coding-turn-only. Unattached TUI gets the
    next-turn report receipt.
  - **web**: SignalBridge already forwards objective events
    (`workspace_live.ex:718`); no Notify involvement (Locked Decision 13
    boundary).

**S7 — parity/streaming contract (M5).** Channel descriptors gain optional
`streaming:` (default `:turn_complete`); `ChannelParity.row/2` reads the
declaration instead of the `:98` hardcode; `verify/1` validates the enum and
that `:live_region` is only declared by local surfaces/TUI. Declarations:
web + tui `:live_region`; telegram/discord/slack/matrix/whatsapp/signal
`:progress_messages`; email `:turn_complete`. A NEW
`channel_parity_test.exs` (the M5 focused command; no parity unit test
exists today — only `v055_tui_channel_eval_test.exs` touches
`ChannelParity`) asserts declaration-derived rows, and the `docs/operator`
channel matrix regenerates.

**S7b — capability-declared in-place status (M7, Locked Decision 11).**
Channel descriptors gain a second additive field `status_update_mode:`
(`:append_only | :edit_in_place`, default `:append_only`); `ChannelParity`
derives a new column from it exactly as S7 derives `streaming:`, and
`verify/1` validates the enum. Declarations: telegram/discord/slack/matrix
`:edit_in_place`; email/whatsapp/signal `:append_only`; tui/web are local
surfaces. NEW client functions per adapter (none exists today — Current Code
State): telegram `editMessageText` (Bot API), discord
`PATCH /channels/{channel_id}/messages/{message_id}` (REST), slack
`chat.update` (Web API), matrix `m.replace` relation via the client's
whitelisted PUT send. The delivery ledger persists the status-message id per
(fan-out, channel), so restart continues editing the same message. Where
`:edit_in_place` is declared, subsequent status updates EDIT that message;
edit failure falls back to an append, audits the reason, and replaces the
stored provider id. Completion reports and `:confirmation_request`
notifications are ALWAYS new messages — never edits. Throttle interplay: an
edit is a delivery for the min-interval/coalescing window (edits cannot
evade the throttle).

**S8 — web/TUI full experience (M9).** Web: workspace objectives surface
renders fan-out trees (parent + child status chips, live via the existing
`:objective_event` path), kickoff ack in the chat thread, steering affordances
(per-child cancel buttons dispatching the registered actions through the
normal action path — LiveViews render and dispatch only), join report
rendering, notify settings in Settings Central with the Security Central
boundary copy. **Per-objective in-shell destination (operator fold-in,
2026-07-18 intake walk):** the fan-out tree work also gives every objective a
per-objective in-shell view (`workspace:objectives` destination with an
objective param), and the v1.0.2 M5 chat chips re-point to it — restoring the
per-objective deep-links M5's shell-consolidation lost, where users feel it. TUI: fan-out ack rendering, live status region while attached
(S6), `status`/steer/cancel by reply, escape-cancel offer, notify settings via
settings CLI; quitting with active fan-outs surfaces the active-run count and
what happens next: attached `allbert serve` work continues, while standalone
quit waits a bounded grace for the current operation's durable boundary, then
invokes tiered cancellation; safe work may resume next boot, while interrupted
unsafe/unknown work parks as `uncertain_effect` with retry/skip affordances.
There is no implied automatic daemon handoff. Attached status uses ephemeral
`Channels.TUI.Subscriptions`, not a durable request sink.

**S9 — eval rows + release gate (M10).** Extend the EvalInventory milestone
enum with `:v11` FIRST (the v1.0.2 lesson), then gate-bound rows (S9 list in
the request flow §Security Eval Rows): decomposition-grants-nothing,
notify-default-off, notify-cross-user-denied, notify-redaction,
steer-cannot-approve, cancel-kill-scope, fanout-cross-user-steer-denied.
Add `fanout-ack-cross-user-001` (foreign receipt acknowledgement denied) and
`fanout-report-ack-cross-user-001` (foreign report receipt denied),
`fanout-notify-origin-ref-cross-account-001` (exact-origin remap denied), and
`fanout-notify-consent-free-text-001` (free text cannot enable notify).
`@release_v11_steps = @release_v1_steps ++ @v11_focused_steps`; focused steps:
objectives fan-out suites, runs supervision/crash/rehydration proofs,
cancellation tier tests, notify authority suite, steering eval, parity
verify, per-adapter outbound unit suites (fixture transports), migration
round-trip, and the `:v11` AssertBinding sweep. Runner + step-fn clones write
`release_evidence/v11/`.

**S10 — dependency refresh (M11).** Standing rule: three-root `hex.outdated`
review table, dedup, disposition per entry (applied/scoped-out/blocked),
bounded applies in own commits. Named review items carried from v1.0.2:
req_llm (locked 1.13.0), phoenix_live_view `~> 1.1.0` pin, exqlite drift.

**S11 — docs, closeout, binary release (M12).** CHANGELOG `## v1.1.0`;
version bump lockstep (three `mix.exs`, SW `CACHE_NAME` + gzip — guards
enforce); operator docs (channels matrix, objectives guide, settings
reference, onboarding touchpoints); ADRs 0083/0084/0085 flipped to Accepted
in their proving commits; release-rehearsal §§1–6 (binary release: tag
WITHOUT `[skip-artifacts]` → CI → cosign → publish → tap fill → install
rehearsal + packaged smokes); automated all-surface parity/authority evidence
plus the one focused warm-TUI flagship validation in request-flow §J; post-tag
archive/roadmap administration.

## Development Lane Annotation

- **Parallel workstreams:** WS-A (S1+S2 objectives model + scheduler/executor — the
  spine); WS-B (S4 cancellation/OS-kill — evaluates and adapts packaged
  erlexec/MuonTrap/container substrates; touches Execution/sandbox, disjoint
  from objectives until the RunServer integration point); WS-C (S7 parity
  field + S6 per-adapter `deliver_outbound` deltas + S7b edit clients —
  eight independent plugin surfaces, parallelizable per adapter once the
  Notify boundary contract is fixed); WS-D may perform the S10 dependency
  REVIEW in parallel, but lockfile/application waves wait for M11.
- **Serial barriers:** M0 v1.0.5 baseline before everything. S1 strictly
  before S2 (durable model before executor).
  S2 before S3 (nothing to ack without runs); S3's Runtime continuation
  contract before its caller integrations, which rejoin only after all
  direct Runtime callers prove delivery-before-start. The app-registry
  boundary check is a pre-dispatch M2 barrier. S2 before S4's RunServer
  integration, and before S5 (nothing to steer). The `Channels.Notify`
  CONTRACT (S6 core) before the per-adapter fan-in of WS-C. S7's descriptor
  field before the parity regeneration in M5 acceptance. S7b (M7) after
  S7's descriptor-field pattern and the S6 Notify boundary (it extends
  both). M9 after S3/S5/S6
  (web/TUI consume all three). M10 after all feature milestones (gate binds
  them). M11 after feature churn settles (validate final topology once).
  M12 last; the tag is operator-held.
- **Focused tests/gates:** per-milestone focused commands are binding below;
  `release.v1` after every contract-adjacent milestone M1–M7;
  `inventory --check-tags` after every new test file (new files take
  a primary lane per test-strategy; runs/supervision tests are
  `global_process_serial` unless their audit proves ownership); no
  `mix | tail` pipes; metrics store records cited for every timing claim.
- **Full-precommit timing:** once at the M10 barrier and again at M12 RC
  assembly.
- **External smokes:** focused smokes on every configured released channel at
  M6/M7; M12 repeats the authoritative packaged matrix. Simulated adapter
  fixtures remain mandatory for gated WhatsApp/Signal.
- **Rejoin points:** parity matrix + operator channels doc regenerated at M5
  (and the `status_update_mode` column at M7) and re-checked at M12; drift
  review + docs reconciliation at M12 before
  tagging; triad archive + roadmap turnover only after published-artifact
  validation.
- **Integrated rejoin gates:** after M4, rejoin S1–S4 and run the focused
  objectives/runtime/execution suites plus `release.v1` before channel work
  depends on cancellation. After M8, rejoin S1–S8 and run fan-out ack,
  scheduler, cancellation, notify, decomposer, steering, and security-focused
  suites before either UI surface starts M9.

### Focused command matrix

Commands run from the OWNER APP directory (root `mix test` does not accept
`apps/…`-prefixed paths; plugin test dirs are addressed `../../plugins/…`
relative to `apps/allbert_assist`, the shape the gate runner itself uses —
`allbert.test.ex` `cd: app_cwd(owner)`). The child `test` alias migrates the
owned per-boot DB automatically.

| Milestone | Primary lane | Binding focused command |
|---|---|---|
| M0 | `global_process_serial` | `MIX_ENV=test mix allbert.channels --parity && MIX_ENV=test mix allbert.test metrics` (repo root; record, do not mutate baselines) |
| M1 | `db_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/objectives/fanout_test.exs test/allbert_assist/objectives/fanout_migration_test.exs test/allbert_assist/plan_build_test.exs` |
| M2 | `global_process_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/objectives/runs/scheduler_test.exs test/allbert_assist/objectives/runs/supervision_test.exs test/allbert_assist/actions/app_registry_boundary_test.exs` |
| M3 | `global_process_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/intent/decomposer_test.exs test/allbert_assist/runtime/fanout_ack_test.exs test/allbert_assist/runtime/fanout_callers_test.exs test/allbert_assist/public_protocol/acp_fanout_test.exs && cd ../allbert_assist_web && MIX_ENV=test mix test test/allbert_assist_web/public_protocol/openai_fanout_test.exs` |
| M4 | `external_runtime_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/execution/process_owner_test.exs test/allbert_assist/execution/process_group_cancel_test.exs test/allbert_assist/objectives/delegate_cancel_test.exs` |
| M5 | `pure_async` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/channels/channel_parity_test.exs` |
| M6 | `db_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/channels/notify_test.exs test/allbert_assist/channels/tui_subscriptions_test.exs test/allbert_assist/objectives/migration_round_trip_test.exs ../../plugins/allbert.email/test ../../plugins/allbert.signal/test` |
| M7 | `external_runtime_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/channels/notify_edit_test.exs ../../plugins/allbert.telegram/test ../../plugins/allbert.discord/test ../../plugins/allbert.slack/test ../../plugins/allbert.matrix/test` |
| M8 | `global_process_serial` | `cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/intent/steering_test.exs test/allbert_assist/objectives/fanout_steering_test.exs` |
| M9 | `liveview_serial` | `(cd apps/allbert_assist_web && MIX_ENV=test mix test test/allbert_assist_web/live/workspace) && (cd apps/allbert_assist && MIX_ENV=test mix test test/allbert_assist/channels/tui_test.exs test/allbert_assist/cli/tui_test.exs)` |
| M10 | `security_eval_serial` | `MIX_ENV=test mix allbert.test release.v11` (repo root) |
| M11 | mixed | `MIX_ENV=test mix allbert.test release.v11 && MIX_ENV=test mix allbert.test release.v1` (repo root, after every applied dependency wave) |
| M12 | mixed | `MIX_ENV=test mix precommit && MIX_ENV=test mix allbert.test release && MIX_ENV=test mix allbert.test release.v1 && MIX_ENV=test mix allbert.test release.v11` (repo root) |
| M12.15 | mixed serial | `MIX_ENV=test mix allbert.test release.v11` (repo root; frozen v1 plus the explicit 11-row sweep, core fan-out, channel/TUI, and web/OpenAI steps) followed by `MIX_ENV=test mix allbert.test release.v1`, `MIX_ENV=test mix allbert.test prepush`, and `MIX_ENV=test mix allbert.test release` at one clean SHA |
| M12.16 | mixed serial | focused Coordinator/Lifecycle/Runtime/TUI regressions + warnings-as-errors/format/Credo/docs, then `MIX_ENV=test mix allbert.test release.v11` at the clean pushed FV candidate; by explicit operator sequencing, `MIX_ENV=test mix allbert.test release` follows successful FV-01 and any focused remediation, before release closeout |
| M12.17 | `global_process_serial` + `db_serial` | exact raw-TUI ordered-turn regression, complete TUI + Runtime acknowledgement/caller + fan-out steering suites, static/docs/manifest gates, then `MIX_ENV=test mix allbert.test release.v11` at the clean pushed FV candidate; authoritative release still follows successful FV-01 |
| M12.20 | `global_process_serial` + `db_serial` + production-profile subprocess | fail-closed Coordinator recovery, pending-steer preservation, progress attempt reset, the five missing v1.1 release bindings, and actual pool-1 Allbert persistence/fan-in proof; then warnings/format/Credo/docs/Hex/Dialyzer, `release.v11`, pre-push, and authoritative release at one clean pushed SHA |

Configured-channel smokes are paste-executable and serial:
`MIX_ENV=test mix allbert.test external-smoke -- telegram`, `-- email`,
`-- matrix`, `-- discord`, `-- slack`, plus matching `-- inbound_telegram`,
`-- inbound_email`, `-- inbound_matrix`, `-- inbound_discord`, and
`-- inbound_slack`. Gated-channel evidence uses `-- whatsapp` and `-- signal`.
M6 runs delivery/inbound rows for every configured released channel; M7
repeats the edit-capable delivery rows; M12 repeats the authoritative set
against the packaged release. At least one released remote channel must have
live evidence at RC, or the operator must explicitly disposition the missing
live row; an empty configuration is not silently treated as all-channel PASS.

## Security Posture

This release ADDS an authority class and is planned like it:

- **Autonomous notify (ADR 0084)** is the only new grant: settings-gated per
  channel, default OFF, enforced at the single `Channels.Notify` boundary
  (settings + Security policy + identity-map target re-derivation +
  origin-thread binding + throttle), fully audited, redacted
  (`Runtime.Redactor` on all status/report text), and gate-bound by `:v11`
  eval rows. Attached-local-surface push (web LiveView, attached TUI) is
  explicitly OUTSIDE the class — no unattended transport send occurs.
- **Nothing else grants.** Decomposition output, steering directives,
  fan-out/run/child ids, and supervised processes carry zero authority; every
  child step executes registered actions with unchanged confirmation classes;
  free text never approves a confirmation; `send_channel_message` keeps
  `confirmation: :required` (operator-initiated sends are NOT relaxed by
  notify settings — different class, different boundary).
- **Cancellation kills only what the run spawned** (ADR 0085 scope rule:
  os_pid/process-group captured at spawn, kill addressed to that group only);
  OTP supervision remains a non-security boundary (AGENTS.md).
- **Inbound trust unchanged:** steering rides the existing channel identity
  mapping/trust classes (ADR 0016/0056/0059); an unmapped sender cannot
  steer, cancel, or receive notifications. E2EE-origin rules (signal) stand.
- Secrets stay encrypted/redacted end-to-end incl. notify payloads, audit
  rows, and release evidence.

## Non-Goals

- Workflow YAML `parallel_steps`/`for_each` promotion (stays reserved,
  ADR 0041).
- Edit-in-place status on channels without a declared edit capability —
  whatsapp (no business-initiated edit in the Cloud API), signal
  (partial/recent CLI support), email stay `:append_only` (Locked
  Decision 11); no adapter is forced to fake edits.
- Nested fan-out (a child framing its own fan-out) — single level in 1.1;
  the model rejects nested proposals.
- Remote/distributed delegate agents, operator no-code agent authoring, and
  the shared delegate-agent behaviour extraction (parked, future-features).
- Flipping whatsapp/signal release availability (code paths ship; release
  status is a separate operator decision).
- Typing indicators, presence, read receipts.
- Cross-user or multi-tenant fan-out semantics (single local-user model
  stands).

## Milestones

Execution modes: `[gate]` gate-proved-contract, `[smoke]` scripted host smoke,
`[browser]` real-browser manual, `[host]` cross-platform manual, `[model]`
real-egress model manual, `[channel]` live channel manual.

- **M0: Recon verify + baseline.** `[gate]` Start from shipped stable v1.0.5.
  Re-verify every Current Code State anchor at HEAD (file:line drift table in
  Build Progress); snapshot the parity matrix (`ChannelParity.table/1`
  output) and the channels operator doc as the before-state; record turn/gate
  baselines from the metrics store (decide per-turn, quick/high/prepush
  walls); confirm the signal-daemon and email-outbound gaps still hold.
  Record the actual post-v1.0.5 lane topology (v1.0.3 shipped no conversion
  claim), whether `objectives.fanout.rollout_mode` is absent in the neutral
  baseline, and every direct `Runtime.submit_user_input/1` caller.
  Also carry the app-registry entry's "verify first": establish whether any
  release since v0.15 already validates app-registry membership at the
  action boundary (Runner + app-scoping checks) — record the finding; the
  M2 pre-dispatch barrier scopes to whatever gap remains.
  *Acceptance:* anchor drift table + baseline table + the boundary-check
  verification finding committed in Build Progress before M1.
- **M1: Objectives fan-out model + migration (S1, ADR 0083).** `[gate]`
  Additive migration with the exact S1 columns/states, parent lookup index,
  unique non-null receipt digests, origin-ref digest, and compare-and-set
  acknowledgement transitions, with round-trip proof; `Objectives.Fanout`
  frame/children/join_status/report; join reduction + partial-failure
  enumeration red-first; plan/build coexistence proven (existing
  `start_plan_run` suites green unchanged). ADR 0083 stays Proposed until M2
  proves the executor. *Acceptance:* focused fan-out suite green; migration
  round-trip green; objectives + plan_build suites green unchanged;
  `release.v1` green.
- **M2: Supervised run executor + registry + backpressure (S2, ADR 0083).**
  `[gate]` Runs.Supervisor/Registry/Scheduler/Coordinator/RunServer plus
  `Objectives.Lifecycle` per S2;
  fair round-robin across fan-outs and FIFO within each; live Coordinator
  recovery; full child propose/evaluate/authorize/execute/observe lifecycle
  (model proposer fixture-stubbed at M2 — proposal ACCURACY is M3's corpus);
  the `retry_safety` sweep of the shipped action catalog (Locked Decision 19
  — table in Build Progress; read-only/idempotent actions `:safe`);
  per-operation settings pin red-first (Locked Decision 9: a settings write
  mid-run is visible at the next lifecycle operation, not mid-operation);
  signal-bus `run.*` emission;
  parked-confirmation non-blocking proof (one child blocked on
  `needs_confirmation`, siblings progress). Supervision proofs: concurrent
  independent progress (no engine-agent serialization — asserted by
  timing/tracing), forced `Process.exit(pid, :kill)` → bounded restart →
  honest join, BEAM restart
  rehydration (covering BOTH a `:safe` in-flight auto-resume AND an
  `:unknown` park), global/per-fan-out capacity and decomposition-limit
  independence, queue observability, durable
  FIFO/attempt
  reconstruction, unique Registry keys, and `:unsafe`/`:unknown`
  uncertain-effect recovery blocked for explicit retry/skip. Close the M0
  app-registry membership gap here, before any background dispatch. ADR 0083
  remains Proposed until M3 proves its ack/start decision.
  *Acceptance:* all named
  proofs green as focused suites; Repo contention measured and recorded from
  the metrics store; no polling loop (code review + signal-driven asserts);
  `release.v1` green.
- **M3: Stage-0 decomposer + visible-list ack (S3).** `[gate]`+`[model]`
  Deterministic splitter + model proposer + `:single` fallback; broad
  automatic eligibility guards; kickoff-ack response fields and start
  receipt (additive, ADR 0029 checked); idempotent kickoff and pending-report
  delivery acknowledgements
  wired through every Runtime caller (nine chat surfaces, CLI, ACP/OpenAI
  controllers, Jobs); API hold-until-join continuation proof (non-streaming
  response completes with the join report; timeout returns kickoff while the
  report remains pending until report-receipt acknowledgement; real chunked
  SSE flushes kickoff before start and streams status then report; ACP keeps
  reading and routes `session/cancel` while prompt work runs — Locked Decisions
  17/26/29); no child starts before
  acknowledgement; delivery failure persists blocked and retryable;
  `confirm_before_start` confirmation path; decomposer NEVER consulted on
  steering turns. Accuracy: a decomposition corpus of at least 200 rows,
  with at least 50 fanout-positive, 100 single-negative, and 50
  steering/active-objective-negative rows, is added to the intent eval
  harness. The automatic-mode gate requires fan-out precision ≥ 0.97,
  fan-out recall ≥ 0.85, and false-positive rate ≤ 0.01 on the combined
  single/steering-negative set; every channel/surface family is represented.
  The existing golden/corpus suites stay green
  (routing regression = the number one risk of a new Stage-0).
  *Acceptance:* numeric corpus floor recorded and gate-bound; golden set +
  corpus +
  cross-surface accuracy unchanged; caller matrix proves
  delivery-before-start, duplicate acknowledgement, failed delivery, and
  restart-safe retry; failed next-turn transport does not consume a report;
  exact-origin cross-account denial; overflow-clarification/no-task-loss proof;
  the absent-key release default changes from `explicit` to `automatic` only
  after these gates while an explicitly stored `explicit` remains; manual
  `[model]` spot-check recorded. ADR 0083 flips Accepted here.
- **M4: Cancellation + interruption, all three tiers (S4, ADR 0085).** `[gate]`
  CancelToken + between-operation checkpoints; supervised shutdown drain;
  supervised ProcessOwner lifetime independent of RunServer; execution-owner
  loss cleanup; red-first capability matrix for packaged `erlexec`, MuonTrap,
  and container-runtime handles; scoped pid/process-group/container capture
  in LocalRunner, skill-script runner, and sandbox command backends;
  SIGTERM→grace→SIGKILL
  escalation; timeout paths converge on the same kill (no more orphaned
  children on `Task.shutdown`); `cancel_objective_run` action drives
  cooperative → supervised → OS tiers with the reached tier recorded. Proof
  fixture: a spawn-a-sleeping-child-and-grandchild script whose process group
  is verified dead after cancel (red-first: prove today's orphan first);
  adjacent-run isolation and substrate-handshake failure fail-closed proofs
  on macOS and Linux; WSL2 cooperative/supervised tiers proven with its
  best-effort descendant-containment limitation recorded; process-group
  escape limitations recorded, with cgroup
  containment proved where configured; delegate token propagation and both
  shipped delegate checkpoint
  adoptions. ADR 0085 flips
  Accepted here. *Acceptance:* orphan-regression proof; per-tier focused
  suites; sandbox/skill-script/coding suites green unchanged; TUI escape
  offer works in an attached session.
- **M5: Channel parity/streaming contract renegotiation (S7).** `[gate]`
  Descriptor `streaming:` field + parity derivation + `verify/1` rules +
  declarations for all nine surfaces; parity/channel docs regenerate; the
  contract change is additive (absent field = `:turn_complete`).
  *Acceptance:* parity suite green with the new column honest per channel;
  `release.v1` green (Tier-2 additive check); before/after matrix snapshot
  in Build Progress.
- **M6: Autonomous-notify authority + all-adapter delivery (S6, ADR 0084).**
  `[gate]`+`[channel]` Settings schema fragments (all keys, defaults OFF);
  `Channels.Notify` boundary with the full check chain + coalescing/throttle
  + audit + redaction; confirmation-request notify kind reusing
  `ConfirmationCallback` for approval; next-turn `pending_reports` fallback;
  per-adapter deltas exactly as enumerated in S6 (email `deliver_outbound`
  NEW; signal daemon WIRED; threading hints for
  telegram/discord/slack/matrix/whatsapp; ephemeral TUI attached-session
  subscription + rendering with no durable PID/function;
  web untouched by Notify). Add the operational delivery ledger with its
  reserved/sending/terminal state machine, unique delivery keys, definitive
  versus uncertain error classification, and retention policy, plus the
  redacted monthly `Runtime.Audit :channel_notify` writer at the specified
  Allbert Home path. ADR 0084 remains Proposed
  until M7 proves its edit/restart decision. *Acceptance:*
  notify authority suite (default-off suppression, opt-in delivery, level +
  throttle, exact-origin/cross-account denial, identity-reproved one-time
  consent callback + successful-delivery marker, redaction, restart restoration, terminal
  dedupe, uncertain-send suppression/fallback, and audit persistence) green;
  additive ledger migration upgrades a pre-M6 database and rolls back/forward
  in the migration harness without losing existing rows;
  per-adapter outbound unit
  suites green on fixture transports; full simulated end-to-end evidence for
  whatsapp/signal is mandatory while ReleaseAvailability-gated, and their
  live smoke is required only when released and operator-configured. Live
  `[channel]` smoke on every other operator-configured channel is recorded
  in Build Progress; audit rows verified.
- **M7: Capability-declared in-place status (S7b).** `[gate]`+`[channel]`
  Descriptor `status_update_mode:` field + parity derivation (extends S7);
  the four adapter edit implementations (client functions are NEW: telegram
  `editMessageText`, discord `PATCH /channels/{channel_id}/messages/{message_id}`,
  slack `chat.update`, matrix `m.replace` relation via the whitelisted PUT);
  `Channels.Notify` edit-or-append logic — ONE status message per fan-out
  per channel where declared — with audited append fallback on edit
  failure; throttle interplay tests (an edit is a delivery for the
  min-interval window); completion/confirmation always new messages.
  *Acceptance:* per-adapter edit unit suites green on fixture transports;
  restart resumes the same provider message id;
  edit-failure fallback proof; parity matrix shows the new column honestly;
  completion/confirmation-never-edited proof; `release.v1` green. ADR 0084
  flips Accepted here.
- **M8: Steer-vs-new classifier + steering actions (S5).** `[gate]`+`[model]`
  Steering classifier (deterministic + optional model assist, advisory);
  child-reference resolution; `steer_objective_run`/`cancel_objective_run`
  actions (registered, param contracts, Registry-addressed delivery, next
  step boundary application); status queries from durable state; clarify
  fallback on low confidence; free-text-never-approves proof. Superseding a
  parked confirmation resolves it `cancelled` with
  `superseded_by_steer` + steering event id before replanning. Steering
  corpus has at least 150 rows with at least 25 each for
  steer/adjust/cancel/status/new-request plus ambiguous multi-target rows.
  Overall macro precision and recall are each ≥ 0.90; cancel precision is
  ≥ 0.99; free-text approval false positives are exactly zero. Existing
  intent accuracy suites remain unchanged. The operator-pulled
  App-Registry membership check is already implemented at the M2 action
  boundary; M8 consumes it and carries only its regression proof.
  *Acceptance:* numeric corpus floor gate-bound; accuracy suites green;
  end-to-end steer + cancel
  (cooperative and OS tiers) proven against a running fan-out in a focused
  integration test; redirect/skip cancels a parked confirmation before
  replanning; M2's boundary regression remains green.
- **M9: Web + TUI full experience (S8).** `[gate]`+`[browser]` Workspace
  fan-out tree, chat kickoff ack, live child status, steering affordances
  dispatching registered actions, join report rendering, Settings Central
  notify panel with boundary copy; TUI ack/live-region status/steer/cancel/
  escape-offer + settings CLI; TUI attached-session subscription registry
  with unregister/no-persist proof. LiveViews render-and-dispatch only (checked in
  review). *Acceptance:* liveview + TUI focused suites green; manual
  `[browser]` workspace walkthrough + one warm `mix allbert.tui` session
  recorded (AGENTS.md validation default).
- **M10: Eval rows + release.v11 gate (S9).** `[gate]` `:v11` enum extension
  FIRST; the eleven-row (minimum) eval set as AssertBinding tests; gate wiring
  `@release_v11_steps = @release_v1_steps ++ @v11_focused_steps` with runner/
  evidence clones (`release_evidence/v11/`). *Acceptance:* `release.v11`
  green first roll target; every new authority/deny path bound to a gate
  step; `release.v1` green.
- **M11: Dependency refresh (S10).** `[gate]` Standing-rule review table +
  bounded applies + scoped suites; own commit per wave; carried review items
  dispositioned (req_llm, PLV pin, exqlite). *Acceptance:* disposition table
  complete; scoped suites plus `release.v11` and `release.v1` green after
  every applied wave; M10 evidence is superseded by the final post-refresh
  run.
- **M12: Docs, RC, binary release + closeout (S11).**
  `[gate]`+`[smoke]`+`[host]`+`[browser]`+`[model]`+`[channel]` Purpose-bar
  check against every Purpose outcome (AGENTS.md — deferrals need operator
  sign-off); docs deliverables; version bump lockstep + CHANGELOG; final
  measurement tables from the metrics store; `mix precommit` + authoritative
  `mix allbert.test release` + `release.v1` + `release.v11` green at the RC;
  operator-held annotated tag `v1.1.0` (NO `[skip-artifacts]`); CI publish +
  cosign; tap fill; install rehearsal; the all-surface automated contract
  matrix and one focused flagship operator validation (request flow §J)
  executed and recorded; then post-tag
  archive/roadmap administration. *Acceptance:* Definition of Done complete;
  ledger recorded; triad archived only after published-artifact validation.

## Documentation Deliverables

- ADR 0083, 0084, 0085 (Proposed → Accepted at their proving milestones).
- `docs/plans/v1.1-request-flow.md` maintained as implementation changes.
- Operator docs: channels/parity matrix regeneration, objectives/fan-out
  guide section, Settings reference (fanout + autonomous_notify keys),
  Security Central notify boundary copy, notify-ledger/audit location and
  uncertain-delivery recovery, TUI help.
- Developer docs: test-strategy lane additions for runs/supervision suites;
  delegate-agents doc cross-reference to the run executor.
- `CHANGELOG.md` `## v1.1.0`.
- Roadmap ladder item 5 + future-features lifecycle updates at closeout
  (operator-owned; flagged intake candidates surfaced, never self-served).
- Triad archived to `docs/plans/archives/` after artifact validation.

## Definition Of Done

- Fan-out model + fair Scheduler/full-lifecycle executor landed with
  live/boot crash recovery, backpressure, and join-honesty proofs; ADR 0083 Accepted —
  `[gate-proved-contract]`.
- Every Runtime caller implements the idempotent delivery/start continuation;
  no child runs before kickoff delivery, failures stay blocked, duplicate
  acknowledgement cannot double-start, and retry survives restart —
  `[gate-proved-contract]`.
- Pending reports remain durable until their identity-bound delivery receipt
  is acknowledged; failed/uncertain next-turn delivery cannot lose or
  cross-deliver a report, and exact origin refs are re-proved at send time —
  `[gate-proved-contract]`.
- OpenAI-compatible/ACP hold-until-join delivers the join report in-band with
  real SSE/ACP continuation and cancellation, with timeout fallback to kickoff
  plus acknowledged `pending_reports` (Locked Decisions 17/26/29) —
  `[gate-proved-contract]`.
- Decomposer ships with a pre-M3 numeric contract: minimum corpus size and
  per-class coverage, a false-positive ceiling for automatic fan-out,
  precision/recall floors, and ZERO regression on the
  existing intent accuracy suites — `[gate-proved-contract]`.
- All three cancellation tiers landed incl. OS child-process kill with the
  orphan-regression proof; ADR 0085 Accepted — `[gate-proved-contract]`.
- Autonomous notify enforced at one boundary, default OFF, audited, redacted,
  restart-safe through the delivery ledger and append-only markdown audit,
  eval-row-bound; ADR 0084 Accepted; all nine chat surfaces deliver
  ack/status/steer per their primitives (email outbound implemented; signal
  daemon wired) — `[gate-proved-contract]` + `[channel]` attestations.
- Parity matrix derives streaming from declarations — `[gate-proved-contract]`.
- Capability-declared in-place status landed: the four declaring channels
  (telegram/discord/slack/matrix) edit ONE status message per fan-out with
  audited append fallback; completion/confirmation never edited; edits
  throttle-counted — `[gate-proved-contract]`.
- Steering corpus floor recorded; free-text-never-approves and
  steer-cannot-lower-confirmation proven — `[gate-proved-contract]`.
- Web operator behavior is gate-proved and the focused warm-TUI flagship
  walkthrough is operator-attested.
- `mix allbert.test release`, `release.v1`, `release.v11` GREEN at the tagged
  commit — `[gate-proved-contract]`.
- v1.1.0 published as a BINARY release: cosign-signed GitHub Latest, tap
  filled, install rehearsal recorded — `[operator-attested-smoke]`.
- The all-surface parity/authority matrix is gate-proved; remote-provider live
  smokes remain release-environment diagnostics rather than duplicative manual
  feature acceptance — `[gate-proved-contract]`.
- Dependency disposition table complete — `[gate-proved-contract]`.
- Docs deliverables + closeout administration complete — docs.
