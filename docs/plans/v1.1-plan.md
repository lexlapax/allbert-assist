# Allbert v1.1.0 Asynchronous Background Agent Fan-Out With In-Channel Steering

Request flow: `docs/plans/v1.1-request-flow.md`
ADRs: three new ADRs ship with this release — ADR 0083 (objectives parallel
child fan-out — binding on M1/M2), ADR 0084 (autonomous channel notification
authority — a NEW authority class, binding on M6), ADR 0085 (cooperative
cancellation and child-process kill — binding on M4). Consumes ADR 0021/0022
(delegate substrate), ADR 0016/0056/0059 (channel identity/trust), ADR 0063
(outbound compose boundary — the operator-initiated contrast), ADR 0029 (typed
response contracts — additive only), ADR 0041 (workflow YAML — fan-out does
NOT route through it), ADR 0058 (channel daemon supervision), ADR 0026
(runtime facades), and the v1.0 public-contract freeze (`release.v1` stays
green).

Status: **implementation-ready draft — planned 2026-07-18 against main at the
v1.0.2 M8 window.** Current Code State anchors verified 2026-07-18, including
a full read of the eight channel-adapter plugins under `plugins/` (the earlier
apps/-only recon missed them). M0 re-verifies anchors at build-start HEAD
(the v1.0.2 tag is expected to land first). Not started.

Source: roadmap ladder item 3 (`docs/plans/roadmap.md:43-50`) and the
future-features flagship intake (`docs/plans/future-features.md:1011-1037`,
operator intake 2026-07-18), plus the merged 1.1-slice enablers
Mid-Action Interruption / In-Flight Kill and Child-Process Cancellation
Semantics (`future-features.md:164-185`). Operator decisions of 2026-07-18
locked the concurrency model, decomposition posture, report-back authority
class, and all-channels rollout (Locked Decisions below).

**Open questions for the operator (everything else is settled):**

1. **RESOLVED (operator, 2026-07-18): App-Registry Membership Check At Action
   Boundary is IN 1.1.** The future-features slice annotation (`Slice: 1.1`)
   wins over the roadmap's 1.5/1.6 enabler listing (roadmap reconciled in the
   same commit as this plan). It also fits the flagship's surface: fan-out
   dispatches actions from BACKGROUND contexts, so the boundary must stop
   trusting caller-supplied app identity before that surface widens. M0
   carries the entry's "verify first" (whether a later release already added
   a check); M7 carries the implementation (the steering milestone already
   owns the action-boundary surface). No other open questions: the remaining
   judgment calls (ack semantics, settings-pin policy, cancel confirmation
   class, no edit-API adoption, signal inbound wiring, next-turn report
   fallback) are settled with evidence as Locked Decisions 8–14.

## Build Progress

Implementation not started. Each milestone: implement → focused test →
drift-check → doc update → commit (`v1.1 M<n> <small title>`). Build entries
land here per the v1.0.2 precedent (root-caused drift fixes, measured
acceptance from the test-metrics store, intake candidates held for operator
disposition — never written to future-features unilaterally).

## Purpose

Ship the 1.1 flagship: a prompt that decomposes into multiple tasks fans out
supervised background agents, streams their status, joins, and reports back to
the originating channel — while the channel stays open and mid-flight input is
contextually routed as steering vs a new request.

1. **Non-blocking fan-out turns.** A decomposable prompt returns an in-channel
   kickoff ack (the visible task list) in seconds; the work continues in a
   supervised OTP tree with durable state in Objectives. Channels stop owning
   turn latency for decomposed work.
2. **Parallel child-objectives with an honest join.** Child objectives under
   `parent_objective_id` execute concurrently under DynamicSupervisor with
   explicit backpressure; the join report always enumerates per-child
   outcomes; crash/restart rehydrates from Objectives (ADR 0083).
3. **In-channel steering.** Mid-flight input on the same thread is classified
   (advisory) as steer / adjust / cancel / status query / new request;
   effectful steering routes through registered actions and never lowers a
   confirmation class. Cancellation is real: cooperative cancel between
   operations, supervised shutdown, and OS child-process kill semantics
   (ADR 0085) — retiring the "operators can only wait out a long action" debt.
4. **Authorized report-back on every channel.** A NEW settings-governed
   authority class (default OFF, ADR 0084) lets Allbert autonomously deliver
   status/completion notifications per channel; without opt-in, reports wait
   for the next turn. All nine surfaces (tui, web, telegram, email, discord,
   slack, matrix, whatsapp, signal) get kickoff-ack, status/completion
   delivery per their primitives, and steer-by-reply.
5. **The parity contract tells the truth.** `streaming: "turn_complete"` stops
   being hardcoded; every channel declares its streaming capability and the
   parity matrix derives from declarations.

## Expected Inputs

- v1.0.2 tagged and published (binary Latest, tap at 1.0.2) with the
  test-isolation seams (ADR 0082), the metrics store
  (`.test_metrics/runs.jsonl` + `mix allbert.test metrics`), and the
  turn-scoped settings pin (`Store.with_resolved_settings/1`) landed. If the
  v1.0.2 tag has not landed at build start, M0 blocks until it does — this
  plan's baselines and gate composition assume it.
- The objectives substrate: durable objectives/steps/events
  (`objectives.ex`), the serialized engine agent, `parent_objective_id`,
  `source_channel`/`source_surface` attribution (v1.0.1), and the
  delegate-agent substrate (ADR 0021/0022, `AgentRegistry` +
  `:delegate_agent`).
- The channel substrate: eight plugin adapters + the web local surface, the
  `Channels.Outbound` boundary (ADR 0063), channel identity maps
  (ADR 0016/0056/0059), `ConfirmationCallback` typed commands, and the
  channel parity matrix.
- The gate machinery: `@release_v1_steps` prefix precedent
  (`allbert.test.ex:5526`, v101/v102 composition at `:5740/:5922`),
  EvalInventory + SecurityEvalCase (milestone enum at
  `test/support/security_fixtures/eval_inventory.ex:16-57` — extend it, the
  v1.0.2 M1 drift lesson), release-rehearsal runbook, release-artifacts CI.
- Recorded baselines from the metrics store: decide per-turn ~1,042ms
  post-M8.4; gate walls quick ~442s / high ~883s / prepush ~897s (v1.0.2 M8.4
  tables) — M0 re-baselines at HEAD.

## Current Code State

Verified 2026-07-18 on main (v1.0.2 M8 window). M0 re-verifies at build HEAD.

**Turn model — synchronous everywhere, one existing async escape.**
`Runtime.submit_user_input/1` (`apps/allbert_assist/lib/allbert_assist/runtime.ex:115-138`)
runs the full turn inline via `run_agent_turn/2` (`:508` coding branch,
`:517` default) under `@default_timeout_ms 120_000` (`:44`). Every channel
adapter blocks its poll/webhook/gateway handler on this call for the turn
duration; none passes `timeout_ms`. The web workspace is the exception in
presentation only: `WorkspaceLive` wraps the same blocking call in
`start_async` (`workspace_live.ex:2566`, results at `handle_async :ask`
`:762-785`) and receives push updates via SignalBridge PubSub
(`:84` subscribe; `:718` `{:objective_event, _}`; `:722` `{:fragment, _}`).
The request map already carries a `stream_event_sink` field
(`runtime.ex:59`, threaded at `:486/:501`) — the TUI coding turn is its only
consumer today; v1.1 reuses this seam rather than inventing a second sink.

**Settings pin — per-process, per-turn.** The whole intent turn runs under one
turn-scoped resolved-settings snapshot (`intent_agent.ex:170-184` wrapping
`respond_pinned`, `settings/store.ex:98-106` `with_resolved_settings/1`,
process-dictionary pin). The pin cannot cross process boundaries — background
run processes MUST establish their own (Locked Decision 9).

**Objectives — durable but strictly sequential.** The engine is ONE serialized
JidoBacked agent (`objectives/engine/agent.ex`, `use JidoBacked` at `:26`,
signal dispatch `timeout: :infinity` at `:160`) with signal routes for
frame/propose/evaluate/authorize/execute/observe/advance/cancel/continue/prune
(`:15-24`). One `current_step_id` per objective (`objectives/objective.ex:32`);
statuses `open running blocked completed cancelled failed abandoned`
(`objective.ex:11`); step statuses incl. `blocked`/`cancelled` and a
`confirmation_id` column (`objectives/step.ex:11,:30`). `parent_objective_id`
exists (`objective.ex:31`, migration `20260517000000_add_objectives.exs:17`)
and is consumed ONLY by plan/build runs (`plan_build.ex:42`,
`actions/plan_build/start_plan_run.ex:19`) — a fan-out discriminator column is
required for coexistence. `source_channel`/`source_surface` are plumbed at
create (`objectives.ex:82-84`, cast list `:35,:44,:46`) but NOTHING consumes
them for report-back (repo-wide grep: only unrelated e2ee threading uses).
Public API: `create_objective/1` (`objectives.ex:134`), `list_objectives/2`
(`:186`), `active_objectives/1` (`:220`), `create_step/1` (`:249`),
`transition_step/3` (`:282`), `create_event/1` (`:297`), `cancel/3` (`:111` —
no confirmation gate today), `continue/2` (`:122`).

**Delegation — blocking, monitored for eviction only.**
`AgentRegistry.dispatch/4` is a blocking `Jido.AgentServer.call/3`, default
5s (`objectives/agent_registry.ex:48-60`); registry monitors `:DOWN` solely to
evict dead entries (`:112-120`) — no join, no cancel, no status. The
`delegate_agent` action waits inline with default 180s, cap 900s
(`actions/objectives/delegate_agent.ex:43-44`, `delegate_timeout_ms/1`
`:155-159`).

**Intent — one Decision, active objectives already compete.** The engine emits
exactly one Decision through the two-stage router
(`intent/router/default_router.ex:17-24` → `InputGuard`/`Prefilter`/
`Disambiguator`; `prefilter.ex`, `disambiguator.ex`). A Stage-0 decomposer is
greenfield. Active objectives already join the candidate pool
(`intent/engine.ex:1223-1252` `objective_candidates/3`, up to 5
open/running/blocked) and win as `:continue_objective` decisions
(`engine.ex:273-291` `decision_attrs_for_candidate` for `kind: :objective`) —
this is the steer-vs-new seam to extend.

**Confirmations — channel-neutral records, no out-of-band delivery.**
`Confirmations.Origin` carries channel/thread/surface + `response_target`
(`confirmations/origin.ex:14-34`, `:29`) but nothing DELIVERS a
background-raised confirmation to a remote channel; approval-side plumbing
already exists channel-neutrally via typed commands
`ALLBERT:APPROVE|DENY|SHOW:<id>` (`channels/confirmation_callback.ex:15-33`)
and inline buttons on capable channels. Workspace `Emitters`
(`workspace/emitters.ex:35-147` incl. `objective_lifecycle/3` at `:119`) and
`Fragment` publication (`workspace/fragment.ex:324` via
`Jido.Signal.Bus.publish(AllbertAssist.SignalBus, ...)`;
bus started at `application.ex:34`) are workspace-scoped only.

**Channel parity — streaming hardcoded.** `channel_parity.ex:98` pins
`streaming: "turn_complete"` for every row ("until a later ADR changes that
contract" — this release's ADR 0084/M5). The `outbound` column is honest: it
live-checks `function_exported?(module, :deliver_outbound, 3)`
(`:141-155`). `Channels.Outbound.send/4` (`channels/outbound.ex:24-42`) is the
single operator-initiated boundary; the callback shape is
`deliver_outbound(target, body, opts) :: {:ok, map()} | {:error, term()}`
(`:21-22`), duck-typed (no adapter declares the behaviour).
`send_channel_message` (ADR 0063) is `confirmation: :required` and
identity-allowlist-gated before dispatch
(`actions/channels/send_channel_message.ex:17,:64-118,:149-160`).

**Per-adapter reality (full plugins/ read, 2026-07-18).** All eight adapters
are supervised GenServers (one child each under `Channels.Supervisor`,
`channels/supervisor.ex`); all block their inbound handler on
`Runtime.submit_user_input`; **none calls an edit API or typing indicator**
(verified: zero editMessageText / message-PATCH / chat.update / m.replace /
typing calls across all channel dirs):

| Channel | `deliver_outbound/3` | Transport | Inbound | Threading | Buttons | Status-update primitive available |
|---|---|---|---|---|---|---|
| telegram | `plugins/allbert.telegram/.../telegram/adapter.ex:915` | Bot API `sendMessage` (`client.ex:34`) | long-poll `:poll` loop (`adapter.ex:54`, 25s getUpdates, backoff ≤60s) | `reply_to_message_id` + `message_thread_id` (`:688`) | yes (inline keyboard + `answerCallbackQuery` `:823`) | post new message (edit API exists upstream, unused) |
| tui | **ABSENT** | local TTY | TTY loop (`adapter.ex:104,:112`), escape cancel (`:121,:173`) | `:rich` | typed commands | **richest**: async coding turn (`:437`), `stream_event_sink: self()` (`:447`), LiveRegion progressive render (`:186,:516`), live status line |
| email | **ABSENT** (inbound-triggered `deliver_reply` SMTP only, `adapter.ex:~466`) | gen_smtp | IMAP poll (`adapter.ex:47`, 60s) | `in_reply_to`/subject | no | threaded reply email only (digest-class) |
| discord | `plugins/allbert.discord/.../discord/adapter.ex:608` | REST `POST /channels/{id}/messages` (`client.ex:25`) | WebSocket gateway port (`adapter.ex:73,:115`) | native threads (`client.ex:77`) + `message_reference` | yes (interaction ack only, `gateway_port/real.ex:308`) | post new message |
| slack | `plugins/allbert.slack/.../slack/adapter.ex:650` | Web API `chat.postMessage` (`client.ex:45`) | Socket Mode WS (`adapter.ex:63,:79`) | `thread_ts` (`client.ex:272`) | yes | post new message (no `chat.update` in client) |
| matrix | `plugins/allbert.matrix/.../matrix/adapter.ex:601` (only adapter reading `opts`) | CS API `PUT .../send/m.room.message/{txn}` (`client.ex:22`); client whitelists GET/PUT send/sync ONLY (`client.ex:140-141`) | `/sync` long-poll (`adapter.ex:50,:122`, 30s) | `m.thread` relations (`:500-501`) | no (`:link` primitive) | post new message; `allowed_room_ids` outbound validation (`:630`) |
| whatsapp | `plugins/allbert.whatsapp/.../whatsapp/adapter.ex:793` (ReleaseAvailability-gated `:794`) | Cloud API `POST {phone_number_id}/messages` (`client.ex:28`) | HTTP webhook (`adapter.ex:32,:62`; controller `whatsapp_webhook_controller.ex:32`) | `context_message_id` reply chain | yes (`client.ex:42/:94`) | post new message |
| signal | `plugins/allbert.signal/.../signal/adapter.ex:526` (ReleaseAvailability-gated `:527`) | signal-cli JSON-RPC over UDS (`client.ex:17,:63`) | **GAP: daemon receiver not wired** — `Daemon.daemon_child_spec/2` (`daemon.ex:9-19`, MuonTrap per ADR 0058) has NO callers; `handle_notification` (`adapter.ex:54`) reachable only via `simulate_daemon_notification/2` (`:31`) | timestamp reply chain | no | post new message (after daemon wiring) |

**Cancellation/kill — the debt is real.** `Execution.LocalRunner` runs
`System.cmd` inside `Task.async` and `Task.shutdown(:brutal_kill)`s on timeout
(`execution/local_runner.ex:48-57,:59-70`) — the BEAM task dies but no OS pid
is captured and no process group is killed; the same shape exists in
`skill_script_runner.ex` and the sandbox command backends
(`sandbox/backends/command.ex`, `container_runner.ex`). The TUI escape path
cancels only the coding turn presentation. `CodingTurnSupervisor` is the
supervised-async template to generalize
(`coding/turn_supervisor.ex:145-159`: `Task.Supervisor.async_nolink` +
registration + await; `Registry` precedent `AllbertAssist.Coding.TurnRegistry`
at `application.ex:35`; `Task.Supervisor` at `:36`).

**Workflow YAML cannot fan out — by design.** ADR 0041 reserves
`parallel_steps`/`for_each` (`future-features.md:1099-1114`). v1.1 does not
route through it (Locked Decision 6).

**Eval/gate precedent.** EvalInventory milestone enum must be extended
(`eval_inventory.ex:16-57`; `:v066`/`:v1` were added late as a v1.0.2 M1
drift fix — do not repeat). Gate composition:
`@release_v11_steps = @release_v1_steps ++ @v11_focused_steps`
(`allbert.test.ex:5526/:5740/:5922` precedent); no predecessor bespoke steps.

## Locked Decisions

Decisions 1–4 are operator-locked (2026-07-18). Decisions 5–14 are derived
during planning from the locked four plus code evidence; each records its
rationale so the operator can veto at plan review.

1. **Concurrency = parallel child-objectives (operator).** A fan-out frames
   CHILD objectives under `parent_objective_id`, each executed by a supervised
   run process (DynamicSupervisor per run scope, `restart: :temporary`,
   Registry-addressed per-run keys); join = `Process.monitor` + child status
   reduction into the parent; durable state lives in Objectives (no private
   goal loops — the OTP tree is the executor); crash/restart ⇒ rehydrate from
   Objectives. ADR 0083 binds the model.
2. **Decomposition = model proposes, visible list (operator).** A Stage-0
   decomposer MAY use the local model to propose the task list (the
   `propose_steps` precedent); the list is ALWAYS surfaced in-channel before
   fan-out starts (default-on ack); deterministic/explicit decomposition works
   without a model; decomposition output is advisory-only and grants nothing
   (AGENTS.md: `objective_id`/`step_id` are never authority).
3. **Report-back authority = settings-level autonomous notify (operator).**
   Per-channel operator settings (Settings Central, default OFF) authorize
   autonomous status/completion notifications on that channel. Security
   Central holds the boundary. This is a NEW authority class with its own ADR
   (0084). The `turn_complete` parity contract is renegotiated per channel
   with an explicit streaming capability field.
4. **Rollout = ALL channels at once; steering extends `:continue_objective`;
   cancellation ships both tiers (operator).** Every adapter
   (tui/web/telegram/email/discord/slack/matrix/whatsapp/signal) gets
   kickoff-ack, status/completion delivery per its primitives, and
   steer-by-reply in 1.1. The steering classifier is advisory; effectful
   cancel/steer routes through registered actions + the confirmation floor
   (steering may never LOWER any action's confirmation class). Cancellation
   ships the merged enablers: cooperative cancel token between operations +
   supervised shutdown, AND OS child-process kill semantics
   (`future-features.md:164-185`). ADR 0085 binds cancellation.
5. **OTP pattern constraints (operator, baked into seams).** Supervised
   GenServer/Jido.Agent per the pragmatic-substrate rule with the choice
   documented in `@moduledoc`; never bare `Task.async` for steerable agents;
   monitors, not polling; Registry-addressed steering; signal-bus push status
   (no polling loops); DynamicSupervisor `max_children` backpressure with an
   explicit FIFO queue policy; no GenStage/Flow; Security Central authority
   unchanged — a supervised process has no authority by virtue of being
   supervised.
6. **No workflow-YAML routing (derived, ADR 0041).** Fan-out is an
   objectives-native mechanism; `parallel_steps`/`for_each` stay reserved
   vocabulary. Rationale: concurrency semantics belong in the supervised
   runtime, not operator-authored YAML; the ADR 0041 operability rationale
   stands.
7. **Additive-only contract evolution (derived, freeze).** The runtime
   response gains only additive fields (`fanout` summary, `pending_reports`)
   per ADR 0029 Tier-2 rules; channel descriptors gain an optional
   `streaming:` field with the current behavior as default; `release.v1`
   stays green throughout. No non-additive migration: the objectives schema
   change is additive columns + index (the 1.5-horizon migration-runner
   cluster is not pulled forward).
8. **Ack semantics (derived from "default-on ack").** The kickoff ack — the
   decomposition list with child ids/titles — IS the fan-out turn's response,
   delivered synchronously on every channel; visibility cannot be disabled.
   By default runs start immediately after framing (decomposition is advisory
   and each child step retains full action authority, so starting runs grants
   nothing). A new setting `objectives.fanout.confirm_before_start`
   (default `false`) converts kickoff into a durable confirmation for
   operators who want a hard gate. Rationale: matches the intake's "per user
   instruction or default behavior" and keeps the confirmation floor intact.
9. **Settings-pin policy for background runs (derived).** Each run process
   establishes its OWN resolved-settings pin per step execution
   (`Store.with_resolved_settings/1` around each step), because (a) the M8.4
   pin is process-dictionary-scoped and cannot cross into spawned processes,
   and (b) a fan-out may outlive the originating turn by hours — freezing the
   parent turn's snapshot for the whole run would make operator setting
   changes (e.g. disabling autonomous notify) invisible until restart.
   Semantics: deterministic within a step; operator changes take effect at
   the next step/notify boundary. Documented in the RunServer `@moduledoc`
   and tested red-first.
10. **Cancel needs no confirmation; steering never approves (derived).**
    `cancel` of your own fan-out/child is the safety action and follows the
    existing `Objectives.cancel/3` precedent (no confirmation today —
    `objectives.ex:111`); the OS-kill tier executes only against processes
    the run itself spawned (ADR 0085 scope rule). Steering directives are
    advisory reframes recorded durably; they may cause new effectful steps
    which confirm at their own actions, and free-text steering NEVER maps to
    confirmation approval — approvals stay on the typed-command/button path
    (`ConfirmationCallback`). Gate-bound eval rows enforce both.
11. **No edit-API adoption in 1.1 (derived).** Remote-channel status delivery
    is append-only new messages (coalesced + throttled). No adapter calls an
    edit API today and Matrix's client hard-whitelists methods; adopting
    edit-in-place status across six providers would multiply per-adapter
    scope for presentation polish. Edit-based in-place status is recorded as
    an intake candidate at closeout (operator disposition), not built.
12. **Signal inbound daemon is wired in-scope; live gating unchanged
    (derived).** "ALL channels" includes signal steer-by-reply, which is
    impossible while `Daemon.daemon_child_spec/2` has no caller — M6 wires
    the MuonTrap daemon receiver under the adapter (ADR 0058 pattern).
    WhatsApp/signal remain `ReleaseAvailability`-gated exactly as today; v1.1
    changes code paths, not release status — their validation runs simulated
    (webhook/notification fixtures) plus live where the operator has a
    configured live channel.
13. **Notify-OFF fallback = next-turn report (derived).** With autonomous
    notify OFF (the default), completed fan-outs deliver their join report as
    part of the user's NEXT turn on that thread (additive `pending_reports`
    response field rendered by every adapter) and are always visible in the
    web/TUI objectives surfaces. Attached live local surfaces (an open
    workspace LiveView, an attached TUI session) render push status without
    the ADR 0084 authority — the user is present and no unattended transport
    send occurs; ADR 0084 defines this boundary precisely.
14. **Fan-out discriminator (derived).** Additive columns `fanout_role`
    (`"parent"`/`"child"`/nil) and `join_policy` (default `"all_terminal"`)
    plus an index on `parent_objective_id` distinguish fan-out children from
    the existing plan/build `parent_objective_id` consumers
    (`plan_build.ex:42`) — the recon's "unused" claim was wrong; coexistence
    is explicit.

## Operator-Visible Win

Ask for a multi-part job on ANY channel — "research X, summarize Y, and draft
Z" from Telegram, the TUI, or the workspace — and Allbert answers in seconds
with the task list and starts working. The channel stays usable: ask for
status, redirect a task, or cancel one, in plain language, from the same
thread. Opt a channel into autonomous notify and completion reports arrive on
their own; leave it off and the report greets the next message. Long-running
actions finally die when told to — including their OS child processes.

## Planned Interfaces

- `AllbertAssist.Objectives.Fanout` — `frame/2` (decomposition → parent +
  children), `children/1`, `join_status/1`, `report/1` (redaction-safe join
  report).
- `AllbertAssist.Objectives.Runs.{Supervisor, Coordinator, RunServer,
  Registry, CancelToken}` — the supervised executor (S2), Registry keys
  `{:run, objective_id}` / `{:fanout, parent_objective_id}`.
- `AllbertAssist.Intent.Decomposer` — Stage-0 (S3): `propose/2` returning
  `{:fanout, [task]} | :single`, model-optional, advisory.
- `AllbertAssist.Channels.Notify` — the runtime-initiated notification
  boundary (S6, ADR 0084): `deliver(user_id, channel, kind, payload, opts)`;
  authority-checked (settings + Security policy + identity map + origin
  binding) before any adapter dispatch; delivery via the existing
  `deliver_outbound/3`.
- New registered actions: `steer_objective_run` (`confirmation: :none`,
  advisory reframe), `cancel_objective_run` (`confirmation: :none`, scope
  child|fanout, both tiers of ADR 0085); `list_objectives`-family output
  extended with fan-out trees (additive).
- New signals (full taxonomy in the request flow):
  `allbert.objectives.fanout.{proposed,started,joined}`,
  `allbert.objectives.run.{started,progress,blocked,steered,completed,failed,cancelled}`,
  `allbert.channels.notify.{delivered,suppressed,failed}`.
- New Settings Central keys (schema fragments, ADR 0031):
  `objectives.fanout.enabled` (default `true`),
  `objectives.fanout.max_concurrent_runs` (default 3, clamp 1..8),
  `objectives.fanout.max_children` (default 8, clamp 2..16),
  `objectives.fanout.confirm_before_start` (default `false`);
  `channels.<id>.autonomous_notify.enabled` (default `false`),
  `channels.<id>.autonomous_notify.level`
  (`"completion"` | `"status_and_completion"`, default `"completion"`),
  `channels.<id>.autonomous_notify.min_interval_seconds` (default 30,
  clamp 5..600). Email's level is forced `"completion"` (digest-class).
- Channel descriptor additive field `streaming:`
  (`:turn_complete | :progress_messages | :live_region`, default
  `:turn_complete`), consumed by `ChannelParity` (S7).
- Runtime response additive fields: `fanout` (parent id, children, ack list),
  `pending_reports` (Locked Decision 13).
- `mix allbert.test release.v11` — the release gate.

## Code Seams

**S1 — objectives fan-out model (M1, ADR 0083).** Additive migration
(`fanout_role`, `join_policy`, index on `parent_objective_id`).
`Objectives.Fanout.frame/2` creates the parent + children in one transaction,
copying `source_channel`/`source_surface`/`source_thread_id`/`session_id` to
each child; `children/1` and `join_status/1` are pure reads;
`report/1` builds the enumerated per-child join report (redaction-safe via
`Security.Redactor`). Join reduction: parent terminal iff all children
terminal; parent `completed` when ≥1 child completed, `failed` when ALL
failed, `cancelled` when cancelled before any completion; the report always
enumerates. Objective events record every transition. The serialized engine
agent is untouched except rehydration (S2); existing single-objective flows
prove identity via the objectives suites.

**S2 — supervised run executor (M2, ADR 0083).**
`Objectives.Runs.Supervisor` (DynamicSupervisor, `max_children` from
settings-clamped value) + `Objectives.Runs.Registry` (Elixir Registry,
unique keys) added to the application tree near the coding precedent
(`application.ex:35-36`). `Coordinator` (GenServer per active fan-out,
`restart: :temporary`): FIFO queue of unstarted children, starts ≤
`max_concurrent_runs`, monitors runs, applies the bounded restart policy (one
supervised restart per child, then terminal `failed`), performs join
reduction, emits `fanout.joined`. `RunServer` (plain GenServer — pragmatic
substrate: thin idle→running→cancelling lifecycle, Registry-addressed
steering mailbox; Jido.Agent's routing buys nothing here; documented in
`@moduledoc`): executes the child's step pipeline
(authorize → `Actions.Runner.run/3` → observe → advance) directly against
the Objectives API — NOT through the serialized engine agent — pinning
settings per step (Locked Decision 9), emitting `run.*` signals on the
SignalBus, checking its `CancelToken` between operations, and parking on
`needs_confirmation` (step `confirmation_id`, status `blocked`) without
blocking siblings. Boot rehydration: extend the engine's `rebuild_state/1`
window scan to restart coordinators for parents with non-terminal children.
Backpressure proof: queued children start only as slots free; kill -9 →
bounded restart → honest join; BEAM restart mid-fan-out resumes.

**S3 — Stage-0 decomposer + ack (M3).** `Intent.Decomposer.propose/2` runs
BEFORE the router for eligible turns (no active-app handoff, no typed
command, text length/shape heuristics): deterministic splitter first
(explicit enumerations, "then/and then" chains, numbered lists), model
proposal second (local model, `propose_steps`-style prompt, JSON list,
bounded by `max_children`), `:single` fallback on any doubt — a wrong
`:single` costs nothing (today's behavior). Decision path: a `:fanout`
proposal frames via S1, starts via S2, and the turn response is the kickoff
ack (list + child ids + steering hint), built additively into the runtime
response and rendered per adapter. `confirm_before_start` converts framing
into a durable confirmation instead (children created `open`, runs start on
approval through the existing confirmation actions). The decomposer NEVER
runs on steering turns (S5 classifies against active fan-outs first).

**S4 — cancellation both tiers (M4, ADR 0085).**
`Objectives.Runs.CancelToken`: per-run token (Registry-keyed atomics/ETS
flag) checked by the RunServer between operations and offered to actions via
runner context (`context[:cancel_token]`, cooperative — long-running actions
opt in by checkpointing). Supervised shutdown: `RunServer` terminate drains
to the last durable step; `Task.Supervisor.async_nolink` children shut down
per the coding template. OS tier: `Execution.LocalRunner` (and
`skill_script_runner`, sandbox `command.ex`) move from bare `System.cmd` to a
port-based spawn capturing `:os_pid`, run under a process-group leader
(`setsid` wrapper where available), with kill semantics
SIGTERM → grace (config, default 5s) → SIGKILL to the group; timeout paths
(`local_runner.ex:53-56`) use the same kill instead of orphaning. TUI escape
extends beyond coding turns: escape during an attached fan-out offers
cancel. `cancel_objective_run` action drives: cooperative flag → supervised
shutdown → OS kill, recording tier reached in the objective event.

**S5 — steering (M7).** Extend the `:continue_objective` seam: fan-out
parents/children in the candidate pool carry fan-out metadata
(`engine.ex:1223-1252` collection + `:273-291` decision attrs). A new
`Intent.Steering` classifier (advisory: deterministic verb/reference
heuristics + optional local-model assist) refines a `:continue_objective`
decision targeting an active fan-out into
`{:steer | :adjust | :cancel | :status | :new_request}` with a child
reference resolution (ordinal, title fuzzy match, "the research one").
`status` answers from durable state (no engine dispatch); `steer/adjust` →
`steer_objective_run` (directive recorded as an objective event, delivered
Registry-addressed to the RunServer, applied at the next step boundary —
never mid-operation); `cancel` → `cancel_objective_run`; `new_request` falls
through to the normal pipeline. Free text never approves confirmations
(Locked Decision 10; eval row). Misclassification cost is bounded: steer
directives are advisory and reversible; cancel asks the classifier for high
confidence or falls back to a clarify turn.

**S6 — autonomous notify + per-adapter delivery (M6, ADR 0084).**
`Channels.Notify` boundary: resolves the fan-out's origin
(`source_channel` + thread ref via the v1.0.1 attribution and
`channel_thread_refs`), re-derives the transport target from the identity
map (never from stored free text), checks — in order — channel live-use,
`channels.<id>.autonomous_notify.enabled`, level, min-interval throttle
(coalescing statuses per fan-out), Security policy, then dispatches via the
adapter's `deliver_outbound/3` with a threading hint in `opts`. Every
decision (delivered/suppressed/failed + reason) is audited and signaled.
Background-raised confirmations are a distinct notify kind
(`:confirmation_request`) rendered with the channel's approval primitives
(buttons where available, typed `ALLBERT:APPROVE:<id>` everywhere) — the
existing `ConfirmationCallback` path completes the loop untouched.
Per-adapter work items (from the Current Code State table):
  - **telegram**: pass threading hint (`reply_to_message_id`/thread id) from
    `opts`; kickoff/status/completion render via existing renderer; buttons
    for confirmation notify. (Outbound exists — smallest delta.)
  - **email**: IMPLEMENT `deliver_outbound/3` via the existing `SmtpClient`
    reply path (closes the ADR 0063 gap for email as a side effect);
    completion-only level; threaded reply (`in_reply_to`) to the origin
    message.
  - **discord**: threading hint → `message_reference`/thread id on
    `create_message`.
  - **slack**: threading hint → `thread_ts` on `chat_post_message`.
  - **matrix**: threading hint → `m.thread` relation on `send_message`
    (client already PUT-capable; `allowed_room_ids` validation stands).
  - **whatsapp**: threading hint → `context_message_id`; stays
    ReleaseAvailability-gated.
  - **signal**: wire the MuonTrap daemon receiver
    (`Daemon.daemon_child_spec/2` → supervised under the adapter's plugin
    child spec per ADR 0058) so inbound/steer works; notify via existing
    `send_message`; stays ReleaseAvailability-gated.
  - **tui**: no `deliver_outbound` (correct — local surface): attached
    sessions render push status via the existing `stream_event_sink`/
    LiveRegion machinery subscribed to `run.*` signals; unattached TUI gets
    the next-turn report.
  - **web**: SignalBridge already forwards objective events
    (`workspace_live.ex:718`); no Notify involvement (Locked Decision 13
    boundary).

**S7 — parity/streaming contract (M5).** Channel descriptors gain optional
`streaming:` (default `:turn_complete`); `ChannelParity.row/2` reads the
declaration instead of the `:98` hardcode; `verify/1` validates the enum and
that `:live_region` is only declared by local surfaces/TUI. Declarations:
web + tui `:live_region`; telegram/discord/slack/matrix/whatsapp/signal
`:progress_messages`; email `:turn_complete`. The parity test and
`docs/operator` channel matrix regenerate.

**S8 — web/TUI full experience (M8).** Web: workspace objectives surface
renders fan-out trees (parent + child status chips, live via the existing
`:objective_event` path), kickoff ack in the chat thread, steering affordances
(per-child cancel buttons dispatching the registered actions through the
normal action path — LiveViews render and dispatch only), join report
rendering, notify settings in Settings Central with the Security Central
boundary copy. TUI: fan-out ack rendering, live status region while attached
(S6), `status`/steer/cancel by reply, escape-cancel offer, notify settings via
settings CLI.

**S9 — eval rows + release gate (M9).** Extend the EvalInventory milestone
enum with `:v11` FIRST (the v1.0.2 lesson), then gate-bound rows (S9 list in
the request flow §Security Eval Rows): decomposition-grants-nothing,
notify-default-off, notify-cross-user-denied, notify-redaction,
steer-cannot-approve, cancel-kill-scope, fanout-cross-user-steer-denied.
`@release_v11_steps = @release_v1_steps ++ @v11_focused_steps`; focused steps:
objectives fan-out suites, runs supervision/crash/rehydration proofs,
cancellation tier tests, notify authority suite, steering eval, parity
verify, per-adapter outbound unit suites (fixture transports), migration
round-trip, and the `:v11` AssertBinding sweep. Runner + step-fn clones write
`release_evidence/v11/`.

**S10 — dependency refresh (M10).** Standing rule: three-root `hex.outdated`
review table, dedup, disposition per entry (applied/scoped-out/blocked),
bounded applies in own commits. Named review items carried from v1.0.2:
req_llm (locked 1.13.0), phoenix_live_view `~> 1.1.0` pin, exqlite drift.

**S11 — docs, closeout, binary release (M11).** CHANGELOG `## v1.1.0`;
version bump lockstep (three `mix.exs`, SW `CACHE_NAME` + gzip — guards
enforce); operator docs (channels matrix, objectives guide, settings
reference, onboarding touchpoints); ADRs 0083/0084/0085 flipped to Accepted
in their proving commits; release-rehearsal §§1–6 (binary release: tag
WITHOUT `[skip-artifacts]` → CI → cosign → publish → tap fill → install
rehearsal + packaged smokes); per-channel manual validation matrix (request
flow §Validation); post-tag archive/roadmap administration.

## Development Lane Annotation

- **Parallel workstreams:** WS-A (S1+S2 objectives model + executor — the
  spine); WS-B (S4 cancellation/OS-kill — touches Execution/sandbox, disjoint
  from objectives until the RunServer integration point); WS-C (S7 parity
  field + S6 per-adapter `deliver_outbound` deltas — eight independent plugin
  surfaces, parallelizable per adapter once the Notify boundary contract is
  fixed); WS-D (S10 dependency refresh — lockfile + scoped suites).
- **Serial barriers:** M0 baseline before everything (and blocks on the
  v1.0.2 tag). S1 strictly before S2 (durable model before executor).
  S2 before S3 (nothing to ack without runs), before S4's RunServer
  integration, and before S5 (nothing to steer). The `Channels.Notify`
  CONTRACT (S6 core) before the per-adapter fan-in of WS-C. S7's descriptor
  field before the parity regeneration in M5 acceptance. M8 after S3/S5/S6
  (web/TUI consume all three). M9 after all feature milestones (gate binds
  them). M10 after feature churn settles (validate final topology once).
  M11 last; the tag is operator-held.
- **Focused tests/gates:** per-milestone focused suites named in each
  milestone; `release.v1` after every contract-adjacent milestone (M1, M3,
  M5, M6); `inventory --check-tags` after every new test file (new files take
  a primary lane per test-strategy; runs/supervision tests are
  `global_process_serial` unless their audit proves ownership); no
  `mix | tail` pipes; metrics store records cited for every timing claim.
- **Full-precommit timing:** once at the M9 barrier and again at M11 RC
  assembly.
- **External smokes:** per-channel live smokes at M11 only (§Validation
  matrix); simulated adapter fixtures carry M6 acceptance.
- **Rejoin points:** parity matrix + operator channels doc regenerated at M5
  and re-checked at M11; drift review + docs reconciliation at M11 before
  tagging; triad archive + roadmap turnover only after published-artifact
  validation.

## Security Posture

This release ADDS an authority class and is planned like it:

- **Autonomous notify (ADR 0084)** is the only new grant: settings-gated per
  channel, default OFF, enforced at the single `Channels.Notify` boundary
  (settings + Security policy + identity-map target re-derivation +
  origin-thread binding + throttle), fully audited, redacted
  (`Security.Redactor` on all status/report text), and gate-bound by `:v11`
  eval rows. Attached-local-surface push (web LiveView, attached TUI) is
  explicitly OUTSIDE the class — no unattended transport send occurs.
- **Nothing else grants.** Decomposition output, steering directives,
  fan-out/run/child ids, and supervised processes carry zero authority; every
  child step executes registered actions with unchanged confirmation classes;
  free text never approves a confirmation; `send_channel_message` keeps
  `confirmation: :required` (operator-initiated sends are NOT relaxed by
  notify settings — different class, different boundary).
- **Cancellation kills only what the run spawned** (ADR 0085 scope rule:
  os_pid/process-group captured at spawn, kill addressed to that group only);
  OTP supervision remains a non-security boundary (AGENTS.md).
- **Inbound trust unchanged:** steering rides the existing channel identity
  mapping/trust classes (ADR 0016/0056/0059); an unmapped sender cannot
  steer, cancel, or receive notifications. E2EE-origin rules (signal) stand.
- Secrets stay encrypted/redacted end-to-end incl. notify payloads, audit
  rows, and release evidence.

## Non-Goals

- Workflow YAML `parallel_steps`/`for_each` promotion (stays reserved,
  ADR 0041).
- Edit-API in-place status updates on remote channels (Locked Decision 11 —
  intake candidate at closeout).
- Nested fan-out (a child framing its own fan-out) — single level in 1.1;
  the model rejects nested proposals.
- Remote/distributed delegate agents, operator no-code agent authoring, and
  the shared delegate-agent behaviour extraction (parked, future-features).
- Flipping whatsapp/signal release availability (code paths ship; release
  status is a separate operator decision).
- Typing indicators, presence, read receipts.
- Cross-user or multi-tenant fan-out semantics (single local-user model
  stands).

## Milestones

Execution modes: `[gate]` gate-proved-contract, `[smoke]` scripted host smoke,
`[browser]` real-browser manual, `[host]` cross-platform manual, `[model]`
real-egress model manual, `[channel]` live channel manual.

- **M0: Recon verify + baseline.** `[gate]` Block until the v1.0.2 tag.
  Re-verify every Current Code State anchor at HEAD (file:line drift table in
  Build Progress); snapshot the parity matrix (`ChannelParity.table/1`
  output) and the channels operator doc as the before-state; record turn/gate
  baselines from the metrics store (decide per-turn, quick/high/prepush
  walls); confirm the signal-daemon and email-outbound gaps still hold.
  Also carry the app-registry entry's "verify first": establish whether any
  release since v0.15 already validates app-registry membership at the
  action boundary (Runner + app-scoping checks) — record the finding; the
  M7 implementation item scopes to whatever gap remains.
  *Acceptance:* anchor drift table + baseline table + the boundary-check
  verification finding committed in Build Progress before M1.
- **M1: Objectives fan-out model + migration (S1, ADR 0083).** `[gate]`
  Additive migration (`fanout_role`, `join_policy`,
  `parent_objective_id` index) with round-trip proof; `Objectives.Fanout`
  frame/children/join_status/report; join reduction + partial-failure
  enumeration red-first; plan/build coexistence proven (existing
  `start_plan_run` suites green unchanged). ADR 0083 stays Proposed until M2
  proves the executor. *Acceptance:* focused fan-out suite green; migration
  round-trip green; objectives + plan_build suites green unchanged;
  `release.v1` green.
- **M2: Supervised run executor + registry + backpressure (S2, ADR 0083).**
  `[gate]` Runs.Supervisor/Registry/Coordinator/RunServer per S2; per-step
  settings pin red-first (Locked Decision 9: a settings write mid-run is
  visible at the next step, not mid-step); signal-bus `run.*` emission;
  parked-confirmation non-blocking proof (one child blocked on
  `needs_confirmation`, siblings progress). Supervision proofs: concurrent
  independent progress (no engine-agent serialization — asserted by
  timing/tracing), kill -9 → bounded restart → honest join, BEAM restart
  rehydration, `max_concurrent_runs` queue observability, unique Registry
  keys. ADR 0083 flips Accepted in this commit. *Acceptance:* all named
  proofs green as focused suites; Repo contention measured and recorded from
  the metrics store; no polling loop (code review + signal-driven asserts);
  `release.v1` green.
- **M3: Stage-0 decomposer + visible-list ack (S3).** `[gate]`+`[model]`
  Deterministic splitter + model proposer + `:single` fallback; eligibility
  guards; kickoff-ack response fields (additive, ADR 0029 checked); ack
  rendering through the shared renderer path so every adapter shows the list
  with zero adapter changes (adapters render turn responses today);
  `confirm_before_start` confirmation path; decomposer NEVER consulted on
  steering turns. Accuracy: a decomposition mini-corpus (fanout-positive,
  single-negative, steering-negative rows) added to the intent eval harness
  with a recorded floor — the existing golden/corpus suites stay green
  (routing regression = the number one risk of a new Stage-0).
  *Acceptance:* corpus floor recorded and gate-bound; golden set + corpus +
  cross-surface accuracy unchanged; manual `[model]` spot-check of model
  proposals recorded.
- **M4: Cancellation + interruption, both tiers (S4, ADR 0085).** `[gate]`
  CancelToken + between-operation checkpoints; supervised shutdown drain;
  port-based OS spawn with os_pid/process-group capture in LocalRunner,
  skill-script runner, sandbox command backends; SIGTERM→grace→SIGKILL
  escalation; timeout paths converge on the same kill (no more orphaned
  children on `Task.shutdown`); `cancel_objective_run` action drives
  cooperative → supervised → OS tiers with the reached tier recorded. Proof
  fixture: a spawn-a-sleeping-child script whose OS pid is verified dead
  after cancel (red-first: prove today's orphan first). ADR 0085 flips
  Accepted here. *Acceptance:* orphan-regression proof; per-tier focused
  suites; sandbox/skill-script/coding suites green unchanged; TUI escape
  offer works in an attached session.
- **M5: Channel parity/streaming contract renegotiation (S7).** `[gate]`
  Descriptor `streaming:` field + parity derivation + `verify/1` rules +
  declarations for all nine surfaces; parity/channel docs regenerate; the
  contract change is additive (absent field = `:turn_complete`).
  *Acceptance:* parity suite green with the new column honest per channel;
  `release.v1` green (Tier-2 additive check); before/after matrix snapshot
  in Build Progress.
- **M6: Autonomous-notify authority + all-adapter delivery (S6, ADR 0084).**
  `[gate]`+`[channel]` Settings schema fragments (all keys, defaults OFF);
  `Channels.Notify` boundary with the full check chain + coalescing/throttle
  + audit + redaction; confirmation-request notify kind reusing
  `ConfirmationCallback` for approval; next-turn `pending_reports` fallback;
  per-adapter deltas exactly as enumerated in S6 (email `deliver_outbound`
  NEW; signal daemon WIRED; threading hints for
  telegram/discord/slack/matrix/whatsapp; TUI attached-session rendering;
  web untouched by Notify). ADR 0084 flips Accepted here. *Acceptance:*
  notify authority suite (default-off suppression, opt-in delivery, level +
  throttle, cross-user denial, redaction) green; per-adapter outbound unit
  suites green on fixture transports; simulated inbound for
  whatsapp/signal; live `[channel]` smoke on every operator-configured live
  channel recorded in Build Progress; audit rows verified.
- **M7: Steer-vs-new classifier + steering actions (S5).** `[gate]`+`[model]`
  Steering classifier (deterministic + optional model assist, advisory);
  child-reference resolution; `steer_objective_run`/`cancel_objective_run`
  actions (registered, param contracts, Registry-addressed delivery, next
  step boundary application); status queries from durable state; clarify
  fallback on low confidence; free-text-never-approves proof. Steering
  mini-corpus (steer/adjust/cancel/status/new-request rows) with recorded
  floor; existing intent accuracy suites unchanged. **Plus (operator-pulled
  into 1.1, 2026-07-18): App-Registry Membership Check At Action Boundary**
  — close whatever gap M0's verification found: the action boundary
  (`Actions.Runner` + app-scoping) validates app-registry membership instead
  of trusting caller-supplied app identity, red-first with a
  spoofed-app-identity regression test; especially load-bearing here because
  fan-out dispatches actions from background contexts. *Acceptance:* corpus
  floor gate-bound; accuracy suites green; end-to-end steer + cancel
  (cooperative and OS tiers) proven against a running fan-out in a focused
  integration test; the boundary check enforced with its regression test (or
  M0's finding that it already exists, recorded with anchors).
- **M8: Web + TUI full experience (S8).** `[gate]`+`[browser]` Workspace
  fan-out tree, chat kickoff ack, live child status, steering affordances
  dispatching registered actions, join report rendering, Settings Central
  notify panel with boundary copy; TUI ack/live-region status/steer/cancel/
  escape-offer + settings CLI. LiveViews render-and-dispatch only (checked in
  review). *Acceptance:* liveview + TUI focused suites green; manual
  `[browser]` workspace walkthrough + one warm `mix allbert.tui` session
  recorded (AGENTS.md validation default).
- **M9: Eval rows + release.v11 gate (S9).** `[gate]` `:v11` enum extension
  FIRST; the seven-row (minimum) eval set as AssertBinding tests; gate wiring
  `@release_v11_steps = @release_v1_steps ++ @v11_focused_steps` with runner/
  evidence clones (`release_evidence/v11/`). *Acceptance:* `release.v11`
  green first roll target; every new authority/deny path bound to a gate
  step; `release.v1` green.
- **M10: Dependency refresh (S10).** `[gate]` Standing-rule review table +
  bounded applies + scoped suites; own commit per wave; carried review items
  dispositioned (req_llm, PLV pin, exqlite). *Acceptance:* disposition table
  complete; validation suites green.
- **M11: Docs, RC, binary release + closeout (S11).**
  `[gate]`+`[smoke]`+`[host]`+`[browser]`+`[model]`+`[channel]` Purpose-bar
  check against every Purpose outcome (AGENTS.md — deferrals need operator
  sign-off); docs deliverables; version bump lockstep + CHANGELOG; final
  measurement tables from the metrics store; `mix precommit` + authoritative
  `mix allbert.test release` + `release.v1` + `release.v11` green at the RC;
  operator-held annotated tag `v1.1.0` (NO `[skip-artifacts]`); CI publish +
  cosign; tap fill; install rehearsal; the per-channel manual validation
  matrix (request flow §Validation) executed and recorded; then post-tag
  archive/roadmap administration. *Acceptance:* Definition of Done complete;
  ledger recorded; triad archived only after published-artifact validation.

## Documentation Deliverables

- ADR 0083, 0084, 0085 (Proposed → Accepted at their proving milestones).
- `docs/plans/v1.1-request-flow.md` maintained as implementation changes.
- Operator docs: channels/parity matrix regeneration, objectives/fan-out
  guide section, Settings reference (fanout + autonomous_notify keys),
  Security Central notify boundary copy, TUI help.
- Developer docs: test-strategy lane additions for runs/supervision suites;
  delegate-agents doc cross-reference to the run executor.
- `CHANGELOG.md` `## v1.1.0`.
- Roadmap ladder item 3 + future-features lifecycle updates at closeout
  (operator-owned; flagged intake candidates surfaced, never self-served).
- Triad archived to `docs/plans/archives/` after artifact validation.

## Definition Of Done

- Fan-out model + supervised executor landed with crash/rehydration,
  backpressure, and join-honesty proofs; ADR 0083 Accepted —
  `[gate-proved-contract]`.
- Decomposer ships with a recorded accuracy floor and ZERO regression on the
  existing intent accuracy suites — `[gate-proved-contract]`.
- Both cancellation tiers landed incl. OS child-process kill with the
  orphan-regression proof; ADR 0085 Accepted — `[gate-proved-contract]`.
- Autonomous notify enforced at one boundary, default OFF, audited, redacted,
  eval-row-bound; ADR 0084 Accepted; all nine surfaces deliver
  ack/status/steer per their primitives (email outbound implemented; signal
  daemon wired) — `[gate-proved-contract]` + `[channel]` attestations.
- Parity matrix derives streaming from declarations — `[gate-proved-contract]`.
- Steering corpus floor recorded; free-text-never-approves and
  steer-cannot-lower-confirmation proven — `[gate-proved-contract]`.
- Web + TUI experiences validated (`[browser]` + warm TUI session).
- `mix allbert.test release`, `release.v1`, `release.v11` GREEN at the tagged
  commit — `[gate-proved-contract]`.
- v1.1.0 published as a BINARY release: cosign-signed GitHub Latest, tap
  filled, install rehearsal recorded — `[operator-attested-smoke]`.
- Per-channel validation matrix executed (live where configured, simulated
  where release-gated) and recorded in the ledger —
  `[operator-attested-smoke]`.
- Dependency disposition table complete — `[gate-proved-contract]`.
- Docs deliverables + closeout administration complete — docs.
