# Allbert v0.57 Pi-mode Coding Surface

Request flow: `docs/plans/v0.57-request-flow.md`

ADR: `docs/adr/0068-pi-mode-coding-surface-and-local-coding-trust-tier.md`
(being drafted separately — the authority/trust contract for this version).

Rationale: `docs/archives/pi-integration-rethink.md` (the Pi-vs-Allbert analysis;
§3 "borrow/don't-borrow", §4 the 7-stage pipeline, §7 sequencing, §8
"what not to do", §9 concept→home map).

Status: in progress; M0 contracts/API/upstream verification, M1 read/search/path
substrate, M2 write/edit file effects, M3 bash runner contract, M4 stream-event
pipeline/live-render substrate, M5 async turn-execution boundary, M6 task-level
cancellation/queued steering, M7 local-coding trust/approval/grants, M8 session
slash matrix, M9 deterministic eval/release gate, M9.1 session-scope remediation,
M9.2 live provider streaming/cancel, M9.3-M9.8 agent-loop remediation, and
M9.9-M9.25 third-pass/operator-validation remediation and M9.26-M9.30 terminal
input remediation are implemented. Live
S4.9 validation after M9.25 invalidated the Esc-helper capture strategy; M9.25's
slash/control-byte hygiene remains valid, and M9.26-M9.30 replace that failed
helper strategy with the proven input-driver path. Validation can resume at S4.9
after restarting `mix allbert.tui` on M9.30+ code.
The 2026-06-23 post-implementation audit added **M9.1/M9.2** because two readiness gaps
remained: (1) the live production LLM path still used the synchronous
turn-complete agent path rather than a real `ReqLLM.stream_text` stream, so
assistant-token streaming and provider `StreamResponse.cancel` were substrate-tested
but not live; (2) the six coding actions were globally `exposure: :agent`, making
session scoping rely on descriptor absence instead of an explicit boundary. M9.1
implements the session-scope exposure guard and M9.2 wires the real provider
stream/cancel path. The second-pass audit added **M9.3-M9.8** because the live stream
was still prose-only: the six coding tools were not model-callable, and TUI session
history was not handed through the streaming path. M9.3-M9.8 bind the six tools as
session-local `ReqLLM.Tool` definitions, execute model-proposed calls through
`Actions.Runner.run/3`, append bounded tool results into `ReqLLM.Context`, update
the TUI-held session context, add model-reachability eval coverage, and update warm
operator validation. Release closeout now depends on deterministic gates plus warm
operator validation against a configured streaming/tool-capable coding profile.
The third-pass audit remediation added operator-visible confirmation timing docs,
effectful loop authority tests, agent-loop boundary tests, and cancellation handoff
clarification before the next audit cycle. Warm operator validation on 2026-06-24
then added M9.13-M9.14: M9.13 fixed an engine/router override that could route a
Pi-mode coding turn to a non-coding app action, and M9.14 split the interactive
Pi-mode tool-loop model profile from the codegen fallback, added an explicit
pseudo-tool diagnostic, and fixed streamed confirmation handoff preservation and
rendering. The next warm-validation finding added M9.15: the TUI adapter now owns
final async-turn rendering in the channel GenServer, defers the next prompt until
the active Pi-mode turn has rendered its stream/final response, and updates S4
validation prompts so line-oriented terminal input is copy/paste-safe. The next
warm-validation finding added M9.16: the transient Owl live block now renders only
compact stream progress instead of raw tool arguments/results or long assistant
text, leaving the final answer as the canonical transcript output. The follow-on
warm-validation finding added M9.17: `mix allbert.tui` now defaults to
transcript-stable progress output instead of Owl erase/repaint blocks, so compact
byte/tool progress remains visible in scrollback without leaving blank rows. The
next warm-validation finding added M9.18: transcript progress is now coalesced so
assistant byte counts advance at bounded intervals and tool/result progress prints
only when it changes. The next S4.4 warm-validation finding added M9.19: typed
approval of a pending coding write could resolve the confirmation as `denied`
because approval resume rebuilt the target action without the original Pi-mode
session/cwd-jail metadata, and the policy session check did not recognize the
persisted mixed-key `%{"main?" => true}` session shape. M9.19 persists a narrow
coding-origin snapshot on confirmation records, restores it for coding-action
approval resumes while preserving live trust settings, and normalizes mixed
session keys at the policy boundary. The next S4.6 warm-validation finding added
M9.20: `accept-edits` correctly auto-applied the disposable write, but
model-proposed `bash pwd` was denied as local execution disabled before Pi-mode
approval policy could prompt. M9.20 documents the missing Level 1 execution
preflight: validation homes must enable `execution.local.enabled`, constrain
execution to the repo root, allow only `pwd`/`printf` for the S4 bash checks, and
keep `execution.local.require_confirmation=true`. The follow-on S4.6
warm-validation finding added M9.21: once local execution was enabled, the live
model still used the `bash` tool's `command` field for simple `pwd`, which the
implementation classified as raw shell and denied. M9.21 makes the `bash` boundary
operator-friendly and systemic: simple command strings normalize to argv and still
pass the Level 1 allowlist/confirmation path; shell syntax stays raw-shell-tier
only; argv execution also explicitly refuses opaque sub-agent executables. The
next S4.7 warm-validation finding added M9.22: approving `bash pwd` with
`--remember exact` resumed and completed the command, but the remembered-grant
step failed with `:resource_ref_not_found` because the generic confirmation
handoff looked only for `params_summary.resource_refs`. M9.22 adds the
coding-command remembered-grant branch so Pi-mode `bash` confirmations remember
from canonical resume params while generic resource grants keep their existing
resource-ref contract.
The next S4.9 warm-validation findings added M9.23-M9.25. They proved the
cancellation spine itself was correct but that the v0.55 line-oriented input
substrate could not reliably deliver a standalone Esc key during an active async
turn. M9.23 tried a BEAM side-channel monitor; M9.24 tried direct `/dev/tty`
ownership; M9.25 tried a helper-owned `/dev/tty` reader and also added useful
slash/control-byte hygiene. Live S4.9 validation after M9.25 returned
`Esc cancellation monitor unavailable: {:escape_monitor_helper_exit, 2,
["/bin/sh: /dev/tty: Device not configured"]}`, so the Esc-helper strategy is
superseded rather than accepted. M9.26-M9.30 add a narrow interrupt-capable TUI
input substrate before release closeout. This does **not**
undo v0.55: v0.55's scrollback-native, line-oriented channel remains the correct
base for typed turns, approvals, slash commands, and split payloads.

Implementation-plan readiness fourth/fifth pass 2026-06-23. NEW in the
2026-06-21 replan. The readiness passes corrected the trust-tier model (a named
**local-coding operator** tier at sandbox **Level 1**, not "sandbox level 0"),
reconciled the ADR 0067 foundation citation and the v0.55 static-split vs.
v0.57-net-new-streaming boundary, replaced the mistaken "full-file context = Pi"
claim with Pi's actual chunked-read discipline, locked `bash` as tier-restricted
raw shell, and added per-milestone Implementation blocks + Settings Central keys.
The third/fourth passes added the coder-ergonomics parity layer, fixed the tool
taxonomy and slash-command effect matrix, and split the work into M0-M9. The fifth
pass verified the named substrate against real code (`deps/req_llm` `Context`/
`StreamResponse`/`StreamChunk`, `Owl.LiveScreen`, `Security.Policy`/`Decision`,
`Resources.Grants`), named the confirmation-cost seam, reused `Resources.Grants`
for "always allow", dropped `:coding_session_write`, and added Pi-mode session
entry / cwd-jail binding plus the memory-scope non-goal.

> **Upstream dependency:** v0.57 builds directly on **v0.55**: the
> TUI/terminal channel substrate (`plugins/allbert.tui`, `channel_id: "tui"`,
> provider `"terminal"`, supervised under `Channels.Supervisor`, identity map +
> `ChannelThread` + `channel_events` dedupe) and the **v0.55 split
> tool-result payload** (the typed runtime response contract, ADR 0029, carrying
> `model_payload` distinct from `surface_payload`, dispatched
> through the unified surface renderer, ADR 0030). Those were owned by v0.55 and
> should already be present by the time v0.57 starts. The Readiness Sweep must
> re-confirm the current code still has both before v0.57 implementation; if either
> is missing or regressed, block and reconcile the v0.55/v0.56 sequencing first.
> v0.57 must not silently absorb the terminal channel or the split-result contract.

## Purpose

Give Allbert a **Pi-mode coding surface**: a gated terminal coding loop that
feels as minimal as Pi and as **intuitive as Claude Code / Codex / Gemini CLI** (a
tight six-tool surface — read-only/sensitive `read`/`grep`/`glob` plus effectful
`write`/`edit`/`bash` — a sub-1000-token prompt+tool-defs budget, streamed diffs
**and** assistant-text token streaming, coder-facing approval modes,
Esc-to-cancel, a familiar slash set, deliberate chunked-read context discipline)
**while running on the one authority spine** — the same
`AllbertAssist.Actions.Runner.run/3`, Security Central, trace, and markdown-first
memory as every other surface.

Stated structurally (rationale §4): Allbert's cognitive runtime is the
seven-stage machine **Receive → Interpret → Frame/resume objective →
Propose/evaluate steps → Authorize step → Execute (`Actions.Runner.run/3`) →
Observe/advance.** Pi is `{Receive → Interpret(implicit) → Execute(YOLO) →
Observe}` — it deletes stages 3–5. v0.57 makes **stages 6–7 and the tool surface
Pi-minimal** (read/search/file/shell tools, streamed diffs + token streaming,
observable, sub-1000-token prompt+tool-defs budget)
**while leaving stages 3–5 fully intact**.
A Pi-mode session is one where the objective is "pair on this repo," step proposal
is trivial, and authorization runs under the **local-coding operator trust tier
at sandbox Level 1** for a single trusted local operator. The gates are *present
but cheap*, never *absent*.

This is a **channel/app under ADR 0016 on the existing spine** — NOT a sibling
runtime, NOT a new authority. What lands:

1. **Six default tools:** three **read-only but sensitive** tools
   (`read`/`grep`/`glob`) + three **effectful** tools (`write`/`edit`/`bash`),
   all routed through the action runner and Security Central. Read/search tools
   run without a confirmation prompt but still enforce cwd jail, ignore policy,
   output caps, redaction, trace, and audit; `bash` runs host processes at
   sandbox Level 1.
2. **Coder-facing approval modes** (`default` / `accept-edits` / `plan` / `tier`)
   plus persistent **"always allow this command"** per repo — a presentation +
   persistence layer over Security Central (Policy allowlist + confirmations),
   stored under Allbert Home by repo fingerprint, revocable and auditable,
   granting **no new authority**.
3. A named **"local-coding operator"** trust tier (ADR 0056 lineage, running at
   ADR 0009 **Level 1**) — single trusted operator, main session, terminal
   channel; never the default, never for channel-originated or generated-code
   sessions. Not a sandbox level; never "level 0".
4. **Net-new live rendering:** a coding-diff live-region renderer + progressive
   tool-argument streaming + **assistant-text token streaming** over the v0.55
   static split payload, plus **Esc-to-cancel** and a **queued-correction** steer.
5. A sub-1000-token prompt+tool-defs budget + chunked-read context discipline; a
   familiar coding **slash set** (`/help`, `/mode`, `/model`, `/clear`, `/init`,
   `/diff`, `/compact`) plus local lifecycle aliases (`/quit`, `/exit`) with an
   explicit effect matrix; a mid-session model switch (`req_llm` history
   handoff); and the minimalism budget as a standing coding policy.

## Readiness Sweep
- **Contract fit.** The Pi-mode surface is a delivery+driver experience over the
  v0.55 `tui` channel; it owns no intent/Security Central/Resource Access/
  confirmation storage/conversation history. Coding effects reach the host *only*
  through registered actions executed by `Actions.Runner.run/3`. The runner +
  Security Central decide; the surface renders and dispatches.
- **Reuse fit.** Reuse `plugins/allbert.tui` (normalize → `Identity.resolve` →
  `Channels.derive_session_id` → `Runtime.submit_user_input` → `ChannelThread` →
  `channel_events`), the v0.55 **static** split-result contract
  (`AllbertAssist.Runtime.Response` `model_payload`/`surface_payload`, ADR 0067
  extending ADR 0029/0030) as the payload v0.57's **net-new** streaming renders
  over, the existing `actions/` boundary — specifically `run_shell_command`
  (argv-only Level 1) and `execution/skill_script_runner` (timeout + brutal-kill)
  — as the template for the `bash` policy, the ADR 0056 trust-tier convention +
  ADR 0009 Level 1 for the tier, the v0.03 skill loader
  (`AllbertAssist.Skills`) + `AGENTS.md` hierarchy for context, and req_llm
  message-history serialization/streaming for the model switch and live turn.
  Cross-provider conversion is best-effort; M0/M4 must verify the concrete
  `ReqLLM.Context`, `ReqLLM.StreamResponse`, and `ReqLLM.StreamChunk` APIs against
  vendored `deps/req_llm` source because Context7 currently resolves `Req`, not a
  first-class `req_llm` package. ADR 0047/0051 remain provider doctor/capability
  contracts, not this handoff.
- **Upstream check.** Confirm v0.55 delivered the `tui` channel AND the **static**
  split tool-result payload (`model_payload`/`surface_payload`) through the typed
  response contract + renderer. v0.55 did **not** ship a progressive coding-diff
  renderer or streaming substrate; diff rendering is v0.57 M4 and token
  streaming/cancel is v0.57 M5. If the `tui` channel or the static split is
  missing/regressed, stop and reconcile v0.55/v0.56 before implementing v0.57.
- **Authority check (AGENTS.md Non-Negotiables).** Advisory output is never
  authority: the coding model may propose `read`/`write`/`edit`/`bash` calls but
  may not bypass `Actions.Runner.run/3`, Security Central, or confirmations. The
  bash action is policy-bounded host execution behind a registered action — OTP
  supervision is **not** an OS security boundary. No model-decides-it's-done for
  effectful or generated-code work. No sub-agent-via-bash-spawn.
- **Library fit.** No new library is required. `owl` (v0.55, terminal render)
  supports live-updating blocks, but its own docs position it as CLI/script UI
  tooling rather than a full-screen terminal framework; Esc handling and turn
  cancellation are Allbert TUI/input-loop work. `req_llm` covers provider-neutral
  context, streaming chunks, and cancellation hooks in local source, but M0/M4/M5
  must verify provider-specific tool-call streaming and cancellation semantics.

## Coding-Surface Ergonomics & CLI Parity
The surface must feel intuitive to a developer arriving from Claude Code, Pi,
Codex CLI, or Gemini CLI. The minimalism is in the *authority spine and default
surface*, **not** in the interaction model — the everyday coding ergonomics those
tools share are first-class deliverables here, expressed as Allbert primitives.

| Convention | Claude Code / Codex / Gemini / Pi | Allbert Pi-mode (v0.57) |
|---|---|---|
| **Tool surface** | read/write/edit/bash + Grep/Glob (Claude, Gemini); Codex/Pi = shell does search | **Six**: read-only/sensitive `read`/`grep`/`glob` + effectful `write`/`edit`/`bash` (registered actions). Beyond the six → MCP lazy disclosure. |
| **Code search** | first-class Grep/Glob, gitignore-aware, unprompted | `grep`/`glob` actions run without a confirmation prompt but still pass Runner/Security Central, cwd jail, ignore policy, output caps, trace, and redaction; `bash` `rg`/`find` also available at the tier. |
| **Approval UX** | named modes (acceptEdits/plan/yolo...) + persistent "always allow" | modes `default` / `accept-edits` / `plan` / `tier` + Allbert Home-rooted per-repo "always allow this command" — a view over Security Central, no new authority. |
| **Streaming** | assistant text streams token-by-token; diffs/output render live | **net-new v0.57**: explicit stream-event contract, assistant-text token streaming, progressive tool-argument diff streaming, and turn-complete fallback. |
| **Interrupt / steer** | Esc cancels; queued correction / Enter-to-steer | **Esc-to-cancel** is Allbert input-loop/runtime work (not Owl-provided): cancel model stream + running action best-effort, keep prior work, partial-turn trace, queued correction; `bash` timeout/brutal-kill underneath. |
| **Slash set** | /help /mode /model /clear /init /diff /compact ... | `/help`, `/mode`, `/model`, `/clear`, `/init`, `/diff`, `/compact` — slash-allowlisted and non-routable, with a per-command effect/action matrix. |
| **Context file** | AGENTS.md / CLAUDE.md / GEMINI.md auto-load + @file + /init | `AGENTS.md` hierarchy (Allbert already uses it — Codex made AGENTS.md the native default, so this is **parity**) + `@file` mentions + `/init`. |
| **Loop / "done"** | loop until model stops emitting tool calls, then return to prompt | same interactive REPL: the turn ends when the model stops calling tools. "Deterministic acceptance / model never decides done" applies to **effectful/generated-code acceptance gates**, not to ending a conversational turn. |

Two of these choices carry **runtime dependencies beyond rendering** and are flagged
in the milestones: assistant-text **token streaming** needs the runtime to emit
model token deltas (today's v0.55 response is turn-complete), and **interrupt**
needs clean turn cancellation (abort the in-flight model call + any running action
with a partial-turn trace; `bash` brutal-kill exists, model-call abort is net-new).

## Locked Decisions
| # | Decision | Chosen value | Trade-off if changed |
|---|---|---|---|
| 1 | Surface authority | Pi-mode is a **channel/app on the one spine** (ADR 0016): every coding effect routes through `Actions.Runner.run/3` + Security Central. The surface has **no authority beyond any other channel**. | A sibling runtime or a direct-effect coding loop reintroduces Pi's YOLO threat model — the thing Allbert exists to reject. |
| 2 | Tool surface | **Six default tools**: read-only/sensitive `read`, `grep`, `glob` + effectful `write`, `edit`, `bash`. Each is a registered Jido action via `Actions.Registry`/`Runner`. `read`/`grep`/`glob` use the `:coding_file_read` permission and run without a confirmation prompt, but still enforce cwd jail, symlink policy, ignore policy, binary handling, output caps, redaction, trace, and audit. `write`/`edit` use `:coding_file_write`; `bash` uses `:coding_shell_execute`. Dotted strings such as `permissions.coding_file_write` are Settings Central keys, not permission atoms. Capabilities **beyond** the six are lazy-disclosed (MCP-first, ADR 0038/0039), not pre-loaded. | Omitting `grep`/`glob` reads as impoverished to Claude Code / Gemini users (2 of the 3 ship them first-class) and forces an MCP round-trip just to search a repo. A *wide* default surface beyond search would defeat the minimalism budget. |
| 3 | Trust tier | A named **"local-coding operator"** trust tier (ADR 0056 lineage) running at **sandbox Level 1** (ADR 0009): single trusted operator, main session, `tui` channel. **Never** the default; **never** for channel-originated inbound or generated-code sessions. The tier lowers only the *confirmation burden*; policy/redaction/trace/audit still run at Level 1. It is **not** a sandbox level and never "level 0". | An implicit or default tier turns a low-friction convenience into Pi-by-default and breaks the action boundary. Mislabeling it "level 0" collides with ADR 0009 (level 0 = inert/no-exec) and conflates caller-trust with isolation. |
| 4 | Acceptance | **Deterministic acceptance rules**, not model confidence. For effectful or generated-code work the model never "decides it's done"; evidence + confirmation rule (same posture Allbert took against Hermes). | Model-confidence completion contradicts "advisory output is never authority" and the codegen committee's deterministic gates. |
| 5 | Prompt + context | A **sub-1000-token prompt+tool-defs budget** (system prompt and all six tool definitions combined, against the configured tokenizer). Context discipline follows **Pi's documented practice**: chunked reads (offset/limit) + separate context-gathering sessions + file artifacts, **not** whole-file ingestion. Reuse the `AGENTS.md` hierarchy + v0.03 skill loader; `@file` mentions resolve through the same bounded read action, never raw path ingestion. | A large default budget or whole-file context bloats the surface. (Note: full-file ingestion is *not* a Pi finding — Pi reads in bounded chunks and notes models resist full reads; do not attribute it to Pi.) |
| 6 | Live rendering | **Net-new in v0.57:** a coding-diff live-region renderer + progressive tool-argument rendering **and assistant-text token streaming**, built on the v0.55 **static** split payload (`model_payload` vs. `surface_payload`, ADR 0067) through the unified renderer (ADR 0030). M0 defines the stream events (`assistant_token_delta`, `tool_call_argument_delta`, `tool_call_argument_complete`, `tool_result_delta`, `turn_cancelled`, `turn_complete`). The terminal renders ephemeral deltas and reconciles to the final model/surface payloads; the model sees only the clean model payload. | Treating streaming as a v0.55 inheritance understates the work; v0.55 ships only the static split. A turn-complete reply (no token streaming) feels dead next to every peer CLI. A single conflated payload re-bloats model context. |
| 7 | Model switch | Mid-session model switch is an **operator affordance** over req_llm's canonical `Context` handoff (verified through Context7 when available, otherwise through vendored `deps/req_llm` docs/source), surfaced in the `tui` session; it changes the model, never the authority path. ADR 0047/0051 remain provider doctor/capability contracts and do not own this handoff. | Hiding the switch wastes a structurally-cheap req_llm capability Pi proved is worth surfacing. |
| 8 | Sub-agents | Delegation uses **restricted-registry delegate agents**, never an opaque `bash`-spawned sub-agent. | Self-spawn via bash is an unobservable, unrestricted child — the Hermes "narrow the child's tools" lesson, violated. |
| 9 | `bash` semantics | **Raw shell strings, but only at the local-coding operator tier** (Pi-faithful). For every other caller `bash` is unavailable or falls back to argv-only `run_shell_command`. Tier-restricted raw shell is bounded by cwd-confinement to the repo root, a wall-clock timeout + brutal-kill (reuse `skill_script_runner`), full trace/audit, and the stance that the host/container — not OTP — is the security boundary. | A general raw-shell action would break `run_shell_command`'s anti-injection (argv-only) posture for untrusted callers. Restricting raw shell to one trusted local operator keeps the convenience without widening the channel-originated attack surface. |
| 10 | Approval modes | Coder-facing modes `default` / `accept-edits` / `plan` / `tier`, plus persistent **"always allow this command"** per repo. This is a **confirmation-cost seam over Security Central** — implemented in `Security.Decision.build/1` so it drops only the interactive prompt while **preserving the `:needs_confirmation` decision, policy, risk, trace, and audit**; it changes confirmation cost, never the authority path. `plan` executes only read/search; it never executes `write`/`edit`/`bash`. "Always allow" **reuses the existing `Resources.Grants` remembered-grant lifecycle** (durable, expirable, revocable, audited, Allbert Home-rooted) with a command-scoped grant keyed by repo fingerprint + permission + cwd + canonical (normalized/redacted) command — **not** a parallel allowlist store. The `tier` mode is the local-coding operator tier (cheapest). | No coder-facing modes means a non-tier session prompts on every effect (permission fatigue — the exact thing Claude/Codex/Gemini solved with modes + remembered allowances). A second approval-memory store would drift from `Resources.Grants`. Modes that granted authority would violate "advisory output is never authority." |
| 11 | Interrupt / steer | **Esc cancels** a running turn (work done so far is kept + traced) and a **queued correction** lets the operator redirect without killing the turn. Cancellation cleanly aborts the in-flight model stream when the provider/ReqLLM path supports it and cancels or brutal-kills any running action by policy; failures degrade to bounded timeout + partial-turn trace, never silent orphaning. Owl provides live blocks, not the Esc semantics. | No interrupt is jarring — Esc-to-cancel is a universal reflex. Cancellation is a runtime concern (model-call abort + partial-turn trace), so it is scoped as net-new work, not assumed from v0.55. |
| 12 | Slash set | A familiar coding **slash allowlist**: `/help` (router-local), `/diff` + `@file` (read via `:coding_file_read`), `/init` (file write via `:coding_file_write`), and `/mode` / `/model` / `/clear` / `/compact` as **ungated session/model ops** (in-memory, no persistent or external effect, **no permission atom** — gating them would be ceremony no peer CLI imposes). All reuse the v0.55.1 slash-allowlist branch, are non-routable (allowlist only), and create no model turn. There is **no** `:coding_session_write` atom. | Only `/model` would feel bare; coders expect a baseline. Routable slash commands would re-introduce the operator/agent-surface boundary problem v0.56 M14a fixed. Permission-gating `/clear`/`/compact` would prompt-fatigue the coder for a no-op. |

## Milestones

Lane summary: **M0** contracts (serial barrier) -> **M1** read/search/path
substrate -> (**M2** write/edit split-payload actions ∥ **M3** bash/raw-shell
runner contract — both after M1) -> **M4** streaming event contract + live
renderer (after M0+M2; can overlap M3) -> **M5** async turn-execution boundary ->
**M6** cancel/steer runtime -> **M7** trust tier + approval modes + remembered
command grants -> **M8** prompt/context/model switch/slash matrix + session entry
-> **M9** evals + operator-validation handoff (serial barrier). Each milestone's **Implementation**
block lists Modules / Files / Settings / Tests / Verify so any agent can build and
verify it independently.

### M0: Contracts + ADR + Upstream/API Verification  *(serial barrier)*
Land ADR 0068 as the authority contract; confirm the v0.55 upstreams (`tui`
channel + **static** split tool-result) exist or block. Define the six tools'
params/results/signals, permission atom vs. Settings key vocabulary, stream-event
contract, trust-tier resolver, and approval-mode vocabulary before effectful code.
- **Implementation:**
  - **Modules/signatures:** `Security.Policy`/`Security.PermissionGate` gain
    `coding_tier(context) :: :local_coding_operator | :none` and an
    `approval_mode/1` reader. Permission atoms are exactly
    `:coding_file_read`, `:coding_file_write`, and `:coding_shell_execute`;
    dotted values such as `permissions.coding_file_write` are Settings Central
    keys. (There is **no** `:coding_session_write` atom — session slashes
    `/clear`/`/compact`/`/model` are non-routable slash-allowlist entries, not
    permission-gated effects; see M8.)
  - **Permission registration checklist (per atom).** Adding a `:coding_*` atom
    is a six-site edit in `Security.Policy`, all required: (1) `@permission_settings`
    (atom → `permissions.<name>` key); (2) `@default_decisions`; (3) the `@type
    permission` union; (4) `permission_classes/0`; (5) a `safety_floor/2` clause;
    (6) a `reason/5` clause for trace/status. Floors: `:coding_file_read` →
    `:allowed`; `:coding_file_write` and `:coding_shell_execute` →
    `:needs_confirmation`. Mirror the existing `:command_execute` registration.
  - **Confirmation-cost seam (declared here, implemented in M7).**
    `Security.Decision.build/1` now has the tier/mode-aware seam that can set
    `requires_confirmation: false` **while preserving the `:needs_confirmation`
    decision, policy, trace, and audit**. The seam suppresses only the prompt.
  - **Event contract:** define `assistant_token_delta`,
    `tool_call_argument_delta`, `tool_call_argument_complete`,
    `tool_result_delta`, `turn_cancelled`, and `turn_complete`.
  - **Dependency/API check (verified 2026-06-23, present in `deps/req_llm`):**
    `Owl.LiveScreen` live blocks (already used by the TUI status bar);
    `ReqLLM.Context` (`merge_response/2` for the model-switch handoff),
    `ReqLLM.StreamResponse` (`stream`, `process_stream/2` callbacks, and the
    `cancel: (-> :ok)` field for stream cancellation), and `ReqLLM.StreamChunk`
    (`:content`/`:thinking`/`:tool_call`/`:meta`). Re-confirm provider-specific
    tool-call chunk shapes at M4/M6.
  - **Tests:** trust-tier resolution fixture; approval-mode vocabulary fixture;
    stream-event schema fixture; the three `:coding_*` atoms register at all six
    `Policy` sites with the declared floors; no effectful path exists yet.
  - **Verify:** ADR-fixture resolution + mode tables green; upstream/API check
    green.
- Gate: ADR 0068 accepted; upstream/API check green.
- **Implemented 2026-06-23:** ADR 0068 is Accepted; the v0.55 `tui` channel,
  `Runtime.Response` static `model_payload`/`surface_payload` contract, TUI
  surface renderer, and `Owl.LiveScreen` live-block substrate are covered by the
  M0 contract test. Context7 and vendored `deps/req_llm` verification confirmed
  `ReqLLM.Context.merge_response/2,3`, `ReqLLM.StreamResponse` streaming/cancel
  fields plus `tokens/1`, `tool_calls/1`, `process_stream/2`, and
  `ReqLLM.StreamChunk` `:content`/`:thinking`/`:tool_call`/`:meta` constructors.
  Security Central now registers exactly the three M0 coding permission atoms
  (`:coding_file_read`, `:coding_file_write`, `:coding_shell_execute`) at the
  policy/settings/risk sites with the declared floors; `:coding_session_write`
  remains absent. `Security.Policy`/`PermissionGate` exposed the initial
  `coding_tier/1` and `approval_mode/1` readers, and `Coding.StreamEvent` defines
  the six stream-event types for M4-M6. M7 later wired those readers through
  Settings Central and the live `Security.Decision` confirmation-cost seam.

### M1: Read/Search/Path Substrate  *(read-only but policy-bounded)*
Register `read`, `grep`, and `glob` as read-only/sensitive coding actions through
`Actions.Registry` and `Actions.Runner.run/3`. They run without a confirmation
prompt, but still enforce cwd jail, symlink policy, ignore policy, binary
handling, output caps, redaction, trace, and audit.
- **Implementation:** `Actions.Coding.{Read,Grep,Glob}` plus `Coding.PathPolicy`
  and `Coding.Search`. M1 uses direct Elixir file-walk/globbing so read/search
  stays inside the policy/read substrate; `bash`/process execution remains M3/M6
  scope.
- **Settings:** `coding.workspace.cwd_jail`, `coding.read.default_limit`,
  `coding.read.max_bytes`, `coding.search.max_results`,
  `coding.search.max_output_bytes`, `coding.search.respect_gitignore`,
  `coding.search.respect_allbertignore`, `permissions.coding_file_read`.
- **Implemented:** `Actions.Coding.{Read,Grep,Glob}` are `:agent` actions,
  `confirmation: :not_required`, permissioned by `:coding_file_read`, and return
  relative-path, redacted, bounded payloads. `Coding.PathPolicy` resolves the cwd
  jail, rejects path traversal and symlink escape, rejects binary reads, and exposes
  the bounded read helper that `/diff`/`@file` will consume in M8. `Coding.Search`
  honors `.gitignore`, `.allbertignore`, `.git/`, result caps, and output caps.
- **Tests:** all three actions resolve via Runner; shared bounded read path is
  tested for later `@file`; grep/glob honor cwd jail and ignore policy; no search
  path mutates files; output is redacted/truncated.
- Gate: read/search substrate green before M2/M4 consume it.

### M2: Write/Edit Actions + Split-Payload Diffs  *(file effects)*
Register `write` and `edit` as effectful coding actions with permission
`:coding_file_write`. `write` creates files; `edit` performs exact-match
replacement and returns a clear miss so the model re-reads and retries. Results
produce clean `model_payload` plus renderable `surface_payload` diff for M4.
- **Implementation:** `Actions.Coding.{Write,Edit}` plus
  `Coding.FileEffects`. Both actions are `:agent`, `confirmation: :required`,
  `resumable?: true`, resolve through `Actions.Registry`/`Actions.Runner.run/3`,
  check `:coding_file_write` before filesystem validation, and then create a
  durable confirmation through the existing confirmation/resume path. `write`
  refuses overwrites and only creates a new file whose existing parent resolves
  inside the cwd jail. `edit` reads an existing text file, requires a non-empty
  exact match, refuses over-broad replacements above the configured cap, and
  applies the replacement only after approval.
- **Settings:** `coding.workspace.cwd_jail`, `coding.write.max_bytes`,
  `coding.edit.max_replacements`, `permissions.coding_file_write`.
- **Implemented:** split payloads carry redacted, bounded diff previews with
  relative paths only. Confirmation output redacts raw write content and exact-match
  strings; the existing confirmation store also redacts secret-shaped resume
  params at persistence time, so the default confirmation path must not be used as
  a secret-write channel. Secrets belong in the existing Settings/secrets paths,
  not in pending file-edit confirmations.
- **Tests:** write/edit resolve only through Runner; default decision is
  confirmation-needed until M6 modes/tier make the prompt cheap; approval resumes
  and mutates ordinary content; permission denial blocks before filesystem
  validation; normalized path, symlink, overwrite, binary, exact-match-miss, and
  outside-jail escapes are refused; split payload contains no UI/chrome in
  `model_payload` and redacts secret-shaped content.
- Gate: M2 depends on M1 path policy; M4 consumes M2's diff payload contract.

### M3: Bash + Raw-Shell Runner Contract  *(host execution)*
Register Pi-mode `bash` as an effectful coding action with permission
`:coding_shell_execute`. Preserve existing `run_shell_command` argv-only semantics
for non-tier callers. Raw shell strings are accepted only when
`coding.bash.allow_raw_shell` is true **and** the local-coding operator tier
resolves; otherwise raw shell is denied before any command execution.
- **Implementation:** `Actions.Coding.Bash`, `Coding.BashSpec`, and the existing
  `Execution.CommandSpec` + `Execution.LocalRunner` Level 1 execution path.
  Argv-style commands normalize through `CommandSpec` with execution-policy
  allowed-command, cwd, timeout, output, root, and env allowlist checks. Raw shell
  normalizes to `/bin/sh -lc` only at the local-coding tier with the raw-shell
  setting enabled. Both paths run through `Actions.Runner.run/3`,
  Security Central, confirmation/resume, redaction, and trace surfaces; opaque
  sub-agent launch words (`codex`, `claude`, `gemini`, `opencode`, `cursor`,
  `antigravity`) are denied as a policy bypass.
- **Settings:** `coding.workspace.cwd_jail`, `coding.bash.timeout_ms`,
  `coding.bash.max_output_bytes`, `coding.bash.allow_raw_shell`,
  `permissions.coding_shell_execute`.
- **Implemented:** `bash` is registered as a resumable coding action with
  permission `:coding_shell_execute`, `confirmation: :required`, and split
  `model_payload`/`surface_payload` summaries. Approval resumes the stored command
  via the generic confirmation path. Confirmation output redacts raw shell strings,
  argv args, and env payloads; output previews are bounded and redacted. The cwd
  jail is shared with M1/M2. Environment keys outside the execution policy
  allowlist are denied, not silently dropped. Raw shell remains off by default and
  unavailable to non-tier callers.
- **Tests:** bash resolves through Runner; cwd jail, timeout, output caps, env
  allowlist, and redaction are enforced; raw shell fails for non-tier callers;
  argv-only fallback remains intact; bash cannot spawn opaque sub-agents as an
  authority bypass.
- Gate: M3 depends on M1 path policy and precedes M6 raw-shell cheap gate.

### M4: Stream Event Pipeline + Live Renderer  *(rendering, no authority)*
Build the live-region render substrate the v0.55 static split lacks. It consumes
the M0 stream-event contract and M2 split diff payloads: assistant token deltas
render as live prose, tool-call argument deltas render as progressive diffs, and
final payloads reconcile to `model_payload`/`surface_payload`. Rendering grants no
authority and must degrade to the v0.55 turn-complete static render.
- **Implemented:** runtime stream-event emitter (ReqLLM stream ->
  Allbert runtime events), TUI live-region renderer over `owl`, and progressive
  tool-argument parser.
- **Settings:** `coding.streaming.enabled`, `coding.streaming.turn_complete_fallback`.
- **Implemented:** `Coding.StreamPipeline` converts provider-neutral
  `ReqLLM.StreamChunk` content/thinking/tool-call chunks into validated
  `Coding.StreamEvent` maps and emits turn-complete events from normalized runtime
  responses. `Coding.StreamRenderer` is the pure, authority-free state machine for
  assistant token deltas, progressive tool-call arguments, tool-result deltas,
  cancellation events, and final `surface_payload` reconciliation. The TUI
  renderer accepts `stream_events` and falls back to final turn-complete split
  payloads; `Channels.TUI.LiveRegion` wraps the existing `Owl.LiveScreen`
  add/update/await/flush pattern for live coding blocks. M4 does not create the
  async turn handle or Esc cancellation path; M5/M6 own those runtime controls.
  M9.2 connects this pipeline to the live coding-turn provider path.
- **Tests:** assistant tokens render incrementally and reconcile; progressive-arg
  chunks render in order; `surface_payload` never leaks into `model_payload` or
  memory; turn-complete fallback works.
- Gate: M4 depends on M0 event contract and M2 split diff payloads.

### M5: Async Turn-Execution Boundary  *(runtime prerequisite for cancellation)*
**Net-new runtime work, and the prerequisite the synchronous v0.55 runtime lacks.**
Today `Runtime.submit_user_input/1` calls the agent runner and `Actions.Runner.run/3`
**synchronously and blocking** (`runtime.ex:115`, `runner.ex:85`) — there is no
handle to an in-flight turn, so it cannot be cancelled. M5 moves a coding turn's
model call (and its cancellable actions) onto a **supervised Task** behind the
runtime, registered so it is referenceable and killable, with a clean partial-turn
trace on shutdown. This is what makes M6's true Esc-to-cancel possible (option a:
abort the in-flight model call, not merely abandon it at the channel). No authority
change — same runner, same Security Central, same trace.
- **Implementation:** a per-turn supervisor + turn registry (e.g.
  `Runtime.Turn`/`Coding.TurnSupervisor`) that runs the model call as a
  `Task` under a supervisor and tracks it by session/turn id; the coding loop awaits
  the task with a monitor; shutdown writes a partial-turn trace. Reuses
  `ReqLLM.StreamResponse` (the `cancel` field + `process_stream/2`) from M4 so the
  streamed turn is the cancellable unit.
- **Settings:** `coding.turn.max_ms` (hard ceiling), `coding.turn.supervised`
  (default true; falls back to synchronous turn-complete when false).
- **Implemented:** `AllbertAssist.Coding.TurnSupervisor` wraps explicitly marked
  coding turns in `AllbertAssist.TaskSupervisor`, registers the in-flight task in
  `AllbertAssist.Coding.TurnRegistry` by turn id, and converts task shutdown or
  timeout into a partial runtime response with `:cancelled`/`:timed_out` status.
  The partial response follows the ordinary runtime path: response signal,
  assistant-message persistence, trace recording, and turn-completed signal. Normal
  non-coding turns remain synchronous; `coding.turn.supervised=false` preserves the
  v0.55 turn-complete fallback. M5 exposes the addressable/shutdown-capable task
  boundary that M6 uses for Esc/task cancellation and queued steering.
  Post-implementation audit note: M5 makes turns addressable and cancellable at the
  task boundary, but it does not by itself convert the synchronous agent call into a
  provider stream.
- **Tests:** a coding turn runs under a supervisor and is addressable by turn id;
  killing the task yields a partial-turn trace and no orphaned model call/action;
  the synchronous fallback still produces the v0.55 turn-complete result; no
  authority path changes.
- Gate: M5 depends on M4 stream events; precedes M6.

### M6: Cancellation + Queued Steering  *(live-turn control)*
Make the live turn controllable on the M5 boundary. **Esc cancels a running turn
for real (option a):** the in-flight model stream is aborted via the
`ReqLLM.StreamResponse` `cancel` callback registered by the running turn task,
then the supervised turn task is shut down through `Coding.TurnSupervisor`'s
registry-level cancel path (grace window, then hard-kill fallback if the task is
still alive). Running actions remain bounded by their own Runner/action policy
timeouts and brutal-kill behavior; work done so far is kept, a `turn_cancelled`
stream event is emitted, and a **partial-turn trace** is written. A **queued
correction** redirects the next turn without killing the current one. (Where a
provider stream cannot be aborted, registry task shutdown still frees the turn and
the late result is discarded — but the default and tested path is a real stream
cancel.)
- **Implementation:** TUI coding-mode input-loop Esc handler -> turn registry
  cancel -> registered `StreamResponse.cancel.()` callback + bounded task exit;
  one-slot queued-correction buffer; partial-turn trace writer; normal non-coding
  TUI turns stay synchronous.
- **Settings:** `coding.steer.enabled`, `coding.cancel.grace_ms`.
- **Tests:** Esc/cancel mid-turn invokes the registered stream cancel callback,
  shuts down the supervised runtime task, emits `turn_cancelled`, writes the
  partial-turn trace, leaves no registered orphan, and starts the queued correction
  as the next coding turn without granting authority. M3 bash/action tests remain
  the policy proof for command timeout/output caps and brutal-kill behavior.
- **Post-implementation audit note:** the cancel registry, task shutdown, queued
  steering, and stream-cancel callback path are implemented. M9.2 registers the
  real provider `StreamResponse.cancel` returned by the coding-turn
  `ReqLLM.stream_text` call.
- Gate: M6 depends on M5 async boundary and M3 bash cancellation behavior.

### M7: Local-Coding Trust Tier + Approval Modes + Remembered Command Grants  *(serial authority gate)*
Implemented 2026-06-23. M7 lands the **local-coding operator** tier and
coder-facing approval modes.
Security Central resolves the tier only for the trusted operator in the main
session on `tui`; all other callers keep the full gate and argv-only shell path.
Modes and remembered grants change confirmation cost only, never authority.
- **Implementation:**
  - `Security.Policy.coding_tier/1` now resolves from Settings Central plus the
    normalized request context: `coding.pi_mode.enabled=true`, a configured
    `coding.trusted_operator_id`, actor match, main session, and `tui` channel are
    all required; channel-originated, scheduled, and generated-code contexts never
    resolve the tier. `Security.Context` preserves the normalized M7 fields
    Security Central needs (`session.main?`, coding mode, trust flags, and hash-only
    command-grant refs) and labels `tui` as local trust.
  - `Security.Decision.build/1` implements the **M0 confirmation-cost seam**:
    `accept-edits` suppresses only `write`/`edit` prompts, `tier` suppresses local
    coding file/shell prompts, remembered command grants suppress only their exact
    bash prompt, and `plan` denies `write`/`edit`/`bash`. The returned decision
    remains `:needs_confirmation` when the policy floor says so; policy, risk,
    trace, audit, and redaction still run. `Actions.Outbound.Gate` executes only
    when the decision is allowed, an approved confirmation resumes, or a
    `:needs_confirmation` decision has `requires_confirmation: false`.
  - **Reuse the existing remembered-grant mechanism**, do not build a parallel
    allowlist. "Always allow this command" extends
    `AllbertAssist.Resources.Grants` / `resource_grants.remembered` with
    `AllbertAssist.Coding.CommandGrants`: a **command-scoped** record keyed by repo
    fingerprint + permission + realpath-canonical cwd + canonical command hash.
    The stored record keeps hash-only command/cwd refs plus bounded metadata
    (`executable`, arg count, mode), so raw argv/raw shell text does not enter the
    Security decision context. Same lifecycle (TTL, revoke, list, audit); same
    Allbert Home-rooted storage convention as resource grants — not a new
    `coding/allowlists/*.json` store.
- **Settings:** `coding.default_approval_mode`, `coding.trusted_operator_id`,
  `coding.pi_mode.enabled`, `coding.bash.allow_raw_shell`,
  `coding.command_grants.default_ttl_ms`, `coding.command_grants.max_entries_per_repo`.
- **Tests:** trusted/main/`tui` resolves tier; channel-originated/scheduled/
  generated-code callers do not; the cheap gate **preserves the decision + trace +
  audit and only drops the prompt**; `plan` blocks `write`/`edit`/`bash`;
  `accept-edits` auto-approves only file edits; remembered command grants are scoped
  by repo fingerprint + canonical command + permission + cwd, expire, list, revoke,
  and audit through the existing grant lifecycle.
- Gate: M7 follows M1-M3 and rejoins before M8; focused M7 coverage is
  `m7_trust_approval_test.exs` plus the M2/M3/Security/Resources regressions.

### M8: Prompt + Context + Model-Switch + Slash Matrix + Session Entry  *(parallel lanes)*
- Lane A: sub-1000-token prompt+six-tool-defs budget, named tokenizer, chunked
  read context discipline, `AGENTS.md` hierarchy, and `@file` through bounded read.
- Lane B: mid-session model switch over req_llm message-history serialization
  (`ReqLLM.Context.merge_response/2`), surfaced as `/model <name>`, best-effort
  across providers and no authority change.
- Lane C: coding slash set. `/help` is router-local; `/diff` and `@file` use
  `:coding_file_read`; `/init` writes through `:coding_file_write`. `/mode`,
  `/model`, `/clear`, and `/compact` are **non-routable slash-allowlist** session
  ops with **no permission gate** (in-memory session/model controls with no
  persistent or external effect — gating them would be permission-fatigue ceremony
  no peer CLI imposes). All slashes are non-routable and absent from
  `agent_modules/0`.
- Lane D: **Pi-mode session lifecycle.** How the operator **enters/exits** Pi-mode
  (a `/pi [path]` affordance on the `tui` channel) and how the **working repo binds
  to `coding.workspace.cwd_jail`** — resolved to an absolute root, symlink-checked,
  and **pinned at session start** so every one of the six tools (and raw shell) is
  jailed to it for the session's life. Exiting Pi-mode returns the `tui` channel to
  its normal assistant turn.
- **Settings:** `coding.prompt.token_budget`, `coding.prompt.tokenizer`,
  `coding.model_profile`.
- **Tests:** prompt + six tool defs fit budget; default turn exposes only six
  tools; slashes create no model turn and are absent from `agent_modules/0`;
  `/init` writes through `:coding_file_write`; `/mode`/`/clear`/`/compact`/
  `/model` are not permission-gated and never prompt; `plan` mode blocks `/init`;
  entering Pi-mode pins the cwd jail (symlink + normalized-path checked) and
  exiting restores the normal turn; model switch preserves session history.
- **Implemented (M8):** `Coding.Prompt` owns the compact system prompt + six-tool
  definitions under `coding.prompt.token_budget` with named tokenizer
  `simple_words`; `Coding.Session` owns per-TUI-session cwd jail/model/mode/ReqLLM
  context state; `/pi [path]` pins the real cwd jail for the session; the six coding
  actions remain registered capabilities but are absent from `agent_modules/0` and
  callable only through Pi-mode session context; `/mode`,
  `/model`, `/clear`, and `/compact` mutate only in-memory session state; `/diff`
  and `@file` call the registered `read` action; `/init` calls the registered
  `write` action and attaches the standard TUI approval handoff when confirmation
  is required. Focused coverage:
  `m8_session_slash_test.exs` plus existing `tui_test.exs`.
- Gate: M8 follows M7 and rejoins before M9.

### M9: Evals, Hardening, Operator-Validation Handoff  *(serial barrier)*
`:v057` security/coding eval lane + `release.v057` deterministic gate; opt-in
interactive coding-session smoke where the harness allows; ADR 0068 accepted and
cross-linked; docs (request-flow, roadmap §v0.57, vision, agent-context-map,
operator Pi-mode coding guide). Version/CHANGELOG closeout remains after warm TUI
operator validation.
- **Implementation:** `test/security/v057_coding_eval_test.exs`; `:v057` rows in
  `EvalInventory`; a `release.v057` lane bundling disposable migration, coding
  action units, async-turn-boundary + streaming/cancel units, trust-tier/approval/
  remembered-grant units, slash-matrix + session-entry units, M9 live provider
  streaming/cancel units, and a release-home secret scan. M9 also hardened
  `write`/`edit` split payloads so TUI diff previews stay in `surface_payload`
  while `model_payload` carries only bounded receipts, hashes, and confirmation
  summaries.
- **Verify:** `mix allbert.test release.v057` deterministic + reproducible;
  trust-tier, approval-mode, remembered-grant, slash-effect, cheap-gate-preserves-
  decision, no-authority, live provider streaming/cancel, and M9.2 path rows green;
  secret scan clean.

### M9.1: Post-Implementation Remediation - Session Scope + Audit Truth  *(implemented)*
Reason: the post-implementation audit found that authority correctness was strong,
but two release-readiness claims were overstated. First, M4-M6 built and tested the
stream/cancel substrate with fake chunks and callbacks, while the production coding
turn still used the synchronous agent path and did not open a real provider stream.
Second, the six coding actions were present in `agent_modules/0`; out-of-session
read/search denial depended on the absence of intent descriptors plus permission
defaults rather than an explicit session boundary.

- **Implemented boundary fix:** `read`/`grep`/`glob`/`write`/`edit`/`bash` are
  now `exposure: :internal` registered capabilities, not general intent-agent
  actions. They stay invokable through `Actions.Runner.run/3` from the TUI Pi-mode
  path, but are absent from `Registry.agent_modules/0` and public protocol tool
  candidates.
- **Implemented runtime guard:** `Coding.SessionGuard` normalizes TUI/runtime
  context, requires active Pi-mode session metadata (`coding.pi_mode_enabled`,
  pinned `cwd_jail`/workspace root), and requires the existing
  `:local_coding_operator` tier before any coding action touches path policy,
  file effects, search, or shell normalization. Out-of-session direct invocation
  returns a denied action result before filesystem access.
- **Implemented public-surface defense:** `PublicProtocol.ExposureFilter` now blocks
  coding execution modes/permissions defensively, and the regression tests assert
  all six names are rejected even if an operator attempts to allowlist them.
- **Implemented eval update:** added `pi-mode-tools-denied-out-of-session-001` to the
  v0.57 eval inventory and release test module.
- **Model setting guidance:** `coding.model_profile` remains the single persisted
  Pi-mode model selector. The default `pi_coding_local` should point at a local or
  private profile that emits real provider tool-call chunks with streaming support.
  It is intentionally distinct from `coding_local`, which remains the
  codegen-committee fallback.
  Hosted coding profiles may be used only by explicit operator choice after accepting
  source-code egress. `/model <profile>` changes only the in-memory session profile;
  it grants no authority and never changes permission, trust-tier, or cwd-jail
  decisions. M9.2 uses this selected profile for the live `ReqLLM.stream_text`
  call; release validation must use a profile/provider path that supports
  streaming, real tool calls, and `StreamResponse.cancel` before claiming live
  provider abort.
- **Verify:** focused gate is
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/coding/m1_read_search_actions_test.exs apps/allbert_assist/test/allbert_assist/coding/m2_write_edit_actions_test.exs apps/allbert_assist/test/allbert_assist/coding/m3_bash_action_test.exs apps/allbert_assist/test/allbert_assist/coding/m7_trust_approval_test.exs apps/allbert_assist/test/security/v057_coding_eval_test.exs apps/allbert_assist/test/allbert_assist/actions/registry_test.exs apps/allbert_assist/test/allbert_assist/public_protocol/exposure_filter_test.exs`.

### M9.2: Live Provider Streaming + Provider-Level Cancel  *(implemented)*
M9.2 closes the live-turn readiness gap. Pi-mode coding turns now use the selected
`coding.model_profile` to open `ReqLLM.stream_text`, feed provider
`ReqLLM.StreamChunk` values through `Coding.StreamPipeline.emit_stream_response`,
emit assistant-token deltas incrementally to the TUI live sink, append a final
`turn_complete` reconciliation event, and register the returned
`ReqLLM.StreamResponse.cancel` callback with `Coding.TurnSupervisor`. The
turn-complete fallback remains for disabled/invalid streaming paths and for
providers that cannot stream, but that fallback is no longer the primary v0.57
claim.

- **Implemented streaming boundary:** `Coding.StreamingTurn` resolves the coding
  model profile through Settings Central in production, builds the Pi-mode prompt,
  opens `ReqLLM.stream_text`, redacts provider chunks through
  `Coding.StreamPipeline`, and records bounded diagnostics without persisting raw
  provider payloads.
- **Implemented live event path:** `StreamPipeline.emit_stream_response/3` emits
  each event as the provider stream is consumed, and the TUI async coding turn
  passes a supervised-turn live sink so `Channels.TUI.LiveRegion` can update before
  turn completion.
- **Implemented provider cancel:** the provider `StreamResponse.cancel` callback is
  registered on the active coding turn; Esc invokes that callback before bounded
  registry task shutdown and partial-turn trace preservation.
- **Verify:** focused gate is
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/coding/m9_streaming_turn_test.exs`
  plus the M4-M6 tests; `mix allbert.test release.v057` includes the M9 streaming
  turn test in its `coding_m0_m9_units` lane. Manual release validation must still
  use a real configured streaming/tool-capable coding provider/profile.

### M9.3: Agentic Coding Tool Loop  *(implemented)*
Reason: the second-pass implementation audit found that M9.2 made the live
assistant stream real, but the stream was still prose-only. The documented v0.57
core loop -- model proposes `read`/`grep`/`glob`/`write`/`edit`/`bash`, Allbert
executes only through `Actions.Runner.run/3`, then the model observes the bounded
tool result and continues -- is now implemented in v0.57. Leaving it as future work
would have turned Pi-mode into a streaming advisor rather than the intended local
coding agent.

- **Implemented:** `Coding.ToolLoop` binds the six coding actions as `ReqLLM.Tool`
  definitions only inside active Pi-mode coding turns. Tool callbacks must call
  `Actions.Runner.run/3`; they must not call action modules, filesystem APIs, shell
  APIs, or permission helpers directly.
- **Implemented loop:** consume each `ReqLLM.StreamResponse` once, emit assistant-token
  and tool-argument stream events as chunks arrive, inspect the materialized
  response's tool calls, execute each call through the Runner, append model-visible
  `ReqLLM.Context.tool_result/3` messages, and open the next stream. The turn ends
  only when the model returns final assistant text with no actionable tool calls.
- **Authority invariant:** M9.1 remains authoritative. The tools stay
  `exposure: :internal`, public protocol exposure keeps rejecting them, and
  `Coding.SessionGuard` remains the first line inside every coding action.
  Model output is advisory; the Runner/Security Central decision is the authority.
- **Confirmation invariant:** `write`/`edit`/`bash` confirmation behavior is
  unchanged. A model-proposed effect may return a confirmation-needed tool result;
  the loop must surface that state rather than treating the proposal as approval.
- **Failure invariant:** unknown tool names, malformed arguments, runner denials,
  confirmation-needed results, and action errors are returned to the model as
  bounded tool results and recorded in the response metadata/diagnostics. They do
  not bypass the gate and do not silently disappear.

### M9.4: Session Context + Model Settings Handoff  *(implemented)*
Reason: `Coding.Session` already owns a `ReqLLM.Context`, and `/model <profile>`
already changes the session profile, but the live streaming path was rebuilding a
fresh context for each turn. That breaks the documented mid-session model and
conversation-history handoff.

- **Implemented:** the TUI adapter passes the session `ReqLLM.Context` into the live
  runtime coding turn, appends the operator request to it, and writes the updated
  context back to the TUI-held Pi-mode session when the turn finishes.
- **Implemented model settings:** `coding.model_profile` remains the persisted default
  for `/pi` session start. `/model <profile>` remains an in-memory session switch
  only. The streaming/tool loop must use the active session profile and never grant
  authority through model choice.
- **Provider validation target:** v0.57 coding validation must use a configured coding profile
  whose provider supports streaming and tool use. Hosted profiles require explicit
  operator source-egress acceptance; there is no silent hosted fallback.

### M9.5: Tool-Loop Stream Events + Response Evidence  *(implemented)*
Reason: the live renderer must make tool use observable, and release evidence must
prove model-proposed tools reached the Runner instead of only proving direct
unit-level action invocation.

- **Implemented:** emit `tool_call_argument_delta`,
  `tool_call_argument_complete`, and `tool_result_delta` events during the loop,
  then reconcile the final `turn_complete` payload.
- **Implemented response evidence:** include bounded coding-turn metadata for executed tool calls,
  statuses, confirmation states, and the active model profile/provider. Preserve the
  existing `direct_answer` action entry while appending coding tool action summaries
  from Runner results.

### M9.6: Agent-Loop Tests + Release Eval  *(implemented)*
Reason: the prior green gates covered registered actions, guards, streaming
substrate, and provider cancel, but not model reachability. The release gate must
assert the exact path that failed the audit.

- **Implemented focused tests:** added a fake streaming-provider test where the model emits a
  `read` tool call, Allbert executes it through `Runner.run/3`, appends a tool
  result to `ReqLLM.Context`, opens a second stream, and returns final assistant
  text. Added coverage that effectful model-proposed tools route through Runner and
  preserve confirmation/denial states.
- **Implemented eval update:** added `pi-mode-agent-loop-model-tools-001` to the v0.57 eval
  inventory. It asserts model-proposed tool reachability inside an active
  Pi-mode session and continued out-of-session denial.

### M9.7: Operator Validation Update  *(implemented)*
Reason: warm TUI validation must catch the exact failure the second-pass audit
found. The operator checklist cannot stop at streaming prose, slash commands, or
direct Runner unit tests.

- **Updated validation target:** operator validation must include a real warm TUI session
  where the selected coding model reads/searches the repo through model-proposed
  tools, attempts a small edit/write through the normal confirmation path, and runs
  a bounded shell command through `bash`.
- **Updated evidence target:** the transcript must record streamed assistant text,
  model-driven tool activity, confirmation behavior for effects, provider cancel
  behavior, selected `coding.model_profile`, and release evidence path.

### M9.8: Third-Audit Handoff Gate  *(implemented)*
Reason: after M9.3-M9.7 land, a third implementation audit should be able to verify
that docs, evals, code paths, and operator validation instructions all describe the
same product.

- **Gate:** run the focused coding/tool-loop tests, `MIX_ENV=test mix compile
  --warnings-as-errors`, `MIX_ENV=test mix allbert.test release.v057`, and
  `git diff --check`.
- **Handoff:** docs must state the implemented state precisely: six tools are
  model-callable only inside active Pi-mode coding turns, still internal outside
  that path, and still action-runner/security-gated.

### M9.9: Confirmation Semantics Operator Clarity  *(implemented)*
Reason: the third-pass audit confirmed the authority model is correct, but the
operator-facing docs did not plainly explain the mode-dependent loop behavior after
a model-proposed effect returns `needs_confirmation`.

- **Implemented docs:** `default` mode creates pending confirmations for
  `write`/`edit`/`bash` and does not mutate files or run commands before approval.
  The current model loop may finish with a pending-confirmation response; approving
  the confirmation resumes the registered action later, not the same LLM stream.
- **Implemented docs:** `accept-edits` can suppress only `write`/`edit` prompts and
  `tier` can suppress trusted local file/shell prompts. In those modes, the effect
  can complete during the same loop only because Security Central preserved the
  `:needs_confirmation` decision while dropping the interactive prompt.
- **Invariant:** model output remains advisory in every mode. Mode/profile choice
  changes confirmation cost, not permission, cwd jail, raw-shell limits, trace, or
  audit.

### M9.10: Effectful Loop Authority Tests  *(implemented)*
Reason: direct action/eval tests prove `Gate.run/3` prevents effects before
confirmation, and one streaming-loop test proves read-tool execute-and-continue.
The gate should also assert the full streaming loop with model-proposed
`write`/`edit`/`bash` effects.

- **Implemented tests:** model-proposed `write`, `edit`, and `bash` in `default`
  mode return `needs_confirmation`, expose an approval handoff, append bounded
  tool results to `ReqLLM.Context`, and prove no file mutation or command
  execution happens before approval.
- **Gate:** focused M9 streaming tests passed; run `release.v057` again after
  M9.11-M9.12 so the full remediation queue is covered by one fresh release
  evidence file.

### M9.11: Agent Loop Boundary Tests  *(implemented)*
Reason: the loop boundary should lock the exact safety shape that the audit now
depends on, not only direct action units.

- **Implemented tests:** multi-tool sequences, cwd escape attempts through
  model-proposed tools, raw-shell-without-tier attempts through model-proposed
  tools, and the `@max_tool_iterations` stop condition.
- **Gate:** focused M9 streaming tests passed; run `release.v057` again after
  M9.12 so the full remediation queue is covered by one fresh release evidence
  file.

### M9.12: Cancellation Handoff Clarification  *(implemented)*
Reason: provider-stream Esc cancellation is implemented and tested. The third-pass
audit found that cancellation during an already-running tool action is bounded by
the supervised turn and command timeout behavior, but this distinction is not yet a
clear validation contract.

- **Implemented docs:** distinguish provider-stream cancellation from in-flight
  tool-action cancellation. The former invokes `ReqLLM.StreamResponse.cancel`; the
  latter is bounded by turn task shutdown and action/command timeouts unless a
  later milestone adds child-process cancellation semantics.
- **Implemented tests:** added focused coverage proving a turn with no registered
  provider stream cancel still shuts down through `Coding.TurnSupervisor.cancel/3`,
  returns `stream_cancel: :not_registered`, emits `turn_cancelled`, and writes the
  partial-turn trace.
- **Gate:** focused M6/M9 cancellation and streaming tests passed; run
  `release.v057` again after this milestone so the full remediation queue is
  covered by one fresh release evidence file.

### M9.13: Coding-Turn Route Pinning  *(implemented)*
Reason: warm operator validation exposed a production routing drift after the
agent-loop remediation. In an active `/pi` session, the S4 read/search prompt was
accepted as a coding turn at the TUI boundary but later routed through the ordinary
intent/app path, producing `Note could not be read: :not_found` from a notes action
instead of invoking the Pi-mode `read`/`grep` tool loop. The bug was architectural:
`IntentAgent` recognized `coding_turn?` but still allowed the engine/router result
to override the deterministic `direct_answer` coding route.

- **Implemented fix:** Pi-mode coding turns now pin the `direct_answer` streaming
  route before engine/router/clarification overrides can select a non-coding app
  action. The TUI session metadata remains the source of coding authority; this
  change only prevents an already-marked coding turn from leaving the Pi-mode
  runtime lane.
- **Validation evidence:** after the fix, the warm S4 read/search prompt no longer
  invoked the notes-file action and no longer returned `Note could not be read`.
  Focused M9 streaming tests passed, and `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix
  allbert.test release.v057` passed. The remediation commit was `4ef78b2d`
  (`v0.57 M9.13 coding turn routing remediation`).

### M9.14: Pi-Mode Tool-Profile Compatibility + Streamed Approval Handoff  *(implemented)*
Reason: the next S4 default-mode write validation exposed related contract
gaps. First, the selected local coding model could answer with textual
tool-looking markup such as `<function=write>` instead of provider tool-call
chunks, so no Runner action executed and no confirmation could be created. Second,
once a tool-call-capable local profile produced a real `needs_confirmation`
result, the TUI renderer preferred `stream_events` over `approval_handoff`, hiding
the exact `ALLBERT:APPROVE:<id>` command in streamed coding-turn responses. The
validation probe then found one more boundary issue: the outer `direct_answer`
route wrapper could replace the coding-tool handoff with a synthetic read-only
route handoff, yielding `ALLBERT:APPROVE:` without the confirmation id.

- **Implemented model-profile split:** `coding.model_profile` now defaults to
  `pi_coding_local`, backed by `qwen2.5:7b` in the local provider catalog. This
  separates the interactive Pi-mode tool-loop profile from `coding_local`, which
  remains available for codegen-committee work. The `pi_mode_coding` model-doctor
  row now checks `coding.model_profile` instead of reporting a future placeholder.
- **Implemented compatibility diagnostic:** if a coding stream returns textual
  pseudo-tool markup and no real tool calls, `Coding.StreamingTurn` returns a
  profile-compatibility error, records `diagnostic.status = :pseudo_tool_text`,
  and guarantees no file, shell, or memory effect was applied. This is a systemic
  failure mode, not a validation-only workaround.
- **Implemented TUI handoff rendering:** the TUI renderer now appends the standard
  approval handoff after rendered stream events whenever a streamed response also
  carries `approval_handoff`, so default-mode `write`/`edit`/`bash` confirmations
  are visible in the transcript.
- **Implemented route-wrapper handoff preservation:** `IntentAgent` now preserves
  an executable handoff already returned by the action response, enriches the
  top-level response with the handoff's confirmation id, and attaches the handoff
  to the matching action name instead of blindly attaching the route-level decision
  handoff to the first action.
- **Validation evidence:** direct local model probes during validation found
  `qwen3-coder:latest` emitted pseudo-tool text, `qwen2.5-coder:7b` produced an
  empty streamed response for the write prompt, and `qwen2.5:7b` produced a real
  provider `write` tool call with `needs_confirmation`. A TUI adapter probe then
  showed the session entering with `model=pi_coding_local` and the internal action
  result returning `needs_confirmation`; before the renderer fix, the approval
  command was hidden by stream-event rendering. A second adapter probe exposed the
  route-wrapper handoff-id loss. After the fix, the async warm-TUI adapter probe
  rendered `Approval: conf_... target=write` plus exact
  `ALLBERT:APPROVE:conf_...`, `ALLBERT:DENY:conf_...`, and
  `ALLBERT:SHOW:conf_...` commands.
- **Gate:** focused coding/session/settings/TUI tests passed:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/tui_test.exs apps/allbert_assist/test/allbert_assist/coding/m8_session_slash_test.exs apps/allbert_assist/test/allbert_assist/coding/m9_streaming_turn_test.exs apps/allbert_assist/test/security/v057_coding_eval_test.exs apps/allbert_assist/test/allbert_assist/actions/settings_actions_test.exs apps/allbert_assist/test/allbert_assist/settings_test.exs`
  (`109 tests, 0 failures`). `MIX_ENV=test mix compile --warnings-as-errors`
  passed. `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057`
  passed after M9.14 (`90` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-13379/home/release_evidence/v057/release-v057-1782319331.json`.

### M9.15: Prompt-Safe Async TUI Turn Rendering  *(implemented)*
Reason: warm S4.3 validation exposed a terminal turn-loop defect and a validation
handoff ambiguity. The async Pi-mode worker task could render the final response
after the adapter had already opened the next `allbert:default>` prompt, making
the terminal appear to write over the operator's question and delaying live stream
events behind a blocking `IO.gets` prompt. The S4.3 validation prompt also included
a hard line break in documentation even though the TUI input loop is line-oriented,
which could submit only `Read the top of` as the first turn if pasted literally.

- **Implemented output ownership fix:** async Pi-mode worker tasks now run with a
  quiet task state: they compute the response, update trace/session state, and send
  the reply back to the TUI adapter, but they do not print directly and do not
  mutate Owl live blocks themselves.
- **Implemented prompt scheduling fix:** when a Pi-mode turn is accepted
  asynchronously, the adapter does not schedule the next input prompt until the
  matching `:coding_tui_turn_finished` message is handled. The adapter clears the
  live stream/status regions, emits the final rendered response from the GenServer,
  then opens the next prompt. If a queued correction immediately starts another
  async turn, the prompt remains deferred for that new turn as well.
- **Implemented validation handoff fix:** the S4 instructions now state that
  natural-language validation prompts are single-line TUI inputs unless a step
  explicitly says otherwise, and S4.3 now gives a copy/paste-safe one-line prompt.
- **Gate:** focused TUI regression coverage proves the auto-input loop does not
  open another prompt or print the final worker response before the async Pi-mode
  turn is released. `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/channels/tui_test.exs` passed (`17`
  tests, `0` failures). Focused cancellation/session/streaming coverage passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/coding/m6_cancel_steer_test.exs
  apps/allbert_assist/test/allbert_assist/coding/m8_session_slash_test.exs
  apps/allbert_assist/test/allbert_assist/coding/m9_streaming_turn_test.exs`
  (`22` tests, `0` failures). `MIX_ENV=test mix compile --warnings-as-errors`
  passed. `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057`
  passed after M9.15 (`91` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-8194/home/release_evidence/v057/release-v057-1782321019.json`.

### M9.16: Transcript-Stable Live Stream Rendering  *(implemented)*
Reason: the next S4.3 warm-validation run showed that prompt ownership was fixed,
but the transient Owl live block was still using the detailed transcript renderer.
During processing it could paint raw streamed tool JSON, path arguments, or large
tool-result bodies, and `LiveRegion.clear/1` then erased that block in a way that
left blank vertical gaps in transcript capture. That made the operator-visible
screen noisy while running and made release evidence harder to read after the final
answer rendered.

- **Implemented live/transcript renderer split:** `StreamRenderer.render/2` keeps
  the detailed transcript/default renderer for non-live use, but accepts
  `mode: :live`. The TUI `Renderer.stream_state/2` uses that live mode for Owl
  blocks.
- **Implemented compact live output:** live mode renders only bounded progress:
  assistant byte count, tool names, tool-result count, and cancellation status. It
  deliberately excludes raw tool arguments, file paths, JSON payloads, large read
  results, and shell output. The final rendered response remains the canonical
  transcript-visible answer.
- **Implemented validation contract update:** operator validation now treats raw
  transient JSON/tool bodies, repaint scars, and blank live-block gaps as TUI
  rendering failures. Tool argument/result details remain available through the
  stream-event trace/evidence path rather than the live terminal block.
- **Gate:** focused stream-renderer tests cover compact live rendering and prove
  the live block omits path/JSON arguments while the detailed renderer still
  preserves transcript-mode detail: `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/coding/m4_stream_rendering_test.exs`
  passed (`4` tests, `0` failures). The focused TUI regression lane also passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/channels/tui_test.exs` (`17` tests,
  `0` failures). `MIX_ENV=test mix compile --warnings-as-errors` passed.
  `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057` passed
  after M9.16 (`91` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-7173/home/release_evidence/v057/release-v057-1782321793.json`.

### M9.17: Scrollback-Stable TUI Progress Transport  *(implemented)*
Reason: the next S4.3 warm-validation run confirmed that compact live content was
useful, including streaming byte counts, but the Owl live-screen transport still
erased the transient block on completion and left blank rows in `script`-captured
transcripts. The remaining defect was transport-level: compact progress was still
rendered through an erase/repaint block.

- **Implemented launcher change:** `mix allbert.tui` now starts the supervised TUI
  child with `live_screen?: false` by default. This keeps validation and ordinary
  operator transcripts in stable scrollback instead of terminal-live-block mode.
- **Implemented output progress region:** when `live_screen?` is false, Pi-mode
  coding turns still create a stream progress region, but it writes compact
  progress as ordinary output lines: assistant byte count, tool names, tool-result
  count, cancellation, and `Turn complete`. `clear/1` is a no-op for this output
  mode, so it cannot create blank erase rows.
- **Preserved detailed surfaces:** the detailed transcript/default stream renderer
  and final response rendering still preserve final answer/detail behavior. The
  compact progress path does not emit raw tool arguments, JSON, large result bodies,
  or the final answer payload before the final renderer.
- **Gate:** focused output-progress rendering passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/coding/m4_stream_rendering_test.exs`
  (`5` tests, `0` failures). Focused TUI/launcher coverage passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/channels/tui_test.exs
  apps/allbert_assist/test/mix/tasks/allbert_tui_test.exs` (`20` tests, `0`
  failures). `MIX_ENV=test mix compile --warnings-as-errors` passed.
  `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057` passed
  after M9.17 (`92` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-13378/home/release_evidence/v057/release-v057-1782322595.json`.

### M9.18: Coalesced Transcript Progress  *(implemented)*
Reason: S4.3 validation after M9.17 removed blank erase rows, but ordinary
scrollback progress was still printed once per streamed assistant-token delta
because the byte count changed on every provider chunk. The transcript became a
wall of repeated `Tool calls`, `Tool results`, and `Assistant streaming` lines.

- **Implemented progress coalescing:** output-mode progress now emits tool names
  only when the tool set changes, tool-result counts only when the count changes,
  assistant byte counts on the first token and then at bounded byte intervals, and
  completion/cancellation once.
- **Preserved operator feedback:** byte counts still move during longer turns, but
  not for every token. Tool progress remains visible without repeating unchanged
  state.
- **Validation contract update:** S4.3 should show a few compact progress lines,
  not hundreds of repeated lines. Repetition per token is now a TUI rendering
  failure. The full Operator Validation section was also made explicit end to end:
  setup/preflight/session commands, S4 prompts, disposable files, approval/grant
  placeholders, post-session grep checks, and report-back fields now avoid
  inferred operator steps.
- **Gate:** focused stream/output-progress tests cover the coalescing behavior;
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/coding/m4_stream_rendering_test.exs`
  passed (`5` tests, `0` failures), and focused TUI/launcher coverage passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/channels/tui_test.exs
  apps/allbert_assist/test/mix/tasks/allbert_tui_test.exs` (`20` tests, `0`
  failures). `git diff --check`, `MIX_ENV=test mix format --check-formatted`, and
  `MIX_ENV=test mix compile --warnings-as-errors` passed.
  `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057` passed
  after M9.18 (`92` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-6978/home/release_evidence/v057/release-v057-1782323373.json`.

### M9.19: Typed Approval Resume Context  *(implemented)*
Reason: S4.4 validation reached the correct pending-confirmation handoff, and
`/diff tmp/v057-default-confirmation.txt` correctly showed the file absent before
approval. But typing the exact `ALLBERT:APPROVE:<id>` command resolved the
confirmation as `denied` instead of resuming the write. The write effect remained
safe, but the operator handoff was not release-ready.

- **Implemented durable coding origin:** confirmation records now persist a
  redaction-safe coding-origin snapshot for coding actions: cwd jail/workspace
  root, Pi-mode enabled flag, approval mode/model metadata, session shape, and
  disallowed-origin flags. It deliberately does not persist
  `coding.trusted_operator_id`; current Settings Central or the live approval
  context still supplies trust.
- **Implemented resume restoration:** `approve_confirmation` restores that
  origin snapshot for coding `write`/`edit`/`bash` resumes before re-running the
  target through `Actions.Runner.run/3`. Existing live coding context is merged
  in so direct tests and CLI approval surfaces keep their current trust fields.
  Resolver metadata remains separate, so the audit still records where the
  operator approved from.
- **Implemented mixed-session normalization:** `Security.Policy` now treats a
  context with atom top-level `:session` and persisted string nested
  `"main?" => true` as a main session. This closes the durable YAML round-trip
  shape that caused the local-coding tier check to fail during resume.
- **Validation contract update:** S4.4 approval must resolve the confirmation as
  approved, set `target_resumed?=true`, and create exactly the requested file. An
  `APPROVE` command that prints `Confirmation <id> is denied` is a validation
  failure.
- **Gate:** focused coding approval-resume coverage passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/coding/m2_write_edit_actions_test.exs
  apps/allbert_assist/test/allbert_assist/coding/m8_session_slash_test.exs
  apps/allbert_assist/test/allbert_assist/channels/tui_test.exs` (`29` tests,
  `0` failures). `MIX_ENV=test mix format --check-formatted` and
  `MIX_ENV=test mix compile --warnings-as-errors` passed.
  `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057` passed
  after M9.19 (`92` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-13378/home/release_evidence/v057/release-v057-1782329944.json`.

### M9.20: Bash Validation Execution Preflight  *(implemented)*
Reason: S4.6 warm validation proved the `accept-edits` write behavior but the
next model-proposed `bash pwd` returned a denial for disabled local execution.
That was not an approval-mode bug: `accept-edits` intentionally does not suppress
`bash`, but Pi-mode's `bash` action first loads the ADR 0009 Level 1 local
execution policy. With the default `execution.local.enabled=false`, the command is
denied before Security Central can create the expected bash confirmation.

- **Root-cause finding:** the operator-validation setup named Pi-mode settings
  (`coding.pi_mode.enabled`, trusted operator, cwd jail, model profile, approval
  mode) but omitted the lower `execution.local.*` policy required by the `bash`
  action. Unit tests configured that policy directly, so deterministic gates were
  green while the manual validation home was incomplete.
- **Documentation remediation:** S1 now records `V057_REPO_ROOT`, pins
  `coding.workspace.cwd_jail` to it, sets `execution.local.enabled=true`, scopes
  `execution.local.allowed_roots` to the same repo root, allows only `pwd` and
  `printf` for the S4.6/S4.7 bash checks, and keeps
  `execution.local.require_confirmation=true`. S4.6 now explicitly treats
  `:local_execution_disabled` as a setup/preflight failure and tells the operator
  to apply S1's execution settings before re-running the `bash pwd` prompt.
- **Authority implication:** this does not broaden Pi-mode authority. The coding
  session guard, local-coding tier, cwd jail, argv-only non-raw-shell policy,
  command allowlist, Security Central confirmation, remembered-grant scoping,
  trace, and audit still all have to pass. The change only makes the disposable
  validation home match the policy stack that the implementation and tests
  already require.
- **Operator implication:** after applying M9.20 setup to the same
  `ALLBERT_HOME`, the operator should resume at S4.6 by re-running only
  `Use the bash tool to run pwd. Do not use read, grep, glob, write, or edit.`
  The expected result is a pending confirmation for `pwd`, not immediate command
  execution and not a local-execution-disabled denial.
- **Gate:** docs-only remediation; `git diff --check` is sufficient before
  commit unless code changes are added in the same milestone.

### M9.21: Operator-Friendly Bash Command Strings  *(implemented)*
Reason: after M9.20 setup was applied, S4.6 still failed operator validation. The
model emitted a real `bash` tool call, but it supplied simple command text
(`command: "pwd"`) instead of the lower-level `executable`/`args` shape. The
implementation treated every `command` string as raw shell, so the turn was denied
under the raw-shell restriction and the assistant tried to plan around the denial.
That was safe, but it was not an operator-friendly or model-robust Pi-mode
contract.

- **Implemented argv fallback:** `Coding.BashSpec` now parses plain command
  strings with `OptionParser.split/1` when no explicit executable is supplied and
  no shell syntax is present. `command: "pwd"` becomes the same argv command as
  `executable: "pwd", args: []`; `command: "printf 'hello world\n'"` becomes an
  argv `printf` with arguments. These still pass `Execution.CommandSpec`, the
  Level 1 enabled flag, cwd-root policy, command allowlist, env allowlist, timeout
  and output limits, Security Central confirmation, remembered-grant matching,
  trace, and audit.
- **Preserved raw-shell boundary:** command strings containing shell operators,
  redirection, backgrounding, command substitution, backticks, or newline command
  separators remain raw-shell requests. They still require
  `coding.bash.allow_raw_shell=true` and the local-coding operator tier, and are
  denied otherwise.
- **Closed argv sub-agent gap:** the no-sub-agent rule now applies to argv too.
  `codex`, `claude`, `gemini`, `opencode`, `cursor`, and `antigravity` executables
  are refused before command execution even if a future operator accidentally adds
  one to `execution.local.allowed_commands`.
- **Tool-schema guidance:** the model-facing `bash` tool description now prefers
  `executable` plus `args`, explains the safe `command` fallback, and names the
  raw-shell-only operators. This guides models without trusting model compliance.
- **Validation contract update:** S4.6 accepts either `executable`/`args` or a
  plain `command` string for `pwd`, but the result must be a pending `bash`
  confirmation. A denial followed by a prose workaround proposal is a validation
  failure.
- **Gate:** focused bash/action and streaming-loop coverage passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/coding/m3_bash_action_test.exs
  apps/allbert_assist/test/allbert_assist/coding/m9_streaming_turn_test.exs`
  (`20` tests, `0` failures). Focused v0.57 security eval passed:
  `MIX_ENV=test mix test apps/allbert_assist/test/security/v057_coding_eval_test.exs`
  (`10` tests, `0` failures). `git diff --check`,
  `MIX_ENV=test mix format --check-formatted`, and
  `MIX_ENV=test mix compile --warnings-as-errors` passed.
  `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057` passed
  after M9.21 (`93` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-13378/home/release_evidence/v057/release-v057-1782333708.json`.

### M9.22: Command-Grant Remember Handoff  *(implemented)*
Reason: S4.7 warm validation approved the pending `bash pwd` confirmation from a
second terminal with `mix allbert.confirmations approve <id> --remember exact`.
The target `bash` action resumed and completed, but the CLI then failed with
`:resource_ref_not_found` while trying to create the remembered grant. That was a
handoff mismatch: generic resource grants are remembered from
`params_summary.resource_refs`, while Pi-mode command grants deliberately use
hash-only canonical command params and store through `Coding.CommandGrants`.

- **Implemented command-grant branch:** `Resources.GrantHandoff` now detects
  confirmations whose target action is `bash` and whose permission is
  `coding_shell_execute`. For `--remember exact`, it builds the remembered command
  grant from the stored `resume_params_ref` through `Coding.CommandGrants.remember/2`
  instead of requiring generic resource refs.
- **Preserved generic resource semantics:** non-command confirmations still use
  the existing `resource_refs` flow and still return `:resource_ref_not_found`
  when an operator asks to remember a grant for a confirmation with no resource
  reference. Pi-mode command grants remain exact-only; other scopes return
  `{:remember_scope_unavailable, scope}`.
- **Operator implication:** when S4.7 succeeds, the second terminal should print
  `status=approved`, `Target: bash status=completed`, and `Remembered grant: ...`
  rather than failing after the command completes. The grant remains listed,
  shown, revoked, and matched through the shared `resource_grants.remembered`
  lifecycle.
- **Gate:** focused bash/action plus confirmations CLI coverage passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/coding/m3_bash_action_test.exs
  apps/allbert_assist/test/mix/tasks/allbert_confirmations_test.exs`
  (`15` tests, `0` failures). `git diff --check`,
  `MIX_ENV=test mix format --check-formatted`, and
  `MIX_ENV=test mix compile --warnings-as-errors` passed.
  `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057` passed
  after M9.22 (`94` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-13379/home/release_evidence/v057/release-v057-1782344996.json`.

### M9.23: Terminal Esc Handoff During Async Turns  *(implemented; invalidated by S4.9)*
Reason: S4.9 warm validation proved provider-stream cancellation and task
shutdown were implemented, but the live operator keypress did not reach that
path. While a Pi-mode coding turn was running, the adapter intentionally did not
schedule the next blocking `IO.gets` prompt, so pressing Esc was echoed by the
terminal as literal `^[` bytes and the turn ran to completion.

- **Root-cause finding:** the cancellation model had two halves: the supervised
  turn registry and provider `StreamResponse.cancel` callback were live, but the
  interactive terminal had no side-channel key reader during an accepted async
  turn. Line-oriented prompt ownership fixed prompt overlap in M9.15, but left
  Esc unreachable until a new prompt opened.
- **Implemented TUI event handoff:** `mix allbert.tui` now starts a transient
  no-echo, noncanonical Esc monitor only while an interactive async Pi-mode turn
  is active. A standalone Esc sends a `:coding_tui_escape` event to the adapter,
  which reuses the existing `cancel_current_turn_state/2` path. The monitor is
  stopped and the terminal mode is restored when cancellation is requested, the
  turn finishes, or the adapter exits. Non-interactive adapter use and direct
  test/API cancellation remain unchanged.
- **Original operator expectation, superseded:** M9.23 expected S4.9 to show
  cancellation feedback instead of literal `^[` scrollback. Later validation
  proved this expectation false; do not use M9.23 as passing Esc evidence.
- **Gate:** focused TUI coverage passed:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/tui_test.exs`
  (`18` tests, `0` failures). Focused cancellation/streaming/TUI coverage passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/coding/m6_cancel_steer_test.exs
  apps/allbert_assist/test/allbert_assist/coding/m9_streaming_turn_test.exs
  apps/allbert_assist/test/allbert_assist/channels/tui_test.exs`
  (`34` tests, `0` failures). `git diff --check`,
  `MIX_ENV=test mix format --check-formatted`, and
  `MIX_ENV=test mix compile --warnings-as-errors` passed.
  `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057` passed
  after M9.23 (`95` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-13378/home/release_evidence/v057/release-v057-1782346611.json`.

  **Supersession:** later warm validation still showed literal `^[` bytes and a
  normal `Turn complete`; this proved the BEAM side-channel reader did not own
  the operator's live terminal input. M9.23 remains useful history but is not a
  passing Esc-cancel implementation.

### M9.24: Direct Controlling-TTY Esc Monitor  *(implemented; invalidated by S4.9)*
Reason: the next S4.9 warm validation still printed literal `^[` characters and
the original coding turn ran to `Turn complete`. The operator was pressing Esc;
the TUI was echoing it back. M9.23 started a side-channel monitor, but it still
read from `IO.getn(:stdio, ...)`, which goes through the Erlang standard I/O
server/group-leader path rather than directly reading the PTY used by the
interactive terminal.

- **Root-cause finding:** the cancellation supervisor and adapter event were live,
  but the monitor did not own the same input stream that `script ... mix
  allbert.tui` presents to the operator. In that shape, Esc could still be echoed
  as terminal text before the monitor observed it.
- **Implemented terminal ownership fix:** `EscapeMonitor` now snapshots and sets
  terminal mode through `stty ... < /dev/tty`, opens `/dev/tty` directly in binary
  read mode, and polls that controlling terminal while the async coding turn is
  active. It restores the original mode when the turn finishes, cancellation is
  requested, the monitor is stopped, or the adapter exits. The adapter-side
  cancellation event remains the same `:coding_tui_escape` handoff introduced in
  M9.23.
- **Research note:** Elixir's `System.cmd/3` documentation calls out standard I/O
  handling and the `:use_stdio` boundary for direct terminal interaction, while
  Erlang's I/O docs describe terminal capabilities as tied to terminal devices.
  The production fix therefore avoids the BEAM line-oriented `:stdio` path for
  the Esc byte and reads from the controlling terminal device.
- **Original operator expectation, superseded:** if S4.9 still showed literal
  `^[` after M9.24, the remaining problem was terminal capture compatibility for
  the operator's wrapper rather than the provider stream or cancellation
  supervisor. Later validation proved M9.24 was not sufficient; do not use M9.24
  as passing Esc evidence.
- **Gate:** focused cancellation/streaming/TUI coverage passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/coding/m6_cancel_steer_test.exs
  apps/allbert_assist/test/allbert_assist/coding/m9_streaming_turn_test.exs
  apps/allbert_assist/test/allbert_assist/channels/tui_test.exs`
  (`34` tests, `0` failures). A PTY smoke of `stty -g < /dev/tty`, temporary
  no-echo/noncanonical mode, and restore passed when run with terminal access.
  `git diff --check`, `MIX_ENV=test mix format --check-formatted`,
  `MIX_ENV=test mix compile --warnings-as-errors`, and
  `MIX_ENV=test mix allbert.test docs` passed.
  `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057` passed
  after M9.24 (`95` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-8194/home/release_evidence/v057/release-v057-1782347367.json`.

  **Supersession:** later warm validation still showed literal `^[` bytes. Direct
  `/dev/tty` ownership from the BEAM process was not sufficient under the
  release-validation launcher, so this is recorded as a failed implementation
  approach rather than release evidence.

### M9.25: Helper-Owned Esc Capture And Slash Hygiene  *(partially implemented; Esc capture invalidated)*
Reason: S4.9 validation after M9.24 still showed literal `^[` scrollback and the
original turn ran to `Turn complete`. The same terminal works for Codex/Claude
Code, so the defect was Allbert's TUI ownership, not the operator's keypress. The
next probe also showed `/exit` being submitted to the model; the most likely
cause is echoed Esc/control bytes prefixing the next line invisibly so the local
quit/slash recognizer no longer sees a leading `/`.

- **Root-cause finding:** a BEAM process reading `/dev/tty` was still not a strong
  enough terminal owner in the live `script ... mix allbert.tui` path. It could
  fail to win the input race without producing operator-visible evidence.
- **Implemented helper-owned capture:** `EscapeMonitor` now starts a tiny
  supervised `/bin/sh` helper for the duration of an async coding turn. The helper
  snapshots terminal mode, sets `/dev/tty` to no-echo noncanonical polling mode,
  prints `READY` after it is armed, consumes bytes from `/dev/tty` outside the
  BEAM group-leader path, prints `ESC` when it reads byte `27`, and restores the
  original terminal mode on exit. The adapter starts the monitor only when the
  helper reports `READY`; helper startup failure is rendered visibly in the TUI.
- **Implemented slash hygiene:** TUI line normalization now strips leading
  terminal control bytes before slash/quit recognition, so hidden Esc bytes cannot
  turn `/exit`, `/quit`, or other slash commands into model-routed natural
  language. `/exit` and `/quit` are now listed in `/help`.
- **Operator implication:** M9.25 made helper startup failure visible and kept
  `/exit`/`/quit` local through slash/control-byte hygiene, but live validation
  proved the helper path did not satisfy Esc capture. Literal `^[`, a visible
  `Esc cancellation monitor unavailable: ...` diagnostic, or model-routed
  `/exit`/`/quit` remains release-blocking.
- **Gate:** focused cancellation/streaming/TUI coverage passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/coding/m6_cancel_steer_test.exs
  apps/allbert_assist/test/allbert_assist/coding/m9_streaming_turn_test.exs
  apps/allbert_assist/test/allbert_assist/channels/tui_test.exs`
  (`37` tests, `0` failures). A PTY smoke of the helper script printed `READY`,
  consumed an actual Esc byte, printed `ESC`, and restored the terminal.
  `git diff --check`, `MIX_ENV=test mix format --check-formatted`,
  `MIX_ENV=test mix compile --warnings-as-errors`, and
  `MIX_ENV=test mix allbert.test docs` passed.
  `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test release.v057` passed
  after M9.25 (`98` coding unit tests, `14` eval tests, secret scan clean);
  evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-13379/home/release_evidence/v057/release-v057-1782348438.json`.

  **Supersession:** live S4.9 after M9.25 printed
  `Esc cancellation monitor unavailable: {:escape_monitor_helper_exit, 2,
  ["/bin/sh: /dev/tty: Device not configured"]}`. The helper handshake made the
  failure visible and the slash/control-byte hygiene remains correct, but the
  helper-owned `/dev/tty` path is not a valid Esc-capture substrate for release.
  Do not repeat S4.9 expecting M9.25 to pass; use the M9.30+ input-driver
  validation flow after M9.30 re-gates the slice.

### M9.26: Terminal Input Substrate Correction  *(planned; release-blocking)*
Reason: S4.9 proved the Esc failure is not a provider-stream bug, not a task
supervision bug, and not a full-screen-rendering problem. It is an input
substrate mismatch: v0.55 intentionally chose scrollback-native line input, while
v0.57 added a single-key interrupt requirement that needs character-level input
during active turns.

- **Boundary correction:** v0.55 remains valid and must not be redone. Its
  contract is typed-command/line input, same-channel confirmations, native
  scrollback, and split payload rendering. v0.57 extends that channel additively
  with an interrupt-capable input driver for Pi-mode coding turns.
- **Architecture finding:** line-mode/canonical input can echo `^[` and buffer Esc
  until a line delimiter; a BEAM side-channel reader and helper-owned `/dev/tty`
  process are not sufficient under the warm-validation `script ... mix
  allbert.tui` launcher. The next implementation must own the terminal input path
  intentionally, not race the existing line reader.
- **Non-goal:** do not move to a full-screen or alternate-screen TUI merely to
  solve Esc. Preserve v0.55's scrollback-native output model; add the smallest
  proven input substrate that can deliver line events plus key events.
- **Docs gate:** `docs/adr/0067-tui-terminal-channel.md` must clarify that the
  v0.55 line-oriented TUI is still the base channel and that interrupt-capable
  input is an additive Pi-mode requirement, not a rollback. ADR 0068 must require
  that Esc capture is authority-neutral and still routes only through
  `Coding.TurnSupervisor` and provider stream cancel.

### M9.27: Interrupt-Capable Terminal Proof Harness  *(implemented)*
Reason: before wiring a new input path into production TUI turns, the terminal
substrate had to prove it can capture a standalone Esc under the same transcript
wrapper shape used by operator validation.

- **Harness requirements:** press-and-release Esc is consumed without Enter,
  without `^[` in scrollback, and without waiting for a new prompt; ordinary
  typed lines, Backspace, Enter, `/exit`, and `/quit` still behave locally; terminal
  mode is restored after normal exit, cancellation, crash, and Ctrl-C.
- **Proof shape:** the harness must run outside unit-test-only fakes and record a
  short transcript/evidence artifact. A PTY-only smoke that does not launch
  `mix allbert.tui` is insufficient.
- **Implementation lane:** this is a serial substrate spike before the remaining
  validation lane. Do not continue release validation past S4.8 until this proof
  exists.
- **Implemented harness:** `mix allbert.tui --input-driver-proof` starts the new
  `AllbertAssist.Channels.TUI.InputDriver` in raw OTP shell mode, renders
  `allbert:proof>`, and exits with `PROOF:ESC` when a standalone Esc byte is
  consumed. It also accepts ordinary line input so the same driver can prove line
  events.
- **Implemented substrate:** `InputDriver` owns raw/cooked terminal mode through
  `:shell.start_interactive({:noshell, :raw | :cooked})`, reads characters in a
  linked reader process, emits line events, emits standalone Esc key events,
  handles Backspace, and restores cooked mode on shutdown. The module is
  injectable for tests and does not invoke actions or grant authority.
- **Proof evidence:** focused TUI tests passed:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/tui_test.exs`
  (`23` tests, `0` failures). A PTY proof passed with
  `MIX_ENV=test mix allbert.tui --input-driver-proof`; sending Esc printed
  `PROOF:ESC` with no literal `^[`. The stricter transcript-wrapper proof passed
  with `MIX_ENV=test script -q /tmp/allbert-v057-input-driver-proof.txt mix
  allbert.tui --input-driver-proof`; the transcript contains `PROOF:ESC` and no
  literal `^[`.

### M9.28: Interrupt-Capable TUI Input Driver  *(implemented)*
Replace the active-turn Esc side helper with a real input-driver boundary for
interactive `mix allbert.tui`.

- **Contract:** the driver emits two event types to the TUI adapter: completed line
  events for normal prompt input and key events for active-turn controls. Esc
  during an accepted async Pi-mode turn maps to the existing
  `cancel_current_turn_state/2` path. Slash commands are recognized locally before
  any model turn. Hidden terminal control bytes are stripped before slash matching.
- **Authority:** the input driver grants no permissions and invokes no actions.
  It only delivers operator input events. All file/shell effects remain behind
  `Actions.Runner.run/3`, Security Central, confirmations, resource grants, and
  the cwd jail.
- **Output model:** keep transcript-stable progress and final scrollback output.
  Do not introduce alternate-screen ownership, full-viewport redraw, or prompt
  repainting over operator text.
- **Tests:** add focused driver tests for line input, Esc cancellation, slash
  lifecycle commands, restoration on failure, and no model routing for `/exit` or
  `/quit`. Keep the existing cancellation/provider tests, but treat them as
  cancellation-spine coverage, not terminal-input proof.
- **Implemented production wiring:** normal `mix allbert.tui` now starts
  `InputDriver` for the interactive auto-input child, disables the old
  `EscapeMonitor`, and routes driver line events through the same TUI adapter
  submission path used by line input. Accepted async Pi-mode turns put the driver
  in active-turn mode, so a standalone Esc sends `:operator_escape` into the
  existing `cancel_current_turn_state/2` and `Coding.TurnSupervisor.cancel/3`
  path. `/exit` and `/quit` still stop the local TUI before any model turn.
- **Authority check:** the input driver emits only operator input events. It never
  invokes tools, grants permissions, resolves confirmations, or touches file/shell
  effects. All coding effects remain behind `Actions.Runner.run/3`, Security
  Central, cwd jail, confirmations, and resource grants.
- **Smoke evidence:** focused TUI tests passed:
  `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/channels/tui_test.exs`
  (`24` tests, `0` failures). `MIX_ENV=test mix compile --warnings-as-errors`
  passed. A normal-launch smoke passed with
  `ALLBERT_HOME=/tmp/allbert-v057-m928-smoke MIX_ENV=test script -q
  /tmp/allbert-v057-m928-tui-smoke.txt mix allbert.tui`: pressing Esc at the
  prompt printed `No coding turn is currently running.`, `/exit` exited locally,
  and the transcript contains no literal `^[`.

### M9.29: Operator Validation Rewrite For Terminal Input  *(implemented)*
Rewrite S4.9 after M9.27/M9.28 land.

- **Stop rule now:** if validation prints literal `^[`, prints
  `Esc cancellation monitor unavailable: ...`, or routes `/exit`/`/quit` to the
  model, stop at S4.9. Do not proceed to S5 or release closeout.
- **Future pass criteria:** S4.9 must show tool/assistant progress, consume a
  standalone Esc without `^[`, print cancellation feedback, invoke the registered
  provider `StreamResponse.cancel` callback before bounded task shutdown, reject
  late tool results after cancellation, accept the queued correction as a new
  operator line, and keep `/exit`/`/quit` local.
- **Evidence:** retain the transcript, focused input-driver proof artifact,
  focused test output, `release.v057` evidence, and the final operator validation
  transcript together.
- **Implemented validation handoff:** `docs/plans/v0.57-request-flow.md` now tells
  operators to restart the TUI on M9.30+ code, run the long read/grep prompt,
  press a real standalone Esc while streaming, and expect immediate cancellation
  without `^[`. It separates old-code stop rules from the new pass criteria,
  requires `/exit`/`/quit` to remain local, and keeps S5 blocked until S4.9 passes
  on M9.30+ code.
- **Operator implication:** validation resumes at S4.9 after M9.30 re-gates.
  Earlier S4.1-S4.8 evidence can be reused only if it was produced with the same
  disposable validation home and no relevant settings changed.

### M9.30: Re-Gate And Fourth Post-Remediation Audit  *(implemented)*
After M9.28 and M9.29 land, re-run the gate set before release closeout.

- **Focused gate:** terminal input-driver tests, TUI channel tests, streaming turn
  tests, cancellation tests, and coding eval tests.
- **Docs gate:** `git diff --check`, docs hygiene, and a semantic grep for stale
  M9.23-M9.25 success language.
- **Release gate:** `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test
  release.v057`.
- **Manual gate:** restart a fresh warm TUI validation home and resume operator
  validation at S4.9 only after earlier S4 evidence is still current. If any
  operator-visible behavior differs from the rewritten request-flow expectations,
  record it in this plan before another implementation pass.
- **Implemented gate evidence:** focused terminal/TUI/cancellation/streaming/eval
  gate passed:
  `MIX_ENV=test mix test
  apps/allbert_assist/test/allbert_assist/channels/tui_test.exs
  apps/allbert_assist/test/mix/tasks/allbert_tui_test.exs
  apps/allbert_assist/test/allbert_assist/coding/m6_cancel_steer_test.exs
  apps/allbert_assist/test/allbert_assist/coding/m9_streaming_turn_test.exs
  apps/allbert_assist/test/security/v057_coding_eval_test.exs`
  (`53` tests, `0` failures). `MIX_ENV=test mix compile --warnings-as-errors`,
  `git diff --check`, `MIX_ENV=test mix allbert.test docs`, and the stale-helper
  semantic grep passed. `ALLBERT_TEST_KEEP_TMP=1 MIX_ENV=test mix allbert.test
  release.v057` passed (`101` coding unit tests, `14` eval tests, secret scan
  clean); evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v057/p0-6787/home/release_evidence/v057/release-v057-1782361642.json`.
- **Validation handoff:** project is ready for the next operator validation cycle.
  Restart `mix allbert.tui` on M9.30+ code and resume at S4.9 in
  `docs/plans/v0.57-request-flow.md`.

## Settings Central Keys
All new settings register in the Settings Central schema (specs + defaults +
`safe_write_keys`); no module reads ad-hoc config. Defaults are conservative
(Pi-mode off; raw shell off until the tier resolves).

| Key | Default | Purpose |
|---|---|---|
| `coding.pi_mode.enabled` | `false` | Master enable for the Pi-mode coding surface. |
| `coding.trusted_operator_id` | `nil` | The single operator the local-coding tier resolves for. |
| `coding.bash.allow_raw_shell` | `false` | Permit raw shell strings — **honored only when the local-coding tier resolves**; argv-only otherwise. |
| `coding.workspace.cwd_jail` | `.` | Directory the `read`/`grep`/`glob`/`write`/`edit`/`bash` actions are confined to, resolved from the process/repo cwd until `/pi [path]` pins the session root, then symlink-checked at session start. |
| `coding.bash.timeout_ms` | `120000` | Wall-clock timeout before brutal-kill. |
| `coding.bash.max_output_bytes` | `120000` | Output cap for `bash` before truncation/redaction. |
| `execution.local.enabled` | `false` | Existing Level 1 local execution master switch; v0.57 `bash` cannot reach Pi-mode confirmation until this is true in the validation/operator home. |
| `execution.local.allowed_roots` | `[]` | Existing Level 1 execution root allowlist; v0.57 validation sets it to the repo root even though Pi-mode also narrows execution to the active cwd jail. |
| `execution.local.allowed_commands` | `["pwd", "ls", "find", "rg", "cat", "sed", "head", "tail", "wc"]` | Existing Level 1 command allowlist; v0.57 manual validation constrains it to `["pwd","printf"]` for the remembered-grant checks. |
| `execution.local.require_confirmation` | `true` | Existing Level 1 confirmation posture; v0.57 validation keeps this true so `bash` prompts unless an exact remembered command grant applies. |
| `coding.write.max_bytes` | `120000` | Maximum bytes a single `write` may create or a single `edit` may leave in the target file. |
| `coding.edit.max_replacements` | `1` | Maximum replacements per exact-match `edit` call unless explicitly expanded. |
| `coding.prompt.token_budget` | `1000` | Combined system-prompt + all six tool-defs ceiling. |
| `coding.prompt.tokenizer` | `simple_words` | Named tokenizer used by the budget gate; fallback approximations fail closed if over budget. |
| `coding.model_profile` | `pi_coding_local` | Model profile the Pi-mode session starts with. `pi_coding_local` should resolve to a local/private profile that emits real provider tool-call chunks for repo work; hosted profiles require explicit operator egress acceptance. `/pi` resolves it through Settings Central and reports profile errors before starting; `/model` changes only the in-memory session profile. |
| `coding.read.default_limit` | `2000` | Default chunked-read line limit (offset/limit). |
| `coding.read.max_bytes` | `120000` | Maximum bytes a single `read` action may return before truncation/redaction. |
| `coding.search.max_results` | `100` | Maximum grep/glob matches returned to the model and surface. |
| `coding.search.max_output_bytes` | `120000` | Maximum search output bytes before truncation/redaction. |
| `coding.search.respect_gitignore` | `true` | Search defaults to repo ignore rules. |
| `coding.search.respect_allbertignore` | `true` | Search also honors repo-local `.allbertignore` rules for Allbert-specific exclusions. |
| `coding.default_approval_mode` | `default` | Default coder-facing mode: `default` / `accept-edits` / `plan` / `tier`; session overrides are ephemeral unless explicitly saved. |
| `coding.command_grants.default_ttl_ms` | bounded | Optional default expiry for remembered command grants (reuses the `Resources.Grants` lifecycle, not a parallel store). |
| `coding.command_grants.max_entries_per_repo` | bounded | Limit for per-repo remembered command grants. |
| `coding.turn.supervised` | `true` | Run a coding turn's model call under a supervised Task so it is cancellable; falls back to synchronous turn-complete when false. |
| `coding.turn.max_ms` | bounded | Hard ceiling on a single coding turn before forced shutdown. |
| `coding.streaming.enabled` | `true` | Enable v0.57 coding stream-event rendering: assistant text token deltas + progressive tool-argument rendering. |
| `coding.streaming.turn_complete_fallback` | `true` | Keep the v0.55 split-payload fallback when live stream events are unavailable or invalid. |
| `coding.steer.enabled` | `true` | Allow Esc-to-cancel a running turn and a queued correction once the M9.28 interrupt-capable TUI input driver is active. |
| `coding.cancel.grace_ms` | bounded | Grace window before a cancellable action falls back to timeout/brutal-kill behavior. |
| `permissions.coding_file_read` | `allowed` | Permission atom `:coding_file_read`; covers bounded `read`/`grep`/`glob` and `/diff`/`@file`. |
| `permissions.coding_file_write` | `needs_confirmation` | Permission atom `:coding_file_write`; covers `write`, `edit`, and `/init`. |
| `permissions.coding_shell_execute` | `needs_confirmation` | Permission atom `:coding_shell_execute`; covers Pi-mode `bash`. |

## Security / Eval Rows (`:v057`)
- `pi-mode-tools-route-through-runner-001` (every `read`/`grep`/`glob`/`write`/
  `edit`/`bash` resolves through `Actions.Registry` and executes via
  `Runner.run/3`; no direct effect/read path).
- `pi-mode-tools-denied-out-of-session-001` (the six coding actions are internal,
  rejected by public protocol exposure, absent from `agent_modules/0`, and deny
  direct invocation without active Pi-mode session metadata plus the local-coding
  operator tier).
- `pi-mode-permission-vocabulary-001` (permission atoms are exactly
  `:coding_file_read`, `:coding_file_write`, and `:coding_shell_execute`,
  registered at all six `Security.Policy` sites with the declared floors; Settings
  keys are `permissions.*`; no dotted permission atom and no `:coding_session_write`
  is introduced).
- `pi-mode-read-search-policy-bounded-001` (`read`/`grep`/`glob` are
  read-only/sensitive, run without a confirmation prompt, enforce cwd jail,
  ignore policy, symlink policy, binary handling, output caps, redaction, trace,
  and audit, and never mutate).
- `pi-mode-bash-policy-bounded-001` (`bash` refuses outside policy — cwd-jail +
  timeout enforced; OTP/BEAM is not treated as the security boundary; the
  host/container is).
- `pi-mode-bash-raw-shell-tier-only-001` (raw shell strings resolve **only** at the
  local-coding operator tier; every other caller is argv-only or refused).
- `pi-mode-write-edit-cwd-jail-001` (`write`/`edit` refuse normalized path escape,
  symlink escape, and writes outside `coding.workspace.cwd_jail`).
- `pi-mode-file-effects-tier-gated-001` (`write`/`edit` get the same
  trusted-tier/full-gate split as `bash`; channel-originated, scheduled, and
  generated-code callers cannot get cheap file effects).
- `local-coding-tier-trusted-only-001` (the tier resolves only for a single trusted
  operator + main session + `tui`, at sandbox Level 1).
- `local-coding-tier-not-default-001` (default resolution is never the tier).
- `local-coding-tier-rejects-channel-origin-001` (channel-originated inbound /
  scheduled / generated-code → full gate, no tier escalation, no raw shell).
- `pi-mode-deterministic-acceptance-001` (effectful/generated-code work is never
  accepted on model confidence; evidence + confirmation rule).
- `split-result-no-ui-leak-001` (the `surface_payload`/diff never leaks into
  `model_payload`; the model sees only the clean payload).
- `pi-mode-prompt-token-budget-001` (assembled system prompt **+ the six tool
  definitions combined** are < the configured budget against the named tokenizer).
- `pi-mode-context-discipline-chunked-001` (default context gathering uses chunked
  reads (offset/limit) + artifacts, not whole-file ingestion).
- `pi-mode-default-tool-surface-001` (a default coding turn exposes exactly the six
  tools — `read`/`write`/`edit`/`bash`/`grep`/`glob`; the rest are lazy-disclosed).
- `pi-mode-agent-loop-model-tools-001` (inside an active Pi-mode coding turn, the
  six session-local `ReqLLM.Tool` definitions are model-callable, model-proposed
  calls route through `Actions.Runner.run/3`, confirmation-needed effects remain
  pending, and out-of-session tool attempts still deny).
- `pi-mode-approval-mode-grants-no-authority-001` (`default`/`accept-edits`/`plan`/
  `tier` change only confirmation cost; `plan` executes only read/search; no mode
  grants authority beyond its definition).
- `pi-mode-cheap-gate-preserves-decision-001` (the tier/mode confirmation-cost seam
  drops only the interactive prompt — the `:needs_confirmation` decision, policy,
  risk, trace, and audit are unchanged).
- `pi-mode-command-grant-scoped-revocable-auditable-001` ("always allow `<cmd>`"
  reuses the `Resources.Grants` lifecycle: suppresses the prompt only for that repo
  fingerprint + canonical (normalized/redacted) command + permission + cwd; entries
  expire when configured, are listable/revocable/audited, and never become a blanket
  bypass).
- `pi-mode-turn-supervised-cancellable-001` (a coding turn runs under a supervised
  Task addressable by turn id; killing it yields a partial-turn trace and no
  orphaned model call/action).
- `pi-mode-stream-event-contract-001` (runtime emits the M0 stream-event contract
  and reconciles deltas to final `model_payload`/`surface_payload`).
- `pi-mode-assistant-text-streams-001` (provider stream chunks emit assistant token
  deltas incrementally and reconcile to the final split payload).
- `pi-mode-interrupt-clean-cancel-001` (Esc invokes the registered provider
  `ReqLLM.StreamResponse.cancel` callback before supervised task shutdown and
  partial-turn trace preservation).
- `pi-mode-terminal-input-driver-001` (under the warm `script ... mix
  allbert.tui` launcher, the TUI input driver consumes standalone Esc without
  Enter or literal `^[`, emits normal line events for typed prompts, restores
  terminal mode, and preserves native scrollback output).
- `pi-mode-slash-control-byte-hygiene-001` (`/exit`, `/quit`, and slash commands
  remain local and non-routable even after leading terminal control bytes are
  present in the input buffer).
- `pi-mode-slash-effects-action-backed-001` (`/help` is router-local; `/diff` and
  `@file` use `:coding_file_read`; `/init` uses `:coding_file_write`; `/mode`,
  `/model`, `/clear`, and `/compact` are non-routable session ops with **no**
  permission gate; all are traced or audited according to effect).
- `pi-mode-coding-slash-non-routable-001` (`/help`/`/mode`/`/model`/`/clear`/
  `/init`/`/diff`/`/compact` create no model turn, are slash-allowlisted only, and
  are absent from `agent_modules/0`).
- `pi-mode-model-switch-preserves-authority-001` (mid-session switch changes the
  model via req_llm message-history handoff, never the Security Central path).
- `pi-mode-no-bash-subagent-001` (no bash-spawned sub-agent; delegation only via
  restricted-registry delegate agents).
- `pi-mode-no-authority-001` (the surface cannot bypass Security Central, set the
  active app, or grant resource access).

## Documentation Deliverables
- ADR 0068 (Pi-mode coding surface + local-coding operator trust tier) accepted.
- Operator Pi-mode coding-session setup + trust-tier guide.
- The minimalism budget recorded as a standing coding policy (prompt/tool budget +
  the "does this belong in the kernel?" gate on new ADRs).
- Roadmap §v0.57, vision, and agent-context-map reconciliation; the
  `pi-integration-rethink.md` sequencing items 3–5 marked delivered.

## Non-Goals
*(from the Pi analysis §8 "what not to do")*
- **No YOLO-by-default.** The local-coding operator tier is a named, gated,
  non-default tier (sandbox Level 1) — never the default, never for
  channel-originated or generated-code sessions.
- **No weakening of the action boundary / Security Central / confirmations.** The
  gates are made *cheap at the tier*, not *absent*; every effect still routes
  through `Actions.Runner.run/3` at Level 1.
- **No "sandbox level 0" label / no isolation relaxation.** The tier changes the
  confirmation burden, not the sandbox level; host execution stays Level 1.
- **No model-decides-it's-done** for effectful or generated-code work;
  deterministic acceptance rules.
- **Keep MCP-first with lazy disclosure** (ADR 0038/0039) for capabilities
  **beyond the six default tools**; do not adopt Pi's no-MCP stance. The minimalism
  is in the *default* surface, not a ban — and code search (`grep`/`glob`) is part
  of that default surface, not an MCP round-trip.
- **No sub-agent-via-bash-spawn**; use restricted-registry delegate agents.
- **No sibling runtime**; Pi-mode is a channel/app on the one spine — same
  authority boundary, same trace, same memory.
- No full-screen or alternate-screen TUI rewrite to solve Esc; reuse the v0.55
  scrollback-native terminal surface and add only the M9.28 interrupt-capable
  input substrate once the proof harness passes.
- **No long-term memory pollution.** Coding tool I/O (reads, searches, diffs, shell
  output) stays in the session trace; it is **not** auto-promoted to markdown-first
  long-term memory unless the operator explicitly saves it — coding context lives in
  the session, as in peer CLIs.
- **No parallel approval-memory store.** "Always allow this command" reuses the
  existing `Resources.Grants` remembered-grant lifecycle, not a second allowlist
  subsystem.

## Definition Of Done
v0.57 is done when:
- a Pi-mode coding surface runs as a channel/app on the `tui` channel, exposing
  exactly **six default tools** — read-only/sensitive `read`/`grep`/`glob` plus
  effectful `write`/`edit`/`bash` — each resolving through `Actions.Registry` and
  executing via `Actions.Runner.run/3` under Security Central;
- a named "local-coding operator" trust tier (ADR 0056 lineage, running at ADR
  0009 **Level 1**, ADR 0068) plus coder-facing approval modes
  (`default`/`accept-edits`/`plan`/`tier`) and `Resources.Grants`-backed per-repo
  "always allow" make the gate present-but-cheap **only** for a single trusted
  operator in the main session on `tui` (and enable raw-shell `bash` only there)
  via a confirmation-cost seam that **preserves the decision/trace/audit and drops
  only the prompt**, grant no authority, support list/revoke/audit/expiry, and
  provably cannot be reached by channel-originated, scheduled, or generated-code
  sessions, and are never the default;
- a coding turn runs under a **supervised Task** (the async turn-execution
  boundary), the live region streams **assistant text token-by-token** and renders
  diffs via the **net-new v0.57** stream-event contract and live-region substrate
  over the v0.55 static split payload (ADR 0067/0029/0030) with no `surface_payload`
  leaking into model context, and the M9.28 interrupt-capable TUI input driver
  makes **Esc cancel** a running turn for real (registered `StreamResponse.cancel`
  callback + bounded registry task shutdown, `turn_cancelled`, partial-turn trace)
  with a queued correction available, without literal `^[` scrollback or model
  routing of `/exit`/`/quit`;
- the coding prompt **+ six tool definitions combined** are under the token budget,
  context gathering uses chunked reads + artifacts (not whole-file ingestion), the
  default turn exposes only the six tools, the coding slash set
  (`/help`/`/mode`/`/model`/`/clear`/`/init`/`/diff`/`/compact`) is non-routable —
  `/init` action-backed, `/mode`/`/model`/`/clear`/`/compact` ungated session ops —
  entering Pi-mode pins the working repo to `coding.workspace.cwd_jail` (symlink + normalized-
  path checked) at session start, and a mid-session model switch preserves session
  history via req_llm without touching the authority path;
- acceptance for effectful/generated-code work is deterministic (evidence +
  confirmation), with no model-confidence completion and no bash-spawned sub-agent;
  the ordinary interactive turn ends when the model stops calling tools; coding tool
  I/O stays session-scoped and is not auto-promoted to long-term memory;
- `:v057` evals + `release.v057` + the full release gate pass across M0-M9;
- ADR 0068, the minimalism-budget coding policy, roadmap, vision, and
  agent-context-map agree, and `pi-integration-rethink.md` items 3–5 are delivered.
