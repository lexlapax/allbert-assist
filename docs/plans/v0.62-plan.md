# Allbert v0.62 Packaging & Entry Points

Request flow: `docs/plans/v0.62-request-flow.md`
ADR: `docs/adr/0076-packaging-distribution-and-unified-cli.md` (packaging,
distribution, and the unified `allbert` CLI, accepted here);
`docs/adr/0070-tui-operator-console-and-read-only-operator-actions.md`
(mix-free TUI operator console convergence, completed here);
`docs/adr/0067-tui-terminal-channel.md` (the terminal surface the console runs
on); `docs/adr/0073-cross-surface-contract.md` (the one runtime/action/settings
spine every entry point routes through); `docs/adr/0006-security-central.md`
(packaging changes how Allbert is installed and invoked, not what any surface may
do).

Status: planned — **deepened by the 2026-07-05 implementation-readiness pass**
(four-lens review: plan-depth/codebase grounding, external research, adversarial,
zoom-out; all findings folded in below; four operator decisions signed
2026-07-05 — see Locked Decisions 5/8/12/14). Pre-1.0 product capability
release 2 of 3; followed by v0.63 onboarding/profiles and the v0.64 product RC.

Source: `docs/plans/roadmap.md` `## v0.62: Packaging & Entry Points`; moved
before guided onboarding in the post-v0.58 product-readiness review so the
onboarding flow is built on the final binary, CLI, daemon, and secret-vault
model instead of on soon-to-be-replaced `mix` entry points.

## Purpose

Make Allbert installable and invokable as a product, not a checkout. Ship a
release-built `allbert` binary with no Elixir/OTP toolchain required on the
user's machine, a Homebrew + curl install path, a unified grouped CLI dispatcher
that separates operator commands from developer/CI commands, background-daemon
management with a visible health check, completion of the ADR 0070 mix-free TUI
console convergence, and OS secret-vault credential handling. This release
**implements** the entry-point / grouped-CLI UX **designed in v0.60 (ADR 0077,
v0.60 M5; concrete artifact: `docs/design/entry-point-cli-ux.md`)** — the
command taxonomy is a v0.60 design input, not invented or "settled" blind here.
The First-Model-Path integration is its own milestone (M4), not a packaging
footnote. v0.63 onboarding then teaches the implemented entry points.

## Expected Inputs

- v0.39 through v0.61b implemented and accepted (v0.61b released as 0.61.1).
- The v0.60 Product Experience Design accepted: the entry-point / grouped-CLI UX
  design (ADR 0077, v0.60 M5, `docs/design/entry-point-cli-ux.md` — taxonomy
  table, grouped-help model, **bare-`allbert` first-run dispatcher, first-run
  detection table, first-model-state enum**, and the Mix-to-`allbert` mapping)
  that this release implements, and the First-Model-Path decision (ADR 0078,
  v0.60 M3, **Accepted**: assisted-local default + BYOK fallback) that governs
  how a freshly installed Allbert reaches its first usable model.
- ADR 0076 ready for acceptance (amended by this readiness pass: Bakeware
  struck, Distribution Trust section added, CLI process model recorded);
  ADR 0070 partial convergence in place from v0.55.1 / v0.58.
- The current Allbert Home layout and the current flat `mix allbert.*` task
  inventory (**46 core + 9 plugin task modules = 55** as of this readiness pass;
  the "53 at the v0.58 closeout review" figure is superseded).
- The runtime/action/settings spine (ADR 0073), Security Central (ADR 0006), and
  the TUI terminal channel (ADR 0067).
- v0.61/v0.61b's web shell/landing/brand work, so packaged `allbert serve` has a
  professional web surface to expose.
- The v0.59 Allbert Home export/import substrate and the ADR 0046 settings
  `schema_version` version contract: the packaged-install Home-layout paths and
  the Settings-Central secret-vault *references* added here are additive under
  ADR 0046. The plaintext/encrypted-credentials -> OS-secret-vault move is a
  **separate operator migration outside ADR 0046's scope** (ADR 0046 Non-Goals);
  v0.64 M6 re-validates portability on the resulting packaged layout.
- The existing credential substrate this release extends rather than invents:
  `Settings.Secrets` (AES-256-GCM `secrets.yml.enc`), `Settings.KeyCustody`
  (decrypt-once custody with redacting Inspect), and the schema `secret://`
  reference types — see Current Code State.

## Operator-Visible Win

A technical prosumer installs Allbert from a packaged binary with no Elixir/OTP
on their machine, runs `allbert serve` to start the runtime + web workspace +
channels as a managed background process, watches a health check go green, drives
day-to-day operation through `allbert <group> <command>` and the mix-free TUI
console without touching `mix`, and stores provider credentials in the OS
keychain/secret service instead of a settings file.

## Planned Interfaces

- A release-built, ERTS-bundled `allbert` binary (one artifact per target
  triple; see the artifact matrix in Locked Decisions).
- A Homebrew formula/tap and a curl install script, with checksum verification
  and a manifest-driven uninstall.
- A unified `allbert <group> <command>` dispatcher:
  `ask | chat | tui | serve | admin <area> | gen`, with coherent grouped
  `--help`, **plus the bare-`allbert` first-run/resume dispatcher** from the
  v0.60 design (first-run detection + first-model-state consumption — M3).
- An operator/dev command split: operator commands on the binary; developer and
  CI commands remain `mix`-only in a checkout.
- `allbert serve`, service install/uninstall for launchd/systemd (Tier 1),
  and a visible runtime/web/channels health check.
- First-Model-Path integration (M4): Ollama detect → guided install (official
  channels, behind explicit confirmation) → curated model pull → hardware-floor
  degrade to BYOK.
- Completed ADR 0070 TUI console coverage for day-to-day admin inspection.
- OS secret-vault credential storage on the **three-tier backend** (Locked
  Decision 12): OS vault where available, documented encrypted-store fallback,
  env injection for automation.
- `:v062` eval rows, a deterministic `mix allbert.test release.v062` gate, and a
  separate **artifact smoke harness** (the mix gate cannot execute the packaged
  binary — see M8).

## Platform Support Tiers

Cross-platform packaging is a deliberate cost multiplier — a binary, a background
service, and a different OS secret vault per platform — so v0.62 sets **explicit
support tiers** rather than implying uniform support:

- **Tier 1 — macOS and Linux.** Fully supported and **freeze-blocking** for v1.0.
  The `allbert` binary, the Homebrew + curl install, the `launchd` / `systemd`
  daemon, and the macOS Keychain / Linux Secret Service vaults (on the
  three-tier fallback model) must all work and are required by the v1.0
  acceptance matrix.
- **Tier 2 — Windows.** Supported via **WSL2** (a Linux target). Native Windows
  packaging, a Scheduled-Task daemon, and Windows Credential Manager are
  **best-effort / beta and explicitly NOT v1.0 freeze-blocking** unless a later
  decision promotes them. The v1.0 acceptance-matrix Windows criterion is
  satisfied by WSL2, not native Windows.

This tiering is an explicit scope decision, not an assumption: Tier 1 is committed
v0.62 work; Tier 2 native-Windows support is out of the v1.0 freeze bar.

## Current Code State (the ground truth v0.62 changes)

Recorded by the 2026-07-05 readiness pass; every M0–M8 Implementation block
builds on or fixes something in this list.

1. **No packaging exists.** No `releases:`/`rel/`/escript in any mix.exs; no
   install script, tap, or CI artifact pipeline.
2. **Umbrella shape.** Two apps (`allbert_assist`, `allbert_assist_web`) plus
   **13 source-tree plugins compiled into the core app** via `elixirc_paths`
   (`apps/allbert_assist/mix.exs:37-55`) and a vendored path dep
   `{:memento, path: "../../vendor/memento"}`. Plugin *code* is compile-time;
   plugin *registration* is runtime manifest discovery (fact 4).
3. **Native deps** (each a packaging risk): `exqlite` 0.36 (SQLite NIF,
   elixir_make + cc_precompiler, ships precompiled artifacts), `erlexec` 2.3.4
   (**rebar3-built C++ port executable**), `muontrap` 1.8.0 (elixir_make C
   **port executable** in priv, used by the Signal channel daemon). `lazy_html`
   is `only: :test` — correctly excluded from a release. esbuild/tailwind are
   `runtime: Mix.env() == :dev` — build-time only.
4. **Boot-time checkout-path defects** (P0s for M1; verified file:line):
   - `apps/allbert_assist/lib/allbert_assist/database.ex:15-18` —
     `@stocksage_migrations Path.expand("../../../../plugins/stocksage/priv/repo/migrations", __DIR__)`
     freezes the **build machine's checkout path** into the artifact at compile
     time; `migration_paths/1` then silently filters missing dirs, so a
     packaged install gets a DB with **no stocksage tables and no error**.
   - `apps/allbert_assist/lib/allbert_assist/settings/provider_catalog.ex:10` —
     `@catalog_path Path.expand(...__DIR__)`, same compile-time-path class;
     `configure_jido_model_aliases!()` runs at boot.
   - `apps/allbert_assist/lib/allbert_assist/plugin/discovery.ex:257-264` —
     manifest discovery **walks up from `File.cwd!()`** looking for `plugins/`;
     `plugin/validator.ex:521` expands `plugins/<id>` against `File.cwd!()`.
     A packaged binary launched from `~` or by launchd (cwd `/`) registers
     **zero plugins** — no Telegram/Email/Signal/TUI channels, no stocksage.
   - The good pattern to copy: `memory/active_memory.ex:440-447` resolves via
     `:code.priv_dir` with a cwd fallback.
5. **Prod config debt** (M1 work items): `config/runtime.exs:80` — endpoint
   `server: true` exists **only as a comment** (a release boots but never
   listens; no `PHX_SERVER` handling); prod raises without
   `ALLBERT_HOME`/`DATABASE_PATH` and `SECRET_KEY_BASE` (a daemon inherits no
   shell env, and the raise-path re-implements env reading instead of using
   `Paths.home/0`'s `~/.allbert` fallback); `config/prod.exs` is untouched
   phx.new boilerplate (`url: [host: "example.com"]`, compile-time `force_ssl`
   + HSTS that would redirect LAN access to a nonexistent HTTPS endpoint,
   `cache_static_manifest` that crashes boot unless release assembly runs
   `assets.deploy`).
6. **Release-unsafe environment detection.** `settings/secrets.ex:480-482`
   `production?()` probes `Mix.env()` — Mix is absent in an OTP release, so the
   check returns false and the `ALLBERT_SETTINGS_MASTER_KEY` requirement
   **silently degrades** to the dev-style `.settings_key` file. Same Mix-probe
   class at `database.ex:144` and `plugins/allbert.browser/.../driver.ex:19`.
7. **Task inventory.** 46 task modules in `apps/allbert_assist/lib/mix/tasks/`
   + 9 in plugins = **55** (not 53). **41 of 46 use `Mix.shell`/`Mix.Task.run`**
   — the binary dispatcher cannot invoke task modules; each operator command
   must be re-fronted onto actions/report modules. ~18 tasks fit no group in
   the v0.60 mapping (see the M3 disposition table requirement), including the
   operator-facing `home.export`/`home.import`.
8. **Every task boots a full second runtime in-process**
   (`Mix.Task.run("app.start")`), and boot migrations auto-run **silently**
   when `RELEASE_NAME` is set (`application.ex:17`,
   `database.ex:131-137`, `log: false`) — the raw material of the CLI
   process-model decision (Locked Decision 5) and the upgrade story (M2/M5).
9. **Dev/packaged collision defaults.** Dev defaults to
   `~/.allbert/db/allbert.sqlite3` (`config/dev.exs:14-24`) — the **same** path
   a packaged install would use via `Paths.db_path` — and both default to port
   4000. Two BEAM VMs on one SQLite file is the known v0.53 failure mode.
10. **Credential substrate already reference-based.** Settings schema secret-ref
    types (`providers.*.api_key_ref`, channel `secret://` refs,
    `:secret_ref_or_nil`); `AllbertAssist.Settings.Secrets` (AES-256-GCM
    `secrets.yml.enc` + `.settings_key` under `Store.root()`);
    `Settings.KeyCustody` (decrypt-once closure-held values, redacting
    `Inspect`); runtime resolution profile `api_key_ref` → custody
    (`settings/model_runtime.ex:117`). **Plus a parallel env bypass:**
    `config/runtime.exs:15-19` reads `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/
    `OPENROUTER_API_KEY`/`GOOGLE_API_KEY` into `:req_llm` **once at boot**.
11. **TUI is a source-tree plugin** (`plugins/allbert.tui`, registered via
    discovery fact 4 — M6's fate is coupled to the M0/M1 plugin answer). Its
    input driver uses `:shell.start_interactive({:noshell, :raw})`
    (`input_driver.ex:260-263`) — raw-mode behavior through a release launcher
    is an untested spike risk. Current slash allowlist: `/status
    /confirmations /events /channels /intents /models /settings get` (+
    coding-session, `/help`). Registered `Operator*` internal reads:
    Status/Confirmations/Events/Channels/SettingGet; `LocalRuntimeDoctor` and
    `StartLocalRuntime` already exist as registered internal actions
    (`actions/registry.ex:349-350`) — M4 builds on them.
12. **No health surface.** No `/health` route in `router.ex`; the obvious
    sources for M5's check exist (`OperatorStatus` action, `Channels.Supervisor`
    child specs).
13. **cwd-derived security-scope defaults.** `intent_agent.ex:2465-2468` (and
    the execution policy/command-spec/sandbox call sites) build
    `Scope.directory_subtree(File.cwd!())` fallbacks — under a daemon with cwd
    `/` the default shell-exec scope silently widens; M5 pins the service
    `WorkingDirectory` and audits these.

## Research Grounding (surveyed 2026-07-05; sources in the readiness-pass record)

- **Bakeware is dead** (repo archived 2024-09-18, README points at Burrito).
  The spike is a **two-way** choice: Burrito 1.5.x vs a hand-wrapped
  `mix release` tarball + thin launcher.
- **Burrito 1.5.x realities:** build host needs **Zig 0.15.2 pinned** (+xz/7z
  for Windows targets); it fetches "universal machine" OTP/ERTS tarballs from a
  **project-run third-party CDN** at build time (OTP 29.0.1 confirmed served —
  a supply-chain decision to accept or replace with own-CI ERTS); the binary
  **self-extracts** the release to a per-OS cache dir on first run (upgrade
  residue + uninstall must call `maintenance uninstall`); and its cross-build
  NIF recompilation only detects `elixir_make` deps and forces
  `zig cc -shared` — **rebar3-built erlexec is invisible to it and
  muontrap/erlexec are port executables, not shared objects**, so
  cross-compiling this dep tree with Burrito is not viable.
- **Therefore: native per-target CI builds** on matching runners regardless of
  the wrapper choice (`mix release` cannot cross-compile either). GitHub
  Actions runners exist for macos-arm64, macos-x64, linux-x64 (ubuntu-22.04
  for the old-glibc floor), linux-arm64.
- **exqlite ships precompiled NIFs** (elixir_make precompiler + cc_precompiler)
  — helps native builds; does not make cross-builds safe.
- **macOS distribution nuance:** curl/scp do **not** set the quarantine
  attribute and Homebrew manages it — so the two v0.62 install paths run
  unsigned binaries without Gatekeeper prompts; browser-downloaded artifacts DO
  get quarantined and post-Sequoia there is no ctrl-click override. Bare
  Mach-O binaries can be signed + notarized (hardened runtime, `notarytool`)
  but **cannot be stapled**; self-extraction vs hardened-runtime library
  validation is a known pain class to test whenever signing lands.
- **Homebrew mechanics:** third-party tap (`homebrew-tap` repo) + formula (or
  cask — casks are Homebrew's stated home for binary-only packages and now work
  on Linux) with per-platform `url`+`sha256` pointing at GitHub release
  artifacts. Building Elixir from source in a formula is a non-starter.
  Precedent: Next LS (Burrito binaries + self-hosted tap); Livebook Desktop
  (hand-rolled per-OS bundles).
- **install.sh conventions:** versioned immutable artifact URLs; SHA256SUMS
  published with the release and verified inside the script; script wrapped in
  `main()` and download-then-inspect friendly; **uninstall manifest** of
  installed paths. Cosign/sigstore-signed checksums are the emerging standard
  and are cheap in GitHub Actions (recorded as a candidate, not required).
- **No maintained Elixir/Erlang OS-keychain library exists (2026).** Realistic
  mechanisms: shell out to `security` (macOS) / `secret-tool` (Linux
  libsecret), or reuse the docker-credential-helper protocol/binaries. Linux
  Secret Service needs a D-Bus session + keyring daemon — **typically absent on
  headless servers**, which is a core daemon deployment. Precedent for the
  fallback tier: gh CLI (keyring → documented file fallback → env), docker.
- **Service management:** `launchctl bootstrap gui/$UID` (legacy
  `launchctl load` is deprecated); `systemd --user` units +
  `loginctl enable-linger` (user units keep the daemon in the user session
  where Secret Service lives — this materially eases the vault story); logs to
  `~/Library/Logs/<name>/` / `journalctl --user -u allbert`.
- **Ollama:** official channels are `brew install ollama` (maintained core
  formula) on macOS and the official install script/tarball on Linux;
  MIT-licensed; never re-host artifacts. Hardware floor guidance: ~8 GB RAM
  floor for a 3–4B curated default; 7–8B models want 16 GB.
- **ERTS bundling gotchas:** build Linux artifacts against an old-glibc floor
  (ubuntu-22.04) or ship musl-dynamic; **fully static linking is off the table**
  for a NIF-loading app; OTP's crypto NIF links libcrypto dynamically by
  default — a Homebrew-built dev ERTS breaks on machines without Homebrew
  OpenSSL, so the shipped ERTS must have portable crypto linkage (verify with
  `otool -L`/`ldd` in the gate); pin the ERTS/OTP version as a CI input.

## Locked Decisions

Rows 5, 8, 12, 14 were **signed by the operator on 2026-07-05** during the
readiness pass. Remaining rows are proposed here and ratified at request-flow
**S1** before build (the v0.61b S2 pattern); the spike-dependent rows (2, 3)
are ratified at **S2** with the spike report.

| # | Decision | Choice | Why not the alternative |
|---|---|---|---|
| 1 | Spike candidates | **Burrito 1.5.x vs hand-wrapped `mix release` tarball + launcher** (Bakeware struck — archived upstream). | A 3-way spike against a dead tool wastes the time-box. |
| 2 | Build strategy | **Native per-target CI builds** on matching runners; no cross-compilation. Ratified with the spike report (S2). | Burrito cross-builds mis-handle rebar3 deps and port executables (erlexec, muontrap) in this tree. |
| 3 | Artifact matrix | Proposed: **macos-arm64, linux-x64 (glibc ≥ 2.35), linux-arm64**; macos-x64 and linux-musl explicit yes/no at S2. | Every added triple is a CI runner + test lane forever. |
| 4 | ERTS provenance | Decide at S2: Burrito's CDN builds vs own-CI-built OTP; either way the ERTS/OTP version is **pinned** and its crypto linkage portable. | An unpinned third-party CDN fetch is an unrecorded supply-chain dependency. |
| 5 | **CLI process model (SIGNED 2026-07-05)** | **Attach-first**: `allbert` commands connect to the running daemon (local socket/distribution RPC); an embedded runtime boots only when no daemon runs, under single-writer discipline. | Embedded-always means a second BEAM on one SQLite file + concurrent boot migrations on every command — the known v0.53 failure mode. |
| 6 | Bare-`allbert` first-run | **v0.62 M3 owns** the bare-`allbert` dispatcher, the first-run detection table, and first-model-state consumption from `entry-point-cli-ux.md`; **v0.63 owns wizard semantics**. Onboarding-state storage shape decided in M3, recorded for v0.63. | Leaving it unowned drops the v0.60 design's first-invocation UX; v0.63's wizard needs the hook. |
| 7 | `allbert chat` fallback | Ship the temporary terminal chat fallback only if the TUI console (M6) misses the window — the design doc's conditional; decide at S4. | Unconditionally shipping a throwaway surface duplicates the TUI. |
| 8 | **Ollama execution authority (SIGNED 2026-07-05)** | **Own milestone (M4); Allbert executes** the official install path + model pull **behind explicit operator confirmations with trace/egress recording** (AGENTS.md external-installer rule), hardware-floor check, decline→BYOK. | Instructions-only weakens ADR 0078's one-click path; leaving it inside M1 hides its UX/egress/failure surface. |
| 9 | Curated default model | A ~3–4B model with an **8 GB RAM floor** (16 GB gates anything larger); the specific model is selected in M4 and ratified at S4 (ADR 0078 delegates selection to v0.62). | 7–8B defaults exclude the 8 GB machines the prosumer tier actually has. |
| 10 | Install layout | Proposed: keep **`~/.allbert`** as Allbert Home (already the `Paths.home/0` fallback); binary + release payload live in the package manager's domain (brew prefix / opt) plus the Burrito extraction cache if Burrito wins. No XDG migration in v0.62. Ratify at S3. | An XDG/Library migration now churns v0.59 export/import and v0.63 onboarding copy for zero operator value. |
| 11 | Daemon service scope | **Per-user services** (`launchctl bootstrap gui/$UID`; `systemd --user` + offer `enable-linger`), `WorkingDirectory` pinned to Allbert Home; never system units in v0.62. | System units run outside the user session where the vault lives and widen scope semantics. |
| 12 | **Vault backend (SIGNED 2026-07-05)** | **Three-tier**: (1) OS vault via shell-out (`security` / `secret-tool`) where available, (2) documented fallback to the existing encrypted `Settings.Secrets` store (headless), (3) env injection for automation. Env-key bypass (`:req_llm` boot env) is folded into tier 3 and documented. | Strict-vault fails closed on headless daemons; a custom native helper is a fifth per-target artifact. |
| 13 | Daemon secrets bootstrap | M5 ships the interim story: install generates `SECRET_KEY_BASE` (persisted mode-600 under Allbert Home) and wires an `EnvironmentFile`/plist-env; M7's vault then owns provider keys. Master-key handling follows tier 12.2 until vault tier 1 covers it. Mix-probe `production?()` is replaced with release-safe detection in M1. | Reordering the vault before the daemon blocks M5 on the hardest milestone; shipping a daemon that cannot boot is not an option. |
| 14 | **Signing posture (SIGNED 2026-07-05)** | **v0.62 ships unsigned via brew/curl only** (both quarantine-free), publishes SHA256 checksums; signing + notarization is a **named v0.64 RC work item** (Apple Developer account decision included). The spike notes the self-extraction × hardened-runtime interaction for that future test. | Signing now costs an account + CI secrets + the unstapleable-binary caveat for paths v0.62 doesn't ship (browser downloads). |
| 15 | Upgrade/coexistence scope | v0.62 ships: **backup-before-migrate** (DB copy before boot migrations on version change), explicit **migration logging** (drop `log: false`), and a **coexistence policy** (packaged daemon defaults to port 4000; the dev checkout must set `ALLBERT_HOME`/port — documented, plus a same-DB-lock guard). Automated rollback is **deferred to v0.64** and recorded in Non-Goals. | Silent irreversible migrations on `brew upgrade` with no backup is data loss waiting; full rollback machinery is RC scope. |
| 16 | Version/tag | Releases as **0.62.0**; git tag held for the operator after the final validation pass. | The v0.61/v0.61b lesson: manual validation generates feedback. |

## Cross-Cutting Invariants (apply to every milestone)

- **One spine:** every dispatcher command, daemon surface, TUI read, and vault
  operation routes through the registered-action spine (Runner →
  PermissionGate → context) or is a pure read — never a direct store write.
  The eval rows check this by **inventory**, not spot-check.
- **No new authority:** no new permission atom, capability class,
  confirmation-floor change, or route. The four **new network touches**
  (install-script artifact fetch, brew tap fetch, Ollama installer fetch,
  model pull) are enumerated in ADR 0076's Distribution Trust section; the
  latter two run only behind explicit operator confirmation with trace
  recording (Locked Decision 8).
- **No telemetry / no phone-home / no auto-update check** in the binary
  (Distribution Trust).
- **No secret in logs, traces, release evidence, `ps` output, or recommended
  shell history** — the v0.52 `.env` handling lesson extended to the vault.
- **No internal rename**; packaging relocates artifacts, never renames
  modules/atoms/keys.
- **Docs-in-triad**: implementation details land in this plan / the request
  flow / ADR 0076 — no sidecar planning docs.

## Milestones

Lane summary: **M0** Packaging Feasibility Spike → **M0.1** v0.61b Carried-In
Web Tickets → **M1** Packaged Binary & Release Correctness → **M2** Install
Path & Distribution Trust → **M3** Unified CLI Dispatcher + First-Run
Detection → **M4** First-Model-Path Integration → **M5** Daemon & `allbert
serve` → **M6** ADR 0070 TUI Console Completion → **M7** OS Secret-Vault
(three-tier) → **M8** Evals + `release.v062` Gate + Artifact Smoke + Closeout.

Workstreams and barriers (AGENTS.md): **M0 is a hard serial barrier** — no
M1–M8 build work before the spike report is ratified (S2). M0.1 is independent
and may run any time. After M1, two parallel workstreams exist: the
distribution stream (M2) and the entry-point stream (M3→M4); M5 needs M1+M3
(attach model), M6 needs M3, M7 needs M5's bootstrap story. M8 is the serial
closeout. Sign-off gates: S1 (Locked Decisions), S2 (spike report), S3
(install-layout & trust spec, gates M2), S4 (CLI taxonomy delta + curated
model, gates M3/M4), S5 (vault UX + platform fallback matrix, gates M7).

### M0: Packaging Feasibility Spike
**Status:** planned

Run a time-boxed feasibility spike **before committing M1-M8**, because this
codebase has **no packaging today** and the riskiest unknowns are front-loaded.
The spike must prove, on a single Tier-1 OS, that a release-built, ERTS-bundled
`allbert` binary:

1. boots the supervision tree with **no Elixir/OTP toolchain present**;
2. opens the **SQLite database** through the `exqlite` NIF from the packaged
   artifact;
3. **executes the `erlexec` and `muontrap` port binaries** from the packaged
   artifact (the readiness pass identified these — not exqlite — as the
   highest-risk native unknowns: rebar3-built and port-executable classes);
4. serves the **compiled web assets** (esbuild/tailwind `assets.deploy` output)
   over `allbert serve` — which first requires the `server: true`/`PHX_SERVER`
   fix (Current Code State 5), so the spike patches it locally;
5. **registers and starts at least one source-tree plugin** from the packaged
   layout — i.e. manifest + `priv/` + `skills/` discovery works outside a
   checkout (Current Code State 4), not merely "the BEAM code is present";
6. proves the **TUI raw-mode input** (`{:noshell, :raw}`) works through the
   release launcher (Current Code State 11);
7. prototypes the **attach transport** for Locked Decision 5 (a running
   packaged daemon accepts a second `allbert` invocation via local
   socket/distribution without booting a second runtime); and
8. on Linux, boots on one non-Ubuntu distro (glibc-floor / musl-shim check).

**Explicit gate:** if the spike cannot prove all eight on a Tier-1 OS with the
chosen tool (Burrito 1.5.x or a hand-wrapped OTP release — Bakeware is
archived and struck), the M1 packaging mechanism is reconsidered before any
further M1-M8 work begins. The spike result **chooses** the packaging
mechanism and settles Locked Decisions 2/3/4; the operator ratifies the spike
report at **S2**.

- **Implementation:**
  - **Modules / Files:** a throwaway `rel/`/`releases:` config on a branch;
    local patches for `server: true` and the compile-time paths as needed to
    reach the proofs (the durable fixes land in M1); a spike-report section
    appended to this plan (M0 as-built record), not a sidecar doc.
  - **Inputs:** Research Grounding (Burrito realities, ERTS provenance,
    glibc floor); Current Code State 3-6, 11.
  - **Settings:** none.
  - **Tests:** none in-repo (the spike is evidence-producing, not gate-wired);
    its findings become M1 work items + the M8 artifact-smoke design.
  - **Outputs:** the spike report (mechanism choice, artifact matrix, ERTS
    provenance recommendation, failure notes), ratified at S2.
  - **Verify:** request-flow **S2**.
- **Acceptance:** all eight proofs pass on one Tier-1 OS, or the mechanism is
  re-chosen and the spike re-run; Locked Decisions 2/3/4 filled in; S2 signed.

### M0.1: v0.61b Carried-In Web Tickets (point milestone)
**Status:** planned — independent of the packaging lane; land any time before M8.

The v0.61b post-implementation audit's carried-in tickets, owned here so they
cannot drop silently (they previously had no owning milestone):

- Migrate `persist_approval_media_response` (`workspace_live.ex`) onto a
  registered action — the one remaining direct context write from a LiveView
  (direct `Conversations.get_thread` + `append_assistant_message`; recorded in
  the v0.61b plan's Security Posture notes).
- Remove the pre-existing dead-CSS orphans: `.operator-catalog-page`,
  `.workspace-utility-actions`, `.workspace-utility-head`,
  `.workspace-utility-link-disabled`, plus the `UtilityDrawer` test-only zombie
  chain (`structural.ex` component + `drawer_shell_*` pattern helpers +
  `[data-workspace-pattern="drawer-shell"]` motion rule; `:utility_drawer`
  never appears in a tree).
- The deferred a11y batch (v0.61b M9.2): light-mode accent `#7c6cf0` on white
  at 3.99:1; arrow-key/roving-tabindex keyboarding on `role="menu"` surfaces;
  live-toggled reduce-motion/high-contrast attributes reaching the sidebar.

- **Implementation:** `workspace_live.ex` + a new registered action
  (`:conversation_write`-class, exposure `:internal`) with registry exact-list
  updates; `app.css` + `structural.ex`/`patterns.ex` removals with test
  reconciliation; the a11y batch in `app.css`/components with computed-ratio
  and semantics assertions.
- **Acceptance:** all three tickets landed with tests; registry diff is exactly
  the one new internal action; full web suite green.

### M1: Packaged Binary & Release Correctness
**Status:** planned — pure packaging; First-Model-Path moved to M4.

Build an OTP release and wrap it per the spike's chosen mechanism as a
self-contained `allbert` binary, so the artifact carries its own runtime and no
Elixir/OTP toolchain is required on the user's machine. The binary boots the
same supervision tree the checkout boots. Packaging is a delivery change, not a
runtime authority change. M1 owns making the codebase **release-correct** —
every Current Code State 4/5/6 defect is a named work item here:

- **Release config:** `releases:` spec (both apps + included applications),
  `runtime_config_path`, release steps running `assets.npm` + `assets.deploy`
  before assembly; ERTS per Locked Decision 4.
- **Serve wiring:** `PHX_SERVER`/`server: true` handling; `config/prod.exs`
  boilerplate replaced (localhost-serving defaults, no `example.com`, no
  blanket `force_ssl`; a `/health`-excluded posture recorded for M5).
- **Path fixes:** `database.ex` stocksage-migrations and
  `provider_catalog.ex` catalog paths move to release-safe resolution
  (`:code.priv_dir` / artifact-relative, migrations relocated into a repo
  `:priv` or copied into the artifact) with a gate assertion that packaged
  `migration_paths` is non-empty per plugin; `plugin/discovery.ex` +
  `validator.ex` gain a packaged-layout root (release-root plugin dir +
  `ALLBERT_HOME/plugins`) replacing the cwd walk-up as the primary mechanism.
- **Release-safe env detection:** the Mix-probing `production?()` class
  (Current Code State 6) replaced with release-safe detection
  (`:init.get_argument`/release env or a compile-env), so master-key
  enforcement holds in the artifact.
- **Home defaults:** prod `runtime.exs` falls back to `Paths.home/0`
  (`~/.allbert`) instead of raising when `ALLBERT_HOME` is absent;
  `SECRET_KEY_BASE` generation moves to the install/first-boot path (Locked
  Decision 13) instead of a raise.
- **Packaged-plugin constraint recorded:** a packaged install can never gain
  new plugin *code* (compile-time `elixirc_paths`); Home-dir declarative
  entries and operator-confirmed dynamic drafts remain the runtime extension
  paths. This is a documented product fact ADR 0076 carries, and v0.63
  onboarding copy must respect it.

- **Implementation:**
  - **Modules / Files:** root `mix.exs` (`releases:`), `config/runtime.exs`,
    `config/prod.exs`, `apps/allbert_assist/lib/allbert_assist/database.ex`,
    `settings/provider_catalog.ex`, `plugin/discovery.ex`,
    `plugin/validator.ex`, `settings/secrets.ex` (+ the other Mix-probe
    sites), CI workflow for the per-target artifact matrix (Locked Decision 3).
  - **Inputs:** the ratified spike report; Research Grounding (glibc floor,
    OpenSSL linkage, ERTS pin).
  - **Settings:** none.
  - **Tests:** unit/proof tests for release-safe path resolution (packaged
    layout simulated via tmp dirs), migration-paths non-empty per plugin,
    release-safe `production?()` semantics; the artifact-level proofs live in
    M8's smoke harness.
  - **Outputs:** a CI-built artifact per target triple; a locally buildable
    `mix release`-based binary.
  - **Verify:** M8 artifact smoke boots the binary in a temp HOME with no
    toolchain and all M0 proofs re-run mechanically.
- **Acceptance:** the binary boots the full tree (plugins registered, web
  served, DB migrated with stocksage tables present) from a temp HOME on a
  Tier-1 OS with no toolchain; no compile-time checkout path survives (grepped
  + gate-asserted); master-key enforcement verified in release mode.

### M2: Install Path & Distribution Trust
**Status:** planned — gated on the S3 install-layout & trust spec sign-off.

Ship a Homebrew formula/tap (or cask per Research Grounding — decided at S3)
and a curl install script with an uninstall path. Both paths must verify the
release artifact (SHA256 against the release's published checksum file),
install only documented files (install manifest consumed by uninstall), and
leave Allbert Home/user data alone on uninstall unless the operator explicitly
asks for removal (`--purge` semantics documented). If Burrito wins the spike,
uninstall also handles the self-extraction cache (`maintenance uninstall`).
Upgrade runs backup-before-migrate per Locked Decision 15.

The **in-plan install-layout & distribution-trust spec** (authored during M2,
signed at S3, recorded as an M2 as-built section here) carries: the file map
per install path; Allbert Home layout deltas (Locked Decision 10); artifact
verification scheme; the signing posture (Locked Decision 14); the enumerated
network-touch list; the no-telemetry statement; and tap/artifact-hosting
ownership (the request-flow's `https://.../install.sh` placeholder becomes a
real, operator-owned domain/repo decision at S3).

- **Implementation:**
  - **Modules / Files:** `homebrew-tap` repo (formula/cask), `install.sh` +
    `uninstall` manifest logic (in-repo under `scripts/` or the tap — decided
    at S3), CI release workflow publishing artifacts + SHA256SUMS (cosign
    candidate per Research Grounding), backup-before-migrate hook in the boot
    migration path (with logging — Locked Decision 15).
  - **Inputs:** S3-signed spec; Locked Decisions 10/14/15.
  - **Settings:** none.
  - **Tests:** install-script lint/dry-run harness; checksum-verification
    negative test; uninstall-manifest round-trip; backup-before-migrate unit
    proof. Per-OS live installs are S-steps.
  - **Outputs:** `brew install <tap>/allbert` and
    `curl ... | sh` both working on fresh Tier-1 machines.
  - **Verify:** request-flow per-OS install/uninstall/upgrade matrices.
- **Acceptance:** both paths install, verify, uninstall (Home preserved), and
  upgrade (backup taken, migrations logged) on both Tier-1 OSes; the trust
  spec is signed and reflected in ADR 0076.

### M3: Unified CLI Dispatcher + First-Run Detection
**Status:** planned — gated on the S4 taxonomy-delta ratification.

A single `allbert <group> <command>` surface subsumes the flat Mix-task sprawl
behind grouped help, **implementing `docs/design/entry-point-cli-ux.md`**:
`ask | chat | tui | serve | admin <area> | gen` **plus the bare-`allbert`
first-run/resume dispatcher** (Locked Decision 6) — the 6-row first-run
detection table, the 7-state first-model-state enum (consumed for real in M4),
and routing into the v0.63 wizard target (a stub/pointer until v0.63 builds
it). Operator commands move onto the binary; developer and CI commands stay
`mix`-only. Each command routes through the same runtime/action/settings spine
and remains subject to Security Central, confirmations, and surface policy.

**Process model (Locked Decision 5, signed):** attach-first. The dispatcher
detects a running daemon (pidfile/socket under Allbert Home) and executes
against it over the local attach transport proven in M0; when no daemon runs,
it boots an embedded runtime **under single-writer discipline** (same
boot-migration guard as the daemon; concurrent second-writer attempts fail
with guidance rather than corrupting the SQLite file).

**Disposition table:** M3's build recon opens with a **per-task disposition
table for all 55 task modules** — each mapped to a dispatcher command (with its
backing action/read), kept `mix`-only (dev/CI), or explicitly retired. The
v0.60 mapping's ~18 unmapped tasks (incl. operator-facing
`home.export`/`home.import` → `admin home export|import`) are resolved here
and ratified at S4. Because 41/46 core tasks are Mix-coupled, operator
commands are **re-fronted** onto the actions/report modules the tasks wrap —
task modules are not invoked by the binary.

- **Implementation:**
  - **Modules / Files:** a new CLI entry module set under
    `apps/allbert_assist/lib/allbert_assist/cli/` (dispatcher, group routers,
    help renderer, attach client, first-run detector), release `:start`
    integration; the disposition table as an M3 section in this plan.
  - **Inputs:** `docs/design/entry-point-cli-ux.md` (taxonomy/help/first-run);
    Locked Decisions 5/6/7; Current Code State 7/8.
  - **Settings:** onboarding-state storage shape decided here (Locked
    Decision 6) — additive under ADR 0046 if a settings key, else a Home-dir
    state file; recorded for v0.63.
  - **Tests:** dispatcher routing tests (every command → named action/read —
    the inventory eval row), attach-vs-embedded selection tests, single-writer
    guard test, first-run detection table tests (6 rows), grouped-help
    snapshot; `mix`-only commands asserted absent from the binary surface.
  - **Outputs:** the full operator surface on the binary; the Mix→`allbert`
    mapping doc deliverable generated from the disposition table.
  - **Verify:** request-flow dispatcher S-steps + the M8
    `cli-command-inventory-spine-map-001` row.
- **Acceptance:** every disposition-table row implemented as mapped; bare
  `allbert` implements the design's state machine; attach + embedded modes
  both proven; no dispatcher command bypasses the spine (inventory-asserted).

### M4: First-Model-Path Integration (ADR 0078)
**Status:** planned — own milestone per the signed Locked Decision 8; gated on
S4 (curated model ratified).

The v0.60 First-Model-Path decision is **assisted-local default + BYOK
fallback** (managed-hosted rejected). v0.62 implements it: the first-run path
(M3's detector) **detects an existing Ollama install and, if absent, offers a
guided install through the official channels** (`brew install ollama` on
macOS; the official script/tarball on Linux — never re-hosted), **then manages
a one-click pull of the curated default model**. Allbert **executes** these
steps itself — behind **explicit operator confirmation per step, with
trace/egress recording** (the AGENTS.md external-installer rule made
first-class), building on the existing `LocalRuntimeDoctor` /
`StartLocalRuntime` registered actions rather than greenfield. The Ollama
runtime is a **managed external dependency, not bundled** (keeping the binary
light). If the machine is below the curated model's hardware floor (Locked
Decision 9: 8 GB RAM floor for the ~3–4B default) or the user declines, first
run degrades to the BYOK path (provider key through the M7 vault; until M7
lands, through the existing encrypted store).

**Contingency (ADR 0078's own degrade path):** if robust Ollama integration
proves too costly in the v0.62 window, ship BYOK-primary for 1.0 — recorded as
a deliberate, documented tradeoff at S4, not silently.

- **Implementation:**
  - **Modules / Files:** extend `LocalRuntimeDoctor`/`StartLocalRuntime` +
    new registered internal actions for guided install / model pull (each
    confirmation-gated, trace-recorded); first-model-state resolution feeding
    M3's enum; hardware-floor probe; CLI + (existing) web affordance surfaces.
  - **Inputs:** ADR 0078; Locked Decisions 8/9; M3's first-run detector.
  - **Settings:** none new (model profile selection uses existing keys).
  - **Tests:** state-enum resolution tests (all 7 states incl.
    `below_hardware_floor`); confirmation-gating negative tests (no install or
    pull without an approved confirmation, trace present); decline→BYOK path;
    floor-probe unit tests.
  - **Outputs:** a fresh install reaches a first usable local model with
    explicit consent at each network/execution step, or lands on BYOK.
  - **Verify:** `ollama-install-and-pull-confirmed-001` +
    `first-model-state-enum-001` rows; request-flow fresh-machine S-step.
- **Acceptance:** all 7 first-model states reachable and correct; every
  install/pull step confirmation-gated with trace; floor + decline degrade
  paths proven; curated model choice signed at S4.

### M5: Daemon Management & `allbert serve`
**Status:** planned

`allbert serve` starts the runtime, web workspace, and channels as a managed
process and installs/uninstalls an OS service when explicitly requested —
per-user services only (Locked Decision 11): `launchctl bootstrap gui/$UID`
plists on macOS, `systemd --user` units (+ offered `enable-linger`) on Linux,
`WorkingDirectory` pinned to Allbert Home (closing the cwd-scope widening in
Current Code State 13, with an audit of the `Scope.directory_subtree(cwd)`
call sites). Service env comes from a generated `EnvironmentFile`/plist env
carrying `ALLBERT_HOME` and the interim `SECRET_KEY_BASE` (Locked Decision
13). A health check covers runtime up, web reachable, and channels supervised
— surfaced both as `allbert admin status` (over the existing `OperatorStatus`
action) and a minimal HTTP `/health` route (readiness for the S-step "watch it
go green"). Service install grants no authority beyond the surfaces already
available from a checkout.

- **Implementation:**
  - **Modules / Files:** service templates + install/uninstall commands under
    the M3 CLI tree; `router.ex` `/health`; health aggregation over
    `OperatorStatus` + `Channels.Supervisor` child specs; single-writer
    boot-migration guard shared with M3; log-path conventions per OS.
  - **Inputs:** Research Grounding (launchd/systemd conventions); Locked
    Decisions 11/13/15.
  - **Settings:** none.
  - **Tests:** health aggregation unit tests; service-template rendering
    tests; migration-guard tests. Live service lifecycle is per-OS S-steps +
    the M8 smoke harness (daemonless `serve` mode).
  - **Outputs:** a managed background Allbert with visible health.
  - **Verify:** `serve-health-readonly-001` row; request-flow daemon
    crash/restart/log S-steps.
- **Acceptance:** install/start/health/stop/uninstall proven on both Tier-1
  OSes; daemon boots with no shell env; cwd-scope audit recorded; health check
  green from both surfaces.

### M6: ADR 0070 TUI Console Completion
**Status:** planned

Absorb the remaining day-to-day admin-inspection reads into registered
read-only internal actions so an operator does not need raw `mix` for normal
operation. The reads follow the ADR 0070 pattern: `exposure: :internal`,
routed through `Actions.Runner.run/3`, slash-allowlisted, excluded from
intent-router candidates, redacted/truncated, and mutation-free.

**The remaining set is enumerated** (Current Code State 11; ratified at S4
with the disposition table): new slash surfaces for the already-registered
reads with none (`ListJobs`, `ShowObjective`, memory reads, `TraceSummary`,
`RegistryHealth`), plus registered reads + slashes for the
operator-inspection-shaped tasks currently outside the action boundary
(`threads`, `workspace`, `public_protocol`, `self_improvement`; `home
export/import` land as M3 `admin` commands; `mcp_server`/`acp_server` are
serve-adjacent and stay CLI). Anything not in the table is explicitly declared
outside "day-to-day" — nothing absorbed silently.

- **Implementation:**
  - **Modules / Files:** new `Operator*` action modules (registry
    `@internal_actions` exact-list updates), `slash_commands.ex` allowlist,
    TUI render paths; packaged-TUI dependency on M0 proof 6.
  - **Inputs:** ADR 0070; the M3 disposition table.
  - **Settings:** none.
  - **Tests:** per-read action tests (internal exposure, redaction,
    mutation-free), slash-allowlist exact list, intent-router exclusion
    (corpus), registry exact lists.
  - **Outputs:** mix-free day-to-day inspection from the packaged binary.
  - **Verify:** `tui-convergence-readonly-internal-001` (enumerated
    inventory) + `tui-convergence-not-intent-candidate-001` rows.
- **Acceptance:** the enumerated read set implemented; ADR 0070 marked
  converged; TUI works from the packaged artifact (M0 proof carried through
  M8 smoke).

### M7: OS Secret-Vault Credential Handling (three-tier)
**Status:** planned — gated on the S5 vault UX + platform-matrix sign-off.

Settings Central holds secret references; secret values live on the
**three-tier backend** (Locked Decision 12, signed): tier 1 the OS vault via
shell-out (`security` on macOS, `secret-tool` on Linux — no maintained Elixir
library exists; the docker-credential-helper protocol is the recorded
alternative if shell-out proves brittle), tier 2 the **existing encrypted
`Settings.Secrets` store** as the documented fallback where no vault is
reachable (headless Linux daemons), tier 3 env injection for automation (the
`:req_llm` boot-env bypass is documented as this tier and its keys surfaced in
`allbert admin` inspection as "env-provided"). M7 is a **backend swap behind
the existing substrate** — `Secrets`/`KeyCustody`/schema `secret://` refs stay
the interface (Current Code State 10). Existing plaintext/encrypted settings
credentials migrate only through an explicit, redacted operator step
(`allbert admin settings migrate-secrets`). No raw secret is logged, rendered,
included in release evidence, or written into shell history by the recommended
flow. Home export/import (v0.59) **excludes tier-1 vault values** — export
carries references + tier-2 material only, documented (the ADR 0046 seam).

- **Implementation:**
  - **Modules / Files:** a vault-backend behaviour behind
    `Settings.Secrets`/`KeyCustody` with `MacKeychain`/`LinuxSecretService`/
    `EncryptedFile` adapters + tier resolution (detection + documented
    override); the migration command; redaction sweeps.
  - **Inputs:** Locked Decisions 12/13; Research Grounding (vault mechanisms,
    headless reality, gh-CLI precedent); S5-signed vault UX + platform matrix
    (in-plan section: per-platform backend cells incl. the headless cell,
    key naming/scoping, failure modes).
  - **Settings:** no new Settings Central key planned; tier override via env
    (`ALLBERT_VAULT_BACKEND`) — ratified at S5.
  - **Tests:** backend behaviour contract tests (fake vault), tier-resolution
    tests (vault-absent → tier 2 with explicit notice, never silent), migration
    round-trip with redaction scan, export-excludes-vault-values proof; live
    Keychain/secret-tool round-trips are per-OS S-steps.
  - **Outputs:** credentials in the OS vault on desktops, an honest documented
    story on headless, one migration command.
  - **Verify:** `secret-vault-no-leak-001`, `vault-tier-resolution-explicit-001`,
    `secret-vault-migration-redacted-001` rows; per-OS vault rehearsal S-steps.
- **Acceptance:** all three tiers implemented and honestly surfaced; migration
  proven on both Tier-1 OSes; no-leak sweeps green; export/import seam
  documented.

### M8: Evals + `release.v062` Gate + Artifact Smoke + Closeout
**Status:** planned

Two-layer verification, because the mix gate is checkout-bound and **cannot
execute the packaged artifact**:

1. **`mix allbert.test release.v062`** (deterministic, checkout): migrate /
   format / compile-warnings-as-errors / credo-strict / dialyzer / the v062
   proof + sweep tests (eval rows below) / regression proofs / docs gate —
   asserting everything source-provable: release config present, no
   compile-time checkout paths (grep-proof), release-safe `production?()`,
   dispatcher inventory map, vault tier logic, migration-paths non-empty,
   `server:` wiring, registry exact lists.
2. **Artifact smoke harness** (CI, per target where runners allow): build the
   release, spawn the binary in a temp `ALLBERT_HOME`, assert boot, plugin
   registration count, `/health` green, an `allbert admin status` attach
   round-trip, stocksage tables present, no Mix modules in the image, and
   ERTS crypto-linkage portability (`otool -L`/`ldd`). Per-OS
   brew/curl/service/vault checks that CI cannot honestly reproduce are
   operator S-steps in the request flow.

Then close out: ADR 0076 accepted (with the Distribution Trust section) at the
final sign-off; ADR 0070 marked converged; docs deliverables (below) written;
version bump to **0.62.0**; CHANGELOG entry; roadmap/README/context-map
updates; tag **held for the operator** (Locked Decision 16).

- **Implementation:**
  - **Modules / Files:** `mix/tasks/allbert.test.ex` (`release.v062` lane),
    `eval_inventory.ex` (`:v062` milestone + rows below),
    `test/security/v062_sweep_eval_test.exs` + the v062 proof tests; the smoke
    harness as a CI workflow + `scripts/` runner (artifact-level, outside the
    mix gate by design — recorded in the lane's coverage notes so the gate's
    breadth is not overread).
  - **Verify:** the full lane green; the smoke harness green per artifact;
    request-flow final S-step.
- **Acceptance:** gate + smoke green; eval rows pass; ADRs
  accepted/converged; docs complete; version/CHANGELOG/tag-held recorded.

## Operator UX-Refinement Carryover — MOVED to v0.61b (historical record)

The eight operator UX-feedback items captured during v0.61 manual validation
were owned and released by the **v0.61b UX Refinement point release (0.61.1,
tagged 2026-07-05)**: `docs/plans/v0.61b-plan.md` (M0–M9.5), ADR 0080. That
move made v0.62 pure packaging scope and resolved the former duplicate-"M7"
carryover labeling. The v0.61b post-implementation audit's small web-cleanup
tickets are owned here as **M0.1** (previously they had no owning milestone).
v0.62 builds on the v0.61b-consolidated shell.

## Security / Eval Rows (`:v062`)

Registered in `eval_inventory.ex` with assert atoms and owner test modules
(the v0.61b pattern — names alone are not rows); the request-flow's earlier
10-name list is superseded by this set:

- `packaging-no-authority-change-001` — registry/permission/Settings-key/route
  diff vs v0.61.1 is empty (or exactly the named M0.1/M4/M6 internal actions);
  owner: v062 sweep.
- `cli-command-inventory-spine-map-001` — the **full disposition table**: every
  dispatcher command maps to a named action/read; no command reaches stores
  directly (inventory-asserted, not sampled); owner: dispatcher proof test.
- `cli-operator-dev-split-no-new-command-001` — the binary surface contains
  exactly the table's operator rows; dev/CI rows absent; owner: dispatcher
  proof test.
- `cli-attach-single-writer-001` — attach-first selection + the embedded
  single-writer guard (second-writer attempt fails with guidance); owner:
  dispatcher proof test.
- `first-run-no-silent-egress-001` — bare `allbert`/first-run detection
  performs zero network I/O without explicit consent; owner: M3/M4 proof test.
- `first-model-state-enum-001` — all 7 design-doc states resolve, incl.
  `below_hardware_floor` → BYOK degrade; owner: M4 proof test.
- `ollama-install-and-pull-confirmed-001` — guided install and model pull each
  execute only behind an approved confirmation with trace/egress records;
  owner: M4 negative tests.
- `install-artifact-verified-001` — install paths verify SHA256 against the
  published checksums; tampered artifact fails; owner: M2 script harness.
- `uninstall-preserves-home-001` — uninstall consumes the manifest, leaves
  Allbert Home intact absent `--purge`; owner: M2 script harness.
- `serve-health-readonly-001` — health surfaces resolve through read-only
  paths; service install writes only the documented unit/plist as the invoking
  user; owner: M5 proof test.
- `tui-convergence-readonly-internal-001` — the **enumerated** completed read
  inventory, each `:internal`, redacted, mutation-free; owner: M6 sweep.
- `tui-convergence-not-intent-candidate-001` — none of the M6 reads appear in
  intent-router candidates (`agent_modules/0`); owner: corpus/registry tests.
- `secret-vault-no-leak-001` — no raw secret in logs, traces, release
  evidence, or inspection output across the vault flows; owner: M7 sweep.
- `vault-tier-resolution-explicit-001` — vault-absent environments resolve to
  tier 2 with an explicit surfaced notice, never silently; owner: M7 tests.
- `secret-vault-migration-redacted-001` — the migration step round-trips with
  redaction scans green; owner: M7 tests.
- `home-layout-package-paths-documented-001` — packaged-install paths +
  extraction cache + vault references documented and matching as-built; owner:
  docs sweep.
- `adr-0076-accepted-001` / `adr-0070-converged-001` — status lines +
  Distribution Trust section present; owner: v062 sweep.

## Settings Central Keys (new in v0.62)

**None planned.** Vault tier resolution is runtime detection with an env
override (`ALLBERT_VAULT_BACKEND`, ratified at S5); onboarding-state storage
shape is decided in M3 (settings key only if additive under ADR 0046, else a
Home-dir state file); layout preferences remain client-side. Secret
*references* use the existing schema types (Current Code State 10).

## Upgrade, Rollback & Coexistence (scope per Locked Decision 15)

In scope for v0.62: backup-before-migrate on version-changed boot; migration
logging (drop `log: false`); the packaged-vs-checkout coexistence policy
(documented separation via `ALLBERT_HOME`/port + the single-writer guard);
service-worker/asset cache correctness across binary upgrades (the versioned
`?vsn` + SW cache-name mechanism from v0.61b, re-verified in the smoke
harness). **Deferred to v0.64 (recorded, not silent):** automated rollback /
down-migrations; delta upgrades; signed binaries (Locked Decision 14).

## Security Posture

No new authority. Packaging changes how Allbert is installed and invoked, not
what any surface may do. The binary, dispatcher, daemon, TUI console, and vault
all route through the existing spine and remain subject to Security Central,
durable confirmations, action boundaries, and redaction policy.

Recorded with teeth rather than as posture prose:

- The release's **four new network touches** (install-script artifact fetch,
  brew tap fetch, Ollama installer fetch, curated model pull) are enumerated
  in ADR 0076's Distribution Trust section; the latter two execute only behind
  explicit operator confirmations with trace records (Locked Decision 8,
  `ollama-install-and-pull-confirmed-001`).
- The binary carries **no telemetry, no phone-home, no auto-update check**.
- Daemonization must not widen defaults: `WorkingDirectory` pinned; the
  cwd-derived `Scope.directory_subtree` fallbacks audited in M5.
- Release mode must not weaken enforcement: the Mix-probe `production?()`
  class is fixed in M1 (Current Code State 6) and gate-asserted.
- The env-key bypass (`:req_llm`) is documented as vault tier 3 and surfaced
  in inspection — never an invisible side channel.

## Non-Goals

- Not a native desktop GUI client. The web workspace remains the operator UI
  through v1.0.
- No hosted/remote distribution service or plugin auto-update.
- No new capability or permission class.
- No guided onboarding wizard. v0.63 builds that on top of these entry points
  (v0.62 M3 ships the first-run detection + wizard routing hook only).
- No final product RC. v0.64 owns integrated install/onboard/work validation,
  **signing/notarization (Locked Decision 14), and automated rollback (Locked
  Decision 15)**.
- No native-Windows packaging (Tier 2, WSL2 only).
- No new plugin-code loading mechanism for packaged installs (the compile-time
  constraint is documented, not solved, in v0.62).

## Documentation Deliverables

- Install/uninstall guide and the explicit Platform Support Tiers (Tier 1
  macOS/Linux freeze-blocking; Tier 2 Windows WSL2/best-effort), including the
  coexistence policy and upgrade/backup behavior.
- `allbert` CLI reference, grouped commands, and the **generated Mix task →
  `allbert` command mapping from the M3 disposition table**.
- `allbert serve` / daemon operations guide (per-OS service management, log
  locations) and health-check reference.
- OS secret-vault credential guide: the three-tier model, per-platform matrix
  (incl. headless), and the plaintext/encrypted-to-vault migration.
- ADR 0070 TUI operator-console reference updated to the completed read set.
- Allbert Home layout notes for packaged-install paths, the extraction cache
  (if Burrito), and vault references.
- ADR 0076 finalized with the Distribution Trust section.

## Definition Of Done

v0.62 is done when:

- the M0 feasibility spike passed all eight proofs (or explicitly re-chose the
  packaging mechanism) and its report was ratified at S2 before M1-M8 work
  began;
- the M0.1 carried-in web tickets landed with tests;
- a packaged `allbert` binary installs via Homebrew and curl and runs on a
  Tier-1 OS (macOS, Linux) with no Elixir/OTP toolchain — plugins registered,
  stocksage migrations applied, web served; Windows is satisfied via WSL2,
  with native-Windows support explicitly Tier 2 / non-freeze-blocking;
- `allbert <group> <command>` exposes the grouped operator surface per the
  ratified disposition table, attach-first with the single-writer guard, with
  bare-`allbert` first-run detection implementing the v0.60 design;
- first-run reaches a usable model via the confirmed guided Ollama path or
  degrades honestly to BYOK (all 7 states);
- `allbert serve` runs runtime + web + channels as a managed per-user daemon
  with a visible health check and a pinned working directory;
- the mix-free TUI console covers the enumerated day-to-day read set through
  read-only internal actions, from the packaged binary;
- provider credentials resolve through the three-tier vault model with
  redacted migration and no raw secret leak;
- ADR 0076 is accepted (with Distribution Trust) and ADR 0070 is marked
  converged;
- `mix allbert.test release.v062`, the artifact smoke harness, docs, and
  warning gates pass;
- version reads 0.62.0 with the tag held for the operator; no known
  warning/security/doc drift remains.
