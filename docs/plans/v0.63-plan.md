# Allbert v0.63 Guided Onboarding & Profiles

Request flow: `docs/plans/v0.63-request-flow.md`
ADR: `docs/adr/0069-operator-onboarding-flow.md` (re-scoped from a v0.59
TUI-only sequencing sliver into a real guided wizard, accepted here);
`docs/adr/0075-user-category-settings-profiles.md` (repo-maintained
persona/profile scope, accepted here). Implements the onboarding flow and
applies the persona model designed in v0.60 "Product Experience Design"
(`docs/adr/0077-product-experience-design-and-information-architecture.md`), and
consumes the First-Model-Path decision resolved in v0.60 M3
(`docs/adr/0078-first-model-path.md`). Renders through
`docs/adr/0074-web-design-system-and-ux-language.md` (web surface) and
`docs/adr/0067-tui-terminal-channel.md` (interactive terminal surface); seats
the wizard in v0.61's overhauled web surface and suggested-action affordances;
runs on v0.62's packaged binary, grouped CLI, `allbert serve`, and OS
secret-vault model.

Status: planned — **plan deepened to implementation-ready 2026-07-07** (Current
Code State, Locked Decisions, Cross-Cutting Invariants, per-milestone
Implementation/Acceptance, Settings Keys, and enumerated `:v063` eval rows added;
reconciled to the as-built v0.62 three-tier vault and ADR 0080 shell). Pre-1.0
product capability release 3 of 3; followed by the v0.64 product RC.

Source: `docs/plans/roadmap.md` `## v0.63: Guided Onboarding & Profiles`;
moved after packaging in the post-v0.58 product-readiness review so first-run
guidance teaches the final `allbert` entry points and secret-vault model.

## Purpose

Turn first-run from a prototype-grade scatter of steps into a genuine guided
onboarding capability. The current onboarding objective and web panel mostly
sequence existing Mix commands; a non-developer user still needs docs and
checkout-oriented commands to reach first value. v0.63 closes that gap by
**implementing the v0.60-designed onboarding flow** (ADR 0077/0069) as a shared
two-track wizard in web and terminal, provider/model setup with masked
credential entry and inline doctor verification, and **applying the v0.60
persona model** (ADR 0077/0075) as repo-maintained user-category profiles that
seed Settings Central defaults with explicit operator review. The wizard UX and
the persona model are inputs from v0.60; v0.63 builds and ships them on the
v0.61 overhauled surface and the v0.62 packaging.

## Expected Inputs

- v0.60 designed and accepted: the onboarding-flow design (its M4), the
  persona/profile model (its M4), and the resolved First-Model-Path decision
  (its M3 -> ADR 0078), all under ADR 0077 (Experience Design & IA). v0.63
  implements and applies these; it does not invent or re-decide them.
- v0.61 implemented and accepted: professional web shell, landing, empty states,
  and the suggested-action affordances that seat this wizard.
- v0.62 implemented and accepted: packaged `allbert` binary, grouped CLI,
  `allbert serve`, mix-free TUI coverage, and OS secret-vault references.
- ADR 0069 and ADR 0075 ready for acceptance here (the implementation/application
  ADRs for the v0.60-designed flow and persona model).
- Existing Settings Central, provider/model doctors, channel pairing, app/intent
  catalogs, and ADR 0072 recommended-model-per-purpose matrix.

## Operator-Visible Win

A brand-new operator launches Allbert, the web wizard auto-opens on first run
after `allbert serve`, they pick QuickStart, reach a working model through the
**First-Model Path resolved in ADR 0078** (assisted-local QuickStart by guided
Ollama install/pull, with BYOK as Advanced/fallback — never an assumed
pre-existing key or managed-hosted default), store/verify any BYOK credential
through the three-tier vault and inline doctor, review and apply a user-category profile,
and reach a working first chat without
hand-editing config or reading docs. The same flow is available through an
interactive terminal wizard for headless installs.

## Planned Interfaces

- A shared onboarding state machine rendered by web and terminal surfaces.
- QuickStart and Advanced tracks.
- Provider/model setup with masked-key entry, three-tier-vault storage, inline doctor,
  local and hosted provider choices, and provider switching without config edits.
- The First-Model Path for the zero-key/zero-model case (assisted-local
  QuickStart by guided Ollama install/pull, with honest BYOK as
  Advanced/fallback), consumed as resolved in ADR 0078 (v0.60 M3) — applied
  here, not chosen here.
- User-category profile selection: researcher, developer, writer, ops, general.
- Explicit review/confirm before any profile seeds settings or suggestions.
- First useful chat checkpoint and visible health/trust-spine confirmation.
- Registered settings action to apply a profile outside first-run onboarding.
- `:v063` eval rows and deterministic `mix allbert.test release.v063` gate.

## Current Code State (the ground truth v0.63 changes)

v0.63 is **not greenfield onboarding** — it reconciles and upgrades existing
scaffolding and adds the greenfield persona layer on a mature Settings Central.
The as-built substrate this release touches (verified 2026-07-07, post-v0.62.0):

1. **Objective-backed onboarding wizard (v0.39-era, command-sequencing grade).**
   `apps/allbert_assist/lib/allbert_assist/onboarding.ex` — a plain context module
   (no process state) that frames/resumes one first-run objective on the Objectives
   runtime tables, with a hardcoded 9-step flow (`welcome_scope`,
   `pick_provider_profile`, `resolve_credential_or_endpoint`, `run_doctor`,
   `pick_model_profile`, `toggle_model_assisted_intent`,
   `optional_channel_registration`, `identity_slot_preview`, `done`). Each step
   carries an evidence snapshot and a suggested **Mix command** to run — i.e. it
   sequences existing commands rather than performing guided setup. Action
   `.../actions/onboarding/step_complete.ex` (`onboarding_step_complete`,
   `:objective_write`, internal, no confirmation) records progress. Web
   LiveComponent `apps/allbert_assist_web/.../workspace/components/onboarding.ex`
   (`#workspace-onboarding-panel`). Catalog wiring: `surface/catalog.ex`
   (`:onboarding_panel`) and `workspace/catalog.ex` (`:core_onboarding_panel`) —
   already cross-surface aware. Mix task `lib/mix/tasks/allbert.onboard.ex`.
2. **First-run detection state machine (v0.62 M3), built as a v0.63 seam.**
   `apps/allbert_assist/lib/allbert_assist/cli/first_run.ex` — `detect/0` returns
   one of six product states (`:home_missing`, `:schema_incompatible`,
   `:onboarding_incomplete`, `:first_model_not_ready`, `:profile_unreviewed`,
   `:product_ready`); "onboarding complete" is a **Home marker file
   `<Home>/onboarding.json`** (deliberately outside Settings Central). Its own
   moduledoc says "v0.63 owns the wizard semantics this hooks into," and both
   `profile_reviewed` (marker key) and `:profile_unreviewed` (state) are
   **placeholders** — `mark_onboarding_complete/0` just sets them true. `cli.ex`'s
   bare-`allbert` `first_run/0` prints guidance strings, several literal "(v0.63)"
   stubs. The v0.62 CLI taxonomy maps `onboard → admin onboarding`
   (`cli/commands.ex:113`), which today backs a **summary read**
   (`first_run.ex` `onboarding_summary/0`), not an interactive wizard.
3. **First-Model Path (v0.62 M4) — shipped and mature; consumed as-is.**
   `cli/first_run.ex` `first_model_state/1` → seven states (`local_ready`,
   `runtime_missing`, `runtime_unhealthy`, `model_missing`, `below_hardware_floor`,
   `byok_ready`, `blocked`); probe `first_model/ollama.ex` (binary / loopback
   server / curated model), hardware floor `first_model/hardware.ex`; confirmation-
   gated actions `first_model/detect.ex` (read-only), `install_ollama.ex`
   (`:command_execute`, resumable), `pull_model.ex` (`:external_network`,
   loopback-only, resumable). ADR 0078's assisted-local path **is real code**, so
   the ADR 0078 BYOK-primary degrade branch does not apply to v0.63.
4. **Settings Central — mature; the only seam profiles use.** `settings.ex`
   (`get/put/resolve/list/validate`), `settings/schema.ex` (`@safe_write_keys`
   allowlist, dotted-key namespacing, `safe_write_key?/known_key?` gates),
   `settings/fragments.ex` + `fragment.ex` (core/app/plugin fragment owners),
   `settings/vault.ex` + `secrets.ex` + `key_custody.ex` — secrets are `*_ref`
   pointers into the **three-tier** backend (v0.62 Locked Decision 12: OS vault →
   encrypted-store fallback → env), never inline. Model/provider profiles already
   exist (`list_model_profiles`, `set_active_model_profile`, ADR 0072 recs) and are
   distinct from personas.
5. **Persona / user-category layer — fully greenfield.** No persona data model,
   registry, seed file, or Settings key exists; only ADR 0075 and
   `docs/design/persona-model.md`. The existing free-text `persona.md`/identity
   memory is explicitly *unrelated* (ADR 0075).
6. **Web workspace shell — ADR 0080 consolidated sidebar (v0.61b as-built).** The
   `onboarding_panel` already renders inside the shell on web/CLI/TUI; v0.63
   extends this surface, it does not add new surface plumbing.

**Reconciliation this creates (Locked Decision 1):** the objective-status notion
of "onboarding complete" and the `<Home>/onboarding.json` marker are today two
disjoint notions. v0.63 unifies them: `onboarding.ex` becomes the single shared
state machine, and the marker + first-model states + profile-review become its
inputs — one source of truth.

## Research Grounding

Surveyed 2026-07-07 (post-v0.62.0). Verifiable local-first / BYOK reference tools
whose patterns map to Allbert's constraints:

- **Goose (`goose configure`)** — interactive provider+model wizard, auto-detects
  provider from a pasted key, stores the key in the **OS keychain with a
  `secrets.yaml` file fallback** when the keyring is unavailable, and warns "do not
  store API keys in project files." This is precisely Allbert's three-tier vault;
  v0.63 improves on Goose by **surfacing which tier the key landed in** (Keychain /
  encrypted-store / env) as a trust feature rather than a silent fallback.
- **Ollama** — first run = install → pull a small default model → chat, with a
  three-way **local / hosted / both** mode prompt that discovers models and
  auto-pulls when missing. Validates ADR 0078's assisted-local QuickStart; v0.63
  keeps the pull confirmation-gated (no silent install).
- **LM Studio / Jan** — GUI model-browser set the "zero-config first model" bar
  the plan already cites; met by *managing* the first model, not assuming a key.
- **Cline / Aider / OpenCode** — BYOK-first with a "manually enter API key" path
  and an **OpenAI-compatible custom endpoint** (base URL + key), confirming
  "provider switch without config edit" and a custom-endpoint option are
  table-stakes.

**Persona vs system-prompt distinction (name it explicitly):** competitor
"persona" features are often a `SOUL.md` system-prompt that *changes behaviour*.
Allbert's ADR 0075 persona is the opposite — a **seed-only settings profile** that
pre-populates operator-tunable defaults + suggestions after review and grants
nothing. ADR 0075's own context already cites OpenClaw QuickStart defaults and Msty
personas as the competitive bar; the seed-only choice is the deliberate
trust-preserving contrast.

*(Honesty note: web searches for the external references "openclaw" and
"hermes-gent" returned low-confidence/likely-fabricated specifics and are not
relied on beyond ADR 0075's existing competitive framing.)*

## Locked Decisions

| # | Decision | Choice | Why not the alternative | Signed |
|---|---|---|---|---|
| 1 | Two disjoint onboarding mechanisms (objective flow + `onboarding.json` marker) | **Unify**: upgrade `onboarding.ex` into the single shared state machine; marker, first-model states, and profile-review are its inputs | A bridge keeps two sources of truth for "complete" and the placeholder drift already in `first_run.ex` | 2026-07-07 |
| 2 | Persona preset storage | **Declarative `priv/` catalog** (per-persona files) validated by a persona registry module | Elixir modules are heavier to review/add and blur "reviewed project metadata"; a settings-fragment hides personas inside schema and loses the standalone reviewable artifact | 2026-07-07 |
| 3 | Interactive terminal wizard form | **One shared state machine**, rich TUI console (ADR 0067/0070) when a TTY is present + line-oriented `allbert onboard` prompt fallback for headless | TUI-only breaks headless installs; line-CLI-only drops the TUI console convergence v0.62 M6 delivered | 2026-07-07 |
| 4 | Credential storage tier in the wizard | **Three-tier, surfaced**: write via Settings `*_ref`, and the wizard shows which tier the value landed in | An "OS vault only" assumption is wrong on headless hosts (encrypted-store tier) and hides the trust posture | 2026-07-07 |
| 5 | First-Model Path option for QuickStart | **Assisted-local (Ollama), as shipped in v0.62 M4**; BYOK is Advanced/fallback; managed-hosted rejected | ADR 0078's BYOK-primary *degrade* branch is moot — assisted-local shipped; re-deciding would re-litigate ADR 0078 | 2026-07-07 |
| 6 | Provider choices | Named local + hosted providers **plus an OpenAI-compatible custom endpoint**; provider switch writes settings, edits no file | Named-only providers fail the table-stakes custom-endpoint case (Cline/Aider) | 2026-07-07 |
| 7 | `allbert onboard` command | **Upgrade** it from the shipped `admin onboarding` summary read to the interactive terminal wizard entry point (reconcile the v0.62 CLI taxonomy) | Leaving it a read leaves the terminal wizard with no packaged entry point | 2026-07-07 |
| 8 | Persona semantics | **Seed-only settings profile**, explicitly NOT a system-prompt / `SOUL.md` persona; grants no authority | A behaviour-changing persona is exactly the authority/trust creep ADR 0075 forbids | 2026-07-07 |

## Cross-Cutting Invariants (apply to every milestone)

- **No new authority or permission class.** Onboarding is an experience over
  existing flows; every effectful step routes through existing registered actions,
  doctors, gates, and confirmations. Security Central (ADR 0006) is unchanged.
- **Settings-Central-only writes.** All settings and profile seeds write through
  Settings Central over `@safe_write_keys`; onboarding is not a config side channel.
- **Seed-only profiles.** Personas seed defaults + suggestions after review/confirm;
  they lower no confirmation floor, grant no egress, connect no channel, store no
  secret.
- **No secret leak.** Masked-key entry writes only `*_ref` pointers through the
  three-tier vault; raw values never echo, log, or appear in traces/diagnostics.
- **No silent egress or silent install.** Wizard convenience never bypasses the
  v0.62 M4 confirmation floor on Ollama install / model pull / hosted egress.
- **One shared flow, no surface fork.** Web and terminal render the same state
  machine and step IDs; neither forks onboarding logic.
- **Docs-in-triad.** Implementation detail lives in this plan / the request flow /
  ADR 0069 + 0075 — no sidecar planning docs.

## Settings Central Keys (what personas seed)

Personas seed **only existing `@safe_write_keys`** (per the `persona-model.md`
pre-audit). No new Settings key or permission class is introduced by v0.63. The
seed surface, with the shipped enums:

- `operator.communication_style` (`concise|balanced|detailed`),
  `operator.handoff_detail` (`brief|concrete_next_steps|full_context`).
- `model_preferences.primary` (profile reference — kept as a reviewed preference;
  never changes the QuickStart model-pull requirement),
  `model_preferences.tasks.coding` (wildcard task preference).
- `coding.model_profile`, `coding.default_approval_mode`, `coding.read.default_limit`,
  `coding.search.max_results`, `coding.search.max_output_bytes` (bounded).
- `intent.router_embedding_profile`, `intent.router_model_profile`,
  `intent.router_escalation_profile`.
- `active_memory.enabled`, `active_memory.top_k`, `active_memory.chunk_max_bytes`.
- `runtime.diagnostics_verbosity` (`quiet|normal|verbose`), `objectives.trace_detail`
  (`operator|debug`).

**Objective defaults** are seeded only via existing `objectives.*` safe-write keys
with pinned values (M4 fixes the exact keys/values; no fuzzy "objective defaults").
**Suggested apps/channels/intents are UI highlights, not settings writes** — they
become active only when the operator configures them through existing
confirmations. Any candidate key that is not already a safe-write key is a
**schema decision M4 must call out explicitly**, never a silent write. Exact
per-persona values are enumerated in **M4** and ADR 0075.

## First-Model Path (consumed from ADR 0078 — decided in v0.60 M3)

QuickStart's "fastest first chat" requires a **reachable model**, and the flow
must define — explicitly, not by assumption — what happens for a user who arrives
with **neither a hosted API key nor a local model installed**. This is the single
biggest "does it feel 1.0" risk in onboarding (the competitive zero-config bar set
by LM Studio and Jan is met by managing the first model). v0.63 **does not decide
this**: the decision was made upstream in v0.60 M3 and recorded in ADR 0078, which
selected among:

- **(a) Assisted local model.** Detect or instruct an Ollama install and one-click
  pull a small default model, so the fast path needs no key. Cost: a managed local
  dependency and model download weight.
- **(b) Managed hosted default.** Ship a default hosted free/low-tier provider so
  the user can chat before configuring their own key. Cost: an Allbert-operated
  credential/relay, its abuse/cost policy, and an ongoing-cost owner.
- **(c) Bring-your-own-key, honestly framed.** QuickStart explicitly requires a
  key or a running local model and says so up front, with the most assisted
  possible key / Ollama setup. Cost: not truly zero-config; a weaker first-value
  story versus the competitive bar.

**Explicit gate:** v0.63 does not assume a key or model already exists, and it
does not re-litigate the option. It **builds QuickStart against the path ADR 0078
resolved**: the QuickStart fast path, the Operator-Visible Win, and the
acceptance-matrix "first useful chat" criterion are all defined against the
ADR 0078 decision — never against an assumed pre-existing model.

## Guided Wizard Flow (implements the v0.60 design — ADR 0077/0069)

v0.63 implements the onboarding flow designed in v0.60 M4 (ADR 0077); ADR 0069
is the implementation ADR accepted here. The wizard is a single shared flow model
with two render surfaces:

- **One flow, two surfaces.** Web and terminal render one state machine; neither
  forks onboarding logic.
- **Two tracks.** QuickStart minimizes decisions and skips channel/integration
  setup to reach a working conversation quickly; Advanced exposes provider,
  model, channel, app/profile, and diagnostics choices.
- **Steps:** launch/source check -> provider/model setup -> user-category profile
  selection/review -> optional channel/integration setup -> first useful chat ->
  health/trust-spine confirmation.
- **Trust spine as a feature.** The wizard explains confirmation, permission,
  traces, and local inspectability as safety properties, not setup friction.

## User-Category Profile System (applies the v0.60 persona model — ADR 0077/0075)

v0.63 applies the persona model designed in v0.60 M4 (ADR 0077); ADR 0075 is the
application ADR accepted here. The shipped categories and what each persona means
come from v0.60; v0.63 realizes them as repo-maintained settings profiles.

- **Repo-maintained declarative presets** live in the source tree and are
  reviewed like other trusted project metadata.
- **Shipped categories:** `researcher`, `developer`, `writer`, `ops`,
  `general`.
- **Each profile specifies:** Settings Central defaults, suggested apps,
  suggested channels, suggested intents, and recommended model-purpose mappings.
- **Seed-only semantics.** Profiles grant no authority, lower no confirmation
  floor, and enable nothing effectful without the normal gates.
- **Explicit application.** Applying a profile always shows exactly what will be
  seeded before confirmation.

## Security Posture

No new authority. Onboarding is an experience over existing flows: every
effectful step routes through existing actions, settings, doctors, gates, and
confirmations. Profiles seed defaults only. Masked credentials write through the
v0.62 **three-tier** secret backend (OS vault → encrypted-store fallback → env) as
Settings `*_ref` pointers and never echo/log raw values; the wizard surfaces which
tier the value landed in (Locked Decision 4). See the Cross-Cutting Invariants and
Locked Decisions sections above for the full set.

## Non-Goals

- No new capability surface or permission class.
- No runtime authorization, trust tier, or per-user authority model from
  profiles.
- No learned, distilled, or auto-generated personas.
- No native desktop GUI onboarding.
- No onboarding-flow or persona-model design, and no First-Model-Path decision;
  v0.60 owns experience design (ADR 0077) and the First-Model Path (ADR 0078).
- No web-surface or affordance overhaul; v0.61 owns the surface this seats in.
- No packaging or CLI redesign; v0.62 owns entry points.
- No final product RC; v0.64 owns integrated validation.

## Milestones

The old First-Model-Path resolution milestone is removed: that decision is made
upstream in v0.60 M3 (ADR 0078) and consumed here. v0.63 builds QuickStart and
the "first useful chat" criterion against the ADR 0078 path rather than deciding
it.

**Workstreams and barriers.** M1 (the shared state machine) is the serial barrier
— M2–M6 render or extend it and cannot start until its state/step/persistence
contract is fixed (gated by S1). M3 (provider/model + vault) and M4 (persona
system) are independent once M1 lands (M3 gated by S2, M4 by S3). M5 (web) and M6
(terminal) both render M1–M4 and can proceed in parallel (S4/S5). M7 (trust-spine +
eval gate) closes over all of them (S6). The 8 wizard step IDs
(`welcome`→`track_select`→`model_path`→`profile_select`→`profile_review`→
`health_check`→`first_chat`→`optional_connect`, from `onboarding-flow.md`) are the
shared contract every surface renders.

### M1: Shared onboarding state machine (the barrier)

**Status:** planned. Implements the v0.60 flow (ADR 0077/0069); resolves Locked
Decision 1 (unify) and Decision 7 (`onboard` entry point).

Upgrade `onboarding.ex` from a command-sequencing objective wrapper into the single
authoritative wizard state machine: the 8 step IDs, their transitions, validation,
and track (QuickStart/Advanced) branching, with one persistence model that
subsumes the objective-status and `<Home>/onboarding.json` marker split.

- **Implementation:**
  - *Modules/Files:* `apps/allbert_assist/lib/allbert_assist/onboarding.ex`
    (state machine: `steps/0`, `advance/2`, `state/1`, `resume/1`, track);
    reconcile persistence so `first_run.ex` `detect/0` reads one source of truth
    (retire the `profile_reviewed`/`:profile_unreviewed` placeholders into real
    states written by `profile_review`); wire `allbert onboard` (`cli/commands.ex`)
    to launch the interactive wizard, not the summary read.
  - *Inputs:* `first_run.ex` `detect/0` (6 product states) + `first_model_state/1`
    (7 states); the 8-step design in `onboarding-flow.md`.
  - *Settings:* none new; onboarding-complete stays a Home marker (outside Settings
    Central), now written by the state machine's terminal step.
  - *Tests:* extend `test/allbert_assist/onboarding_test.exs` — step transitions,
    QuickStart vs Advanced sequence, resume-from-any-step, terminal-marker write;
    a "no surface fork" test that both surfaces drive identical step IDs.
  - *Outputs:* a resumable, surface-agnostic wizard state; `allbert admin
    onboarding` still returns a summary derived from the same state.
  - *Verify:* request-flow **S1**.
- **Acceptance:** one state machine drives both surfaces; `detect/0` has a single
  "onboarding complete" source; every step is resumable; `onboard` launches the
  wizard; no placeholder state remains.

### M2: QuickStart and Advanced tracks against the First-Model Path

**Status:** planned. Consumes ADR 0078 (Locked Decision 5).

Implement the two tracks over M1's machine. QuickStart minimizes decisions and
reaches first useful chat via the `model_path` step driven by the 7 first-model
states; Advanced exposes provider/model/profile/channel choices up front.

- **Implementation:**
  - *Modules/Files:* track branching in `onboarding.ex`; `model_path` step consumes
    the existing `first_model_detect`/`install_ollama`/`pull_model` actions
    (confirmation-gated, no silent install); map each of the 7 states to the wizard
    response table in `onboarding-flow.md` §Failure And Repair States.
  - *Inputs:* `first_model/*` actions; hardware floor; `onboarding-flow.md`
    QuickStart/Advanced UX.
  - *Settings:* model/provider settings written through Settings Central after
    review; secrets only as `*_ref`.
  - *Tests:* per-state wizard-response tests (`local_ready`→continue,
    `runtime_missing`→guided-install-with-BYOK-fallback, `below_hardware_floor`→BYOK,
    `blocked`→smallest-repair+resume); QuickStart-must-not-end-without-model-or-
    repairable-blocker.
  - *Outputs:* a track-aware flow that reaches `first_chat` or a specific repairable
    blocker.
  - *Verify:* request-flow **S2**.
- **Acceptance:** QuickStart reaches a working first chat through the ADR 0078 path
  from any of the 7 states (or shows a specific repairable blocker), with no docs or
  config edits; Advanced exposes full control without making QuickStart feel
  incomplete.

### M3: Provider/model setup — masked entry, three-tier vault, inline doctor

**Status:** planned. Resolves Locked Decisions 4 and 6.

The `model_path`/provider step: masked credential entry, write through the
three-tier vault as a Settings `*_ref`, inline `doctor` round-trip verification, and
provider switching (named local/hosted + OpenAI-compatible custom endpoint) without
config edits.

- **Implementation:**
  - *Modules/Files:* reuse `settings/vault.ex`+`secrets.ex` for `*_ref` writes and
    the existing provider/model doctors (`doctor_model_profile.ex`,
    `settings/model_doctor.ex`); surface the **tier the value landed in** (Keychain
    / encrypted-store / env) from `vault.ex` resolution; provider switch =
    Settings write, no file edit.
  - *Inputs:* provider catalog + ADR 0072 recs; three-tier vault (v0.62 M7).
  - *Settings:* `model_preferences.*`, provider settings, secret `*_ref` keys.
  - *Tests:* masked-entry redaction (no raw key in response/log/trace); vault-tier
    surfaced correctly on keyring-present vs headless; doctor round-trip pass/fail;
    provider switch writes settings and edits no file; custom OpenAI-compatible
    endpoint accepted.
  - *Outputs:* a verified, switchable provider/model with a visible storage tier.
  - *Verify:* request-flow **S2**.
- **Acceptance:** credentials store masked through the three-tier vault, verify
  inline via doctor, and switch providers (incl. custom endpoint) without editing
  config; the wizard shows which tier the secret landed in; no raw secret leaks.

### M4: User-category persona system (greenfield)

**Status:** planned. Applies ADR 0077/0075; resolves Locked Decisions 2 and 8 and
closes the `persona-model.md` open set.

Build the persona layer: a declarative `priv/` catalog, a registry module, a
registered review/confirm apply action, and the **exact per-persona seed values**.

- **Implementation:**
  - *Modules/Files:* `priv/personas/<persona_id>.{yaml|json}` catalog (the 8-field
    seed envelope: `persona_id`, `label`, `settings_seeds`, `suggested_apps`,
    `suggested_channels`, `suggested_intents`, `model_purpose_map`,
    `first_chat_prompts`); a persona **registry module**
    (`AllbertAssist.Personas` — load + validate the catalog at boot, reject unknown
    keys / non-safe-write settings); a **registered apply action**
    (`apply_persona_profile`, permission `:settings_write`, exposure `:internal`,
    `confirmation: :required`) that computes a **review diff** against current
    Settings and seeds via `Settings.put/3` over `@safe_write_keys` only.
  - *Exact seed values* (all seed only existing safe-write keys — see Settings
    Central Keys above): `general` — `communication_style=balanced`,
    `handoff_detail=concrete_next_steps`, `model_preferences.primary`=local default.
    `researcher` — `communication_style=detailed`, `handoff_detail=full_context`,
    `active_memory.enabled=true` (+ tuned `top_k`), router-embedding emphasis (advice).
    `developer` — `communication_style=concise`,
    `coding.default_approval_mode`=conservative, `coding.model_profile`/
    `model_preferences.tasks.coding` recommended after source-egress review.
    `writer` — `communication_style=detailed`, `handoff_detail=full_context`,
    memory tuning after review. `ops` — `communication_style=concise`,
    `runtime.diagnostics_verbosity=verbose`, `objectives.trace_detail=debug`.
    Model-purpose maps + suggested apps/channels/intents are post-first-chat advice
    only (never QuickStart model pulls); exact rows land in ADR 0075.
  - *Inputs:* `persona-model.md` seed table + safe-write pre-audit.
  - *Settings:* seeds only existing safe-write keys; any new key is flagged as an
    explicit schema decision, not written silently.
  - *Tests:* catalog loads/validates all 5 personas; apply action shows the review
    diff and writes **nothing** before confirm; seed-only (no authority/floor
    change); skip does not block first chat; `apply_persona_profile` outside
    onboarding works with the same review/confirm; secrets never seeded.
  - *Outputs:* 5 reviewable personas + an apply action usable in and out of the
    wizard.
  - *Verify:* request-flow **S3**.
- **Acceptance:** each shipped persona is a reviewed `priv/` preset that seeds only
  safe-write settings + suggestions after an explicit review/confirm that shows
  exactly what will be written; grants no authority; is applicable outside first-run.

### M5: Web wizard

**Status:** planned. Extends the existing `onboarding_panel` LiveComponent through
ADR 0074 + the ADR 0080 consolidated sidebar.

- **Implementation:**
  - *Modules/Files:* extend
    `apps/allbert_assist_web/.../workspace/components/onboarding.ex` +
    `surface/catalog.ex` (`:onboarding_panel`) to render M1's steps, the M3 provider
    step (masked entry), and the M4 review diff; auto-open on first run after
    `allbert serve`; seat in the ADR 0080 single sidebar (retire any `/preview`
    scaffold terms).
  - *Tests:* LiveView tests for auto-open on first run, each step render, masked
    entry, review-diff render, first-chat checkpoint.
  - *Verify:* request-flow **S4**.
- **Acceptance:** the web wizard auto-opens on first run and drives M1–M4 through the
  design system on the consolidated shell to a first useful chat.

### M6: Interactive terminal wizard

**Status:** planned. Resolves Locked Decision 3 (shared machine; TUI console +
line-CLI fallback).

- **Implementation:**
  - *Modules/Files:* render M1's state machine in the TUI console (ADR 0067/0070)
    when a TTY is present; a line-oriented `allbert onboard` prompt flow
    (`cli/` area) for headless installs — both drive identical step IDs; masked
    prompt entry that never echoes the secret; honor the three-tier vault (show the
    tier).
  - *Tests:* both terminal renderings drive identical step IDs (shared-flow test);
    masked prompt redaction; headless (no-TTY) falls back to the line flow.
  - *Verify:* request-flow **S5**.
- **Acceptance:** a headless operator completes the same flow through `allbert
  onboard`; a TTY operator gets the TUI console; neither forks the logic.

### M7: Trust-spine surfacing, first-chat checkpoint, eval gate

**Status:** planned.

- **Implementation:**
  - *Modules/Files:* a trust-spine confirmation surface (explains confirmation /
    permission / trace / local inspectability as a feature) in both surfaces; the
    first-useful-chat checkpoint; register the 10 `:v063` eval rows +
    `adr-0069-accepted-001`/`adr-0075-accepted-001` in `eval_inventory.ex`; compose
    the deterministic `mix allbert.test release.v063` gate (migrate / format /
    warnings-as-errors / credo-strict / dialyzer / the `:v063` proof + sweep /
    docs).
  - *Tests:* the `:v063` sweep rows (see Security / Eval Rows); ADR-accept rows.
  - *Verify:* request-flow **S6**.
- **Acceptance:** the trust spine is surfaced as a feature; `release.v063` is
  deterministic and green; ADR 0069 + 0075 are Accepted.

## Security / Eval Rows (`:v063`)

Enumerated with assert semantics + owner test module (names alone are not rows;
registered in `eval_inventory.ex`). Mapped to milestones:

- `onboarding-no-authority-from-profiles-001` — applying any persona grants no
  permission/egress/floor change (M4). owner: onboarding/persona eval test.
- `onboarding-no-secret-leak-001` — no raw credential in wizard output/log/trace
  (M3/M6).
- `onboarding-settings-central-only-writes-001` — every onboarding write goes
  through Settings Central (M1/M3/M4).
- `profile-seeds-defaults-only-001` — persona apply writes only `@safe_write_keys`
  defaults/suggestions (M4).
- `provider-key-masked-vault-entry-redaction-001` — masked entry, `*_ref` write,
  redacted (M3).
- `provider-switch-no-config-edit-001` — provider switch writes settings, edits no
  file (M3).
- `profile-apply-explicit-review-001` — apply writes nothing before review/confirm
  (M4).
- `wizard-shared-flow-no-surface-fork-001` — web + terminal drive identical step
  IDs (M1/M5/M6).
- `trust-spine-surfaced-001` — trust-spine confirmation surface present (M7).
- `quickstart-fastest-first-chat-001` — QuickStart reaches first chat via the ADR
  0078 path from each first-model state (M2).
- `adr-0069-accepted-001` / `adr-0075-accepted-001` — ADR status flips at closeout
  (M7).

## Vision Check

v0.63 preserves the v0.62/1.0 vision: onboarding is an **experience over existing
flows**, not a new product surface. It grants no new authority, adds no permission
class or Settings key beyond seeding existing safe-write keys, and keeps the
runtime/action/settings spine (ADR 0073) and Security Central (ADR 0006) unchanged.
Personas are configuration seeds, not behaviour — distribution of a better first
run, not new power.

## Documentation Deliverables

- Operator onboarding guide for web and terminal QuickStart/Advanced tracks.
- Provider/model setup and switching guide for local + hosted providers.
- User-category profile reference: shipped profiles and exactly what each seeds.
- Trust-spine onboarding note.
- ADR 0069 and ADR 0075 accepted and cross-referenced.

## Definition Of Done

v0.63 is done when:

- a new operator can complete first-run onboarding through the web wizard and the
  terminal wizard over one shared flow that implements the v0.60 design;
- QuickStart is built against the ADR 0078 First-Model Path (decided in v0.60 M3)
  and reaches a working first chat through it without docs or config edits — for a
  user starting from the state that path explicitly assumes, not from an assumed
  pre-existing key;
- provider/model setup stores masked credentials through the three-tier vault
  (surfacing the tier), verifies inline with doctor, supports local + hosted +
  OpenAI-compatible custom-endpoint providers, and switches providers without
  config edits;
- repo-maintained profiles seed Settings Central defaults and suggestions only
  after explicit review/confirm;
- the trust spine is surfaced as a feature;
- ADR 0069 and ADR 0075 are accepted;
- `mix allbert.test release.v063`, docs, and warning gates pass with no
  onboarding secret leak or profile-authority regression;
- no known warning/security/doc drift remains.
