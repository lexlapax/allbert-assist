# Allbert v0.63 Guided Onboarding & Profiles

Request flow: `docs/plans/v0.63-request-flow.md`
ADR: `docs/adr/0069-operator-onboarding-flow.md` (re-scoped from a v0.59
TUI-only sequencing sliver into a real guided wizard, accepted here);
`docs/adr/0075-user-category-settings-profiles.md` (repo-maintained
persona/profile scope, accepted here). Implements the onboarding flow and
applies the persona model designed in v0.60 "Product Experience Design"
(`docs/adr/0077-product-experience-design-and-information-architecture.md`), and
consumes the First-Model-Path decision resolved in v0.60 M3
(`docs/adr/0078-first-model-path.md`). Renders through
`docs/adr/0074-web-design-system-and-ux-language.md` (web surface) and
`docs/adr/0067-tui-terminal-channel.md` (interactive terminal surface); seats
the wizard in v0.61's overhauled web surface and suggested-action affordances;
runs on v0.62's packaged binary, grouped CLI, `allbert serve`, and the as-built
**three-tier secret backend** (OS vault → encrypted-store fallback → env).

Status: planned — **fourth-pass implementation-readiness edit 2026-07-08**
(Current Code State, Locked Decisions, Cross-Cutting Invariants, per-milestone
Implementation/Acceptance, Settings Keys, enumerated `:v063` eval rows, and
operator-first CLI/UX contract added; reconciled to the as-built v0.62 three-tier
vault and ADR 0080 shell; fourth-pass cleanup normalized the automation flag,
handoff readiness mapping, reset/objective seam, and research-grounding caveats).
Pre-1.0 product capability release 3 of 3; followed by the v0.64 product RC.

Source: `docs/plans/roadmap.md` `## v0.63: Guided Onboarding & Profiles`;
moved after packaging in the post-v0.58 product-readiness review so first-run
guidance teaches the final `allbert` entry points and secret-vault model.

## Purpose

Turn first-run from a prototype-grade scatter of steps into a genuine guided
onboarding capability. The current onboarding objective and web panel mostly
sequence existing Mix commands; a non-developer user still needs docs and
checkout-oriented commands to reach first value. v0.63 closes that gap by
**implementing the v0.60-designed onboarding flow** (ADR 0077/0069) as a shared
two-track wizard in web and terminal, provider/model setup with masked
credential entry and inline doctor verification, and **applying the v0.60
persona model** (ADR 0077/0075) as repo-maintained user-category profiles that
seed Settings Central defaults with explicit operator review. The wizard UX and
the persona model are inputs from v0.60; v0.63 builds and ships them on the
v0.61 overhauled surface and the v0.62 packaging.

## Expected Inputs

- v0.60 designed and accepted: the onboarding-flow design (its M4), the
  persona/profile model (its M4), and the resolved First-Model-Path decision
  (its M3 -> ADR 0078), all under ADR 0077 (Experience Design & IA). v0.63
  implements and applies these; it does not invent or re-decide them.
- v0.61 implemented and accepted: professional web shell, landing, empty states,
  and the suggested-action affordances that seat this wizard.
- v0.62 implemented and accepted: packaged `allbert` binary, grouped CLI,
  `allbert serve`, mix-free TUI coverage, and the three-tier secret backend
  (OS vault → encrypted-store fallback → env) with Settings `*_ref` references.
- ADR 0069 and ADR 0075 ready for acceptance here (the implementation/application
  ADRs for the v0.60-designed flow and persona model).
- Existing Settings Central, provider/model doctors, channel pairing, app/intent
  catalogs, and ADR 0072 recommended-model-per-purpose matrix.

## Operator-Visible Win

A brand-new operator launches Allbert, the web wizard auto-opens on first run
after `allbert serve`, they pick QuickStart, reach a working model through the
**First-Model Path resolved in ADR 0078** (assisted-local QuickStart by guided
Ollama install/pull, with BYOK as Advanced/fallback — never an assumed
pre-existing key or managed-hosted default), store/verify any BYOK credential
through the three-tier vault and inline doctor, review and apply a user-category
profile, and reach a working first chat without hand-editing config or reading
docs. The operator sees one next action at every point: Ready, Needs model,
Needs credentials, Needs runtime, or Needs review. The same flow is available
through an interactive terminal wizard for headless installs.

## Planned Interfaces

- A shared onboarding state machine rendered by web and terminal surfaces.
- QuickStart and Advanced tracks.
- Official `allbert onboard --quickstart` and `allbert onboard --advanced` entry
  points, plus default `allbert onboard` resume behavior, confirmed `--reset`, and
  automation `--non-interactive` + `--authorize` (pre-authorization via the
  confirmation approve path).
- Provider/model setup with masked-key entry, three-tier-vault storage, inline doctor,
  local and hosted provider choices, and provider switching without config edits.
- The First-Model Path for the zero-key/zero-model case (assisted-local
  QuickStart by guided Ollama install/pull, with honest BYOK as
  Advanced/fallback), consumed as resolved in ADR 0078 (v0.60 M3) — applied
  here, not chosen here.
- User-category profile selection: researcher, developer, writer, ops, general.
- Explicit review/confirm before any profile seeds settings or suggestions.
- First useful chat checkpoint and visible health/trust-spine confirmation.
- Registered settings action to apply a profile outside first-run onboarding.
- `:v063` eval rows and deterministic `mix allbert.test release.v063` gate.

## Current Code State (the ground truth v0.63 changes)

v0.63 is **not greenfield onboarding** — it reconciles and upgrades existing
scaffolding and adds the greenfield persona layer on a mature Settings Central.
The as-built substrate this release touches (verified 2026-07-07, post-v0.62.0):

1. **Objective-backed onboarding wizard (v0.39-era, command-sequencing grade).**
   `apps/allbert_assist/lib/allbert_assist/onboarding.ex` — a plain context module
   (no process state) that frames/resumes one first-run objective on the Objectives
   runtime tables, with a hardcoded 9-step flow (`welcome_scope`,
   `pick_provider_profile`, `resolve_credential_or_endpoint`, `run_doctor`,
   `pick_model_profile`, `toggle_model_assisted_intent`,
   `optional_channel_registration`, `identity_slot_preview`, `done`). Each step
   carries an evidence snapshot and a suggested **Mix command** to run — i.e. it
   sequences existing commands rather than performing guided setup. Action
   `.../actions/onboarding/step_complete.ex` (`onboarding_step_complete`,
   `:objective_write`, internal, no confirmation) records progress. Web
   LiveComponent `apps/allbert_assist_web/.../workspace/components/onboarding.ex`
   (`#workspace-onboarding-panel`). Catalog wiring: `surface/catalog.ex`
   (`:onboarding_panel`) and `workspace/catalog.ex` (`:core_onboarding_panel`) —
   already cross-surface aware. Mix task `lib/mix/tasks/allbert.onboard.ex`.
2. **First-run detection state machine (v0.62 M3), built as a v0.63 seam.**
   `apps/allbert_assist/lib/allbert_assist/cli/first_run.ex` — `detect/0` returns
   one of six product states (`:home_missing`, `:schema_incompatible`,
   `:onboarding_incomplete`, `:first_model_not_ready`, `:profile_unreviewed`,
   `:product_ready`); "onboarding complete" is a **Home marker file
   `<Home>/onboarding.json`** (deliberately outside Settings Central). Its own
   moduledoc says "v0.63 owns the wizard semantics this hooks into," and both
   `profile_reviewed` (marker key) and `:profile_unreviewed` (state) are
   **placeholders** — `mark_onboarding_complete/0` just sets them true. `cli.ex`'s
   bare-`allbert` `first_run/0` prints guidance strings, several literal "(v0.63)"
   stubs. The v0.62 CLI taxonomy maps `onboard → admin onboarding`
   (`cli/commands.ex:113`), which today backs a **summary read**
   (`first_run.ex` `onboarding_summary/0`), not an interactive wizard.
   v0.63 owns the operator-facing wizard semantics; it must not expose this raw
   six-state product taxonomy as the onboarding UX.
3. **First-Model Path (v0.62 M4) — shipped and mature; consumed as-is.**
   `cli/first_run.ex` `first_model_state/1` probes the model path and returns the
   technical readiness reasons the current code knows (`local_ready`,
   `runtime_missing`, `runtime_unhealthy`, `model_missing`,
   `below_hardware_floor`, `byok_ready`). Probe code lives in
   `first_model/ollama.ex` (binary / loopback server / curated model) and
   `first_model/hardware.ex`; confirmation-gated actions
   `first_model/detect.ex` (read-only), `install_ollama.ex` (`:command_execute`,
   resumable), and `pull_model.ex` (`:external_network`, loopback-only,
   resumable) are the implementation seam. ADR 0078's assisted-local path **is
   real code**, so the ADR 0078 BYOK-primary degrade branch does not apply to
   v0.63. v0.63 adds an operator-facing readiness mapper over these **six** probe
   states per the **Readiness Label Mapping Contract** (below). Note there is **no
   `:blocked` state** in the shipped code — earlier drafts speculated one; the plan
   does not surface it. `Needs credentials` and `Needs review` are produced by the
   provider step and the `detect/0` profile layer, not by `first_model_state/1`.
4. **Settings Central — mature; the only seam profiles use.** `settings.ex`
   (`get/put/resolve/list/validate`), `settings/schema.ex` (`@safe_write_keys`
   allowlist, dotted-key namespacing, `safe_write_key?/known_key?` gates),
   `settings/fragments.ex` + `fragment.ex` (core/app/plugin fragment owners),
   `settings/vault.ex` + `secrets.ex` + `key_custody.ex` — secrets are `*_ref`
   pointers into the **three-tier** backend (v0.62 Locked Decision 12: OS vault →
   encrypted-store fallback → env), never inline. Model/provider profiles already
   exist (`list_model_profiles`, `set_active_model_profile`, ADR 0072 recs) and are
   distinct from personas.
5. **Persona / user-category layer — fully greenfield.** No persona data model,
   registry, seed file, or Settings key exists; only ADR 0075 and
   `docs/design/persona-model.md`. The existing free-text `persona.md`/identity
   memory is explicitly *unrelated* (ADR 0075).
6. **Web workspace shell — ADR 0080 consolidated sidebar (v0.61b as-built).** The
   `onboarding_panel` already renders inside the shell on web/CLI/TUI; v0.63
   extends this surface, it does not add new surface plumbing.

**Reconciliation this creates (Locked Decision 1):** the objective-status notion
of "onboarding complete" and the `<Home>/onboarding.json` marker are today two
disjoint notions. v0.63 unifies them: `onboarding.ex` becomes the single shared
state machine, and the marker + first-model states + profile-review become its
inputs — one source of truth.

## Research Grounding

Surveyed 2026-07-07 (post-v0.62.0). Verifiable local-first / BYOK reference tools
whose patterns map to Allbert's constraints:

- **Goose (`goose configure`)** — interactive provider+model wizard, auto-detects
  provider from a pasted key, stores the key in the **OS keychain with a
  `secrets.yaml` file fallback** when the keyring is unavailable, and warns "do not
  store API keys in project files." This is precisely Allbert's three-tier vault;
  v0.63 improves on Goose by **surfacing which tier the key landed in** (Keychain /
  encrypted-store / env) as a trust feature rather than a silent fallback.
- **Ollama** — first run = install → pull a small default model → chat, with a
  three-way **local / hosted / both** mode prompt that discovers models and
  auto-pulls when missing. Validates ADR 0078's assisted-local QuickStart; v0.63
  keeps the pull confirmation-gated (no silent install).
- **LM Studio / Jan** — GUI model-browser set the "zero-config first model" bar
  the plan already cites; met by *managing* the first model, not assuming a key.
- **Cline / Aider / OpenCode** — BYOK-first with a "manually enter API key" path
  and an **OpenAI-compatible custom endpoint** (base URL + key), confirming
  "provider switch without config edit" and a custom-endpoint option are
  table-stakes.
- **OpenClaw** — official public docs verify a terminal `openclaw onboard` path,
  app onboarding, QuickStart vs Advanced flow selection, re-run/reset semantics,
  custom providers (including OpenAI-compatible endpoints), and non-interactive
  secret-reference automation. Treat it as a competitive operator-UX reference,
  not a protocol/API precedent: the earlier pass-2 notes that named a specific
  risk flag or protocol shape are not relied on as verified requirements.
  Allbert should copy the operator contract it needs: official
  `allbert onboard --quickstart`, `--advanced`, default resume, confirmed reset,
  automation with explicit input refs, and one shared state machine.
- **Hermes Agent** — official docs keep the easy path small: `hermes setup`,
  choose or connect a provider, run the first chat, then add gateway/tools/skills.
  They also separate non-secret config from secrets and use `SOUL.md` as a
  behaviour persona. Allbert should preserve the "first chat before optional
  integrations" posture while keeping ADR 0075 profiles seed-only, not a system
  prompt or behaviour override.

**Persona vs system-prompt distinction (name it explicitly):** competitor
"persona" features are often a `SOUL.md` system-prompt that *changes behaviour*.
Allbert's ADR 0075 persona is the opposite — a **seed-only settings profile** that
pre-populates operator-tunable defaults + suggestions after review and grants
nothing. ADR 0075's own context already cites OpenClaw QuickStart defaults and Msty
personas as the competitive bar; the seed-only choice is the deliberate
trust-preserving contrast.

Official references used for the implementation-readiness contract: OpenClaw's onboarding
wizard (`https://docs.openclaw.ai/start/wizard`), CLI setup reference
(`https://docs.openclaw.ai/start/wizard-cli-reference`), and automation docs
(`https://docs.openclaw.ai/start/wizard-cli-automation`); Hermes Agent's
quickstart
(`https://hermes-agent.nousresearch.com/docs/getting-started/quickstart`),
configuration (`https://hermes-agent.nousresearch.com/docs/user-guide/configuration`),
personality (`https://hermes-agent.nousresearch.com/docs/user-guide/features/personality`),
and security (`https://hermes-agent.nousresearch.com/docs/user-guide/security`)
docs. "Hermes Agent" replaces the earlier ambiguous Hermes reference.

## Locked Decisions

| # | Decision | Choice | Why not the alternative | Signed |
|---|---|---|---|---|
| 1 | Two disjoint onboarding mechanisms (objective flow + `onboarding.json` marker) | **Unify**: upgrade `onboarding.ex` into the single shared state machine; marker, first-model states, and profile-review are its inputs | A bridge keeps two sources of truth for "complete" and the placeholder drift already in `first_run.ex` | 2026-07-07 |
| 2 | Persona preset storage | **Declarative `priv/` catalog** (per-persona files) validated by a persona registry module | Elixir modules are heavier to review/add and blur "reviewed project metadata"; a settings-fragment hides personas inside schema and loses the standalone reviewable artifact | 2026-07-07 |
| 3 | Interactive terminal wizard form | **One shared state machine**, rich TUI console (ADR 0067/0070) when a TTY is present + line-oriented `allbert onboard` prompt fallback for headless | TUI-only breaks headless installs; line-CLI-only drops the TUI console convergence v0.62 M6 delivered | 2026-07-07 |
| 4 | Credential storage tier in the wizard | **Three-tier, surfaced**: write new wizard-entered secrets via Settings `*_ref` to OS vault or encrypted-store fallback; env tier is detected and read-only | Treating env as a writable tier creates a dead-end prompt and hides that the operator must set env outside Allbert | 2026-07-08 |
| 5 | First-Model Path option for QuickStart | **Assisted-local (Ollama), as shipped in v0.62 M4**; BYOK is Advanced/fallback; managed-hosted rejected | ADR 0078's BYOK-primary *degrade* branch is moot — assisted-local shipped; re-deciding would re-litigate ADR 0078 | 2026-07-07 |
| 6 | Provider choices | Named local + hosted providers **plus an OpenAI-compatible custom endpoint**; provider switch writes settings, edits no file | Named-only providers fail the table-stakes custom-endpoint case (Cline/Aider) | 2026-07-07 |
| 7 | `allbert onboard` command | **Upgrade** it from the shipped `admin onboarding` summary read to the interactive terminal wizard entry point; default behavior resumes the active wizard | Leaving it a read leaves the terminal wizard with no packaged entry point | 2026-07-08 |
| 8 | Persona semantics | **Seed-only settings profile**, explicitly NOT a system-prompt / `SOUL.md` persona; grants no authority | A behaviour-changing persona is exactly the authority/trust creep ADR 0075 forbids | 2026-07-07 |
| 9 | Official onboarding CLI modes | Ship `allbert onboard --quickstart` and `allbert onboard --advanced`; bare `allbert onboard` resumes or starts with the track chooser | Hiding track choice behind web-only UI leaves terminal/headless operators with a weaker product path | 2026-07-08 |
| 10 | Non-interactive onboarding + automation authorization | Ship automation `allbert onboard --non-interactive` with explicit input flags; the canonical authorization flag is `--authorize`. A deprecated `--accept-risk` compatibility alias is accepted for older automation, warns, and routes to the same durable approval path. Each confirmation-gated step is created + approved as a durable, traced `Confirmations` record (v0.62 M8.14 mechanism), never an `approved?` shortcut or floor bypass. See the Non-Interactive Authorization & Input Contract | Prompting in automation hangs; silent defaults are unsafe; a raw approval bypass would violate the confirmation floor and add an authority side channel | 2026-07-08 |
| 11 | Re-run and reset | Resume is the default; `--reset` requires explicit confirmation and only resets onboarding/profile marker state in v0.63 | Destructive Home cleanup or secret/config rewrites are too broad for an onboarding retry | 2026-07-08 |
| 12 | Operator readiness language | Render five UX states (`Ready`, `Needs model`, `Needs credentials`, `Needs runtime`, `Needs review`) per the **Readiness Label Mapping Contract** — six real model-probe states (no `:blocked`); `Needs credentials`/`Needs review` come from the provider/product layer, not the probe | Exposing internal atoms makes first-run feel like a test harness; an incomplete/incorrect mapping (e.g. `below_hardware_floor`→"Needs credentials") mislabels the blocker | 2026-07-08 |

## Cross-Cutting Invariants (apply to every milestone)

- **No new authority or permission class.** Onboarding is an experience over
  existing flows; every effectful step routes through existing registered actions,
  doctors, gates, and confirmations. Security Central (ADR 0006) is unchanged.
- **Settings-Central-only writes.** All settings and profile seeds write through
  Settings Central over `@safe_write_keys`; onboarding is not a config side channel.
- **Seed-only profiles.** Personas seed defaults + suggestions after review/confirm;
  they lower no confirmation floor, grant no egress, connect no channel, store no
  secret.
- **No secret leak.** Masked-key entry writes only `*_ref` pointers through the
  three-tier vault; raw values never echo, log, or appear in traces/diagnostics.
  The env tier is read-only: if a secret resolves from env, the wizard reports it
  as operator-provided and verifies it, but new masked entry stores to OS vault or
  encrypted-store fallback only.
- **No silent egress or silent install.** Wizard convenience never bypasses the
  v0.62 M4 confirmation floor on Ollama install / model pull / hosted egress.
- **Operator UX over internal atoms.** Wizard surfaces show one next action in
  operator language; raw product/model probe atoms are for tests, traces, and
  diagnostics.
- **One shared flow, no surface fork.** Web and terminal render the same state
  machine and step IDs; neither forks onboarding logic.
- **Docs-in-triad.** Implementation detail lives in this plan / the request flow /
  ADR 0069 + 0075 — no sidecar planning docs.

### Readiness Label Mapping Contract

The shipped `first_run.ex` `first_model_state/1` emits exactly **six** states (there
is **no `:blocked`** — do not surface it). The operator sees one of five readiness
labels; the mapping is authoritative and every label has a real source:

| Source | Origin | Operator label |
|---|---|---|
| `local_ready` | model probe | **Ready** |
| `byok_ready` | model probe | **Ready** (hosted egress posture visible) |
| `runtime_missing` | model probe | **Needs runtime** |
| `runtime_unhealthy` | model probe | **Needs runtime** |
| `model_missing` | model probe | **Needs model** |
| `below_hardware_floor` | model probe | **Needs review** (local box below the RAM floor → recommend BYOK/existing endpoint; *not* "Needs credentials") |
| hosted/BYOK provider chosen, no key present | **provider step** (not a model-probe state) | **Needs credentials** |
| `:profile_unreviewed` | **`detect/0` product state** (profile layer, not the model probe) | **Needs review** |

`Needs credentials` and `Needs review` are produced by the provider step and the
product/profile layer respectively — not by `first_model_state/1`. Raw internal
atoms are for traces/tests only (invariant "Operator UX over internal atoms").

### Non-Interactive Authorization & Input Contract

`allbert onboard --non-interactive` suppresses prompts for automation. It does
**not** bypass the confirmation floor. The confirmation-gated onboarding steps
(`install_ollama` `:command_execute`, `pull_model` `:external_network`,
`apply_persona_profile` `:settings_write`) proceed only when the operator supplies
the canonical `--authorize` flag. `--accept-risk` remains a deprecated
compatibility alias for older automation; it must warn, stay out of primary help
copy, and route to the exact same durable approval path as `--authorize`.
`--authorize` is an **explicit, durable, logged pre-authorization**: each gated
step is still **created and approved as a durable `Confirmations` record with a
trace**, routed through the existing confirmation approve path (reusing the v0.62
M8.14 confirmation-completion + `admin confirmations approve` mechanism) — it
never sets `confirmation.approved?` ad hoc and never skips the floor. This grants
**no new authority**: it is a pre-recorded operator approval of *existing* gated
actions, fully auditable.

**Required inputs (refuse on any missing required input; secrets only as `*_ref`
or env, never literals):**

| Step | Required in `--non-interactive` | Supplying flags |
|---|---|---|
| Track | yes | `--quickstart` \| `--advanced` |
| Provider/model | if not `local_ready` | `--provider`, `--model`, `--endpoint` (custom) |
| BYOK credential | if hosted/BYOK chosen | `--key-ref` (a vault/env ref, never a literal) |
| Ollama install / model pull | authorization | `--authorize` (records + approves the durable confirmation) |
| Persona | optional (skip allowed) | `--profile`; apply requires `--authorize` |

Missing a required input → the run **refuses** with the specific missing input;
it never prompts, never silently defaults a credential/model/profile, and never
stores an implicit secret.

## Settings Central Keys (what personas seed)

Personas seed **only existing `@safe_write_keys`** (per the `persona-model.md`
pre-audit). No new Settings key or permission class is introduced by v0.63. The
seed surface, with the shipped enums:

- `operator.communication_style` (`concise|balanced|detailed`),
  `operator.handoff_detail` (`brief|concrete_next_steps|full_context`).
- `model_preferences.primary` (profile reference — kept as a reviewed preference;
  never changes the QuickStart model-pull requirement),
  `model_preferences.tasks.coding` (wildcard task preference).
- `coding.model_profile`, `coding.default_approval_mode`, `coding.read.default_limit`,
  `coding.search.max_results`, `coding.search.max_output_bytes` (bounded).
- `intent.router_embedding_profile`, `intent.router_model_profile`,
  `intent.router_escalation_profile`.
- `active_memory.enabled`, `active_memory.top_k`, `active_memory.chunk_max_bytes`.
- `runtime.diagnostics_verbosity` (`quiet|normal|verbose`), `objectives.trace_detail`
  (`operator|debug`).

**Objective defaults** are seeded only via existing `objectives.*` safe-write keys
with pinned values; no fuzzy "objective defaults" are allowed. **Suggested
apps/channels/intents are UI highlights, not settings writes** — they become
active only when the operator configures them through existing confirmations. Any
candidate key that is not already a safe-write key is a **schema decision M4 must
call out explicitly**, never a silent write.

Exact v0.63 profile writes (all values are reviewable and written only after
confirm; arrays are written as Settings values, not extra model pulls):

| persona | Settings writes |
|---|---|
| `general` | `operator.communication_style=balanced`; `operator.handoff_detail=concrete_next_steps`; `model_preferences.primary=local`; `objectives.enabled=true`; `objectives.max_steps_per_turn=3`; `objectives.max_loop_count=5`; `objectives.trace_detail=operator` |
| `researcher` | `operator.communication_style=detailed`; `operator.handoff_detail=full_context`; `model_preferences.primary=local`; `active_memory.enabled=true`; `active_memory.top_k=8`; `active_memory.chunk_max_bytes=4096`; `intent.router_embedding_profile=embedding_local`; `intent.router_model_profile=router_local`; `intent.router_escalation_profile=router_escalation_local`; `objectives.enabled=true`; `objectives.max_steps_per_turn=6`; `objectives.max_loop_count=8`; `objectives.trace_detail=operator` |
| `developer` | `operator.communication_style=concise`; `operator.handoff_detail=concrete_next_steps`; `model_preferences.primary=local`; `coding.default_approval_mode=plan`; `coding.model_profile=pi_coding_local`; `coding.read.default_limit=4000`; `coding.search.max_results=200`; `coding.search.max_output_bytes=240000`; `model_preferences.tasks.coding=["pi_coding_local","coding_local","coding","capable","local"]`; `objectives.enabled=true`; `objectives.max_steps_per_turn=5`; `objectives.max_loop_count=8`; `objectives.trace_detail=operator` |
| `writer` | `operator.communication_style=detailed`; `operator.handoff_detail=full_context`; `model_preferences.primary=local`; `active_memory.enabled=true`; `active_memory.top_k=6`; `active_memory.chunk_max_bytes=4096`; `objectives.enabled=true`; `objectives.max_steps_per_turn=4`; `objectives.max_loop_count=6`; `objectives.trace_detail=operator` |
| `ops` | `operator.communication_style=concise`; `operator.handoff_detail=brief`; `model_preferences.primary=local`; `runtime.diagnostics_verbosity=verbose`; `objectives.enabled=true`; `objectives.max_steps_per_turn=5`; `objectives.max_loop_count=8`; `objectives.trace_detail=debug`; `intent.router_model_profile=router_local`; `intent.router_escalation_profile=router_escalation_local` |

Hosted-egress warnings are review-copy attached to `developer`, `researcher`, and
`ops` model-purpose advice when a hosted profile is present or selected; they are
not Settings writes.

### Provider / credential keys (M3)

Beyond persona seeds, the M3 provider/model step reuses the **existing** provider
and secret keys — it introduces no new persona-style schema:

- Provider/model selection: `providers.*` (provider config, incl. a custom
  OpenAI-compatible **base-URL** key under the chosen provider) and
  `model_preferences.primary` — written through Settings Central after review
  (`set_provider_credential` and the provider settings path; `set_provider_credential`
  is `:settings_secret_write`, `confirmation: :not_required`, so masked-key *writes*
  are operator-initiated and not a floor concern — distinct from the confirmation-
  gated install/pull steps).
- Secrets: `secret://providers/<name>/api_key`-style `*_ref` pointers only; the
  literal value lands in the resolved vault tier (OS vault → encrypted-store → env)
  and the env tier is read-only (Locked Decision 4). No raw key is written to
  settings.

If M3 needs a genuinely new provider/endpoint key not already in the schema, it is
an explicit schema decision called out in M3, never a silent write.

## First-Model Path (consumed from ADR 0078 — decided in v0.60 M3)

QuickStart's "fastest first chat" requires a **reachable model**, and the flow
must define — explicitly, not by assumption — what happens for a user who arrives
with **neither a hosted API key nor a local model installed**. This is the single
biggest "does it feel 1.0" risk in onboarding (the competitive zero-config bar set
by LM Studio and Jan is met by managing the first model). v0.63 **does not decide
this**: the decision was made upstream in v0.60 M3 and recorded in ADR 0078, which
selected among:

- **(a) Assisted local model.** Detect or instruct an Ollama install and one-click
  pull a small default model, so the fast path needs no key. Cost: a managed local
  dependency and model download weight.
- **(b) Managed hosted default.** Ship a default hosted free/low-tier provider so
  the user can chat before configuring their own key. Cost: an Allbert-operated
  credential/relay, its abuse/cost policy, and an ongoing-cost owner.
- **(c) Bring-your-own-key, honestly framed.** QuickStart explicitly requires a
  key or a running local model and says so up front, with the most assisted
  possible key / Ollama setup. Cost: not truly zero-config; a weaker first-value
  story versus the competitive bar.

**Explicit gate:** v0.63 does not assume a key or model already exists, and it
does not re-litigate the option. It **builds QuickStart against the path ADR 0078
resolved**: the QuickStart fast path, the Operator-Visible Win, and the
acceptance-matrix "first useful chat" criterion are all defined against the
ADR 0078 decision — never against an assumed pre-existing model.

## Guided Wizard Flow (implements the v0.60 design — ADR 0077/0069)

v0.63 implements the onboarding flow designed in v0.60 M4 (ADR 0077); ADR 0069
is the implementation ADR accepted here. The wizard is a single shared flow model
with two render surfaces:

- **One flow, two surfaces.** Web and terminal render one state machine; neither
  forks onboarding logic.
- **Two tracks.** QuickStart minimizes decisions and skips channel/integration
  setup to reach a working conversation quickly; Advanced exposes provider,
  model, channel, app/profile, and diagnostics choices.
- **Steps (canonical 8 step IDs — one order across the triad):** `welcome` ->
  `track_select` -> `model_path` -> `profile_select` -> `profile_review` ->
  `health_check` -> `first_chat` -> `optional_connect` (health verification
  precedes first useful chat; optional channel/integration setup is deferred to
  last). This is the single authoritative order — ADR 0069 and the request-flow
  match it.
- **Operator readiness language:** the model/provider step maps internal probe
  reasons to operator states per the **Readiness Label Mapping Contract**
  (`Ready` / `Needs model` / `Needs credentials` / `Needs runtime` / `Needs
  review`) and always shows the next action, not the raw probe atom.
- **Trust spine as a feature.** The wizard explains confirmation, permission,
  traces, and local inspectability as safety properties, not setup friction.

## User-Category Profile System (applies the v0.60 persona model — ADR 0077/0075)

v0.63 applies the persona model designed in v0.60 M4 (ADR 0077); ADR 0075 is the
application ADR accepted here. The shipped categories and what each persona means
come from v0.60; v0.63 realizes them as repo-maintained settings profiles.

- **Repo-maintained declarative presets** live in the source tree and are
  reviewed like other trusted project metadata.
- **Shipped categories:** `researcher`, `developer`, `writer`, `ops`,
  `general`.
- **Each profile specifies:** Settings Central defaults, suggested apps,
  suggested channels, suggested intents, and recommended model-purpose mappings.
- **Seed-only semantics.** Profiles grant no authority, lower no confirmation
  floor, and enable nothing effectful without the normal gates.
- **Explicit application.** Applying a profile always shows exactly what will be
  seeded before confirmation.

## Security Posture

No new authority. Onboarding is an experience over existing flows: every
effectful step routes through existing actions, settings, doctors, gates, and
confirmations. Profiles seed defaults only. Masked credentials write through the
v0.62 **three-tier** secret backend (OS vault → encrypted-store fallback → env) as
Settings `*_ref` pointers and never echo/log raw values; new entries land in OS
vault or encrypted-store fallback, env-provided secrets are read-only, and the
wizard surfaces the tier (Locked Decision 4). See the Cross-Cutting Invariants
and Locked Decisions sections above for the full set.

## Non-Goals

- No new capability surface or permission class.
- No runtime authorization, trust tier, or per-user authority model from
  profiles.
- No learned, distilled, or auto-generated personas.
- No native desktop GUI onboarding.
- No onboarding-flow or persona-model design, and no First-Model-Path decision;
  v0.60 owns experience design (ADR 0077) and the First-Model Path (ADR 0078).
- No web-surface or affordance overhaul; v0.61 owns the surface this seats in.
- No packaging or CLI redesign; v0.62 owns entry points.
- No final product RC; v0.64 owns integrated validation.

## Milestones

The old First-Model-Path resolution milestone is removed: that decision is made
upstream in v0.60 M3 (ADR 0078) and consumed here. v0.63 builds QuickStart and
the "first useful chat" criterion against the ADR 0078 path rather than deciding
it.

**Workstreams and barriers.** M1 (the shared state machine) is the serial barrier
— M2–M6 render or extend it and cannot start until its state/step/persistence
contract is fixed (gated by S1). M3 (provider/model + vault) and M4 (persona
system) are independent once M1 lands (M3 gated by S2, M4 by S3). M5 (web) and M6
(terminal) both render M1–M4 and can proceed in parallel (S4/S5). M7 (trust-spine +
eval gate) closes over all of them (S6). The 8 wizard step IDs
(`welcome`→`track_select`→`model_path`→`profile_select`→`profile_review`→
`health_check`→`first_chat`→`optional_connect`, from `onboarding-flow.md`) are the
shared contract every surface renders.

### M1: Shared onboarding state machine (the barrier)

**Status:** built (2026-07-08). Implements the v0.60 flow (ADR 0077/0069); resolves
Locked Decision 1 (unify) and Decision 7 (`onboard` entry point).

**As-built note.** Rather than a wholesale rewrite of the ~700-line objective-backed
`onboarding.ex` (which the 9-step objective test + web LiveComponent still lock), the
authoritative wizard machine was **layered** onto the existing engine: a marker-derived
8-step state machine (`wizard_start/2`, `wizard_state/1`, `wizard_advance/3`,
`wizard_resume/1`, `wizard_reset/1`, `wizard_status/1`, `wizard_steps/0`,
`wizard_tracks/0`, `readiness_label/1`) whose single source of truth is the FirstRun
Home marker (`<Home>/onboarding.json`). The legacy objective flow is retained as durable
trace + back-compat and retires as surfaces migrate (M5/M6/M7). `first_run.ex` gained
the marker seams (`merge_marker/1` public, `reset_onboarding/0`, `read_marker/0`,
`mark_profile_reviewed/0`); `mark_onboarding_complete/0` no longer force-sets
`profile_reviewed` (so `:profile_unreviewed` is now a real state that `profile_review`
flips). `reset/1`'s objective cancel is **best-effort** (`rescue → :ok`) so `--reset`
always clears the marker even when the objective store is unavailable. The `onboard`
top-level verb + `AllbertAssist.CLI.Areas.Onboarding` dispatcher (`strict:` OptionParser)
are wired in `commands.ex` (`@operator` + `@task_dispositions`). `--authorize` is the
canonical flag (deprecated `--accept-risk` alias). Per-step effectful actions +
`--authorize` pre-authorization land in M2–M4/M6. Green: dialyzer 0, credo 0, format
clean, full CLI suite + onboarding/first_run/commands + new `Areas.Onboarding` test.

Upgrade `onboarding.ex` from a command-sequencing objective wrapper into the single
authoritative wizard state machine: the 8 step IDs, their transitions, validation,
and track (QuickStart/Advanced) branching, with one persistence model that
subsumes the objective-status and `<Home>/onboarding.json` marker split.

- **Implementation:**
  - *Modules/Files:* `apps/allbert_assist/lib/allbert_assist/onboarding.ex`
    (state machine: `steps/0`, `start/2`, `advance/2`, `state/1`, `resume/1`,
    `reset/1`, `status/1`, track).
  - *CLI plumbing (name it — `onboard` is NOT a top-level command today):* today
    only `admin onboarding` exists, dispositioned `{:read, …, :onboarding_summary}`
    which **discards flags** (`run_read/3` calls the fn with no args). v0.63 makes
    `onboard` a **new top-level verb** (Locked Decision 7): add an `@operator`
    entry in `cli/commands.ex`, change its disposition to a new
    `AllbertAssist.CLI.Areas.Onboarding` dispatcher (matching the 27 existing areas
    + the OptionParser `strict:`/`switches:` convention), off `{:read}`. ADR 0076's
    CLI taxonomy gains a top-level setup verb through ADR 0069's v0.63 amendment.
  - *Persistence unification + reset:* reconcile so `first_run.ex` `detect/0` reads
    one source of truth — retire `profile_reviewed`/`:profile_unreviewed` from
    placeholders into real states written by `profile_review`. `reset/1` must clear
    **both** stores: (a) the `<Home>/onboarding.json` marker — add a **clear/reset
    seam to `first_run.ex`** (today `write_marker/1` only *merges*; there is no
    delete/reset), and (b) cancel/reframe the active onboarding objective via the
    existing Objectives cancel path (`Objectives.cancel/3` / `cancel_objective`).
    The missing seam is the onboarding-specific reset wrapper that identifies the
    active onboarding objective and preserves all unrelated objective history.
    `--reset` is confirmation-gated and preserves all other Home data
    (secrets/settings/traces/memory/caches).
  - *CLI contract:* `allbert onboard` resumes active state or starts with the track
    chooser; `--quickstart`/`--advanced` start/resume that track; `--reset`
    (confirmed) resets both stores per above; `--non-interactive` suppresses prompts
    and canonical `--authorize` pre-authorizes the gated steps via the confirmation
    approve path — see the **Non-Interactive Authorization & Input Contract**;
    deprecated `--accept-risk` is accepted as a warning compatibility alias only;
    refuses on missing required input.
  - *Inputs:* `first_run.ex` `detect/0` (6 product states) + `first_model_state/1`
    probe reasons mapped to operator readiness labels; the 8-step design in
    `onboarding-flow.md`.
  - *Settings:* none new; onboarding-complete stays a Home marker (outside Settings
    Central), now written by the state machine's terminal step.
  - *Tests:* extend `test/allbert_assist/onboarding_test.exs` — step transitions,
    QuickStart vs Advanced sequence, resume-from-any-step, terminal-marker write;
    a "no surface fork" test that both surfaces drive identical step IDs; CLI
    contract tests for default resume, `--quickstart`, `--advanced`, confirmed
    `--reset`, and non-interactive refusal on missing input.
  - *Outputs:* a resumable, surface-agnostic wizard state; `allbert admin
    onboarding` still returns a summary derived from the same state.
  - *Verify:* request-flow **S1**.
- **Acceptance:** one state machine drives both surfaces; `detect/0` has a single
  "onboarding complete" source; every step is resumable; `allbert onboard` (new
  top-level verb) launches/resumes the wizard and `--quickstart`/`--advanced`
  select the track; confirmed `--reset` clears both persistence stores and
  preserves the rest of Home; `--non-interactive` refuses on missing required input
  and `--authorize` records a durable confirmation + trace per gated step (never a
  bypass); no placeholder state remains.

### M2: QuickStart and Advanced tracks against the First-Model Path

**Status:** built (2026-07-08). Consumes ADR 0078 (Locked Decision 5).

**As-built note.** M1 already shipped the probe→label mapper (`readiness_label/1`)
and track branching (`maybe_skip_optional`, QuickStart defers `optional_connect`). M2
adds the **track-aware `model_path` guidance** — `model_path_guidance/1` (probe+track)
and `model_guidance_for/2` (label+track, no re-probe) return a `model_guidance` map:
operator headline, the single `next_action` string, a routed `action`
(`:start_chat`/`:install_runtime`/`:pull_model`/`:choose_provider`), `repairable?`,
and `reaches_chat?`. The **no-dead-end invariant** is enforced by construction and
tested across all six probe outcomes × both tracks: `ready` reaches chat, every other
outcome is `repairable?: true` with a concrete action. Advanced appends an inline
"(Advanced: …)" provider/model affordance QuickStart omits; both keep the same routed
action. `below_hardware_floor → :needs_review → :choose_provider` (BYOK), never the old
wrong "Needs credentials". Operator copy carries no raw probe/readiness atoms (tested).
The CLI dispatcher renders `headline` + `Next:` at the `model_path` step (or any
non-ready readiness). Green: dialyzer 0, credo 0.

Implement the two tracks over M1's machine. QuickStart minimizes decisions and
reaches first useful chat via the `model_path` step driven by a simple
operator-facing readiness mapper over first-model probes; Advanced exposes
provider/model/profile/channel choices up front.

- **Implementation:**
  - *Modules/Files:* track branching in `onboarding.ex`; `model_path` step consumes
    the existing `first_model_detect`/`install_ollama`/`pull_model` actions
    (confirmation-gated, no silent install); map technical probe reasons to
    operator states (`Ready`, `Needs model`, `Needs credentials`,
    `Needs runtime`, `Needs review`) and then to one next action. The wizard may
    retain internal reasons such as `runtime_missing`, `runtime_unhealthy`,
    `model_missing`, `below_hardware_floor`, or `byok_ready` for traces/tests, but
    those names are not the operator UX.
  - *Inputs:* `first_model/*` actions; hardware floor; `onboarding-flow.md`
    QuickStart/Advanced UX.
  - *Settings:* model/provider settings written through Settings Central after
    review; secrets only as `*_ref`.
  - *Tests:* readiness-mapping tests per the **Readiness Label Mapping Contract**
    (`local_ready`/`byok_ready`→`Ready`,
    `runtime_missing`/`runtime_unhealthy`→`Needs runtime`,
    `model_missing`→`Needs model`, `below_hardware_floor`→`Needs review`; and the
    non-probe sources: provider-step-no-key→`Needs credentials`,
    `:profile_unreviewed`→`Needs review`); QuickStart-must-not-end-without-model-
    or-repairable-next-action; no raw internal atom in operator copy.
  - *Outputs:* a track-aware flow that reaches `first_chat` or a specific repairable
    blocker.
  - *Verify:* request-flow **S2**.
- **Acceptance:** QuickStart reaches a working first chat through the ADR 0078 path
  from every first-model probe outcome, or shows one specific repair action in
  operator language, with no docs or config edits; Advanced exposes full control
  without making QuickStart feel incomplete.

### M3: Provider/model setup — masked entry, three-tier vault, inline doctor

**Status:** built (2026-07-08). Resolves Locked Decisions 4 and 6.

**As-built note.** M3 reuses the existing action layer — no new schema. The masked
credential write stays `set_provider_credential` (`:settings_secret_write`,
`confirmation: :not_required`), the inline round-trip stays `Settings.ModelDoctor`/
`doctor_model_profile`, provider switch stays `set_active_model_profile` + the
`providers.*.base_url`/`endpoint_kind` keys. The M3 wizard-layer contribution is the
shared **interpretation** module both surfaces render:
`AllbertAssist.Onboarding.ProviderStep` — (1) `vault_tier_report/1` composes
`Vault.resolve/0` + `Vault.Env.env_provided/0` into an operator-language tier report
(OS keychain / encrypted store are writable; env is read-only "provided by
environment", never a write target); (2) `credential_status/2` yields the
provider-step `:needs_credentials` readiness (hosted chosen, no key) — the one label
the probe never emits; (3) `interpret_doctor/1` maps a ModelDoctor summary
(`endpoint_ok`/`credential_ok`/`model_available`) to pass/fail + one repair action.
All three are pure/injectable (tested without keyring or network). `readiness`/
`model_action` gained `:needs_credentials`/`:enter_credentials`; `readiness_label/1`
keeps the narrower `probe_readiness` return (dialyzer). The CLI shows the storage tier
+ env-provided keys at the `model_path` step. Masked-write redaction is covered by the
existing `SettingsActionsTest`; a ProviderStep integration test re-verifies no echo +
retrievable ref + reportable tier. Green: dialyzer 0, credo 0, 58 onboarding/CLI tests.

The `model_path`/provider step: masked credential entry, write through the
three-tier vault as a Settings `*_ref`, inline `doctor` round-trip verification,
and provider switching (named local/hosted + OpenAI-compatible custom endpoint)
without config edits. Env-backed secrets are supported as detected inputs but are
read-only from the wizard.

- **Implementation:**
  - *Modules/Files:* reuse `settings/vault.ex`+`secrets.ex` for `*_ref` writes and
    the existing provider/model doctors (`doctor_model_profile.ex`,
    `settings/model_doctor.ex`); surface the **tier the value landed in** (Keychain
    / encrypted-store / env) from `vault.ex` resolution; new masked entry writes
    only to OS vault or encrypted-store fallback, while env tier is reported as
    "provided by environment" and repaired by showing the required env var name;
    provider switch = Settings write, no file edit.
  - *Inputs:* provider catalog + ADR 0072 recs; three-tier vault (v0.62 M7).
  - *Settings:* `model_preferences.*`, provider settings, secret `*_ref` keys.
  - *Tests:* masked-entry redaction (no raw key in response/log/trace); vault-tier
    surfaced correctly on keyring-present vs headless; env-tier detection is
    read-only and does not try to write; doctor round-trip pass/fail; provider
    switch writes settings and edits no file; custom OpenAI-compatible endpoint
    accepted.
  - *Outputs:* a verified, switchable provider/model with a visible storage tier.
  - *Verify:* request-flow **S2**.
- **Acceptance:** credentials store masked through the three-tier vault, verify
  inline via doctor, and switch providers (incl. custom endpoint) without editing
  config; the wizard shows which tier the secret landed in; no raw secret leaks.

### M4: User-category persona system (greenfield)

**Status:** built (2026-07-08). Applies ADR 0077/0075; resolves Locked Decisions 2
and 8 and closes the `persona-model.md` open set.

**As-built note.** Greenfield catalog + registry + apply action, all seed-only.
`priv/personas/{general,researcher,developer,writer,ops}.yaml` carry the 8-field
envelope with the exact pinned `settings_seeds` from the §Settings Central Keys table.
`AllbertAssist.Personas` embeds the catalog at compile time (release-safe) and exposes
`all/0`/`ids/0`/`fetch/1`/`get/1`/`settings_seeds/1` + `validate!/0`/`validate/2`;
`validate!/0` runs at boot from `application.ex` (fail-fast on an unknown envelope key
or a non-safe-write seed key), matching the `ProviderCatalog` boot pattern. The
`apply_persona_profile` action (`:settings_write`, `exposure: :internal`,
`confirmation: :required`, `resumable?: true`) computes a **review diff** (each seed
`current → proposed` + `changed?`, the highlight-only suggestions, model-purpose advice
+ hosted-egress flag, an explicit `grants: all-false` statement) and **writes nothing**
until an approved confirmation resumes it. Wiring required three seams matching the
`migrate_secrets` precedent: a `safety_floor(:settings_write, apply_persona_profile) →
:needs_confirmation` clause (policy.ex), inclusion in `@generic_resume_actions`
(approve_confirmation.ex re-runs it via the Runner with `approved?: true`), and
placement in `@internal_actions` (kept off the general agent surface). Defense in depth:
`apply_seeds` re-checks `safe_write_key?` per key even post-validation.
`first_chat_prompts` are consumed at the `first_chat` step (M7), not here. Green:
dialyzer 0, credo 0, 380 action tests + persona/apply/registry/param-contract inventory.

Build the persona layer: a declarative `priv/` catalog, a registry module, a
registered review/confirm apply action, and the **exact per-persona seed values**.

- **Implementation:**
  - *Modules/Files:* `priv/personas/<persona_id>.{yaml|json}` catalog (the 8-field
    seed envelope: `persona_id`, `label`, `settings_seeds`, `suggested_apps`,
    `suggested_channels`, `suggested_intents`, `model_purpose_map`,
    `first_chat_prompts`); a persona **registry module**
    (`AllbertAssist.Personas` — load + validate the catalog at boot, reject unknown
    keys / non-safe-write settings); a **registered apply action**
    (`apply_persona_profile`, permission `:settings_write`, exposure `:internal`,
    `confirmation: :required`) that computes a **review diff** against current
    Settings and seeds via `Settings.put/3` over `@safe_write_keys` only.
  - *Exact seed values:* the five rows in **Settings Central Keys** are the
    implementation contract. `general`, `researcher`, `developer`, `writer`, and
    `ops` all write their listed safe-write keys after review/confirm; none of
    those writes are advisory placeholders. Model-purpose maps + suggested
    apps/channels/intents remain post-first-chat advice only (never QuickStart
    model pulls) and do not write settings.
  - *Review-diff render contract (shared, web + terminal):* the diff shows each
    `settings_seeds` key with `current → proposed` and marks unchanged keys as
    "no change"; the non-writing `suggested_apps`/`suggested_channels`/
    `suggested_intents` render in a visually distinct "will highlight, not write"
    group; `model_purpose_map` renders as advice with any hosted-egress warning.
    Nothing writes until confirm. `first_chat_prompts` are consumed at the
    `first_chat` step as the suggested starter prompts (not settings; not applied
    at profile_review).
  - *Inputs:* `persona-model.md` seed table + safe-write pre-audit.
  - *Settings:* seeds only existing safe-write keys; any new key is flagged as an
    explicit schema decision, not written silently.
  - *Tests:* catalog loads/validates all 5 personas; apply action shows the review
    diff and writes **nothing** before confirm; seed-only (no authority/floor
    change); skip does not block first chat; `apply_persona_profile` outside
    onboarding works with the same review/confirm; secrets never seeded.
  - *Outputs:* 5 reviewable personas + an apply action usable in and out of the
    wizard.
  - *Verify:* request-flow **S3**.
- **Acceptance:** each shipped persona is a reviewed `priv/` preset that seeds only
  safe-write settings + suggestions after an explicit review/confirm that shows
  exactly what will be written; grants no authority; is applicable outside first-run.

### M5: Web wizard

**Status:** built (2026-07-08). Extends the existing `onboarding_panel` LiveComponent
through ADR 0074 + the ADR 0080 consolidated sidebar.

**As-built note.** Auto-open: the workspace shell resolves the onboarding canvas when
landing with no explicit destination and onboarding is not yet complete, via a new
pure/injectable decision module `AllbertAssistWeb.Workspace.FirstRun.auto_open?/1`
gated on exactly `FirstRun.detect/0 in [:onboarding_incomplete, :profile_unreviewed]`.
It deliberately excludes `:first_model_not_ready` (model setup is reachable elsewhere)
**and** the infra states `:home_missing`/`:schema_incompatible` (a misconfiguration,
not a first-run — auto-opening there would loop uselessly and, in the test harness
where the LiveView resolves a db-less home, would hijack every bare-`/workspace`
mount). `workspace_live.ex` `resolve_canvas_destination/1` consults it only when no
`destination` param is present; an explicit destination always wins. The
`onboarding_panel` LiveComponent gained a shared **M1 wizard section**
(`#workspace-onboarding-wizard`) rendering the operator readiness label (never a raw
probe atom), the QuickStart/Advanced start controls, the 8 canonical steps with
current/done markers, per-step Continue, and Reset — all driving the M1 machine
(`wizard_start`/`wizard_advance`/`wizard_reset`) directly (marker-backed, not a store
write); the legacy objective panel is retained beneath it (retires with the flow).
Tests: a pure `FirstRunTest` for the auto-open decision (incl. the infra-state
exclusions) + two LiveView tests (readiness label with no atom leak; start→advance
through M1) with marker snapshot/restore. Green: full workspace LiveView suite.

- **Implementation:**
  - *Modules/Files:* extend
    `apps/allbert_assist_web/.../workspace/components/onboarding.ex` +
    `surface/catalog.ex` (`:onboarding_panel`) to render M1's steps, the M3 provider
    step (masked entry), and the M4 review diff; drive only the M1 commands
    (`start`, `advance`, `resume`, `reset`, `status`) through registered events;
    seat in the ADR 0080 single sidebar (the retired `/preview` route scaffold is
    not used). Web copy uses operator readiness labels, never internal probe atoms.
  - *Auto-open mechanism:* on workspace mount the LiveView calls
    `first_run.ex` `detect/0`; when it is not `:product_ready` (onboarding
    incomplete / profile unreviewed) the wizard panel opens automatically after
    `allbert serve`; once complete it does not reopen.
  - *Tests:* LiveView tests for auto-open on first run, each step render, masked
    entry, review-diff render, first-chat checkpoint.
  - *Verify:* request-flow **S4**.
- **Acceptance:** the web wizard auto-opens on first run and drives M1–M4 through the
  design system on the consolidated shell to a first useful chat.

### M6: Interactive terminal wizard

**Status:** planned. Resolves Locked Decision 3 (shared machine; TUI console +
line-CLI fallback).

- **Implementation:**
  - *Modules/Files:* render M1's state machine in the TUI console (ADR 0067/0070)
    when a TTY is present; a line-oriented `allbert onboard` prompt flow
    (`Areas.Onboarding` dispatcher) for headless installs — both drive identical
    step IDs; implement `allbert onboard`, `--quickstart`, `--advanced`, confirmed
    `--reset`, and `--non-interactive` + `--authorize` (per the Non-Interactive
    Authorization & Input Contract); masked prompt entry that never echoes the
    secret; honor the three-tier vault (show the tier and treat env as read-only).
  - *Tests:* both terminal renderings drive identical step IDs (shared-flow test);
    masked prompt redaction; headless (no-TTY) falls back to the line flow;
    non-interactive mode refuses missing required inputs and never prompts.
  - *Verify:* request-flow **S5**.
- **Acceptance:** a headless operator completes the same flow through `allbert
  onboard`; a TTY operator gets the TUI console; neither forks the logic.

### M7: Trust-spine surfacing, first-chat checkpoint, eval gate

**Status:** planned.

- **Implementation:**
  - *Modules/Files:* a trust-spine confirmation surface (explains confirmation /
    permission / trace / local inspectability as a feature) in both surfaces; the
    first-useful-chat checkpoint; register the 14 `:v063` eval rows +
    `adr-0069-accepted-001`/`adr-0075-accepted-001` in `eval_inventory.ex`; compose
    the deterministic `mix allbert.test release.v063` gate (migrate / format /
    warnings-as-errors / credo-strict / dialyzer / the `:v063` proof + sweep /
    docs).
  - *Tests:* the `:v063` sweep rows (see Security / Eval Rows); ADR-accept rows.
  - *Verify:* request-flow **S6**.
- **Acceptance:** the trust spine is surfaced as a feature; `release.v063` is
  deterministic and green; ADR 0069 + 0075 are Accepted.

## Security / Eval Rows (`:v063`)

Enumerated with assert semantics + owner test module (names alone are not rows;
registered in `eval_inventory.ex`). Owner modules are the three greenfield v0.63
suites: **[Sec]** `AllbertAssist.Onboarding.SecurityEvalTest`, **[Flow]**
`AllbertAssist.Onboarding.FlowEvalTest`, **[Sweep]**
`AllbertAssist.Security.V063SweepEvalTest`. Mapped to milestones:

- `onboarding-no-authority-from-profiles-001` — applying any persona grants no
  permission/egress/floor change (M4). owner: [Sec].
- `onboarding-no-secret-leak-001` — no raw credential in wizard output/log/trace
  (M3/M6). owner: [Sec].
- `onboarding-settings-central-only-writes-001` — every onboarding write goes
  through Settings Central (M1/M3/M4). owner: [Sec].
- `profile-seeds-defaults-only-001` — persona apply writes only `@safe_write_keys`
  defaults/suggestions (M4). owner: [Sec].
- `provider-key-masked-vault-entry-redaction-001` — masked entry, `*_ref` write,
  redacted (M3). owner: [Sec].
- `provider-env-tier-read-only-001` — env-provided secrets are detected, verified,
  and surfaced as read-only; wizard entry does not try to write to env (M3). owner:
  [Sec].
- `provider-switch-no-config-edit-001` — provider switch writes settings, edits no
  file (M3). owner: [Sec].
- `profile-apply-explicit-review-001` — apply writes nothing before review/confirm
  (M4). owner: [Sec].
- `wizard-shared-flow-no-surface-fork-001` — web + terminal drive identical step
  IDs (M1/M5/M6). owner: [Flow].
- `wizard-operator-readiness-copy-001` — operator surfaces show the mapped labels
  (`Ready`/`Needs …`) per the Readiness Label Mapping Contract and one next action,
  never a raw probe atom (M2/M5/M6). owner: [Flow].
- `onboarding-noninteractive-authorize-no-bypass-001` — `--non-interactive
  --authorize` refuses missing required inputs, never prompts, and never stores an
  implicit secret; deprecated `--accept-risk` aliases to the same path with a
  warning; **and each confirmation-gated step (install/pull/persona-apply) records
  a durable `Confirmations` record + trace through the approve path — no
  `approved?` shortcut, no floor bypass** (M1/M6). owner: [Sec].
- `onboarding-reset-preserves-home-001` — confirmed `--reset` clears both
  onboarding persistence stores (marker + objective) and never deletes Home data,
  secrets, settings, traces, memory, or caches (M1/M6). owner: [Sec].
- `trust-spine-surfaced-001` — trust-spine confirmation surface present (M7).
  owner: [Flow].
- `quickstart-fastest-first-chat-001` — QuickStart reaches first chat via the ADR
  0078 path from each first-model probe outcome, or shows one repair action (M2).
  owner: [Flow].
- `adr-0069-accepted-001` / `adr-0075-accepted-001` — ADR status flips at closeout
  (M7). owner: [Sweep].

## Vision Check

v0.63 preserves the v0.62/1.0 vision: onboarding is an **experience over existing
flows**, not a new product surface. It grants no new authority, adds no permission
class or Settings key beyond seeding existing safe-write keys, and keeps the
runtime/action/settings spine (ADR 0073) and Security Central (ADR 0006) unchanged.
Personas are configuration seeds, not behaviour — distribution of a better first
run, not new power.

## Upgrade, Rollback & Coexistence

The persistence unification (Locked Decision 1) changes how "onboarding complete"
is stored, so existing Homes need a defined migration:

- **Already-fully-onboarded Homes (v0.62).** `<Home>/onboarding.json` with
  `onboarding_complete=true` (and the placeholder `profile_reviewed=true`) is read
  as complete by the unified state machine — no re-onboarding is forced. Because
  `profile_reviewed` was a placeholder (always set by `mark_onboarding_complete/0`),
  such Homes are treated as **profile-review-skipped**, not profile-applied; the
  operator can apply a persona later via `apply_persona_profile` without re-running
  first-run.
- **Partially-onboarded Homes (objective mid-flight).** The old objective-status
  flow and the marker were disjoint; on first v0.63 launch the state machine
  **reconciles to the marker** as the authoritative source and reframes/cancels any
  stale in-flight onboarding objective (the reconcile is one-way; the Objectives
  runtime tables are no longer a second source of "complete").
- **Retirement.** `:profile_unreviewed`/`profile_reviewed` become real states
  written by `profile_review`; the objective-status notion of onboarding completion
  is retired as a completion source (the objective may remain as a trace artifact,
  but `detect/0` reads only the marker).
- **Rollback.** Reverting to a v0.62 binary still reads `onboarding.json`; no schema
  migration is destructive. `--reset` (confirmation-gated) is the supported way to
  re-run onboarding, and it preserves all other Home data.

## Documentation Deliverables

- Operator onboarding guide for web and terminal QuickStart/Advanced tracks.
- Provider/model setup and switching guide for local + hosted providers.
- User-category profile reference: shipped profiles and exactly what each seeds.
- Trust-spine onboarding note.
- ADR 0069 and ADR 0075 accepted and cross-referenced.

## Definition Of Done

v0.63 is done when:

- a new operator can complete first-run onboarding through the web wizard and the
  terminal wizard over one shared flow that implements the v0.60 design;
- `allbert onboard` (new top-level verb), `--quickstart`, `--advanced`, confirmed
  `--reset`, and `--non-interactive` + canonical `--authorize` behave as documented
  — resume is the default; `--reset` clears both persistence stores and nothing
  else; `--authorize` records a durable confirmation + trace per gated step (no
  bypass); deprecated `--accept-risk` remains only as a warning compatibility alias;
- QuickStart is built against the ADR 0078 First-Model Path (decided in v0.60 M3)
  and reaches a working first chat through it without docs or config edits — for a
  user starting from the state that path explicitly assumes, not from an assumed
  pre-existing key — while showing simple operator readiness labels and one next
  action;
- provider/model setup stores masked credentials through the three-tier vault
  (surfacing the tier, treating env as read-only), verifies inline with doctor,
  supports local + hosted + OpenAI-compatible custom-endpoint providers, and
  switches providers without config edits;
- repo-maintained profiles seed Settings Central defaults and suggestions only
  after explicit review/confirm;
- the trust spine is surfaced as a feature;
- ADR 0069 and ADR 0075 are accepted;
- `mix allbert.test release.v063`, docs, and warning gates pass with no
  onboarding secret leak or profile-authority regression;
- no known warning/security/doc drift remains.
