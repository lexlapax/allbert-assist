# Allbert v0.49 Vision And Image Generation

Request flow: `docs/plans/v0.49-request-flow.md`

ADRs:
- `docs/adr/0051-provider-capability-preferences.md` (provider capability/
  preference substrate, consumed from v0.48).
- `docs/adr/0042-audio-image-and-media-resource-classes.md` (image/screenshot
  amendments accepted at v0.49 M1 closeout).
- `docs/adr/0047-provider-doctor-contract.md` (v0.49 vision/image-doctor
  amendment).

Status: implemented as `0.49.0`; M7-M10 remediation is complete
(deterministic gate hardened; OpenAI, Gemini, local Ollama, and Gemma 4
vision-candidate live smokes passed; drift closeout done). Ready for operator
manual validation before release tag.

Source: current roadmap sequencing plus the v0.48 provider-capability handoff.
The older archive citation is historical context only.

## Purpose

v0.49 adds the second non-text modality — image **input** (paste/upload +
screenshot analysis) and image **generation** — on the v0.48 provider
capability/preference substrate, without a separate image-provider framework.

The implementation shortcut for v0.49 is that the underlying LLM library,
`ReqLLM`, exposes APIs that should cover both halves:

1. **Vision input** is a multimodal text call: an image is attached as a
   `ReqLLM.Message.ContentPart` (`ContentPart.image_url/1` or
   `ContentPart.image/2` for base64 + `media_type`) on an ordinary chat message to a
   `vision_input`-capable text model. There is **no new provider adapter** —
   v0.49 extends the existing text-generation call path to carry an image part.
2. **Image generation** is a registered action wrapping
   `ReqLLM.generate_image/3` (with the current provider/model IDs proven in
   M1), reading bytes via
   `ReqLLM.Response.image_data/1`. There is **no hand-rolled HTTP adapter** —
   `ReqLLM` owns provider HTTP, credentials, and request shape exactly as text
   generation does today.

M1 must prove those APIs inside Allbert's app-started runtime. A
`mix run --no-start` probe is not sufficient because ReqLLM provider discovery
initializes during application startup; the M1 probe must exercise
`ReqLLM.Providers.list/0`, `ReqLLM.Images.validate_model/1`, and fixture-backed
request paths with no live network in the release lane.

Consequently v0.49 is materially **smaller than v0.48 voice**: it needs no
`Voice.ProviderHTTP`-style substrate, no per-deployment-mode adapter behaviour,
and **no ADR 0011 amendment** (provider network posture is already owned by
`ReqLLM`, as for text). The new work is media resources, image plumbing into
the text path, an image-generation action, permission/operation classes,
workspace paste/upload + rendering, doctor fields, media redaction, catalog
profiles, and evals.

## Readiness Sweep

Status: complete through M10; implemented as `0.49.0`. Deterministic gates,
OpenAI live smoke, Gemini live smoke, local Ollama media smoke, and Gemma 4
local vision-candidate smoke have green evidence. v0.49 is ready for operator
manual validation before release tag.

- **Vision fit.** Vision/image is a modality over the runtime, not a separate
  runtime. Media composes with CLI/workspace/channels through Resource Access,
  Security Central, confirmations, traces, and audits. Generated/observed media
  is descriptive evidence, never authority; nothing auto-promotes to memory.
- **Roadmap fit.** v0.49 follows v0.48 voice and precedes v0.50 Artifacts
  Central; Channel Pack 1 follows as v0.51.
  It proves the v0.48 capability/preference substrate against a second modality
  family (image) and completes the v0.42 media-resource vision (audio in v0.48,
  image/screenshot here; generated media added here).
- **Past-plan fit.** Reuses, in place: ADR 0051 capability/preference + the
  `Models.for/2` resolver; v0.48 profile `media` metadata; the v0.48
  display-only `usage`/`cost` pass-through; the v0.48 workspace media-upload +
  server-side bounds pattern (from the mic flow); v0.48 media trace redaction;
  the capability-aware provider doctor (ADR 0047); and the deterministic
  fake-provider release-gate style. `vision_input`/`image_generation` are
  already in the shipped capability vocabulary (ADR 0051; `ProviderCatalog`).
- **Future-plan fit.** `video_input` stays vocabulary/metadata only (parked).
  v0.50 Artifacts Central is the artifact-management version: a uniform
  content-addressed store covering user uploads plus Allbert-created/found
  media; v0.49 image files remain bounded input/output resources, not the
  canonical artifact store. v0.51 channel plugins may forward image/media
  references but do not own provider authority. Nothing here pre-commits the
  v1.0 freeze.
- **Multimodal bridge fit.** Provider profiles may describe local or online
  multimodal models, but v0.49 implements only the image/screenshot-to-text
  and text-to-image bridge. v0.48 continues to own speech-to-text and
  text-to-speech. Generic audio understanding, video input, and video
  generation stay future work until a later plan adds capability-specific
  actions, resource classes, doctor fields, permissions, and release evidence.
- **ADR fit.** ADR 0042 gains the image/screenshot amendment (the audio portion
  shipped in v0.48). ADR 0047 gains image-doctor fields. ADR 0051 is consumed
  unchanged. **No ADR 0011 amendment** — `ReqLLM` owns provider HTTP for vision
  input and image generation, as it does for text.
- **req_llm-reuse correction.** Unlike v0.48 voice (no req_llm audio support,
  so hand-built HTTP), v0.49 vision/image-gen ride req_llm's native multimodal
  `ContentPart` input and `generate_image/3`. The plan deliberately avoids
  reproducing the voice provider-HTTP/adapter machinery.

## Locked Decisions

These choices are binding for v0.49. Changing any requires an explicit
re-decision here and in the request-flow document before code lands.

| # | Decision | Chosen value | Trade-off if changed |
|---|---|---|---|
| 1 | Provider substrate | Reuse the v0.48 capability-aware provider/preference framework; vision/image models are just providers. | A parallel image-provider stack would duplicate secrets/doctor/onboarding/settings/traces. |
| 2 | Vision-input path | `vision_input` is a **multimodal `ContentPart`** on the existing `ReqLLM` text-generation call; the resolver picks a `vision_input`-capable text profile and the action attaches the image. No new adapter. | A bespoke vision adapter would duplicate req_llm's multimodal support and the text path. |
| 3 | Image-generation path | A registered `generate_image` action wrapping `ReqLLM.generate_image/3` (provider profile → `"provider:model"`); bytes via `Response.image_data/1` → a bounded image resource. No hand-rolled HTTP. | Hand-rolled HTTP would re-introduce the v0.48 ProviderHTTP/ADR-0011 burden req_llm already covers. |
| 4 | Provider scope | req_llm-backed OpenAI/Gemini remote profiles for vision input + image generation, plus deterministic fixture support for tests. M9 accepts opt-in local Ollama `vision_ollama`/`image_ollama` profiles only through the existing ReqLLM/OpenAI-compatible boundary; no local ProviderHTTP adapter is added. | A non-ReqLLM local image provider would re-introduce ProviderHTTP + an ADR 0011 amendment; silently leaving local media unexamined would miss the intended multimodal-provider shape. |
| 5 | Media source | `image://capture/<id>` and `screen://capture/<id>` identify **operator-supplied** paste/upload media — NOT autonomous OS screen capture. Vision analysis may also target a browser screenshot surfaced through the v0.43 `browser_screenshot` read operation / `screenshot_ref`; v0.49 implements this through `analyze_browser_screenshot`, which resolves only an existing `cache://browser/...` screenshot artifact and maps it into the image-input path. | Autonomous screen capture is a large new authority (like live mic) and is out of v0.49 scope. |
| 6 | Image normalization | **Constrain requests to provider-accepted formats/dimensions/size; for generated outputs, sniff returned bytes, store with the actual safe image extension, and validate against Allbert's system-safe generated-image formats plus byte/pixel bounds.** No resize/convert binary dependency in v0.49. | A resize/convert helper (vix/ImageMagick-class) adds a dependency; treating a provider's requested output format as returned-byte authority breaks when providers return another safe image MIME. |
| 7 | Permission floors | `image_input` (operator-supplied) floor `:allowed` with redaction; `image_generate` floor is derived from the resolved profile's `media.deployment_mode` (fake → `:allowed`; remote/unresolved/unknown → `:needs_confirmation`), reusing the v0.48 `voice_floor` mechanism — which defaults everything that is not a fake/bundled-local mode to `:needs_confirmation` (fail-safe-to-confirm, never auto-`:allowed`). | Treating image generation as `:allowed` would let billable provider calls run without confirmation. |
| 8 | Cost visibility | Display-only pass-through `usage`/`cost` from `ReqLLM.Response` (`source: :unavailable` when absent); no aggregation/enforcement. | A spend subsystem would expand v0.49 beyond the modality. |
| 9 | Test lane | `release.v049` exercises real action/resolver/redaction paths through deterministic req_llm `Req.Test` fixtures + image fixtures; fake provider is fixture support. Opt-in `.env` live smokes (OpenAI/Gemini) are manual before tag. | Live provider tests are credentialed/non-deterministic; fake-only misses the product contract. |
| 10 | Capability semantics | Capability names are routing predicates; media modalities/formats/limits live in profile `media` metadata and never grant permission. | Coupling routing to one vendor's API shape; metadata-as-authority. |

## Capability And Provider Contract

v0.49 implements the `vision_input` and `image_generation` capabilities from
the shipped vocabulary (ADR 0051). Catalog/settings gain vision + image
profiles (mirroring the v0.48 voice profiles); `providers` reuses the existing
`openai`/`gemini` entries (secret refs) — no new provider type:

The model IDs below are the M1-selected defaults. They were pinned against
app-started ReqLLM provider/model discovery, current provider docs, and
deterministic request-fixture scope; later closeout still runs the stale-ID
drift scan before release.

```yaml
model_profiles:
  vision_openai:
    provider: openai
    model: gpt-5.2                       # verified at M1
    capabilities: [text_generation, vision_input]
    media: { input_modalities: [text, image], output_modalities: [text],
             deployment_mode: remote_credentialed,
             image_formats_supported: [png, jpeg, webp, gif],
             max_image_bytes: 20971520, max_image_pixels: 33177600 }
  vision_gemini:
    provider: gemini
    model: gemini-2.5-flash              # verified at M1
    capabilities: [text_generation, vision_input]
    media: { input_modalities: [text, image], output_modalities: [text],
             deployment_mode: remote_credentialed,
             image_formats_supported: [png, jpeg, webp], max_image_bytes: 20971520 }
  vision_ollama:
    provider: local_ollama
    model: qwen3-vl:8b                   # accepted at M9 through Ollama OpenAI-compatible docs
    capabilities: [text_generation, vision_input]
    media: { input_modalities: [text, image], output_modalities: [text],
             deployment_mode: local_endpoint,
             image_formats_supported: [png, jpeg, webp], max_image_bytes: 10485760 }
  vision_fake:
    provider: fake_media
    model: fixture-vision
    capabilities: [vision_input]
    media: { input_modalities: [image], output_modalities: [text],
             deployment_mode: fake, image_formats_supported: [png] }
  image_openai:
    provider: openai
    model: gpt-image-1.5                 # verified with validate_model/1 at M1
    capabilities: [image_generation]
    media: { input_modalities: [text], output_modalities: [image],
             deployment_mode: remote_credentialed, image_formats_supported: [png] }
  image_gemini:
    provider: gemini
    model: gemini-3.1-flash-image       # verified with validate_model/1 at M1
    capabilities: [image_generation]
    media: { input_modalities: [text], output_modalities: [image],
             deployment_mode: remote_credentialed, image_formats_supported: [png] }
  image_ollama:
    provider: local_ollama
    model: x/z-image-turbo              # accepted at M9 through Ollama experimental OpenAI-compatible image endpoint
    aliases: [x/z-image-turbo:latest, z-image-turbo]
    capabilities: [image_generation]
    media: { input_modalities: [text], output_modalities: [image],
             deployment_mode: local_endpoint, image_formats_supported: [png] }
  image_fake:
    provider: fake_media
    model: fixture-image
    capabilities: [image_generation]
    media: { input_modalities: [text], output_modalities: [image],
             deployment_mode: fake, image_formats_supported: [png] }

model_preferences:
  capabilities:
    vision_input: [vision_openai, vision_gemini]
    image_generation: [image_openai, image_gemini]
```

The resolver (`Models.for/2`, `candidates_for/2`) selects by capability with the
same enable/capability validation and ranked candidate list as v0.48.

For `image_generation`, `image_formats_supported` on the selected profile
constrains the outbound requested format. The generated file itself is governed
by returned bytes: `generate_image` sniffs the image header, writes the local
file with the actual safe extension, records sniffed MIME/format metadata, and
then runs `ImageBounds.validate_generated/3` with Allbert's system-safe
generated-output formats (`png`, `jpeg`/`jpg`, `webp`, `gif`) plus the existing
global/profile byte and pixel limits. A provider returning JPEG bytes to a PNG
request is therefore accepted only as a bounded JPEG output; a provider
returning an unsupported or unparsable image is still rejected.
`Models.for/2` returns the resolved profile; `candidates_for/2` returns the
ranked candidate list. The one-bounded-pass call-failure fallback (retryable →
next candidate; non-retryable → stop) with per-profile floor re-authorization is
**not** built into the resolver — it is implemented in the consuming action,
mirroring `transcribe_voice` (which re-authorizes the floor per candidate and
recurses to the next `candidates_for/2` profile only on a retryable provider
error). Fixture-only fake profiles must not be operator
default preferences or release-validation targets. `fake_media` is a sample
fixture-provider label; if the catalog already has a different fixture provider
shape, M1 should use that existing test seam instead of adding an
operator-visible provider type.

## Multimodal Bridge Profile Boundary

v0.49 may describe a local or online model as multimodal in profile metadata,
but there is intentionally no catch-all `multimodal` capability and no
all-purpose media router. Each media direction remains a named capability with
its own action/resource/security contract:

| Capability | Direction | v0.49 status | Local/online note |
|---|---|---|---|
| `speech_to_text` | audio -> text | Shipped in v0.48; unchanged here. | Local Allbert voice runtime, OpenAI, and Gemini remain v0.48-owned paths. |
| `text_to_speech` | text -> audio | Shipped in v0.48; unchanged here. | Local Allbert voice runtime, OpenAI, and Gemini remain v0.48-owned paths. |
| `vision_input` | image/screenshot -> text | Implemented in v0.49. | Remote OpenAI/Gemini through ReqLLM; optional `vision_ollama` uses the existing local Ollama OpenAI-compatible endpoint. |
| `image_generation` | text -> image | Implemented in v0.49. | Remote OpenAI/Gemini through ReqLLM; optional `image_ollama` uses Ollama's experimental OpenAI-compatible image endpoint; fixture support is test-only. |
| `video_input` | video/sampled frames -> text | Vocabulary/metadata only. | Requires a future plan before any local or online provider can execute it. |
| video generation or generic audio understanding | text/media -> video or audio -> non-transcript reasoning | Not in v0.49. | Requires future capability names, permissions, doctor fields, resource classes, and release evidence. |

Gemma 4 Ollama tags that advertise text+image input (for example
`gemma4:e4b`) are local `vision_input` candidates, not image-generation models.
The v0.49 live smoke supports `ALLBERT_V049_VISION_MODEL=gemma4:e4b` to prove a
named installed candidate through the same Allbert profile/request path without
making it the default `vision_ollama` selection.

## Media Resource And Authority Model

v0.49 accepts the image/screenshot slice of ADR 0042:

- `image://capture/<id>` and `screen://capture/<id>` identify operator-supplied
  paste/upload media (Locked Decision 5). The ids are opaque, Allbert-Home-
  local, and never provider-selected. v0.49 does **not** add autonomous OS
  screen capture.
- Vision analysis may also target a browser screenshot surfaced through the
  v0.43 `browser_screenshot` **read operation** / `screenshot_ref`.
  `analyze_browser_screenshot` resolves only an existing `cache://browser/...`
  artifact from the Browser cache, maps it to `screen://capture/<id>` metadata,
  and invokes the existing `direct_answer` vision path. It does not capture the
  OS screen or grant screenshot authority from the resource id itself.
- Image input is constrained (Locked Decision 6): format ∈ profile
  `image_formats_supported`, size ≤ `min(vision.media.max_bytes, profile
  max_image_bytes)`, dimensions ≤ `min(vision.media.max_pixels, profile
  max_image_pixels)`; oversized/
  unsupported is denied **before** the provider call. No resize/convert.
- Media content is default-off for retention and redacted from traces by
  default. Traces record bounded metadata only: resource URI, byte size,
  dimensions, MIME type, provider profile, content hash, and redaction status.
  Raw image/screenshot bytes never appear in traces, audits, diagnostics,
  LiveView assigns, or CLI output.

New permission classes:

- `:image_input` — floor `:allowed` (operator-supplied content, redacted).
- `:image_generate` — floor derived from the resolved profile's
  `media.deployment_mode`, reusing the v0.48 `voice_floor` mechanism: fake →
  `:allowed`; remote/unresolved/unknown → `:needs_confirmation`
  (fail-safe-to-confirm, never auto-`:allowed`).

New operation classes / origins:

- origin kind `:image_input`
- operation `:image_input`
- operation `:image_generate`

## Workspace Media

- Paste/upload crosses the web boundary via a Phoenix LiveView binary upload;
  `:image_input` size/type/dimension bounds are enforced **server-side**
  (client-reported values are untrusted), reusing the v0.48 mic-upload pattern.
- Rendering uses the existing validated workspace surface/catalog (provenance,
  fallback text); **no model-generated UI code** is executed.
- Generated images are written to a bounded Allbert-Home media path; retention
  is default-off and operator-removable.

## Provider Doctor

`doctor` reuses ADR 0047's redacted envelope and adds, for vision/image
profiles, additive fields only. New ADR 0047 v0.49 fields:
`:image_input_supported` (ADR 0047 reserved), `:image_generation_supported`,
`:image_formats_supported`. Reused v0.48 doctor fields (not new):
`:provider_usage_metadata_available`, `:fixture_probe_ok`. Dispatched via the
existing `mix allbert.model doctor <profile>`. Read-only; never emits raw
URLs/secrets/media paths.

## Expected Inputs

- **ADR 0051 + `Models.for/2`** capability resolver (v0.48).
- **`ReqLLM`** — multimodal `ContentPart` image input; `generate_image/3` +
  `Response.image_data/1`/`image_url/1`; app-started provider/model discovery
  and `Images.validate_model/1` for image-generation IDs.
- **v0.48 media metadata + display-only usage/cost** pass-through.
- **v0.48 workspace media-upload + server-side bounds** pattern (mic flow).
- **v0.48 media trace redaction** helpers (extended to image URIs/bytes).
- **v0.43 browser screenshot** read operation / `screenshot_ref` (mapped into
  an image resource for analysis; no existing screenshot resource URI).
- **v0.41 development-lane contract** — every milestone carries a lane.

## Operator-Visible Win

An operator can paste or upload a screenshot/image, ask Allbert about it, and
generate diagrams/images on demand — with visible provider/cost metadata and
default-off retention, and with binary content kept out of traces.

First-time walkthrough:

1. Operator (vision enabled) pastes a screenshot into `/workspace` and asks
   "what's failing in this stack trace?".
2. The upload becomes a bounded `image://capture/<id>`; size/type/dimensions
   validated server-side.
3. The vision flow resolves `vision_input` to a capable text profile and sends
   the image as a `ContentPart` on the text call; the answer renders inline.
4. Operator asks "generate a simple architecture diagram"; `generate_image`
   resolves `image_generation`, confirms (remote floor), runs
   `ReqLLM.generate_image/3`, writes a bounded media resource, and renders it
   with display-only provider/cost metadata.
5. Traces hold only redacted metadata; nothing auto-promotes to memory.

## Planned Interfaces

- Resources `image://capture/<id>`, `screen://capture/<id>`.
- `vision_input` plumbed into the existing text-generation call path
  (`ContentPart` attachment); no new module behaviour.
- Registered action `generate_image` wrapping `ReqLLM.generate_image/3`.
- Workspace image paste/upload + render surface.
- `doctor` image fields via `mix allbert.model doctor <profile>`.
- Settings: `vision.*`/`image.*` namespace (`schema_version: 1`, ADR 0046) —
  enable toggle, retention, bounds, redaction; capability preferences for
  `vision_input`/`image_generation`.
- `mix allbert.test release.v049`.

## Security Posture

- Media input is input modality only; it does not change action authority.
- Provider capability metadata and doctor success never grant permission.
- Operator-supplied media only; no autonomous screen capture.
- Image generation is effectful/provider-backed and confirmation-gated for
  remote providers; cost/usage is display-only.
- Binary image/screenshot content is bounded, default-off for retention, and
  absent from traces/audits/assigns/CLI.
- Workspace rendering executes no model-generated UI code.
- No new attack surface beyond the image resource classes + two permission
  classes; provider HTTP/credentials remain req_llm's, as for text.

## Non-Goals

- No video ingestion, sampled-frame analysis, or video generation.
- No autonomous OS screen capture.
- No arbitrary remote media fetch.
- No permanent media retention by default.
- No canonical content-addressed artifact store in v0.49. Uploaded, generated,
  or browser-found media may carry hashes and redacted metadata, but durable
  deduplication, lifecycle policy, provenance, and cross-surface artifact lookup
  are deferred to the v0.50 Artifacts Central milestone.
- No model-generated UI code.
- No non-ReqLLM image provider (and therefore no ProviderHTTP/ADR 0011 work)
  in v0.49.
- No image resize/transcode dependency in v0.49 (constrain + reject instead).

## Development-Lane Annotations

Per ADR 0049. The deterministic `release.v049` lane uses fixture images, fake
vision/image providers (req_llm `Req.Test`), and disposable `ALLBERT_HOME`.
That lane remains required, but the post-M6 release audit found it is not
sufficient release authority by itself. Before the release tag, v0.49 also
requires env-gated live smokes for OpenAI and Gemini, plus a local Ollama
decision/proof through the existing ReqLLM/OpenAI-compatible boundary.

| M | Parallel workstreams | Serial barriers | Gate evidence + rejoin |
|---|---|---|---|
| M1 | ADR 0042 image amendment + ADR 0047 image-doctor fields; ADR 0051 capability consumption; catalog vision/image profiles + `vision.*`/`image.*` settings; app-started ReqLLM model probe | catalog/settings validation, schema_version, model-ID refresh | focused catalog/settings tests; app-started ReqLLM probe evidence; docs reconciled before M2 |
| M2 | `image://`/`screen://` resources; `:image_input`/`:image_generate` permission + operation classes + floors; media redaction for image URIs/bytes; image bounds (constrain/reject) | resource URI normalization, policy floors, redactor | ResourceURI/OperationClass/Security.Policy/Runtime.Redactor/ImageBounds tests; browser_screenshot reconciliation |
| M3 | vision-input plumbing (`ContentPart` on the text path) + resolver `vision_input`; workspace paste/upload + server-side bounds + render | text-path multimodal context payload; LiveView upload (`liveview_serial`) | vision flow tests through fake provider; workspace upload/render tests; browser smoke if UI changes; v0.50 artifact-store forward pin |
| M4 | `generate_image` action wrapping `ReqLLM.generate_image/3`; deployment-mode floor + confirmation; display-only cost; bounded generated-media resource | image-gen action; confirmation; media write bounds (`home_fs_serial`) | generate_image fixture tests; cost-display test; confirmation test |
| M5 | security evals; operator/developer/security docs; `release.v049`; version/changelog closeout | `EvalInventory` (`:v049`), release lane | security eval lane; `mix allbert.test release.v049`; full release gate before tag |
| M6 | browser screenshot analysis remediation + numbered operator validation checklist | Browser cache refs and `direct_answer` image-input bridge | browser screenshot bridge tests; operator checklist docs; `git diff --check` |
| M7 | deterministic gate hardening for `allbert.test commit` | changed-file inspection must be injectable/stubbed | task test covers clean, mixed, and docs-only trees; `release.v049` no longer depends on the live worktree |
| M8 | env-gated v0.49 live smoke harness | real provider calls require disposable `ALLBERT_HOME`, explicit env opt-in, and no secret printing | OpenAI and Gemini doctor -> vision input -> image generation -> confirmation approval evidence |
| M9 | local Ollama multimodal profile decision | only accepted through ReqLLM/OpenAI-compatible local endpoint; no non-ReqLLM ProviderHTTP work in v0.49 | local Ollama vision/image probe evidence or explicit deferral record |
| M10 | release revalidation and drift closeout | clean tracked tree; no stale green evidence after red gate | `release.v049`, full `release`, live smokes, status/doc/changelog reconciliation |

## Milestones

### M1: Capability/Provider Profiles, ADR Amendments

Status: implemented.

Closeout evidence (2026-06-07):

- `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/settings/provider_catalog_test.exs apps/allbert_assist/test/allbert_assist/settings/model_preferences_test.exs apps/allbert_assist/test/allbert_assist/settings_test.exs`
  passed with 54 tests and 0 failures.
- `MIX_ENV=test mix compile --warnings-as-errors` passed.
- App-started ReqLLM probe in disposable `ALLBERT_HOME` verified provider
  registration plus selected defaults:
  `vision_openai` → `openai:gpt-5.2`, `vision_gemini` →
  `google:gemini-2.5-flash`, `image_openai` → `openai:gpt-image-1.5`,
  `image_gemini` → `google:gemini-3.1-flash-image`.

Implementation tasks:

- Write the ADR 0042 image/screenshot amendment (`image://`, `screen://`,
  `generate_image` action contract, bounds, retention, binary redaction) and
  the ADR 0047 image-doctor fields; consume ADR 0051 unchanged.
- Add vision/image `model_profiles` (including fixture-only fakes) + `media`
  metadata and the `vision_input`/`image_generation` preference lists to the
  catalog/settings. The current `ProviderCatalog` media validation must be
  extended beyond audio keys to accept and validate
  `image_formats_supported`, `max_image_bytes`, and `max_image_pixels`.
- Add the `vision.*`/`image.*` settings fragment (`schema_version: 1`) with
  defaults (vision off by default), safe-write keys, and invariants. Extend
  `model_preferences` schema/defaults beyond the current text/STT/TTS shape so
  `vision_input` and `image_generation` are first-class capability
  preferences.
- Verify the vision/image-generation model IDs against current OpenAI/Gemini
  docs and app-started ReqLLM behavior. The proof must run after application
  startup and cover `ReqLLM.Providers.list/0`,
  `ReqLLM.Images.validate_model/1`, and fixture-backed request paths. Treat
  `ReqLLM.Images.supported_models/0 == []` under `mix run --no-start` as an
  initialization artifact, not release evidence.

Acceptance criteria:

- Catalog profiles validate `vision_input`/`image_generation` + `media`.
- Settings invariants enforced at write time; preferences resolve through
  `Models.for/2`.
- App-started ReqLLM probe records the selected model IDs and proves the
  fixture paths without live network.
- ADR 0042 image amendment + ADR 0047 image fields accepted.

Automated tests: catalog/settings validation; resolver capability selection for
`vision_input`/`image_generation`.

Request-flow handoff: document the capability/profile shape.

### M2: Media Resources, Permissions, Redaction, Bounds

Status: implemented.

Closeout evidence (2026-06-07):

- `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/resources/resource_uri_test.exs apps/allbert_assist/test/allbert_assist/resources/operation_class_test.exs apps/allbert_assist/test/allbert_assist/security/permission_gate_test.exs apps/allbert_assist/test/allbert_assist/runtime/redactor_test.exs apps/allbert_assist/test/allbert_assist/settings_test.exs apps/allbert_assist/test/allbert_assist/resources/image_bounds_test.exs`
  passed with 84 tests and 0 failures.
- `MIX_ENV=test mix compile --warnings-as-errors` passed.
- The initial sandboxed focused-test run failed only with the known Mix PubSub
  local socket `:eperm`; the same command passed after running with local
  socket permission.

Implementation tasks:

- Add `image://capture/<id>` + `screen://capture/<id>` normalization/derivation
  + origin kind `:image_input` in `ResourceURI` (mirroring the `mic://capture`
  derivation/origin-kind seam); keep `screen://` inert and reserve browser
  screenshot analysis for the explicit `analyze_browser_screenshot` bridge.
- Add `:image_input`/`:image_generate` permission classes + operation classes +
  the deployment-mode floor in `OperationClass` and `Security.Policy` (reuse
  the v0.48 mechanism).
- Extend `Runtime.Redactor` media redaction to image resource URIs/paths and
  ensure raw bytes never enter traces; add the constrain/bounded-reject image
  checks.

Acceptance criteria: malformed media URIs rejected; floors match Locked
Decision 7; oversized/unsupported image denied before provider call; redaction
covers image bytes/paths.

Automated tests: ResourceURI, OperationClass, Security.Policy, Redactor, bounds.

Request-flow handoff: document media authority invariants.

### M3: Vision Input Flow + Workspace Media

Status: implemented.

Closeout evidence (2026-06-07):

- `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/resources/image_metadata_test.exs apps/allbert_assist/test/allbert_assist/resources/image_bounds_test.exs apps/allbert_assist/test/allbert_assist/actions/intent/direct_answer_test.exs apps/allbert_assist/test/allbert_assist/runtime/trace_test.exs apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs`
  passed with 83 tests and 0 failures after rerunning with local socket
  permission for the known Mix PubSub sandbox `:eperm`.
- After hardening truncated-JPEG parsing, `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/resources/image_metadata_test.exs`
  passed with 3 tests and 0 failures under the same local socket permission.
- `MIX_ENV=test mix compile --warnings-as-errors` passed.
- `MIX_ENV=test mix test apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs:1812`
  passed with 1 test and 0 failures after the upload-helper warning fix.
- Browser smoke on `http://localhost:4059/workspace` with disposable
  `ALLBERT_HOME=/private/tmp/allbert-v049-m3-browser-home`: image control rendered
  disabled with `vision.enabled=false`; after
  `mix allbert.settings set vision.enabled true` and reload, the file input was
  present, enabled, accepted `image/*`, and composer controls did not overlap.

Implementation tasks:

- Plumb image `ContentPart` into the text-generation call (model_runtime /
  the chat path) for `vision_input`-resolved profiles. This is a **structural
  change**, not a pure additive seam: `DirectAnswer.ReqLLMAnswerer` currently
  passes a plain string prompt to `ReqLLM.generate_text/3`, and the repo has no
  existing `ReqLLM.Context`/`ContentPart` usage to mirror. M3 restructures the
  vision-resolved path to build a `ReqLLM.Context` content-part message list
  using `Context.user/2` with `ContentPart.text/1` plus
  `ContentPart.image/2` for operator-supplied bytes. `Context.with_image/4` is
  URL-oriented and may be used only when the input is already a provider-safe
  image URL. Keep the text-only path unchanged; guard current direct-answer
  text behavior with a regression test. Submit the answer as normal runtime
  output.
- Add workspace image paste/upload (LiveView binary upload, server-side
  bounds) and validated rendering (no generated UI code).
- Support analyzing a browser screenshot referenced via the v0.43
  `browser_screenshot` read operation / `screenshot_ref` by resolving the
  Browser cache ref and feeding it into the existing `direct_answer`
  `image_inputs` path with `source: :browser_screenshot` provenance.

Post-implementation audit remediation (2026-06-07): the M5 closeout audit
found that the docs promised browser screenshot analysis while only inert
`screen://` scaffolding had shipped. v0.49 now closes that gap with
`analyze_browser_screenshot`, a `Cache.fetch/1` resolver for existing
`cache://browser/...` artifacts, a real browser-action test, and the
`vision-browser-screenshot-analysis-001` security eval row.

Acceptance criteria: a fixture image/screenshot is analyzed via the fake
vision provider; oversized/unsupported denied; raw bytes absent from traces/
assigns; rendering executes no model-generated UI code.

Automated tests: vision flow (fake provider) tests; workspace upload/render
tests; redaction tests.

Operator validation:

```sh
export ALLBERT_HOME="$(mktemp -d /tmp/allbert-v049-m3.XXXXXX)"
mix allbert.settings set vision.enabled true
PORT=4049 mix phx.server   # /workspace: paste a screenshot and ask about it
```

Request-flow handoff: document the vision-input data flow.

### M4: Image Generation Action

Status: implemented.

Implementation tasks:

- Add the `generate_image` registered action wrapping `ReqLLM.generate_image/3`
  (resolved `image_generation` profile → `"provider:model"`); read bytes via
  `Response.image_data/1`; write a bounded media output and return the local
  image file plus redacted metadata for runtime/workspace rendering.
- Apply the deployment-mode floor + confirmation; return display-only
  `usage`/`cost` pass-through.
- Replicate the v0.48 per-action one-bounded-pass call-failure fallback in the
  `generate_image` action (since it is not in the resolver): on a retryable
  provider error, advance once to the next `candidates_for(:image_generation)`
  profile and re-authorize the deployment-mode floor per candidate, mirroring
  `transcribe_voice`; non-retryable → stop.

Acceptance criteria: image generation runs through the fake provider in the
gate; remote profiles confirm; cost/usage is pass-through display-only;
generated media is bounded and redacted in traces/action metadata.

Automated tests: generate_image fixture tests; confirmation/floor test;
cost-display test.

Request-flow handoff: document the image-generation flow.

Closeout evidence (2026-06-07):

- `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/actions/generate_image_test.exs apps/allbert_assist/test/allbert_assist/actions/registry_test.exs`
  passed with 14 tests and 0 failures after rerunning with local socket
  permission for the known Mix PubSub sandbox `:eperm`.
- `MIX_ENV=test mix compile --warnings-as-errors` passed.
- Commit `fc9ecc83` registered `generate_image` as internal/resumable, added
  fake/Req.Test-backed generation, remote confirmation resume, one bounded
  retry, bounded generated-file writes under Allbert Home, and redacted image
  metadata with display-only usage/cost.

### M5: Security Evals, Docs, Closeout

Status: implemented.

Implementation tasks:

- Add the eval rows from `v0.49-request-flow.md` to `EvalInventory` (milestone
  `:v049`, eval surface `:vision_modality`) with backing assertions.
- Ship `mix allbert.test release.v049` (fake providers + fixture images, no
  live network); secret scan; reproducible evidence to
  `<ALLBERT_HOME>/release_evidence/v049/`.
- Operator vision/image guide; developer vision/provider guide; security-
  hardening media section. Run the M5 Closeout Discipline.

Acceptance criteria: all evals pass on fixtures while exercising real action/
resolver/redaction paths; `release.v049` green + reproducible; docs agree.

Closeout evidence (2026-06-07):

- `MIX_ENV=test mix test apps/allbert_assist/test/security/v049_vision_modality_eval_test.exs apps/allbert_assist/test/security/security_eval_case_test.exs apps/allbert_assist/test/mix/tasks/allbert_test_task_test.exs`
  passed with 17 tests and 0 failures.
- `MIX_ENV=test mix allbert.test release.v049` passed with image policy/core
  (`99 tests, 0 failures`), vision input (`11 tests, 0 failures`), image
  browser screenshot bridge (`12 tests, 0 failures`), image generation action
  (`15 tests, 0 failures`), workspace image input (`65 tests, 0 failures`),
  vision security eval (`19 tests, 0 failures`), and a clean v0.49 media
  secret scan that includes `cache/browser`. Evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v049/p0-11013/home/release_evidence/v049/release-v049-1780886771.json`.
- Final `MIX_ENV=test mix allbert.test release` passed with static compile,
  deps, format, Credo strict, 1,525 core tests, 123 web tests, 197 StockSage
  tests, 12 channel-plugin tests, and Dialyzer. Evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release/p0-1218/home/release_evidence/gates/release-2026-06-08T03_08_24Z.json`.
- `MIX_ENV=test mix compile --warnings-as-errors` passed.

#### M5 Closeout Discipline

Single-purpose closeout commit; complete only when every box is checked:

1. `mix.exs` version → `0.49.0`.
2. child app mix versions → `0.49.0`.
3. `AllbertAssist.App.CoreApp.version/0` → `"0.49.0"`.
4. `CHANGELOG.md` `## v0.49.0` section citing the evals + `release.v049`.
5. `mix allbert.test` help lists `release.v049`.
6. plan + request-flow statuses → implemented as `0.49.0`.
7. ADR 0042 image amendment + ADR 0047 image fields accepted.
8. roadmap, vision, agent-context-map, README, operator/developer/security docs
   match implementation; v0.51 still points to the shared media substrate.
9. stale model-ID/doc drift scan completed against the provider catalog and
   code (where stale vendor IDs such as `gpt-4o` / `gemini-3.5-flash` may
   linger — the v0.49 docs themselves use placeholders, not those IDs); verify
   the v0.49 selected IDs (`gpt-5.2`, `gemini-2.5-flash`,
   `gpt-image-1.5`, `gemini-3.1-flash-image`) against app-started ReqLLM
   discovery.
10. `git diff --check` clean for the doc/code closeout diff.

Operator closeout test:

```sh
mix allbert.test release.v049
# evidence: <ALLBERT_HOME>/release_evidence/v049/release-v049-<ts>.json
```

### Post-M6 Release-Stop Remediation Plan

Status: open. The post-M6 release audit blocks the v0.49 tag until every item
below has fresh evidence from a clean tracked source tree. Earlier green
`release.v049` evidence remains useful implementation evidence, but it no
longer grants release authority after the later red gate.

Release blockers:

1. **No live-provider release evidence.** The deterministic `release.v049` lane
   uses fake/fixture providers by design. v0.49 still needs real OpenAI and
   Gemini proof for vision input and image generation before the tag.
2. **Worktree-sensitive gate behavior.** `allbert.test commit` currently reads
   the live git working tree, so a dirty docs-only tree can steer the test into
   a different branch than the one the unit test expects. `docs()` returning
   `:ok` is necessary but not sufficient; the test must not depend on ambient
   repository state.
3. **Local multimodal provider decision missing.** Allbert already has a local
   Ollama text provider. Current Ollama docs expose image input through local
   OpenAI-compatible chat completions and experimental image generation through
   `/v1/images/generations`; v0.49 must either prove a ReqLLM-compatible local
   profile or record why it remains deferred.
4. **Status drift after a red gate.** Plan/request-flow/roadmap/changelog
   status must not imply tag readiness until the deterministic and live lanes
   are green again.

#### M7: Gate Determinism Hardening

Status: implemented.

Implementation tasks:

- Introduce a test seam for changed-file inspection in `Mix.Tasks.Allbert.Test`
  (for example an injected `changed_files` callback, app env, or pure helper
  that can be exercised directly).
- Cover clean, mixed code/docs, and docs-only dirty tree cases without reading
  the real worktree in `allbert_test_task_test.exs`.
- Keep the docs-only optimization, but ensure `run(["commit"])` returns a
  stable value and still prints non-release-evidence guidance.

Acceptance criteria:

- `MIX_ENV=test mix test apps/allbert_assist/test/mix/tasks/allbert_test_task_test.exs`
  passes from a clean tree and from an unrelated dirty tree.
- `MIX_ENV=test mix allbert.test release.v049` reaches the same branch behavior
  regardless of unrelated uncommitted docs.

Closeout evidence (2026-06-08):

- Added the `:gate_changed_files` test seam for commit-gate branch selection.
- `MIX_ENV=test mix test apps/allbert_assist/test/mix/tasks/allbert_test_task_test.exs`
  passed with 9 tests and 0 failures after rerunning with local socket
  permission for the known Mix PubSub `:eperm` sandbox condition.

#### M8: Live OpenAI/Gemini Vision Smoke Harness

Status: implemented; live provider execution still pending M10/operator
credentialed validation.

Implementation tasks:

- Add `scripts/v049_vision_live_smoke.exs`, guarded by
  `ALLBERT_V049_LIVE_SMOKE=1`, and requiring a disposable `ALLBERT_HOME`.
- Support `ALLBERT_V049_PROVIDER=openai|gemini` and an explicit input image
  path such as `ALLBERT_V049_IMAGE=/path/to/image.png`.
- Configure credentials through Settings Central secrets, never command-line
  arguments or printed output.
- Run this flow: provider/profile doctor -> real `vision_input` analysis of the
  supplied image -> real `generate_image` -> confirmation approval -> output
  metadata/redaction scan.
- Record model/profile names, redacted doctor summary, generated resource
  metadata, confirmation id/status, and redaction scan result without raw media
  bytes or secrets.

Acceptance criteria:

- OpenAI smoke passes or identifies the exact live provider/model failure for
  `vision_openai` and `image_openai`.
- Gemini smoke passes or identifies the exact live provider/model failure for
  `vision_gemini` and `image_gemini`.
- If any selected model ID is stale, update the provider catalog, docs, tests,
  and validation evidence before M10.

Closeout evidence (2026-06-08):

- Added `scripts/v049_vision_live_smoke.exs` with explicit
  `ALLBERT_V049_LIVE_SMOKE=1` opt-in, disposable `ALLBERT_HOME` enforcement,
  Settings Central secret storage, real `doctor_model_profile`, real
  `direct_answer` vision input, real `generate_image` confirmation approval,
  redacted evidence JSON, and evidence redaction checks.
- `mix run --no-start scripts/v049_vision_live_smoke.exs` refused to run
  without `ALLBERT_V049_LIVE_SMOKE=1` before provider setup, as intended.
- `ALLBERT_V049_LIVE_SMOKE=1 mix run --no-start scripts/v049_vision_live_smoke.exs`
  refused to run without a disposable `ALLBERT_HOME` before provider setup, as
  intended.

#### M9: Local Ollama Multimodal Decision

Status: implemented; local live execution remains operator/environment
dependent because it requires installed Ollama media models.

Implementation tasks:

- Probe current local Ollama through the existing `local_ollama`
  `openai_compatible` provider and `http://localhost:11434/v1` base URL.
- For vision input, test a local vision model through the same ReqLLM
  `ContentPart` path used by OpenAI/Gemini. Candidate model IDs must come from
  current Ollama docs and local `ollama list`/`ollama show` evidence.
- For image generation, test whether `ReqLLM.generate_image/3` can target
  Ollama's experimental OpenAI-compatible `/v1/images/generations` shape and
  return bytes through `ReqLLM.Response.image_data/1`.
- Add `vision_ollama` and/or `image_ollama` profiles only when the proof stays
  inside ReqLLM/OpenAI-compatible provider routing and Settings Central. Do not
  add a bespoke local media HTTP adapter in v0.49.
- Keep video out of v0.49. No first-party Ollama video generation endpoint was
  identified in the current docs; video needs a later capability/resource/
  permission/doctor/release-evidence plan.

Acceptance criteria:

- Either local Ollama vision/image profiles ship with focused tests, doctor
  evidence, and live smoke instructions, or the plan records the exact
  unsupported seam and defers local image execution without weakening the
  OpenAI/Gemini release bar.

Closeout evidence (2026-06-08):

- Current Ollama OpenAI-compatible docs expose image input on
  `/v1/chat/completions` with `qwen3-vl:8b`, and experimental image generation
  on `/v1/images/generations` with `x/z-image-turbo`; no first-party video
  generation endpoint was identified.
- Added opt-in `vision_ollama` and `image_ollama` profiles through the existing
  `local_ollama` `openai_compatible` provider. The default
  `vision_input`/`image_generation` preference lists remain OpenAI/Gemini only,
  so operators must explicitly select the local profiles for local media
  validation.
- Extended `scripts/v049_vision_live_smoke.exs` to accept
  `ALLBERT_V049_PROVIDER=ollama`; it doctors `vision_ollama` and
  `image_ollama`, then runs the same vision -> image generation -> confirmation
  -> redacted evidence path as the remote smokes.

#### M10: Release Revalidation And Drift Closeout

Status: implemented and ready for operator manual validation before release
tag. Gemini image generation has passed with the validation key; local Ollama
and Gemma 4 vision-candidate smokes are also green.

Implementation tasks:

- Start from a clean tracked tree except explicitly ignored/untracked operator
  artifacts.
- Re-run focused M7-M9 tests, `MIX_ENV=test mix allbert.test release.v049`, and
  the full `MIX_ENV=test mix allbert.test release`.
- Run the OpenAI and Gemini live smoke harness. Run the local Ollama smoke when
  `qwen3-vl:8b` and `x/z-image-turbo` are installed.
- Reconcile v0.49 status in the plan, request-flow, roadmap, changelog, and
  operator/developer docs based on the new evidence.

Acceptance criteria:

- Deterministic release evidence is green after the gate-hardening fix.
- Real-provider smoke evidence exists for OpenAI and Gemini, including the live
  selected model IDs or their remediated replacements.
- Local Ollama profiles are present as opt-in v0.49 local media options, with
  structural profile proof and operator live-smoke evidence when the local
  models are installed.
- M10 moves v0.49 back to "ready for operator manual validation before release
  tag" only after focused tests, warning gate, OpenAI/Gemini live smokes, local
  Ollama live smoke, Gemma 4 vision-candidate smoke, and release gates pass.

Closeout evidence (2026-06-08):

- Focused M7-M9 revalidation passed:
  `MIX_ENV=test mix test apps/allbert_assist/test/mix/tasks/allbert_test_task_test.exs apps/allbert_assist/test/allbert_assist/settings/provider_catalog_test.exs apps/allbert_assist/test/allbert_assist/settings/model_preferences_test.exs apps/allbert_assist/test/allbert_assist/settings_test.exs`
  ran 64 tests with 0 failures.
- `MIX_ENV=test mix allbert.test release.v049` passed. Evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release-v049/p0-11013/home/release_evidence/v049/release-v049-1780886771.json`.
- `MIX_ENV=test mix allbert.test release` passed. Evidence:
  `/var/folders/nc/r_scv0hd78x07x908ymg5mk80000gn/T/allbert_test_gates/release/p0-1218/home/release_evidence/gates/release-2026-06-08T03_08_24Z.json`.
- OpenAI live smoke passed for `vision_openai`/`image_openai`, including real
  doctors, real image input, confirmation-gated image generation, generated
  resource metadata, and redaction scan. Evidence:
  `/tmp/allbert-v049-openai.wd0zIU/release_evidence/v049/live-vision-openai-1780886149.json`.
- Gemini live smoke passed for `vision_gemini`/`image_gemini`, including real
  doctors, real image input, confirmation-gated image generation, generated
  resource metadata, and redaction scan. Gemini returned JPEG bytes to the PNG
  request; the system-level generated-output normalization stored and recorded
  the result as bounded `image/jpeg` metadata without widening the Gemini
  provider profile. Evidence:
  `/tmp/allbert-v049-gemini.cQbb9E/release_evidence/v049/live-vision-gemini-1780886180.json`.
- Local Ollama live smoke passed for `vision_ollama`/`image_ollama` with
  `qwen3-vl:8b` and `x/z-image-turbo`; the local image doctor accepts the
  installed `x/z-image-turbo:latest` tag as an availability alias. Evidence:
  `/tmp/allbert-v049-ollama.Z4w0Sj/release_evidence/v049/live-vision-ollama-1780886386.json`.
- Gemma 4 local vision-candidate smoke passed with
  `ALLBERT_V049_VISION_MODEL=gemma4:e4b`; image generation remained on
  `x/z-image-turbo`. Evidence:
  `/tmp/allbert-v049-gemma4.v2qeT4/release_evidence/v049/live-vision-ollama-1780886599.json`.

## Documentation Deliverables

- Operator vision/image guide.
- Developer vision + image-generation provider guide.
- Security-hardening media redaction/retention section.
- ADR 0042 image/screenshot amendment.
- ADR 0047 image-doctor fields.
- Roadmap/vision/agent-context-map v0.49 reconciliation.
- M7-M10 remediation evidence: deterministic gate hardening, OpenAI/Gemini live
  smokes, local Ollama profile proof or operator live-smoke evidence, and
  status/evidence drift closeout.

## Definition Of Done

v0.49 is done when:

- vision input works by attaching an image `ContentPart` to a resolved
  `vision_input` text profile via `ReqLLM`, from workspace (paste/upload) and
  from a v0.43 `browser_screenshot` read operation / `screenshot_ref` mapped
  into the image-resource path;
- image generation works through a `generate_image` action wrapping
  `ReqLLM.generate_image/3`, confirmation-gated for remote, with display-only
  cost;
- `image://`/`screen://` media are operator-supplied, bounded, default-off for
  retention, and absent from traces;
- the `vision_input`/`image_generation` capabilities resolve through the v0.48
  preference substrate with no new provider HTTP/adapter;
- the v0.49 security eval set + `release.v049` pass on fakes, browser
  screenshot fixtures, and image fixtures;
- OpenAI and Gemini live vision/image smokes pass, and the local Ollama media
  profiles are validated through ReqLLM/OpenAI-compatible routing structurally
  plus operator live-smoke evidence when the local models are installed;
- roadmap/vision/agent-context-map/ADR 0042/0047/CHANGELOG reflect the shipped
  state.
