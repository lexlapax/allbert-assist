# Allbert v0.04 Request Flow: Security Central Foundation

This document is the living request-flow map for v0.04 Security Central
Foundation. It should be updated during implementation as planned module names
become actual module names.

## Status

Status: planned.

v0.04 is a foundation milestone. It adds no new execution powers. It widens the
security vocabulary before v0.05 action-backed skills and later confirmations,
execution adapters, jobs, and channels.

## Source Documents

- Plan: `docs/plans/v0.04-plan.md`
- Roadmap: `docs/plans/roadmap.md`
- Security ADR: `docs/adr/0006-security-central.md`
- Settings ADR: `docs/adr/0004-domain-settings-engine.md`
- Skills ADR: `docs/adr/0003-skill-manifests-as-capability-contracts.md`

## Boundary

Security Central evaluates runtime context against policy. It does not store
operator policy itself and does not execute actions.

Settings Central remains responsible for:

- operator settings
- permission defaults
- skill trust and enablement settings
- provider and model profiles
- encrypted secret storage
- settings audit records

Security Central is responsible for:

- normalizing actor, channel, session, request, skill, action, resource, and
  external-content context
- resolving effective policy from Settings Central and built-in safety floors
- returning structured security decisions
- naming risk tier, confirmation requirement, redaction obligations, audit
  metadata, and trace metadata
- providing compatibility for `PermissionGate.authorize/2`

## Planned Decision Flow

```text
runtime/action request
  -> Security.Context
  -> Security.Policy
  -> Security.Decision
  -> PermissionGate compatibility response
  -> action result metadata
  -> trace and audit metadata
```

The compatibility response must preserve current fields:

- `permission`
- `decision`
- `reason`
- `requires_confirmation`
- `source`

The widened decision should also include:

- `risk`
- `redaction`
- `audit`
- `trace`
- `context`
- `trust_boundary`
- `policy`

## Planned Module Map

- `AllbertAssist.Security`: facade for security status and authorization.
- `AllbertAssist.Security.Context`: normalized actor/channel/session/skill/action
  context.
- `AllbertAssist.Security.Decision`: canonical decision data.
- `AllbertAssist.Security.Policy`: policy lookup and built-in safety floors.
- `AllbertAssist.Security.Risk`: risk-tier vocabulary.
- `AllbertAssist.Security.Redactor`: central redaction policy for values and
  metadata.
- `AllbertAssist.Security.Audit`: security audit event shape.
- `AllbertAssist.Security.PermissionGate`: compatibility layer.

## Planned Trust Boundaries

Security Central should model at least these boundaries:

- local operator input
- CLI and LiveView channels
- future remote channels
- selected skill and skill source
- skill resources such as scripts, references, and assets
- registered action execution
- future shell/process execution
- future external content and external services
- future online skill import and package installation
- memory and trace persistence

## Operator-Visible Behavior

v0.04 may add read-only security status surfaces if they remain informational:

- current permission defaults
- current safety boundaries
- skill trust summary
- secret status counts, never raw secrets
- trace/redaction posture
- planned sandbox/import policy posture

These surfaces must not enable execution or modify policy unless a later plan
explicitly adds write behavior.

## Verification Sketch

Implementation should verify:

- old PermissionGate decisions remain behavior-compatible
- widened decisions include risk, confirmation, redaction, audit, trace, and
  context metadata
- raw secrets are never rendered in security status, traces, audits, or action
  metadata
- untrusted skills and unknown actions do not gain permission from metadata
- unknown permissions and execution modes deny by default
