# Allbert v0.39b Request Flow - Identity Slot And Active Memory

Status: planned; first revision after post-v0.38 readiness review.

Implementation details live in `docs/plans/v0.39b-plan.md`. Algorithm spec
lives in `docs/research/active-memory-retrieval.md`.

## Source Documents

- `docs/plans/v0.39b-plan.md`
- `docs/research/active-memory-retrieval.md`
- `docs/plans/v0.21-plan.md` (memory review/retrieval substrate)
- `docs/plans/v0.27-plan.md` (app memory namespace declaration contract)
- ADR 0021 (advisory provider boundary — embedding retrieval reserved for
  future provider)

## Main User/Operator Flows

- Operator optionally writes `identity` markdown files via the v0.21 memory
  review surface. Each file is one `Memory.Entry`; one or many files both
  work.
- Operator submits a prompt; the runtime runs `retrieve_active_memory`
  before composing the model call.
- Runtime composes the model call with top-K reviewed memory chunks scoped
  to `{thread_id, active_app, identity}` (see "Neutral-Context Retrieval
  Behavior" in the plan for `active_app: nil` semantics).
- Operator inspects the `### Active Memory` trace subsection (placed after
  `### Intent Candidates`, before `### Memory Review`) to see which
  chunks were retrieved, their score breakdown, and which candidates were
  excluded.

## Data Flow

`Runtime.submit_user_input/1` → registered `retrieve_active_memory` action →
deterministic lexical scoring over reviewed-promoted memory entries scoped to
`{thread_id, active_app, identity_namespace}` → snapshot the candidate set
once per turn → chunk each `Memory.Entry` body into
`active_memory.chunk_max_bytes` windows → top-K chunks (bounded by
`active_memory.top_k`) → model context composition → reply → trace metadata.

System namespace declarations flow through
`AllbertAssist.Memory.SystemNamespaces` (new in v0.39b M2) → registered via
`AllbertAssist.App.Registry` with a reserved `:_system` app id → recognized
by `Memory.validate_writable_namespace/2` for write authorization.

## Authority Invariants

- The identity persona files are inert content; they never grant permission
  or execute.
- `retrieve_active_memory` is read-only; it cannot promote, mutate, or
  infer durable memory.
- Retrieval results are advisory context, not authority.
- Same query + same memory state + same settings → same chunks
  byte-for-byte (deterministic and replayable).
- Snapshot rule: concurrent v0.21 writes during scoring are reflected on
  the next turn, not the current one.
- No embeddings; no learned ranking; no LLM-driven scoring in v0.39b.
- System-namespace writes use the existing `:memory_write` permission;
  no new permission class.
- Neutral-context retrieval (`active_app: nil`) surfaces identity + general
  chunks only; app-tagged chunks for non-active apps are excluded.

## Handles / CLI Surfaces

- Existing `mix allbert.memory review` paths (used to write/edit the
  identity namespace).
- `mix allbert.memory list --namespace identity` (new flag added in M4;
  does not exist today).
- `mix allbert.memory list --category identity` (new flag using the
  new `:identity` category from M2).
- `mix allbert.memory retrieve --query "..."` (new developer/operator
  helper added in M4 that runs `retrieve_active_memory` and prints the
  deterministic top-K result with score breakdown).
- `identity` memory namespace under `<ALLBERT_HOME>/memory/identity/`.
- Settings Central `active_memory.*` keys.

## Security Eval Rows

- `identity-memory-inert-001`
- `active-memory-read-only-001`
- `active-memory-no-promotion-001`
- `active-memory-cross-namespace-no-leak-001`
- `active-memory-deterministic-replay-001`
- `identity-namespace-not-app-owned-001`
- `active-memory-neutral-context-no-app-leak-001`
- `active-memory-trace-section-placement-001`
- `active-memory-snapshot-race-001`

## Smoke Checklist

- Create or skip the identity persona file(s) from the memory review
  surface; verify writes land under `<ALLBERT_HOME>/memory/identity/`.
- Submit a prompt and confirm `### Active Memory` trace metadata is
  present at the pinned placement (after `### Intent Candidates`, before
  `### Memory Review`).
- Submit the same prompt twice with no state changes and confirm
  identical retrieved chunk ids in the same order (replayability).
- Promote a chunk via v0.21 review and confirm subsequent retrievals
  reflect the change on the next turn (snapshot rule).
- Confirm cross-thread chunks do not leak into the current thread
  retrieval unless tagged appropriately.
- Confirm neutral-context retrieval (`active_app: nil`) surfaces
  identity + general chunks and excludes app-tagged chunks for
  non-active apps.
- Verify `mix allbert.memory list --namespace identity` returns identity
  entries only.
- Verify `mix allbert.memory list --category identity` returns the same
  set (deterministic alternative).
- Verify `mix allbert.memory retrieve --query "..."` prints the same
  chunks as the trace metadata for the same query and state.

## Docs Closeout Checklist

- Research note exists and matches the plan body's settings shape.
- `docs/operator/active-memory.md` filled in (no longer a stub).
- Operator onboarding doc updated to point the v0.39 identity-slot
  preview at `docs/operator/active-memory.md`.
- Security-hardening doc adds active-memory eval lines.
- Agent context map routes v0.39b work and notes the system-namespace
  extension.
- Future-features keeps embedding retrieval and distillation parked;
  cross-references the new `:identity` category as the deterministic
  precursor to System Memory Distillation.
- Version metadata bumped per M5 (target `0.39.1` or per project
  versioning convention).
- CHANGELOG entry added.
