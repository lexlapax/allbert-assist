# Allbert v0.53 — Channel Pack 1 Retro-Validation (Telegram, Email) + Channel Pack 2 (Matrix, WhatsApp, Signal) + System-Wide Custody/Trust Constructs

Request flow: `docs/plans/v0.53-request-flow.md`

ADRs:
- `docs/adr/0016-channel-adapter-boundary-and-identity-mapping.md` (channel
  boundary + approval primitives; v0.52 amendment binding).
- `docs/adr/0056-channel-inbound-trust-tier.md` (v0.53 amendment — public
  signed-webhook inbound; reuses the v0.51 HTTP ingress).
- `docs/adr/0057-cross-channel-conversation-threading.md` (the threading
  substrate consumed by all three channels).
- `docs/adr/0058-key-custody-and-channel-daemon-supervision.md` (NEW — in-BEAM
  KeyCustody + external `signal-cli` daemon supervision).
- `docs/adr/0059-channel-trust-class-and-relay-gating.md` (NEW — the
  `trust_class` field + unified-view/resume gating that completes ADR 0057's
  E2EE-origin promise).
Consumed: ADR 0014 (local identity), ADR 0017 (plugin contract), ADR 0046
(settings schema), ADR 0049 (development lanes), ADR 0050 (dependency/toolchain
compatibility — `muontrap`/`erlexec`), ADR 0006 (Security Central).

Status: planned; deepened from a skeleton in the v0.53 implementation-readiness
passes. One large release (M0-M10); substrate/constructs built first so the
channel adapters are native to them.

Source: `docs/archives/version-1.0-planning-03.md`, post-v0.37 planning pass;
v0.53 readiness deepening.

## Purpose

Expand Allbert into mobile/personal messaging channels through the existing v0.16
channel boundary, v0.17 plugin contract, the v0.52 ADR 0016 approval-primitive
amendment, and the v0.52 ADR 0057 threading substrate — **and finish the
system-wide constructs v0.52 declared but did not build**, because Matrix,
WhatsApp, and Signal force them:

- **Signal** is always end-to-end encrypted and integrates only by supervising an
  external `signal-cli` daemon that holds account keys on disk.
- **WhatsApp Cloud API** is a public signed webhook (server-readable plaintext to
  Allbert), not an outbound WS like Discord/Slack.
- **Matrix** is a pure HTTP `/sync` client; encrypted rooms are out of scope (no
  Elixir E2EE support), so v0.53 targets unencrypted Matrix rooms.

These break v0.52's assumptions (in-process WS clients, no E2EE, no phone PII), so
v0.53 adds: **Key Custody** (ADR 0058), **channel trust-class gating** (ADR 0059,
completing ADR 0057's E2EE-origin promise), a **public signed-webhook ingress**
(ADR 0056 amendment, reusing v0.51), **descriptor reply-key/quote-TTL
consumption**, and **phone-PII redaction**. Scope tightened per prior passes:

- **Viber** is documented on paper as a validated WhatsApp-twin (signed-webhook
  reuse, `:flat`, `:button`+`:link`, non-E2EE, opaque no-PII id) but **not built**
  in v0.53 — its ~€100/month standing bot fee makes it poor value to run. See
  `future-features.md` "Viber Channel Adapter".
- **iMessage parked** (macOS-only platform policy). **SMS parked.**
- **Signal kept in scope** via the `signal-cli` daemon (ADR 0058); `signald` is
  deprecated and "libsignal" is a library, not a runnable bot — neither is used.
- **Channel Pack 1 retro-validation is release-blocking.** v0.53 opens by
  bringing the already-shipped Telegram and email channels to Discord/Slack
  validation parity before new adapters land: live provider smokes, doctors,
  operator guides, manual checks, evidence, and leak scans.

## Readiness Sweep

Status: implementation-ready for M0 after this second-pass reconciliation. This
pass keeps the ADR 0016 `:list` fallback contract mandatory for every channel
descriptor and makes the WhatsApp signed-webhook ingress seam explicit before
implementation starts.

- **Vision fit.** v0.53 expands channel reach without a new runtime or implicit
  account system; external input still translates into signals through
  `Runtime.submit_user_input/1`. The two new system-wide constructs (KeyCustody,
  trust-class) strengthen existing substrate (secrets, threading) rather than
  adding parallel authority.
- **Roadmap fit.** v0.53 follows v0.52 (which shipped the threading substrate) and
  finishes the constructs v0.52 deferred. Channel Pack 2 is the stress test that
  proves the v0.52 constructs generalize to E2EE, webhook, and daemon channels.
- **Past-plan fit (reused as-is).** `Conversations.ChannelThread` + the three
  mapping tables; the `threading:`/`primitives:` descriptor pattern + registry
  validation; the plugin scaffold (`Plugins.<Provider>` +
  `Channels.<Provider>.{Adapter,Client,Parser,Renderer}` + `Settings.Fragment` +
  manifest + doctor); the external-smoke harness pattern; `Settings.Secrets`
  AES-256-GCM + master-key chain; `Runtime.Redactor`/`Security.Redactor`; the
  v0.51 authenticated HTTP ingress (body cap, secure headers, rate-limit) for the
  WhatsApp webhook.
- **New-build fit (honest).** v0.52 left declaration-only or missing: a
  `trust_class` field + gating (none today), descriptor `reply_key_type` /
  `quote_ttl_ms` (declared, **not validated, not consumed**), in-memory key
  custody (secrets decrypt on every read), external key-holding daemon
  supervision (none in channels), and phone-number redaction (none). The public
  inbound webhook is **more net-new than "a branch"**: v0.51's ingress does
  **not** preserve the raw body (its `PublicProtocolBodyReader` streams/discards),
  and its auth is bearer-token-hardwired — so M4 builds net-new raw-body
  preservation **and** a signature-auth plug (the v0.51 token-auth is not
  reused). M1-M4 build all of this as net-new substrate; M6-M8 consume it.
- **Dependency fit.** KeyCustody adds **no** new third-party dependency, but M0
  makes **`plug_crypto` an explicit direct dep** (previously transitive via
  `plug`; KeyCustody's `prune_args_from_stacktrace/1`/`secure_compare/2` and the
  webhook HMAC plug both depend on it). Daemon supervision adds **`muontrap`**
  (Linux) with **`erlexec`** for macOS/dev (ADR 0050). M0 resolved
  `muontrap 1.8.0`, `erlexec 2.3.4`, and retained `plug_crypto 2.1.1`. No
  Vault/OpenBao, no `cloak`/`enacl` (rejected as overkill / unmaintained-NIF for
  a single-operator local app).
- **ADR fit.** ADR 0056 amended (public webhook). ADR 0058 new (custody +
  daemon). ADR 0059 new (trust-class). ADR 0057 consumed + completed.

Readiness guardrails:
- **v0.52 substrate is tagged/frozen (`v0.52.0`, 2026-06-14) — guardrail
  satisfied.** v0.53 M1-M3 build directly on v0.52 substrate now shipped in
  `v0.52.0`: `Conversations.ChannelThread.resolve_reply_target/2`, the three ADR
  0057 migrations (`thread_channel_refs`/`conversation_message_refs`/
  `cross_channel_identity_links`), `Conversations.UnifiedHistory.{show_thread/3,
  resume_thread_on_channel/1}`, `Plugin.Validator` (threading validation), and
  `Settings.Secrets.get_secret/2`. M0 still re-verifies each named seam against
  the tagged `v0.52.0` before M1-M3 code lands. (All seams confirmed present in
  the tagged tree during this readiness pass.)
- Substrate/constructs (M0-M4) and Channel Pack 1 retro-validation (M5) land
  before the Channel Pack 2 adapters (M6-M8); each adapter is native to
  KeyCustody, trust-class, the webhook ingress, and the consumed descriptor
  flags. Critical path: M0-M4 → M5 → M6-M8 → M9 → M10. **M5 validates the
  already-shipped Telegram/email channels and has no hard dependency on the
  M0-M4 substrate (it touches none of the new constructs), so it may proceed in
  parallel with M0-M4; it is release-blocking for M6-M8, not blocked by M0-M4.**
- Every v0.53 channel descriptor preserves ADR 0016's mandatory `:list`
  fallback. Higher-fidelity primitives (`:button`, `:typed_command`, `:link`)
  are provider-specific overlays, not replacements for the fallback path.
- The plan must not claim locked/zeroed/protected memory for KeyCustody (ADR
  0058 honesty clause). It claims only: decrypted once, one `:sensitive`
  supervised process, excluded from crash dumps/introspection, never logged,
  redacted on inspect.
- The WhatsApp signed webhook reuses the v0.51 HTTP ingress controls by adding
  a webhook-aware raw-body/pre-parser branch to the shared Phoenix/Plug ingress,
  not by mounting a parallel controller path after JSON parsing.
- The deterministic provider-stub lane is the fast/CI gate; **required
  real-provider live smokes per existing provider and new platform** are release
  authority before the v0.53 tag. That includes M5 Telegram/email evidence plus
  M6-M8 Matrix/WhatsApp/Signal evidence.

## M0 Locked Decisions

| # | Decision | Chosen value | Trade-off if changed |
|---|---|---|---|
| 1 | Channel scope | Build Matrix + WhatsApp + Signal in v0.53. Viber documented on paper + deferred (standing fee). iMessage/SMS parked. | Building Viber adds a ~€100/mo standing fee for a self-host; dropping Signal would leave the daemon construct without its primary consumer. |
| 2 | WhatsApp backend | **Cloud API only** — public signed webhook (`X-Hub-Signature-256`), WABA `phone_number_id` identity, Meta-decrypted (`:server_readable`), needs a tunnel for self-host. whatsapp-web bridge parked. | The whatsapp-web bridge is ToS-gray, breaks on protocol changes, and needs a supervised key-holding daemon; Cloud API is the maintainable official path. |
| 3 | Matrix scope | **Unencrypted rooms only**, pure `Req` + `/sync` client. Encrypted-room support (Rust crypto NIF / mautrix bridge) is parked. | No maintained Elixir library supports Matrix E2EE (polyjuice 0.4.4/2021, matrix_sdk 0.2.0, olm-elixir unfinished); encrypted rooms would be a separate large effort. |
| 4 | Signal integration | **Supervise `signal-cli` in daemon mode**, JSON-RPC over a local control channel: preferred `0600` UNIX socket, or an explicitly configured `127.0.0.1`-only TCP/HTTP endpoint with auth/ACL controls (ADR 0058). `signald` (deprecated) and raw `libsignal` are not used. | There is no Signal bot API; the daemon is the only viable path and is a key-holding external process requiring explicit supervision/trust. |
| 5 | Key Custody | **In-BEAM `:sensitive` KeyCustody GenServer** on the existing `:crypto` + `plug_crypto` (0 new deps); decrypt-once. A separate OS key-daemon / Vault is rejected (no real boundary gain for a single operator; server overkill). | A separate daemon/Vault adds operational surface without improving the trust boundary when the BEAM is the consumer; locked-memory claims are not achievable on the BEAM and are not made. |
| 6 | Daemon supervision dep | **`muontrap`** (Linux: cgroup kill + bounds) with **`erlexec`** (macOS/dev). Raw `Port` rejected for a long-running daemon. | Raw `Port` orphans grandchildren on a BEAM crash; muontrap/erlexec give reliable kill-on-crash. |
| 7 | Trust-class gating | **Full** (ADR 0059): a `trust_class` field on the mapping tables; the unified view excludes `:e2ee_origin` cross-channel content by default (audited opt-in); resume onto a weaker class requires an audited operator confirmation. | Document-only would leave ADR 0057's E2EE-origin promise unbuilt; per-message hard prohibition would break the unified-view value the construct exists to deliver. |
| 8 | Public webhook ingress | **Reuse** v0.51's rate-limiter (generic `{surface, client_id}` keying), secure-headers, body-cap, router pipeline, error envelope; **build net-new** raw-body preservation (the v0.51 body reader streams/discards) + a signature-auth plug (the bearer-token auth is not reusable for HMAC). No second ingress (ADR 0056 amendment). | A bespoke webhook ingress would duplicate v0.51's hardened path; pretending the token-auth/raw-body already exist would under-budget M4. |

## Key Custody (ADR 0058)

`AllbertAssist.Settings.KeyCustody` is a supervised GenServer that decrypts the
`secrets.yml.enc` envelope **once** (via the existing `Settings.Secrets`
master-key chain + `:crypto` AES-256-GCM) and serves secret material to in-VM
callers through `fetch/1`, replacing today's decrypt-on-every-read. Hardening
(achievable): `process_flag(:sensitive, true)` (crash-dump/observer hardening,
but not treated as a local `:sys.get_state` denial), masked `format_status/1`,
an `Inspect` that prints no secrets, secrets held as zero-arity closures,
`Plug.Crypto.prune_args_from_stacktrace/1`
+ `secure_compare/2`, and audited fetch. **Not claimed:** locked/zeroed/protected
memory or in-VM isolation between Allbert's own modules (the BEAM provides
neither; ADR 0058 honesty clause). `put_secret`/`delete_secret` invalidate the
cache. Zero new dependencies.

## Channel Trust-Class And Relay Gating (ADR 0059)

Every channel declares a `trust_class:` (`:e2ee_origin` | `:server_readable` |
`:local`); `ChannelThread` stamps it onto `thread_channel_refs` /
`conversation_message_refs` (new `trust_class` column, default
`:server_readable`). Signal = `:e2ee_origin`; WhatsApp/Viber/Telegram/Slack/
Discord/email = `:server_readable`; web/CLI = `:local`. The **unified history
view** excludes `:e2ee_origin` content from a *different* channel by default
(audited per-request/setting opt-in); **`resume_thread_on_channel`** onto a
weaker class requires an audited operator confirmation naming the downgrade.
v0.53 retrofits the web + CLI unified-view surfaces to honor `trust_class`. The
construct controls audited *exposure decisions*, not cryptographic guarantees
after E2EE termination (stated plainly in docs).

## Descriptor Flag Consumption + Phone Redaction

- **`reply_key_type` / `quote_ttl_ms` (declared in v0.52, now consumed):**
  `ChannelThread.resolve_reply_target/2` reads them. Signal uses
  `reply_key_type: :timestamp` (quote-by-timestamp+author); WhatsApp uses
  `quote_ttl_ms` (~30-day heuristic upper bound) to degrade `:reply_chain` →
  `:flat` when the quote window closes, without changing the canonical thread.
- **Phone-PII redaction:** `Security.Redactor` gains a phone-number rule
  (E.164) so WhatsApp/Signal `external_user_id` and any phone-bearing
  `receiver_account_ref` are redacted in `channel_events`, traces, audits, and
  the unified view. **Signal identity keys on ACI (UUID)**, not the rotatable
  phone number/PNI.

## Public Signed Webhook Ingress (ADR 0056 amendment)

WhatsApp Cloud API POSTs inbound events to a public HTTPS endpoint. **Reuse is
partial and the net-new plumbing is named honestly** (verified against the
shipped v0.51 ingress):

- **Reused as-is:** the v0.51 `RateLimiter` (its `{surface, client_id}` key is
  generic — buckets scope by provider install, e.g. `whatsapp_webhook` + WABA /
  `phone_number_id`), the `PublicProtocolHeaders` secure-header posture,
  `PublicProtocolBodyCap`, the router `:public_protocol_api` pipeline, and the
  `HttpIngress` error envelope.
- **Net-new (does not exist in v0.51):**
  1. **Raw-body preservation.** v0.51's `PublicProtocolBodyReader` delegates to
     `Plug.Conn.read_body/2`, which streams and discards the body — after
     `Plug.Parsers` the raw bytes are gone. M4 enhances the body reader (or adds a
     pre-parser step) to cache the raw bytes in `conn.private` **before**
     `Plug.Parsers` JSON decoding, so an HMAC can be computed over them.
  2. **A signature-auth plug** (`PublicProtocolWebhookAuth`), **distinct from the
     bearer-token `PublicProtocolAuth`** — the v0.51 token auth
     (`HttpIngress.authenticate` extracts `x-allbert-client-id` +
     `Authorization: Bearer`) is **not reused**. The webhook plug verifies
     `X-Hub-Signature-256` = `:crypto.mac(:hmac, :sha256, app_secret, raw_body)`
     (not the removed `:crypto.hmac/3`) with `Plug.Crypto.secure_compare/2`, the
     `app_secret` fetched via `Settings.KeyCustody`, then rate-limits on the
     install bucket.

The controller answers the `hub.challenge` verification handshake without
granting runtime authority. Absent/invalid signature → rejected before parse and
before runtime work. Self-host without a public URL uses a tunnel (documented).
The same construct is the (deferred) Viber twin's ingress with a Viber-specific
verifier (`X-Viber-Content-Signature`, keyed by the auth token).

## Channel Approval Primitives

Each adapter declares its supported subset per the v0.52 ADR 0016 amendment;
`Approval.Handoff` picks the highest-fidelity primitive at delivery time.
`Plugin.Validator` still rejects any channel descriptor that omits `:list`.

- **Matrix**: `typed_command`, `link`, `list`. No stable portable Matrix
  bot-button primitive is claimed for v0.53; bots render formatted HTML + links,
  with `:list` as the universal fallback.
- **WhatsApp**: `button`, `typed_command`, `link`, `list`. Interactive reply
  buttons need **no template approval** (only *templates* are reviewed); buttons
  work in the 24h customer-service session, with `typed_command`/`link`/`list`
  outside it.
- **Signal**: `typed_command`, `link`, `list`. Text-first; no interactive
  buttons.
- **Viber (deferred)**: `button`, `typed_command`, `link`, `list` (keyboard
  `reply` buttons round-trip the payload as a normal message).

## Channel Threading Capabilities + Trust Class

Each adapter consumes ADR 0057 (canonical `conversation_threads.id`; provider ids
never grant authority) and ADR 0059 (trust class):

| Channel | `threading:` | `reply_key_type` | `trust_class` |
|---|---|---|---|
| Matrix | `:native_threads` (m.thread, stable since spec v1.4) with `:reply_chain` fallback | `:opaque_id` | `:server_readable` (unencrypted rooms) |
| WhatsApp | `:reply_chain` (WAMID `context.message_id`); degrade to `:flat` past `quote_ttl_ms` | `:opaque_id` | `:server_readable` |
| Signal | `:reply_chain` (quote by timestamp+author) | `:timestamp` | `:e2ee_origin` |
| Viber (deferred) | `:flat` (no reply/thread primitive) | n/a | `:server_readable` |

## Security Posture

Each provider gets explicit Settings Central schema, identity mapping,
retry/dedupe, and pairing/recovery docs. Channel metadata never grants action
permissions; all effectful work routes through registered actions, Security
Central, confirmations, traces, audits. v0.53-specific invariants: the
`signal-cli` control endpoint is local-only (preferred `0600` UNIX socket, or
`127.0.0.1`-only TCP/HTTP with auth/ACL controls) and its key files `0600` (ADR 0058);
the WhatsApp webhook verifies the raw-body HMAC before parsing (ADR 0056
amendment); phone numbers are redacted everywhere; KeyCustody never leaks secret
material via inspect/`:sys.get_state`/crash dump; `:e2ee_origin` content is gated
out of the default cross-channel view and downgrade-on-resume is confirmed +
audited (ADR 0059).

## Development-Lane Annotations

Per ADR 0049, every milestone carries a lane annotation; all implementation
milestones rejoin through the full release gate. The `release.v053` lane uses
provider stubs + an in-process signal-cli stub; the **required real-provider live
smokes per existing provider and new platform** (`external-smoke --
telegram|inbound_telegram|email|inbound_email|matrix|whatsapp|signal`) are the
release authority before tag, not optional.

| M | Parallel workstreams | Serial barriers | Gate evidence + rejoin |
|---|---|---|---|
| M0 | ADR 0056 amendment + ADR 0058 + ADR 0059 acceptance; descriptor `trust_class:` field + registry validation; mandatory `:list` fallback preserved; `muontrap`/`erlexec` dep decision (ADR 0050); descriptor reply-key/quote-TTL consumption contract; signed-webhook ingress seam locked | dep review; descriptor schema; ingress reuse plan | focused: descriptor validation (`trust_class:` + `:list`); dep compile; `mix compile --warnings-as-errors`; rejoin: ADRs plan-accepted + deps locked before M1 |
| M1 | KeyCustody GenServer (ADR 0058): decrypt-once, `:sensitive`, masked status/inspect, closures, audited fetch | Settings supervision; cache invalidation on put/delete | focused: KeyCustody fetch/invalidate, no-leak (inspect/`:sys.get_state`/crash-dump fixtures), audit-on-fetch; lanes: app_env_serial; rejoin: secrets served from custody, decrypt-on-read removed |
| M2 | Trust-class substrate (ADR 0059): `trust_class` column migration; ChannelThread stamping; unified-view + resume gating; **web/CLI unified-view retrofit** | migration; UnifiedHistory gating; resume downgrade confirmation | focused: trust-class stamping, default-exclude e2ee_origin, opt-in audited, resume-downgrade confirm+audit; lanes: db_serial + liveview_serial; rejoin: gating green; web/CLI honor trust_class |
| M3 | reply_key_type + quote_ttl_ms consumption (ChannelThread/degradation); phone-PII redaction (Security.Redactor); Signal ACI identity contract | ChannelThread.resolve_reply_target; Redactor phone rule | focused: reply-by-timestamp, quote-TTL degrade-to-flat, phone redaction, ACI-keyed identity; lanes: db_serial; rejoin: substrate flags consumed; phones redacted |
| M4 | Public signed webhook ingress (ADR 0056 amendment): webhook-aware body cap/body reader before `Plug.Parsers`; `X-Hub-Signature-256` raw-body verify + `hub.challenge` handshake; reuse v0.51 secure headers + rate limiter with provider/install buckets | endpoint/router/controller; raw-body preservation; signature verifier; v0.51 ingress wiring | focused: raw-body available before parse, signature verify before JSON decode, bad-sig reject, challenge handshake, body-cap/rate-limit/secure-headers; lanes: liveview_serial + global_process_serial; rejoin: webhook ingress green |
| M5 | Channel Pack 1 retro-validation (Telegram + email) — first real-provider validation of the v0.16 channels before Channel Pack 2: provider-fidelity audit (v0.52 M8R-style) + remediations; per-provider external smokes, provider doctors (ADR 0047), and operator guides built to Discord/Slack parity; live real-provider validation | fidelity audit (Telegram, email) in parallel; smoke/doctor/guide scaffolding; remediation lanes (e.g. email Content-Transfer-Encoding decoding, Telegram inline-keyboard/callback_data limits) | focused: telegram/email plugin load + doctors + per-provider smoke skip-checks green; live: `external-smoke -- telegram`, `-- inbound_telegram`, `-- email`, `-- inbound_email` + operator manual checks with recorded evidence + raw-token leak scans; lanes: external_runtime_serial + db_serial; rejoin: both channels validated live, evidence recorded, leak scans clean |
| M6 | Matrix plugin (`Plugins.Matrix` + modules + `Settings.Fragment` + manifest + doctor): raw `Req` + `/sync`, unencrypted rooms, `:native_threads`/`:reply_chain`, `typed_command`/`link`/`list`, MXID identity | plugin discovery/registry; adapter supervisor child spec | focused: Matrix plugin load + simulated `/sync` event + thread-ref write + doctor + CLI; lanes: db_serial + global_process_serial; rejoin: Matrix loads, simulated inbound green |
| M7 | WhatsApp plugin (Cloud API): webhook (M4), `:reply_chain` + quote-TTL, phone-redacted WABA identity, in-session `:button`, `typed_command`/`link`/`list`, `:server_readable` | plugin discovery/registry; webhook→adapter path | focused: WhatsApp plugin load + simulated webhook event + reply-chain/quote-TTL + button + phone redaction + doctor; lanes: db_serial + liveview_serial; rejoin: WhatsApp loads, simulated inbound green |
| M8 | Signal plugin: supervise `signal-cli` daemon (ADR 0058) via muontrap/erlexec; JSON-RPC over local control channel (preferred 0600 UNIX socket; loopback TCP/HTTP only with auth/ACL controls); `:reply_chain` reply-by-timestamp; ACI identity; `typed_command`/`link`/`list`; `:e2ee_origin` | daemon supervision child spec; control-channel/key file perms; JSON-RPC client | focused: Signal plugin load + stubbed daemon JSON-RPC + reply-by-timestamp + ACI identity + e2ee_origin stamping + local-control/auth/perms assertions + doctor; lanes: external_runtime_serial + db_serial; rejoin: Signal loads, stubbed-daemon inbound green |
| M9 | Pairing/setup UX (Matrix token, WhatsApp WABA, Signal QR link); identity mapping + cross-channel links; delivery/retry/dedupe per provider | identity-link store; `channel_events` dedupe; per-provider retry | focused: pairing flows, identity-link no-auto-merge, dedupe/retry per provider; lanes: db_serial; rejoin: identity + pairing + delivery green for all three |
| M10 | security evals (`:v053`); M5 Telegram/email evidence verification; required Telegram/email + Matrix/WhatsApp/Signal real-provider smokes; docs; `release.v053`; closeout; ADR 0056 amendment + 0058 + 0059 Status flips | `EvalInventory` (`:v053`); full release gate; required live smokes before tag; doc reconciliation | full `:v053` lane (stubs); `mix allbert.test release`; `release.v053`; **required** `external-smoke -- telegram`, `-- inbound_telegram`, `-- email`, `-- inbound_email`, `-- matrix`, `-- whatsapp`, `-- signal` with recorded evidence before tag; rejoin: docs match; version → 0.53.0 |

## Milestones

### Milestone 0: Contracts — ADRs, Descriptor `trust_class`, Deps
Status: implemented; M1 may build on these contracts.

Implementation tasks:
- Accept (plan-level) the ADR 0056 v0.53 amendment, **ADR 0058**, and **ADR
  0059**; M10 closeout flips their Status to Accepted after release evidence.
- Add the `trust_class:` channel descriptor field + `Plugin.Validator` rejection
  of missing/invalid values; default `:server_readable` for v0.52 channels.
- Reconfirm the channel descriptor primitive contract: Matrix =
  `[:typed_command, :link, :list]`, WhatsApp =
  `[:button, :typed_command, :link, :list]`, Signal =
  `[:typed_command, :link, :list]`; all descriptors keep `:list`.
- Lock the dependency decision (ADR 0050): `muontrap ~> 1.8` (+ `erlexec ~> 2.3`
  for macOS/dev) for daemon supervision; promote **`plug_crypto` to an explicit
  direct dep** (today transitive via `plug`); KeyCustody adds no other dep.
- Add `Plugin.Validator` coverage for `trust_class:` (enum
  `:e2ee_origin|:server_readable|:local`). The validator today checks `threading:`
  only; `reply_key_type`/`quote_ttl_ms` validation lands in M3 when they become
  load-bearing.
- Document the `reply_key_type`/`quote_ttl_ms` consumption contract M3 will
  build and the raw-body signed-webhook ingress contract M4 will build (incl. the
  net-new raw-body preservation + signature-auth plug, not the bearer-token auth).

Acceptance: ADRs plan-accepted; descriptor `trust_class:` validated (rejects
missing/invalid); `:list` fallback retained; deps (incl. explicit `plug_crypto`)
compile and are recorded; consumption + webhook ingress contracts documented.
Implementation result: shipped Telegram/email/Discord/Slack descriptors now
declare `trust_class: :server_readable`; local CLI/LiveView descriptors declare
`:local`; `Plugin.Validator` rejects missing/invalid trust classes; M0 resolved
`muontrap 1.8.0`, `erlexec 2.3.4`, and direct `plug_crypto 2.1.1`.

Automated tests: descriptor validation (`trust_class:` + `:list`); dep compile.
Evidence: `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/plugin/validator_test.exs apps/allbert_assist/test/allbert_assist/channels_test.exs apps/allbert_assist/test/security/v052_channel_pack_eval_test.exs`
passed 29 tests, 0 failures; `MIX_ENV=test mix compile --warnings-as-errors`
passed.

Request-flow handoff: document the construct contracts.

### Milestone 1: Key Custody (ADR 0058)
Status: implemented; M2 may build on custody fetches.

Implementation tasks:
- Built `AllbertAssist.Settings.KeyCustody` GenServer (decrypt-once via the
  existing master-key chain; `:sensitive`; masked `format_status`; `Inspect`
  override; secrets-as-closures; audited `fetch/1`; cache invalidation on
  put/delete). `Settings.Secrets.get_secret/2`, status, and list-status now route
  through custody; writes continue to use the existing encrypted file writer and
  invalidate custody. Zero new deps.

Acceptance: secrets served from custody (no decrypt-on-every-read); custody state
never exposes raw secret values via inspect/`:sys.get_state`/status fixtures;
fetches audited. OTP on this development/runtime stack still returns local
`:sys.get_state` for `:sensitive` GenServers, so v0.53 relies on no-secret state
shape and masked rendering rather than claiming local introspection denial.
**No claim** of locked/zeroed memory anywhere.

Automated tests:
- `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/settings/key_custody_test.exs apps/allbert_assist/test/allbert_assist/settings_test.exs apps/allbert_assist/test/allbert_assist/actions/settings_actions_test.exs`
  (66 tests, 0 failures; rerun outside sandbox after Mix PubSub `:eperm`).

Request-flow handoff: custody path + honest non-claims documented.

### Milestone 2: Trust-Class Substrate + Gating + Web/CLI Retrofit (ADR 0059)
Status: implemented; M3 may build on stamped trust metadata.

Implementation tasks:
- Added migration `20260614005300_add_trust_class_to_channel_thread_refs` for
  `trust_class` on `thread_channel_refs` +
  `conversation_message_refs` (default `:server_readable`). `ChannelThread`
  normalizes/stamps it; runtime inbound refs inherit channel descriptor
  `trust_class`; direct calls default to `:server_readable`.
- Gated `UnifiedHistory.show_thread/3` (default-exclude cross-channel
  `:e2ee_origin`; audited opt-in) and `resume_thread_on_channel/1` (audited
  downgrade confirmation). Per-request opt-in and
  `conversations.unified_history.include_e2ee_origin` Settings Central fallback
  are both supported. Web/CLI unified-view surfaces were retrofitted: CLI exposes
  `--include-e2ee-origin`; LiveView visible channel chips carry `data-trust-class`.

Acceptance: trust-class stamped per channel; e2ee_origin excluded by default;
opt-in + downgrade are confirmed and audited; web/CLI honor the gating.

Automated tests:
- `MIX_ENV=test mix compile --warnings-as-errors`
- `MIX_ENV=test mix allbert.ecto.migrate --quiet`
- `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/conversations/channel_thread_test.exs apps/allbert_assist/test/allbert_assist/conversations/unified_history_test.exs apps/allbert_assist/test/mix/tasks/allbert_conversations_test.exs apps/allbert_assist_web/test/allbert_assist_web/live/workspace_live_test.exs apps/allbert_assist/test/allbert_assist/actions/registry_test.exs`
  plus `apps/allbert_assist/test/allbert_assist/settings_test.exs`
  (`allbert_assist`: 73 tests, 0 failures; `allbert_assist_web`: 69 tests, 0
  failures; rerun outside sandbox after Mix PubSub `:eperm`).

Request-flow handoff: trust-class gating flow documented.

### Milestone 3: Reply-Key/Quote-TTL Consumption + Phone Redaction
Status: implemented; M4 may build on consumed reply-key/quote-TTL substrate and
system-wide phone redaction.

Implementation tasks:
- `ChannelThread.resolve_reply_target/2` consumes `reply_key_type`
  (`:timestamp` for Signal) and `quote_ttl_ms` (degrade `:reply_chain` → `:flat`
  past the window for WhatsApp). The resolved target now returns
  `declared_threading`, effective `threading`, `reply_key_type`, provider reply
  key metadata, JSON-ish `quote_window`, and explicit `degradation` reason.
- Add `Plugin.Validator` coverage for `reply_key_type` (enum
  `:opaque_id|:timestamp`) and `quote_ttl_ms` (positive integer), now that they
  are load-bearing.
- `Security.Redactor` phone-number (E.164) rule; channel event create/update
  redacts phone-bearing event ids/summaries before persistence; unified history
  redacts phone-bearing content, provider refs, and receiver account refs.
- Signal identity links require ACI UUIDs (`aci:<uuid>` normalized to UUID) and
  reject phone/PNI-shaped ids.

Acceptance: reply-by-timestamp and quote-TTL degradation work; the validator
rejects invalid `reply_key_type`/`quote_ttl_ms`; phone numbers redacted in
channel_events/traces/audits/unified view; ACI-keyed Signal identity.

Automated tests:
- `MIX_ENV=test mix compile --warnings-as-errors` (passed).
- `MIX_ENV=test mix test apps/allbert_assist/test/allbert_assist/conversations/channel_thread_test.exs apps/allbert_assist/test/allbert_assist/conversations/unified_history_test.exs apps/allbert_assist/test/allbert_assist/plugin/validator_test.exs apps/allbert_assist/test/allbert_assist/runtime/redactor_test.exs apps/allbert_assist/test/allbert_assist/channels_test.exs`
  (57 tests, 0 failures; rerun outside sandbox after Mix PubSub `:eperm`).
- `mix format --check-formatted`, `git diff --check` (passed).

Request-flow handoff: document flag consumption + redaction.

### Milestone 4: Public Signed Webhook Ingress (ADR 0056 amendment)
Status: planned.

Implementation tasks:
- Phoenix endpoint/router/controller path for the WhatsApp webhook under the
  `:public_protocol_api` pipeline. **Net-new (v0.51 lacks these):** enhance the
  body reader to cache the raw bytes in `conn.private` **before** `Plug.Parsers`
  (v0.51's reader streams/discards), and a `PublicProtocolWebhookAuth` plug,
  distinct from the bearer-token `PublicProtocolAuth`, that verifies
  `X-Hub-Signature-256` = `:crypto.mac(:hmac, :sha256, app_secret, raw_body)`
  (not the removed `:crypto.hmac/3`) with `Plug.Crypto.secure_compare/2`, the
  `app_secret` from `Settings.KeyCustody`, **before JSON decoding**.
  `hub.challenge` verification handshake. **Reused as-is:** v0.51 secure headers,
  body-cap, and the `RateLimiter` keyed on the install bucket (`whatsapp_webhook`
  + WABA / `phone_number_id`). Tunnel docs for self-host.

Acceptance: valid signature accepted, absent/invalid rejected before parse and
before runtime; challenge handshake answered; body-cap/rate-limit/secure-headers
enforced.

Automated tests: raw body preserved before parser; signature verify before JSON
decode; bad-sig reject; challenge; ingress controls.

Request-flow handoff: document the webhook ingress.

### Milestone 5: Channel Pack 1 retro-validation (Telegram + email)
Status: planned. Telegram and email shipped in v0.16/v0.17 and were retrofitted for cross-channel threading in v0.52, but — unlike Discord and Slack — they were never validated against the real provider (stub-only: `Req.Test` for Telegram, `FakeImapClient`/`FakeSmtpClient` for email; no external smokes, doctors, or operator guides). v0.52's live validation proved stub-green is not provider-green (it surfaced nine real bugs). This milestone pays down that debt to full Discord/Slack parity before any Channel Pack 2 adapter (M6+) lands. The shared improper-list serializer hardening from v0.52 (`Security.Redactor.redact/1`, `Conversations.json_safe/1`) already protects both; the remaining exposure is provider-specific rendering, parsing, and transport.

Implementation tasks:
- Provider-fidelity audit (the v0.52 M8R5–R7 analog) of each Telegram/email Adapter/Parser/Renderer/Client against real provider behavior; land fixes as remediation lanes — at minimum **M5R1 email Content-Transfer-Encoding decoding** (base64 / quoted-printable bodies, charset/MIME-word headers; real mail is almost always encoded) plus RFC-5322 header correctness, and **M5R2 Telegram inline-keyboard / `callback_data`** (64-byte limit and keyboard JSON shape — the direct analog of the Slack `invalid_blocks` defect), plus `getUpdates` long-poll/offset and callback-ack edge cases.
- Build the missing scaffolding to parity: per-provider **external smokes** (`telegram`/`inbound_telegram`, `email`/`inbound_email`, provider-targeted like the v0.52 split and runnable live under the fixed harness), **provider doctors** (`telegram_doctor`, `email_doctor` — ADR 0047 redacted envelope, live transport + credential checks), and **operator guides** (`docs/operator/telegram-channel.md`, `docs/operator/email-channel.md`) with researched sandbox setup + cleanup.

Acceptance: telegram/email plugin load + doctors + per-provider smoke skip-checks green deterministically; then, against a real Telegram bot and a real SMTP/IMAP mailbox, the per-provider smokes pass and the operator manual checks pass — inbound (text + Telegram voice), outbound/threading, approval (Telegram inline buttons / email typed commands), unmapped-sender rejection, reconnect/poll-resume — with recorded evidence and clean raw-token/credential leak scans. Remediation lanes (M5R1, M5R2, …) land green and are recorded.

Automated tests: telegram/email plugin load + doctor envelopes + per-provider smoke modules compile/skip cleanly + the M5R1/M5R2 remediation regressions tracked as the deterministic `:v053` eval rows `email-content-transfer-encoding-decoded-001` and `telegram-callback-data-within-64b-001` (run in `release.v053`, no provider network).

Request-flow handoff: add the Telegram + email operator manual-validation steps (sandbox setup, per-provider smokes, live inbound/outbound/approval/rejection, cleanup) mirroring the v0.52 request-flow.

### Milestone 6: Matrix Plugin
Status: planned. `threading: :native_threads` (m.thread) / `:reply_chain`
fallback; `trust_class: :server_readable`; unencrypted rooms only.

Implementation tasks:
- `./plugins/allbert.matrix/` mirroring the v0.52 plugin scaffold; raw `Req` +
  `/sync` long-poll inbound + `PUT .../send` outbound; bot user + access token;
  MXID identity; `typed_command`/`link`/`list` primitives; `matrix_doctor`;
  `mix allbert.channels matrix` CLI.

Acceptance: a fixture Matrix client `/sync` round-trips; thread refs recorded;
unencrypted-only documented; doctor returns an ADR 0047 envelope.

Automated tests: plugin load + simulated `/sync` + thread-ref write + doctor + CLI.

Request-flow handoff: document the Matrix flow.

### Milestone 7: WhatsApp Plugin (Cloud API)
Status: planned. `:reply_chain` + quote-TTL; `:server_readable`; webhook (M4).

Implementation tasks:
- `./plugins/allbert.whatsapp/`; raw `Req` outbound to the Cloud API; inbound via
  the M4 webhook; WABA `phone_number_id` identity (phone redacted); in-session
  `:button` + `typed_command`/`link`/`list`; `whatsapp_doctor`; CLI.

Acceptance: a simulated webhook event round-trips; reply-chain + quote-TTL
degradation; phone redaction; in-session button; doctor envelope.

Automated tests: plugin load + simulated webhook + reply/quote-TTL + button +
phone redaction + doctor + CLI.

Request-flow handoff: document the WhatsApp flow.

### Milestone 8: Signal Plugin (signal-cli daemon)
Status: planned. `:reply_chain` reply-by-timestamp; ACI identity;
`trust_class: :e2ee_origin`; daemon via ADR 0058.

Implementation tasks:
- `./plugins/allbert.signal/`; supervise `signal-cli` in daemon mode
  (muontrap/erlexec) under the plugin supervisor; JSON-RPC over a local control
  channel (preferred `0600` UNIX socket; loopback TCP/HTTP only with auth/ACL
  controls); account keys under `<ALLBERT_HOME>/signal/` (0700/0600);
  QR device-link as an audited operator action; quote-by-timestamp;
  ACI-keyed identity; `typed_command`/`link`/`list`; `signal_doctor`; CLI. A test stub
  stands in for the real daemon in `release.v053`.

Acceptance: a stubbed-daemon JSON-RPC round-trips; reply-by-timestamp; ACI
identity; `:e2ee_origin` stamped; local-control endpoint, auth, and UNIX-socket
permissions asserted as applicable; key files asserted 0600; doctor envelope.

Automated tests: plugin load + stubbed daemon + reply-by-timestamp + ACI +
e2ee_origin + local-control/auth/perms + doctor + CLI.

Request-flow handoff: document the Signal daemon flow.

### Milestone 9: Pairing/Setup UX, Identity, Delivery
Status: planned.

Implementation tasks:
- Pairing/setup flows (Matrix token, WhatsApp WABA + tunnel, Signal QR link);
  per-provider identity mapping + explicit cross-channel identity links (no
  auto-merge); delivery/retry/dedupe per provider via `channel_events`.

Acceptance: each channel pairs/sets up runs-as-written; identity links
never auto-merge; dedupe/retry hold per provider.

Automated tests: pairing flows; identity-link no-auto-merge; dedupe/retry.

Request-flow handoff: document pairing + identity + delivery.

### Milestone 10: Security Evals, Real-Provider Smokes, Closeout
Status: planned.

Implementation tasks:
- Add the `:v053` eval rows (below) to `EvalInventory` with backing tests
  (provider + signal-cli stubs); ship `mix allbert.test release.v053`
  (deterministic, no network).
- Add or verify the **required real-provider live smokes**:
  `mix allbert.test external-smoke -- telegram`, `-- inbound_telegram`, `-- email`,
  `-- inbound_email`, `-- matrix`, `-- whatsapp`, and `-- signal`. Each is opt-in
  to *run locally* (needs real credentials) but passing it with recorded evidence
  is mandatory before tag. M5 evidence may be reused at M10 only if the recorded
  commit, settings schema, and provider remediations still match the release
  candidate; otherwise rerun the affected smoke.
- Operator docs (Telegram/email retro-validation; Matrix/WhatsApp/Signal setup,
  tunnel, signal-cli pairing, fragility notes); developer docs
  (`key-custody.md`, `channel-trust-class.md`); Viber-on-paper + iMessage/SMS
  parked in future-features; agent-context-map; roadmap/vision reconciliation.
  Run the closeout discipline.

Security eval rows (`:v053`):
- `key-custody-no-leak-001`, `key-custody-fetch-audited-001`
- `signal-cli-control-endpoint-local-001`, `signal-cli-unix-socket-0600-001`,
  `signal-cli-keyfiles-0600-001`
- `trust-class-stamped-per-channel-001`,
  `e2ee-origin-excluded-default-unified-view-001`,
  `e2ee-origin-optin-audited-001`, `resume-downgrade-confirmed-audited-001`
- `whatsapp-webhook-signature-verify-before-parse-001`,
  `whatsapp-webhook-bad-signature-deny-001`
- `phone-number-redaction-001`, `signal-aci-identity-not-phone-001`
- `reply-by-timestamp-001`, `quote-ttl-degrade-to-flat-001`
- `descriptor-flag-validation-001` (registry rejects invalid `trust_class:` /
  `reply_key_type:` / `quote_ttl_ms:`; every descriptor keeps `:list`)
- `matrix-unencrypted-rooms-only-001`
- `provider-thread-not-authority-001` (reaffirmed for all five v0.53-touched
  channels: Telegram, email, Matrix, WhatsApp, Signal)
- `identity-link-no-auto-merge-001`
- `channel-metadata-not-authority-001`
- **Channel Pack 1 retro-validation (M5) regressions:**
  `email-content-transfer-encoding-decoded-001` (base64 / quoted-printable bodies
  + MIME-word headers decode correctly; M5R1) and
  `telegram-callback-data-within-64b-001` (inline-keyboard JSON is well-formed and
  `callback_data` stays ≤ 64 bytes; M5R2) — deterministic, no provider network

Acceptance: all `:v053` evals pass on stubs; `release.v053` green + reproducible;
the **required Telegram/email retro-validation smokes and Matrix/WhatsApp/Signal
real-provider smokes pass with recorded evidence before tag**; the full release
gate is green; docs agree.

#### M10 Closeout Discipline
1. `mix.exs` + child app versions → `0.53.0`. 2. `CoreApp.version/0` →
`"0.53.0"`. 3. `CHANGELOG.md` `## v0.53.0` cites evals + `release.v053` + the
Telegram/email and Matrix/WhatsApp/Signal real-provider smokes. 4. `mix
allbert.test` help lists `release.v053` and every v0.53 external-smoke selector. 5. plan +
request-flow Status → implemented as `0.53.0`. 6. ADR 0056 amendment + ADR 0058 +
ADR 0059 Status → Accepted. 7. roadmap/vision/agent-context-map/README/operator/
developer docs match. 8. future-features keeps Viber (on paper) + iMessage + SMS
parked. 9. `git diff --check` clean.

Operator closeout test:
```sh
mix allbert.test release.v053
# evidence: <ALLBERT_HOME>/release_evidence/v053/release-v053-<ts>.json
# Required real-provider live smokes (release authority before tag):
mix allbert.test external-smoke list
mix allbert.test external-smoke -- telegram
mix allbert.test external-smoke -- inbound_telegram
mix allbert.test external-smoke -- email
mix allbert.test external-smoke -- inbound_email
mix allbert.test external-smoke -- matrix
mix allbert.test external-smoke -- whatsapp
mix allbert.test external-smoke -- signal
# evidence: <ALLBERT_HOME>/release_evidence/v053/external-smoke-<provider>-<ts>.json
```

## Documentation Deliverables
- Operator setup guides: `docs/operator/telegram-channel.md`,
  `docs/operator/email-channel.md`, `docs/operator/matrix-channel.md`,
  `docs/operator/whatsapp-channel.md` (incl. tunnel + 24h session),
  `docs/operator/signal-channel.md` (signal-cli pairing, daemon, fragility).
- Developer guides: `docs/developer/key-custody.md` (ADR 0058 + honest
  non-claims), `docs/developer/channel-trust-class.md` (ADR 0059).
- Security-hardening updates (the `:v053` eval rows).
- ADR 0056 amendment + ADR 0058 + ADR 0059; agent-context-map; future-features
  (Viber on paper; iMessage/SMS parked).

## Non-Goals
- No Viber **build** in v0.53 (documented on paper, deferred — standing fee).
- No iMessage (parked); no SMS (parked).
- No whatsapp-web bridge (Cloud API only); no WhatsApp multi-number.
- No Matrix **encrypted** rooms (no Elixir E2EE); no Matrix appservice namespace.
- No Vault/OpenBao/OS-keychain-as-primary store; no separate OS key-daemon.
- No claim of locked/zeroed/protected memory for KeyCustody.
- No silent live cross-channel mirroring; no auto-merged identity links.
- No public AG-UI/A2UI; no hosted multi-user authorization; no cloud sync.

## Definition Of Done

v0.53 is done when:
- Telegram and email reach Discord/Slack validation parity: provider doctors,
  provider-targeted outbound + inbound external smokes, operator guides, manual
  inbound/outbound/approval/rejection/poll-resume checks, recorded evidence, and
  clean raw-token/credential leak scans, with the M5R1/M5R2 remediations covered
  by deterministic `:v053` eval rows so they cannot silently regress;
- Matrix, WhatsApp (Cloud API), and Signal (signal-cli daemon) adapters simulate
  inbound/reply, map identity explicitly, and honor the v0.52 approval-primitive
  + ADR 0057 threading contracts;
- **KeyCustody (ADR 0058)** serves secrets from one `:sensitive` decrypt-once
  process (no decrypt-on-every-read), excluded from crash dumps/introspection,
  never logged, redacted on inspect — with **no** locked/zeroed-memory claim;
- the **`signal-cli` daemon** is supervised (muontrap/erlexec), its control
  endpoint is local-only (preferred `0600` UNIX socket; loopback TCP/HTTP only
  with auth/ACL controls), key files are `0600`, and pairing is audited;
- **trust-class gating (ADR 0059)** stamps every ref, excludes `:e2ee_origin`
  from the default cross-channel unified view (audited opt-in), and requires an
  audited confirmation to resume E2EE-origin content onto a weaker class; web +
  CLI honor it;
- the **WhatsApp webhook** verifies the raw-body HMAC before parsing inside the
  reused v0.51 ingress; `reply_key_type`/`quote_ttl_ms` are consumed; phone
  numbers are redacted and Signal keys on ACI;
- the `:v053` eval set + `release.v053` pass, **and the required Telegram/email
  retro-validation plus Matrix/WhatsApp/Signal real-provider live smokes pass
  with recorded evidence before tag**;
- roadmap/vision/agent-context-map/ADRs/CHANGELOG reflect the shipped state;
  Viber (on paper) + iMessage + SMS remain parked.
