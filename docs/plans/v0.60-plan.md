# Allbert v0.60 Product Experience Design

Request flow: `docs/plans/v0.60-request-flow.md`

ADRs:
- `docs/adr/0077-product-experience-design-and-information-architecture.md`
  (**Accepted (v0.60)** — the unified technical-prosumer journey, the
  information-architecture / navigation / screen-composition redesign, and the
  "final UX layout redesign" that ADR 0074 explicitly deferred as a baseline
  rather than a layout redesign).
- `docs/adr/0078-first-model-path.md` (**Accepted (v0.60)** — the First-Model
  Path decision: assisted-local vs managed-hosted vs honest-BYOK, the QuickStart
  "first useful chat" contract, and the packaging implications handed to v0.62).
- Builds on the v0.58 design-system substrate (ADR 0030 unified catalog + ADR
  0073 cross-surface contract + ADR 0074 web design system) and the v0.59
  hardened substrate (portability, settings version contract, the ADR 0065 param
  seam). Designs **inputs** for ADR 0074 (v0.61 overhaul), ADR 0076 (v0.62
  packaging), ADR 0069 + ADR 0075 (v0.63 onboarding + personas) without
  implementing them.

Status: planned. Design-first release **inserted 2026-06-30 before the v0.61
Presentation Layer Overhaul**. v0.60 ships **no new authority, capability, or
runtime behavior beyond a navigable walking skeleton** — it is a design +
scaffolding release. Its deliverables are specifications, two accepted ADRs, and
a placeholder-only walking skeleton whose routes resolve and pass a nav/a11y
smoke. It does not implement the presentation overhaul (v0.61), packaging /
entry points (v0.62), guided onboarding or personas (v0.63), or the integrated
product RC (v0.64). Security Central is unchanged.

## Purpose

The downstream build order is **unchanged and correct**: v0.61 overhauls the
presentation layer, v0.62 packages the entry points, v0.63 builds guided
onboarding and personas, v0.64 is the product RC, and v1.0 freezes the public
contracts. What v0.60 changes is **sequencing**: it front-loads the *one* unified
technical-prosumer journey **design** so that presentation, packaging, and
onboarding are each built against a single designed experience instead of being
designed-as-they-go.

Without v0.60, each downstream release would discover the product shape while
implementing it: v0.62 would design entry points that v0.63 onboarding then has
to retrofit, and v0.63 onboarding would retroactively reshape the v0.61
presentation surface it was supposed to land on. That rework is expensive and,
critically, **some of it lands on contracts v1.0 freezes**. v1.0 freezes the
presentation contracts (the Surface DSL, the workspace substrate, the catalog
rendering boundary). The structural redesign that ADR 0074 deferred — the
chat-primary re-layout, the information architecture, the navigation model — must
therefore be **designed now (v0.60) and built in v0.61, before the freeze**. A
redesign discovered after v0.61 ships, or during the v0.64 RC, is a frozen-
contract break.

v0.60 produces six design outcomes plus a navigable skeleton, all on the existing
authority spine:

1. **One product-experience spec.** The install → first-run → onboard →
   first-value → daily-use journey, written once, for the technical-prosumer 1.0
   audience, as the contract every downstream release builds against (M1).
2. **An information architecture & navigation model.** Sitemap, screen inventory,
   navigation model, and workspace/screen composition — the structural redesign
   ADR 0074 deferred — accepted as ADR 0077 (M2).
3. **A First-Model Path decision.** Assisted-local vs managed-hosted vs honest-
   BYOK, the QuickStart "first useful chat" contract, and the packaging
   implications, accepted as ADR 0078 (M3).
4. **An onboarding-flow & persona-model design.** The two-track wizard UX and the
   persona/profile model as design specs feeding v0.63 (the ADR 0075 content) (M4).
5. **An entry-point & CLI UX design.** Command taxonomy, first-run detection, and
   wizard launch as a spec feeding v0.62 (the ADR 0076 entry points) (M5).
6. **A walking skeleton + design-system gap analysis.** A navigable nav shell with
   placeholder screens implementing the IA (the gate artifact and the v0.61 build
   scaffold), plus the token/component/pattern gaps the overhaul needs (M6, M7).

The authority boundary is unchanged. No new capability, no new authority surface,
no confirmation-floor change, no egress grant, no contract freeze. The walking
skeleton renders placeholders through the existing catalog; it grants nothing.

## Foundations & Handoff
- **Builds on:** the v0.58 uniform design-system base — design tokens (color,
  type, spacing, radius, motion scales), the prop-driven component-variant
  registry, the pattern library, one app shell, and the unified catalog as the
  rendering boundary for every page (ADR 0030/0073/0074); the v0.59 hardened
  substrate (portability envelope, settings version contract, the ADR 0065
  Runner param seam). v0.60 designs **on top of** this substrate; it does not
  rebuild it.
- **Hands off to:**
  - **v0.61 Presentation Layer Overhaul** — consumes the ADR 0077 IA / navigation
    / composition redesign and the M7 design-system gap analysis; implements the
    deferred layout redesign and the professional craft pass on top of the M6
    walking skeleton (the skeleton is v0.61's scaffold).
  - **v0.62 Packaging & Entry Points** — consumes the M5 entry-point & CLI UX
    spec (command taxonomy, first-run detection, wizard launch) and the ADR 0078
    packaging implications; implements the designed `allbert` entry points
    (ADR 0076).
  - **v0.63 Guided Onboarding & Profiles** — consumes the M4 onboarding-flow &
    persona-model design (the ADR 0075 content) and the ADR 0078 First-Model Path;
    implements the two-track wizard and applies personas (ADR 0069 + ADR 0075).
  - **v0.64 Product RC** — validates the integrated journey designed here against
    the as-built surfaces.
  - **v1.0 freeze** — freezes the presentation contracts that the ADR 0077
    redesign reshapes; v0.60 + v0.61 ensure the redesign lands **before** the
    freeze rather than breaking it after.

## Cross-Cutting Invariants (apply to every milestone)
- **Design + scaffolding only; no new runtime behavior.** v0.60 ships no new
  capability class, no new authority surface, no confirmation-floor change, and no
  egress grant. The only running code it adds is a navigable walking skeleton of
  placeholder screens. Security Central is unchanged and remains the sole
  authority boundary.
- **The catalog stays the rendering boundary.** The walking skeleton renders
  through the unified catalog inside the one app shell (ADR 0030/0073/0074). No
  model-generated or data-generated markup is introduced; placeholder screens are
  static catalog/token-consuming compositions.
- **Placeholder screens carry no live data and grant no authority.** Skeleton
  routes resolve, render a navigable shell, and render placeholder content; they
  do not read business state, do not compose effectful action DTOs as live
  affordances, and grant no authority or egress. They exist to prove the IA is
  navigable and to scaffold v0.61.
- **Settings Central is unchanged.** v0.60 adds no Settings key (see the keys
  table). The persona model, First-Model Path, and onboarding flow are **designs**
  of settings-seeded behavior, not new keys or seeded values.
- **Designs are inputs, not implementations.** M4 designs the ADR 0075 persona
  content, M5 designs the ADR 0076 entry points, M7 feeds ADR 0074 — none of these
  ADRs are implemented in v0.60. v0.60 accepts only ADR 0077 and ADR 0078.
- **One spine.** Nothing in v0.60 reaches `Settings.Store`, `Confirmations`, or
  business logic directly; the skeleton consumes the catalog/shell contract only.

## Current Code State (the ground truth v0.60 designs on top of, 2026-06-30)
- **v0.58 design system.** The design tokens, the prop-driven component-variant
  registry, the pattern library, and the one app shell (navigation, header /
  switchers, mobile shellbar) exist and are the substrate the redesign is drawn
  against. The reduced-motion and high-contrast accessibility axes are in the
  token system. The unified surface catalog (`Surface.Renderer`, ADR 0030) is the
  rendering boundary for every page.
- **ADR 0074 deferral.** ADR 0074 explicitly shipped a *design-system baseline,
  not a final UX layout redesign* (ADR 0074 §57), and explicitly **deferred the
  chat-primary re-layout** (ADR 0074 §71): chat as the primary surface, ephemerals
  as modals, the structural information architecture. That deferred redesign is
  exactly the M2 deliverable, accepted as ADR 0077.
- **Current routes.** `/` is a thin landing page (an ADR-accepted thin-landing
  exception); `/workspace` is the canonical operator surface; `/jobs`,
  `/objectives`, and the v0.58 operator panels (Intents, Settings/Models, Surface
  Policy) render through the catalog/shell. The IA redesign (M2) re-describes this
  inventory as a designed sitemap; the walking skeleton (M6) renders the redesigned
  inventory as placeholder routes.
- **v0.59 substrate.** Portability (export / dry-run import), the settings version
  contract (first-class `schema_version`, fail-closed boot check), and the ADR 0065
  Runner param seam are landed and freeze-ready. v0.60 adds nothing to this spine;
  it only designs the experience that sits on top of it.
- **Downstream ADRs in Proposed state.** ADR 0069 (onboarding), ADR 0075
  (personas), and ADR 0076 (packaging) are Proposed and anchor the downstream
  releases. v0.60 produces the **design content** that those ADRs and their
  releases consume; it does not move them to Accepted.
- **Gate harness.** The `release.v059` lane lives in
  `apps/allbert_assist/lib/mix/tasks/allbert.test.ex`; `release.v060` mirrors it
  with design-artifact, ADR-acceptance, walking-skeleton, and nav/a11y steps.

## Locked Decisions
| # | Decision | Chosen value | Trade-off if changed |
|---|---|---|---|
| 1 | Scope | Design-first: produce the journey spec, the IA/nav/composition redesign (ADR 0077), the First-Model Path decision (ADR 0078), the onboarding/persona design, the entry-point/CLI UX design, the design-system gap analysis, and a navigable placeholder walking skeleton. **No presentation, packaging, onboarding, or RC implementation.** | Pulling build scope into v0.60 re-creates the design-as-you-go coupling v0.60 exists to remove. |
| 2 | Build order | Downstream build order (v0.61 presentation → v0.62 packaging → v0.63 onboarding → v0.64 RC → v1.0 freeze) is **unchanged and correct**; v0.60 only front-loads the *design*, not the build. | Re-ordering builds would not fix the real problem (no unified design) and would churn the roadmap. |
| 3 | Freeze-trap | v1.0 freezes presentation contracts (Surface DSL, workspace substrate, catalog boundary). The structural redesign ADR 0074 deferred is **designed now (v0.60), built in v0.61**, before the freeze. | A redesign discovered after v0.61 or during the v0.64 RC is a frozen-contract break. |
| 4 | ADRs accepted in v0.60 | Exactly two: ADR 0077 (Product Experience Design & Information Architecture) and ADR 0078 (First-Model Path). | Accepting ADR 0074/0075/0076/0069 here would claim implementation v0.60 does not do. |
| 5 | First-Model Path | Decided in ADR 0078 across assisted-local vs managed-hosted vs honest-BYOK, with a QuickStart "first useful chat" contract and packaging implications handed to v0.62. | Leaving the first-model question open forces v0.62 packaging and v0.63 onboarding to each guess, re-coupling them. |
| 6 | Walking skeleton | Navigable routes + nav shell + placeholder screens implementing the IA; **no live data, no authority**. It is the gate artifact and the v0.61 build scaffold. | A skeleton that reads live data or grants authority is a runtime change v0.60 forbids; a non-navigable skeleton fails to prove the IA or scaffold v0.61. |
| 7 | Persona / onboarding design | M4 designs the ADR 0075 persona content and the two-track wizard UX as **specs feeding v0.63**; it does not add personas, settings, or a wizard. | Implementing personas here splits ownership with v0.63 and adds seeded settings v0.60 forbids. |
| 8 | Entry-point / CLI design | M5 designs the command taxonomy, first-run detection, and wizard launch as a **spec feeding v0.62 / ADR 0076**; it ships no binary, no escript, no new task. | Implementing entry points here duplicates v0.62 and adds packaging scope. |
| 9 | Gate semantics | The design-release gate is real: ADR 0077/0078 Accepted **plus** the design artifacts present and docs-gate clean **plus** a walking skeleton whose routes resolve and pass nav/a11y smoke **plus** the docs gate. | A docs-only gate would let an un-navigable or undesigned release pass; a build-style gate would demand implementation v0.60 does not own. |

## Milestones

Lane summary: **M1** Product Experience Spec → **M2** Information Architecture &
Navigation Model (→ ADR 0077) → **M3** First-Model Path Decision (→ ADR 0078) ∥
**M4** Onboarding Flow & Persona Model Design ∥ **M5** Entry-Point & CLI UX Design
→ **M6** Walking Skeleton (implements the M2 IA) → **M7** Design-System Gap
Analysis → **M8** Evals + `release.v060`. M1 is the source spec every other
milestone refines. M2 must land before M6 (the skeleton implements the IA). M3,
M4, and M5 are parallel design tracks that each consume M1 and feed a named
downstream release. M7 reads the M2 composition and M6 skeleton against the v0.58
token/component substrate. M8 is the serial closeout that gates ADR acceptance,
artifact presence, and skeleton navigation. Each milestone's **Implementation**
block lists Artifacts / Inputs / Outputs (and, for M6/M8, Modules / Files / Tests
/ Verify).

### M1: Product Experience Spec
**Status:** planned.

Write the single unified product-experience spec for the technical-prosumer 1.0
audience: the **install → first-run → onboard → first-value → daily-use** journey,
end to end, as one document. This is the source-of-truth narrative every
downstream release builds against. It names, for each stage, the operator's goal,
the surfaces involved, the "what good looks like" bar, and the failure modes that
read as sub-1.0 (blank-field first-run, toolchain install friction, no first-value
path, undifferentiated config). It is descriptive of the *target* experience, not
of current code; gaps between target and current become the downstream backlog.

The spec is deliberately cross-surface: it spans web, CLI/TUI, and channels, and
it threads the First-Model Path (M3), onboarding (M4), and entry points (M5) into
one journey rather than three disconnected designs. It defines the **first-value
moment** ("first useful chat") that M3 then makes a concrete path.

- **Implementation:**
  - **Artifacts:** `docs/design/product-experience-spec.md` — the five-stage
    journey, per-stage goals / surfaces / bar / failure modes, the first-value
    definition, and a journey map that names which downstream release owns each
    stage's implementation.
  - **Inputs:** the v0.58 maturity review (substrate vs surface gap), the
    competitive bar (opinionated presets, two-track onboarding, packaged install,
    fastest-first-chat), the current routes/surfaces in Current Code State.
  - **Outputs:** the journey contract M2-M5 refine; the first-value definition M3
    operationalizes; the per-stage ownership map M8's handoff drift-check reads.
- **Acceptance:** one document covers all five stages for the technical-prosumer
  audience with goals, surfaces, the quality bar, and failure modes; it names the
  first-value moment; it maps each stage to its owning downstream release; it is
  docs-gate clean.

### M2: Information Architecture & Navigation Model (→ ADR 0077)
**Status:** planned.

Design the structural redesign ADR 0074 deferred: the **sitemap**, the **screen
inventory**, the **navigation model**, and the **workspace / screen composition**.
This is the "final UX layout redesign" ADR 0074 explicitly called out as deferred
(ADR 0074 §57, §71) — chat-primary layout, ephemerals-as-modals, and the
information architecture that ties the screens together. The output is accepted as
**ADR 0077 (Product Experience Design & Information Architecture)**.

The IA re-describes the current flat utility inventory (`/`, `/workspace`,
`/jobs`, `/objectives`, operator panels) as a designed hierarchy: primary surfaces
vs secondary / operator surfaces, the navigation shell that exposes them, the
workspace composition (zones, primary chat surface, ephemerals), and the screen-
composition rules every redesigned screen follows. It is expressed against the
v0.58 catalog/token contract — it is a **layout and composition** design, not a
new rendering mechanism. The IA is the contract M6 implements as a walking
skeleton and v0.61 implements as the real overhaul.

- **Implementation:**
  - **Artifacts:** `docs/design/information-architecture.md` (sitemap, screen
    inventory, navigation model, workspace/screen composition rules);
    `docs/adr/0077-product-experience-design-and-information-architecture.md`
    (Accepted (v0.60)).
  - **Inputs:** M1 journey spec; ADR 0074 deferral (§57 baseline-not-redesign,
    §71 deferred chat-primary re-layout); the v0.58 catalog/shell/token contract;
    the current route inventory.
  - **Outputs:** the route/sitemap list M6 renders as placeholder screens; the
    composition rules M7 gap-analyzes against the token/component substrate; the
    presentation contract v0.61 implements before the v1.0 freeze.
- **Acceptance:** ADR 0077 is Accepted (v0.60); the IA doc defines a sitemap, a
  complete screen inventory, a navigation model, and workspace/screen composition
  rules expressed through the catalog/token contract; the deferred ADR 0074
  redesign is now designed; docs-gate clean.

### M3: First-Model Path Decision (→ ADR 0078)
**Status:** planned.

Decide how a brand-new operator gets to their **first useful chat** — the
first-value moment M1 defines — and accept it as **ADR 0078 (First-Model Path)**.
The decision is across three options:

- **assisted-local** — Allbert helps the operator run a local model (download /
  detect / configure) so the first chat needs no external account or key;
- **managed-hosted** — Allbert offers a hosted/managed model path for an immediate
  first chat with no local setup;
- **honest-BYOK** — Allbert is honest that the operator brings their own key, and
  optimizes the BYOK path to the shortest possible first chat.

ADR 0078 records the chosen path (or the chosen default + supported alternates),
the **QuickStart "first useful chat"** contract (what the operator does, what
Allbert does, how many steps, where a key/model is required), and the **packaging
implications** that v0.62 must satisfy (what must ship in the packaged binary, what
the first-run detection must check, what the vault/credential model must support).
The decision shapes M4 onboarding (the QuickStart track) and M5 entry points
(first-run detection), so it lands early.

- **Implementation:**
  - **Artifacts:** `docs/adr/0078-first-model-path.md` (Accepted (v0.60));
    `docs/design/first-model-path.md` — the QuickStart "first useful chat" path,
    the option analysis, and the packaging-implications handoff.
  - **Inputs:** M1 first-value definition; ADR 0072 recommended model profiles;
    the v0.59 settings/secret-reference substrate; the packaging constraints
    ADR 0076 anchors.
  - **Outputs:** the QuickStart contract M4 onboarding designs around; the
    packaging implications v0.62 implements; the first-run model-state check M5
    first-run detection designs.
- **Acceptance:** ADR 0078 is Accepted (v0.60) and names the chosen path across
  assisted-local / managed-hosted / honest-BYOK; the QuickStart "first useful chat"
  contract is defined; the packaging implications are enumerated for v0.62;
  docs-gate clean.

### M4: Onboarding Flow & Persona Model Design
**Status:** planned.

Design the **two-track onboarding wizard UX** (QuickStart vs Advanced, with the
"fastest first chat" QuickStart path from M3) and the **persona / profile model** —
both as design specs that **feed v0.63**. This milestone designs the **ADR 0075
content**: the user-category personas (`researcher`, `developer`, `writer`,
`ops`, `general`), what each persona seeds (Settings Central defaults, suggested
apps / channels / intents, recommended model-purpose mapping per ADR 0072), and
the explicit review/confirm application step. It also designs the wizard's
surfaces (web wizard through the catalog/shell, CLI/TUI wizard), step sequence,
and how the QuickStart track reaches first value with the fewest steps.

This is design only: no wizard is built, no persona is added to the repo, no
Settings key or seeded value is created. The output is the spec v0.63 implements
(ADR 0069 wizard + ADR 0075 personas).

- **Implementation:**
  - **Artifacts:** `docs/design/onboarding-flow.md` (two-track wizard UX, step
    sequence, surfaces, QuickStart vs Advanced); `docs/design/persona-model.md`
    (the persona/profile model, per-persona seed contents, the review/confirm
    application model — the ADR 0075 content).
  - **Inputs:** M1 journey (onboard + first-value stages); M3 QuickStart contract;
    ADR 0069 (onboarding), ADR 0075 (personas), ADR 0072 (model recommendations);
    the v0.58 catalog/shell for the web wizard surface.
  - **Outputs:** the v0.63 onboarding + persona implementation spec; the persona
    seed model the v0.63 release reviews and applies through Settings Central.
- **Acceptance:** the two-track wizard UX is specified end to end; the persona
  model (categories, seed contents, review/confirm application) is specified as
  the ADR 0075 content; both are marked as v0.63 design inputs with no v0.60
  implementation; docs-gate clean.

### M5: Entry-Point & CLI UX Design
**Status:** planned.

Design the unified entry-point and CLI UX — **command taxonomy**, **first-run
detection**, and **wizard launch** — as a spec **feeding v0.62 / ADR 0076**. This
designs how the packaged `allbert` binary presents itself: the grouped command
taxonomy (operator commands vs developer/CI commands), the first-run detection
that recognizes an un-onboarded Home and routes to the onboarding wizard (M4), the
first-model-state check (M3) that the first-run path consults, and the
`allbert` → wizard launch sequence. It reshapes the current 53 flat mix tasks into
a designed operator-facing taxonomy.

This is design only: no binary, no escript, no `releases:`, no new task, no Home-
layout change ships in v0.60. The output is the entry-point spec v0.62 implements.

- **Implementation:**
  - **Artifacts:** `docs/design/entry-point-cli-ux.md` (command taxonomy, grouped
    `--help` model, first-run detection, first-model-state check, wizard launch
    sequence).
  - **Inputs:** M1 install + first-run stages; M3 first-model-state; M4 wizard
    launch target; ADR 0076 (packaging / unified CLI), ADR 0070 (TUI operator
    console), ADR 0067 (TUI/terminal channel); the current flat mix-task inventory.
  - **Outputs:** the v0.62 packaging + entry-point implementation spec; the
    first-run detection contract v0.62 builds and v0.63 onboarding launches into.
- **Acceptance:** the command taxonomy, first-run detection, and wizard-launch
  sequence are specified as the v0.62 / ADR 0076 design input; no entry-point code
  ships in v0.60; docs-gate clean.

### M6: Walking Skeleton
**Status:** planned.

Build a **navigable walking skeleton** that implements the M2 information
architecture: the navigation shell exposing the IA navigation model, and a
**placeholder screen at every route in the IA sitemap**, rendered through the
unified catalog inside the one app shell. This is the **gate artifact** (the
proof the IA is navigable and a11y-clean) and the **v0.61 build scaffold** (the
route + nav + screen structure v0.61 fills in with the real overhaul).

The skeleton is placeholder-only: each screen renders a static, token/catalog-
consuming placeholder composition with no live data, no business-state reads, and
no effectful affordances. Routes resolve (every IA route returns a rendered shell),
the nav shell exposes the IA's navigation model (active-route + navigation between
screens resolve), and the existing accessibility axes hold (FocusTrap, high-
contrast, reduced-motion) across every skeleton screen. It introduces no new
authority, no new capability, and no model-/data-generated markup.

- **Implementation:**
  - **Modules / Files:** new placeholder LiveView/route modules under
    `apps/allbert_assist_web/` for each IA route; the router entries; a nav-shell
    composition that consumes the v0.58 app shell + the M2 navigation model;
    placeholder screen compositions built from the variant registry / pattern
    library.
  - **Inputs:** the M2 IA sitemap, screen inventory, navigation model, and
    composition rules; the v0.58 catalog/shell/token contract.
  - **Settings:** none (see the keys table).
  - **Tests:** every IA route resolves to a rendered catalog/shell response; the
    nav shell renders the IA navigation model and active-route resolution works;
    FocusTrap / high-contrast / reduced-motion hold on every skeleton screen; no
    placeholder screen reads business state or composes an effectful affordance;
    no model-/data-generated markup is present. Evals
    `walking-skeleton-routes-resolve-001`, `walking-skeleton-nav-shell-001`,
    `walking-skeleton-a11y-smoke-001`, `no-new-authority-design-only-001`.
  - **Verify:** boot the web surface; navigate every IA route through the nav
    shell; confirm each resolves to a placeholder screen with no live data;
    confirm reduced-motion / high-contrast hold; confirm no authority is granted.
  - **Outputs:** the navigable scaffold v0.61 fills in; the gate artifact M8
    asserts.
- **Acceptance:** every IA route resolves through the catalog/shell with a
  placeholder screen; the nav shell exposes the IA navigation model and inter-
  screen navigation resolves; the a11y axes hold across all skeleton screens; the
  skeleton grants no authority and renders no live or model-/data-generated data.

### M7: Design-System Gap Analysis
**Status:** planned.

Analyze the v0.58 token / component / pattern substrate against the M2 composition
rules and the M6 skeleton, and enumerate the **gaps the v0.61 overhaul needs**:
missing tokens (elevation/surface tones, density scales), missing or insufficient
component variants (emphasis, density, empty-state atoms), and missing patterns
(empty / first-run states, the navigation/composition patterns the IA assumes).
This is the **ADR 0074 input** for the v0.61 amendment — it tells the overhaul
exactly what the design system must grow, so v0.61 extends the registry/pattern
library rather than hand-rolling per-page HEEx.

- **Implementation:**
  - **Artifacts:** `docs/design/design-system-gap-analysis.md` — the token gaps,
    the component-variant gaps, and the pattern gaps, each tied to the M2
    composition rule or M6 screen that requires it, with the owning v0.61 work.
  - **Inputs:** M2 composition rules; the M6 skeleton; the v0.58 token set,
    variant registry, and pattern library; ADR 0074.
  - **Outputs:** the v0.61 design-system extension backlog; the ADR 0074 v0.61
    amendment input.
- **Acceptance:** the gap analysis enumerates token, component-variant, and
  pattern gaps, each mapped to the composition rule / skeleton screen that needs
  it and to the v0.61 work that fills it; it is marked as the ADR 0074 v0.61
  input; docs-gate clean.

### M8: Evals + `release.v060` Gate
**Status:** planned.

Define the `release.v060` lane in
`apps/allbert_assist/lib/mix/tasks/allbert.test.ex` and bundle the v0.60 evidence:
the `:v060` eval rows (design-artifact presence, ADR acceptance, walking-skeleton
route resolution + nav/a11y smoke, design-only no-authority, and the handoff
drift-check), plus formatter / compile-warnings-as-errors / Credo-strict / docs
gate. The gate proves a **real** design release: ADR 0077 and ADR 0078 Accepted,
the design artifacts present and docs-gate clean, and a walking skeleton whose
routes resolve and pass the nav/a11y smoke.

- **Implementation:**
  - **Modules / Files:** `mix/tasks/allbert.test.ex` (`@release_v060_steps`,
    `release_v060/0`, `def run(["release.v060"])`); the `:v060` eval module and the
    walking-skeleton route/nav/a11y tests; `EvalInventory` `:v060` rows.
  - **Settings:** none.
  - **Tests / lane steps:** (1) prepare disposable DB; (2) formatter check; (3)
    compile with warnings as errors; (4) Credo strict; (5) design-artifact
    presence (each `docs/design/*.md` deliverable exists, is non-empty, and is
    docs-gate clean); (6) ADR acceptance (ADR 0077 and ADR 0078 both report
    Accepted (v0.60)); (7) walking-skeleton route-resolution test (every IA route
    resolves through the catalog/shell); (8) nav/a11y smoke (nav shell exposes the
    IA model; FocusTrap / high-contrast / reduced-motion hold); (9) the `:v060`
    eval rows; (10) docs gate; (11) release-evidence write.
  - **Verify:** `mix allbert.test release.v060` exists, exits 0, prints an evidence
    JSON path under the disposable release Home, and is reproducible; `mix help
    allbert.test` lists `release.v060`.
- **Acceptance:** the lane exists, is deterministic, bundles all v0.60 evidence
  (ADRs Accepted + artifacts present + skeleton navigable + a11y clean + docs
  gate), and is the authoritative design-release handoff gate.

## Settings Central Keys (new in v0.60)

**None.** v0.60 is design + scaffolding and adds no Settings key. The persona model
(M4), the First-Model Path (M3), and the onboarding flow (M4) are **designs** of
settings-seeded behavior whose keys and seeded values, if any, are introduced by
the implementing downstream releases (v0.63 personas/onboarding, v0.62 packaging)
through Settings Central, not by v0.60. The walking skeleton (M6) reads no setting
and writes none.

## Security / Eval Rows (`:v060`)
Design-artifact presence:
- `product-experience-spec-present-001` — the M1 journey spec exists, covers
  install → first-run → onboard → first-value → daily-use, names the first-value
  moment, maps stages to owning releases, and is docs-gate clean.
- `information-architecture-spec-present-001` — the M2 IA doc defines a sitemap,
  screen inventory, navigation model, and workspace/screen composition rules.
- `onboarding-persona-design-present-001` — the M4 two-track wizard UX and persona
  model (the ADR 0075 content) are present and marked as v0.63 design inputs.
- `entry-point-cli-ux-design-present-001` — the M5 command taxonomy, first-run
  detection, and wizard-launch spec are present and marked as the v0.62 design
  input.
- `design-system-gap-analysis-present-001` — the M7 token/component/pattern gap
  list is present, mapped to composition rules / skeleton screens, and marked as
  the ADR 0074 v0.61 input.

ADR acceptance:
- `adr-0077-accepted-001` — ADR 0077 (Product Experience Design & Information
  Architecture) reports Accepted (v0.60) and contains the IA / nav / composition
  redesign ADR 0074 deferred.
- `adr-0078-first-model-path-accepted-001` — ADR 0078 (First-Model Path) reports
  Accepted (v0.60), names the chosen assisted-local / managed-hosted / honest-BYOK
  path, defines the QuickStart "first useful chat" contract, and enumerates the
  v0.62 packaging implications.

Walking skeleton:
- `walking-skeleton-routes-resolve-001` — every IA-sitemap route resolves through
  the catalog/shell to a placeholder screen, with no model-/data-generated markup.
- `walking-skeleton-nav-shell-001` — the nav shell exposes the IA navigation model;
  active-route resolution and inter-screen navigation resolve.
- `walking-skeleton-a11y-smoke-001` — FocusTrap, high-contrast, and reduced-motion
  hold across every skeleton screen.

Design-only invariants:
- `no-new-authority-design-only-001` — v0.60 adds no capability, authority, egress,
  or Settings key; skeleton screens read no business state and grant no authority.
- `rc-design-handoff-no-drift-001` — the handoff enumerates the v0.61 / v0.62 /
  v0.63 consumers with their concrete consumed design artifacts and pulls no
  presentation, packaging, onboarding, or RC **build** scope into v0.60.

## Design Review & Skeleton-Navigation Checklist (S0-S6)

The full command-bearing operator/reviewer steps live in
`docs/plans/v0.60-request-flow.md` (S0-S6). At a glance, the design-release gate is
proven by:
- **S0** task visibility (`release.v060`, the design docs, the skeleton routes);
- **S1** design-artifact presence + docs-gate clean (M1, M2, M4, M5, M7);
- **S2** ADR acceptance (ADR 0077 + ADR 0078 Accepted (v0.60));
- **S3** First-Model Path review (chosen path, QuickStart contract, packaging
  implications);
- **S4** walking-skeleton navigation (every IA route resolves; nav model exposed);
- **S5** nav/a11y smoke (FocusTrap / high-contrast / reduced-motion across screens;
  no live data; no authority);
- **S6** `release.v060` gate + handoff drift-check (ADRs Accepted + artifacts
  present + skeleton navigable + docs gate; v0.61/v0.62/v0.63 consumers enumerated).

## `release.v060` Gate
A deterministic, reproducible lane that **bundles the design-release evidence**:

1. prepare disposable DB;
2. `format_check` (`mix format --check-formatted`);
3. `compile_warnings_as_errors` (`mix compile --warnings-as-errors`);
4. `credo_strict` (`mix credo --strict`);
5. `design_artifacts_present` — each `docs/design/*.md` v0.60 deliverable exists,
   is non-empty, and is docs-gate clean;
6. `adr_acceptance` — ADR 0077 and ADR 0078 both report Accepted (v0.60);
7. `walking_skeleton_routes` — every IA-sitemap route resolves through the
   catalog/shell to a placeholder screen;
8. `walking_skeleton_nav_a11y` — the nav shell exposes the IA navigation model and
   FocusTrap / high-contrast / reduced-motion hold across skeleton screens;
9. `v060_evals` — the `:v060` eval rows pass;
10. `docs_gate` (`mix allbert.test docs`);
11. release-evidence write under the disposable release Home
    (`release_evidence/v060/`).

The gate is the authoritative handoff: it proves the ADRs are Accepted, the design
artifacts are present and clean, and the walking skeleton is navigable and a11y-
clean. `mix allbert.test release.v060` exits 0 and prints
`release.v060 evidence: <path>`.

## Non-Goals
- **No presentation overhaul.** The IA/redesign is *designed* here and *built* in
  v0.61. No layout rebuild, no real screens, no craft pass in v0.60.
- **No packaging or entry points.** No `allbert` binary, escript, `releases:`,
  install script, or Home-layout change; the entry-point UX is *designed* (M5) and
  *built* in v0.62.
- **No onboarding or personas implementation.** No wizard, no persona added to the
  repo, no seeded Settings; *designed* (M4), *built* in v0.63.
- **No integrated product RC.** That is v0.64.
- **No new authority, capability, egress, confirmation-floor change, or Settings
  key.** Security Central is unchanged.
- **No live data, model-generated UI, or data-generated markup** in the walking
  skeleton; placeholders only, through the catalog.
- **No new ADR acceptance beyond ADR 0077 and ADR 0078.** ADR 0074/0075/0069/0076
  receive design *inputs*, not implementation.
- **No contract freeze** (→ v1.0).

## Definition Of Done
v0.60 is done when:
- the M1 product-experience spec covers install → first-run → onboard →
  first-value → daily-use for the technical-prosumer audience, names the first-
  value moment, and maps each stage to its owning downstream release;
- the M2 information architecture (sitemap, screen inventory, navigation model,
  workspace/screen composition) is designed and accepted as **ADR 0077
  (Accepted (v0.60))**, delivering the structural redesign ADR 0074 deferred;
- the M3 First-Model Path is decided and accepted as **ADR 0078 (Accepted
  (v0.60))** across assisted-local / managed-hosted / honest-BYOK, with the
  QuickStart "first useful chat" contract and the v0.62 packaging implications;
- the M4 onboarding-flow & persona-model design (the two-track wizard UX + the
  ADR 0075 persona content) is a complete v0.63 design input with no v0.60
  implementation;
- the M5 entry-point & CLI UX design (command taxonomy, first-run detection,
  wizard launch) is a complete v0.62 design input with no v0.60 implementation;
- the M6 walking skeleton renders a placeholder screen at every IA route through
  the catalog/shell, exposes the IA navigation model in the nav shell, holds the
  FocusTrap / high-contrast / reduced-motion a11y axes across all screens, reads no
  live data, and grants no authority;
- the M7 design-system gap analysis enumerates the token / component / pattern gaps
  the v0.61 overhaul needs, each mapped to a composition rule / skeleton screen, as
  the ADR 0074 v0.61 input;
- the `:v060` eval rows and `mix allbert.test release.v060` pass deterministically
  with redacted evidence, the docs gate is clean, and the handoff drift-check
  enumerates the v0.61 / v0.62 / v0.63 consumers with their concrete consumed
  design artifacts;
- no new authority, capability, egress, Settings key, route-level live data,
  model-generated UI, or build-scope implementation leaked into v0.60; Security
  Central is unchanged; and roadmap / vision / agent-context-map agree on the v0.60
  boundary (design + scaffolding only; presentation → v0.61, packaging → v0.62,
  onboarding/personas → v0.63, product RC → v0.64).
