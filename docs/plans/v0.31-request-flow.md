# Allbert v0.31 Request Flow - Runtime And UI-Substrate Consolidation

Implementation details live in `docs/plans/v0.31-plan.md`.

v0.31 is intentionally mostly invisible to operators. The request-flow purpose
is to keep behavior stable while implementation seams move underneath the
runtime.

## Operator-Visible Contract

| Area | v0.31 behavior |
|---|---|
| Routes | Same as v0.30: `/agent`, `/settings`, and `/stocksage/*` remain until v0.32 |
| Settings | Same keys, defaults, write confirmations, audit events, and secret redaction |
| StockSage | Same analysis, memory, canvas, queue, trends, and reflection flows |
| Workspace | Same durable tiles, ephemeral surfaces, offline edit behavior, and card rendering |
| Security | Same action-boundary checks, confirmation decisions, remembered grants, Resource Access posture, and eval expectations |
| Dynamic code | Still forbidden; v0.34 will define a sandboxed exception |

## Flow 1: Existing Action Execution Through New Action Facade

1. Operator invokes an existing action from CLI, LiveView, job, objective step,
   or StockSage.
2. The caller still resolves through `AllbertAssist.Actions.Registry`.
3. The runner invokes the migrated action through `AllbertAssist.Action`
   metadata.
4. Security Central receives the same permission, app scope, resource context,
   and confirmation metadata as v0.30.
5. The returned response normalizes through `AllbertAssist.Runtime.Response`
   without changing operator-visible status or copy.

Acceptance:

- Existing action ids keep working.
- Confirmation-required, denied, completed, and error outcomes preserve
  current UX.
- Private Jido command modules remain unavailable as action candidates.

## Flow 2: Settings Read And Write Through Fragments

1. Operator lists, searches, reads, or updates a Settings Central key.
2. Settings Central discovers the key through registered schema fragments.
3. The same validation, default, redaction, secret handling, and confirmation
   logic applies.
4. Writes go through the existing registered settings actions and audit path.

Acceptance:

- Every pre-v0.31 setting remains present.
- Unknown keys are rejected as before.
- Secret values are never rendered raw.
- Fragment metadata never grants permission.

## Flow 3: Surface Rendering Through One Catalog

1. Workspace or StockSage produces a `Surface` tree using existing component
   atoms.
2. The unified catalog validates component atoms, primitive status, app-declared
   catalog metadata, and renderer availability.
3. The web renderer dispatches through the single catalog path.
4. Existing cards render with the same DOM test handles expected by v0.30.

Acceptance:

- Existing StockSage cards render in both released route families during
  v0.31.
- Unknown component atoms fail closed.
- App catalog metadata cannot smuggle a renderer or action authority.

## Flow 4: Extension Contribution Discovery

1. Startup discovers shipped source-tree plugins, apps, actions, skills,
   settings fragments, child specs, and diagnostics.
2. The extension registry normalizes contributions into one inspectable index.
3. App and plugin identities remain distinct, but callers no longer need to
   query several registries for the same contribution class.

Acceptance:

- Existing shipped plugins and StockSage register exactly once.
- Home-plugin manifests remain metadata-only unless already trusted/compiled
  by reviewed source.
- Contribution metadata does not grant trust, permission, routes, or execution.

## Flow 5: Audit, Trace, Path, Persistence, And Redaction

1. Existing runtime work writes traces, audit events, persistence records, and
   redacted UI/CLI output.
2. Callers use the shared facade for the relevant substrate.
3. Existing on-disk files and database rows remain readable.
4. Output redaction remains at least as strict as v0.30.

Acceptance:

- Temporary `ALLBERT_HOME` tests do not touch a real user home.
- Trace/audit records keep stable event vocabulary.
- Redaction covers secrets and Resource Access payloads.

## Milestone Update Log

Use this table during implementation.

| Milestone | Status | Notes |
|---|---|---|
| M1 Boundary inventory and facade map | Implemented | Added `AllbertAssist.Boundary`, `docs/developer/runtime-boundary-map.md`, targeted module-doc shim notes, boundary inventory tests, and explicit StockSage plugin registration in namespaced-memory tests. Focused seam suite, compile, Credo, Dialyzer, precommit, and `git diff --check` passed. |
| M2 Dead-code and compatibility cleanup | Implemented | Removed obsolete `AllbertAssist.Workspace.Catalog.component_renderer/1`, deleted its test-only probe, and updated active roadmap/developer docs to point renderer dispatch at `AllbertAssistWeb.Workspace.Renderer` until M7. Other M1 shims still have live callers and remain owned by M7/M8. |
| M3 Shared redaction and path substrate | Implemented | Added `AllbertAssist.Runtime.Paths` and `AllbertAssist.Runtime.Redactor`, migrated runtime-facing redaction callers to the new facade, kept legacy path/redaction behavior stable, and added regression tests for Allbert Home roots plus Resource Access, StockSage, sandbox-trial, struct, and secret redaction. Focused suite, compile, Credo, Dialyzer, precommit, and `git diff --check` passed. |
| M4 Shared audit, persistence, and trace substrate | Implemented | Added `AllbertAssist.Runtime.Audit`, `AllbertAssist.Runtime.Persistence`, and `AllbertAssist.Runtime.Trace`; routed shell/package/external/skill audit callers, Security Central audit metadata, trace recording, workspace body persistence, and Fragment body decoding through the new facades without changing disk formats. Focused audit/persistence/trace/workspace/action suite passed. |
| M5 `AllbertAssist.Action` and capability registry | Implemented | Added `AllbertAssist.Action` as the thin Allbert wrapper over `Jido.Action`, migrated registered core and StockSage actions to the wrapper, removed the duplicate central registry capability map, and taught `AllbertAssist.Actions.Registry` to derive capability metadata from action modules. `AllbertAssist.Actions.Multiply` remains an unregistered raw Jido test fixture. Focused action/registry/runner/security/StockSage suites passed. |
| M6 Typed runtime response contract | Implemented | Added `AllbertAssist.Runtime.Response` as the shared completed/confirmation/denied/advisory/error/unsupported response contract; routed `AllbertAssist.Runtime`, `AllbertAssist.Actions.Runner`, `PermissionGate.response_status/1`, and representative objective branches through it while preserving existing operator-facing copy and map payloads. Focused response/runtime/runner/objective suites and compile passed. |
| M7 Unified surface catalog, renderer, and extension registry | Implemented | Added `AllbertAssist.Surface.Catalog` as the single component/catalog/renderer authority, routed workspace and StockSage app-surface rendering through `AllbertAssistWeb.Surface.Renderer`, removed the StockSage-only surface renderer and v0.30 pass-through workspace adapters, and added `AllbertAssist.Extensions.Registry` as the read-only app/plugin contribution facade. Focused catalog, renderer, extension-registry, boundary, `/agent`, and `/stocksage/*` suites passed. |
| M8 Settings fragments and single authority | Implemented | Added `AllbertAssist.Settings.Fragment` and `AllbertAssist.Settings.Fragments`, routed `Settings.Schema` schema/default/safe-write assembly through registered core/app/plugin fragments, preserved every existing key/default/validation/secret behavior, and kept Settings Central actions as the write path. `PermissionGate` remains an amended compatibility shim over Security Central because live callers still exist; future deletion requires a separate parity pass. Focused settings/security/StockSage schema suites passed. |
| M9 Closeout and downstream docs | Implemented | Bumped version metadata to `0.31.0`, accepted ADR 0026-0031, updated README/CHANGELOG/roadmap/request-flow/developer context, reconciled downstream v0.32-v0.35 handoffs, prepared the operator manual verification checklist, and passed the final release gate. |

## Release Verification

- Existing manual operator smokes from v0.30 still pass on v0.31.
- No Chrome route-change verification is required until v0.32 because v0.31 is
  not a route or product-shell reset. The one UI-risk change, M7 renderer
  consolidation, is covered by the `/agent`, `/stocksage/*`, workspace
  renderer, and StockSage card render tests.
- Full release gate includes focused seam tests, `mix format
  --check-formatted`, `mix compile --warnings-as-errors`,
  `mix credo --strict`, `mix dialyzer`, `mix precommit`, and
  `git diff --check`.

## Operator Manual Verification

Run against a disposable `ALLBERT_HOME`, not a real user home:

1. Start the app with an empty temporary home and migrate:
   `ALLBERT_HOME=/private/tmp/allbert-v031-manual mix ecto.migrate.allbert`
   followed by `ALLBERT_HOME=/private/tmp/allbert-v031-manual mix phx.server`.
2. Open `/agent` and confirm the v0.30 workspace shell still loads.
3. Seed or run a StockSage analysis tile as in the v0.30 manual smoke and
   confirm the tile renders the real StockSage card markup, not a stub.
4. Open `/stocksage/analyses` and confirm the StockSage app surface still
   renders independently.
5. Open `/settings` and update a safe, non-secret setting such as
   `operator.communication_style`; confirm the action still requires the
   expected Settings Central path and the setting survives reload.
6. Run `mix allbert.security status` and `mix allbert.security review --recent`
   against the same temporary home and confirm no unexpected warnings appear.
